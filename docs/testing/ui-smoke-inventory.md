# UI smoke inventory

**Generated from `apps/desktop/scripts/ui-smoke/spec.json` by `render-inventory.mjs` — do not hand-edit this file. Edit `spec.json` and re-run `npm run ui-smoke:doc`.**

68 rows across 18 screens (3 manual — see each row's own reason). Run with `npm run ui-smoke` (requires `RUN_DESKTOP_AGENT` set to a value other than `default`; see `apps/desktop/.claude/skills/run-desktop/SKILL.md`).

## landing

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `landing-render` | _(none)_ | marker: `Corpus Ingest Diagnostic`<br>expect: `NEW CHARACTER`, `LOAD CHARACTER`, `CAMPAIGN MANAGER`, `DM TOOLKIT`, `Browse Equipment Catalog`, `Browse Spell Catalog`, `Browse Class Progression`, `Browse Race Traits`, `Browse Monster Catalog`, `Browse Companion Catalog`, `Browse Intelligent Item Components`, `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Every landing affordance from LandingScreen.tsx in one render check -- action banners, all seven browse links, and the diagnostic link. Banner titles are asserted in the CASE THEY ACTUALLY RENDER: ActionBanner's title span carries `textTransform: 'uppercase'`, and uiProbe.ts's bodyText is captured via `innerText` (which reflects applied CSS, unlike `textContent`), so the live screen text is 'NEW CHARACTER' etc., not the mixed-case prop string 'New\nCharacter'. Confirmed live 2026-09-17 against the running app's own probe snapshot -- the original mixed-case single-word expects were a spec defect, not an app defect (see docs/testing/ui-smoke-inventory.md history). |
| `landing-ruleset-chips` | _(none)_ | marker: `Pathfinder 1e`<br>expect: `Pathfinder 1e`, `Solarus Arcanum`, `Traveller MGT 2e`, `Cyberpunk Red`, `Starfinder 1e`, `World of Darkness`, `Coming soon` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | RULE_SETS in LandingScreen.tsx: one selectable rule set (Pathfinder 1e) plus five 'Coming soon' stubs, all rendered as chips. |
| `landing-campaign-gate` | _(none)_ | marker: `Campaign`<br>expect: `Choose a local Drive-synced folder under ⚙ Settings → Google Drive to enable the Campaign Manager.` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | computeCampaignManagerAccessGate(isGoogleDriveConfigured()): on a fresh profile with no Drive folder configured, the Campaign Manager banner is disabled and shows this exact hint (campaignManagerAccessGate.ts). This row is only meaningful on a profile where Drive is genuinely unconfigured -- as of this cycle, settings-tab-google-drive-save (now real, needed by every campaign-*/dm-toolkit-*/encounter-builder-* row) genuinely configures Drive via saveGoogleDriveConfig(), which persists in this profile's real localStorage across the whole run and across cycles on a shared, non-reset dev profile. Once any such row has ever run against this profile, this row's own disabled-state assertion no longer holds and it will read red -- not a regression in this row's own logic, but an inherent, expected conflict between asserting the pre-configuration state and exercising the real post-configuration flows on the same persistent storage. A clean read of this row needs a Drive-unconfigured profile (e.g. --only this row alone on a profile that has never run a Drive-save row, or clearing localStorage's google-drive-config key by hand first). |

## settings

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `settings-open` | click "⚙" | marker: `OPTIONS`<br>expect: `Appearance`, `Google Drive`, `Update`, `Bug Report`, `Enhancement Request`, `Developer` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SettingsModal's options rail renders all six SETTINGS_TABS at once regardless of the active tab. The gear button's target name is '⚙' (its trimmed textContent, per uiProbe.ts's nameOf() priority), not its aria-label 'Open settings' (App.tsx) -- textContent wins over aria-label whenever both are non-empty. Marker is 'OPTIONS' (uppercase) -- same CSS-uppercase-transform-vs-innerText mismatch as landing-render: the rail's own section label renders as 'OPTIONS', the mixed-case 'Options' string never appears at all. Confirmed live 2026-09-17. |
| `settings-tab-appearance` | _(setup: settings-open)_; click "Appearance" | marker: `Base color scheme`<br>expect: `Base color scheme`, `Theme`, `Manage…`<br>selects non-empty: `select#1`, `select#2` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a listed select has zero options; a click target not found on screen (layout drift) |  | AppearancePanel's mode select and theme select carry no accessible name in source, so the probe falls back to document-order select#1/select#2 (see uiProbe.ts collectSelects). |
| `settings-theme-select-and-manage-themes-modal` | _(setup: settings-open)_; _(setup: settings-tab-appearance)_; click "Manage…"; key Escape | marker: `Community themes`<br>expect: `Community themes` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | ThemeBrowserModal (role=dialog, aria-label 'Manage themes', h2 'Community themes'); portals to document.body (see uiProbe.ts's document.body observation note), opened via AppearancePanel's 'Manage…' button, closed with Escape. |
| `settings-tab-google-drive` | _(setup: settings-open)_; click "Google Drive" | marker: `Google account`<br>expect: `Google account`, `My Drive/TTRPG Campaigns` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | GoogleDrivePanel's account and folder-path fields. 'you@example.com' (the account field's own `placeholder` attribute) can NEVER appear in bodyText -- uiProbe.ts captures bodyText via `innerText`, which does not include an empty input's placeholder text at all (that's a separate DOM attribute, not rendered content); this row was unpassable as originally written. Reassigned to 'Google account' (the field's own visible label) as marker, and kept 'My Drive/TTRPG Campaigns', which DOES render live -- inside the folder field's help text ('Destination folder for campaign subfolders, e.g. My Drive/TTRPG Campaigns.'), not its placeholder. Confirmed live 2026-09-17. |
| `settings-tab-update` | _(setup: settings-open)_; click "Update" | marker: `Update`<br>expect: `Update` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | UpdateSection mounts async (loadMountTimeState); the SettingsModal header itself always reads 'Update' regardless of load state, so this is a safe marker/expect while the global forbid rule covers the 'stuck on Loading' failure mode. |
| `settings-tab-bug` | _(setup: settings-open)_; click "Bug Report" | marker: `Report a bug`<br>expect: `Observed behavior`, `Expected behavior`, `Reproduction steps`, `Preserve bug draft (not yet submittable)` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | BugReportComposer render, empty-state submit button reads 'Preserve bug draft' until required fields are filled. allowGlobalForbid suppresses the global 'failure' forbid string here only -- the tab's own intro copy reads 'captures an observable failure with structured, required fields', describing what the form is FOR, not reporting an actual one; confirmed live 2026-09-17 this is the sole source of that string on this screen. |
| `settings-tab-enhancement` | _(setup: settings-open)_; click "Enhancement Request" | marker: `Request an enhancement`<br>expect: `Tester goal`, `Current friction or limitation`, `Requested capability or improvement`, `Preserve enhancement draft (not yet submittable)` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | EnhancementRequestComposer render, empty-state submit button. |
| `settings-tab-developer` | _(setup: settings-open)_; click "Developer" | marker: `Developer`<br>expect: `BACKEND`, `BUILD`, `PLATFORM`, `FEATURE CHECK`, `DATA SOURCE` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; row forbid string present (`Unreachable`); a click target not found on screen (layout drift) |  | Per DoD: the Developer tab's Backend card must show a reachable version/commit, never 'Unreachable', when the Tauri IPC bridge is alive (load_backend_health). Section labels are asserted in the CASE THEY ACTUALLY RENDER -- same CSS-uppercase-transform-vs-innerText mismatch as landing-render (see its own note); 'Backend'/'Build' incidentally passed before only via coincidental substring hits elsewhere on the page ('Backend feature check' heading, 'Builds ship from...' body text), not the labels themselves. Confirmed live 2026-09-17. |
| `settings-close-esc` | _(setup: settings-open)_; key Escape | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Esc closes the modal (SettingsModal's own keydown handler) and lands back on the landing screen underneath it. |
| `settings-tab-google-drive-save` | click "⚙"; click "Google Drive"; click "you@example.com"; type "ui-smoke@example.com"; click "My Drive/TTRPG Campaigns"; type "/tmp/claude-1000/-home-ubuntu-workspace-repos-codex/6badc5b8-ae3b-4359-80c5-cd0b1598973e/scratchpad/ui-smoke/drive"; click "Save"; key Escape | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; row forbid string present (`Choose a local Drive-synced folder`); a click target not found on screen (layout drift) |  | Real GoogleDrivePanel Save: fills the account-email and folder-path fields (matched by their own `placeholder`, per uiProbe.ts's nameOf() chain, since neither carries an aria-label) and clicks Save, which calls saveGoogleDriveConfig() and is confirmed live to render 'Connected as ui-smoke@example.com -- campaigns will be created under ...'. Escape closes the modal back to landing; CharacterHubPage only recomputes computeCampaignManagerAccessGate() on ITS OWN next render, which the modal's close (a state change in App.tsx, re-rendering the whole subtree) reliably triggers -- confirmed live: the Campaign Manager banner's disabledHint ('Choose a local Drive-synced folder...') is gone immediately after this row, which is exactly the forbid check here. Every downstream row that needs Campaign Manager access (campaign-*, dm-toolkit-*, encounter-builder-*) depends on this row first. Safe to compose from ANY screen, not just landing: the ⚙ gear is a global affordance (present on every screen this harness visits) and Escape returns to whichever screen was underneath, unchanged -- but this row itself is only ever invoked fresh from landing (resetToLanding() runs before any row's own setup/steps), so 'Corpus Ingest Diagnostic' is a safe marker either way. |

## equipmentCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `equipment-catalog-open-and-search` | click "Browse Equipment Catalog"; click "Search by name…"; type "Longsword" | marker: `Longsword`<br>expect: `Longsword` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | EquipmentCatalogScreen: open from the landing link, filter to a known weapon by name. |
| `equipment-catalog-chip-filter-and-back` | click "Browse Equipment Catalog"; click "Magic Items"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | The category chip row (All/Arms & Armor/General/Magic Items/Equipment Mods) plus Back returning to the landing screen. |

## spellCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `spell-catalog-open-and-search` | click "Browse Spell Catalog"; click "Search by name…"; type "Fireball" | marker: `Fireball`<br>expect: `Fireball` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SpellCatalogScreen filtered to a known evocation spell. |
| `spell-catalog-chip-filter-and-back` | click "Browse Spell Catalog"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Book/school chip row exists above the search box (see verify-on-screen.sh SEARCH_Y history for `spell`); this row exercises Back. |

## classCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `class-catalog-open-and-search` | click "Browse Class Progression"; click "Search by class name…"; type "Fighter" | marker: `Fighter`<br>expect: `Fighter` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | ClassCatalogScreen filtered to a known base class. |
| `class-catalog-back` | click "Browse Class Progression"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing. |

## raceCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `race-catalog-open-and-search` | click "Browse Race Traits"; click "Search by trait or race name…"; type "Elf" | marker: `Race Traits`<br>expect: `Elf` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | RaceCatalogScreen, Standard traits tab (default), filtered to a known race. |
| `race-catalog-alternate-traits-tab` | click "Browse Race Traits"; click "Alternate racial traits" | marker: `Race Traits`<br>expect: `Alternate racial traits` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | RACE_CATALOG_VIEWS second tab mounts AlternateTraitPicker, which has no search box of its own (verify-on-screen.sh's --no-search/--tab alternate precedent). |
| `race-catalog-back` | click "Browse Race Traits"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing. |

## monsterCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `monster-catalog-open-and-search` | click "Browse Monster Catalog"; click "Search by name…"; type "Ankheg" | marker: `Ankheg`<br>expect: `Ankheg` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | MonsterCatalogScreen filtered to the Bestiary 1 record verify-on-screen.sh's own SKILL.md example already cites. |
| `monster-catalog-back` | click "Browse Monster Catalog"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing. |

## companionCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `companion-catalog-open-and-search` | click "Browse Companion Catalog"; click "Search by name…"; type "Eidolon" | marker: `Eidolon`<br>expect: `Eidolon` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | CompanionCatalogScreen filtered to a known ACG record (see 'Arcanist: first ACG/APG class to reach Computed' precedent). |
| `companion-catalog-back` | click "Browse Companion Catalog"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing. |

## intelligentItemCatalog

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `intelligent-item-catalog-open-and-search` | click "Browse Intelligent Item Components"; click "Search by name or family…"; type "Speech" | marker: `Speech`<br>expect: `Communication` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | IntelligentItemCatalogScreen filtered to the 'Int Item / Communication Speech' component record (data/corpus/core_rulebook/equipment/equipmods/intelligent_item_communication_speech.json). |
| `intelligent-item-catalog-back` | click "Browse Intelligent Item Components"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing. |

## corpusIngestDiagnostic

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `corpus-ingest-diagnostic` | click "Corpus Ingest Diagnostic" | marker: `scope:`<br>expect: `scope:`, `book` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; row forbid string present (`scope: 0 books`, `scope: 0 book.`); a click target not found on screen (layout drift) |  | CorpusIngestDiagnosticPanel's 'scope: N book(s).' line -- N must be positive on any real corpus checkout. |

## create

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `create-character-render` | click "New<br>Character" | marker: `Create a character`<br>expect: `Character name`, `Player name`, `Race`, `Class`, `Create character`<br>selects non-empty: `character-race`, `character-class` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a listed select has zero options; a click target not found on screen (layout drift) |  | This is the item-8-class check that historically catches the race-select-empty defect (see the harness spec's own precedent): the race select must carry more than zero <option>s, not merely render. |
| `create-character-fill-and-submit` | _(setup: create-character-render)_; click "character-name"; type "Smoke Test PC"; click "player-name"; type "UI Smoke Harness"; select; select; click "Create character"; scroll (40 ticks); key Shift | marker: `Smoke Test PC is ready`<br>expect: `Smoke Test PC is ready`, `Your character was computed and saved.`, `ARMOR CLASS`, `FORTITUDE SAVE` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Real submit, confirmed live 2026-09-17: fills the two plain-text identity fields (DOM-addressable by id), picks Race=Elf (CRB) and Class=Wizard via the new `select` op (Wizard so the caster-only sheet-action-add-spell row downstream has a real character to load), and clicks Create character. buildCreateCharacterOutcomeSurface's real headline/detail/highlight text renders ('<name> is ready' / 'Your character was computed and saved.' / uppercase ARMOR CLASS·MELEE ATTACK BONUS·BASE ATTACK BONUS·FORTITUDE SAVE·REFLEX SAVE·WILL SAVE tiles -- uppercase per the same CSS-textTransform-vs-innerText note as landing-render). The select targets are option-text substrings unique to each select's own concatenated option list (see the select op's own $comment entry), not the selects' ids. This mints a fresh saved 'Smoke Test PC' character on every run -- by design, so load-character-select-and-open and its downstream sheet-* rows have a character whose exact name this profile's many pre-existing fixture characters cannot collide with by EXACT match (this profile carries two unrelated 'Aldric Ironhand' saves already, which made that name ambiguous for a name-matched click). The trailing scroll-up + key are both load-bearing, not cosmetic: the outcome panel pushes 'Back' to a negative on-screen y, which broke the NEXT row's resetToLanding() (raw xdotool coordinate click against the probe's cached rects) until fixed -- confirmed live 2026-09-17 that a pure xdotool scroll does NOT itself trigger a fresh probe report (uiProbe.ts's MutationObserver fires on DOM mutation, not scroll/resize), so the probe file kept serving PRE-scroll rects to the next row no matter how many ticks were scrolled; a harmless DOM-command-channel `key` step right after the scroll forces `executeCommand`'s unconditional post-command `report()` (see uiProbe.ts's startCommandChannel), which re-reads getBoundingClientRect() against the NOW-scrolled page and writes the corrected rects other rows' resetToLanding() reads. |
| `create-character-back` | click "New<br>Character"; click "Back" | marker: `Corpus Ingest Diagnostic`<br>expect: `Corpus Ingest Diagnostic` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Back returning to landing without submitting. |

## load

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `load-character-render` | click "Load<br>Character" | marker: `Load`<br>expect: `Load`, `Delete`, `Export`, `Clone`, `Import`, `Cancel` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | LoadCharacterScreen's action bar renders regardless of selection (buttons disabled, not absent); the seeded default character (character_hub::seed_default_character_if_needed) is expected present in the list on a fresh profile. |
| `load-character-select-and-open` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; click "Back"; click "Load<br>Character"; click "Smoke Test PC"; click "Load" | marker: `☰ Menu`<br>expect: `☰ Menu` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Real row+Load, confirmed live 2026-09-17. The setup mints a fresh 'Smoke Test PC' (see create-character-fill-and-submit's own note on why not the seeded/legacy 'Aldric Ironhand' -- ambiguous by exact-name match on this profile), then this row's own steps back out to landing, open Load Character, click the row by its own displayLabel (matches via the prefix tier: CharacterListRow's button textContent is displayLabel followed by raceLabel/classSummary/savedAt with no separator, so an exact name is a startsWith prefix of it, same mechanism the existing landing-render rows already rely on), and Load into the sheet. Every sheet-* row below points its own `setup` at this row. |
| `load-character-delete-clone` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-menu-clone)_; click "✕"; click "Smoke Test PC (Copy)"; click "Delete" | marker: `Deleted`<br>expect: `Deleted`, `(Copy)` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Real delete, confirmed live 2026-09-17: LoadCharacterScreen's handleDelete() is gated by a plain `window.confirm(...)`, and in this Xvfb + WebKitGTK harness that call resolves `true` immediately with no interactive dialog ever appearing on screen (observed directly, repeatedly) -- so unlike the Tauri-plugin native file dialogs (manual-character-import/export, manual-portrait-upload), this one is not actually unaddressable, it is simply automatic. The clone-then-delete-the-clone churn-safe pattern is the DoD's own guidance; this row does both halves for real and leaves the profile exactly as it found it (net of the fresh non-clone 'Smoke Test PC' the setup chain also mints, same as every other row that depends on create-character-fill-and-submit). |
| `manual-character-import` | _(none)_ | _(none — manual row)_ | _(not run automatically)_ | handleImport opens a native OS file-open dialog (Tauri's dialog plugin) -- xdotool has no DOM to drive inside it. | LoadCharacterScreen's 'Import' button. |
| `manual-character-export` | _(none)_ | _(none — manual row)_ | _(not run automatically)_ | handleExport / the sheet menu's 'Export' item open a native OS file-save dialog. | LoadCharacterScreen's 'Export' button and CharacterSheet's menu 'Export' item both terminate in the same native dialog. |

## sheet

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `sheet-open-for-tabs` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_ | marker: `☰ Menu`<br>expect: `☰ Menu` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Real anchor row now that load-character-select-and-open is confirmed live: every sheet-tab-* / sheet-action-* row below declares `setup: ["sheet-open-for-tabs"]`, so this one row reaching the sheet for real (loading the fresh caster 'Smoke Test PC') is what makes all of them real. |
| `sheet-tab-weapons` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Weapons" | marker: `Add Weapon`<br>expect: `Add Weapon` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[0]. |
| `sheet-tab-defense` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Defense" | marker: `HIT POINTS`<br>expect: `HIT POINTS`, `EQUIPMENT EFFECTS`, `SAVING THROWS` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[1]. Damage/Heal HP-adjust controls also live on this tab (see sheet-action-hp-adjust). Marker changed from 'Damage Reduction' to 'HIT POINTS' (uppercase per the section heading's own CSS text-transform, same innerText-vs-source-case note as landing-render), confirmed live 2026-09-17: this row now actually reaches the Defense tab (previously cascaded red from sheet-open-for-tabs being manual), and the 'Damage Reduction' section is honest-empty -- it does not render at all -- for a character with no DR source, which every character this row can now load is (the freshly-created 'Smoke Test PC'). 'HIT POINTS'/'EQUIPMENT EFFECTS'/'SAVING THROWS' are unconditional DefenseTab content. |
| `sheet-tab-gear` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Gear" | marker: `Add Armor`<br>expect: `Add Armor`, `Add Gear` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[2]. |
| `sheet-tab-spells` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Spells" | marker: `Spells`<br>expect: `Spells` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[3]. Marker is the 'Spells' tab-nav button itself (always present) rather than 'Add Spell', which is withheld for non-caster builds by design (addSpellAffordanceModel.ts) and would make this row falsely red on a non-caster seed character. |
| `sheet-tab-pets` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Pets" | marker: `Pets`<br>expect: `Pets` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[4]. Marker/expect are the 'Pets' tab-nav button itself; the companion panel is honest-empty for a character with no companion class feature, so no content string is asserted beyond that. |
| `sheet-tab-feats` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Feats" | marker: `Add Feat`<br>expect: `Add Feat` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[5]. |
| `sheet-tab-actions` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Actions" | marker: `Actions`<br>expect: `Actions` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SHEET_TABS[6]. Trait-add control (sheet-action-trait-add) lives on this tab. |
| `sheet-action-add-armor-gear-picker` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-gear)_; click "Add Armor"; key Escape | marker: `Add Armor`<br>expect: `Add Armor` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | ItemPickerModal (role=dialog, h2 title = the kind's own title, here 'Add Armor'; portals to document.body, see uiProbe.ts) opened via the Gear tab's Add Armor button (dispatch table in CharacterSheet.tsx also backs Add Gear/Add Spell/Add Feat through the same modal), then closed with Escape. This row is now reachable for the first time (previously cascaded red from sheet-open-for-tabs being manual) and, unlike sheet-action-add-feat's identical click-then-Escape shape (confirmed green live 2026-09-17), consistently reproduces red on THIS specific catalog: the 'key: Escape' command's own ack times out (5000ms) after Add Armor's picker opens, on every one of 3 separate fresh-launch attempts this cycle -- app defect, not a spec/harness wiring issue (steps unchanged from the pre-existing spec; only `setup` was rewired). Escalated live 2026-09-17: a direct 30-SECOND wait for the Escape ack (well past any plausible IPC/render slowness) STILL timed out, and a screenshot taken right after showed the Gear tab rendered normally with no dialog open at all -- this is not the modal being merely slow, the whole DOM command channel's poll loop (uiProbe.ts's self-rescheduling setTimeout chain) appears to stop entirely after opening this specific catalog, most likely a main-thread-blocking render/layout cost specific to the armor item list. In a natural sequential run (not isolated --only), this cascades: every row after it in the SAME app session also reports 'could not reach the landing screen' until the app is relaunched (confirmed live: sheet-action-level-up-dialog, sheet-action-skill-allocation-dialog and sheet-action-hp-adjust all cascaded BLOCKED immediately after this row's own failure in one continuous run). Left red per this cycle's own brief rather than softened; a real fix likely needs the armor catalog's own render path profiled, not a harness timeout increase (30s already exceeded). |
| `sheet-action-add-feat` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-feats)_; click "Add Feat"; key Escape | marker: `Add Feat`<br>expect: `Add Feat` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Same ItemPickerModal, feat-catalog mode (list_feats_for_character); dialog h2 title is 'Add Feat'. |
| `sheet-action-add-spell` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-spells)_; click "Add Spell"; key Escape | marker: `Add Spell`<br>expect: `Add Spell` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Same ItemPickerModal, spell-catalog mode; dialog h2 title is 'Add Spell'. Real row, confirmed live 2026-09-17: sheet-open-for-tabs now loads the fresh 'Smoke Test PC' (Elf Wizard 1, created by create-character-fill-and-submit) -- resolveAddSpellAffordance offers 'Add Spell' for a Wizard (listIngested/classNotInCorpus both resolve to Offered), unlike this profile's Human-Fighter seed/legacy characters, which addSpellAffordanceModel.ts would withhold it for. |
| `sheet-action-level-up-dialog` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "+" | marker: `Level up`<br>expect: `Level up` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | LevelUpDialog (role=dialog, aria-label + h2 both 'Level up'; portals to document.body) opened via the round accent button beside the Level stat tile. That button's own accessible name is its icon glyph '+' (no aria-label of its own), not 'Level up' as originally guessed -- confirmed live 2026-09-17 (this row previously always cascaded red from sheet-open-for-tabs being manual, so the guess was never exercised). Deliberately does NOT close the dialog with a trailing Escape (unlike sheet-action-add-armor-gear-picker/add-feat/add-spell, whose marker text is ALSO the name of the still-present triggering button, so it survives closing): 'Level up' appears nowhere else on the sheet once the dialog closes, so asserting it must happen while still open. resetToLanding()'s own Escape-first dialog handling (confirmed to cover LevelUpDialog, see its own comment) closes it for whatever row runs next. |
| `sheet-action-skill-allocation-dialog` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "Manage skill allocation"; key Escape | marker: `Manage skill allocation`<br>expect: `Manage skill allocation` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | SkillAllocationDialog (role=dialog, aria-label + h2 both 'Manage skill allocation'; portals to document.body) opened via SkillsBox's own button of the same name. |
| `sheet-action-hp-adjust` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-defense)_; click "amount"; type "1"; click "Damage"; click "amount"; type "1"; click "Heal" | marker: `HIT POINTS`<br>expect: `HIT POINTS`, `NONLETHAL` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | DurabilityPanel lives inside DefenseTab. Both buttons are disabled until the 'amount' input holds a positive number, so a bare click (no type step first) would silently no-op -- this row damages then heals the same amount back so a repeated unattended run is net-zero on HP. Marker/expect uppercased (CSS text-transform vs innerText, same note as sheet-tab-defense) -- confirmed live 2026-09-17. |
| `sheet-action-money-adjust` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-gear)_; click "gp amount"; type "1"; click "Add"; click "gp amount"; type "1"; click "Spend" | marker: `MONEY`<br>expect: `MONEY`, `GP` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | MoneyPanel lives inside the Gear tab, not a standalone tab of its own. Same disabled-until-typed constraint as HP; this row adds then spends the same 1gp back for a net-zero unattended run. The Gear tab also has its own unrelated 'Add' buttons (Add Armor/Add Gear) -- MoneyPanel's plain 'Add' is only unambiguous while no ItemPickerModal is open, which sheet-action-add-armor-gear-picker's own Escape guarantees by the time this row's reset-to-landing/setup chain re-runs it independently. Marker/expect uppercased ('MONEY' not 'Money') -- same CSS text-transform-vs-innerText note as sheet-tab-defense; confirmed live 2026-09-17. |
| `sheet-action-trait-add` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; _(setup: sheet-tab-feats)_ | marker: `TRAITS`<br>expect: `TRAITS`<br>selects non-empty: `Trait to add` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a listed select has zero options; a click target not found on screen (layout drift) |  | 'Trait to add' select (aria-label confirmed exact live 2026-09-17, 54 options) plus an 'Add' confirm button, inside TraitsSection -- which CharacterSheet.tsx renders at the END of FeatsTab, not ActionsTab as originally guessed (setup now points at sheet-tab-feats, not sheet-tab-actions; confirmed live by reading the source directly after the original guess produced no 'Add a trait…' text on the Actions tab). Marker changed from the select's own default-option text 'Add a trait…' (a <select>'s option text is not reliable innerText content across browsers/webviews) to the section's own always-present heading, rendered uppercase 'TRAITS' via CSS text-transform (same innerText-vs-source-case note as landing-render). Only rendered when `available.length > 0`, i.e. the character has an un-taken trait remaining -- true for the freshly-created 'Smoke Test PC', which starts with none of the 53 ultimate_campaign traits taken. |
| `sheet-menu-recompute` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "☰ Menu"; click "Recompute" | marker: `Recomputed derived stats from the current on-disk build.`<br>expect: `Recomputed derived stats from the current on-disk build.` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Top menu bar 'Recompute' item; handleRecompute()'s own status message is the marker. |
| `sheet-menu-clone` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "☰ Menu"; click "Clone" | marker: `Cloned as`<br>expect: `Cloned as`, `(Copy)` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Top menu bar 'Clone' item; onCloned triggers CharacterHubPage's reload(). Real click, confirmed live 2026-09-17: the status line reads exactly `Cloned as "Smoke Test PC (Copy)".` -- load-character-delete-clone (setup: [..., "sheet-menu-clone"]) deletes the clone this row produces right after, so an unattended repeated run does not accumulate clones (see that row's own note on why the delete half is safe to automate too). |
| `sheet-menu-export` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "☰ Menu" | marker: `Export`<br>expect: `Open`, `Recompute`, `Clone`, `Export`, `Print` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | menuItems dispatch table in CharacterSheet.tsx. This row only opens the menu and reads its five item labels -- it never clicks 'Export' itself (that opens a native OS save dialog via Tauri's dialog plugin, a genuinely unaddressable different mechanism from the window.confirm() calls this cycle confirmed DO auto-resolve -- see the top-level manual-character-export row), so it was never actually unsafe to run unattended; the prior 'manual' flag skipped it without ever exercising its own steps at all (run.mjs returns 'manual' before running a manual row's steps), which is exactly the mislabeled-for-lack-of-tracing case this cycle's brief called out. Confirmed live 2026-09-17. |
| `sheet-close` | _(setup: create-character-render)_; _(setup: create-character-fill-and-submit)_; _(setup: load-character-select-and-open)_; click "✕" | marker: `Load`<br>expect: `Load` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | The '✕' (title 'Close character') button returns to sheetReturnMode, which is 'load' when opened from Load Character (CharacterHubPage.tsx). |
| `manual-portrait-upload` | _(none)_ | _(none — manual row)_ | _(not run automatically)_ | PortraitUpload opens a native OS file-open dialog for the image file. | PortraitUpload control in the sheet's left rail (save_character_portrait). |

## dm-toolkit

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `dm-toolkit-open-and-kind-tabs` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; click "Save"; click "Cancel"; click "DM<br>Toolkit" | marker: `World`<br>expect: `World`, `Timeline`, `Place`, `People`, `Clue`, `Scene`, `Rule` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | All seven record-kind tabs render simultaneously as a nav row, not one at a time. Real row, confirmed live 2026-09-17: the actual rendered tab labels are the plural DM_KIND_LABELS[...].tab strings ('Places', 'People', 'Clues', 'Scenes', 'Rules'), not the singular record-kind names this row's own `expect` list originally used -- most singular forms are true substrings of their plural rendering (Place/Places, Clue/Clues, Scene/Scenes, Rule/Rules), which `screenContains` matches, but 'Person'/'People' is an irregular plural (not a substring at all) and was fixed to 'People' after failing live. The setup chain configures Drive, lands on Campaign Manager, and creates 'Smoke Campaign' (via campaign-create, ending on its Edit screen); this row's own first two steps click that Edit screen's 'Save' (confirmed live to land on campaign-list, persisting the campaign) and then campaign-list's 'Cancel' back to landing, satisfying DmToolkitScreen's own 'at least one campaign must exist' gate before clicking DM Toolkit. |
| `dm-toolkit-create-record` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; _(setup: dm-toolkit-open-and-kind-tabs)_; click "+ New world note"; type "Smoke Test World Record"; click "Save" | marker: `Smoke Test World Record`<br>expect: `Smoke Test World Record` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | startCreate() for the active kind ('World' by default); button label is dynamic ('+ New ' + DM_KIND_LABELS[kind].singular.toLowerCase()) and renders as '+ New world note' (DM_KIND_LABELS.World.singular is 'World note'), not '+ New world' as originally guessed. RecordForm's Title <input> carries `autoFocus` and no accessible name of its own, so the `type` step here needs no preceding `click` -- confirmed live 2026-09-17 the field is already focused the instant the form mounts. Real create, title shows in both the record list and the detail pane once saved. |
| `dm-toolkit-edit-record` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; _(setup: dm-toolkit-open-and-kind-tabs)_; _(setup: dm-toolkit-create-record)_; click "Smoke Test World Record"; click "Edit"; type "Smoke Test World Record Edited"; click "Save" | marker: `Smoke Test World Record Edited`<br>expect: `Smoke Test World Record Edited` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Record detail pane is editable inline once selected (draftFromRecord/updateDmRecord). Real row, confirmed live 2026-09-17: the list row's own accessible name is its VisibilityBadge text plus title concatenated with no separator ('World noteGM onlySmoke Test World Record'), so it is only addressable via the `contains` match tier -- targeting the record's own title text (a real substring) finds it. RecordForm re-mounts (and re-autoFocuses its Title input) on entering edit mode, so the `type` step again needs no preceding `click`, same as dm-toolkit-create-record. |
| `dm-toolkit-delete-record` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; _(setup: dm-toolkit-open-and-kind-tabs)_; click "+ New world note"; type "Smoke Test World Record To Delete"; click "Save"; click "Smoke Test World Record To Delete"; click "Delete" | marker: `WORLD`<br>expect: `WORLD` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; row forbid string present (`Smoke Test World Record To Delete`); a click target not found on screen (layout drift) |  | deleteDmRecord in dmRecordModel.ts, gated by `window.confirm(...)`. Real row, confirmed live 2026-09-17: same as load-character-delete-clone's own note, `window.confirm()` resolves `true` immediately with no interactive dialog ever appearing in this Xvfb + WebKitGTK harness, so this is genuinely automatable, not merely tolerated. DmToolkitScreen always operates on `campaigns[0]` (the FIRST campaign, not the most recently created one), so every dm-toolkit-* row's separately-created 'Smoke Campaign' actually accumulates records in the SAME shared campaign across repeated cycles/reruns rather than each getting an isolated one (confirmed live: 'No world notes yet' failed here because an earlier cycle's leftover records were still present) -- this row creates and deletes a record under its OWN distinctly-titled name ('...To Delete', not dm-toolkit-create-record's plain title) and asserts that specific title's absence via `forbid`, which stays correct regardless of how many unrelated records other rows/cycles have left behind in the shared campaign. |
| `dm-toolkit-export` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; _(setup: dm-toolkit-open-and-kind-tabs)_ | marker: `Export GM copy`<br>expect: `Export GM copy`, `Export player handout` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Two export buttons exist -- GM ('Export GM copy') and player-facing ('Export player handout'); confirmed live 2026-09-17 (both label texts differ from the originally-guessed 'Export (GM)'/'Export (players)'). This row only proves both buttons render -- it never clicks either: exportConsole() -> runDmConsoleExport -> a real Tauri `save()` native OS file dialog (dmConsoleExportFlow.ts), the same genuinely-unaddressable mechanism as manual-character-export, not the window.confirm() class of dialog this cycle confirmed auto-resolves. The row was previously flagged 'manual' without ever running its own steps at all (a manual row returns before setup/steps run) -- exactly the mislabeled-for-lack-of-tracing case, now traced and safe. |
| `encounter-builder-add-monster-and-rating` | click "New<br>Character"; click "character-name"; type "Smoke Test PC"; click "player-name"; type "UI Smoke Harness"; select; select; click "Create character"; click "Back"; click "⚙"; click "Google Drive"; click "you@example.com"; type "ui-smoke@example.com"; click "My Drive/TTRPG Campaigns"; type "/tmp/claude-1000/-home-ubuntu-workspace-repos-codex/6badc5b8-ae3b-4359-80c5-cd0b1598973e/scratchpad/ui-smoke/drive"; click "Save"; key Escape; click "Campaign<br>Manager"; click "Create"; click "campaign-name"; type "Smoke Campaign"; click "player@example.com"; type "player1@example.com"; click "Create campaign"; click "Save"; click "Cancel"; click "DM<br>Toolkit"; click "Encounter builder"; click "Smoke Test PC"; click "Search monsters"; type "Ankheg"; click "AnkhegCR 3"; wait 8000ms | marker: `RATING`<br>expect: `ENCOUNTER LEVEL` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; row forbid string present (`Add at least one party member and one monster.`); a click target not found on screen (layout drift) |  | EncounterBuilderScreen.tsx; opened via DmToolkitScreen's 'Encounters' quiet-button (setShowEncounters) -- actually labeled 'Encounter builder' on screen, not 'Encounters' as originally guessed. Fully self-contained (no `setup` reuse) rather than composed from other rows: this row crosses three unrelated screen families (character creation, Drive settings, campaign creation) in one linear path, and run.mjs's `setup` is flat/non-recursive (see the top-level $comment), so composing it from smaller rows would silently skip THEIR OWN ancestors -- a single explicit steps list here is the correct, verified-live-2026-09-17 shape rather than a fragile-looking shortcut. Adds the freshly-created 'Smoke Test PC' as a party member (exact-name match; monster search result's own accessible name is 'AnkhegCR 3', its name plus CR concatenated with no separator) and confirms the engine's real rating renders (uppercase 'RATING'/'ENCOUNTER LEVEL' headings, same CSS-transform note as landing-render) in place of the 'Add at least one party member and one monster.' empty state. The trailing `wait` is load-bearing: `rate_encounter`'s IPC round trip after adding the monster reproducibly took longer than waitForMarker's own 12s budget on 2 separate fresh-launch attempts this cycle (both reached the correct rated state seconds after the row had already reported red), consistent with this harness's own documented wildly-varying IPC latency. |

## campaign-list

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `campaign-manager-list` | _(setup: settings-tab-google-drive-save)_; click "Campaign<br>Manager" | marker: `Load`<br>expect: `Load`, `Create`, `Edit`, `Cancel` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | CampaignManagerScreen's always-present action bar. Real row, confirmed live 2026-09-17 now that settings-tab-google-drive-save configures a local Drive folder first, un-gating the Campaign Manager banner (see landing-campaign-gate). |

## campaign-create

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `campaign-create` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; click "Create"; click "campaign-name"; type "Smoke Campaign"; click "player@example.com"; type "player1@example.com"; click "Create campaign" | marker: `Edit campaign`<br>expect: `Edit campaign` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | Real create, confirmed live 2026-09-17: fills the campaign name (by id, no accessible label of its own) and one member email (by placeholder, same shape as CreateCampaignScreen's own field), then submits. createCampaign()'s onCreated navigates straight to EditCampaignScreen ('Edit campaign' h2) for the new campaign -- reaching that screen IS the confirmation this row checks, since the submitted field VALUES ('Smoke Campaign', 'player1@example.com') are input values, not rendered text, and so don't appear in bodyText themselves (campaign-manager-list's own list-row text is where the created name becomes checkable prose -- see campaign-edit). |

## campaign-edit

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `campaign-edit` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; click "Save" | marker: `Campaign Manager`<br>expect: `Smoke Campaign` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | EditCampaignScreen shares the same party-member field shape as CreateCampaignScreen. Real edit+save, confirmed live 2026-09-17: campaign-create's setup already lands here; this row's own click submits handleSave() (updateCampaign + writeCampaignLocalFolderArtifacts), whose onSaved() navigates back to Campaign Manager -- EditCampaignScreen's own transient 'Saved. Wrote ...' status text is set and then the component unmounts on the same tick (confirmed live: never actually observable via the probe), so the real, stable confirmation is the campaign-list screen's own row for 'Smoke Campaign' rendering afterward, which IS real textContent. |

## campaign-sheet

| id | steps | marker / expected | red condition | manual reason | notes |
|---|---|---|---|---|---|
| `campaign-sheet-tabs` | _(setup: settings-tab-google-drive-save)_; _(setup: campaign-manager-list)_; _(setup: campaign-create)_; click "Save"; click "Smoke Campaign"; click "Load" | marker: `Party Resources`<br>expect: `Party Resources`, `Adventure Log`, `Maps`, `Wiki` | marker absent; a global forbid string present; 'Loading' still present after the wait budget; any expect string missing; a click target not found on screen (layout drift) |  | CampaignSheet's ASSET_TABS render together as a tab row over the shared markdown-asset panel. Real row, confirmed live 2026-09-17: 'Save' returns from the Edit screen to the Campaign Manager list (see campaign-edit's own note), where the freshly-created 'Smoke Campaign' row is selected by its own real list-row text and then Loaded. |

