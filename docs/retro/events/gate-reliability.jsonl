{"actor": "gate-reliability", "actor_source": "env", "actual": "cargo-target category scanned only the scratchpad root and ~/.cache; the codex-target-* convention under ~/workspace and /tmp — the dirs the SD-29 operating discipline itself mandates — was invisible to it", "claimed": "would reclaim: 0 item(s), 0.0B (2026-08-10 run, with ~40G of orphaned codex-target-* dirs on disk)", "derived": false, "id": "1786486909347-gate-reliability-b05c5f", "origin": "agent", "repo": {"branch": "tranche/9", "head": "8b621552", "worktree": "codex"}, "source": "cli", "subject": "scripts/reclaim.sh", "summary": "scripts/reclaim.sh claimed would reclaim: 0 item(s), 0.0B (2026-08-10 run, with ~40G of orphaned codex-target-* dirs on disk); actual cargo-target category scanned only the scratchpad root and ~/.cache; the codex-target-* convention under ~/workspace and /tmp — the dirs the SD-29 operating discipline itself mandates — was invisible to it", "ts": "2026-08-11T22:21:49Z", "type": "correction", "verified_by": "RETRO_DISABLE=1 bash scripts/reclaim.sh --only cargo-target before/after the fix: 0 candidates before, both live siblings' dirs discovered (and correctly skipped as too young) after"}
{"actor": "gate-reliability", "actor_source": "env", "actual": "once reclaim.sh scans ~/workspace and /tmp by default, the existing '--older-than 0 --apply' case (line ~168) would have deleted a live sibling agent's real target dir — age gate bypassed, no build process running at that instant; fixed by defaulting RECLAIM_WORKSPACE_ROOT/RECLAIM_ORPHAN_TMP_ROOT to nonexistent paths in _sandboxed_env", "claimed": "the suite is hermetic: --scratchpad-root/--cache-root overrides keep every case off the real filesystem", "derived": false, "id": "1786486909763-gate-reliability-ce7285", "origin": "agent", "repo": {"branch": "tranche/9", "head": "8b621552", "worktree": "codex"}, "source": "cli", "subject": "scripts/tests/test_reclaim.py", "summary": "scripts/tests/test_reclaim.py claimed the suite is hermetic: --scratchpad-root/--cache-root overrides keep every case off the real filesystem; actual once reclaim.sh scans ~/workspace and /tmp by default, the existing '--older-than 0 --apply' case (line ~168) would have deleted a live sibling agent's real target dir — age gate bypassed, no build process running at that instant; fixed by defaulting RECLAIM_WORKSPACE_ROOT/RECLAIM_ORPHAN_TMP_ROOT to nonexistent paths in _sandboxed_env", "ts": "2026-08-11T22:21:49Z", "type": "correction", "verified_by": "inspection of test_reclaim.py:168 against the new scan roots; suite re-run green with the hermetic env"}
{"actor": "gate-reliability", "actor_source": "env", "avoidable_by": "the self-test that caught it: scripts/tests/test_reclaim_orphan_targets.sh open-handle case went red; bash -x trace showed the path present in OPEN_FD_TARGETS while the function returned 1", "cause": "first implementation piped printf into awk; awk's early exit on a HIT sigpipes printf, and under 'set -o pipefail' the pipeline returns 141 — every hit read as a miss, so the open-file-handle guard silently protected nothing. Same function also scanned /proc per candidate, O(candidates x fds), blowing a 60s test timeout", "derived": false, "first_attempt": "printf | awk pipeline with early exit; per-candidate /proc/[0-9]*/fd walk", "id": "1786486921853-gate-reliability-4e3d03", "origin": "agent", "repo": {"branch": "tranche/9", "head": "8b621552", "worktree": "codex"}, "source": "cli", "summary": "redid dir_has_open_handle liveness check in scripts/reclaim.sh: first implementation piped printf into awk; awk's early exit on a HIT sigpipes printf, and under 'set -o pipefail' the pipeline returns 141 — every hit read as a miss, so the open-file-handle guard silently protected nothing. Same function also scanned /proc per candidate, O(candidates x fds), blowing a 60s test timeout", "ts": "2026-08-11T22:22:01Z", "type": "rework", "what": "dir_has_open_handle liveness check in scripts/reclaim.sh"}
{"actor": "gate-reliability", "actor_source": "env", "actual": "reclaim.sh's any_verify_running reads the global process table; on a shared box with sibling agents building (the normal state), verify-logs skips every candidate by design and the test failed environmentally. Test now asserts the documented conservative skip when a live build exists", "claimed": "test_old_verify_log_dir_removed_under_apply is hermetic via --verify-tmp-root", "derived": false, "id": "1786487571647-gate-reliability-7c7ea6", "origin": "agent", "repo": {"branch": "tranche/9", "head": "8b621552", "worktree": "codex"}, "source": "cli", "subject": "scripts/tests/test_reclaim.py", "summary": "scripts/tests/test_reclaim.py claimed test_old_verify_log_dir_removed_under_apply is hermetic via --verify-tmp-root; actual reclaim.sh's any_verify_running reads the global process table; on a shared box with sibling agents building (the normal state), verify-logs skips every candidate by design and the test failed environmentally. Test now asserts the documented conservative skip when a live build exists", "ts": "2026-08-11T22:32:51Z", "type": "correction", "verified_by": "ps -eo comm= showing sibling cargo/rustc during the failing run; suite rerun green after the fix"}
{"actor": "gate-reliability", "actor_source": "env", "derived": false, "detected_by": "git push rejected non-fast-forward; git status -sb behind 22", "id": "1786487689447-gate-reliability-d688be", "impact": "shared-branch race: tranche/9 moved 22 commits ahead during this lane's cycle; merge conflict in append-only progress.md (both sides appended receipts)", "origin": "agent", "recurrence_key": "shared-branch-append-conflict", "repo": {"branch": "tranche/9", "head": "4683e5cc", "worktree": "codex"}, "resolution": "merge commit keeping both appended receipts; no content lost (line counts verified across both parents)", "source": "cli", "summary": "shared-branch race: tranche/9 moved 22 commits ahead during this lane's cycle; merge conflict in append-only progress.md (both sides appended receipts) (detected by git push rejected non-fast-forward; git status -sb behind 22)", "ts": "2026-08-11T22:34:49Z", "type": "incident"}
