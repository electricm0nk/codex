{"actor": "spell-surface", "actor_source": "env", "derived": false, "id": "1785478532080-spell-surface-833bec", "origin": "agent", "reason": "Fixing the two named defects (ARG missing from BOOK_LABELS/BOOK_ORDER, stale 1093/three-book totals) restores a correct label for every served code, so no lookup returns undefined and every book has a filter button. Changing the visible badge from code to full name is a display-convention change affecting all four books equally and every row, which is wider than the brief's two defects", "repo": {"branch": "tranche/7", "head": "5917d473", "worktree": "codex"}, "revisit": "If an operator or tester reports the badge codes read as unexplained on the catalog surface", "scope": "small — one JSX text swap on the filter buttons, larger if the per-row badge changes too", "source": "cli", "summary": "deferred SpellCatalogScreen still renders the short wire code (CRB/APG/ACG/ARG) as the visible text on both the per-book filter buttons and each row's book badge; the full display name reaches the user only via the title tooltip and the intro sentence, which now enumerates all four labels: Fixing the two named defects (ARG missing from BOOK_LABELS/BOOK_ORDER, stale 1093/three-book totals) restores a correct label for every served code, so no lookup returns undefined and every book has a filter button. Changing the visible badge from code to full name is a display-convention change affecting all four books equally and every row, which is wider than the brief's two defects", "ts": "2026-07-31T06:15:32Z", "type": "deferral", "what": "SpellCatalogScreen still renders the short wire code (CRB/APG/ACG/ARG) as the visible text on both the per-book filter buttons and each row's book badge; the full display name reaches the user only via the title tooltip and the intro sentence, which now enumerates all four labels"}
