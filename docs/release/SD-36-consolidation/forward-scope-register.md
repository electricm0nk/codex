---
canonical: true
owner: operator
bundle_id: SD-36
date: 2026-09-15
---

# SD-36 Forward Scope Register

Planned work deferred from SD-36. Each row is a **capability deferral** (not a blocker) — scoped work that was out of scope for this bundle but should land in a successor.

---

## Deferrals recorded during SD-36

| ID | Work | Reason | Target bundle | Notes |
|---|---|---|---|---|
| FS-1 | Semantic dedups in pilot_compute (fable review PC5-3/PC6-1/PC2-3) | Design work, requires adversarial review. Deferred for correctness focus bundle. | SD-36.5 or later (correctness phase) | Logged in SD-36 retrospective; blocks some compute optimizations but does not block release. |
| FS-2 | SD-34 fable-review P1 correctness rows (C2.5) | Separate correctness review bundle. 8 open P1s from fable review (doc link: `docs/release/SD-34-corpus-sheet-completion/references/README.md`). | Correctness bundle (post-SD-36) | Independent scope; should land before Starfinder bundle. |
| FS-3 | rules_tables → data package (181,797 lines) | Starfinder data-driven architecture requires rules as JSON/YAML, not Rust. Burn-down tied to `.lst` citation de-duplication (D6). | Starfinder data-package bundle | 8,592 `.lst` citations (provenance strings in rules_tables) burned down when Rust data becomes external package. Work inventory already frozen; this unblocks that transition. |
| FS-4 | pf1e_dashboard_producer.py extraction (if incomplete during B7) | Extract doneness.py library (~150 lines) from the producer if the extraction is clean; else defer whole producer deletion. | Immediate post-SD-36 fix cycle or next bundle | Recorded in Epic B7 deferral decision if not complete. |
| FS-5 | CI oracle fetch for converter test (optional per D1 Phase 0b) | Sparse clone via `scripts/fetch-pcgen-oracle.sh` in CI test job so gate proves something in CI. Currently: gate skips without checkout. | Next CI infrastructure bundle | Orthogonal to SD-36 scope; improves test coverage but not required for ship. |
| FS-6 | GATE-03: widen `scripts/pcgen_residue_gate.py`'s `TOKEN_SYNTAX_PATTERNS` beyond its current 8 heads to the full PCGen token-head vocabulary `table.rs`'s `row_for_head` already enumerates | Explicit P3 in the SD-35 code review (`scratchpad/review/converter.md` #11); confirmed a real gap (`SPROP:`/`CHOOSE:`/`AUTO:`/`QUALIFY:`/`COST:`/`SPELLKNOWN:`/`NATURALATTACKS:`/`CSKILL:` uncovered) but no current live-code exploit (every uncovered-head hit today sits in a `#[cfg(test)]` fixture that also carries an already-covered pattern). | Next SD-N code-quality/gates pass | No change made this cycle per the SD-36 Epic E brief's own instruction ("GATE-03 (P3): record as a forward-scope row; no change"). |
| FS-7 | engine-P1-3 full fix: thread the source book through `HeldSeed`/`ChosenCharacterState` so `SheetRulePackage::find` can resolve a (kind,slug) collision by the character's own held book, not an alphabetical tie-break | `ChosenCharacterState`'s selection fields (`selected_feats` etc.) are bare compound-string slugs with zero book component, corpus-wide, across all of `pilot_compute` (88k+ lines) and its fixtures -- a schema migration (saved-character version bump, default `core_rulebook`), not a bounded fix. 32 real (kind,slug) collisions with differing converted values are reviewed and pinned as a regression gate in the interim (`tests/sheet_rule_book_collision_census.rs`). | Correctness bundle (post-SD-36), or its own scoped migration cycle | NEEDS HUMAN RULING logged in `codex-morning-log-2026-09-16.md`; retro deferral not filed (a scope decision, not a mistake) -- see commit `670b8546fd`. |
| FS-8 | ~~PC4-1: Warpriest Blessing chooser "recognized" contradiction~~ -- **RESOLVED, fix cycle 2**: `blessing_recognized` now also checks whether either generic pass actually pushed a real explanation for the player's own selection (`blessing_generically_grounded`, bracketing `explanations.len()` around both `push_generic_pool_group_selection_{magnitude,description_magnitude}` calls), not just Destruction/Strength by name. A Rune/Earth/etc. Blessing choice that resolves generically is now `recognized` (the non-blocking `unmodeled` note), never `unsupported`; a selection that resolves to NOTHING (an invented slug, a formula the interpreter refuses) correctly stays `unsupported` -- the exact per-choice success detection this row's own "correct fix" description called for. | Was: the review itself marked this COMPLEX/unresolved; the fix needed per-player-choice success detection through the generic pass's member-level resolution rather than a group-membership check alone. | N/A -- fixed | RED-confirmed test `warpriest_with_a_generically_grounded_blessing_is_recognized_not_unsupported` (`pilot_compute/mod.rs`); retro deferral `1789643677178-sd36-epic-e-30e143` superseded by the fix, `codex-morning-log-2026-09-16.md`'s NEEDS HUMAN RULING bullet answered by the code change rather than a ruling. |
| FS-9 | GATE-02 full roster widening: stratified cross-book PCGen oracle-parity roster + re-run (beyond the 29-character CRB-only roster) | Requires a real PCGen `BatchExporter` invocation per new character (new `.pcg` templates per target book's own class/race/feat/equipment naming) -- infrastructure work, not a doc/code-only change. **Fix cycle 2 update**: widened 29 -> 31 (`EXTRA_BOOK_CLASSES` in `scripts/oracle_harness/sheet_parity.py`: Alchemist L1 and Witch L1, both Advanced Player's Guide, each carrying `CAMPAIGN:Core Rulebook` + `CAMPAIGN:Advanced Player's Guide` per that book's own `PRECAMPAIGN:1,INCLUDES=Core Rulebook`), genuinely re-run end to end (roster -> PCGen `BatchExporter` export -> `sheet_rule_parity` -> compare). Result: lines compared 159->167, chassis compared 382->408, the same 8 pre-existing disagreements reproduce byte-for-byte (none new, none resolved -- the 2 new members introduce zero new disagreements). This is a real start, not the full ask: 31 books ship a `class` or `race_trait` directory today (`ls -d data/sheet_rules/*/{class,race_trait} 2>/dev/null`) and this cycle covers 2 (Core Rulebook, Advanced Player's Guide); equipment/feats exercising CONV-01..04 on a cross-book member were not added. Artifacts: `docs/release/SD-36-consolidation/artifacts/gate-02-oracle-parity-widening/{roster,exports,ours.json,sheet-parity.json}`. | Next SD-N oracle-harness infrastructure pass (remaining ~29 books) | Retro deferral `1789645756930-sd36-epic-e-aa019a` (still applies to the remaining scope; the 2-book start is not itself a repeat of the original deferral). |
| FS-10 | Mechanism B unresolved references: child ability category whose target exists in the pinned oracle but is not a converted unit -- 63 rows | `python3 docs/release/SD-36-consolidation/artifacts/epic-f/scripts/unres2.py` (Epic F's mechanism table, `decisions.md §12`) -- nothing to link to; the target itself was never ingested. No class-closure row sits in this mechanism (Epic F's closure simulation found 0 closures with `B>0`). | Correctness/coverage bundle whenever those source books are widened | Scoped out of Epic F entirely per the operator's Option-A ruling (`decisions.md §12`); no per-class impact measured. |
| FS-11 | Mechanism D unresolved references: PLAIN ability category (2,911 of them `Special Ability`) whose target IS a converted record, but the resolver still misses for an undiagnosed, separate cause -- 3,033 rows corpus-wide (mostly monster/companion `X ~ Y` abilities; 177 of the 3,033 have more than one oracle definition of the key) | `python3 docs/release/SD-36-consolidation/artifacts/epic-f/scripts/unres2.py` -- the resolver defect is NOT the child/parent-category miss option A fixes; root cause not diagnosed (`epic-f-class-completion.md §1` names it explicitly as "NOT diagnosed"). **Epic F closes 2 class closures / 5 rows of this mechanism as part of its own scope** (Antipaladin 1 row, Sanguine Angel 4 rows -- `epic-f-class-completion.md §3b.5`, `closure.py`), because those rows sit inside a class's own grant closure and block a real proficiency answer; the remaining 3,028 rows (corpus-wide, not class-closure-blocking) stay open here. | Correctness bundle (post-SD-36) -- needs its own root-cause diagnosis before a fix can be scoped | Not started; distinct from mechanism A (option A already fixes A in full). |
| FS-12 | Mechanism E unresolved references: plain category, target exists in the oracle but its book/file family is not ingested (e.g. `Sorcerer Bloodline\|Aberrant Bloodline`) -- 3,565 rows corpus-wide | `python3 docs/release/SD-36-consolidation/artifacts/epic-f/scripts/unres2.py` -- correct as a defect; nothing to link to until the source book/family is ingested. 45 class closures carry 164 rows of this mechanism (Ex-Antipaladin 85, Druid 8, Student of Perfection 6, Tactician 6, Magus 5, Dread 4, ...), but `closure_complete` is defined through the ORACLE target's own content (`epic-f-class-completion.md §3.4`): an un-ingested target that carries no weapon grant does not make a class's proficiency answer unknown, so none of the 45 closures block Epic F's F1.5 coverage acceptance. | Next book-ingest pass (per source family) | No class-closure impact measured; purely a book-coverage deferral. |
| FS-13 | Mechanism F unresolved references: target found nowhere in the oracle -- bracketed names, comma-joined lists, case mismatches, nested-paren options; 3+ distinct small parser causes -- 808 rows corpus-wide | `python3 docs/release/SD-36-consolidation/artifacts/epic-f/scripts/unres2.py` -- 17 of the 808 are proficiency-related archetype/feat grants (Kensai, Spire Defender, ARG archetypes), not base-class grants, and block no census class. **Epic F closes 7 class closures / 15 rows of this mechanism as part of its own scope** (Alchemist 9, Slayer 1, Ex-Antipaladin 1, Diabolist 1, Exalted 1, Magus 1, Marksman 1 -- `epic-f-class-completion.md §3b.5`), each fixed by its own named parser cause; the remaining ~793 rows (corpus-wide, not class-closure-blocking) stay open here. | Next code-quality / parser-hardening pass | Not started for the non-class-closure remainder. |
| FS-14 | Option-carrying reference needs an option-carrying `Holdable` -- 203 of 7,119 `_defects/unresolved-references.json` rows name a target with a NESTED parameter (`FEAT\|Skill Focus (Knowledge (Religion))`, `FEAT\|Rapid Reload (Crossbow (Hand))`, `FEAT\|Exotic Weapon Proficiency (Sword (Bastard))`); one of them, Exalted's class prerequisite (`inner_sea_gods:class:exalted`), keeps a prestige class's proficiency answer Unknown (`artifacts/epic-f/reader-remainder.md`, G-O) | A naive balanced split was implemented and measured in F3b2, then reverted: `Holdable` carries no option, so the reference would hold `Skill Focus` with ANY option, and `_vars/v3ecc4923bf829539` ("Dwarven Waraxe Exotic Use") would read `Holds(exotic_weapon_proficiency)` for any Exotic Weapon Proficiency -- a wrong computed number (`artifacts/epic-f/stage-f2-f3/f3b2-converter-receipt.md`, Declined). Re-derive: `python3 -c "import json,re; d=json.load(open('data/sheet_rules/_defects/unresolved-references.json')); print(sum(1 for r in d if re.search(r'\([^()]*\([^()]*\)[^()]*\)\s*$', r.split(': ',1)[1])), len(d))"` | Schema bundle that gives `Holdable` an option (sheet-rule schema + engine held-set) | Named in F3b2b (`artifacts/epic-f/stage-f2-f3/f3b2b-receipt.md`); not a converter fix, a schema one. |
| FS-15 | Oracle `BONUS:SAVE` formula defect on six prestige classes -- the class line's save formula matches no PF1 save form, so each class's carrier mix is refused with `multiclass.save_shape.unrecognized` (never a silent poor save). **Named remainder 6 of 74** prestige carrier mixes (census `prestige_mix_computed=67`, `census-f3c2.json`). Rows (oracle line, book page from the line's own `SOURCEPAGE:`): Evangelist `isg_classes.lst:6` (Inner Sea Gods p.198; Reflex `classlevel()/3+1` under its own `ClassSaveGood_Reflex`, +4 at 10th), Exalted `isg_classes.lst:25` (Inner Sea Gods p.200; `CL+1/3`/`CL+1/3`/`CL+1/2`), Sentinel `isg_classes.lst:48` (Inner Sea Gods p.202; `CL+1/2`/`CL+1/3`/`CL+1/3`), Pure Legion Enforcer `isc_classes.lst:5` (Inner Sea Combat p.32; `CL+3/2`/`CL+1/3`/`CL+3/2`), Ulfen Guard `isc_classes.lst:29` (Inner Sea Combat p.34; `CL+3/2`/`CL+1/3`/`CL+3/2`), Mammoth Rider `ag_classes.lst:260` (Adventurer's Guide p.128; Fortitude `(CL+2)/2`). PCGen divides before it adds, so `classlevel+1/3` is `level + 1/3`: the oracle itself prints +10 at 10th. | Upstream oracle data defect, not an engine or converter miss (`artifacts/epic-f/stage-f2-f3/f3b3-receipt.md` §3, `f3c-remainder.md`). Printing the oracle's number puts a save on the sheet the book does not state; repairing the formula from memory is a fabricated row (`decisions.md §14.1`). | A data-correction pass with a book-table oracle | Closable ONLY by a book-cited override: each row's corrected save progression read from its book's own class table (the six pages above), recorded with that citation, and pinned by a hand-worked test against the table. Until then the six stay Blocked; pinned by `multiclass_fold::tests::the_four_unrecognized_prestige_saves_are_oracle_formula_defects_not_a_missed_shape` and `two_more_prestige_saves_the_f3c_carriers_reach_are_the_same_oracle_formula_defect`. |
| FS-16 | **CLOSED 2026-09-24 by SD-36 F3c3** (`artifacts/epic-f/stage-f2-f3/f3c3-receipt.md`): the converter now carries every `SUBCLASS:` line of the pinned tree (2 classes, Wizard and Psion; 47 lines, 40 converted after 7 superseded reprints) as a class choice with its grants, Psion's canonical discipline is seeded, and `class:psion` is Computed at levels 1-20 (census `computed=63` of 63, `census-f3c3.json`). Original row: Psion class skills: the converter does not carry PCGen `SUBCLASS:` lines. The Psion's base class skills (Autohypnosis, Craft, Knowledge, Profession, Spellcraft) exist ONLY on its discipline `SUBCLASS:` lines (`ultimate_psionics/up_classes.lst:221-248`), and each discipline's `<Discipline> Class Skills` record (converted, e.g. `ultimate_psionics:class_feature:psychokinesis_class_skills`) has no granting edge because the `SUBCLASSLEVEL:1` line that grants it is not converted either. The class-skill reader therefore answers Unknown and the census keeps `class:psion` Blocked on `skill.selected_modifier.class_skill_unknown` at every level (**1 of 63** non-prestige ids, `census-f3c2.json`). | A converter change (convert `SUBCLASS:` as a class choice carrying its `CSKILL:` and `SUBCLASSLEVEL:` grants, then seed a canonical discipline through `class_seeds`); `data/sheet_rules/**` is frozen in the F3c2 engine batch (`artifacts/epic-f/stage-f2-f3/f3c2-receipt.md`). An engine-side oracle row is not a fix: which of Climb / Intimidate / Swim is a class skill depends on the discipline (Psychokinesis grants Intimidate, Psychoportation Climb and Swim, `up_abilities_class.lst:408/410`), so a class-keyed row prints a wrong +3 for some psions. | Next converter batch | The diagnostic keeps firing until the record answers. |
| FS-17 | **Converter half CLOSED 2026-09-25 by SD-36 F3c4b** (`artifacts/epic-f/stage-f2-f3/f3c4b-receipt.md`): every ability-category pick row a converted pick chooser selects and no inventory unit stands for converts as a `pool_option` rule granted by the choice (`sheet_rule/pool_option.rs`; 274 options over 12 pools, 277 pick rows, 3 named). The 32 sorcerer bloodline pick rows are among them; 0 `Sorcerer Bloodline\|` references stay unresolved (65 before); through the pick, **269 of 287** bloodline lines are reachable at sorcerer 20 (0 before; `f3c4b-bloodline-sweep.md`). **Engine half CLOSED 2026-09-25 by SD-36 F3c4** (`artifacts/epic-f/stage-f2-f3/f3c4-receipt-2.md`): the character's Path-A pick links to its converted option by one rule (`sheet_rule::link_path_a_picks`); the class-skill union reads the character's picks (Aquatic Swim 6, hand-worked); the Sorcerer module yields to the record for any bloodline it does not model; the SD-32 generic pool-group pass yields for a linked, held selection (its level-ungated values are gone for 30 sorcerer and 10 bloodrager selections). **30 of 32** bloodlines Computed single-class at every level (`f3c4-bloodline-sweep-after.md`). | -- | -- | Remainder: Imperious and Kobold, Blocked by name (`class_feature.sorcerer.bloodline.converted_option_not_held`, FS-19); Claws held late (FS-18, closed for both bloodline lines by F3c5); the generic pass still prints ungated values for selections the package does not link (cleric domains, shaman spirits, 23 sorcerer and 2 bloodrager groups: `f3c4-pool-pass-scan.log`); Dragon Disciple's `Internal\|Bite` (FS-20, closed by F3c5). |
| FS-18 | CONV-02 single-line principal: a record that converts to ONE line keeps that line as its principal (SD-36 Epic E CONV-02; `principal_carries_more_than_its_line` does not count the record's prose), so the line's own condition gates the whole record. **The 2 bloodline lines CLOSED 2026-09-25 by SD-36 F3c5, as a side effect** (`artifacts/epic-f/stage-f2-f3/f3c5-receipt.md` §5): Draconic and Abyssal Claws (`cr_abilities_class.lst:2444`) now carry their `Internal\|Claw` helper as a `Fact::NaturalAttack`, so the principal carries more than its line; the claw-size line (`PREVARGTEQ:...Power1LVL,7`) moves, gate intact, to a `#weapon0` sibling and the record is held from sorcerer 1 as CRB p.75 states (bloodline sweep: lines held late 20 -> **18 of 287**, all FS-19). Rage Power Animal Fury split the same way (3 records, pinned `f3c5_line_split`). | Open for every other single-line conditional record with prose that carries no grant: a generic fix changes the principal/sibling shape of that population, a converter step with its own population count and pins. | Next converter step | `tests/sd36_bloodline_pick_option.rs` now asserts Claws from level 1 (pin moved in F3c5, retro-logged). |
| FS-19 | **Re-classified 2026-09-25 by SD-36 F3c5** (`artifacts/epic-f/stage-f2-f3/f3c5-receipt.md` §6). The gate is decidable and converted correctly: `PREFACT:1,TEMPLATES,Is<Race>=true` (`arg_abilities_class.lst:13`) converts as `Holds <book>:template:is<race>`, and `CharacterFacts` carries the character's race (`race: Option<String>`, seeded into the held set). The option is **not** correctly ungranted, though. Imperious is a human-only bloodline, and in the oracle a human holds `IsHuman` through human race -> `ABILITY:Internal\|AUTOMATIC\|Racial Traits ~ Human` -> `TEMPLATE:Race ~ Human` (`core_essentials/races/human/human_abilities_race.lst:10`) -> `TEMPLATE:IsHuman` (`human_templates.lst:4-5`, `FACT:IsHuman\|True`). The package's `core_rulebook:ability:racial_traits_human` is filed from the object's `.MOD` row (`human_abilities_globalvar.lst:10`), and its closure omits the base row that carries `TEMPLATE:Race ~ Human`. So neither a human (Imperious) nor a kobold (Kobold, the same shape through `kobold_abilities_race.lst:10`) ever holds the template. Bloodline sweep, probed with the gated race: **0 of 9** lines held for each, and 18 of 287 lines unreachable. Both bloodlines stay Blocked by name, never Computed with an empty bloodline. | Mechanism M (new, FS-22): a record filed from its object's `.MOD` row omits the object's base row. This is a closure-building fix, not a gate fix; the brief allowed no converter change unless the gate itself was mis-converted. | Next converter step (FS-22) | The re-classification is proven by `f3c5-bloodline-sweep.log` rows `sorcerer_bloodline_imperious` (race human) and `sorcerer_bloodline_kobold` (race kobold). |
| FS-20 | **CLOSED 2026-09-25 by SD-36 F3c5** (`artifacts/epic-f/stage-f2-f3/f3c5-receipt.md`). A `CATEGORY:Internal` natural-attack helper row that no inventory unit stands for, and whose whole object carries nothing but one natural attack's bookkeeping, converts as `Fact::NaturalAttack(<attack>)` on the rule that grants it (`sheet_rule/natural_attack.rs`). No new record is written; records stay at 49,450.<br>**Denominators:** the pinned tree declares 794 plain Internal natural-attack-typed ability rows, 0 of them `.MOD`, `.COPY` or `_pfs`. 771 have no unit standing for them. 770 are classified natural-attack-only, answering 771 `(Internal, key-or-name)` pairs. 0 are named (carries-more 0, names-no-attack 0, pair-shared 0).<br>**Results:** `unresolved-references` 6,932 -> **6,252**. The 680 cleared rows are all mechanism E (unres2: E 3,444 -> 2,764) and 0 new rows appear. 681 fact grants land on 546 rules. Dragon Disciple's closure is attested, its reader answers Known(empty), and its carrier mix is Computed: **prestige mixes 68 of 74**. | Remainder: **6** references to a helper sit in a gate (`PREABILITY:1,CATEGORY=Internal,Bite`: Blood Feast, Horrific Gorging, Death Roll, Mythic Aspect of the Beast x2, Evolution Rake). A fact is not holdable, so these stay `MissingRule` terms. | A holdable natural-attack term (schema), if any sheet total ever reads one | None of the 6 is in a census class closure. |
| FS-21 | SD-32 generic pool-group pass, level gate undecidable: `push_generic_pool_group_selection_magnitude` evaluates every `<group> ~ <member>` record at the character's level with no level gate. SD-36 F3c4 made it yield wherever the character's pick links to a held converted pick option; for the rest the held set cannot decide a member's level, so the pass still prints: Cleric Domain 73 selections / 2,960 member values over levels 1..=20, Shaman Spirit 14 / 1,240, Sorcerer Bloodline 23 / 1,040 (wildblooded and other groups with no `sorcerer_bloodline_<x>` option, plus Imperious and Kobold, FS-19), Bloodrager Bloodline 2 / 220; Warpriest Blessing and Cavalier Order print none (`artifacts/epic-f/stage-f2-f3/f3c4-pool-pass-scan.log`). None of these values feeds a sheet total. | A converted pick option for those pools (the converter's pick-row rule found no domain/spirit pick chooser, `f3c4b-receipt.md` §2), then the same yield. | Next converter step | On linked selections the pass had printed 1,960 of 10,420 member values at a level (or for a record) the held set does not hold (Sorcerer 1,822, Bloodrager 138). |
| FS-22 | Mechanism M (named 2026-09-25 by SD-36 F3c5, `artifacts/epic-f/stage-f2-f3/f3c5-receipt.md` §6): a record filed from its object's `.MOD` row omits the object's base row. PCGen applies every `.MOD` row to the object its key names, so the object is the base row plus its `.MOD` rows. The inventory files some objects from a `.MOD` row (`Racial Traits ~ <Race>`: the unit sits at `<race>_abilities_globalvar.lst:10`, a `CATEGORY=Internal\|Racial Traits ~ <Race>.MOD` row), and the record's closure then omits the base row (`<race>_abilities_race.lst:10`, carrying `TEMPLATE:Race ~ <Race>` and the racial casting variables). Proxy population: **143** records whose first closure row is a `.MOD` row, and whose object's base row sits in no record's closure. 42 of them are `racial_traits_<race>` abilities; by kind the 143 are ability 53, class_feature 47, feat 36, spell 3, race 2, race_trait 2. The figure comes from `python3 artifacts/epic-f/scripts/f3c5_mod_base_rows.py`, a Python walk of the pinned tree, not the converter's own index. This mechanism keeps FS-19's Imperious and Kobold options unheld. | A closure-building change: a `.MOD`-anchored record's closure starts at its object's base row, like F3c4b's `mod_fragments` rule for grants. That is a converter step with its own population count and pins. | Next converter step | Measure with the converter's own index first; the Python count is a proxy. |

---

## No deferrals for SD-36 blockers

Every acceptance criterion in `epic-breakdown.md` is scoped to this bundle. No work falls into the "open blocker" category (see `docs/governance/blocker-closure-doctrine.md`). The bundle is 100% scoped or explicitly deferred.

---

