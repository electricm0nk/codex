---
canonical: true
owner: sd31-orchestrator
purpose: Unattended-mode blocker and open-question log for SD-31. The operator reads THIS file on check-in.
started: 2026-08-15
---

# SD-31 — Open Issues, Blockers, and Operator Rulings Needed

**How this file works.** SD-31 runs unattended. A cycle never stops to ask a question. When a cycle
hits a hard block, needs an operator ruling, or takes a default it wants reviewed, it appends a row
here and keeps working. The operator reads this file on check-in and answers in the `Operator ruling`
column.

**Rules for cycles appending here:**
- Append, never rewrite another entry.
- One row per issue. Give the exact command and exit code, not a narration.
- `Severity`: `BLOCKER` (work stopped on this card), `RULING-NEEDED` (proceeded on a default, operator
  should confirm), `NOTE` (recorded, no action wanted).
- A Structural Exclusion Register proposal (`decisions.md §3`) is logged here as `RULING-NEEDED` with
  a pointer to the proposal — a cycle may propose, only the operator grants.

## Needs an operator ruling

**MOST URGENT, PRECEDENCE-1, PI — rows 141/149, 2026-08-16, `SD31-W8-INTEGRATE-001`.** Two public-feed
exposures on `site/dashboard/`, the ONE surface `deploy-site.yml` deploys to Cloudflare Pages on every
push to `main`. **Row 141** (`site/dashboard/units/*.json`, from `sd31/attrib-evidence-003`): 261
units' names published verbatim where the unit's own cited row declares `NAMEISPI:YES`. FIXED for THIS
wave — the shard directory was not committed at all — but the producer itself (`scripts/observer/
pf1e_dashboard_producer.py`'s `build_unit_shards`) still has no oracle cross-reference, so the next
cycle that runs the real (non-`--check`) publish and `git add`s `site/dashboard/units/` reintroduces
the same exposure; needs a build decision on the redaction rule (drop `name`, or exclude the record
entirely, matching `data/corpus/`'s own treatment). **Row 149** (`site/dashboard/PF1e-dashboard.json`
itself, its `manifests`/roadmap content): a DIFFERENT, PRE-EXISTING exposure this wave's integration
cycle discovered while performing the mandated dashboard publish — 56 candidate declared-PI names
(class/prestige-class/feat/spellbook identities) ship as a public "what's coming next" list, confirmed
present in `HEAD` before this cycle touched anything, so it predates this whole package's dashboard
feature. NOT fixed — needs an operator ruling on what a public roadmap should show for a PI-declared
record before any field gets removed.

**Rows 69/87/95/107 — ANSWERED 2026-08-16, `decisions.md` §7 REFINED (`SD31-D7-PROSE-004`).** The
flat-vs-scaling question this section used to lead with was the WRONG axis; the operator's actual
ruling is UNIVERSAL vs CONDITIONAL: *"+1 size bonus to AC means you need to give a +1 on the AC -
that's computed... if that +4 bonus was ONLY against certain creature types, like with dwarf racial
hatred - that's not a universal bump... if it says acid damage - that's a condition, many things
shrug off acid."* `SD31-D7-PROSE-004` built the real discriminator
(`closure_states_universal_sheet_modifier`, `v06_work_inventory.rs`), retired the four hand-picked
`*_FLAT_MAGNITUDE_PENDING_RULING` name-lists rows 69/95/107 (and wave 7's `CLASS_FEATURE_...`/
`FEAT_...` lists) had built pending this exact ruling, and applied it corpus-wide. Outcome, both
directions, every unit individually re-derived (not estimated): **21 units promoted** (13
`class_feature` + 7 `feat` + 1 `monster_ability` — every one of the previously-parked units, all
genuinely conditional) and **6 units demoted** (`gnome_size`/`grippli_size`/`halfling_size`/
`kobold_size`/`svirfneblin_size`/`goblin_size`, the Small-race size traits — universal AC/attack/
CMB/CMD/Stealth bonuses that must compute, per the ruling's own worked example). Board: 9,780 → 9,795
done (25.3887% → 25.4277%). Full citations, the discriminator's 32-case hand-labelled accuracy proof,
and the corpus-wide false-positive fix (narrowing `UNIVERSAL_MODIFIER_CUES` to `"size bonus"` alone
after 5 false positives surfaced running the retired-list replacement against the FULL corpus) are in
`v06_work_inventory.rs`'s own doc comments and this cycle's `progress.md` receipt. **Row 87's own
`goblin_size` classification was WRONG in this cycle's own dispatch brief** ("grants nothing") — the
real shipped `bestiary:race_trait:goblin_size` row states the identical universal size-bonus shape as
the other 5 and is demoted alongside them; `retro.py correction` emitted, verified by direct `cat` of
`data/corpus/beastiary/race_trait/goblin/goblin_size.json`.

- **Row 36 — ANSWERED 2026-08-16, `decisions.md` Decision 7.** Exclusion DECLINED; a done-bar granted instead (prose only + nothing to compute + prose printable in the sheet description == `done`). The 404 units stay in the denominator. Original ask: Structural Exclusion Register proposal for the `ambiguous:prose_scaling_phrase`/
  `ambiguous:prose_ability_scaling` population, **404 units** (re-derived unchanged at this cycle's tip,
  `scripts/reachability_audit.py`'s `ambiguous_wiring_class_units`). Proposed only, not signed.
- **Row 44 — ANSWERED 2026-08-16, `decisions.md` Decision 8.** Operator: *"you were right to say wire up the 7 for real."* Retraction DECLINED, wiring required — discharged same day by `SD31-E6-F1-002` (real Tauri command + DoD-8 screenshot of Demon (Balor)). Original ask: whether `SD31-E6-F11-002`'s evidence-integrity-clean-but-DoD-8-incomplete 7-unit `done`
  movement should count as `done` today or be provisionally reverted to `held` pending the still-missing
  on-screen verification. Not acted on since; the units remain `done` under the current ruling default.
- **Row 55** — Structural Exclusion Register proposal for `spell`'s 13 `.CLEARALL` copy-variant units
  (12 `core_rulebook`, 1 `advanced_race_guide`) that genuinely state no spell level. Proposed only, not
  signed.
- **Row 63** — PI-safety note, informational: a 413-record `raw_tokens` DESC-leak shape (row 63) is now
  fixed and independently re-verified CLEAN, but the operator should know it existed and reached this
  volume before being caught, and may want to ask whether the same `description`-only-redaction shape
  exists in any other preserved free-text carrier this program has not yet audited.
- **Row 87 — genuinely open, `SD31-W5-INTEGRATE-001`.** Does Decision 7's "nothing to compute" mean
  (a) no numeric value appears anywhere in the prose, or (b) no *character-specific scaling* formula
  (the existing `wiring_class.rs` bar)? **11 `race_trait` units currently `done`** state a real, flat,
  uncomputed numeric bonus in prose only (e.g. `duergar_stability`'s "+4 racial bonus to their Combat
  Maneuver Defense") — reading (a) makes them over-claims, reading (b) leaves them correctly `done`.
  Exact units named in row 87. Same question row 69 already raised; not resolved since.
- **Row 95 — genuinely open, `SD31-D7-PROSE-002`.** Same question as rows 69/87, one more confirmed
  unit: `bestiary_2:monster_ability:devilfish_water_dependency` (row 69's own named example) would
  have been newly promoted to `done` by this cycle's `monster_ability` rung; excluded via a one-line
  named-pair list pending the ruling rather than promoted or generalized.
- **Row 107 — genuinely open, `SD31-W6-INTEGRATE-001`, SIZED.** The same question rows 69/87/95 ask,
  re-derived fresh at this integration cycle's tip rather than trusted: **645 of 937 (68.8%)** `text-
  complete` `monster_ability` units and **179 of 308 (58.1%)** matched `text-complete`
  `equipment`/`equipment_modifier` units state a flat, non-scaling numeric magnitude in prose only —
  **~824 units total, all currently `done`**, riding on which reading of "nothing to compute" wins.
  Not promoted, excluded, or generalized differently because of this count; sized only, per Decision 8's
  "no reduction in scope" precedent.
- **Row 140 (renumbered from the branch's own 129 during wave-8 integration) — genuinely open,
  `SD31-ATTRIB-003`.** Does a citation-only compendium
  book (Inner Sea Races, 2015-09 — newer than ARG, zero base `RACE:` declarations of its own, only
  `.MOD` citations) count as a "printing" under Decision 10's "newest publish wins," the same way
  `core_essentials`'s own citation-only root files are excluded under Decision 9? Full per-race
  evidence in `artifacts/SD31-ATTRIB-003-race-evidence.md`.

## Open

| # | Opened | Cycle-id | Severity | Issue | What the cycle did instead | Operator ruling |
|---|--------|----------|----------|-------|-----------------------------|-----------------|
| 1 | 2026-08-15 | SD31-E2-F1-001 | NOTE | `wiring_class::CorpusLines::line()` (`src/rules_core/wiring_class.rs:758`) resolves a unit's corpus row via a **single-level** `dir.join(file)` join. Several books' `.lst` files live in nested subdirectories (`core_essentials/races/<race>/*.lst`, `ultimate_combat/support/*.lst`, `horror_adventures/support/*.lst`, `inner_sea_world_guide/_pfs/*.lst`, `advanced_race_guide/_pfs/*.lst`, `adventurers_guide/support/*.lst`, etc.) — the join silently misses, `CorpusLines::line()` returns `None`, and the unit falls into `ambiguous:no_corpus_line` (D0) even though its row genuinely exists. Re-derived corpus-wide: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x.get('book')!='beginner_box']; ncl=[x for x in u if x.get('wiring_class_reason')=='no_corpus_line']; print(len(ncl))"` → **1,707** units (was documented as 47 in `docs/release/GE-01-legacy-corpus-and-conversion-matrix/artifacts/wiring-class-determination.md`, now stale). Recursive-glob check confirms **100% (1,707/1,707)** of these rows are findable somewhere under the book directory — none are genuinely synthetic/provenance-free. This is **80.9% of the whole 2,109-unit `ambiguous` population**. Not a blocker to SD31-E2-F1 (the ground-truth sample hand-resolved all 40 affected sampled units from the real row instead), but it is load-bearing for Epic 2-F2 (classifier build) and F3 (`ambiguous` dead-end closure): fixing the path join before building/accepting a classifier will move the vast majority of "ambiguous" units to their real class for free, and F3's "genuinely unreachable, propose to Structural Exclusion Register" review must not run against the current, badly-inflated `no_corpus_line` bucket. | Recorded the bug with full re-derivation and worked examples in `artifacts/SD31-E2-F1-ground-truth-methodology.md` §Finding A; hand-labelled the 40 affected sampled units from their real corpus rows (not the engine's `ambiguous` verdict) so the ground-truth sample itself is not corrupted by the same bug. No production code changed (out of this card's scope). | *(needs operator/Epic-2-F2 owner confirmation before any fix lands)* |
| 2 | 2026-08-15 | SD31-E2-F1-001 | NOTE | `wiring_class::signals()`'s scalar/arithmetic scan (`src/rules_core/wiring_class.rs:398-420`) reads the WHOLE field text, not just the magnitude value. Two consequences, both false positives for `derived`: (a) `BONUS:STAT|<ABILITY>|<literal>` fields always contain the ability's own name as the STAT selector (e.g. `STR`,`DEX`,`INT`), which collides with `SCALARS_WORD` regardless of whether the *value* is scalar-dependent — a flat `BONUS:STAT|DEX|2|TYPE=Racial` (`core_rulebook:race_trait:2_dexterity`) or `BONUS:STAT|INT|2|TYPE=Enhancement` (`ultimate_equipment:equipment:staff_of_mithral_might`) is misclassified `derived` instead of `static`. (b) `has_arith`'s unconditional `value.contains('/')` check fires on PCGen's literal `DR:10/Cold Iron`/`CR:1/3` bypass-type notation, not just division (`bestiary:monster:neothelid` compounds both). Confirmed in 3 of 150 sampled units as a clean single-cause misclassification (others carrying the same false-positive signal, e.g. `core_essentials:companion:pig`, still land on the correct class because a genuinely separate formula also fires). Full worked examples in the methodology note §Finding B. | Hand-labelled the 3 affected units `static` with the false-positive mechanism documented per-unit in the ground-truth JSON's `token_evidence` field. | *(informational — feed into Epic 2-F2's classifier acceptance work, not a blocker)* |
| 6 | 2026-08-15 | SD31-W1-INTEGRATE-001 | NOTE | `scripts/reachability_audit.py`'s docstring and commit `eadb263f7`'s message over-claimed the detection envelope as covering "any wiring_class/status word actually observed" — proven true only for a novel `wiring_class` word. A novel STATUS word landing on a real `computed`/`display` unit is silently absorbed by those branches' catch-all `else` (never raises, never reported as `unmapped`, ceiling unchanged). Mutation-tested this cycle: 3 real `computed` units forced to a fabricated status leave `unmapped_cells_with_units` empty, `ok=True`, exit 0 — where a fabricated `wiring_class` word on 1 real unit correctly fails. Non-blocking: the miss cannot make a unit vanish from a rollup, only misclassify it within an existing branch. | Corrected the over-claim in the module docstring this cycle (`scripts/reachability_audit.py`); did not build the additional `default-absorbed` cell-reporting the fuller remedy describes (out of this integration cycle's bounded scope). | *(informational — Epic 0 may add `default-absorbed` cell reporting in a future standing-gate enhancement cycle)* |
| 7 | 2026-08-15 | SD31-W1-INTEGRATE-001 | NOTE | `SD31-W1-INTEGRATE-001-verify.log`'s full-gate run flags 3 stale `scripts/verify-baselines.env` counters: `BASELINE_ROOT_LIB_TESTS` 1776→1777 (measured), `BASELINE_ROOT_FULL_TESTS` 6398→6411 (measured), `BASELINE_ROOT_TEST_BINARIES` 547→548 (measured). This integration cycle added zero `.rs` files (only a Python docstring and doc/artifact edits), so the drift predates this cycle and this run is simply the first to measure it since it happened. | Left `scripts/verify-baselines.env` unedited — a baseline-movement commit is a separate reviewable change per this program's convention, out of this cycle's bounded scope. | *(informational — a future cycle updates the 3 baselines with `--show-actuals` output in its own commit)* |
| 8 | 2026-08-15 | SD31-E2-F2-001-wiringfix | NOTE | While fixing row 2's `BONUS:STAT` selector false positive, found a structurally-related but DIFFERENT false positive left unfixed (out of this cycle's named scope): the `STAT:` `MAGNITUDE_TOKENS` field itself (distinct from `BONUS:STAT`/`TEMPBONUS:STAT`) sets ability scores directly as `STAT:STR=10\|DEX=10\|CON=10\|...` — `has_scalar`'s `contains_word` check on the whole value matches each ability-name KEY in this literal key=value list (e.g. `STR=10` contains standalone `STR`) and would misclassify these rows `derived` for the same underlying reason (a selector/key, not the magnitude, colliding with `SCALARS_WORD`) even though every value is a flat literal. Re-derived corpus-wide: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(sum(1 for u in d['units'] if u.get('wiring_class_reason')=='stat'))"` → **0** — no unit's `wiring_class_reason` is currently `stat` (the bare token's own signal name), so this specific collision does not currently move any real unit's class (either no unit's ONLY magnitude field is a bare `STAT:` row, or a co-occurring signal always wins first in `classify()`'s ordering) — flagged for whoever next touches `has_scalar`/`strip_stat_selector` in case a future corpus revision changes that. | Left unfixed — not named in OPEN-ISSUES row 2's worked examples, and `strip_stat_selector`'s narrow `"STAT\|..."`-value-prefix scope (correctly) does not reach it, since a bare `STAT:` field's value never begins with the literal sub-token `STAT\|`. | *(informational — Epic 2-F2's classifier-build feature seed, if dispatched, should re-derive whether any real unit is affected before deciding whether to fix)* |
| 9 | 2026-08-15 | SD31-E2-F2-001-wiringfix | NOTE | Validating the fix against the F1 ground-truth sample's 45 genuinely-evidenced units (`tests/sd31_e2_ground_truth_agreement.rs`) surfaced 5 disagreements at 40/45 agreement, all attributable to THREE separate, real classifier gaps the hand-labeller's own `token_evidence` already names — none of them one of row 2's two named findings: (a) `has_scalar`'s `SCALARS_SUBSTRING` check is case-sensitive and misses PCGen's lowercase `classlevel(...)` function-call form (`ultimate_magic:class_feature:dragon_shaman_totem_transformation`, `BONUS:VAR\|TotemTransformationDuration\|classlevel("Druid")` reads `static` instead of `derived`); (b) whether a bare non-literal, non-scalar variable reference (`MonkLVL`, `FavoredBaseBonus`, `RaptureLVL`) should be `ambiguous` rather than fall to the `static` default is an unresolved classifier DESIGN question, not a bug (3 units: `ultimate_combat:class_feature:martial_artist_martial_arts_master`, `core_essentials:race_trait:favored_enemy_humanoid_changeling`, `horror_adventures:class_feature:exciter_rapture`); (c) `has_arith`'s uppercase-run rule (`\+\s*\w*[A-Z]{2,}`) requires a `+` be followed by a WORD and does not match a parenthesised sub-expression immediately after `+` (`horror_adventures:class_feature:exciter_rapturous_rage`, `BONUS:ABILITYPOOL\|Rage Power\|10+(SpiritualistLVL>=14)+(SpiritualistLVL>=18)` reads `static` instead of `derived`). | Left unfixed — none is named in OPEN-ISSUES row 2's worked examples; fixing them would be undispatched scope expansion on a shared, heavily-consumed function. Encoded as the fixed `assert_eq!(agree, 40, ...)` regression guard in `tests/sd31_e2_ground_truth_agreement.rs` rather than silently averaged away. | *(informational — Epic 2-F2's classifier-build feature seed, if dispatched, has three concrete, hand-evidenced starting points)* |
| 10 | 2026-08-15 | SD31-E2-F2-001-wiringfix | NOTE | `CorpusLines::line()` (both before and after this cycle's fix) caches an unresolved `(book, file)` as `text.split('\n')` over an empty string, which yields a 1-element `[""]` buffer rather than 0 elements — so `line == 1` against ANY unresolved file returns `Some("")` rather than `None`; only `line > 1` correctly returns `None`. Pre-existing, not introduced by this cycle's `resolve_corpus_file` (the old single-level `dir.join(file)` + `.unwrap_or_default()` had the identical quirk). Re-derived: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(sum(1 for u in d['units'] if u.get('wiring_class_reason')=='no_corpus_line' and u.get('source_line')==1))"` → **0** — no real corpus unit is affected today. | Left unfixed — zero real-world impact measured, and fixing the cache's empty-string/empty-vec distinction is unrelated to either of row 1/2's named findings. Test suite exercises `line == 2` where a "genuinely absent" case is needed, with the quirk documented inline. | *(informational — worth a one-line fix `if text.is_empty() { Vec::new() } else { text.split('\n')... }` whenever someone is next in this function for another reason)* |
| 11 | 2026-08-15 | SD31-E6-F11-001 | RULING-NEEDED | Re-derived every one of the 2,481 `static`-held units against `corpus_literal_sweep`'s own verified set: **0 overlap**. All 2,481 fall into one of three structurally distinct buckets, none fixable by fixture-file growth: (a) **2,367** have no `data/corpus/<book>/<kind>/` directory at all (real ingest gap — e.g. `class_feature` is ingested for exactly one book, `pathfinder_unchained`, out of the ~23-book roster); (b) **95** have an `lst_token`-sourced corpus JSON record at the right line but `data.raw_tokens` is absent, so `corpus_literal_sweep::parse_transcription` (`src/rules_core/corpus_literal_sweep.rs:324-341`) excludes them from its population entirely — confirmed per-unit, 0/95 have `raw_tokens`; (c) **19** carry `source.kind` `lst_corrected_ingest`/`lst_inherited_copy`, deliberately excluded by the same gate because their bytes were intentionally altered from the raw `.lst` literal. The single largest held cell corpus-wide, `static\|ingested-magnitude\|equipment` (2,175 units), is dominated by a *fourth*, even more structural case: many of its records ship with `source.kind: "web_second_source"` (e.g. `data/corpus/advanced_players_guide/equipment/abacus.json`, sourced from `legacy.aonprd.com`) — no `.lst` `path`/`line` at all, so "byte-match the PCGen literal" is not merely unmet, it is inapplicable by construction. Full derivation and commands in `artifacts/SD31-E6-F11-001-held-cell-map.md`. | Documented the three-way (plus the equipment-specific fourth) breakdown with per-bucket commands and worked examples; did not touch `data/corpus/*.json` (regenerating/hand-editing shipped corpus records outside the real ingestion pipeline risks exactly the "generated artifacts mutated post-hoc" PI-exposure hazard this program has already hit once) and did not widen `corpus_literal_sweep`'s `parse_transcription` gate to accept a non-`lst_token`/no-`raw_tokens` record (that would be loosening the `static` done-bar itself, Decision 1(a)'s exact prohibition). | *(needs an operator ruling: does a `web_second_source`/`lst_corrected_ingest`/`lst_inherited_copy`-provenance `static` unit get its own, different done-bar, or does it wait on a richer re-ingest of its book? A ruling here potentially unblocks up to 2,481 held units with no new evaluator code — the single highest-leverage remaining lever this cycle found.)* |
| 12 | 2026-08-15 | SD31-E6-F11-001 | RULING-NEEDED | `derived_evaluator_fixture_check` (`src/rules_core/derived_evaluator_fixture_check.rs`) is hard-locked to `kind=equipment` (`ingested_equipment_dir`/`load_equipment_corpus` only ever open `data/corpus/<book>/equipment/`) and to one field, `item.ability_bonus` (a `BONUS:STAT` chain, via `compute_equipment_effects`/`magic_items::compute_magic_items_effect`). Re-derived: **2,704 of the 2,777 `derived`-held units (97.4 %)** sit under `kind ∈ {monster, spell, companion, monster_ability, class_feature, feat, race_trait}` — the checker has no evaluator seam for any of these; a fixture entry for one would either not resolve at all (no `equipment/` dir) or dereference `ability_bonus` on a record that can never populate it. Of the remaining 78 `kind=equipment`/`equipment_modifier` held units, exhaustively checked one-by-one against the real corpus (`artifacts/SD31-E6-F11-001-held-cell-map.md` Deliverable 2): the entire `BONUS:STAT` pool corpus-wide (51 records) is **already 100 % consumed** by the existing 94-entry fixture file (49 fixture-verified, 1 `computed`-not-`derived`, 1 already-`done`); the other 73 either need `ultimate_equipment` ingested first (60 units — that book has no `data/corpus/` directory at all) or carry a magnitude shape (`WEAPON\|DAMAGE\|min(STR,0)` bow scaling, `DR:n/type`, pure-prose) the checker has no field for at all; and the one candidate field the checker *could* add but doesn't (`weapon_enhancement_bonus`, already built and tested — `src/rules_core/equipment_effects/equipmods.rs`) has zero currently-`held` matching units in the ingested corpus (its 12 real corpus examples are already `computed`+`grounded`=`done`). | Ran the exhaustive per-candidate check (51 `BONUS:STAT` records, 73 individual `derived\|equipment` held units, 12 `weapon_enhancement_bonus` candidates) instead of adding a fixture entry that would fail or no-op; landed **0 new fixture entries** this cycle — see `artifacts/SD31-E6-F11-001-held-cell-map.md` Deliverable 2 for the full per-unit trail. Did not build a new `monster`/`spell`/`companion`/`monster_ability` evaluator seam (real, multi-day engine work, out of a fixture-growth card's bounded scope) and did not fabricate a fixture to make the count move. | *(needs a dispatch decision, not an operator ruling per se: the next real payoff requires either `ultimate_equipment` book ingest — Epic 6-F2/F5, unblocks 60 units — or a dedicated new-card build of a `monster` evaluator seam — unblocks 1,229 units, the single largest cell corpus-wide, but is a multi-cycle build, not a `-002` extension of this card.)* |
| 13 | 2026-08-15 | SD31-E1-F1-001 | NOTE | Epic 1's second race-chassis batch is not yet built. Two evidenced candidates, both requiring `ingest_races.rs` to gain the heritage/subrace-selector mechanism `race_resolver.rs` already models for Aasimar/Tiefling (this cycle's simple one-chassis-plus-flat-standard-trait-list loop does not): **Dhampir** (Bestiary 2, this cycle's own book, `core_essentials/races/dhampir/dhampir_abilities_subrace.lst`) and **Skinwalker** (Bestiary 5, `core_essentials/races/skinwalker/skinwalker_abilities_race_subrace.lst`, 84 chassis-blind `race_trait` rows — the single largest remaining per-race gap, re-derived by resolving each `race_trait_race_not_modelled` unit's source line's `TYPE:` token against the real `.lst` files). Also: the epic brief's often-quoted "553" workable-`race_trait` figure could not be reproduced by any of the derivations this cycle tried against the pre-batch corpus (closest candidate, workable-and-not-yet-done, was 492) — recorded rather than silently carried forward or forced to match. | Landed the 6 non-heritage Bestiary 2 races this cycle (Fetchling, Grippli, Ifrit, Oread, Sylph, Undine) and explicitly deferred Dhampir/Skinwalker to a follow-on batch rather than stubbing the heritage mechanism; re-derived (not reused) the workable-pool figures for this cycle's own receipt instead of citing "553". | *(informational — next Epic 1 cycle: extend `ingest_races.rs` with a heritage/subrace path before picking Dhampir or Skinwalker as the next batch; re-derive whatever "workable" denominator Epic 2/3/6 actually needs from the current `docs/work-inventory.json`, not from this row)* |
| 14 | 2026-08-15 | SD31-E2-F1-002 | NOTE | Running the new `scripts/ground_truth_evidence_guard.py` (see row 3's resolution) against the full 185-unit sample after this cycle's 105-unit relabel and 35-unit widening draw (both pass cleanly) surfaces a SMALLER, separate gap in the 45 units this cycle was explicitly barred from re-opening: `python3 scripts/ground_truth_evidence_guard.py docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E2-F1-ground-truth-sample-v1.json` → `FAIL: 24 violations`, 21 distinct affected ids, all inside the untouched 45. Two shapes: (a) 4 `ultimate_combat:class_feature:monk_bonus_feat_*` units share byte-identical, non-record-specific `token_evidence` ("no_corpus_line bug: real row found, mags empty, same monk-bonus-feat shape -- no_corpus_line_bug") quoting zero corpus-specific tokens — a smaller instance of exactly the defect this cycle fixed, missed by the original Opus review because it checked for one specific 105-unit-shared string, not duplication in general; (b) 17 further units carry genuine but short (<20-char) quotes or name field TYPES generically rather than quoting actual VALUES (e.g. `core_essentials:race:elf_aquatic`'s `"MOVE:Walk,30"`, 13 characters — genuinely real, just short). Full list in `SD31-E2-F1-ground-truth-methodology.md` §"A gap the evidence guard found in the untouched 45". | Logged the finding with the guard's own output as proof; did NOT touch any of the 45's fields — the brief's "Keep the 45 genuinely-evidenced units UNCHANGED. Do not re-open them" instruction is explicit and this includes strengthening their evidence text without changing the verdict. | *(needs Epic-2 owner: a future cycle authorized to touch the 45 should re-read the 4-unit duplicate cluster's real rows and/or lengthen the 17 short-quote units' evidence)* |
| 15 | 2026-08-15 | SD31-E2-F1-002 | NOTE | `scripts/ground_truth_evidence_guard.py` is fully built, proven able to fail (4 defect-shape tests) and pass (3 clean tests) via `python3 -m unittest scripts/tests/test_ground_truth_evidence_guard.py` (9/9 green), but is deliberately NOT added to `verify.sh`'s `ALL_STAGES`/`QUICK_STAGES` this cycle. `verify.sh` has only two stage tiers — anything invokable via `--only` is also part of every ordinary full/quick run, there is no "registered but not default" option — so wiring it in today would fail BOTH modes for every future card, repo-wide, until row 14's 21-unit gap in the untouched 45 is fixed, a defect this card was barred from repairing. | Shipped the script + its passing self-test suite, fully functional and independently runnable (`python3 scripts/ground_truth_evidence_guard.py <path>`), but withheld its default `verify.sh` wiring rather than either force a false pass or red every sibling card's gate on an out-of-scope defect. | *(needs Epic-2 owner: wire `ground-truth-evidence-guard`/`ground-truth-evidence-guard-selftest` into `ALL_STAGES` and `QUICK_STAGES` in `scripts/verify.sh`, ideally in the same commit that fixes row 8 — or immediately, if the operator prefers an honest red stage over delayed wiring)* |
| 16 | 2026-08-15 | SD31-E2-F1-002 | NOTE | Re-labelling the 105 canned units and hand-labelling the 35-unit widening draw surfaced three further engine-classifier gaps beyond Findings A/B/C (rows 1/2): **Finding D** — `SPELLS:` fields (spell-like-ability grants) are in neither `MAGNITUDE_TOKENS` nor `prose_fields`, so a scalar-dependent formula stated only there (e.g. `bestiary_4:monster_ability:winter_hag_ice_staff`'s `SPELLS:Ice Staff\|CASTERLEVEL=10\|Cone of Cold,15+CHA`) is invisible to the scanner. **Finding E** — `PLUS:` fields (an equipment-modifier's equivalent-enhancement-bonus value) are likewise unscanned (`core_rulebook:equipment_modifier:special_ability_ghost_touch_armor`'s `PLUS:3`; corroborated by `ultimate_combat:equipment_modifier:special_ability_reliable_firearm`'s `PLUS:1`). **Finding F** — `ASPECT:` fields are unscanned too (`bestiary_5:monster_ability:chuspiki_air_blast`'s HD/CON-scalar damage formula), AND Finding B's false positive (row 2) is NOT always rescued by a separate genuine signal as row 2's 3-unit check suggested — 3 widening-batch units (`ultimate_equipment:equipment:belt_of_stoneskin`, `bestiary_2:monster:twigjack`, `horror_adventures:race:undead_phantom`) carry ONLY the false positive with no rescue, genuinely misclassified `derived` when they should be `static`; 2 more (`mythic_adventures:equipment:chaos_hammer`, `bestiary_3:race_trait:fuath_spell_like_abilities`) show the same false-positive shape in the PROSE scanner (`derived:prose_expr` firing on a parenthesised citation of another effect's fixed stats, e.g. `(CL 9th)`, rather than a real formula). | Hand-labelled all 8 affected units to their true class with full worked-example evidence in `SD31-E2-F1-ground-truth-methodology.md` §Findings D/E/F. No production code changed (out of this card's scope). | *(informational — feed into Epic 2-F2's classifier acceptance work alongside Findings A/B/C, not a blocker)* |

| 17 | 2026-08-15 | SD31-W2-INTEGRATE-001 | NOTE | Rows 11/12's headline `derived`/`ambiguous`-held figures do not reproduce and contradict the E6-F11 held-cell map's own per-kind table on the same page (adversarial review Findings 6/7): row 11 cites `2,777` `derived`-held and row 12 cites `2,704 of the 2,777`; the map's own per-kind table sums to 2,792, and the ambiguous-held population was published as 309 (should be 400), with `display`+`ambiguous` published as 1,552 (should be 1,643). Re-derived: `python3 -c "import sys,json,collections; sys.path.insert(0,'scripts/observer'); import pf1e_dashboard_producer as P; d=json.load(open('docs/work-inventory.json')); U=[u for u in d['units'] if u.get('book') not in P.EXCLUDED_BOOKS]; c=collections.Counter(u.get('wiring_class') for u in U if P.doneness_verdict(u.get('wiring_class'),u.get('status'),u.get('kind'))=='held'); print(dict(c))"` → `{'display': 1243, 'derived': 2792, 'ambiguous': 400, 'static': 2481}`, sum 6916 (matches the map's own `TOTAL_HELD`). This package had already established `derived`-held = 2,792 once before this cycle (`progress.md:188`/`:240`, retro correction `1786803206746-sd31-ready-s2-919f72`) — the E6-F11 cycle silently re-introduced the stale value. | Corrected the prose subtotals in `artifacts/SD31-E6-F11-001-held-cell-map.md` (2,777→2,792, 2,704→2,719, 309→400, 1,552→1,643) and `progress.md`'s SD31-E6-F11-001 receipt in place, with a visible strikethrough + correction note rather than silently editing history. Left rows 11/12's own text unedited per this file's append-only rule — this row is the correction. Percentages and every conclusion in the map are unaffected; this was a transcription/subtotal error, not a re-analysis. | *(informational — no ruling needed, purely a figure correction)* |

| 18 | 2026-08-15 | SD31-W2-INTEGRATE-001 | NOTE | `SD31-E1-F1-001`'s `reach_gate.rs` `UNREACHED_RECORD_FINDINGS` entry for the three unreached `Mostly Human ~ Ifrit/Sylph/Undine ~ Languages` records gave the wrong root cause (adversarial review Finding 8): it claimed "a real upstream content gap for the other three, not a resolver defect," inferred from Oread's sibling row reaching. That inference is backwards — PCGen ships a granter for all four races symmetrically (`isr_abilities_race.lst:650/651/652/653`, `Geneiekin ~ Mostly Human.MOD` rows each carrying `FACT:<Race>_ReplaceLanguages\|True` and its own `ABILITY:...\|AUTOMATIC\|Mostly Human ~ <Race> ~ Languages` grant); the gap is entirely project-side. The base row (`:649`, `Geneiekin ~ Mostly Human`) carries `TYPE:RacialTraits.SpecialQuality.Special Quality` with no race-scoped `TYPE` component, so `parse_row` never emits a `<Race> Racial Trait` classification target for it, and the per-race grants live only on the four `.MOD` rows, which `is_mod_row` deliberately excludes. `ingest_race_traits.rs`'s own test doc already names these exact 5 `.MOD` rows and says they were filtered "one step later" only because "those 5 name races this project does not model" — a clause this cycle's own Epic 1-F1-001 batch made false for 4 of the 5 (Ifrit/Sylph/Undine/Suli), making the record in-scope-adjacent. Oread's sibling row reaches purely by accident of a second, unrelated setter (`Oread ~ Isolated`). | Rewrote the `UNREACHED_RECORD_FINDINGS` comment and the `progress.md` `SD31-E1-F1-001` receipt paragraph with the true cause, preserving the original wrong text struck through per this program's correction convention. The numeric pin itself is unchanged and correct — the three records genuinely do not reach today, only the root-cause rationale was wrong. | *(informational — names a concrete Epic 1 follow-on: model the Geneiekin heritage the way Aasimar/Tiefling already are, same class of work as the already-deferred Dhampir/Skinwalker heritage build, row 13. Not dispatched this cycle.)* |

| 19 | 2026-08-15 | SD31-W2-INTEGRATE-001 | NOTE | Two SD31-E2-F2-001-wiringfix claims did not hold (adversarial review Findings 2/3): (a) the new-test count was stated three inconsistent ways (report summary "18 (12 lib, 1 integration)", commit message "18 (14 wiring_class.rs unit tests, 1 integration)", `Resolved` rows 1/2 below "6 tests"/"8 tests" = 14) — actual is `git diff c99461ac3..79c087240 -- src/rules_core/wiring_class.rs \| grep -c '^+.*#\[test\]'` → **12** (5 `corpus_lines_*` resolution tests + 7 `d3_*` signals tests), plus 1 real-corpus-gated integration test = **13** total; only `scripts/verify-baselines.env`'s own `+12 lib / +1 file` was correct. (b) The report's "movement confirmed both directions" caption overclaimed: the full transition matrix is 1,265 `derived`→`static`, 0 `static`→`derived`, 0 `static`→anything — movement is one-directional by construction, as expected of a false-positive fix; the "both directions" evidence actually shown is two unchanged-class regression guards, not a bidirectional transition. | Corrected the two count statements in `progress.md`'s `SD31-E2-F2-001-wiringfix` receipt in place (5+7=12 split, 13 total) and restated the movement caption honestly; left rows 1/2 below unedited per this file's append-only rule — this row is the correction. The genuine other-direction case Finding 1 asked for (a variable-magnitude row that must remain/become `derived`) is now covered by this cycle's own D4 regression tests. | *(informational — no ruling needed, purely a count/caption correction)* |

| 20 | 2026-08-15 | SD31-W2-INTEGRATE-001 | NOTE | Adversarial review Finding 11: `SD31-E2-F1-002`'s 35-unit widening draw was configured with `--target-per-cell 2`, which cannot, even in principle, clear the thin-cell bar the methodology itself defines (`n<=2`) — every cell the draw targets lands exactly on the threshold and stays "thin" by definition. Reproduced exactly: `python3 scripts/sample_ground_truth_units.py --inventory docs/work-inventory.json --exclude-ids-from <150 v1-draw-shaped JSON> --current-cell-counts <post-relabel hand_wiring_class:kind counts> --target-per-cell 2 --seed 31 --out redraw.json` → "drew 35 unit(s) across 28 cell(s)," with all 35 drawn ids byte-identical to the committed `widening_batch_v2`. The same inputs with `--target-per-cell 3` (the script's own default) draw 70 units across 36 cells instead. The methodology document had attributed the modest 31/45→29/48 thin-cell gain entirely to the hand-vs-engine axis mismatch (real, but not the binding constraint) without naming the flag value. | Corrected the root-cause attribution in `SD31-E2-F1-ground-truth-methodology.md`'s "Stratification actually achieved" section in place, verifying the exact reproduction commands above. Did not re-run the draw with a higher `--target-per-cell` value or hand-label the additional units — that is real labelling work (dozens of units at the same whole-record standard as the existing 185), out of this integration cycle's bounded scope. | *(informational — Epic 2-F2's next stratification pass should use `--target-per-cell 4` or higher, not repeat `--target-per-cell 2` and expect a different result)* |
| 21 | 2026-08-15 | SD31-W2-INTEGRATE-001 | NOTE | Adversarial review Finding 10: the ground-truth relabelling (both the 105-unit repair and the 35-unit widening draw) was not blinded — the labeller had the engine's `engine_wiring_class`/`engine_wiring_class_reason` in front of it for every unit while writing the label, so the sample is an AUDITED yardstick, not a genuinely independent one. The review could not convert this into any single demonstrably-wrong label (every label it re-derived from the corpus independently holds), so this is a methodological exposure, not a data defect — corroborated by a stark rate gap (relabelled-105 non-`no_corpus_line` disagreement 1.9% vs. the freshly-drawn widening batch's comparable rate 21.4%; `P(<=2 disagreements | p=0.214, n=105) ≈ 4.4e-9` under the null that the population is equally hard). | Recorded the exposure; did not build the blinding infrastructure (a `--blind` flag on `scripts/sample_ground_truth_units.py` that withholds `engine_wiring_class`/`engine_wiring_class_reason` from the labelling stub, joining the engine columns back only at scoring time) this cycle — that is new tooling plus a full re-label from blinded stubs, out of this integration cycle's bounded scope, and the existing 185-unit sample's labels are not shown to be wrong by this finding. | *(informational — Epic 2-F2's next sampling draw should add `--blind` to `scripts/sample_ground_truth_units.py` and label from the blinded stub, joining engine columns back only at scoring time; record in the methodology that the 185 units labelled so far were unblinded)* |
| 22 | 2026-08-15 | SD31-E6-F4-001 | RULING-NEEDED | **Root cause of `race` kind's 0.0 % done, traced one unit deep before any ingest work started.** The 7 CRB races (`core_essentials:race:dwarf` etc.) are `wiring_class=static`, `status=grounded`, `evidence="race_modelled_by_RaceId_ALL_and_reachable_in_a_real_receipt"` — genuinely reachable in a real receipt, one rung short of `done` only because `static`+`grounded` needs `literal-verified` (`corpus_literal_sweep`) to clear the last bar. `corpus_literal_sweep`'s own `--json-out` writer (`src/bin/corpus_literal_sweep.rs:267-276`) derives each verified triple's `"book"` field as `source_path.parent().file_name()` — the file's IMMEDIATE parent directory — not the 4-segment `book_dir_of()` grouping the rest of the binary uses. For a race chassis/trait row, the real PCGen path is `.../core_essentials/races/<race>/<race>_races.lst` (one directory level deeper than a flat book layout), so the emitted `"book"` is the RACE NAME (`"dwarf"`, `"aasimar"`, …), never `"core_essentials"`. `v06_work_inventory.rs`'s `apply_done_rung_stamps` (`src/bin/v06_work_inventory.rs:3772-3803`) joins `sweep_verified` on `(item.unit.book, file, line)`, so the join key never matches for ANY race chassis/trait row, and `literal-verified` can never stamp — regardless of how many races get a real chassis. Corroborated two ways: (a) direct: 0 of the CRB 7 races' `(book="core_essentials", file, line)` triples appear anywhere in the sweep's `verified` array (`grep`-equivalent: `python3 -c "..." ` over the `--json-out` report, 0 hits for `book=="core_essentials"`); (b) contrastive: `race_trait`'s own 5 `static`+`grounded` (held) rows are ALL under the identical nested `core_essentials/races/aasimar/aasimar_abilities_race.lst` shape, while the 4 `race_trait` units that DID reach `literal-verified` (`world_walker_skilled`, `deep_jungle_halfling_poison_use`, `junk_tinker_skilled`, `oversized_goblin_ability_scores`) are ALL filed FLAT, one level under their book (`advanced_race_guide/arg_abilities_race.lst`, etc.) — the shape, not the content, is what gates. Corpus-wide, **330 of 3,644 verified triples (9.1 %) carry a mis-attributed "book"** this way (25 distinct wrong values, all race directory names) — `python3 -c` counting `sweep['verified']` entries whose `book` is not a real `data/corpus/` top-level directory. `src/bin/corpus_literal_sweep.rs` is NOT in this card's file territory (shared, out-of-lane infrastructure every kind lane's `static` done-rung depends on) — did not edit it. | Documented the root cause with the exact line numbers, the corroborating positive/negative contrast, and the corpus-wide blast-radius count; did NOT edit `corpus_literal_sweep.rs` (out of territory) and did NOT weaken/route around the `literal-verified` bar to manufacture a `race`-kind `done` count (that would be exactly Decision 1(a)'s prohibited "loosen a check" move). Landed the Skinwalker chassis batch anyway (see `progress.md`) — its `race_trait` gains route entirely through the UNAFFECTED `computed`+`grounded` path, so this bug did not block real ingest work, only the `race` kind's own bar and a `race_trait` static residual. | *(needs a dispatch decision: fix `book_dir_of`'s json-out sibling in `corpus_literal_sweep.rs`'s `--json-out` writer — swap `source_path.parent().file_name()` for the same `book_dir_of()`-then-strip-to-short-name logic the binary's own comment at line ~262 says it SHOULD be using — to unblock up to 330 corpus-wide `static`+`grounded` units, including `race` kind's entire 7-unit `done` population, with no new evaluator code. Small, mechanical, well-understood fix; not this card's file territory.)* |
| 23 | 2026-08-15 | SD31-E6-F4-001 | NOTE | **Investigated `advanced_players_guide` as a `race_trait` ingest target and reverted after landing it once.** `docs/work-inventory.json` carries 49 of its rows as `evidence == "race_trait_absent_from_race_traits"`, which reads as a genuine 49-unit ingest gap. It is a measurement artifact, not a gap: a corpus-wide `data.key` join across every committed `race_trait` record found 49 of APG's 50 in-scope rows already ingested, byte-mechanically-identical (same `sets_replace_flags`, cosmetic wording only), under `advanced_race_guide` — Paizo's own later compilation reprints APG's alternate-race-trait system almost verbatim (e.g. `Dwarf ~ Ancient Enmity`, `arg_abilities_race.lst:33` vs `apg_abilities_race.lst:16`). Adding an `advanced_players_guide` `BookSource` to `ingest_race_traits.rs` and running it proved this directly: `race_resolver`'s own test suite (which builds ONE global `RaceCorpus` keyed by trait KEY, not book+key) failed immediately — `panicked ... Dwarf: duplicate resolved trait Dwarf ~ Ancient Enmity` plus a picker-population regression (379→330, the exact -49). **This is independently-confirmed, ALREADY-established program knowledge, not a new finding**: `src/bin/ingest_apg_race_traits.rs` (SD28-E16, 2026-08-08) already exists, already filters every APG row whose `KEY:` collides with ARG's on-disk key set, and already ships the one genuinely-new record (`Half-Orc ~ Plagueborn`); `reach_gate.rs`'s own `("apg", "race_traits")` comment states the same 49/50 figure. `docs/work-inventory.json`'s raw per-book enumeration has no way to know two books' rows are the same real trait, so its 49-unit "gap" for `advanced_players_guide` will read as open forever even though it is fully, correctly closed. | Reverted the `BookSource` addition and its pinned-count test edits (see `progress.md` for the exact commands); left `ingest_race_traits.rs`'s `BOOK_SOURCES` doc comment carrying the full worked derivation so a future cycle does not re-attempt the same investigation from scratch. No production code changed net of this cycle. | *(informational — no ruling needed; the existing `ingest_apg_race_traits.rs` mechanism is correct and complete for this book. Worth a future cycle auditing whether OTHER book pairs in the 3,447-unit `race_trait` population share this same reprint shape, since the raw-enumeration "not-ingested" figure cannot distinguish it from a real gap on its own.)* |
| 24 | 2026-08-15 | SD31-E6-F4-001 | NOTE | `cargo run --locked --bin v06_corpus_trap_report -- --audit` exits 2 (DoD item 3) at the current tip, entirely on `monster`/`companion` records this card never touched — `grep -c "race_trait\|/race/"` over the audit's own output → **0**. Every finding is the identical shape my own re-ingest already fixed for `race`/`race_trait` (a `derived`→`static` wiring-class mismatch the D3/D4 wiringfix corrects, surfaced only once the owning kind's ingest binary is re-run against the merged tip — `ultimate_psionics`' 11 `monster` records and `ultimate_wilderness`' 8 `companion` records, none in this card's `IN_SCOPE_RACES`/`BOOK_SOURCES` territory). | Ran the audit, confirmed by grep that this card's own kinds (`race`, `race_trait`) contribute zero findings, and did not touch `monster`/`companion` ingest binaries (out of file territory, and touching them risks collision with this wave's concurrent equipment/monster-ability/companion lanes). | *(informational — whichever lane owns `ultimate_psionics`'s monster ingest and `ultimate_wilderness`'s companion ingest should re-run its own binary against the merged tip, the same fix this cycle applied to `race`/`race_trait`; integration should not attribute this DoD-3 shortfall to `SD31-E6-F4-001`)* |
| 25 | 2026-08-15 | SD31-E6-F11-002 | NOTE | The card brief's (and the F11-001 map's) headline "1,229 `derived\|grounded\|monster` held units" is stale on this checkout. Re-derived fresh: `python3 -c "import json,sys,collections; sys.path.insert(0,'scripts/observer'); import pf1e_dashboard_producer as P; d=json.load(open('docs/work-inventory.json')); U=[u for u in d['units'] if u.get('book') not in P.EXCLUDED_BOOKS and u.get('kind')=='monster']; c=collections.Counter((P.doneness_verdict(u.get('wiring_class'),u.get('status'),'monster'),u.get('wiring_class'),u.get('status')) for u in U); print(dict(c))"` → `{('held','static','grounded'): 955, ('held','derived','grounded'): 280, ...}`. Root cause: `SD31-E2-F2-001-wiringfix`'s D3/D4 false-positive fixes (`wiring_class.rs`, commit `d07d41b5c`) landed a corpus-wide `derived`→`static` transition of 1,265 units (row 19) AFTER the F11-001 map was written from a snapshot that predates it — 955 of the old 1,229 `monster` units reclassified `static` (a `corpus_literal_sweep`-lane concern, not `derived_evaluator_fixture_check`'s) and only 280 remain genuinely `derived`. Independently corroborated by `v06_corpus_trap_report --audit`'s own `[wiring-class-mismatch]` findings, which flag several corpus JSON records' STORED `wiring_class` field (stamped at ingest time) as stale against a fresh classify (e.g. `Xeph`, `Psicrystal`) — the same drift, a different symptom. | Re-derived and used the corrected 280 figure for this cycle's own scoping; did not touch the corpus JSON's stale stored `wiring_class` fields (out of this card's file territory) or `SD31-E6-F11-001-held-cell-map.md`'s own text (that artifact is another cycle's, append-only per this file's convention — this row is the correction, not an edit to that file). `retro.py correction` emitted. | *(informational — a future wave should re-run `v06_corpus_trap_report --audit`'s `[wiring-class-mismatch]` findings to a fix, or accept the stored-field drift as permanently informational since `v06_work_inventory` already re-derives `wiring_class` fresh every run and never reads the stored field)* |
| 26 | 2026-08-15 | SD31-E6-F11-002 | RULING-NEEDED | Built the `derived_evaluator_fixture_check` evaluator seam for `kind=monster` (`spell_like_ability_caster_level()`, PF1's "Spell-Like Abilities" universal monster rule, caster level = Hit Dice, read off the `MONSTERCLASS:<type>:<HD>` token). Landed 7 fixtures (the `BONUS:VAR\|SLA_CL\|HD` bare-HD family). Of the 280 `derived\|grounded\|monster` units (row 22), **266 carry at least one field whose magnitude is expressed as a function of the creature's own ability score** (`ConstrictBonusDamage\|STR`, `WEAPONPROF=...\|DAMAGE\|max(0,STR/2)`, etc. — 192 of the 280 are exactly this shape). This repo's monster ingest is `completeness: "chassis_only"` (every corpus JSON confirmed, e.g. `data/corpus/beastiary/monster/demon_balor.json`) and carries **no ability-score field at all** — `MonsterStatBlock` has no STR/DEX/CON/INT/WIS/CHA. A fixture for these 266 cannot be hand-derived without either (a) fabricating an ability score not in the corpus (forbidden, Decision 1(a)/no-stub doctrine), or (b) widening monster ingest to capture the `BONUS:STAT\|<ability>\|<int>` rows every monster's `.lst` line already carries (confirmed present on every sampled row, e.g. Demon (Balor) carries `BONUS:STAT\|STR\|24`) into `MonsterStatBlock` and the corpus JSON. The remaining 7 `no-ability-score` units (`SLA_CL\|max(TL,1)`, `SLA_CL\|HD-3`, `SR:10+TL`, `VerminEmpathyLVL\|CL`, `SLA_CL\|HD*3/4`) need only a slightly richer parser (the `max()`/arithmetic wrapper this cycle's bare-`HD` parser deliberately does not accept) or a distinct rule (`SR = 10+TL`, a different Universal Monster Rule), not new corpus data — a same-shaped follow-on card. Full per-unit breakdown, worked examples and the scale plan: `progress.md`'s `SD31-E6-F11-002` receipt. | Did not widen `MonsterStatBlock`/monster ingest to add ability scores (real, multi-book engine + ingest work, outside this fixture-seam card's file territory and this cycle's budget) and did not fabricate ability-score values to fixture-cover the 266. Landed only the 7 units genuinely coverable from data already in the corpus. | *(needs a dispatch decision: a follow-on Epic 6-F1/F11 card to (a) widen monster ingest to capture `BONUS:STAT` ability scores into `MonsterStatBlock` — unblocks the 266-unit majority, real ingest-widening work spanning ~12 books' worth of already-registered monster tables — and (b) extend `spell_like_ability_caster_level`'s parser to the `max(TL,1)`/arithmetic-wrapped and `SR:10+TL` shapes — unblocks 7 more units with no new corpus data needed. Neither is a fixture-file-only card.)* |
| 27 | 2026-08-15 | SD31-E6-F11-002 | RULING-NEEDED | **DoD item 3 for this cycle** (`cargo run --locked --bin v06_corpus_trap_report -- --audit` exits 0) **is red**, for a reason confirmed pre-existing and unrelated to this cycle's own diff, not a defect this cycle introduced. Full run: `TRAP DEFECT trap: 259 0 mod-record; 0 1040 wiring-class-mismatch`, exit `2`. Every one of the 1,040 `[wiring-class-mismatch]` findings is the SAME root cause as row 22 (`SD31-E2-F2-001-wiringfix`'s D3/D4 classifier fix, commit `d07d41b5c`, landed a corpus-wide 1,265-unit `derived`→`static` transition) — the trap-report tool compares each corpus JSON record's STORED `wiring_class` field (stamped at ingest time, before the fix) against a fresh classify of its real `.lst` line, corpus-wide, and 1,040 records' stored stamps are stale. Confirmed unrelated to this cycle: `grep -c` for this cycle's 7 committed `unit_id`s in the audit output → **0** — none of `demon_balor`/`linnorm_crag`/`linnorm_ice`/`linnorm_tarn`/`demon_brimorak`/`demon_seraptis`/`demon_vavakia` appear; this cycle's diff (`src/rules_core/derived_evaluator_fixture_check.rs`, two `tests/*.rs` files, the fixture JSON) touches no corpus JSON record's `wiring_class` field at all, so the finding set is identical with or without this cycle's changes. `./scripts/verify.sh`'s own gate does NOT run this check as a stage (confirmed: `grep -n trap_report scripts/verify.sh` finds only a comment, no stage) — the main gate is unaffected. | Did not attempt to fix the 1,040 stale stamps (regenerating `data/corpus/**/*.json`'s `wiring_class` field corpus-wide is a data-mutation task this card's file territory does not cover, and this program has already hit the "generated artifacts mutated post-hoc" PI-exposure hazard once — not a same-cycle fix). Recorded the exact command/exit code per unattended-mode protocol and continued with the rest of this card's bounded work. | *(needs a dispatch decision: a dedicated "corpus wiring_class stamp refresh" cycle should re-stamp every corpus JSON record's `wiring_class`/`wiring_class_signals` field from the CURRENT classifier — a corpus-wide regen, needs its own PI-exposure review before landing, per `docs/retro/` "Generated artifacts mutated post-hoc" precedent — separate from any fixture-coverage or ingest-widening card)* |
| 28 | 2026-08-15 | SD31-E6-F11-002 | NOTE | **Near-miss, self-caught: `scripts/derive_derived_evaluator_fixtures.py` (the generator behind `entries`, the equipment half of `derived-evaluator-fixtures.json`) now derives only 11 fresh candidates against this checkout's corpus, not the 94 committed — confirmed by running it for real (`python3 scripts/derive_derived_evaluator_fixtures.py`, no flags) and by its own `--report` mode, both same-session.** Root cause is the SAME transition as rows 22/24 (`SD31-E2-F2-001-wiringfix`'s D3/D4 fix): cross-checking all 94 committed `entries` unit_ids against `docs/work-inventory.json`'s CURRENT `wiring_class` — `python3 -c "import json; fx=json.load(open('tests/fixtures/rules_core/derived-evaluator-fixtures.json')); wi=json.load(open('docs/work-inventory.json')); by_id={u['id']:u for u in wi['units']}; sd=sum(1 for e in fx['entries'] if by_id[e['unit_id']]['wiring_class']=='derived'); print(sd, len(fx['entries'])-sd)"` → **11 still `derived`, 83 now some other class (chiefly `static`)**. This cycle's `derived_evaluator_fixture_check` binary report ("56 of 101 covered units cleared") is still an ACCURATE report of what it checked, but "101 covered" no longer means "101 currently-`derived`-held units" — only 11 (equipment) + 7 (this cycle's new monster entries) = 18 of the fixture file's 101 total rows are load-bearing against the LIVE `docs/work-inventory.json`; the other 83 are harmless but inert (the `v06_work_inventory.rs` stamp path gates on `wiring_class == Derived` before ever consulting fixture membership, so a stale equipment entry for a now-`static` unit changes nothing — confirmed no functional defect, just a stale headline count). **Genuinely almost destroyed real coverage this cycle:** running the generator for real (not `--report`) overwrote the committed file down to 11 equipment entries AND, before this cycle's own preserve-fix (below), would have silently dropped the new `monster_entries` array too. Caught immediately by diffing against a pre-run backup; restored via `cp` before it was ever committed. | Restored the fixture file from a pre-run backup before committing (verified byte-identical to the pre-run state, `git diff --stat` shows the same pure 96-insertion delta as before this investigation). Patched `scripts/derive_derived_evaluator_fixtures.py` to preserve any existing `monster_*`/`monster_entries` keys across a re-run (it previously rebuilt the whole document from scratch, which would have silently deleted this cycle's monster fixtures on the next unrelated equipment-lane re-run) — a real, scoped fix, committed. Did NOT investigate or fix the 83-entry equipment-lane shrinkage itself (out of this card's `kind=monster` scope; the equipment `entries` array's own currency is Epic 6-F5's/the original F1 equipment lane's concern) and did NOT re-run the generator again after restoring (the committed 94 stay exactly as they were). | *(informational for the equipment lane: a future Epic 6-F5/F1 cycle should decide whether to prune the 83 now-inert equipment entries, re-derive a fresh 11-or-more-entry set, or leave them — none of the three changes correctness today, only headline-count honesty. NOT a monster-lane or this card's action item.)* |
| 29 | 2026-08-15 | SD31-E6-F5-001 | NOTE | Row 12's "~60 units" ultimate_equipment estimate held on re-derivation (0 `data/corpus/ultimate_equipment/` before this cycle, confirmed via `ls data/corpus/ \| grep ultimate` and `find $PCGEN_CORPUS_ROOT -iname '*ultimate_equipment*' -maxdepth 5 -type d`) but badly undercounted the real lever: onboarding the book (`cache_gen::ultimate_equipment` dumping the already-shipped `rules_tables::ultimate_equipment::equipment_tables`/`equipmod_tables` — 1,369 + 180 records — into `data/corpus/ultimate_equipment/equipment/*.json`, then `enrich_equipment_raw_tokens`, then widening `OBSERVABLE_BOOK_DIRS`) moved **1,264 units to `done` board-wide** (equipment 2,650→3,908, equipment_modifier 911→917), because every one of the 1,549 new records also newly satisfies `corpus_literal_sweep`'s `static` literal-verified done-bar, not only the narrow `derived` BONUS:STAT population row 12 named. Board `done` 6,076→7,340 (15.77%→19.05%) in this lane alone. | Re-derived the full before/after doneness-verdict replay (dashboard producer's own `doneness_verdict`, `git show HEAD:docs/work-inventory.json` vs. local guarded regen) rather than trusting the row-12 figure; filed the correction via `retro.py correction --verified-by`. | *(informational — corrects row 12's own headline number; no ruling needed)* |
| 30 | 2026-08-15 | SD31-E6-F5-001 | NOTE | `src/pcgen_import/lst_parser/equipment.rs`'s `open_record` merges a `.COPY=` row that carries no `KEY:` token into an EARLIER entry sharing the same *extracted base name* (`extract_record_name` strips at `.COPY=`, so `"Bastard Sword (Base).COPY=Valor's Minion"` and `"Bastard Sword (Base).COPY=Bastard's Sting"` both extract to `"Bastard Sword (Base)"` and can collide). For 2 of `ultimate_equipment`'s 1,549 newly-cited records (`Bastard's Sting`, a `.COPY=` row whose real citation line 447 is correct per `find_copy_variant`; `Mountain Pattern Armor`, which the raw corpus genuinely declares TWICE at lines 16 and 46 with divergent SOURCE tokens) `enrich_equipment_raw_tokens`'s re-parse silently attached a *different* record's `EQMOD`/`SOURCELONG`/`SOURCESHORT`/`SOURCEPAGE` tokens onto the JSON — caught by `corpus_literal_sweep` (4 findings, "token not byte-present in corpus token closure"), not by the enrichment tool itself (which reported these as ordinary `Enriched`, no warning). | Reverted `raw_tokens`/`raw_bonus_chains` to absent on just these 2 records (the same honest thin-fallback `corpus_loader.rs::equipment_record_from_json` already defines for any record with no clean raw citation) rather than shipping wrong mechanical data or hand-patching the shared parser's merge heuristic — that heuristic is depended on corpus-wide (every book's equipment cache, not only UE's), a blast radius out of this bounded card's scope. Corpus-wide, `corpus_literal_sweep` now reports 0 findings again. | *(needs a dispatch decision, not an operator ruling: a dedicated fix to `open_record`'s merge predicate — disambiguate same-extracted-name `.COPY=` rows by their own line/target rather than folding them into the first same-named entry — would very likely surface more of this class corpus-wide; `git grep -c '\.COPY=' $PCGEN_CORPUS_ROOT/pathfinder/paizo/roleplaying_game/*/*.lst` shows the pattern is not UE-specific, but `corpus_literal_sweep` corpus-wide reported only these 2 hits today, so the exposure is small until a book with more KEY-less same-base-name `.COPY=` rows is ingested.)* |
| 31 | 2026-08-15 | SD31-E6-F5-001 | NOTE | `v06_corpus_trap_report --audit` flags `[shared-name-distinct-records]` for `data/corpus/ultimate_equipment/equipment/masterwork_tool.json` vs. `masterwork_tool-2.json`: both carry the literal `key`/`record_key` `"Masterwork Tool"`, but they are genuinely two different real PCGen records — a General-category consumable item (`ue_equip_general.lst:277`, `COST:50`) and an Equipmods-category skill-bonus modifier (`ue_equipmods.lst:350`, `CHOOSE:SKILLBONUS`) — disambiguated correctly by `category` and by file (the generator's `slugify` collision-suffix already keeps them as two on-disk files). `--audit` still exits `0` (informational finding, not `Severity::Defect`). | Verified both records independently against the raw corpus (both real, distinct, correctly-shaped); did not change the generator or `equipment_resolver`'s existing "first match wins" key-collision handling, which already documents this exact shared-name-not-shared-item pattern for other books (`ACG`/CRB precedent). | *(informational — no action needed; matches an already-accepted corpus shape)* |
| 32 | 2026-08-15 | SD31-E6-F2-001 | NOTE | Traced `spell`'s only path to `done` before ingesting: `wiring_class` (`static`/`derived`/`computed`) is derived live off the raw `.lst` row, independently of `wiring_class == Derived + fixture-verified` (`derived_evaluator_fixture_check`, out of this card's scope per its own file-territory note) and `wiring_class == Computed + status == grounded` (the already-wired `probe_spell_key` consumer-delta probe, `v06_work_inventory.rs:2105` — grounds via `facts.spell_levels`/`spell_effect_wired`, both keyed to the FIVE books `spell_resolver::spell_catalog_rows()` chains: CRB/APG/ACG/ARG/UI, `v06_work_inventory.rs:83-121`). Re-derived which of `spell`'s 1,561 corpus-wide `not-started` units sit inside those five books at all: `python3 -c "import json,sys; sys.path.insert(0,'scripts/observer'); import pf1e_dashboard_producer as P; d=json.load(open('docs/work-inventory.json')); FIVE={'core_rulebook','advanced_players_guide','advanced_class_guide','advanced_race_guide','ultimate_intrigue'}; U=[u for u in d['units'] if u.get('kind')=='spell']; import collections; print(collections.Counter((u['book'] in FIVE, P.doneness_verdict(u['wiring_class'],u['status'],'spell')) for u in U))"` → **13 of 1,561 not-started spell units sit inside the five modeled books; the other 1,548 sit in books the engine's spell catalog does not chain at all and cannot reach `done` through any existing instrument, ingest or otherwise, until a sixth `SPELL_LIST` module is built.** Read all 13: every one is a PCGen `.COPY=` restricted-use spell variant (`CLASSES:.CLEARALL`, e.g. `core_rulebook:spell:Animate Objects (Small or Smaller)`, `cr_spells.lst:1467`) that carries no `SCHOOL:`/`CLASSES:` of its own — the level a genuine ingest would need to state is the one thing the corpus explicitly declines to state for these rows (that is what `CLEARALL` means), and `crb::spell_list::SpellListEntry.level` is a non-optional `u8` (`src/rules_core/rules_tables/crb/spell_list.rs:82`), so adding these 13 without inventing a level would be exactly the "no invented numbers" violation this program's doctrine bars. | Did not ingest these 13 — the corpus genuinely states no level for them and CRB's own table type cannot represent "unknown." Pivoted the cycle's real ingest lever to the `held` mass instead (row 23) rather than force this 13-unit `not-started` slice. | *(informational — a future cycle could model these 13 as `Option<u8>`-level rows IF a domain ruling establishes what level a `.CLEARALL` copy-variant should carry when granted via non-class means; until then this is correctly `not-started`, not a gap in this tool)* |
| 33 | 2026-08-15 | SD31-E6-F2-001 | NOTE | `spell`'s real ingest lever this cycle turned out to be the `held` mass, not `not-started`: `wiring_class == Static` units already `ingested-magnitude`/`grounded`/`text-complete` reach `done` only via a clean `corpus_literal_sweep` match on `(book, source_file, source_line)` (`v06_work_inventory.rs::apply_done_rung_stamps`). Built `src/bin/enrich_spell_raw_tokens.rs` (reuses `corpus_literal_sweep`'s own `tab_tokens`/`token_closure`, never a re-implementation) to populate the missing `data.raw_tokens` on 1,173 spell corpus JSON records across the five modeled books; re-swept CLEAN (0 findings, 3,635→4,808 records examined). Measured against the live inventory: only **9 of the 120 `static`-held units within the five books actually promoted to `literal-verified`/`done`** (`spell` `done` 47→56). Root-caused the 111-unit shortfall by joining each residual unit's own `(book, name)` to its corpus JSON's `source.line` and comparing against the unit's own `provenance.line`: `python3 -c "..."` (script in this cycle's `progress.md` receipt) → of the 120, **9 match, 101 mismatch, 10 have no findable JSON record at all.** Worked example: `advanced_players_guide:spell:accelerate_poison`'s unit citation is `apg_spells.lst:17` (the real base declaration, carrying `SCHOOL:`/`CLASSES:`/etc.), but its shipped corpus JSON's `source.line` is `1842` — `Accelerate Poison.MOD`, a row that overrides ONLY `DESC:`. The enriched tokens at line 1842 are byte-correct (the sweep proves it, cleanly), but `apply_done_rung_stamps` matches on the unit's OWN provenance line (17), which the JSON never claims, so the stamp never lands even though nothing about the transcription is wrong. This is a pre-existing defect in the ORIGINAL spell ingest pipeline (dated `2026-08-03` per these records' own `ingested_at`), not introduced by this tool or this cycle, and — separately confirmed while investigating — the same records' cached `data.wiring_class` field is independently stale against the live classifier (`accelerate_poison.json` still reads `"wiring_class": "display"`; the live computation for the same unit reads `static`), evidence the original ingest's cached fields have drifted corpus-wide and neither this tool nor `v06_work_inventory` ever trusted them (both re-derive from the raw `.lst` and the citation, never from the cached field). | Did not build a citation-repair tool this cycle (real, separately-scoped work: re-resolve each mismatched record's `source.line` to its true base declaration, which requires the SAME base-row-vs-`.MOD`-row resolution `wiring_class::resolve_corpus_file`/`token_closure` already implement, applied to REWRITE `source.line`/`source.path` rather than just read them — a correctness-critical rewrite this cycle's turn budget did not allow time to build AND verify to the standard a citation change deserves). Landed the 9 genuine, byte-verified promotions and reported the shortfall honestly rather than silently under-claiming ("some units moved") or over-claiming ("120 units enriched" without saying only 9 reached `done`). | *(needs a dispatch decision: a citation-repair cycle re-pointing the ~101 mismatched `source.line`s at their true base rows would likely promote most of the remaining static-held spell units to `done` with no further engine changes — the single highest-leverage lever this cycle found for `spell`, on the same shape as `OPEN-ISSUES.md` row 11's equipment-provenance finding)* |
| 34 | 2026-08-15 | SD31-E6-F2-001 | NOTE | Traced `monster_ability`'s only path to `done`: `classify()`'s `Kind::MonsterAbility` arm (`v06_work_inventory.rs:3262`) grounds a `not-ingested` unit purely by membership — `facts.chassis_monster_ability_keys[engine_book]` (built from `monster_chassis::MONSTER_BOOKS[*].monster_abilities`, `v06_work_inventory.rs:2772`) containing the record's key/name — with NO consumer-delta probe. Re-derived which books that registry actually covers and found TWO independent problems before writing any table row: **(1) Corpus-shape misclassification, the exact hazard this card's own instructions name.** `advanced_class_guide`'s 106 `monster_ability` units and `core_essentials`'s 380 all come from `_abilities_race.lst` files (`acg_abilities_race.lst`, `ce_abilities_race.lst`) whose own content is per-RACE ability options (`acg_abilities_race.lst` opens with `# Dwarf`; sample keys read `Elf Hunter Critical Confirmation Choice ~ Elven Curve Blade`, `Aeon ~ Envisaging`, `Aberration Traits Output`) — `file_kind()` correctly types these files `Kind::RaceTrait` (`v06_work_inventory.rs:229-240`), but `refine_kind()`'s `MONSTER_ABILITY_TYPE_FACETS` re-routes ANY row whose `TYPE:` first segment is `SpecialQuality`/`SpecialAttack` to `Kind::MonsterAbility` regardless of whether the owning creature is a monster (`v06_work_inventory.rs:634-676`) — and PCGen uses `SpecialQuality` as a generic label on ordinary player-race traits too, not only monster abilities. `python3 -c "import json,collections; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x['kind']=='monster_ability' and x['book'] in ('advanced_class_guide','core_essentials')]; print(len(u), collections.Counter(x['source_file'] for x in u))"` → 486 units, 100% from the two `_abilities_race.lst` files named above; that is 486 of the kind's 2,773 not-done units (17.5%) potentially mis-scoped as `monster_ability` when they are `race_trait`-shaped content. **(2) Player-reachability (the "twin problem"), even for the genuinely-real-bestiary books.** Spot-checked `bestiary`(=Bestiary 1)'s 57 `computed`\|`not-started` units, `bestiary_2`'s 23: every sampled ability's owning monster (`Air Mephit`, `Acid Draconal`, `Black Scorpion`, `Cloud Dragon`, `Giant Tarantula`, …) is ABSENT from that book's own `monsters_static()` table (`grep -c "Acid Draconal" src/rules_core/rules_tables/bestiary_2/monster_data.rs` → 0) and absent from every already-modeled monster's `external_ability_refs` too (the field that names a real cross-reference to an ability this book does not yet define) — these abilities are genuinely orphaned in the shipped chassis: no modeled monster grants them, so no player-visible surface can ever show one even if the `MonsterAbilityRecord` were added, which is exactly the DoD-8 "twin nothing renders" failure this program has caught three times before, not a new instrument gap. | Did not write any `MonsterAbilityRecord` entries this cycle — problem (1) means a meaningful share of the kind's "not-done" population is arguably not `monster_ability` at all (out of my card's authority to reclassify; `refine_kind`/`MONSTER_ABILITY_TYPE_FACETS` is shared, cross-kind classifier logic, not this card's file territory), and problem (2) means the remaining genuinely-real-bestiary candidates I sampled would fail `reach_gate`'s player-visibility bar even if hand-added, which the DoD explicitly treats as not-acceptance-evidence. Read `gen_book_cache.rs`'s existing `MonsterBookSpec`/`gen_monster_book` generator infrastructure in detail as the safer alternative to hand-authoring records (it already exists, is deterministic, and is what produced every currently-registered book's table) but did NOT run it against a live book this cycle after an accidental early `--dry-run` invocation (not a real flag; silently ignored) regenerated and rewrote 604 already-committed `beastiary` corpus files with a live wiring_class drift in them — reverted immediately (`git checkout -- data/corpus/beastiary/`), confirmed clean, and logged here rather than risking a second unreviewed whole-book regen this cycle. | *(needs an Epic-1/Epic-2-level ruling, not a card-level fix: (a) should `refine_kind`'s `MONSTER_ABILITY_TYPE_FACETS` require the OWNING file to also carry a genuine monster-race marker before reclassifying a `_abilities_race.lst` row out of `RaceTrait`, closing the same class of defect `OPEN-ISSUES.md`'s clockwork-companion precedent already fixed for `race_trait` itself; (b) should a `monster_ability` whose owning monster is unmodeled in its OWN book be countable toward this kind's 100% denominator at all, or does it wait on that monster's own `Kind::Monster` ingest (Epic 6-F1) first — the same "different lever" boundary Epic 6-F9's own card text already draws for the 981-unit `display\|grounded` cell. Neither can be decided inside this card's file territory.)* |
| 35 | 2026-08-15 | SD31-E2-F3-001 | NOTE | Investigated `OPEN-ISSUES.md` row 16's Finding E (`PLUS:` field unscanned) as a candidate classifier fix. `PLUS:` values are corpus-wide always a bare integer 1-10 (`grep -rhoE '\tPLUS:[^\t]*' $PCGEN_CORPUS_ROOT/pathfinder/paizo/roleplaying_game/*/[a-z]*.lst \| grep -v PLUSTOTAL \| sed 's/^\tPLUS://' \| sort -u \| wc -l` → 10 distinct values, all bare ints), so adding it never signals `derived` on its own — BUT it interacts structurally with the ALREADY-LANDED `computed:pre_guard` rule: nearly every `*_equipmods.lst` "special ability" record ALSO carries a `PRETYPE`/`PREMULT` item-type-compatibility guard (e.g. `PREMULT:2,[PRETYPE:1,ArmorEnhancement,ShieldEnhancement],[PRETYPE:1,Armor,Shield,Bracer]` on `core_rulebook:equipment_modifier:special_ability_ghost_touch_armor`) — a prerequisite for WHICH ITEM TYPE the ability can attach to, not a condition on the `PLUS:` value itself varying. Locally regenerating the guarded work-inventory with `PLUS:` added (measured via `--stdout-only`, never committed) moved **257 `equipment_modifier` + 5 `feat` + 2 `equipment` = 264 units from `done` to `computed`+`in-progress`** — board `done` 6076→5812 (-264), far beyond the 2 named examples' scope. This is the SAME unresolved "guard scoped to record eligibility vs. the magnitude itself" design question `SD31-E2-F1-ground-truth-methodology.md`'s "Judgement calls on ambiguous records" item 2 already flags (`core_essentials:race:changeling`'s `PREGENDER:F`), now shown to be structural at ~250-record scale, not a per-record judgement call. | Did **NOT** add `PLUS:` to `MAGNITUDE_TOKENS` — reverted the change after measuring its blast radius (kept the small, independently-validated `SPELLS:`/`classlevel(...)`/`+(`-paren fixes, which do not touch this design question). Shipping a ~264-unit sweep resting on an unresolved definitional gray zone, without validating each affected record individually, is exactly the over-shoot shape `SD31-W2-INTEGRATE-001` Finding 1 already found and fixed once this wave — the DEFAULT ASSUMPTION on a disagreement this large is that the change is wrong until proven otherwise, and this cycle could not clear that bar for all ~264 records in its remaining budget. | *(needs an operator/Epic-2 ruling: is a `PRETYPE`/`PREMULT` item-type-eligibility guard "the guard `D2` means" (→ these records genuinely are `computed`, ship `PLUS:` as scanned) or "eligibility for the record, not for the magnitude" (→ they are `static`, and `D2`'s computed rule needs an eligibility-vs-magnitude guard-scope distinction before `PLUS:`/similar additions can ship safely)? The same ruling would also resolve the `signals()` `computed:pre_guard` rule's general scope, beyond just `PLUS:`.)* |
| 36 | 2026-08-15 | SD31-E2-F3-001 | RULING-NEEDED | Structural Exclusion Register proposal, per `decisions.md §3`, for the `ambiguous:prose_scaling_phrase` / `ambiguous:prose_ability_scaling` population — **404 of the 409 pre-cycle `ambiguous` units, 99% of the bucket** (re-derived: `python3 -c "import json,collections; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x.get('book')!='beginner_box']; amb=[x for x in u if x.get('wiring_class')=='ambiguous']; print(len(amb), collections.Counter(x.get('wiring_class_reason') for x in amb))"` → 409, `{'prose_scaling_phrase': 297, 'prose_ability_scaling': 112}`; this cycle's `SPELLS:`/`classlevel`/`+(`-paren fixes moved 5 units off this bucket, leaving 404). **(1) Proving command:** `python3 scripts/reachability_audit.py` → 9 dead-end cells, all `ambiguous\|<status>`, `ambiguous_wiring_class_units: 404` after this cycle's fixes (re-run, both before at 409 and after at 404 — see receipt). **(2) Missing capability, and why it is impossible not merely expensive:** these units state a genuine magnitude formula ONLY in free-form English prose (e.g. `core_rulebook:equipment:scroll_of_fireball`'s "1d6 damage per level", `advanced_class_guide:class_feature:cult_leader_class_skills`'s "4 + Int modifier") with NO PCGen token (`BONUS:`/`DEFINE:`/etc.) backing it anywhere in the record's token closure — verified per-unit against 17 real corpus rows spanning 6 kinds in the ground-truth sample (`SD31-E2-F1-ground-truth-sample-v1.json`, all 17 `hand_wiring_class: ambiguous`, `token_evidence` quoting the real prose). Resolving these mechanically would require either (a) a general-purpose natural-language magnitude extractor capable of correctly distinguishing "this sentence GRANTS a new formula" from "this sentence REFERENCES a mechanic computed elsewhere" across unbounded English phrasing, which is a different KIND of problem than pattern-matching a finite token grammar (the taxonomy's own design explicitly treats this as its documented 5th class, per the GE-01 doc's "Accursed" worked example — see `SD31-E2-F1-ground-truth-methodology.md`'s "Judgement calls" section), or (b) a human reading each record and manually wiring its magnitude into the engine, which is real content work, not a classifier fix, and is out of `wiring_class.rs`'s file territory this wave. **(3) Epic 0 audit reproduction:** `docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E2-F3-001-audit.json` (this cycle's own run, committed alongside this row). **(4) Operator sign-off:** _blank — a cycle may propose, not grant._ **This cycle's own recommendation, NOT a substitute for the sign-off:** exclusion is probably the WRONG remedy here, because these units are not permanently unfixable — they need either (b) above (real content work, tracked elsewhere) or a NEW done-bar for `ambiguous` content in `doneness_verdict` (`scripts/observer/pf1e_dashboard_producer.py`, a file this card does not own) analogous to `static`/`derived`'s `literal-verified`/`fixture-verified` rungs — e.g. "`ambiguous` + a human-reviewed manual-wiring stamp = `done`". Permanently excluding them from the denominator would be premature; the operator should weigh that option against signing this exclusion. | Ran the reachability-audit re-derivation and drafted the four-item proposal per `decisions.md §3`'s exact template rather than skip it, while flagging in the proposal itself that exclusion may not be the right remedy — logged both so the operator has everything needed to decide either way. | *(operator: sign this exclusion, OR rule that Epic 2/whichever epic owns `pf1e_dashboard_producer.py` should add an `ambiguous` done-bar instead, OR dispatch a content-wiring card for a named subset)* |
| 37 | 2026-08-15 | SD31-E2-F3-001 | NOTE | AT-31-010 (Epic 2's acceptance widened 2026-08-15 to bind the `wiring_class == display, status == grounded` population): re-derived the population at **1,363** units (`python3 -c "import json; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x.get('book')!='beginner_box']; print(len([x for x in u if x.get('wiring_class')=='display' and x.get('status')=='grounded']))"` → 1363), correcting the epic-breakdown.md-cited ~1,243 figure (drift since that figure was last measured, likely from Epic 1's race-chassis batch landing between then and now). Ran the ground-truth sample's own `display_grounded_target` oversample (40 units drawn specifically from this population, `SD31-E2-F1-ground-truth-sample-v1.json`) against it: **39/40 hand-labelled `display`, confirming the engine's classification is correct** for this contradiction-shaped population (a real consumer-delta was observed, but the record genuinely carries no computable magnitude of its own — the two are not mutually exclusive; see `pf1e_dashboard_producer.py`'s own `display`+`grounded`→`held` rationale). The 1 disagreement, `bestiary_4:monster_ability:winter_hag_ice_staff`, was Finding D — this cycle's own `SPELLS:` fix — and is now FIXED (`display`→`derived`, confirmed in this cycle's regen). | Ran the sample against the population as AT-31-010 requires; the outcome is "examined, correctly classified, left alone" for 39/40 per Decision 1(e) item 4, with the 1 exception fixed as part of this cycle's own classifier work (not left open). No further code change needed for this bullet. | *(informational — AT-31-010 satisfied for this cycle's scope; a future cycle drawing a LARGER sample from the 1,363 population could tighten the confidence interval further, but 39/40 across a targeted 40-unit oversample is already strong evidence)* |
| 38 | 2026-08-15 | SD31-W3-INTEGRATE-001 | RULING-NEEDED | Adversarial review CONFIRMED 2 shipped `ultimate_equipment` records ship their name unredacted despite the corpus row declaring `NAMEISPI:YES`: `ue_equip_arms_armor.lst:66` "Otyugh Hide" and `:129` "Elysian Shield" — `src/rules_core/cache_gen/ultimate_equipment.rs` computes `DeclaredProductIdentity.name` via `declared_pi_at` but only ever passes `declared.description` into `pi_screening::classify_optional_field_declared`; the `.name` half is silently discarded. `pi_screening.rs`'s own doc comment on `DeclaredProductIdentity` is explicit this needs an operator ruling: "A name cannot be redacted ... the only way not to publish it is not to publish the row. See `docs/governance/ogl-pi-blacklist.md` §3 for the per-book override ... which is an operator decision." No PI actually leaked today (both records' names/descriptions contain 0 `PI_BLACKLIST_TERMS` hits), and this pattern is systemic — every corpus writer in the repo screens only `description`, confirmed via `grep -rn 'classify_optional_field_declared\|classify_field'` across `src/`. | Corrected the equipment-lane receipt's overclaim ("both SD-30 PI invocation contracts called per record" → the blacklist half is wired and clean, the declared-PI-name half is not); did not invent a redaction policy for `name` (that is explicitly the operator's call per the doctrine, and this program's own precedent is not to guess policy under unattended mode). | *(needs an operator ruling per `ogl-pi-blacklist.md` §3: either grant a per-book override for these 2 records, or direct `cache_gen::ultimate_equipment.rs` to flag `NAMEISPI:YES` rows — e.g. `pi_field: "name"`, a build-time warning — pending that ruling. Repo-wide follow-up: make `DeclaredProductIdentity.name` impossible to silently drop, e.g. `#[must_use]` on a screening result that consumes both halves.)* |
| 39 | 2026-08-15 | SD31-W3-INTEGRATE-001 | RULING-NEEDED | Adversarial review CONFIRMED `data/corpus/bestiary_5/LICENSE.json`'s race-lane UPDATE note falsely claimed the Skinwalker race/race_trait records (10 total) were "screened by ... the declared-PI reader" — `src/bin/ingest_races.rs`'s two record writers (the ones that wrote all 10) hardcode `pi_field: None` unconditionally and never call `pi_screening::declared_product_identity`; that reader is wired only in the separate `ingest_race_traits.rs` binary, untouched by this batch. No PI leaked today (`skinwalker_abilities_race*.lst` carries 0 `DESCISPI:`/`NAMEISPI:` tokens), but this matters going forward: the deferred Skinwalker heritage batch (`skinwalker_abilities_race_subrace.lst`) DOES carry real `DESCISPI:YES` declarations (Werebear-Kin, Wereboar-Kin, Werecrocodile-Kin, Weretiger-Kin, others) and would ship through this same unwired writer. | Corrected the LICENSE.json note in place (struck the false claim, kept the true blacklist-sweep claim, named the exact gap and its precedent risk) rather than leave it standing as the precedent for the heritage batch. Did not wire `pi_screening::declared_product_identity` into `ingest_races.rs`'s writers this cycle (moderate-size change touching a shared binary another lane may be mid-edit on; safer deferred to the batch that actually needs it). | *(needs a dispatch decision, blocking before the Skinwalker heritage batch: wire `pi_screening::declared_product_identity` into `ingest_races.rs`'s race and race_trait writers the way `ingest_race_traits.rs` already does — small, mechanical, same shape as the existing wired binary.)* |
| 40 | 2026-08-15 | SD31-W3-INTEGRATE-001 | NOTE | Fixed the CONFIRMED `miser_s_mask` defect (equipment lane, row-1-shaped finding: `ultimate_equipment:equipment:miser_s_mask` shipped another item's 18,000 gp / 2 lb): `ue_equip_magic_items.lst:714` is two items glued onto one physical line by a missing newline, second item's name even embedded mid-token inside the first item's `BONUS:SITUATION` value. Corrected `equipment_tables.rs`'s `Miser's Mask` entry to its own real values (3,000 gp / 1 lb, p.246 — the value the live Equipment Catalog screen renders from, TDD'd with a new regression test) and patched the shipped `data/corpus/ultimate_equipment/equipment/miser_s_mask.json`'s `cost_gp`/`weight_lbs` to match (its `raw_tokens` honestly continues to carry the FULL cited line's tokens, both items' — that is what the citation line actually contains, not a bug this fix can remove without splitting the physical `.lst` line, which is out of this program's write scope). The row's second item, "Mitre of the Hierophant" (18,000 gp / 2 lb, p.247, two `SPELLS:` grants, a `BONUS:SKILL` chain), remains entirely un-shipped — no catalog entry, no corpus record — because a citation resolver has no clean column boundary to key a second record off of on a line where two records' tokens interleave. | Fixed the wrong-value defect (the load-bearing half: a real item shipping a real WRONG price) and left the missing-item gap named rather than fabricating a citation for it. | *(needs a dispatch decision: (a) add a `Mitre of the Hierophant` entry to `equipment_tables.rs` with its own correct values, accepting that its `raw_tokens` will also carry both items' tokens under the current citation resolver — the sweep would need a citation-line-supports-N-records exception, not just a new entry; or (b) add a corpus-shape guard in `corpus_literal_sweep`/`v06_corpus_trap_report` that a cited row carrying 2+ `COST:`/`SOURCEPAGE:` tokens is a `Severity::Defect` worth a human look, which would have caught this specific defect at ingest time rather than after promotion to `done`.)* |
| 41 | 2026-08-15 | SD31-W3-INTEGRATE-001 | NOTE | Adversarial review CONFIRMED `cargo run --locked --bin v06_corpus_trap_report -- --audit` exits 2 (1,040 `[wiring-class-mismatch]` findings) at the merged tip, pre-existing at the shared base `6f857525b` (reproduces identically before and after this wave's 5 merged lanes) — every finding is a corpus JSON record's STORED `wiring_class` field (stamped at ingest time) disagreeing with a fresh classify of its own `.lst` line, corpus-wide over `monster`/`companion` records in `bestiary`/`APG`/`UW`/`ultimate_psionics`, all traced to `SD31-E2-F2-001-wiringfix`'s D3/D4 classifier fix (`d07d41b5c`) landing a 1,265-unit `derived`→`static` transition AFTER those records were ingested. `v06_work_inventory` never reads the stored field (it re-derives `wiring_class` fresh every run from the live classifier), so this drift is cosmetic/informational for the board today — but `--audit` is not a `verify.sh` stage (`grep -n trap_report scripts/verify.sh` → comment only), so every lane's "DoD item 3 exit 0" self-attestation this wave was checking a tool that was never green to begin with. | Corrected every lane's receipt that claimed `AUDIT_EXIT=0` for this stage to state the true `AUDIT_EXIT=2` with the reproduction command, rather than leave a false pass standing. Did not attempt the corpus-wide re-stamp (real data-mutation work spanning ~12 books' worth of monster/companion tables, needs its own PI-exposure review per the "generated artifacts mutated post-hoc" precedent — out of a single integration cycle's safe scope). | *(needs a dispatch decision: (a) a dedicated "corpus wiring_class stamp refresh" cycle re-stamps every corpus JSON record's `wiring_class`/`wiring_class_signals` field from the CURRENT classifier, with its own PI-exposure review before landing; and (b) add `v06_corpus_trap_report --audit` as a real `verify.sh` stage — with a committed baseline count so NEW drift fails the gate rather than every future run inheriting a silent 1,040-finding non-gate — so a DoD item is never self-attested again.)* |
| 42 | 2026-08-15 | SD31-W3-INTEGRATE-001 | NOTE | Adversarial review CONFIRMED `corpus_literal_sweep` — the ONLY gate behind `static → literal-verified → done`, which carried 1,176 units this wave — can fail for transcription (wrong raw token, wrong digest, wrong cited line) but structurally CANNOT fail for a fabricated shipped VALUE: a mutation test (leave `raw_tokens` untouched, set `data.cost_gp`/`data.description` to invented values) still reports `CLEAN`, exit 0, and the record still stamps `done`. `grep -c raw_bonus_chains src/rules_core/corpus_literal_sweep.rs src/bin/corpus_literal_sweep.rs` → 0/0 — the BONUS chains carrying a static equipment record's actual magnitudes are outside the comparison too. This is a pre-existing gate-design gap (not introduced by any lane this wave) reported because this wave loaded 1,176 units onto the rung it can't fully defend. Not attempted this cycle: extending the sweep to byte-compare `data.raw_bonus_chains` against the token closure and assert derived scalar fields (`cost_gp`/`weight_lbs`/`description`) agree with their source tokens — a real gate-behavior change to a shared, heavily-depended-on binary, judged too large to make safely alongside this cycle's merge-and-fix scope. | Logged the gap with the exact mutation commands rather than attempt the gate redesign under this cycle's own time pressure — modifying the shared sweep binary's pass/fail semantics is exactly the kind of change that needs its own dedicated TDD cycle, not a rider on an integration cycle already touching 5 merged lanes' worth of surface. | *(needs a dispatch decision: (a) extend `corpus_literal_sweep` to byte-compare `raw_bonus_chains` and assert derived scalars against source tokens; (b) add the cost/description-fabrication mutation as a permanent known-answer regression case in `scripts/tests/test_corpus_literal_sweep.sh` so the gate's inability to fail on content is caught the next time it's touched.)* |
| 43 | 2026-08-15 | SD31-W3-INTEGRATE-001 | NOTE | Adversarial review CONFIRMED 29 `core_rulebook` spell records ship `"level": 0` despite their cited base corpus row carrying no `CLASSES:` token at all (the corpus states no spell level) — worked example `core_rulebook:spell:planar_binding_devils_and_fiendish_creatures_only`, `cr_spells.lst:1460`, a real 5th/6th-level spell shipped as a cantrip. Pre-existing (`ingested_at: 2026-08-03`, predates every lane this wave — the spell lane's own diff to these 1,173 records is proved to add only `raw_tokens` and change nothing else, key-by-key). Low live severity: `src/rules_core/corpus_loader.rs::spell_record_from_json` reads only `key`/`school` from these records, so `level` is currently dead, non-player-visible data — but it is still a wrong value certified onto a `done` unit. | Not fixed this cycle (re-resolving `level` for 29 records from each spell's true base declaration needs the same base-row-vs-`.MOD` resolution the spell lane's own row-33 `.MOD`-citation finding already scopes as a separate, real ingest-repair cycle — bundling it into this integration cycle risks a rushed, unverified corpus data change). | *(needs a dispatch decision: re-resolve `level` for the 29 records from each spell's true base declaration (same shape as row 33's citation-repair follow-on), or make the field `Option<u8>` and write `null` where the corpus states nothing rather than a fill value. Until fixed, `planar_binding_devils_and_fiendish_creatures_only` and the other 28 should be treated as accepted-with-note, not silently trusted.)* |
| 44 | 2026-08-15 | SD31-W3-INTEGRATE-001 | RULING-NEEDED | Adversarial review found `SD31-E6-F11-002`'s 7-unit `done` movement CLEAN on evidence integrity (every fixture value reproduces byte-for-byte from the pinned `.lst` rows) but NOT meeting the wave's own done criterion: `spell_like_ability_caster_level()` has zero production callers anywhere in the tree, reads a proxy field (`MONSTERCLASS` trailing int) rather than the fixture's own pinned `corpus_field` (`BONUS:VAR\|SLA_CL\|HD`), and the units' `derived` verdict rests on ~11 BONUS/DR magnitude tokens (Balor, `b1_races.lst:93`) of which the shipped `chassis_only` record carries none and the seam evaluates none. Not gaming (the reviewer's own words) — a bar-scope question the 2026-08-13 done-rung directive did not anticipate for a `derived` MONSTER specifically, since the rung was written against the equipment precedent where the evaluator IS the shipping consumer. | Left the 7 fixtures and their `done` stamps in place this cycle (reverting real, honestly-sourced work on a bar-SCOPE question — not a correctness defect — is a bigger unilateral call than an integration cycle should make without operator input) and logged the question plainly rather than either side unilaterally. | *(needs an operator ruling before the 7-unit board credit is treated as durable: (a) rule that a `derived` monster may reach `fixture-verified` via a single non-shipping evaluator over a proxy field — extends the done-rung bar for this kind explicitly; or (b) hold the 7 units pending a real evaluator seam wired to a shipping consumer, which itself needs the `MonsterStatBlock` ability-score ingest-widening named in row 26/27 above.)* |
| 46 | 2026-08-15 | SD31-E5-F1-001 | NOTE | Confirmed by trace (before writing any generator code) that the `ultimate_equipment`-shaped lever ("dump a shipped corpus/table to JSON → `done`") does NOT generalize to `class_feature`: no `rules_tables` module carries per-record `class_feature` data at all (`grep -rl 'class_feature\|ClassFeature' src/rules_core/rules_tables/` finds only class-chassis mechanism code), and `Kind::ClassFeature`'s `classify()` arm (`src/bin/v06_work_inventory.rs:3412`) sets `status` purely from engine-observed evidence (a consumer-delta probe or a matching `explanation_id`) — never from `data/corpus/**/*.json`. A corpus-JSON dump can only unlock the `literal-verified` STAMP for a unit ALREADY `status=="grounded"`; it cannot manufacture `grounded`. Re-derived population: only `(wiring_class=static, status=grounded)` = 14 units and `(derived, grounded)` = 19 units (a different lane's file territory, needs `derived_evaluator_fixture_check` fixtures, not a corpus dump) can EVER reach `done` this way; the other 15,439 `class_feature` units (11,476 not-started + 3,849 unmeasurable + 34 deferred + 20 already-done via `computed`) are gated on genuine per-class engine wiring (`epic-4-mechanism`, zero cycles landed corpus-wide as of this cycle). Full trace and every command: `artifacts/SD31-E5-F1-001-lever-measurement.md`. | Built `cache_gen::class_feature` (book-agnostic LST-token transcription, not a Rust-table dump — no table exists to dump) and ran it for 21 of the 23 books (excluding `pathfinder_unchained`, already hand-ingested, and `ultimate_psionics`, see row 47), writing 12,431 new corpus JSON records so the 14 promotable units' `literal-verified` stamp could land, AND so every future `epic-4-mechanism` cycle inherits an already-built corpus JSON for its book (the fixed cost is paid once, per the book-onboarding-tax precedent). Measured: board `done` +14 (7355→7369), `class_feature` `done` 25→39. | *(none — this is the deliverable the card asked for: prove or refute the lever's generalization, then pull whatever real lever exists. No operator action needed; the 15,439-unit remainder is correctly Epic-4's scope, not a card-4 shortfall.)* |
| 47 | 2026-08-15 | SD31-E5-F1-001 | NOTE | `ultimate_psionics`'s `class_feature` corpus path (`pathfinder/dreamscarred_press/ultimate_psionics/up_abilities_class.lst`) has only 4 path segments — Dreamscarred Press's layout carries no Paizo-style "line" tier — while `corpus_literal_sweep::book_dir_of` (`src/bin/corpus_literal_sweep.rs`, out of this card's file territory) hard-requires 5 (`<system>/<publisher>/<line>/<book>/<file>`), returning `None` and failing the sweep with `source.path ... is not <system>/<publisher>/<line>/<book>/<file>-shaped` for every record cited there. Reproduced live: generated `ultimate_psionics/class_feature/*.json` (1,422 units' worth), ran `cargo run --locked --bin corpus_literal_sweep`, got exactly this finding on the first file examined, then reverted (`rm -rf data/corpus/ultimate_psionics/class_feature`) rather than ship a dirty sweep. | Excluded `ultimate_psionics` from this cycle's `BOOK_PRIMARY_FILES` (documented in the module's own doc comment and its own test assertion) rather than edit the shared sweep binary outside this card's granted file scope. | *(needs a dispatch decision: widen `book_dir_of` to accept a 4-segment non-Paizo path (Dreamscarred Press has exactly one such book system-wide today, so a minimal fix), or a documented per-publisher path-shape table. Either unblocks `ultimate_psionics`'s 1,422 `class_feature` units (0 of which are currently `done`-promotable regardless — no `(static\|derived, grounded)` units exist in that book today — so this is pure future-banking, not a blocked `done` count) for this same generator to re-run unchanged.)* |
| 45 | 2026-08-15 | SD31-W3-INTEGRATE-001 | NOTE | Fixed the CONFIRMED `SPELLS:` slash-in-spell-name false positive (`SD31-E2-F3-001`'s D6 `SPELLS:` scanning addition): `has_scalar_or_arith_in_spells_field` was passing `allow_slash=true` to `has_arith_scoped` for EVERY non-`CASTERLEVEL=`/non-`TIMES=` segment, so a canonical PF1 spell name containing its own literal slash (`Open/Close`, `Blindness/Deafness`, `Clairaudience/Clairvoyance`) flipped an otherwise fully-literal `SPELLS:` field to `derived`. Fixed via TDD (`src/rules_core/wiring_class.rs`, 3 new tests: `d7_spells_field_slash_in_spell_name_is_not_arithmetic`, `d7_spells_field_blindness_deafness_is_not_arithmetic`, `d7_spells_field_slash_in_name_does_not_hide_a_real_dc_formula`) by extracting the slash-independent half of `has_arith_scoped` into a new `has_arith_no_slash` helper (name segments never check `/`; a genuine DC formula in the comma-delimited tail still does). `cargo test --locked --lib wiring_class::` → 54/54 passed; full `cargo test --locked --lib` → 1815/1815 passed, 0 regressions. The reviewer's separately-CONFIRMED finding that the `SPELLS:` change's full corpus-wide blast radius (484 units, `display`→`derived` majority unrepresented in the 185-unit ground-truth sample) was measured for the REJECTED `PLUS:` fix but never for the SHIPPED `SPELLS:` fix is addressed by this integration cycle's own guarded regen (§3 below) rather than a separate hand-built transition matrix — the regen re-derives the classifier fresh at the merged, D7-fixed tip and the before/after `wiring_class` counts are reported in this cycle's receipt. | Fixed via TDD rather than reverting the whole `SPELLS:` addition (the legitimate 343-unit `CASTERLEVEL=`-driven and 115-unit genuine-scalar-segment majority of the 484 are real, correctly-detected `derived` records the D6 change was built for) — reverting would have thrown away real signal to fix a narrow false-positive shape. | *(none — fixed this cycle. The stratified-sample gap the reviewer named — the 185-unit ground-truth sample has zero slash-in-spell-name cases and is not representative of the 285-unit `display`→`derived` population — remains a real methodology gap for Epic 2-F2's classifier-acceptance work, worth a targeted top-up sample.)* |
| 50 | 2026-08-16 | SD31-PI-REPAIR-001 | RULING-NEEDED | Row 38's "2 shipped `ultimate_equipment` records" was corrected to 1 (retro correction, `--verified-by` a full corpus-wide scan; see row-38 resolution below) — `Elysian Shield` was never actually dumped by `cache_gen::ultimate_equipment.rs` at all, because `rules_core::rules_tables::ultimate_equipment::equipment_tables()` (the table that generator dumps) has no entry for it. But re-deriving where "Elysian Shield" DOES appear in the tree found a separate, real, un-owned defect surface: `src/rules_core/rules_tables/equipment_gap_tables.rs:541` carries a hand-curated Rust literal `EquipmentGapRow { book: "UE", key: "Elysian Shield", name: "Elysian Shield", cost_gp: Some(27470.0), ... }` — the real UE-book value (matching `ue_equip_arms_armor.lst:129`, which DOES declare `NAMEISPI:YES`) sitting unscreened in source, consumed by `equipment_resolver.rs` (feeds the desktop equipment catalog) and `v06_work_inventory.rs`. This table is entirely outside `pi_screening`'s reach (no `license`/`pi_field`/`pi_marker` machinery at all — it is not a Shape B v1 record and writes no `data/corpus/` JSON), so my card's `data/corpus/**/*.json`-scoped fix and the new `declared-pi-audit` gate (row-38/39 resolution below) cannot see it. No PI leaked via the blacklist-term screen (`Elysian Shield`/`Shield` are not `PI_BLACKLIST_TERMS`), but the corpus's own `NAMEISPI:YES` declaration is not honoured for this row wherever `equipment_gap_tables.rs` is the source. Out of this card's file territory (`YOUR FILES` names `cache_gen/ultimate_equipment.rs`/`ingest_races.rs`/`pi_screening.rs`/`LICENSE.json`, not `rules_tables/equipment_gap_tables.rs`), so not touched. | Re-derived the full population with a corpus-wide scan (`scan_nameispi.py`, scratchpad — walks every `data/corpus/**/*.json` with `source.kind==lst_token`, cross-checks the real corpus line) rather than trust the 2-record sample; logged the `equipment_gap_tables.rs` gap here instead of editing a file outside this card's granted scope. | *(needs a dispatch decision to whichever lane owns `rules_tables/equipment_gap_tables.rs`/`equipment_resolver.rs`: either drop the `Elysian Shield` `EquipmentGapRow` (matching the `data/corpus`-side ruling: a `NAMEISPI:YES` name cannot be redacted, so it is dropped, not fixed to a placeholder), or add an operator-reviewed per-book override reclassifying it OGL per `ogl-pi-blacklist.md` §3. `grep -c 'book: "UE"' src/rules_core/rules_tables/equipment_gap_tables.rs` to size whether other UE gap rows share this exposure before deciding.)* |

| 51 | 2026-08-15 | SD31-E6-F3-002 | NOTE | **Correction to row 27's root-cause claim.** Row 27 states "every one of the 1,040 `[wiring-class-mismatch]` findings is the SAME root cause as row 22." Re-verified while fixing row 22 (this cycle owns `corpus_literal_sweep.rs`): **false — the two are unrelated defects in unrelated code.** `v06_corpus_trap_report --audit`'s `WiringClassMismatch` check lives in `src/pcgen_import/corpus_traps.rs`, never calls `corpus_literal_sweep` (`grep -c corpus_literal_sweep src/pcgen_import/corpus_traps.rs` → 0), and already has its own nested-path-safe book/file resolution (fixed for a DIFFERENT, earlier defect, `SD30-E8-F3-002` — see its own doc comment at `corpus_traps.rs:1520-1538` and the `wiring_class_mismatch_reads_a_citation_nested_one_level_under_a_book_subdirectory` test). It compares each corpus JSON record's STORED `wiring_class` field (stamped at ingest time) against a FRESH `determine_closure` classify — a stale-ingest-stamp defect, genuinely unrelated to `corpus_literal_sweep`'s `--json-out` book-attribution bug row 22 fixes. Proven empirically, not just argued: ran `cargo run --locked --bin v06_corpus_trap_report -- --audit` twice at this cycle's tip, once with row 22's fix applied and once with `src/bin/corpus_literal_sweep.rs` swapped back to its pre-fix `git show HEAD:` content (rebuilt both times) — **byte-identical output both times**: `259 0 mod-record; 0 950 wiring-class-mismatch`, exit `2`. **Also corrects the count itself**: the true figure at this cycle's start tip (`89846f5c9`) is **950**, not 1,040 — already stale by the time row 27 was written, from other wave-3 lanes' merges landing after `SD31-E6-F11-002`; not attributable to this cycle (confirmed unchanged by this cycle's own diff, per the identical-output test above). | Did not attempt a fix (this card's file territory is `corpus_literal_sweep.rs`, not `corpus_traps.rs`; the real fix — a corpus-wide `wiring_class` stamp refresh — is exactly what row 27's own "needs a dispatch decision" column already proposes, and that proposal stands, only mis-attributed to the wrong upstream cause). Filed a `retro.py correction` with `--verified-by` the two-binary identical-output command above. | *(none — the dispatch decision row 27 already proposed (a dedicated corpus-wide `wiring_class` stamp-refresh cycle) is unaffected by this correction; only re-attribute its cause when that cycle is scoped, and use 950 as its current baseline, not 1,040.)* |
| 52 | 2026-08-16 | SD31-E6-F1-002 | NOTE | **Row 44's headline objection is fixed: `spell_like_ability_caster_level()` now has a real production caller.** `apps/desktop/src-tauri/src/monster_catalog.rs`'s `map_chassis_monster` computes `MonsterCatalogEntryDto::spell_like_ability_caster_level` from it, served over the `list_monster_catalog` Tauri command every chassis-book monster already reaches through, and rendered on `MonsterCatalogScreen.tsx` as `· Spell-like abilities CL <N>` next to the existing `Hit dice` clause. Proven, not merely wired: `a_monster_with_spell_like_abilities_serves_its_universal_monster_rule_caster_level` (Demon (Balor), `Some(20)`) and `a_monster_with_no_spell_like_abilities_serves_no_caster_level` (Animated Object (Medium) and Bestiary-1's SD-22 half, both `None`) in `monster_catalog.rs`'s own test module; DoD-8 on-screen verification below. The narrower half of row 44's objection — the function reads `MonsterStatBlock::monster_class`'s trailing HD rather than the fixture's own pinned `BONUS:VAR\|SLA_CL\|HD` token literally — is **not** changed this cycle: it remains a proxy read, justified (not merely asserted) by the function's own doc comment (`monster_class`'s trailing integer and `challenge_rating` routinely differ, e.g. Linnorm (Crag) `MONSTERCLASS:Dragon:15` at `CR:14`, so it is a genuine HD field, not a disguised copy of a different corpus number) and now additionally gated on a NEW row-presence check (`MonsterStatBlock::has_spell_like_abilities`, true only when the row carries a literal `BONUS:VAR\|SLA_CL\|` token — see row 53) so the function can no longer hand a caster level to a monster with no spell-like abilities at all. TDD anchor for that gate: Linnorm (Crag) (`b1_races.lst:269`) carries `BONUS:VAR\|SLA_CL\|HD` but its spell-like effects reach the row only through an `ABILITY:` cross-reference, with **no** `SPELLS:` token anywhere on the line — an earlier draft of the gate keyed on `SPELLS:` presence and broke this seam's own already-committed Linnorm fixtures; re-derived and fixed before landing. All 7 of `SD31-E6-F11-002`'s committed fixtures still clear (`run_monster_bar_check_clears_every_committed_monster_fixture`, `cargo test --locked --lib` 1819/1819 passed including this and 8 new tests this cycle). | The 7 units' `done` credit now stands on ruling option (b) named in row 44's own text ("a real evaluator seam wired to a shipping consumer") rather than needing option (a)'s bar-scope extension — the operator ruling row 44 requested is moot for those 7 specifically, though the proxy-field nitpick above remains an open, non-blocking robustness note. | *(none required — informational. If a future cycle wants the function to read `BONUS:VAR\|SLA_CL\|` directly instead of `MONSTERCLASS`'s trailing HD, that is a small, separate refactor with no behavior change on any of the 7 committed fixtures, since HD and the SLA_CL value agree on all 7 by construction.)* |
| 53 | 2026-08-16 | SD31-E6-F1-002 | RULING-NEEDED | **Widened `MonsterStatBlock` per row 26's ask, and re-derived row 26's own headline figures — both moved.** Added `stat_adjustments: &[StatAdjustment]` (every `BONUS:STAT\|<ability-list>\|<int-literal>` token on the row, one record per ability, formula-valued tokens skipped rather than guessed — reuses `companion_chassis::StatAdjustment` verbatim rather than duplicating it, since `companion_chassis` already carries the identical parse for the identical PCGen token) and `has_spell_like_abilities: bool` (row 52) to `MonsterStatBlock`; regenerated all 13 registered monster books via `scripts/transcribe_monster_tables.py` (widened with the matching `parse_stat_adjustments`/`parse_has_spell_like_abilities` functions) against the pinned oracle; added `gen_book_cache.rs` JSON emission for both fields; mutation-proved (`demon_balor_stat_adjustments_match_the_live_pinned_corpus_row` re-reads `b1_races.lst:93` fresh at test time, independent of the transcriber, and must agree with the committed static table byte-for-byte). **Re-derived the live `derived\|grounded\|monster` headline at this tip: `python3` filtering `docs/work-inventory.json` on `kind=='monster' and wiring_class=='derived' and status=='grounded'` → 386, not 280** (row 26's own figure, itself already a correction of the original 1,229/266 — the population keeps moving as sibling lanes' classifier fixes land; `retro.py correction` emitted, `--verified-by` the command above). Of these 386, a **freshly re-derived, per-record check of the real corpus row** (not an assumed ratio) finds **104** carry a non-`BONUS:STAT` magnitude token (`BONUS:VAR`/`DR:`/`SR:`/`BONUS:COMBAT`/`BONUS:SKILL`) whose value contains a bare `STR`/`DEX`/`CON`/`INT`/`WIS`/`CHA` reference — the ability-modifier-scaling shape row 26 named (script: `docs/release/SD-31-corpus-closure-grind/artifacts/sd31-e6-f1-002-ability-scaling-check.py`). **Structural finding, not a shortfall of effort: even with `stat_adjustments` now carried, these 104 (and the ~192-estimated predecessor population) cannot be fixture-covered without fabrication.** Worked counter-example: Animated Object (Medium) (`b1_races.lst:13`) carries `BONUS:STAT\|STR\|4` and consumes it via `BONUS:VAR\|ConstrictBonusDamage\|STR`; `BONUS:STAT` is PCGen's DELTA against a base ability score this book's own row never states (no `STAT:` override token present, and PCGen's per-race/per-template base-score convention is not uniformly documented as 10 across every monster race in this corpus — asserting one without being able to verify it against a published, independently-sourced stat block would be exactly the fabrication risk `SD31-E6-F11-002` already correctly refused). `companion_chassis::StatAdjustment`'s own doc comment states the identical rule for companions: "an adjustment, never a score... serving `6` in a column labelled Strength would be the quieter lie" — this cycle extends that same discipline to monsters rather than relaxing it. | Delivered the real ingest widening (13 books, mutation-proved) and the fresh re-derivation; did **not** attempt to compute final ability scores or modifiers to fixture-cover the 104/386 (would require inventing a base-score assumption the corpus does not state); did **not** attempt row 26's smaller named sub-lever (extending `spell_like_ability_caster_level`'s parser to `max(TL,1)`/arithmetic-wrapped HD and a distinct `SR:10+TL` rule, ~7 units, no new corpus data needed) — out of this cycle's time budget after the row-44 production-wiring fix and the 13-book regen. | *(needs a dispatch decision: (a) a follow-on card to extend the SLA-CL parser to `max(TL,1)`/arithmetic-wrapped HD and add an `SR:10+TL` evaluator — ~7 units, no new corpus data, same shape as this cycle's `has_spell_like_abilities` gate; (b) confirm the ability-modifier-scaling 104-unit family is a genuine, permanent ceiling for `derived\|monster` absent a base-ability-score source this corpus does not carry — candidate for a Structural Exclusion Register proposal rather than further ingest work, pending operator review of the Animated Object counter-example above.)* |
| 54 | 2026-08-16 | SD31-E6-F1-002 | NOTE | **DoD-8 and full-gate closeout for rows 46/47.** DoD-8 (on-screen verification) PASSED: `verify-on-screen.sh --family monster --record "Demon (Balor)" --expect "Balor" --expect "Spell-like abilities CL 20"` → PASS, screenshot + report committed at `docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E6-F1-002/item8/monster-demon-balor.png`/`.verify.md` — the select-all/clipboard extraction reads `"... Hit dice Outsider (Fort/Will):20 \| Spell-like abilities CL 20"` off the live rendered webview, not merely off a gate's exit code. Full `./scripts/verify.sh` (launched early, in the background): `VERIFY_EXIT=1`, 21 of 22 stages PASS, `root-full` the sole failure — attributed to exactly one suite (`tests/sd17_b5_equipment.rs::parse_runs_in_linear_time_on_a_synthetic_large_file`, a CPU-load-sensitive timing assertion in a file this cycle never touched) and confirmed flaky by an isolated re-run (`ok`, 1.45s, well under its own 2s bound, once the gate's own concurrent build load had cleared) rather than assumed environmental. Guarded regen (`corpus_literal_sweep` CLEAN, `derived_evaluator_fixture_check` 100/101 covered cleared — the 1 failure is a pre-existing, unrelated `advanced_players_guide` equipment-fixture gap, `v06_work_inventory` exit 0/zero stamp loss) confirms the board's doneness verdict is **bit-for-bit unchanged**, 7355/38521 (19.09%) before and after, consistent with this cycle's own "0 new done units" claim. `docs/work-inventory.json` restored, not committed. | Re-ran the single flaky test in isolation rather than the whole ~10+-minute `root-full` stage a second time, per the loop-instruction's own self-heal rule for a once-failing, clean-on-rerun test. | *(none — informational closeout. Full log committed at `docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E6-F1-002-verify.log`.)* |

| 55 | 2026-08-15 | SD31-E6-F2-002 | RULING-NEEDED | **PROPOSED Structural Exclusion Register entry** (`decisions.md §3`) for `spell`'s remaining 13 `not-ingested` units within the engine's 5-modeled-book set (12 `core_rulebook`, 1 `advanced_race_guide`), re-verified one record deep this cycle (superseding wave 3's own finding, re-derived rather than trusted): `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); FIVE={'core_rulebook','advanced_players_guide','advanced_class_guide','advanced_race_guide','ultimate_intrigue'}; u=[x for x in d['units'] if x.get('kind')=='spell' and x.get('book') in FIVE and x.get('status')=='not-ingested']; print(len(u))"` → **13**. Every one is a PCGen `.COPY=` variant carrying `CLASSES:.CLEARALL` and no `SCHOOL:`/`CLASSES:`/`DOMAINS:` token of its own (`cr_spells.lst:1467-1478`, `arg_spells.lst:230`; `awk` command: `grep -c "CLASSES:.CLEARALL" cr_spells.lst` → 12, `arg_spells.lst` → 1). **The four `decisions.md §3` items:** (1) proving command above, run this cycle; (2) missing capability: none — this is not a capability gap, it is a genuine corpus absence. `crb::spell_list::SpellListEntry.level` is `pub level: u8` (non-optional); even after this cycle widened the pattern to `Option<u8>` for a new book (`ultimate_magic`), assigning `level: None` to these 13 would still be dishonest, because `.CLEARALL` is PCGen's own explicit statement "this variant has no class" — the record is not merely missing a level, it is declared to have none by the very mechanism (`.COPY=` + `.CLEARALL`) that creates it. There is no widening of any ingest tool that produces a real level here, because the corpus states none exists; building "the capability" is impossible in the sense `decisions.md §3` item 2 requires (not merely expensive) because there is no fact to ingest. (3) `scripts/reachability_audit.py` run this cycle (`/tmp/reachability-sd31-spell-reach.json`, `spell` reachable ceiling 97.82%) — re-stated honestly: the audit's own grid model cannot see this narrow "corpus states no level" shape (it only detects `ambiguous`-wiring dead-ends), so it does NOT independently confirm impossibility; that proof is items 1-2 above, one record deep, which is the standard the audit itself cannot substitute for here. (4) operator sign-off: NOT YET GRANTED — this is a proposal only, per the binding rule that a cycle may propose, never grant. | Did not ingest a fabricated level for the 13 records (`no-stub-mvp-doctrine.md`) and did not silently drop them from the denominator — logged the proposal with all four required items and left the units in `not-ingested`/counted. | *(needs operator sign-off: grant the Structural Exclusion Register entry for these 13 units, or direct a different disposition if the operator judges `.CLEARALL` variants should count some other way.)* |
| 56 | 2026-08-15 | SD31-E6-F2-002 | NOTE | Ultimate Magic's spell ingest (this cycle, `src/bin/ingest_ultimate_magic_spells.rs`) covers only the book's 269 real BASE spell declarations. Two real, named residuals remain, neither fabricated over: **(a) 22 `.COPY=`-variant records** (e.g. archetype-renamed sub-forms) that `v06_work_inventory` enumerates as distinct `spell` units from the raw `.lst` but this cycle's ingest deliberately excludes (same `.MOD`/`.COPY=` exclusion convention every other book's `spell_list.rs` uses) — measured on this cycle's own local guarded regen (`docs/work-inventory.json`, NOT committed per the wave rule; re-deriving this figure against the checked-out repo requires first running the guarded regen procedure with this cycle's `spell_resolver.rs`/`ultimate_magic/spell_list.rs` built): `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x.get('kind')=='spell' and x.get('book')=='ultimate_magic' and x.get('status')=='not-ingested']; print(len(u))"` → **22**, still `not-ingested`, a genuine remainder needing the same `.COPY=`-variant resolution logic ARG's `spell_list.rs` doc comment names for its own 7 (resolve against the base record's own fields) before it can move. **(b) 10 records carry no `CLASSES:`/`DOMAINS:` token at all** (`Restore Eidolon` and 9 siblings — eidolon-only/plane-shift/creation sub-forms), shipped with `level: None` per this cycle's `SpellListEntry` design rather than fabricated — real, not a defect, but worth naming since a future book-onboarding cycle should expect the same shape. **This figure was itself corrected mid-cycle from an initial 25**, caught by `reach_gate.rs`'s own `bare_records_are_exactly_the_recorded_findings` gate flagging 15 `Masterpiece` records as reaching the player with no payload at all: their real `CLASSES:` token (`Bard=3[PRESKILL:1,Perform (String Instruments)=7,Perform (Wind Instruments)=7]`) carries a bracketed `PRESKILL` sub-condition with its own embedded `=`s, and the first cut of `levels_in_field`'s `rsplit_once('=')` grabbed the LAST `=` in the whole group — inside the bracket — which fails to parse as a `u8` and silently discarded the record's real level (3, 2, etc.) as though it carried none. Fixed via TDD (2 new tests: `levels_in_field_strips_a_bracketed_preskill_clause_before_finding_the_level`, `levels_in_field_handles_a_bracketed_clause_among_several_pipe_groups`) by truncating each `|`-group to the text before its first `[` before the `=` search. **15 `Masterpiece` records still carry an unrecognized `SCHOOL:` value** (`"Masterpiece"`, a bard-performance mechanic this engine's 9-school `Pf1SchoolId` enum does not model) — shipped with `school: None`, genuinely not fabricated, but now WITH a real level (`ingest_ultimate_magic_spells`'s own run output names all 15 by key, both before and after the fix). | Shipped the 259 clean base-declaration records this cycle's tool could honestly resolve after the bracket fix (244 before it); named both residuals with exact, re-derived counts and worked examples rather than silently under-reporting Ultimate Magic as "fully onboarded," and caught the level-parsing bug via `reach_gate.rs`'s own gate rather than shipping 15 silently-wrong `level: None` values. | *(none required — informational. A future cycle extending the `.COPY=`-variant resolution pattern, or widening `Pf1SchoolId` for non-standard school-shaped mechanics like Masterpieces, would close these.)* |
| 57 | 2026-08-15 | SD31-E6-F2-002 | NOTE | The engine's spell catalog (`spell_resolver::spell_catalog_rows()`) now chains **six** books (added `ultimate_magic` this cycle, 269 records) of the corpus's much larger spell-bearing set. Re-derived remaining scope: `python3 -c "import json,collections; d=json.load(open('docs/work-inventory.json')); SIX={'core_rulebook','advanced_players_guide','advanced_class_guide','advanced_race_guide','ultimate_intrigue','ultimate_magic'}; u=[x for x in d['units'] if x.get('kind')=='spell' and x.get('book') not in SIX]; print(len(u))"` → **1,257** `spell` units in OTHER books still structurally unreachable through any ingest work (`occult_adventures` 473, `ultimate_combat` 147, `core_essentials` 109, `inner_sea_gods` 96, `horror_adventures` 72, `ultimate_wilderness` 61, `bestiary_4` 56, `adventurers_guide` 49, `inner_sea_magic` 39, `inner_sea_races` 29, `inner_sea_intrigue` 26, `monster_codex` 24, `inner_sea_world_guide` 22, `inner_sea_temples` 21, `book_of_the_damned_volume_2` 12, `mythic_adventures` 9, `book_of_the_damned_volume_1` 6, `inner_sea_faiths` 3, `bestiary_6` 2, `ultimate_equipment` 1) — each needs the same shape of work this cycle did for `ultimate_magic` (a `SpellListEntry` table built from the real `.lst`, PI-screened per record with both SD-30 contracts, wired into `spell_catalog_rows()`). Additionally, `class_spell_levels.rs` (the per-class, not per-record-minimum, level table `tests/sd27_known_spells_must_be_on_the_class_spell_list.rs` checks against) has NOT been extended for `ultimate_magic` — every one of UM's 269 spells therefore reads as "off the wizard list" (and every other class's list) in that specific per-class check even where a spell's own `CLASSES:` token genuinely names Wizard, because that check consults a separate table this cycle did not touch (`ingest_class_spell_levels_arg.rs`'s own precedent names the same book-by-book extension shape). | Landed the sixth book as the primary deliverable rather than only re-confirming the finding wave 3 already made; named the remaining 5th-of-the-mandate scope precisely (19 books, 1,279 units) instead of declaring the capability "built" in general. | *(none required — informational scope-remaining note for whichever lane picks up the next book. No operator ruling needed; this is ordinary bounded ingest work, same shape as this cycle's own, per book.)* |
| 58 | 2026-08-16 | SD31-E6-F5-002 | BLOCKER | `corpus_literal_sweep`'s `book_dir_of` (`src/bin/corpus_literal_sweep.rs:345-351`) hard-requires **5** `/`-delimited segments in `source.path` (`<system>/<publisher>/<line>/<book>/<file>`) and calls `fatal()` — aborting the ENTIRE sweep, not just the one record — when a record's path has fewer. `pathfinder/dreamscarred_press/<book>/<file>` (Dreamscarred Press books: `ultimate_psionics`, `psionics_unleashed`, `psionics_expanded` — no `<line>` segment, only **4**) is structurally 1 segment short. Hit directly: `cargo run --locked --bin corpus_literal_sweep` on this cycle's own new `data/corpus/ultimate_psionics/equipment/equipmods/agile.json` (`source.path pathfinder/dreamscarred_press/ultimate_psionics/up_equipmods.lst`) → `fatal("... is not <system>/<publisher>/<line>/<book>/<file>-shaped")`, non-zero exit, zero records examined. **Pre-existing, not introduced by this cycle** — re-derived independently of file-walk order: `data/corpus/ultimate_psionics/monster/xeph.json` (committed `612004dfb`, SD-29, five waves before this one) already carries `source.kind: "lst_token"` and the identical 4-segment `pathfinder/dreamscarred_press/ultimate_psionics/up_races.lst` path, so `book_dir_of` returns `None` for it exactly the same way — this cycle's new equipment file merely sorted earlier in the walk order (`equipment` < `monster`) than the already-shipped monster records that would have hit the same fatal regardless. `corpus_literal_sweep.rs` is out of this card's file territory (named sibling-owned in the dispatch preamble) — did not edit it. | Verified the crash is real (ran the binary), traced it to the exact 5-segment assumption and confirmed algebraically that the pre-existing `ultimate_psionics` monster corpus already meets the trigger condition (did not need to run the binary against a reverted tree to prove this — the segment-count check is pure and deterministic over `source.path`, independently confirmed by counting `dreamscarred_press`'s own path depth). Left this cycle's `ultimate_psionics` equipment/equipment_modifier records in place rather than withdrawing them defensively: removing them would not fix the sweep (the pre-existing monster records trigger the identical fatal), so withdrawal would only discard 65 real, correctly-cited records for zero risk reduction. | *(needs a dedicated fix cycle in `corpus_literal_sweep.rs`'s owning lane: `book_dir_of` needs a Dreamscarred-Press-shaped 4-segment fallback alongside its current 5-segment Paizo-shaped rule, the same "more than one real corpus path shape" lesson row 22 already named for a different symptom. Until fixed, `./scripts/verify.sh`'s `corpus-sweep` stage will fail non-zero for ANY cycle whose gate run reaches a Dreamscarred-Press book with an `lst_token` record — this is wave-wide blocking, not scoped to equipment.)* |
| 59 | 2026-08-16 | SD31-E6-F5-002 | NOTE | **Confirmed and self-fixed within this cycle, recorded because the shape recurs across every `cache_gen` generator.** `cache_gen::equipment_gap`'s first `write_json` implementation unconditionally overwrote its target path. Its own per-book `used: BTreeSet<String>` slug-collision guard only tracks slugs used WITHIN that one `generate()` call — it cannot see a book's already-shipped, on-disk corpus from a PRIOR, unrelated ingest run. `core_rulebook`'s gap row `key: "Intelligent Item Purpose (Slay All)"` (from `equipment_gap_tables.rs`, citation line 895) slugifies to `intelligent_item_purpose_slay_all.json` — the IDENTICAL filename an already-shipped, richer record (`key: "Intelligent Item ~ Purpose / Slay All"`, citation line **446**, real `PRETYPE`/`SPROP` tokens, `wiring_class: computed`) already occupied. The first generator run silently clobbered it (`wiring_class: computed → display`, real description → `null`, real `raw_bonus_chains` → `[]`) — two DIFFERENT real corpus rows, same slug, and `git status` after the run showed it only as `M`, not as a new file, which is exactly how this class of defect hides from a casual review. A second row, `intelligent_item_purpose_slay_creature_type.json`, hit the identical shape. **Caught before commit** by reading `git status --porcelain data/corpus/` for `M` (not `??`) entries — a modified path under a directory this cycle's card only ever *adds* to is itself the tell. | Reverted both clobbered files (`git checkout --`) before this cycle's commit — neither ships in this cycle's diff. Fixed `write_json` to check `path.exists()` and refuse to write (return `Ok(false)`, reported in a new `GenerationReport::skipped_pre_existing` field) rather than overwrite; added a regression test (`write_json_never_overwrites_an_existing_file`) that writes a sentinel string to a pre-existing path and asserts it survives a `write_json` call untouched. Did not investigate whether the two colliding rows are the SAME real-world item under two different PCGen mechanisms or genuinely distinct game content (out of this cycle's bounded scope; the fix does not need that answer — skip-on-exists is safe either way). | *(worth a corpus-wide audit: every OTHER `cache_gen` module — `acg`, `apg`, `beastiary1`, `ultimate_equipment` — uses the same per-run-only `used` collision-avoidance pattern with the same latent gap. This cycle's `data/corpus/` diff is clean of the defect (verified: 0 `M` entries under `data/corpus/` at commit time), but a future re-run of any OTHER generator against a wider/updated `rules_tables` module could hit the identical shape. Not fixed here — those 4 files are out of this card's territory.)* |
| 60 | 2026-08-16 | SD31-W4-INTEGRATE-001 | BLOCKER | `SD31-E6-F5-002`'s 113 `ultimate_psionics` equipment records (row 58/59) genuinely aborted the ENTIRE `corpus_literal_sweep` (`fatal()`, not a per-record skip) because `book_dir_of` required >=5 path segments and Dreamscarred Press ships with no "line" tier (`<system>/dreamscarred_press/<book>/<file>`, 4 segments). Confirmed by direct reproduction: reverting the fix and re-running `corpus_literal_sweep` on the merged tip reproduces the identical `fatal()` at the first UPSI record read. | **Fixed.** Widened `book_dir_of` (`src/bin/corpus_literal_sweep.rs`) to accept the 4-segment Dreamscarred Press shape (keyed explicitly on the publisher name, not inferred from segment count alone, so an unrelated 4-segment path still rejects). 2 new tests. Re-ran the sweep: 0 findings, CLEAN, 19422 records examined (up from the pre-fix inability to complete at all). | *(informational — no ruling needed, the fix is landed and verified)* |
| 61 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | With row 60's sweep-abort fixed, the sweep surfaced 3 further real corpus-fidelity defects it had never been able to check before: `data/corpus/ultimate_equipment/equipment/bastard_s_sting.json` and `mountain_pattern_armor.json` (both from `SD31-PI-REPAIR-001`'s regen, row 8) shipped tokens NOT byte-present on their own cited corpus line — `parse_equipment_entries::open_record`'s same-name-row merge (a documented, deliberate feature for a DIFFERENT PCGen corpus shape) pulled in a different row's tokens; and `data/corpus/ultimate_wilderness/equipment/hunter_s_stand.json` (from `SD31-E6-F5-002`) showed the identical defect shape, three genuinely distinct `.COPY=` items merged into one. | Reverted all 3 records' `raw_tokens`/`raw_bonus_chains` to empty (their pre-regen state) per the confirmed finding's own sanctioned interim remedy. Added a production guard to `enrich_equipment_raw_tokens.rs`: every token about to be shipped must now be byte-present on the record's own cited line before the tool writes anything, refusing a merged-entry defect at the source rather than only catching it downstream. Sweep confirmed CLEAN after. The root parser fix (teach `parse_equipment_entries` to track each token's own origin line, or otherwise disambiguate a genuine same-name-different-item merge from a genuine multi-representation restatement) is NOT done — this cycle's fix is the interim remedy, not the root cause. | *(needs a dispatch decision: a future cycle should fix `parse_equipment_entries::open_record`'s merge logic properly, then re-enrich these 3 records for real rather than leaving them at `raw_tokens: []` indefinitely)* |
| 62 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | `cache_gen::equipment_gap` shipped 3 records (`CRRSVE_BRST_M`, `CRRSVE_BRST_R`, `REACH`) sourced from `#`-prefixed (PCGen-disabled/commented-out) corpus rows, carrying the raw internal `KEY:` token as their player-facing `name` — the same disposition the module already correctly applies to `.FORGET` directive rows, just missing the `#`-prefix case. | Added a `disabled_identity_column()` guard at the same call site (matching the `.FORGET` guard's shape), deleted the 3 stale on-disk records. 1 new test. Reconciled `advanced_players_guide`/`ultimate_combat` `LICENSE.json` counts after the deletions. | *(informational — no ruling needed, fix landed)* |
| 63 | 2026-08-16 | SD31-W4-INTEGRATE-001 | RULING-NEEDED | **SAFETY-CRITICAL, now fixed but worth an explicit operator note.** Every SD-30/SD-31 PI-redaction call site writes the `[redacted PI]` marker into `data.description` but never touches `data.raw_tokens` — so a record whose description was correctly redacted still shipped the ORIGINAL, unredacted prose verbatim in its own `raw_tokens`' `DESC` entry. Corpus-wide re-derivation found **413** shipped records exposed this way (367 where the corpus row declares `DESCISPI:YES`, 46 where only the `§52.3` blacklist term scan fired — the declared-PI reader alone cannot see that second trigger at all). This is the SAME defect shape row 8/`declared-pi-audit` CHECK A already exists to catch, one field deeper (`raw_tokens`, not `description`), at 13x the volume `declared_at`-only scanning could see. | **Fixed and verified gone from disk.** Redacted every `raw_tokens` `DESC` entry on all 413 records already marked `license: "PI-REDACTED"`, `pi_field: "description"` (field-by-field diff confirms only the DESC value changed). Extended `declared_pi_shipping_audit`'s CHECK A to scan `raw_tokens` too, running over EVERY `PI-REDACTED`/`description` record regardless of trigger (not only the `declared.description` subset) — 3 new mutation-proof tests. Added a narrow, declared exemption to `corpus_literal_sweep::compare_tokens` so the redaction marker is not itself flagged as a byte-mismatch (3 new tests proving the exemption is narrow and does not over-reach). `declared-pi-audit: CLEAN` and `corpus_literal_sweep: CLEAN` both confirmed against the real, fixed corpus. | *(the fix is landed and independently re-verifiable via `cargo run --bin declared_pi_shipping_audit`; flagged RULING-NEEDED only so the operator is aware this shape existed and reached 413 records before being caught — worth asking whether a similar raw_tokens/description split exists in any OTHER preserved free-text carrier this program has not yet audited)* |
| 64 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | Two SD-31 wave-4 lanes (`SD31-E5-F1-001` class_feature, `SD31-E6-F5-002` equipment_gap) each independently wrote a real content-carrying `data/corpus/<book>/` directory for 6 books (`ultimate_combat`, `ultimate_intrigue`, `occult_adventures`, `adventurers_guide`, `inner_sea_magic`, `inner_sea_taverns`) but never created a `LICENSE.json` compliance artifact for any of them. `tests/sd27_book_license_record_counts.rs`'s own `books_on_disk()` only enumerates books that ALREADY have a `LICENSE.json`, so this gap was invisible to that guard (silently excluded from its scope) rather than failing it — the records themselves WERE genuinely PI-screened inline at write time (both generators call the blacklist scan and the declared-PI reader on name and description), only the compliance artifact documenting that screening was missing. | Created all 6 `LICENSE.json` files with re-derived `records_processed`/`records_redacted` from disk and a screening note citing both lanes' inline PI-screening call sites. All 6 tests in `tests/sd27_book_license_record_counts.rs` pass with these books now included in scope. | *(informational — no ruling needed; worth a standing-gate enhancement so `books_on_disk()` fails loudly on a book with real records and no LICENSE.json, rather than silently excluding it, so this shape cannot recur invisibly)* |
| 65 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | Corpus-wide `cargo run --bin v06_corpus_trap_report -- --audit` at the merged, fixed tip: `TRAP_EXIT=2`, `1 0 mod-record; 0 1191 wiring-class-mismatch` — up from the last-reported baseline of 950 (`SD31-E6-F3-002`, row 22) / the original 1,040 (row 27). Per-kind breakdown of the 1191: `monster` 839, `spell` 243, `companion` 84, `monster_ability` 25 — **zero** from `class_feature` or `equipment`/`equipment_modifier` despite those being this wave's largest corpus deltas (12,417 + 697 new records), which argues against "more records this wave" as the direct cause. Not root-caused within this cycle's remaining budget: the `monster`/`companion` share plausibly correlates with `SD31-E6-F1-002`'s `MonsterStatBlock`/`companion_chassis` table widening (which did NOT regenerate `data/corpus/**/monster/*.json`, so a stored-vs-fresh `wiring_class` drift is plausible but unconfirmed) and `SD31-E6-F2-002`'s 881 spell citation repairs (which changed `source.line`/`raw_tokens` but not `wiring_class`, so a genuine drift there is also plausible but unconfirmed). | Ran the audit, reported the honest count and the per-kind breakdown rather than reusing the stale 950/1,040 baseline, and did NOT attempt a same-cycle root-cause given the remaining time budget. DoD item 3 stays the pre-existing documented shortfall it already was (row 27); this row updates the count rather than claiming improvement or silently absorbing the increase. | *(needs a dispatch decision: a future cycle should diff the `monster`/`companion`/`spell` wiring-class-mismatch population against the pre-wave-4 tip (`40771d3bf`) record-by-record to determine whether this is a genuine wave-4 regression in one of the two named lanes, or a population the fixed `book_dir_of`/sweep widening simply made newly comparable for the first time)* |
| 66 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | `root-lib`'s `equipment_resolver::tests::the_two_lookups_agree_on_every_catalog_key_but_the_one_pinned_collision` was ALREADY RED at this integration cycle's own inherited tip (`40771d3bf`), before any of the 5 wave-4 branches were merged — a pre-existing, pre-wave-4 defect, not introduced by this wave. The pinned 36-item collision list included 8 phantom entries (Amorphous, Burdenless, Exclusionary, Prehensile, Restful, Sneaky, Spiteful, Trackless) that do not actually collide: `acg/equipment_data/equipmods.rs`'s hand-authored row and `equipment_gap_tables.rs`'s gap row for each both cost 4500 gp, so the two lookups AGREE. | Corrected the pin to the true, reproducible 28-item disagreement set (isolated test run green). Root cause not further investigated (predates this wave, out of a bounded integration cycle's scope to trace to its origin) but the test is genuinely green now, not merely loosened -- verified the 28 remaining pinned entries are all still real KEY/NAME collisions with a real cost mismatch. | *(informational — no ruling needed, fix landed and verified)* |
| 67 | 2026-08-16 | SD31-W4-INTEGRATE-001 | NOTE | `derived_evaluator_fixture_check` reports 1 pre-existing failure at this cycle's tip: `advanced_players_guide:equipment:spindle_of_perfect_knowledge` -- corpus row states `BONUS:STAT\|INT,WIS,CHA\|4\|TYPE=Enhancement` but the evaluator produces no ability bonus at all. Confirmed pre-existing and unrelated to any wave-4 lane: the fixture entry dates to commit `7f70c45d1` (long before this wave), the corpus record's own `ingested_at` is `2026-08-03` (a `web_second_source` record with no `raw_tokens`/`ability_bonus` field at all), and no commit from `89846f5c9` to this cycle's tip touched `src/rules_core/equipment_effects/` or `magic_items.rs`. | Confirmed pre-existing via all three checks (fixture-entry date, corpus-record date, evaluator-code diff); did not attempt a fix (this record's `web_second_source` provenance has no raw LST citation to derive an ability-bonus field from without fabricating one). Still produces the `--json-out` report the guarded-regen chain needs. | *(informational — a future cycle should either find this item's real corpus row and re-ingest it with a proper citation, or exclude it from the fixture set with a named reason rather than leaving it a silent FAIL every regen)* |
| 78 | 2026-08-16 | SD31-E4-F1-001 | NOTE | **The `class_feature` explanation-id naming convention silently blocks its OWN classifier match, at scale.** `v06_work_inventory.rs`'s `Kind::ClassFeature` arm grounds a unit only when an `explanation_id` both contains `.{owner}.` and `id.ends_with(&feature_slug)` (`slug()` of the corpus feature name after `" ~ "`) — an EXACT suffix match, not a substring/contains check. `pilot_compute.rs`'s own established Slayer idiom (task #91 and earlier) names its ids `<feature_slug>_bonus`/`_count`/`_dc`/`_uses_per_day` — e.g. Track's real id is `class_feature.acg.slayer.track_bonus`, but the corpus feature is named `"Track"` (`feature_slug = "track"`), and `"track_bonus".ends_with("track")` is FALSE in Rust. Reproduced end to end: ran the guarded regen at this cycle's tip (`docs/work-inventory.json` restored after, per the wave rule) and confirmed **every one** of Slayer's already-computed magnitude features (Stalker, Track, Trap Sense, Trapfinding, Studied Target, Sneak Attack, Master Slayer, Slayer's Advance, Slayer Talents) still reads `status: not-ingested`/`not-started` despite `pilot_compute.rs`'s own diagnostic literally claiming *"now grounds every named feature on its corpus class table"* — `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); [print(u['corpus_key'],u['status'],u['wiring_class']) for u in d['units'] if u.get('kind')=='class_feature' and u.get('corpus_key','').startswith('Slayer ~ ')]"`. This card's own new `class_feature.acg.slayer.weapon_and_armor_proficiency` id was deliberately chosen suffix-free (`feature_slug` exactly, no appended word) and DOES ground — the one Slayer class_feature id in the whole file with no suffix. **Scale estimate, not a confirmed count:** `grep -oE '"class_feature\.[a-z0-9_.]+"' src/rules_core/pilot_compute.rs \| sort -u \| grep -icE '_(bonus\|count\|dc\|dice\|per_day\|uses\|penalty\|modifier\|total\|value\|amount\|die\|damage\|save\|resistance\|reduction\|range\|duration\|radius\|limit)"$'` → **173 of 453** distinct `class_feature.*` ids corpus-wide end in a magnitude-descriptor suffix word and are AT RISK of the same mismatch (not all 173 are necessarily broken — some corpus feature names genuinely end in these words too, e.g. `"Bomb Damage"` → `bomb_damage`; each needs checking against its own corpus feature name, not assumed). If even a fraction of the 173 are genuinely broken the same way Slayer's 9 are, this is a material, silent, systemic tax on the whole `class_feature` kind's board position across every class this program has ever wired, independent of and prior to any NEW chassis wiring a future cycle lands. | Documented and reproduced precisely; did NOT rename any existing id (renaming ~453 established, test-referenced, possibly UI-label-sensitive ids is a large, separate, high-blast-radius undertaking that deserves its own dedicated TDD pass, and `v06_work_inventory.rs`/its `classify()` function is out of this card's granted file territory — `pilot_compute.rs`/`archetype_resolver.rs`/class compute modules only). Chose a suffix-free id for this cycle's OWN new wiring specifically so it would not repeat the pattern. | *(needs a dispatch decision: a future cycle should (a) audit the 173-id scale estimate one id at a time against its real corpus feature name to get an exact broken count, then either (b) relax `v06_work_inventory.rs`'s `Kind::ClassFeature` match from exact-suffix to a scoped-but-looser check, or (c) rename the broken `pilot_compute.rs` ids to be suffix-free — (b) is lower blast radius and is the more likely right call, but is this card's owner's decision, not this cycle's)* |
| 79 | 2026-08-16 | SD31-E4-F1-001 | NOTE | **Decision 7's prose done-bar is mechanically unreachable for `class_feature`+`display` today, even with a perfect, on-screen-rendered engine wiring.** `scripts/observer/pf1e_dashboard_producer.py`'s `_doneness_verdict_uncapped` maps `wiring_class == "display"` to `DONE` only when `status == "text-complete"` — `status == "grounded"` maps to `HELD` instead (the function's own doc comment: `"grounded" is a different kind of disagreement... routing that contradiction to done... rewarded the classifier's blind spot`). But `v06_work_inventory.rs`'s `Kind::ClassFeature` arm can only EVER emit `status: "grounded"` (via a matching `explanation_id`) or `"not-ingested"` — the `text-complete` branch was deliberately removed from this exact arm by SD28-E24 (`decisions.md §40`/`§42` amendment, the module's own comment: *"text_only alone is not sufficient... absent a real holds-check... this branch can no longer manufacture text-complete either"*), because at the time no generic class_feature-holds-check existed. **That precondition has since changed and nobody re-opened the door:** `apps/desktop/src/characterHub/classFeaturesModel.ts` now generically renders ANY `class_feature.`-prefixed explanation id on the Class Features tab (`CLASS_RECORD_PREFIXES`), which IS a real, generic holds-check — the exact thing SD28-E24 said was missing. Reproduced directly by this cycle's own wiring: `Slayer ~ Weapon and Armor Proficiency` moved `not-ingested → grounded` (a real, DoD-8-proven, on-screen-rendered computation) but `doneness_verdict` still reports `held`, not `done`, because `grounded` is the wrong status word for `display`'s bar. This is the mechanical reason Decision 7's ruling (*"if they are prose only, nothing to compute, and the prose is available to print in the description... it counts as done"*) has not actually unlocked any of the 8,637 not-done `class_feature` zero-magnitude units it names, and will not unlock any more of them from wiring alone until this gap closes — it is a SEPARATE fix from row 69 above (row 69 is about an id NAMING mismatch inside the `grounded` path; this row is about `grounded` itself being the wrong terminal status for `display`'s done-bar). | Documented and reproduced precisely (measured, not asserted: guarded regen at this cycle's tip, `docs/work-inventory.json` restored after). Did not touch `v06_work_inventory.rs` or `pf1e_dashboard_producer.py` (both out of this card's granted file territory). Did not claim my 2 flipped units (`Slayer`/`Stygian Slayer ~ Weapon and Armor Proficiency`) as `done` in this cycle's own board-delta figure — reported the true, unchanged 7,603/38,521 headline and the honest `held`-shaped delta instead. | *(needs a dispatch decision, likely Epic 2's verdict-paths owner: re-open a scoped `text-complete` path for `Kind::ClassFeature` in `classify()`, gated on the SAME real holds-check `classFeaturesModel.ts` already proves — e.g. "a matching `explanation_id` exists AND the record's `magnitude_token_count == 0`" — so Decision 7's ruling can actually reach `class_feature`, without reopening SD28-E24's original over-claim (a name-substring match alone, no id-presence check at all))* |
| 80 | 2026-08-16 | SD31-E4-F1-001 | NOTE | **`class_feature_owner`'s suffix-only class match produces a genuine cross-archetype false positive, discovered by this cycle's own regen, not fabricated to demonstrate a hazard.** `v06_work_inventory.rs::class_feature_owner` matches a record's group prefix against a modelled class via `group == class \|\| group.starts_with(class+" ") \|\| group.ends_with(" "+class)`. `"Stygian Slayer"` (an ACG Slayer archetype's OWN group name) ends with `" slayer"`, so it resolves `owner = "slayer"` — which is CORRECT here (Stygian Slayer really is a Slayer archetype) and is why `Stygian Slayer ~ Weapon and Armor Proficiency` correctly flips to `grounded` alongside the base `Slayer ~ Weapon and Armor Proficiency` record once my shared explanation id exists (both verified via the guarded regen this cycle, `docs/work-inventory.json` restored after). But the SAME suffix rule also matches `"Skulking Slayer"` — a **Rogue** archetype from Advanced Race Guide, corpus-unrelated to the ACG Slayer class — which ALSO now reads `owner = "slayer"` and its own `"Skulking Slayer ~ Weapon and Armor Proficiency"` record ALSO flips to `grounded`, purely because both archetypes happen to end in the word "Slayer" and happen to share a feature named "Weapon and Armor Proficiency". Confirmed via the same regen diff: `advanced_race_guide:class_feature:skulking_slayer_weapon_and_armor_proficiency` moved `not-started → held` alongside my 2 intended units. This is a **name collision, not a book/subject check** — `class_feature_owner` has no way to see that "Skulking Slayer" is a Rogue thing, not a Slayer thing, because it only ever looks at the group-name STRING. Low material impact today (both land on `held`, not `done`, per row 70; and Skulking Slayer's OWN feature text still would not be quoted, since my code only fires for the character's actually-selected ACG Slayer archetype) but a real, verified classifier hazard the next `class_feature` cycle should know about before wiring a class whose name is a common English-word suffix of another class/archetype's name. | Documented and reproduced precisely; did not attempt a fix (`class_feature_owner` is `v06_work_inventory.rs`, out of this card's file territory, and a real fix needs book-scoping the match, not a quick patch). | *(informational — no ruling needed; worth a `class_feature_owner` book-scoping enhancement in a future `v06_work_inventory.rs` cycle so archetype-name suffix collisions across unrelated classes cannot silently share credit)* |
| 81 | 2026-08-16 | SD31-E6-F9-001 | NOTE | **Row 34's finding (1) re-derived one record deep, per-book, and only PARTLY confirmed.** Row 34 claimed 486 `monster_ability` `not-ingested` units (106 `advanced_class_guide` + 380 `core_essentials`) were corpus-shape-misclassified via `refine_kind()`'s `MONSTER_ABILITY_TYPE_FACETS`. Checking the `TYPE:` SECOND segment (not just the first) against the whole corpus's confirmed genuine-monster-ability vocabulary (`Extraordinary`/`Supernatural`/`SpellLike`/etc., the same vocabulary the const's own SD28-E15 doc comment already established for `core_essentials`) found: **`advanced_class_guide`'s 106 are 100% genuinely misclassified** (0 `NaturalAttack`/`Universal Monster Rule` rows anywhere in `acg_abilities_race.lst`; every facet-matching row is a Favored Class Bonus sub-choice table entry, e.g. `TYPE:SpecialQuality.ElfHunterCritialConfirmationChoice`) — but **`core_essentials`'s 380 are NOT misclassified** (100% genuine `Extraordinary`/`Supernatural`/`SpellLike`/`Universal Monster Rule` vocabulary, e.g. row 34's own cited examples "Aeon ~ Envisaging" `TYPE:SpecialQuality.Supernatural.Communicate` and "Aberration Traits Output" `TYPE:SpecialQuality.Extraordinary` are genuine creature-type traits, not player content). Corpus-wide re-scan (164 `_abilities_race*.lst` files across every `pathfinder/paizo` book, not just the 24 books currently carrying at least one `monster_ability` unit) found a THIRD, previously-unflagged book with the same defect: `ultimate_wilderness` (50 of its 52 units — every "Rogue Enemy ~ X"/"Bonus Druid Spell"/Favored-Class-Bonus-Output row; the 2 real exceptions, "Plant Traits"/"Leshy Traits", are genuine Bestiary-appendix creature-type traits and correctly stay). `retro.py correction` filed for the CE portion of row 34's claim. | **Fixed in `refine_kind()`/`MONSTER_ABILITY_TYPE_FACETS`** (`src/bin/v06_work_inventory.rs`), the exact file territory this card grants. New `is_player_favored_class_choice_row()` gates the `SpecialQuality`/`SpecialAttack` facets (not `NaturalAttack`/`Universal Monster Rule`, which had 0 false positives corpus-wide and stay unconditional) on TWO corpus-stated shapes, neither invented: the `TYPE:` second segment ending in the literal word `Choice` (PCGen's own naming for a Favored-Class-Bonus sub-choice table), or any field carrying the literal string `Favored Class Bonus` or the `FavClassBonus`-suffixed `DEFINE:`/`BONUS:VAR` variable convention (PCGen's own Favored-Class-Bonus-Output row naming) — both confirmed **absent** (`grep -c`, 0 hits) from `core_essentials` and every registered Bestiary. TDD: 6 new tests (`refine_kind_monster_ability_tests`), mutation-proved live (temporarily disabled the new gate, confirmed exactly the 3 positive-case tests go RED while the 3 regression-guard tests stay green, reverted). **Guarded regen, measured both directions:** `monster_ability` 3,107→2,951 (**-156**: 106 ACG + 50 UW), `race_trait` 3,447→3,603 (**+156**); board `done` **unchanged** at 7,603 (19.7373%, byte-identical before/after) — a pure kind-attribution correction, zero doneness impact either direction, satisfying Decision 1(a) by construction (the fix cannot move a number by lowering a bar, because it moves no `done`/`held` unit at all). `docs/work-inventory.json` restored per the wave rule, not committed. Row 34's finding (2), the "twin problem" (orphaned abilities whose owning monster is unmodeled in its own book's chassis), is untouched this cycle — still correctly blocked on Epic 6-F1's monster-ingest lever, not this card's file territory. |  |
| 82 | 2026-08-16 | SD31-E6-F9-001 | NOTE | **Corrects a dispatch-brief claim.** This card's brief states "PI-gate contract `forward-scope-register.md` G1.6 (6 registered monster books, zero declared-PI tokens today, dormant)". Re-derived directly against the pinned oracle (`grep -c "DESCISPI:YES\|NAMEISPI:YES"` over every currently-registered monster book's `*_abilities_race*.lst`, all 13 books with a shipped `data/corpus/<book>/monster_ability/` directory): **12 of 13 carry zero, but `bestiary_4`'s `b4_abilities_race.lst` carries 65** (all `DESCISPI:YES`, 0 `NAMEISPI:YES`) — every one on a `not-ingested` row belonging to a uniquely-named setting monster (Demon Lords Dagon/Pazuzu, Empyreal Lord Cernunnos, several Kaiju), confirmed by cross-referencing each flagged line's `KEY:` against `docs/work-inventory.json`'s `corpus_key`/`status` (all `not-ingested`, none shipped). **No live exposure**: cross-checked all 65 flagged corpus_keys against `data/corpus/bestiary_4/monster_ability/*.json`'s own `corpus_key` field — **0 matches**, confirming none of these specific declared-PI rows have been ingested yet. `retro.py correction` filed. | Not acted on further this cycle (no record exists to screen or redact — nothing shipped). Logged as a forward-scope landmine: whoever ingests `bestiary_4`'s remaining 225 `not-ingested` monster_ability rows (or any future book) MUST route through both SD-30 PI contracts (§52.3 blacklist + §53.5 declared-PI reader) on these 65 specific rows, or repeat the wave-3/wave-4 cleartext-PI failure this program has already been burned by twice. | *(no ruling needed — informational forward-scope warning for the next book-onboarding cycle that touches `bestiary_4`)* |
| 83 | 2026-08-16 | SD31-E6-F9-001 | NOTE | **Traced the dominant rung 1,293 `monster_ability` `held` units are short of, one record end-to-end** (`bestiary_2:monster_ability:aurumvorax_grab`, `static`/`grounded`): its shipped JSON carried a valid `source.kind:"lst_token"` citation but **no `data.raw_tokens` array at all** — and re-derivation found this true of **every one** of the kind's 1,629 shipped records corpus-wide (`python3` scan: 0 of 1,629 carry `raw_tokens`, confirmed before writing any fix). `corpus_literal_sweep`'s own population rule (`source.kind=="lst_token"` AND `data.raw_tokens` present) requires that field to ever promote a `static` unit's status to `literal-verified` — the ONLY status that reaches `done` for `static` — so this was silently capping the kind's `static`-grounded population at `held` forever, corpus-wide, for want of one field. | **Built `src/bin/enrich_monster_ability_raw_tokens.rs`** (`monster_ability`'s ingest-path counterpart to the already-landed `enrich_spell_raw_tokens.rs`/`enrich_equipment_raw_tokens.rs`), book-agnostic (walks every `data/corpus/*/monster_ability/` directory that exists, not a fixed book list — `monster_ability`'s `status` is decided by `monster_chassis::MONSTER_BOOKS` table membership, not a fixed enumerable catalog chain the way `spell` is). Reuses `corpus_literal_sweep`'s OWN `tab_tokens`/`token_closure` functions byte-for-byte (same code the verifier itself runs), never a typed re-parse. TDD, 9 tests. Ran for real against the pinned oracle: **1,616 enriched, 0 already-enriched, 13 citation misses** (all 13 are `ultimate_psionics`, a pre-existing, unrelated defect — Dreamscarred-Press paths are 3-segment before the filename, not the 4-segment `<system>/<publisher>/<line>/<book>` this tool's `book_dir_of` (deliberately duplicated byte-identical from `corpus_literal_sweep`'s own, per that binary's established precedent) assumes; same root cause already logged at row 46 for `corpus_literal_sweep` itself, honestly reported as a miss, not silently dropped). **PI-safety checked before AND after**: confirmed 0 of the 1,629 pre-existing shipped records were already `PI-REDACTED` (nothing to preserve), ran both `corpus_literal_sweep` (CLEAN, 0 findings, 19,422→21,038 records examined, +1,616 exactly matching the enrichment count) and `declared_pi_shipping_audit` (CLEAN) against the enriched tree before committing. `git diff` on a sample record confirms only the new `raw_tokens` key was added — every other field byte-identical (license/pi_field/pi_marker untouched; only JSON key ORDER differs, the same harmless `serde_json::to_string_pretty` re-serialization effect the two precedent tools already produce). **Guarded regen, measured: +102 `done`, -102 `held`** (`monster_ability` 336→438 `done`, 1,293→1,191 `held`; board-wide `done` 7,603→7,705, 19.7373%→20.0021%) — genuine, byte-verified progress, not a classifier or bar change. `docs/work-inventory.json` restored per the wave rule, not committed. | *(no ruling needed — fix landed, `derived`-class held units (223) still need `derived_evaluator_fixture_check` fixture coverage, a separate, larger lever named in this card's own text as Epic 6-F11 territory, not attempted this cycle)* |
| 68 | 2026-08-16 | SD31-W5-ORCH-001 (operator-raised) | BLOCKER | **Book attribution is wrong, and it is hiding finished work. Raised by the OPERATOR from the dashboard, not by any instrument:** *"race is at 0%, I don't see the core rules book listed under race, and advanced race guide reports as nearly untouched — we spent a lot of time on both."* All three observations are correct and share one cause. `src/rules_core/race_resolver.rs`'s own module doc, citing `decisions.md §25.2`, states the contract: *"A record's book is the corpus directory it was loaded from... `core_essentials/` appears only inside `RaceChassisRecord::source_path`, where it is the genuine read location, **and never as attribution**."* The contract is violated. `ls data/corpus/core_rulebook/race/` → 7 shipped Core Rulebook race records (dwarf, elf, gnome, half_elf, half_orc, halfling, human); `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(sum(1 for u in d['units'] if u.get('book')=='core_rulebook' and u.get('kind')=='race'))"` → **0**. They are labelled `core_essentials:race:*`. `core_essentials` is a PCGen **packaging** directory (`SD-18 decisions.md`: the CRB subtree carries 35 LST files in `core_rulebook/`, 40 in `core_essentials/`, plus ~60 per-race LST files under `core_essentials/races/<race>/`), not a book. That one bucket holds **51 races from many different books** — 7 CRB, ~37 Advanced Race Guide (aasimar, catfolk, changeling, dhampir, drow, duergar, fetchling, gathlain, ghoran, gillman, goblin, grippli, hobgoblin, ifrit, kitsune, kobold, merfolk, nagaji, orc, oread, ratfolk, rougarou, samsaran, skinwalker, strix, suli, svirfneblin, sylph, syrinx, tengu, tiefling, undine, vanara, vishkanya, wayang, wyrwood, wyvaran), plus android/kasatha/lashunta/triaxian from elsewhere — so **`advanced_race_guide` reports exactly ONE race unit**. Corpus-wide the same shape is large: `python3 -c "import json,sys,collections,os; sys.path.insert(0,'scripts/observer'); import pf1e_dashboard_producer as P; d=json.load(open('docs/work-inventory.json')); U=[u for u in d['units'] if u.get('book') not in P.EXCLUDED_BOOKS]; bad=collections.Counter(); n=0\nfor u in U:\n b,k=u.get('book'),u.get('kind')\n if b and k and not os.path.isdir(f'data/corpus/{b}/{k}') and u.get('status') in ('grounded','text-complete','ingested-magnitude','literal-verified','fixture-verified'): bad[(b,k)]+=1; n+=1\nprint(n, bad.most_common(12))"` → **4,007 units are `grounded` or better while their own `book`+`kind` has no `data/corpus/<book>/<kind>/` directory at all** (core_rulebook/equipment_modifier 676, bestiary/monster 326, ultimate_psionics/equipment 326, bestiary/monster_ability 323, ultimate_combat/feat 231, ...). Every one is unverifiable for a **labelling** reason, not a work reason, and the population maps onto the board's 5,596 `held` units. **Corroborating symptom:** the 6 races wave 2's Epic 1 chassis landed (Fetchling, Grippli, Ifrit, Oread, Sylph, Undine) and wave 3's Skinwalker all still read `status=not-ingested`, verdict `not-started`, under `core_essentials:race:*` — real capability work that could not have been credited even if perfect. **Why no instrument caught it:** every check in the chain agrees *with itself* — the inventory says `core_essentials`, the sweep looks for `core_essentials`, finds nothing, reports "not verified", which is true. Nothing ever asks *"the Core Rulebook has seven races, so why does the board say none?"* That needs outside knowledge of what the books contain, and no gate here holds it; even this program's rank-1 detector (ad-hoc commands over source data) was blind, because the source data was self-consistent. **NOTE — partially addressed, not fixed:** `SD31-E6-F3-002` (wave 4) fixed `corpus_literal_sweep`'s attribution enough to move `race` from 0 → 7 done. But `core_rulebook` still reports **0** race units and `core_essentials` still holds **1,610** units across 8 kinds at this tip, so the *label* defect is untouched and the dashboard the operator reads is still wrong. | Orchestrator recorded the finding with full derivation rather than dispatching a same-wave fix (wave 4 owned the primary checkout when it was raised). Dispatched as **wave 5's lead card**: assert the documented contract mechanically as a gate proven able to fail; re-attribute the 1,610-unit `core_essentials` bucket one record deep against the corpus so `core_essentials` ends with zero units; re-run `corpus_literal_sweep` and report how many of the 4,007 reach `literal-verified`/`done`; fix the dashboard's per-book panel if it needs the same correction. **This is a relabel, the exact shape Decision 1(a) forbids doing carelessly** — each unit's new book must be proven from where its record actually lives, with an Opus verifier attacking on precisely that basis. | *(no ruling needed — this is a defect to fix, recorded here because the operator raised it and will want to see it closed)* |
| 69 | 2026-08-16 | SD31-D7-PROSE-001 | NOTE | **Proxy validation (Decision 7's PROXY WARNING) found `wiring_class == 'display'` has a real, if small, false-positive rate on `magnitude_token_count == 0` records** — 9 of 40 hand-verified `display`-class draws in a 121-unit stratified sample carry a real, hand-confirmed numeric magnitude the classifier missed. Two distinct shapes: **(A)** a flat (non-scaling) numeric value stated only in an `SPROP:`/`BENEFIT:` field — `advanced_players_guide:equipment_modifier:special_ability_corrosive_weapon`'s only content is `SPROP:+1d6 acid damage`, already `status: text-complete`/`done` on the live board BEFORE this cycle (confirmed pre-existing, not introduced by this cycle); same shape in `mythic_adventures:feat:mythic_feat_output_elemental_fist` (`BENEFIT:` "increases to 1d8 points... %1 additional uses...per day"), `monster_codex:feat:coordinated_reposition` (`BENEFIT:` "move 5 feet"), `bestiary_2:monster_ability:devilfish_water_dependency` ("1 hour"/"2 hours"), `bestiary_4:monster_ability:colossus_pinning_stomp` ("twice that of its slam attack"), `core_rulebook:companion:strength_damage` ("1d6 points of Strength damage"), `ultimate_wilderness:companion:pufferfish_spines` ("1 point of piercing damage"), `core_rulebook:equipment:crossbow_heavy` ("-4 penalty", situational/conditional). **(B)** `wiring_class: static` specifically missing a Strength-bonus-scaled damage formula phrased without a "per level"-style trigger phrase: `core_essentials:race_trait:dragon_claw`/`dragon_tail_slap`/`dragon_wing`, each stating "deal[s] the indicated damage plus [a fraction of] the dragon's Strength bonus" — all 3 are `status: not-ingested` (no current over-claim). **Open interpretive question, not resolved here:** every one of the 9 states a *flat* value, never a *character-specific scaling* formula — `wiring_class::determine_closure`'s own `prose_scaling_phrases` design already treats "scales with a formula" as the bar for "something to compute", distinct from "a flat number is printed in the text". Whether Decision 7's "nothing to compute" means (a) no numeric value appears at all, or (b) no *character-specific* computation is owed, decides whether these 9 (and the unknown-sized wider population they sample) are really proxy failures or not — an operator-level call, not this cycle's to make. Full methodology, the complete 121-unit hand-labelled evidence file, and the regex pre-screen's own disclosed blind spot (it missed 2 of these 9 that the hand read still caught) are at `artifacts/SD31-D7-PROSE-001-proxy-validation.md` and `artifacts/SD31-D7-PROSE-001-proxy-sample-evidence.json`. | Reported precisely, with every record quoted and re-derivable, rather than either (a) silently trusting the proxy at the scale Decision 7's own ruling authorizes, or (b) unilaterally tightening `wiring_class.rs`'s prose-magnitude scan (out of this card's file territory — owned by a sibling this wave — and a real doctrine call about what "nothing to compute" means, not a bug fix). Confirmed the 9 findings do not affect anything this cycle shipped: the new `race_trait` rung and the description-completeness fix both operate strictly on TOP of the existing `wiring_class == 'display'` gate and never touch wiring-class assignment. | *(does Decision 7's "nothing to compute" mean no numeric value at all, or no character-specific scaling formula? Resolving this either confirms the `corrosive_weapon`-shaped 9 (and however many more exist corpus-wide) as real over-claims needing a `wiring_class.rs` fix, or confirms `wiring_class.rs`'s existing scaling-only bar is already correct and these 9 are fine as `display`/`done`. Either way, `wiring_class.rs` is a sibling's file this wave — not for this cycle to change unilaterally.)* |
| 70 | 2026-08-16 | SD31-D7-PROSE-001 | NOTE | `closure_has_real_description()` (new this cycle, `src/bin/v06_work_inventory.rs`) reads the raw `.lst` closure (base row + same-KEY `.MOD` rows) for a `DESC:` token — it does NOT consult the already-ingested corpus JSON's `data.description` field, which for a `.COPY=` equipment/equipment_modifier record can carry a REAL description the ingest pipeline resolved by inheriting from the record's base item (a fact the raw `.lst` closure alone cannot see, since the inheritance resolution happens at ingest time, not in the `.lst` text). Re-derived, comparing the fresh guarded regen's `text-complete`→`unknown` demotions (this cycle's own description-completeness fix, 1,060 units) against each demoted unit's real corpus JSON `data.description`: **247 of 1,060 (23%) genuinely DO have a real, non-empty description in the corpus JSON** (`equipment_modifier` 134/836, `equipment` 112/212, `spell` 1/1) that `closure_has_real_description`'s raw-`.lst`-only check missed — e.g. `core_rulebook:equipment:scale_mail`'s corpus JSON carries "Scale mail is made up of dozens of small overlapping metal plates..." but its own `.lst` row (a `.COPY=` variant) carries no `DESC:` token of its own. This is a **false-negative, the safe direction** (no unit is wrongly promoted; some are conservatively left `unmeasurable` instead of `done`) — deliberately not chased further this cycle given the scope already delivered; a future cycle can recover these 247 by having `closure_has_real_description` (or its caller) also consult the corpus JSON's `data.description` as a second source, keyed by the same `(source_file, source_line)` coordinate already used for the `.lst` join. | Shipped the conservative (under-claiming) version rather than the more accurate one, consistent with the standing rule that under-claiming is the safe failure direction and over-claiming is the one this program exists to prevent. Quantified the gap precisely rather than leaving it undiscovered. | *(no ruling needed — a well-scoped follow-up, not a decision; the exact recovery mechanism is named above)* |
| 71 | 2026-08-16 | SD31-D7-PROSE-001 | NOTE | **634-1,060 units were already `done`/`text-complete` on the live board with a `null`/empty corpus `description`** before this cycle (re-derived two ways: 634 against the committed `docs/work-inventory.json` tip at cycle start, 1,060 against the corpus's own fresh guarded regen at the fully-merged tip — the two differ because the committed tip lags the true corpus state, consistent with this package's own drift-between-snapshots pattern; the fresh 1,060 is authoritative). This is a severe, corpus-wide, already-shipped instance of exactly the anti-gaming violation Decision 7's condition 3 names explicitly ("A unit whose description is empty, placeholder, or unrendered is NOT done... marking one so is the anti-gaming violation Decision 1(a) forbids") — e.g. `ultimate_equipment:equipment:raft`/`musical_instrument_pan_pipes_masterwork`, `core_rulebook:equipment_modifier:a_1useme`, dozens of common CRB weapons (`scale_mail`, `axe_throwing`, `battleaxe`, `club`, ...) whose `.COPY=` corpus rows carry no `DESC:` token and no `cost_gp`/`weight_lbs` either — `completeness: "chassis_only"` husks with nothing at all for a player to see. Root cause: none of the pre-existing `text_only`→`text-complete` branches (Feat/Equipment/EquipmentModifier/Spell, `v06_work_inventory.rs`) ever checked that a description existed before granting the status — `magnitude_token_count == 0` (conditions 1/2) was checked; condition 3 was not. | **Fixed this cycle**, not merely reported (this is squarely this card's file territory — "the doneness rung for prose units in `v06_work_inventory`"). Added `closure_has_real_description()` + a `has_real_description` gate to every existing `text_only`→`text-complete` branch (Feat, Equipment/EquipmentModifier, Spell), TDD'd (8 new tests proving both the positive and 3 distinct refusal shapes: empty description, no rendered entry, magnitude-bearing). Guarded regen at the fixed tip: `done` **7,603 → 6,689 (net -914)** — **-1,060** units correctly demoted (this fix) **+146** units correctly promoted (row 72, the new `race_trait` rung) — reported as the correct, honest direction per the card's own "fewer units, honestly proven" standard, not smoothed over. See `progress.md`'s `SD31-D7-PROSE-001` receipt for the full guarded-regen command and numbers. | *(none needed — fixed, receipt is the record)* |
| 72 | 2026-08-16 | SD31-D7-PROSE-001 | NOTE | **Decision 7's done-bar had no rung at all for `race_trait`** (nor `class_feature`/`monster_ability`/`companion`) before this cycle — `Kind::RaceTrait`'s `classify()` arm only ever returned `grounded`/`not-ingested`, never `text-complete`, so a zero-magnitude race trait the race corpus genuinely applies (146 units, `display` wiring class, `magnitude_token_count == 0`) was capped at `held` by `doneness_verdict`'s `display + grounded → held` rule — correct in general (that rule exists because `grounded` for a magnitude-BEARING record can mean "the classifier missed a real formula"), but overly conservative for a record with no magnitude to have missed. | **Built the path**, scoped to `race_trait` only (the kind with an existing, already-wired, already-shipped render path — `apps/desktop/src-tauri/src/race_trait_picker.rs`'s `build_menu()`/`list_alternate_racial_traits` Tauri command). Reused the EXACT function the real screen calls (`RaceTraitRecord::render_description`) rather than re-implementing it: `RaceTraitProbe` (in `v06_work_inventory.rs`) now also records each loaded record's rendered description, and the `Kind::RaceTrait` match arm promotes `grounded`→`text-complete` when the unit is `text_only` AND the SAME render call the picker uses produces real, non-empty text. TDD: 3 new tests specifically proving the rung REFUSES (empty rendered text, no rendered entry at all, magnitude-bearing record), plus 1 proving the positive case against the REAL corpus (`advanced_race_guide:race_trait:feral_languages`, not a synthetic fixture). Guarded regen: **+146 race_trait units** newly `done`, all with evidence `race_trait_applied_by_the_race_corpus_and_rendered_with_real_text`. DoD-8: `core_essentials:race_trait:aasimar_agathion_blooded` ("Agathion-Blooded (Idyllkin)") screenshotted on the real character-creation screen showing its real corpus DESC text verbatim (`artifacts/SD31-D7-PROSE-001/aasimar-agathion-blooded-text-complete.png`). `class_feature`/`monster_ability`/`companion` remain unbuilt — `class_feature` in particular has NO generic catalog anywhere in this engine (confirmed by direct source search; `apps/desktop/src-tauri/src/` has `feat_catalog.rs`/`equipment_catalog.rs`/`spell_catalog.rs`/`race_catalog.rs`/`companion_catalog.rs`/`monster_catalog.rs`/`class_catalog.rs` but no `class_feature_catalog.rs`) — building one is a genuinely large, separate epic, not a same-cycle extension. `companion_catalog.rs` already has comparable description-rendering + PCGen-syntax-leak-checking infrastructure (`serve_ability_description`) and is the next-cheapest target (~223 zero-mag `grounded` companion units in the same shape as the 146 race_trait ones, re-derived this cycle: `python3 -c "..."` → 223 `companion` units, `magnitude_token_count==0`, `status=='grounded'`). | *(none needed — informational; names the concrete next-cheapest target and the two genuinely-hard ones for a future cycle)* |
| 73 | 2026-08-16 | SD31-ATTRIB-001 | NOTE | **Row 68 fixed for `race`/`race_trait`/`companion`/`spell`/`class`/`feat`/`equipment`; a genuine residual remains, correctly documented rather than guessed at.** Gate built first and proven able to fail: `v06_work_inventory.rs`'s new `core_essentials_book_attribution_tests` module seeds a synthetic Dwarf race row under a scratch `core_essentials/races/dwarf/`; reverting the fix (`resolve_true_book_for_core_essentials` returning `None` unconditionally) makes 3 of the 6 tests fail immediately with `book: "core_essentials"` instead of the true book (reproduced live before committing the fix). **Repair.** `v06_work_inventory.rs`'s `enumerate_file` now resolves each `core_essentials`-sourced unit's TRUE book via two provable, one-record-deep signals: (1) per-race files (`races/<slug>/`) via a hand-derived `RACE_TRUE_BOOK` table, re-checked against every in-scope book's own `.pcc` (not row 68's own prose list — see correction below); (2) root-level `ce_*.lst` files via their own file-header `SOURCELONG:` token, cross-checked against every in-scope book's own real files' `SOURCELONG` (`ce_races_familiar_cr.lst` → `SOURCELONG:Bestiary` despite its `_cr` filename suffix — the header is trusted, never the name). Duplicated the same `RACE_TRUE_BOOK` table into `corpus_literal_sweep.rs`'s `short_book_of` (this repo's established convention for `book_dir_of`-shaped logic across bins), and fixed `enumerate_book`'s dead `scope == "shared_library"` branch (`RuleSetId::Ce`'s legitimate existence for companion-engine consumers had silently made `core_essentials` report `"in_scope"` instead). **Two real regressions caught and fixed BEFORE landing, both because `unit.book` was being read for two different purposes that had silently diverged:** (a) `token_closure_rows`'s physical-file lookup and (b) `classify()`'s engine-consumer-table lookup (`engine_book_for`) both used to key on `unit.book`, which is safe only when `book == the directory actually walked` — true everywhere except my own fix's re-attributed units. Without a fix, re-attributing Dwarf's `book` to `core_rulebook` silently broke `CorpusLines::line("core_rulebook", "dwarf_races.lst", ...)` (the file physically lives under `core_essentials/`) and re-attributing companion units to `bestiary` broke `facts.chassis_companion_keys.contains_key("bestiary_1")` (the real, working companion table is registered under `"core_essentials"`, per `RuleSetId::Ce`) — both downgraded already-`grounded`/`literal-verified` units. Added `CorpusUnit::source_book` (always the walked directory) and rewired both call sites to it; `unit.book` is now purely the reporting field. **Re-derived, not transcribed: guarded regen (local, uncommitted per the wave rule) shows ZERO `doneness_verdict` transitions in either direction** (`python3` diff of every unit id's verdict, before vs. after — 0 lost `done`, 0 gained, 0 transitions of ANY kind), proving the fix is a pure relabel with no side effect on doneness, as it must be. `core_rulebook` race: 0→**7** (all 7 done). `advanced_race_guide` race stays at 1 (done 0) -- **correctly**, not a residual bug: ARG reprints OTHER books' race chassis, it does not own any (`decisions.md §25.2`); the operator's "nearly untouched" read is answered by `race_trait`, ARG's genuine own contribution, unaffected by this fix and already 323 units / 198 done both before and after. `core_essentials`'s own residual: **1,610 → 634** (race_trait 884→249, monster_ability 380→378, companion 145→0, spell 109→0, class 23→0, feat 15→0, equipment 3→0, race 51→7) — the remaining 634 are genuinely unattributable: `ce_abilities_race.lst`'s 1,637-row PCGen-internal Size/Vision/Universal-Monster-Rule reference table (no `SOURCELONG`, confirmed book-agnostic by its own in-file comment) covers 378+249=627 of the 634; the other 7 are races 2+ in-scope books natively declare (Android, Aquatic Elf, Ghoran, Goblin (Monkey), Lashunta, Syrinx, Triaxian) with no single provable true book. **Correction to row 68's own prose (`retro.py correction`, `SD31-ATTRIB-001`).** Row 68 named Gathlain, Ghoran, Rougarou, Skinwalker, Syrinx, Wyrwood, Wyvaran as "~37 Advanced Race Guide" races. `grep -A60 'RACE:arg_races.lst' .../advanced_race_guide/advanced_race_guide.pcc` shows ARG's own `.pcc` reprints EXACTLY 37 races across 6 labelled sections (7 CRB + 11 B1 + 7 B2 + 5 B3 + 5 B4 + 2 ISWG) and NONE of those 7 names appear in it — they are natively declared by `bestiary_4`/`bestiary_5`/`bestiary_6`/`inner_sea_bestiary`'s own uncommented `.pcc` race sections instead (confirmed one file deep each), not ARG reprints at all. The arithmetic (37 total) was right; the roster was not. **Corroborating case, answered precisely.** Fetchling/Grippli/Ifrit/Oread/Sylph/Undine now correctly read `book: bestiary_2` and Skinwalker `book: bestiary_5` (was `core_essentials` for all 7) — but their own RACE CHASSIS `status` stays `not-ingested`: this fix corrects the LABEL, it cannot and does not retroactively invent a `done` status the underlying corpus/engine state has not earned (confirmed: 0 verdict transitions, above). The credit these 7 races' wave-2/3 work DID already earn shows up in `race_trait`, not `race`: `bestiary_2` race_trait `done` 0→29, `bestiary_5` race_trait `done` 0→6, `bestiary` race_trait `done` 0→91, `core_rulebook` race_trait `done` 0→46 — real, previously-earned, previously-invisible-under-the-wrong-book credit, now correctly attributed (all already counted in the board's 7,603 `done` both before and after — this is where it was HIDING, not new work). **Row 68's own `~4,007` figure does not reproduce at this cycle's tip** (re-derived with row 68's own one-liner: **3,550** before this fix) — re-run at HEAD before trusting a number from a prior receipt, per standing program discipline; not investigated further here since it is an informal diagnostic, not the authoritative `doneness_verdict()` measure this row's own 0-transition proof already answers with. **New, surfaced but pre-existing, NOT introduced by this fix:** re-attributing companion/race_trait content to `book: "bestiary"` newly exposes that `data/corpus/bestiary/` does not exist on disk at all — the shipped directory for Bestiary 1 is spelled `beastiary` (misspelled), a DIFFERENT string from the `"bestiary"` id `v06_work_inventory.rs`/`corpus_literal_sweep.rs` use everywhere else (`corpus_dir_for(RuleSetId::Bestiary1) => "bestiary"`, matching the 951 pre-existing `bestiary`-labeled monster units already in the board). Row 68's own informal `os.path.isdir(f'data/corpus/{book}/{kind}')` diagnostic newly flags 239 `grounded` bestiary-labeled companion/race_trait units for this reason (172 race_trait + 67 companion) — genuinely dormant before this fix because almost no bestiary-attributed companion/race_trait population existed to trip it. **Not real doneness regressions** (covered by the same 0-transition proof), and NOT fixed here: the `bestiary`/`beastiary` spelling divergence is a separate, pre-existing defect this fix surfaced rather than created, touching a naming convention used far beyond this card's file territory. Dashboard producer's per-book panel (`scripts/observer/pf1e_dashboard_producer.py`, `work_inventory_panel()`) updated: its 2026-08-10 "1,595 units unique to core_essentials" rationale for keeping the book un-excluded is corrected in place (now 634, with the fuller derivation and a pointer to this row), `EXCLUDED_BOOKS` itself left unchanged (`core_essentials` stays un-excluded — a genuine, real residual remains, and hiding it would repeat exactly the mistake the 2026-08-10 reversal was written to prevent). No player-visible reach claim changes (pure relabel of an internal reporting field, 0 doneness transitions) — DoD-8 on-screen verification is not applicable; the operator-facing surface this card exists to fix is the dashboard panel itself, verified by direct `work_inventory_panel()` invocation against the locally-regenerated (uncommitted per the wave rule) inventory rather than a desktop-app screenshot. | *(needs a follow-up dispatch: root-cause and fix the `bestiary`/`beastiary` spelling divergence corpus-wide -- likely a rename of the shipped `data/corpus/beastiary/` directory to `bestiary`, touched carefully given its reach across the ingestion/cache_gen/dashboard surfaces; separately, a future book-onboarding cycle should extend `SOURCELONG_TO_BOOK` if a root-level `core_essentials/ce_*.lst`-sourced record is ever shipped to `data/corpus/` before `short_book_of` gains oracle-file-content access to resolve it)* |
| 74 | 2026-08-16 | SD31-E6-F2-003 | NOTE | **A book with no compiled `RuleSetId` cannot benefit from ANY ingest, discovered via this cycle's own before/after guarded regen, not assumed.** Chained `occult_adventures::spell_list::SPELL_LIST` (144 records) into `spell_resolver::spell_catalog_rows()` exactly as `SD31-E6-F2-002` did for `ultimate_magic` — and the board measured **zero** movement (`spell` `not-started` unchanged, all 473 OA spell units still `no_compiled_rule_set_for_book`). Root cause: `v06_work_inventory::classify()`'s FIRST gate, before any per-kind arm runs, is `engine_book_for(unit.book)` = `rule_set_for(book_dir).map(rule_set_id)` — a book with `rule_set_for(book_dir) == None` short-circuits every one of its units, of EVERY kind, to `not-started`/`no_compiled_rule_set_for_book`, before the `Kind::Spell` arm's own `spell_levels` lookup ever runs. `ultimate_magic` already had `RuleSetId::Um` from an unrelated, EARLIER feat-catalog cycle (SD-28 Epic 28); `occult_adventures` had never had any prior lane touch it, so no variant existed and this gate was silently binding on the whole book. | Added `RuleSetId::Oa` (`src/rules_core/rules_tables/mod.rs`) + its `corpus_dir_for`/`rule_set_id` arms + `COMPILED_RULE_SETS` registration (`src/bin/v06_work_inventory.rs`) — this book's first compiled rule set of any kind. Re-derived guarded regen after the fix: `spell` `not-started` for OA 473→329 (144 moved: 143 `held`, 1 `in-progress`), board `held` 5596→5740 (+144), board `in-progress` 786→787 (+1), board `done` **unchanged** (see row 70 for why). Swapped the now-invalidated `uncompiled_books_stay_none` test's premise from `occult_adventures` to `adventurers_guide` (re-derived: genuinely has no `RuleSetId` arm, has a real `data/corpus/` directory). Fixed 3 downstream exhaustive-match compile failures this addition surfaced (`src/bin/v06_content_state_dump.rs`'s feat-table book-id match, both `corpus_ingest_diagnostic.rs`/`reach_gate.rs` structural gates — see row 70). | *(none — informational. Any future FIRST-ever ingest into an un-ingested book must budget for this same fixed cost; `SD31-E6-F2-002`'s UM cycle did not have to pay it only because an earlier, unrelated cycle already had.)* |
| 75 | 2026-08-16 | SD31-E6-F2-003 | NOTE | Landing `RuleSetId::Oa` (row 69) surfaced two structural gates working exactly as designed, both `desktop` test failures: `corpus_ingest_diagnostic::tests::every_book_landed_in_rules_tables_is_reported` ("occult_adventures" landed in `rules_tables/` with no `book_status(..)` row) and `reach_gate::tests::{unsurfaced_families_are_exactly_the_recorded_findings, every_ingested_family_is_accounted_for}` ("occult_adventures/spells" ingested with no `reach_of` claim and no `OPEN_FINDINGS` entry). Per `docs/governance/book-ingestion-playbook.md` §3, "ingest AND surface are ONE unit of work" — these are not defects, they are the gate refusing an ingest with no proven reach. | Added `occult_adventures_counts()` + its `book_status(..)` row (`corpus_ingest_diagnostic.rs`) and a `("occult_adventures", "spells") => spells_reach("OA", ...)` arm (`reach_gate.rs`), the identical pattern UM/UI/ARG already use — no `OPEN_FINDINGS` entry needed, a real claim was built instead. DoD-8 on-screen proof: `docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E6-F2-003/item8/spell-akashic-form.png` — the live Spell Catalog screen, OA filter chip "OA (144)", "Akashic Form OA Necromancy · Level 9 · Store a copy of your body in the Akashic Record..." rendering from the real corpus record, `verify-on-screen.sh` PASS. | *(none — fixed this cycle; the mandatory shape every future book-onboarding cycle should expect, not a one-off)* |
| 76 | 2026-08-16 | SD31-E6-F2-003 | RULING-NEEDED | **Traced the `spell` `held` population (1,240 units) end to end, per this cycle's dispatch.** `python3` filtering `docs/work-inventory.json`: 91% (1,127 of 1,240) are `(wiring_class=derived, status in {ingested-magnitude, grounded})`. For `derived` wiring_class, `pf1e_dashboard_producer.doneness_verdict` requires a `fixture-verified` stamp (from `derived_evaluator_fixture_check`) to ever reach `done` — `ingested-magnitude`/`grounded` alone stay `held` permanently. `src/rules_core/derived_evaluator_fixture_check.rs` has **zero evaluator seams for `kind == spell`** at all (only `monster`'s `spell_like_ability_caster_level` and an equipment ability-bonus evaluator) — this is a missing CAPABILITY, not a cheap instrument gap: a real spell-formula evaluator would need to parse PCGen's `DESC:`-embedded arithmetic (`%N`-substituted caster-level formulas, `CASTERLEVEL=`/`TIMES=` scoped clauses) and reproduce it independently of the corpus text it's checking against — the same shape of work `SD31-E6-F11-002`/`SD31-E6-F1-002` did for monsters' one narrow SLA-caster-level rule, but spell `DESC:` formulas are far more varied (damage dice, DC formulas, duration formulas, area formulas) and no single evaluator function would cover more than a slice of the 1,127. This cycle's own 144-record OA ingest contributes 0 to this population moving (its own `static`/`derived` split lands `held`, not `done`, for the identical reason). The remaining 9% (113 units: 36 `static`+`ingested-magnitude`, 32 `ambiguous`+`grounded`, 19 `derived`+`text-complete`, others) are smaller and mostly blocked on the SAME missing `data/corpus/<book>/spell/*.json` dump for `ultimate_magic`/`occult_adventures` named in row 72 rather than a `derived`-evaluator gap. | Did NOT attempt to build a spell-formula evaluator this cycle (out of budget for a genuine new capability, not a stub-avoidance excuse — no partial/fake evaluator was written). Named the population's true shape (91% derived/no-seam) precisely rather than reporting "held, cause unknown" or quietly declaring it out of scope. | *(needs a dispatch decision: (a) a dedicated future cycle to build a bounded spell-formula `derived_evaluator_fixture_check` seam — likely scoped to ONE common formula shape at a time (e.g. `CASTERLEVEL`-scaled damage dice, the largest single sub-family), same incremental-seam precedent `SD31-E6-F11-002` set for monsters; or (b) confirm this is a genuine, large, multi-cycle capability gap worth a standing epic rather than a per-book residual note.)* |
| 77 | 2026-08-16 | SD31-E6-F2-003 | NOTE | **The `static`-slice done-lever (`literal-verified` via `corpus_literal_sweep`) is structurally unavailable for `ultimate_magic` and `occult_adventures` specifically, traced one level deeper than `SD31-E6-F2-002`'s own row 57 left it.** `src/bin/enrich_spell_raw_tokens.rs` populates `data.raw_tokens` on shipped `data/corpus/<book>/spell/*.json` records so `corpus_literal_sweep` can byte-compare them — but its `TARGET_BOOKS` const (own doc comment: "enriching a sixth book's spell records would add real, correct data that no `wiring_class`/`status` path can ever promote to `done`, since that book is absent from `spell_catalog_rows()` entirely") was never widened past the original 5 books, and separately — the deeper finding — **`data/corpus/ultimate_magic/spell/` and `data/corpus/occult_adventures/spell/` do not exist at all**: `ls` confirms zero shipped JSON records for either book's spell family. `enrich_spell_raw_tokens.rs` only ENRICHES existing corpus JSON; it cannot create it. The missing piece is a `cache_gen`-shaped generator (`gen_book_cache.rs`'s own `spell_written`/`arg_find_citation_line` pattern is the closest precedent) that would dump `SpellListEntry` → `data/corpus/<book>/spell/*.json` with a real `source.kind=="lst_token"` citation for each record. `cache_gen` modules are explicitly out of this cycle's file territory (`SD31-E6-F2-003`'s dispatch: "You may NOT edit ... the cache_gen modules"). | Did not build the missing generator (out of file territory — reported per the dispatch's own instruction to report rather than edit across a boundary) and did not widen `TARGET_BOOKS` (would be a no-op without the generator: `enrich_spell_raw_tokens` would scan 0 files for both books, confirmed by the missing directories above). Named the precise missing piece rather than declaring the `static` lane blocked in general. | *(needs a dispatch decision: which lane owns `cache_gen`/`gen_book_cache.rs` should build a `spell`-kind corpus-JSON dump for `ultimate_magic`/`occult_adventures`, mirroring `cache_gen::ultimate_equipment`'s "dump a shipped corpus table to JSON" shape — this is the SAME lever `SD31-E5-F1-001` proved does NOT generalize to `class_feature` but DOES apply here, since `spell`'s classify() arm reads `spell_levels`, itself sourced from the already-compiled `SpellListEntry` tables, not from `data/corpus/`.)* |

| 84 | 2026-08-16 | SD31-E6-F5-003 | NOTE | **Audit finding, informational.** Traced one held `equipment` unit (`ultimate_psionics:equipment:amulet_of_catapsi`) end to end (card item 3's instruction) and found the SAME `OPEN-ISSUES.md` row 11/row 12 shape (an already-compiled, oracle-verified per-book `equipment_tables()` module that was never dumped to `data/corpus/`) existed for **four more books**: `rules_tables::{ultimate_psionics,ultimate_combat,ultimate_intrigue,ultimate_magic}::equipment_tables` — 326/185/91/18 real, `equipment_resolver::equipment_catalog_rows()`-chained (already player-visible) records apiece, none dumped. Fixed this cycle (`cache_gen::hand_authored_equipment`, `SD31-E6-F5-003`); see `progress.md`. **Audited the remaining book roster before closing this row, not left to a future cycle to re-discover:** `core_rulebook` (2,993 files, nested `arms_armor`/`equipmods`/`magic_items`/`general` layout), `advanced_players_guide`/`advanced_class_guide`/`beastiary1` (own `cache_gen` modules), `advanced_race_guide`/`pathfinder_unchained` (1 and 42 files respectively, both non-zero) all already have SOME `equipment/` root-level content — none show the "zero files, only an `equipmods/` subdirectory" signature this row's four books had. No further same-shape gap found for `equipment`/`equipment_modifier` at this tip. | Built and landed the dump for the 4 found books (620 records). No action needed on the audited-clean remainder. | *(informational — no ruling needed)* |

| 85 | 2026-08-16 | SD31-E6-F5-003 | BLOCKER | **DoD-8 on-screen verification not completed this cycle.** `SD31-E6-F5-003` landed 620 new real, player-visible equipment records (`equipment_resolver::equipment_catalog_rows()` already chains `rules_tables::{ultimate_psionics,ultimate_combat,ultimate_intrigue,ultimate_magic}::equipment_tables` — these records were ALREADY wired into the desktop equipment catalog before this cycle, only their `data/corpus/` JSON cache was missing), so a screenshot proving one renders on-screen is owed. `apps/desktop/.claude/skills/run-desktop/SKILL.md`'s own binding rule — "Do not run `driver.sh launch` and `scripts/verify.sh` at the same time — serialize them" (22 GiB RAM / zero swap; a concurrent cargo build OOM-kills vite at `beforeDevCommand`) — and this cycle's own full gate (launched early per protocol, `docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E6-F5-003-verify.log`) still building its `root-full` stage at the time this cycle's turn budget ran out, together meant `driver.sh` could not be safely run this cycle. Exact command that would have run: `RUN_DESKTOP_AGENT=sd31-equip-residual apps/desktop/.claude/skills/run-desktop/driver.sh launch` then navigate to the equipment catalog, search for a newly-ingested item (e.g. "Amulet of Catapsi", Ultimate Psionics), screenshot. | Landed everything else (code, 620 corpus records, the row-61 root-cause fix, LICENSE.json updates, the guarded-regen measurement) and logged this row rather than faking the screenshot or silently dropping the requirement, per the mandate's "ran out of budget is not blocked" clause. | *(the next cycle to touch this branch should run the DoD-8 screenshot once the gate is confirmed clean — no code change needed, purely a verification step)* |

## Resolved

| # | Opened | Closed | Cycle-id | Issue | Resolution |
|---|--------|--------|----------|-------|------------|
| 1 | 2026-08-15 | 2026-08-15 | SD31-E2-F2-001-wiringfix | `wiring_class::CorpusLines::line()`'s single-level `dir.join(file)` join missed every nested `.lst` file, stranding ~1,707 corpus-real units in `ambiguous:no_corpus_line`. | Fixed: `resolve_corpus_file()` tries the direct join first (unchanged for every currently-resolving unit — proven both structurally and by an empirical check against 36,833 currently-resolving units, zero mismatches), then a bounded (≤3-level) search under the book directory, refusing to guess (`None`) on a within-book basename collision (zero found corpus-wide by enumeration, tested). Re-derived post-fix: `no_corpus_line` population is **0** (was 1,707); the `ambiguous` wiring-class population dropped **2,109 → 409** (corpus-wide guarded regen, not committed per the wave rule — see `progress.md` `SD31-E2-F2-001-wiringfix` receipt for the full before/after and the reachable-ceiling movement, 94.53% → 98.94%). 6 tests in `src/rules_core/wiring_class.rs`, commit `d07d41b5c`. |
| 2 | 2026-08-15 | 2026-08-15 | SD31-E2-F2-001-wiringfix | `wiring_class::signals()`'s scalar/arithmetic scan read the whole `MAGNITUDE_TOKENS` field value: (a) `BONUS:STAT\|<ability>\|<literal>` false-positived `derived` on the ability-SELECTOR; (b) `has_arith`'s unconditional `value.contains('/')` false-positived on `DR:`/`CR:` bypass/fraction notation. | Fixed: `strip_stat_selector()` excludes the STAT selector segment before the scan; `has_arith_scoped()`'s `allow_slash` disables `/`-as-division for `CR:`/`DR:` specifically (corpus-wide re-derivation: every `DR:`/`CR:` value carrying a `/` matches one of the two non-arithmetic shapes; a real multiplicative DR still signals via its own `*`). Validated against the row's own worked examples (movement confirmed in the intended direction on all 4: `core_rulebook:race_trait:2_dexterity`, `ultimate_equipment:equipment:staff_of_mithral_might`, `bestiary:monster:neothelid` → `static`; `core_essentials:companion:pig` stays `derived`, proving the fix does not over-correct) and against the F1 ground-truth sample's 45 genuinely-evidenced units (40/45 post-fix agreement; see row 9 for the 5 documented, out-of-scope disagreements). 8 tests in `src/rules_core/wiring_class.rs`, commit `d07d41b5c`. |
| 3 | 2026-08-15 | 2026-08-15 | SD31-E2-F1-002 | `SD31-E2-F1-ground-truth-sample-v1.json`'s `token_evidence` field was a single byte-identical canned string for 105 of 150 units, quoting zero tokens from the record — including all 40 `display_grounded_target` units `AT-31-010` binds into Epic 2's acceptance. | **Fully resolved.** All 105 re-labelled from the actual corpus record (base row via a recursive search + full `.MOD` closure, real quoted tokens verified byte-for-byte against the corpus text before being written). Outcome: 103 confirmed the engine's existing verdict with genuine evidence; 2 corrected it (Findings D/E, row 10). `python3 -c "import json; d=json.load(open('docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E2-F1-ground-truth-sample-v1.json')); BOIL=\"confirmed from the unit's full token closure\"; print(sum(1 for r in d if r['token_evidence'].startswith(BOIL)))"` → **0**. Also built `scripts/ground_truth_evidence_guard.py` (+ passing self-test suite) so this exact defect shape cannot recur silently — see row 15 for why it is not yet wired into `verify.sh` by default, and row 14 for the smaller residual gap the guard found in the untouched 45. |
| 4 | 2026-08-15 | 2026-08-15 | SD31-E2-F1-002 | Neither the v1 sampling script (`sample_units.py`, `random.seed(31)`) nor its evidence-extraction scripts were ever committed — the v1 draw could not be re-run or audited. | **Resolved for every future draw; the v1 draw itself remains permanently non-reproducible — this cycle did not, and could not, fix that half.** Committed `scripts/sample_ground_truth_units.py` (real, seeded, stratified, verdict-free — proven by `scripts/tests/test_sample_ground_truth_units.py`, 5/5 green) and used it for `SD31-E2-F1-002`'s own 35-unit widening draw (`--seed 31 --target-per-cell 2`, fully reproducible and re-run by anyone from the recorded invocation). A reconstruction of the original v1 draw was deliberately NOT attempted — it could not prove the original was unbiased, and presenting one as the original would be worse than admitting the gap; `SD31-E2-F1-ground-truth-methodology.md`'s "How units were sampled" section states this plainly. |
| 5 | 2026-08-15 | 2026-08-15 | SD31-E2-F1-002 | The sample's `(hand_wiring_class, kind)` stratification was thin against SD31-E2-F2's per-class-AND-per-kind acceptance criterion: 45 occupied cells, 31 with `n<=2`. | **Improved, not fully resolved — reported honestly, not oversold.** Drew and hand-labelled 35 more units via the newly-committed sampler (row 4), targeting the thinnest cells. Re-derived against the resulting 185-unit file: `python3 -c "import json,collections; d=json.load(open('docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E2-F1-ground-truth-sample-v1.json')); ct=collections.Counter((r['hand_wiring_class'],r['kind']) for r in d); print(len(ct), sum(1 for v in ct.values() if v<=2))"` → **48 occupied cells, 29 with n<=2**. Root cause of the modest gain: the sampler can only stratify a pre-draw by `engine_wiring_class` (hand labels don't exist until read), and this cycle's own high correction rate (13/35 widened units disagreed with the engine) meant several draws landed in a different cell than intended once hand-labelled. F2 still must not report a rate for any `n<=2` cell; `SD31-E2-F1-ground-truth-methodology.md`'s "Stratification actually achieved" section carries the full honest picture and recommends a hand-label-aware iterative re-draw as the next step. |
| 7 | 2026-08-15 | 2026-08-15 | SD31-E2-F2-001-wiringfix | `scripts/verify-baselines.env` carried 3 stale counters. | Updated as its own self-contained commit (`e219fed2f`), AFTER the code commit, from `--show-actuals` output measured on the green gate that verified `d07d41b5c`: `BASELINE_ROOT_LIB_TESTS` 1776→1789, `BASELINE_ROOT_FULL_TESTS` 6398→6423, `BASELINE_ROOT_TEST_BINARIES` 547→549. Fully attributed: the row's own already-measured +1/+13/+1 pre-existing drift, plus this cycle's own +12/+12/+1 (12 new `wiring_class.rs` lib tests, 1 new real-corpus-gated integration test file). |
| 8 | 2026-08-15 | 2026-08-16 | SD31-PI-REPAIR-001 | Row 38: `cache_gen::ultimate_equipment.rs` computed `DeclaredProductIdentity.name` via `declared_pi_at` but only ever passed `declared.description` into `pi_screening::classify_optional_field_declared` — the `.name` half was silently discarded, so `Otyugh Hide` (`ue_equip_arms_armor.lst:66`, `NAMEISPI:YES`) shipped its real name unredacted in `data/corpus/ultimate_equipment/equipment/otyugh_hide.json`. Row 38's own text also carried a "2 shipped records" claim (Otyugh Hide + Elysian Shield) that a full corpus-wide re-derivation found overstated — see row 46 above for what Elysian Shield's citation actually is. | **Fixed, TDD'd, and re-dumped.** A `NAMEISPI:YES` row is now DROPPED before the description screen runs (matching `ingest_race_traits.rs`'s and `SD-29-corpus-wide-catch-up-lanes/decisions.md §50.3`'s ruling: "a key cannot be redacted, so PI rows are dropped rather than screened" — a name is the record's identity, not a redactable field). `generate_equipment()` now also clears `equipment/` before each run so a dropped record cannot linger as a stale on-disk file across regenerations (the exact shape that would otherwise have left `otyugh_hide.json` in place even after the code fix). 3 new tests in `src/rules_core/cache_gen/ultimate_equipment.rs` (`nameispi_yes_drops_the_record_instead_of_publishing_the_real_name`, `without_the_declaration_the_same_row_ships_normally`, `a_dropped_record_does_not_linger_from_a_prior_run`), confirmed red without the fix by temporarily reverting it and observing the real name in the emitted JSON. Re-ran `cargo run --bin gen_cache_ultimate_equipment` for real (`PCGEN_CORPUS_ROOT` pinned oracle): `1368 equipment` (was 1369), `1 dropped, NAMEISPI:YES: ue_equip_arms_armor.lst:66 Otyugh Hide`; `git status` confirms `otyugh_hide.json` deleted. Corpus-wide re-scan (`scan_nameispi.py`) after the fix: **0** shipped records with an unredacted PI-declared name, across all 11,005 `data/corpus/**/*.json` files (was 1). Re-ran `enrich_equipment_raw_tokens` to restore `raw_tokens`/`raw_bonus_chains` on the regenerated tree. |
| 9 | 2026-08-15 | 2026-08-16 | SD31-PI-REPAIR-001 | Row 39: `data/corpus/bestiary_5/LICENSE.json`'s race-lane note claimed the 10 Skinwalker race/race_trait records were "screened by ... the declared-PI reader" — `src/bin/ingest_races.rs`'s two record writers hardcoded `pi_field: None` unconditionally and never called `pi_screening::declared_product_identity` at all. | **Fixed and TDD'd.** Added `declared_product_identity_of()` (reads a parsed record's own `raw_tokens` — what actually ships — mirroring `ingest_race_traits.rs`'s established pattern). Wired into both writers: the chassis writer now DROPS the whole race (cascading to its traits, matching `decisions.md §50.3`'s "dropping a monster cascades" ruling) on `NAMEISPI:YES`; the trait writer redacts a `DESCISPI:YES` description via `pi_screening::classify_optional_field_declared` and now computes real `license`/`pi_field`/`pi_marker` instead of the hardcoded `Ogl`/`None`/`None`. 6 new tests in `src/bin/ingest_races.rs` (2 reader tests, 1 proving the real redaction call site, 1 proving name-declaration detection, plus the existing 34 continue to pass — 38/38 total). Re-ran `cargo run --bin ingest_races` for real against the pinned oracle: 25 races / 241 traits regenerated, `dropped, NAMEISPI:YES: 0`, `descriptions redacted by DESCISPI:YES: 0` — today's 20+1 in-scope races carry zero declarations (re-verified: `grep -c NAMEISPI:YES\|DESCISPI:YES` on every `IN_SCOPE_RACES` chassis+abilities file → 0), so content is unchanged (only `ingested_at` moved, confirmed via `git diff --stat`: 266 files, 1 line each). The claim in `bestiary_5/LICENSE.json` is corrected in place (append, not rewrite: a new `FIXED (SD31-PI-REPAIR-001, ...)` note appended to `screening_method_note`, plus a new structured, machine-checked `redaction_policy.declared_pi_reader_verified: true` + `declared_pi_reader_writers` pair replacing the old free-text-only claim). Checked every other `data/corpus/*/LICENSE.json` (25 total, `grep -l 'declared-PI reader\|declared_product_identity'`): only `bestiary_5`'s made this claim. |
| 61 | 2026-08-16 | 2026-08-16 | SD31-E6-F5-003 | `parse_equipment_entries::open_record`'s KEY-less name-fallback merge collapsed TWO DIFFERENT `.COPY=`-declared records into one parsed entry whenever they shared the same base-template left-hand side (`extract_record_name` strips a `.COPY=` row to the TEMPLATE side, not the item's own distinct post-`.COPY=` identity) — confirmed against 3 real shipped defects (`bastard_s_sting`/`mountain_pattern_armor`/`hunter_s_stand`) and, once fixed, found to ALSO explain 58 pre-existing `enrich_equipment_raw_tokens` citation misses corpus-wide (mostly `core_rulebook` equipmods) plus 15 of Ultimate Magic's 18 real spellbook items this cycle's own new ingest hit fresh (`Spellbook.COPY=<title>`, all 18 sharing the literal base name `"Spellbook"`). | **Root-caused, not merely worked around.** Two independent, complementary fixes: (1) `EquipmentRecord::tokens_on_line`/`bonus_chains_on_line` (single-citation-line provenance filter, used by `enrich_equipment_raw_tokens.rs`) for the case where a merge is genuinely correct (two lines truly restating one item) but a caller needs only ONE cited line's own tokens; (2) `open_record`'s merge predicate itself, fixed at the true root: a `.COPY=`-declared row (per the corpus's own convention and `Trap::CopyRecord`'s own doc, "`.COPY=` declares a *new* record") now NEVER merges via the KEY-less bare-name fallback, only via an explicit matching `KEY:` (unchanged). 6 regression tests reproducing the real byte content of all 3 historical defects plus the new merge-prevention behavior directly; full equipment/sd17/sd19/sd20/sd27 test suites re-run green. Re-enriched all 3 originally-reverted records for real (`bastard_s_sting`: `[VISIBLE:YES]` only, no foreign `EQMOD`; `mountain_pattern_armor`: 8 tokens from line 16 only, no doubling; `hunter_s_stand`: 4 tokens from line 23 only, no variant leakage) plus every corpus-wide record the same bug had silently left un-enriched: `enrich_equipment_raw_tokens` corpus-wide re-run went from 58 citation misses / 0 merged-entry mismatches to **0 citation misses / 0 merged-entry mismatches**. `corpus_literal_sweep` CLEAN before and after (0 findings), `declared_pi_shipping_audit` CLEAN. |
| 10 | 2026-08-16 | 2026-08-16 | SD31-PI-REPAIR-001 | "Make it unrepeatable": both row 38 and row 39 were the same shape — a screening contract asserted in prose/a doc comment and never actually called in code — and nothing in `verify.sh` could catch either. | Built `src/bin/declared_pi_shipping_audit.rs` + a new `declared-pi-audit` `verify.sh` stage (both `ALL_STAGES` and `QUICK_STAGES`, right after `pi-sweep`). **Check A** (the safety-critical, un-gameable half): walks every `data/corpus/**/*.json`, resolves each `lst_token`-sourced record's real corpus line, and fails if a `NAMEISPI:YES`-declared record exists on disk at all, or a `DESCISPI:YES`-declared record's `data.description` is not exactly `"[redacted PI]"` with `license="PI-REDACTED"`/`pi_field="description"`. **Check B**: a `LICENSE.json` opting into the new structured `redaction_policy.declared_pi_reader_verified: true` + `declared_pi_reader_writers` must have every named writer file literally contain the `declared_product_identity` call, or the gate fails — replacing row 39's unenforced prose claim with a machine-checked one. Mutation-proved: 8 unit tests in the binary (4 per check) construct the exact defect shape (an unredacted NAMEISPI:YES record, an unredacted DESCISPI:YES description, a LICENSE.json naming a writer that doesn't call the reader) and confirm the gate goes red on each, plus a live end-to-end proof against the real repo (`bestiary_5/LICENSE.json` temporarily pointed at a nonexistent writer file → `./scripts/verify.sh --only declared-pi-audit` FAILed exit 1; reverted → PASSed). Run against the real, fixed corpus: `declared-pi-audit: CLEAN`. |
| 22 | 2026-08-15 | 2026-08-15 | SD31-E6-F3-002 | `corpus_literal_sweep --json-out`'s `book` field is derived via `record.source_path`'s single-level `.parent().file_name()`, not the same `book_dir_of()` (4-segment, `<system>/<publisher>/<line>/<book>`) grouping the binary's own `by_book` pass already uses. For a record filed more than one directory level under its book — every `race`/`race_trait` row under `core_essentials/races/<race>/*.lst` — this reads the RACE NAME (`"dwarf"`, `"aasimar"`, …), never a real book, so `v06_work_inventory`'s `(book, source_file, source_line)` join in `apply_done_rung_stamps` never matches and `literal-verified` can never stamp. Confirmed one unit deep against the committed inventory before fixing: `core_essentials:race:dwarf`'s real id/book is `"core_essentials"` — the PCGen ORACLE book directory `v06_work_inventory` raw-enumerated it under (`$PCGEN_CORPUS_ROOT/pathfinder/paizo/roleplaying_game/core_essentials`), NOT `core_rulebook`, the repo's own choice of shipped-record directory for the same unit (`data/corpus/core_rulebook/race/dwarf.json`) — the two are different namespaces, and an earlier draft of this fix wrongly targeted the shipped-record directory before this check caught it (`retro.py correction`, `--verified-by` the `docs/work-inventory.json` grep below). | **Fixed.** New `short_book_of(source_path)` = `book_dir_of(source_path)`'s last path segment, replacing the buggy `.parent().file_name()` call in the `--json-out` writer (`src/bin/corpus_literal_sweep.rs`). 6 new tests (`short_book_of_tests` module): the CRB-dwarf case, a flat-file regression case (old and new agree), a nested-`race_trait` case, a malformed-path rejection case, a corpus-wide regression assertion (every shipped `race`/`race_trait` record's `short_book_of` output agrees with the binary's own trusted `book_dir_of`, zero disagreements), and a synthetic same-subdirectory-name-under-two-books collision proof (`book_alpha`/`book_beta` sharing a `shared_name` subdirectory resolve to their own distinct, correct books) — enumerated first and found ZERO real such collisions exist in the current corpus (`python3` one-liner grouping every shipped record's `source.path` basename by its `book_dir_of` book: 0 basenames shared across >1 real oracle book; `$PCGEN_CORPUS_ROOT/pathfinder/paizo/roleplaying_game/*/races/` enumeration: `core_essentials` is currently the ONLY oracle book with per-race nested subdirectories at all). **Re-derived before/after via a real `--json-out` diff** (buggy binary vs. fixed binary, same corpus, same oracle pin): **330 triples corrected** (`before verified count: 6327`, `after: 6327`, `330` moved from a wrong race-name string to `"core_essentials"`, ALL of them; **5,997 already-correct triples byte-identical before and after — zero regressions**, confirmed by diffing the two `--json-out` reports' `(book, source_file, source_line)` sets). Guarded regen (local, uncommitted per the wave rule — `git checkout -- docs/work-inventory.json` after measuring): board `done` **7,355 → 7,367 (+12)**, 19.09%→19.12% (`scripts/observer/pf1e_dashboard_producer.py`'s own `doneness_verdict`, `EXCLUDED_BOOKS`-filtered); `race` `done` **0 → 7 (+7)** — off zero, all 7 CRB races (`core_essentials:race:dwarf`/`elf`/`gnome`/`half_elf`/`half_orc`/`halfling`/`human`), `held` 7→0; `race_trait` `done` **484 → 489 (+5)**, `held` 151→146. **DoD-8, on-screen:** Dwarf character-creation form (`docs/release/SD-31-corpus-closure-grind/artifacts/SD31-E6-F3-002/dod8-01-newchar.png`) renders "Dwarf racial modifiers: +2 CON, +2 WIS, -2 CHA" applied to the CALCULATED ability scores (CON 14→16, WIS 12→14, CHA 8→6), `Medium` size, `Darkvision 60 ft.` — `core_essentials:race:dwarf`'s value on the player-visible sheet. Also cleared row 27's headline claim that this fix would clear DoD item 3 board-wide — verified it does NOT (unrelated root cause; see row 46). Full receipt: `progress.md` `SD31-E6-F3-002`. |
| 86 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Fixed, adversarial-review finding.** Row 69's own remediation column claimed "Confirmed the 9 findings do not affect anything this cycle shipped" — false: an independent Opus verifier found the rendered-prose gate (`!rendered.trim().is_empty()`) also accepted the `[redacted PI]` PI-screening placeholder as "real text" (5 of the 146 `race_trait` promotions: `core_essentials:race_trait:tiefling_daemon_spawn`/`_devil_spawn`/`_kyton_spawn`/`_oni_spawn`/`_rakshasa_spawn`), and no PI was exposed (the redaction itself works correctly) but the `done` credit was wrong per Decision 7 condition 3 ("a unit whose description is... placeholder... is NOT done"). | Fixed in `v06_work_inventory.rs`: the race_trait rung now reuses `is_real_description_value()` (the SAME refusal every other `text_only`->`text-complete` branch already applies, which already rejects `[redacted pi]`) instead of a bare non-empty check. Added a 4th refusal test (`an_applied_race_trait_whose_rendered_description_is_the_pi_redaction_marker_does_not_read_text_complete`), TDD'd against a reproduction of the real `tiefling_daemon_spawn` shape. Expected effect on the next guarded regen: 146 -> 141 promotions (146 minus the 5 PI-placeholder units). | *(none needed — fixed, this row is the record)* |
| 87 | 2026-08-16 | SD31-W5-INTEGRATE-001 | RULING-NEEDED | **Not fixed — genuinely needs the operator's flat-vs-scaling call row 69 already raised, restated with the exact units now confirmed shipped.** An independent Opus verifier found that at least 11 of the 146 `race_trait` promotions (`core_essentials:race_trait:duergar_stability`, `:dwarf_hatred`, `:svirfneblin_hatred`, `:fetchling_shadow_blending`, `:gnome_size`, `:goblin_size`, `:grippli_size`, `:halfling_size`, `:kobold_size`, `:svirfneblin_size`, `horror_adventures:race_trait:half_orc_stoic`) state a real, flat (non-scaling) numeric bonus in prose only (e.g. duergar_stability's DESC: "+4 racial bonus to their Combat Maneuver Defense") that no engine path computes — `grep -rn` across `src/` and `apps/` for each feature name returns no hits. Row 69's own open question stands unresolved: does Decision 7's "nothing to compute" mean (a) no numeric value appears at all, or (b) no *character-specific scaling* formula (the `wiring_class.rs` `prose_scaling_phrases` bar already in place)? Under reading (a) these 11 are over-claims; under reading (b) they are correctly `done` (a flat printed number the player just reads off the sheet, same as e.g. a feat's flat prerequisite text). | Did not unilaterally exclude or demote these 11 units (excluding units is not this cycle's call per the standing mandate, and `wiring_class.rs` is out of this cycle's file territory besides). Corrected the false "does not affect anything shipped" claim instead (row 86) and named the exact 11 units so whichever cycle gets the operator's ruling can act without re-deriving. | *(needs the SAME operator ruling row 69 asks for — does a flat, non-scaling printed bonus with no engine computation satisfy Decision 7's "nothing to compute", or not? 11 units ride on the answer, all currently `done`.)* |
| 88 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Correction to row 70's own text** (SD31-E4-F1-001, class wiring). Row 70 states "Slayer ~ Weapon and Armor Proficiency moved not-ingested -> grounded (a real, DoD-8-proven, on-screen-rendered computation)". An independent Opus verifier found no screenshot from that cycle exists anywhere on the branch or at this merge tip, and the cycle's OWN receipt contradicts the row in its blockers list ("DoD-8 on-screen screenshot not captured"). `classFeaturesModel.ts` does genuinely render any `class_feature.`-prefixed id generically, so DoD-8 is a real, applicable requirement here, not paperwork — it is simply undischarged. | Per row 70 itself this ships on `held`, not `done` (the base no-archetype grant is `grounded`, capped at `held` by `doneness_verdict` for the reason row 79/70(E4-F1) already document), so no `done` credit rests on the missing screenshot — a DoD-8 gap on a `held` unit, not a `done` one. Not captured this cycle (this integration cycle's own `driver.sh` budget went to the equipment lane's `done`-bearing gap instead, see row 90). | *(a future class_feature cycle, when it builds the archetype-selection surface row 79/E4-F1 already names as the real remedy, should capture Slayer's base (no-archetype) grant on the Class Features tab at the same time — cheap, since the render path is already generic)* |
| 89 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Row 68's own explicit question, answered.** Row 68 asked: "re-run `corpus_literal_sweep` and report how many of the 4,007 reach `literal-verified`/`done`". The honest answer is **0** — this was a pure relabel of the reporting `book` field, and `doneness_verdict`/`apply_done_rung_stamps` never consult `book` (only `wiring_class`+`status`, both untouched by the relabel). Re-derived at THIS integration cycle's fully-merged, fixed tip, with the operator's own two named cells: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(sum(1 for u in d['units'] if u.get('book')=='core_rulebook' and u.get('kind')=='race'))"` -> **7** (was 0 when the operator raised the complaint). `advanced_race_guide`+`race` -> **1** (unchanged, correctly — `decisions.md §25.2`: ARG reprints other books' races and owns none itself; the operator's "nearly untouched" impression is answered by `race_trait`, ARG's own genuine content, not `race`). `core_essentials`'s own residual (any kind) at this tip: **644** (634 per SD31-ATTRIB-001's own receipt + 10 from this cycle's own fixes — chiefly `gathlain` moving back from `bestiary_4` to unattributed, see row 68/73's gathlain correction). | Answered the operator's literal question with the literal command and number, in the place a check-in reads first (this row) as well as in `progress.md`'s own receipt. | *(none needed — this row is the answer)* |
| 90 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Not fixed this cycle — logged with remedy and owning epic, per the standing "never silently dropped" rule.** SD31-E6-F5-003 (equipment residual)'s adversarial review confirmed 50 of its 620 new records (8.1%) cite a corpus row that supports NONE of their own payload: 40 `ultimate_combat` records cite `uc_profs_weapon.lst` (a weapon-PROFICIENCY row, no `COST`/`WT`/`SPROP`) instead of `uc_equip_arms_armor.lst` (the real equipment row, worked example: `catapult_standard.json` ships `cost_gp=800.0` citing `uc_profs_weapon.lst:188`, but the value is only stated at `uc_equip_arms_armor.lst:168`); 10 more (`ultimate_psionics`/`ultimate_magic`/`ultimate_intrigue`) cite a `*_abilities_class.lst` class-ability row belonging to a `class_feature` unit, not the real equipment row. Values shipped are real (never fabricated), but the provenance citation and the record's harvested `raw_tokens` are wrong. Root cause: `equipment_gap.rs::try_files` tries `find_by_key_field` across EVERY `.lst` in the book before `find_exact_first_column` in ANY file, so a `KEY:` hit in a proficiency/class-ability file beats a first-column hit in the real equipment file — a PRE-EXISTING defect in the shared `find_citation` helper (this cycle's own diff there is pure `pub(crate)` visibility widening), first exposed at volume by this cycle's 620 records. Currently harmless for `done`-credit purposes: `apply_done_rung_stamps` only stamps `wiring_class == Static`, and none of the 10 class-ability-cited records is `Static` — a dormant cross-kind credit-leak channel, not a closed one. | Not fixed this cycle (root-cause repair to a SHARED helper touching every future equipment ingest is a properly-scoped TDD cycle of its own, not an integration-cycle patch — the fix needs a regression test pinning the exact Catapult (Standard) pair and then a re-run of `gen_cache_hand_authored_equipment`/`enrich_equipment_raw_tokens` for the affected slugs, which risks re-touching all 620 records' `raw_tokens`). Logged with full remedy so the next `epic-6-ingest-lanes` equipment cycle can act without re-deriving. | *(no ruling needed — a well-scoped follow-up: owning epic `epic-6-ingest-lanes`, remedy is `find_citation` constrained to equipment-shaped candidate files first (basename matching `*equip*`, or accepting a resolved row only if it carries `COST:`/`WT:`/`PROFICIENCY:`/an equipment-shaped `TYPE:`), reporting unresolved rather than accepting a proficiency/class-ability row, then re-citing the 50 named records)* |
| 91 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Not fixed this cycle — systemic gap across ALL THREE ingest lanes this wave, logged with remedy.** `corpus_literal_sweep` compares ONLY `data.raw_tokens` against the cited row's token closure (`src/rules_core/corpus_literal_sweep.rs::compare_tokens`) — it never reads the record's own typed, player-visible fields (`cost_gp`, `weight_lbs`, `name`, `description`). Because the `enrich_*_raw_tokens` binaries harvest `raw_tokens` FROM the cited row, the comparison is tautological at write time: `sweep(record) == sweep(the row the enricher just read)`, always. Confirmed exploitable (not hypothetical) by row 90's finding: `catapult_standard.json` is sweep-CLEAN and `literal-verified`-eligible while its shipped `cost_gp=800` appears nowhere in its (wrong) cited row. A `literal-verified`/`done` credit earned this way proves provenance STABILITY, not payload CORRECTNESS. | Not fixed this cycle (`corpus_literal_sweep.rs` is a wide-reach shared file touched by every ingest lane; adding a typed-field cross-check changes the meaning of an existing done-rung for every kind that uses it, which needs its own dedicated TDD pass and operator awareness that some currently-`done` units may demote). | *(needs a dispatch decision: extend `compare_tokens` with a sibling typed-field pass — `cost_gp<->COST:`, `weight_lbs<->WT:`, `description<->DESC:`/`SPROP:` union, `name/key<->`first column or `KEY:` — and re-run the guarded regen to see the true movement before deciding whether to gate it or just report it)* |
| 92 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | **Row 90's finding turned out to be a live, hard gate failure, not just a doneness-credit defect.** This cycle's first full-gate run caught it directly: `root-full`'s `tests/v06_corpus_trap_report.rs::ingested_record_keys_match_their_cited_line` (a RATCHET test — "the debt cannot silently grow back, and it cannot hide") went RED (`cargo exit 101`) on 10 of `SD31-E6-F5-003`'s new equipment records, exactly the class-ability-cited subset of row 90's 50-record finding: `ultimate_intrigue:Mystic Bolts` (cited row's real `KEY:` is `Warlock ~ Mystic Bolts`), `ultimate_magic:Scroll Blade`/`Scroll Shield` (`Scrollmaster ~ ...`), `ultimate_psionics:Astral Armor`/`Astral Juggernaut`/`Astral Skin`/`Deadly Fist`/`Flurry of Fists`/`Flurry of Strikes`/`Mind Shield` (all `Aegis ~`/`Soulknife Archetype ~`/`Blade Skill ~`/`Meditant ~`). | **Enumerated as known debt in the test's own sanctioned `KNOWN_KEY_MISMATCH_DEBT` allowlist** (`tests/v06_corpus_trap_report.rs`), the SAME mechanism the pre-existing "ACG Naturalist debt" (9 rows, since paid off) used — the test's own doc comment names this as the correct path ("the next mis-keyed ingest... must be added here deliberately, not silently absorbed"), not a loosened comparison or a deleted assertion. Did NOT fix the underlying citation (that is row 90's own remedy, a shared-helper TDD pass out of scope for an integration cycle under gate pressure) and did NOT touch the assertion logic itself — only the enumerated data, with full citation to row 90 and a note that the values these 10 records ship are real (confirmed by the adversarial review), only the provenance is wrong. Re-ran the single test in isolation to confirm green before re-launching the full gate. | *(no ruling needed — the debt is visible and paying it off is row 90's own already-named remedy; whichever cycle re-cites the 50 records should also shrink `KNOWN_KEY_MISMATCH_DEBT` back toward empty as part of that fix, and re-verify `mismatches.len()` drops with it)* |
| 93 | 2026-08-16 | SD31-W5-INTEGRATE-001 | NOTE | Discharged two outstanding DoD-8 gaps this cycle: (1) equipment's largest board claim (+555/+342 this cycle) had no on-screen proof at all -- captured via `verify-on-screen.sh --family equipment --record "Amulet of Catapsi" --expect "16200 gp"` (PASS, a citation independently verified correct, not one of row 90's 50 mis-cited records). (2) `SD31-D7-PROSE-001`'s own DoD-8 screenshot (row 4 of the adversarial review's confirmed findings) proved only the "Alternate racial traits" column; the majority shape (99 of 146 promotions, racial-DEFAULT traits) had no proof. Captured `Native Outsider` (Aasimar) on the "Standard traits" tab. **In doing so, found a real coordinate-calibration bug in the shared `verify-on-screen.sh` harness**: its `race_trait` family's `SEARCH_Y=285` lands on the THIRD row of race-filter chips, not the search box, whenever a race with a long name list wraps chips to 3 rows (25 races does) -- every attempt through the script itself failed with "still shows 10 rows" (`race-trait-languages.FAILED.verify.md`, `race-trait-darkvision.FAILED.verify.md`, `race-trait-native-outsider.FAILED.verify.md`, all committed as evidence, not discarded). Worked around by driving `driver.sh` directly (click the real search box at ~965,323 after clearing a stale race-chip filter at 452,205) rather than via the script. | Did not fix `verify-on-screen.sh`'s coordinate table (shared harness, out of an integration cycle's scope to recalibrate blind under gate pressure) -- worked around it for this cycle's own two captures and left the failed attempts as evidence rather than deleting them. | *(no ruling needed -- a future desktop-driver cycle should recalibrate `SEARCH_Y` for the `race_trait` family, ideally by reading chip-row count dynamically rather than a fixed pixel offset, since the row count is a function of how many races have traits, which changes every ingest cycle)* |
| 94 | 2026-08-16 | SD31-D7-PROSE-002 | NOTE | **Both of `decisions.md §7`'s "structural blocker" and its own follow-up work discharged this cycle.** (1) Extended `SD31-D7-PROSE-001`'s `race_trait` text-complete rung to `Kind::MonsterAbility` (`v06_work_inventory.rs`'s `Kind::MonsterAbility` verdict arm): a text_only, chassis-held ability whose corpus `DESC:` is real now reads `text-complete` instead of `grounded` (which `doneness_verdict` caps at `held` for `display` -- exactly the "board did not move by one unit" cell `SD31-E4-F1-001` hit). Render path is pre-existing, not new: `monster_catalog::serve_ability_description` -> `MonsterAbilityDto.description` -> `MonsterCatalogScreen.tsx`'s `ability.description` paragraph, unconditional for every registered ability per `monster_chassis.rs`'s own "only ability rows WITH an owner are registered" invariant. (2) Recovered row 70's ~247-unit `closure_has_real_description` false-negative (`.COPY=` records whose description was resolved by INHERITANCE at ingest time, invisible to the raw `.lst` closure): added `EngineFacts::corpus_json_descriptions`, a second source reading `data/corpus/<book>/{equipment,spell}/**/*.json`'s `data.description`, joined on `(basename, line, record_key)` -- the coordinate alone is ambiguous for 24 real corpus lines that hold two distinct records (e.g. `acg_equipmods.lst:41` is both `Flying` and `Special Ability ~ Flying ~ Melee`), so the record's own key is a third join component. Guarded regen at this tip (`corpus_literal_sweep`: 0 findings CLEAN; `derived_evaluator_fixture_check`: 100/101, 1 pre-existing unrelated failure `advanced_players_guide:equipment:spindle_of_perfect_knowledge`, `FIXTURE_EXIT=0`; `v06_work_inventory`: `REGEN_EXIT=0`, zero stamp loss): **done 7,340 -> 8,549 (+1,209), 19.05% -> 22.19%**, by kind `monster_ability` +947, `equipment_modifier` +149, `equipment` +112, `spell` +1, **zero units moved away from `done`**. `v06_corpus_trap_report --audit`: 1,191 wiring-class-mismatch findings, byte-identical to the wave's own recorded baseline (row 65) -- confirmed not worsened. DoD-8: `verify-on-screen.sh --family monster --record "Demon (Balor)" --expect "Vorpal Strike" --expect "gains the vorpal weapon quality"` PASS (`artifacts/SD31-D7-PROSE-002/item8/monster-demon-balor.{png,verify.md}`); `--family equipment --record "Scale Mail" --expect "dozens of small overlapping metal plates"` PASS (`artifacts/SD31-D7-PROSE-002/item8/equipment-scale-mail.{png,verify.md}`). Two failed attempts kept as evidence, not discarded (`monster-air-elemental.FAILED.verify.md` -- searched the corpus `KEY:` prefix "Air Elemental" rather than the engine's actual display `name` "Elemental (Air/Medium)"; `monster-elemental-air-medium.FAILED.verify.md` -- the parenthesised/slashed record name matched 44 rows broader than intended, worked around by switching to the already-proven `Demon (Balor)` record instead of debugging the search box's handling of `/` under load). | Did not touch `Kind::ClassFeature` or `Kind::Companion` (the wave's other named `display`+`grounded`/zero-magnitude populations, `progress.md` `SD31-D7-PROSE-001` §9 followups #3 and the E4-F1 Slayer case respectively) -- `class_feature` has no generic corpus-JSON description-render path this cycle could find reused rather than newly built (its `explanations`-based `detail` field on `ClassFeatureRow` renders a COMPUTED derivation, not raw corpus prose), and building one from scratch for two more kinds in one cycle risked the same "big number a reviewer knocks down" failure the card's own text warns against. Left as clearly-scoped remaining work rather than rushed. | *(no ruling needed -- informational; the +1,209 headline is fully re-derivable from the two guarded-regen commands cited above)* |
| 95 | 2026-08-16 | SD31-D7-PROSE-002 | RULING-NEEDED | **Same open question rows 69/87 already raised (does Decision 7's "nothing to compute" mean no numeric value at all, or no character-specific scaling formula?), one more confirmed unit, this time in `monster_ability`.** Row 69's own hand-verified sample already named `bestiary_2:monster_ability:devilfish_water_dependency` ("1 hour"/"2 hours" printed in its `DESC:`, no engine computation) as this exact shape; this cycle's new `monster_ability` rung would otherwise have promoted it to `done` for the first time. Took the CONSERVATIVE default per the card's own instruction: added `MONSTER_ABILITY_FLAT_MAGNITUDE_PENDING_RULING` (`v06_work_inventory.rs`, a `const &[(&str, &str)]` of `(engine_book, key)` pairs, currently one entry) and refused promotion for it specifically, leaving it `grounded`/`held` pending the ruling. **A ONE-LINE change** (clear the list) applies the operator's answer in either direction the moment it lands -- reading (a) keeps it as-is, reading (b) clears it. Did not attempt to generalize this to the unknown-sized wider population (that would require touching `wiring_class.rs`'s `prose_scaling_phrases` detector, a sibling's file this wave, and is the actual fix rows 69/87 are waiting on) -- this is a point exclusion for the one NAMED unit this rung specifically would have newly promoted, not a general solution. | Excluded the one named unit conservatively rather than either (a) promoting it and reproducing the exact over-claim shape rows 69/87 already flagged, or (b) unilaterally widening the exclusion into a heuristic detector that would itself need validating (out of file territory and out of this cycle's scope besides). | *(needs the SAME operator ruling rows 69/87 already ask for -- one more unit is riding on the answer, named above, currently excluded from `done` pending it)* |
| 96 | 2026-08-16 | SD31-E4-F1-002 | BLOCKER | **A THIRD, distinct structural blocker on `class_feature` board movement, beyond row 78's id-naming mismatch and Decision 7's `display`+`grounded`->`held` doneness-verdict cell: `src/bin/v06_work_inventory.rs`'s own `modelled_class_books()` (line ~4604) hardcodes only THREE books' classes as "modelled" -- `ClassId::ALL` (CRB), `ApgClassId::ALL` (APG), `AcgClassId::ALL` (ACG). Every other book's class enum (`PuClassId` for Pathfinder Unchained, and now `UcClassId` for Ultimate Combat, this cycle's own new addition) is entirely absent from this registry.** Consequence, traced one record deep: `Kind::ClassFeature`'s classifier (`classify()`, ~line 4079) calls `class_feature_owner(&unit.key, facts.class_books.keys())` first; for any Gunslinger `class_feature` record this returns `None` regardless of how well `pilot_compute.rs` computes it, so the classifier falls through to the "does the CORPUS declare a class by that name" branch and returns `not-ingested` with evidence `class_feature_of_unmodelled_corpus_class:gunslinger` -- **never even reaching the `explanation_id` `.ends_with()` check row 78 already found broken**, because the `owner` lookup fails one step earlier than that. Confirmed by direct read of `modelled_class_books()`'s body (3 loops, `ClassId`/`ApgClassId`/`AcgClassId` only, no fourth) and cross-checked against `probe_class_feature_effect_wiring`'s own consumer-delta probe, which is scoped to `CLASS_FEATURE_POOLS` (chooser-shaped option pools only) and would not reach Gunslinger's flat automatic features either way. This blocks not only this cycle's own Gunslinger wiring (Grit/Nimble/Gun Training/Gunslinger Initiative, all genuinely computed and reachable via `build_pilot_headless_receipt`, 11 new/updated tests green) but EVERY `class_feature` record for EVERY Pathfinder Unchained or Ultimate Combat class, present or future, however well it is wired in `pilot_compute.rs` -- and by extension almost certainly every OTHER book named in row 78's neighboring OPEN_FINDINGS entries (Occult Adventures, Ultimate Psionics, Ultimate Intrigue, Ultimate Magic, Ultimate Wilderness -- none of those books' class enums appear in `modelled_class_books()` either, not independently re-verified per book this cycle, named as a hypothesis consistent with the pattern). This is a genuinely different mechanism from row 78 (a missing-registry lookup that short-circuits BEFORE the id-suffix check runs, not an id-naming convention issue) and from Decision 7's `display`+`grounded`->`held` cell (a book-registration gap, not a verdict-table mapping) -- three independent blockers now stacked on the same `class_feature` kind, each capping a different, real slice of the 40%-of-the-board population. `src/bin/v06_work_inventory.rs` is lane 1's granted file territory, not this card's (`src/rules_core/pilot_compute.rs`/`archetype_resolver.rs`/class compute modules only) -- reported precisely rather than edited across the boundary. | Wired Gunslinger's real base chassis (BAB/saves, formula-derived from the real corpus `CLASS:Gunslinger` record) and 4 named features (Grit, Nimble, Gun Training, Gunslinger Initiative) in `pilot_compute.rs`, plus 2 of Gunslinger's 4 real archetypes (Pistolero, Mysterious Stranger) in `rules_tables/ultimate_combat/archetype_tables.rs`, with a real `archetype_claiming_slot_entry` `if let`/`else` supersession branch superseding Gun Training (Pistolero) and both Grit and Nimble (Mysterious Stranger) -- proven via `build_pilot_headless_receipt` (11 new tests, all green, covering base case + both supersession cases) per SD31-E4-F1's own acceptance standard, exactly as Slayer's wave-5 precedent did for the `display`+`grounded` blocker. Did NOT touch `v06_work_inventory.rs` (out of this card's file territory) and did NOT chase board movement (per this card's own dispatch: "do not chase board movement yourself and do not touch the doneness path; your deliverable is real wired classes") -- reported the registry gap here instead so lane 1 can register `UcClassId`/`PuClassId`/etc. into `modelled_class_books()` alongside the row-78 id-suffix fix and Decision 7's verdict-table extension. | *(needs a dispatch decision: the fix is almost certainly small -- add `for id in PuClassId::ALL { class_books.insert(...) }` / `UcClassId::ALL` / any future book's class enum to `modelled_class_books()`, mirroring the existing three loops exactly -- but it is lane 1's file and should land alongside row 78's fix and Decision 7's verdict-table extension so all three `class_feature` blockers are re-measured together in one guarded regen rather than three separate partial ones)* |
| 97 | 2026-08-16 | SD31-E4-F1-002 | BLOCKER | **DoD-8 on-screen verification for Gunslinger is blocked one level before the archetype-picker gap OPEN_FINDINGS already names: there is no way to CREATE a Gunslinger character through the desktop app's own character-creation form at all.** Traced directly: `apps/desktop/src/characterHub/CreateCharacterForm.tsx`'s class `<select>` is driven entirely by `CLASS_OPTIONS` (`characterHubModel.ts`, a hardcoded `ClassOption[]` literal covering CRB/APG/ACG/Pathfinder-Unchained classes only, ~40 entries) -- `grep -rn gunslinger apps/desktop/src/` returns zero hits anywhere in the frontend. `classId` state initializes to `CLASS_OPTIONS[0].id` and the dropdown can only ever offer what that array lists; there is no free-text class-id entry point and no character-import/file-load path that bypasses it (confirmed: `CLASS_OPTIONS` is also read pervasively downstream for labels/hit-die/spell-tab routing, so hand-crafting a save file with an unlisted class id would render a genuinely degraded sheet, not a real proof — rejected as a workaround for that reason, not attempted). This is a DIFFERENT, earlier gap than the one `OPEN_FINDINGS`'s `("ultimate_combat", "archetypes")` entry already names (no archetype-selection surface) — that gap is about picking an ARCHETYPE once a Gunslinger exists; this one is about creating a Gunslinger at all. `apps/desktop/src/characterHub/CreateCharacterForm.tsx`/`characterHubModel.ts` are frontend files, outside this card's granted territory (`pilot_compute.rs`/`archetype_resolver.rs`/class compute modules/`rules_tables/*/archetype_tables` only). | Did not fake a screenshot and did not drop the requirement. Reachability is proven the way SD31-E4-F1's own acceptance criterion actually names it — "a headless pilot receipt test through `build_pilot_headless_receipt`, not a unit test on the resolver alone" — via 11 new tests exercising the real production `compute_uc_class_chassis`/`ground_or_block_gunslinger_class_features`/`archetype_claiming_slot_entry` path end to end (base case, both supersession cases, level-gating). Did not attempt `driver.sh` for Gunslinger specifically once the picker gap was confirmed by direct source read (no need to burn a launch cycle proving a negative the source already answers unambiguously). | *(needs a dispatch decision: a future UI-territory cycle should add a `CLASS_OPTIONS` entry for each `epic-4-mechanism`-wired non-CRB/APG/ACG class as its base chassis lands, mirroring the existing Unchained-classes entries' shape and doc-comment discipline; until then, EVERY class this and future `epic-4-mechanism` cycles wire for Occult Adventures/Ultimate Combat/Ultimate Psionics/etc. inherits this same DoD-8 gap, not only Gunslinger)* |
| 98 | 2026-08-16 | SD31-ATTRIB-002 | NOTE | **Row 68's residual, re-derived at HEAD `5d0cd1595` (post-`SD31-W5-INTEGRATE-001`) — 644, matching that cycle's own row-89 figure exactly** (`378 monster_ability + 258 race_trait + 8 race`, re-run `cargo test --locked --bin v06_work_inventory --bin corpus_literal_sweep` -> 109/109 + 9/9 green, `corpus_literal_sweep --json-out` -> 21716/24736, 0 findings, `v06_work_inventory` guarded regen (local, uncommitted per the wave rule) -> board unchanged, 38521 units / 7340 done / 19.05%). **The operator's second cell, CORRECTED by integration-cycle adversarial review (`SD31-W6-INTEGRATE-001`) after this cycle's own claim was checked against the whole record rather than counted.** `advanced_race_guide`+`race` -> **1**, but that 1 unit IS a classifier artifact, not a correct chassis count: the record is `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print([{k:u.get(k) for k in ('name','book','kind','source_file','source_line','wiring_class','status')} for u in d['units'] if u.get('book')=='advanced_race_guide' and u.get('kind')=='race'])"` -> `{'name': 'Race Builder', 'source_line': 53, 'wiring_class': 'static', 'status': 'not-ingested'}` — `arg_races.lst:53`'s `Race Builder\tKEY:Race\t...ABILITY:Internal|AUTOMATIC|Rules ~ Use Race Builder System` is ARG's chargen-system scaffold row, typed `kind: race` by the ingest classifier, not a playable race. `grep -c '^RACE:' arg_races.lst` -> 0 real RACE: rows in the file at all. This cycle's own structural half IS correct and independently re-corroborated: ARG owns 0 REAL races — 37 reprints (`grep -n -i race .../advanced_race_guide.pcc`: Core 7 + B1 11 + B2 7 + B3 5 + B4 5 + ISWG 2 = 37), gate-enforced live in `src/rules_core/race_resolver.rs` (`assert!(corpus.chassis.values().all(|c| c.book_id != "advanced_race_guide"), "ARG must contribute no race chassis")`, citing `decisions.md §25.2`) — and the "nearly untouched" impression the operator raised is answered entirely by `race_trait` (ARG's own genuine content, 323 units / 198 `done`, unaffected by any attribution fix), not `race`. The residual 1 is currently `not-started`, so no `done` credit rests on the mis-typing; a follow-up should decide whether the ingest classifier should exclude `KEY:Race` scaffold rows entirely or re-kind this one unit (tracked as its own row, see below), not close the question as "correct" the way this row originally did. **NEW finding: 516 of the 644-unit residual are further re-attributable, found but not fixed (out of file territory this wave).** `v06_work_inventory.rs`'s `resolve_true_book_for_core_essentials` only scans a file's first 5 lines for a `SOURCELONG:` token (`text.lines().take(5).find_map(...)`, line ~1160); `core_essentials/ce_abilities_race.lst` (545 of the 636 `monster_ability`+`race_trait` residual units cite this one file) has no such header line (confirmed: its own top comment is plain prose, "Everything in the Pathfinder GameMode is run off the Default Internal Ability"), so the whole file falls back to unattributed. But the file's BODY carries 11 mid-file `SOURCELONG:<Book>` directive lines at (pinned-oracle) lines 1273/1624/1794/2221/2275/2342/2361/2406/2420/2432/2441, each setting the source for every row that follows until the next directive — a real PCGen convention, not a guess: line 1273's `SOURCELONG:Bestiary` precedes the file's own `###Block: *** Universal Monster Rules, pages 297-306 ***` header, and the very next rows are "Ability Damage"/"Ability Drain" — Bestiary 1's own Universal Monster Rules appendix, semantically confirmed. Mapping each residual unit's `source_line` to the nearest preceding directive (script: `/tmp/claude-1000/.../scratchpad/analyze_ce.py`, re-runnable against any future regen) resolves **516 of 545**: `bestiary` 263 (`monster_ability` 201 + `race_trait` 62), `bestiary_2` 206 (132+74), `bestiary_3` 41 (30+11), `bestiary_4` 2, `bestiary_5` 1, `bestiary_6` 3. The remaining 29 stay correctly unattributed: 23 precede the file's first directive (lines 18-1272, genuinely the book-agnostic "Default Internal Ability" zone the top-of-file comment describes) and 6 carry `SOURCELONG:Universal Rules` (PCGen's own internal designation, `SOURCESHORT:UR`, not a Paizo book this program tracks). **Not fixed**: the repair needs (a) `resolve_true_book_for_core_essentials` to become source-line-aware — today it takes `(path, text)`, needs the unit's own `source_line` to pick the nearest preceding directive instead of only the file's first 5 lines — and (b) a matching change to `corpus_literal_sweep.rs`'s `short_book_of`, which today does not attempt root-level `ce_*.lst` resolution AT ALL (its own doc comment says so explicitly) — landing (a) without (b) would desync the sweep's join key from `unit.book` and reproduce the exact stamp-loss class `SD31-ATTRIB-001`'s own doc comment warns about. Both changes land in `src/bin/v06_work_inventory.rs`, lane 1's file this wave (per this cycle's own FILES grant) — reported here rather than edited. Zero doneness impact expected either way (`book` is a pure reporting field; every prior relabel in this program proved 0 verdict transitions, most recently `SD31-ATTRIB-001`/`SD31-W5-INTEGRATE-001`). Dashboard producer's per-book-panel doc comments (`scripts/observer/pf1e_dashboard_producer.py`, 3 spots) corrected from the stale 634/249/7 to the current 644/258/8 and this finding, in this cycle's own commit. **Row 73's spelling-divergence follow-up ("`data/corpus/beastiary/` vs `bestiary`, ~239-unit metric consequence"), investigated and found to have ZERO real metric or production consequence — the row's own framing does not survive re-derivation.** Every physical-file-reading consumer (`corpus_loader.rs`, `race_resolver.rs`, `monster_chassis.rs`, `companion_chassis.rs`, `ingest_races.rs`, `race_catalog.rs`, `reach_gate.rs`, and `pf1e_dashboard_producer.py`'s own `_load_beastiary_monsters`) consistently uses the misspelled `"beastiary"` string, matching the real on-disk directory — none of them are broken. Every reporting/engine-key consumer (`v06_work_inventory.rs`'s `unit.book` field, `docs/work-inventory.json`, `pf1e_dashboard_producer.py`'s `work_inventory_panel()` — confirmed by reading its full body, it never calls `os.path.isdir` and works purely from the already-aggregated `book` string) consistently uses the correctly-spelled `"bestiary"`. `CORPUS_DIR_ALIASES = [("beastiary", "bestiary")]` (`v06_work_inventory.rs`) exists precisely to bridge the two for `engine_book_for` lookups and is exercised by a passing test (`every_corpus_book_with_race_traits_resolves_to_an_engine_book`). The ONLY artifact that conflates the two spellings is row 68's own informal, never-committed `os.path.isdir(f'data/corpus/{book}/{kind}')` python one-liner — not a shipped surface. Given zero real consequence and that a rename would touch `monster_chassis.rs`/cache_gen (both explicitly barred to this cycle) plus 40+ further files (`grep -rl beastiary --include=*.rs --include=*.py --include=*.ts --include=*.tsx . 2>/dev/null \| grep -v data/corpus \| grep -v node_modules` -> 40+ hits, many `tests/*.rs` filenames themselves spelled `beastiary1`), a repo-wide rename was NOT attempted this cycle — the risk/reward does not justify crossing that many lanes' territory for a diagnostic-only defect. | Re-derived every figure this card's brief stated rather than trusting them (all matched the wave-5-integration receipt exactly, confirming the board is stable at this tip); found and precisely quantified a genuine, previously-undiscovered 516-unit further-attribution opportunity rather than declaring row 68/73 "done" once the trusted figures reproduced; declined to implement it given the file-territory bar, per this card's own explicit instruction, rather than editing `v06_work_inventory.rs` out of territory; investigated the spelling-divergence claim rather than either implementing an unjustified repo-wide rename or silently dropping row 73's follow-up unanswered. | *(no ruling needed for the spelling divergence — recorded as investigated-and-closed, no fix warranted given zero consequence; the 516-unit finding needs a dispatch decision: a future book-attribution cycle holding write access to `v06_work_inventory.rs` should make `resolve_true_book_for_core_essentials` source-line-aware per the mechanism above, sync `corpus_literal_sweep.rs`'s `short_book_of` in the same commit, and re-run the guarded regen to confirm 0 verdict transitions before landing)* |
| 99 | 2026-08-16 | SD31-W6-INTEGRATE-001 | NOTE | **Follow-up named by row 98's own correction: `advanced_race_guide:race`'s residual 1 unit (`Race Builder`, `arg_races.lst:53`) is a chargen-system scaffold row typed `kind: race` by the ingest classifier, not a real race — a KEY:Race declaration with no `RACE:` token, used only to gate PCGen's own "Race Builder" UI feature.** Currently `not-started`, so no `done` credit rides on it, but the mis-typing means any future `race` completeness metric that reaches 100% of the raw count will never actually clear this unit through a real chassis path — it has none to build. | Not fixed this integration cycle (the ingest classifier is `src/rules_core/ingest/**`, out of this wave's granted merge/repair scope; re-kinding or excluding scaffold rows is a classifier-shape decision, not a data fix). | *(needs a dispatch decision: either exclude `KEY:Race` scaffold rows with no `RACE:` token from the `race` kind at ingest time, or re-kind this single row to a new non-denominator-bearing bucket — either way it is a one-record, well-scoped follow-up for a future `epic-1-race-chassis`/ingest cycle)* |
| 100 | 2026-08-16 | SD31-E6-F2-004 | NOTE | **Ultimate Combat's 2-unit `mod_only` spell residue, named rather than silently dropped.** `ultimate_combat:spell:life_conduit_greater` and `ultimate_combat:spell:life_conduit_improved` (`uc_spells.lst:216-217`, `.MOD` rows attaching `DESC:` to bare base declarations at lines 65-66) are `origin: mod_only` in `docs/work-inventory.json` and are NOT resolved by this cycle's `ingest_ultimate_combat_spells.rs` (which ingests base declarations only, matching `ingest_occult_adventures_spells.rs`'s own established scope boundary for its 328-unit `mod_only` residue). Both base spells (`Life Conduit`, `Life Conduit (Greater)`, `Life Conduit (Improved)`) ARE now in `ultimate_combat::spell_list::SPELL_LIST` with `level: None`/`school: None` (real gap, not fabricated) -- whether the guarded regen's fresh classifier run resolves the 2 `mod_only`-origin work-inventory units against this newly-existing table is downstream of `v06_work_inventory`'s own re-classification, out of this card's file territory to trace further. Population size: 2 (compare OA's 328 — Ultimate Combat's `.MOD`-graft residue is nearly entirely resolved by this one book's own base declarations, unlike OA's cross-class-widening shape). | Did not attempt to resolve the 2 `mod_only` units (out of `ingest_ultimate_combat_spells.rs`'s own declared scope, matching precedent); named here per the standing "never silently dropped" rule. | *(no ruling needed -- low unit count; a future spell-lane cycle checking `mod_only`-origin re-resolution against a newly-widened `SPELL_LIST` table would close this for free if the re-classification doesn't already pick it up)* |
| 101 | 2026-08-16 | SD31-E6-F2-004 | NOTE | **Superseded by its own resolution within the same cycle.** Drafted mid-cycle while `./scripts/verify.sh` was still running past 21+ minutes under heavy shared-box contention (6+ concurrent sibling `verify.sh`/`cargo clippy` invocations observed live) — the gate finished moments later with **VERIFY_EXIT=0, 23/23 stages PASS** (`class-dump` was the final stage: 31/31 computing), so the "not obtained" framing this row originally carried is stale before it was ever read. Left in place rather than deleted (append-only convention) as an honest record of the live uncertainty at draft time. Full summary: `root-lib` 1870, `root-full` 6629/562 suites (all 529 `tests/*.rs` executed), `desktop` 447, `reach` 27 (spell-family claim present), `corpus-sweep` 22937 examined/0 findings, `frontend-test` 99/99, `frontend-typecheck` clean, `clippy` root:47/desktop:7 warnings/0 errors, `class-dump` 31/31. Baseline drift flagged (not a failure): `BASELINE_ROOT_LIB_TESTS` 1867→1870, `BASELINE_ROOT_FULL_TESTS` 6603→6629, `BASELINE_ROOT_TEST_BINARIES` 560→562, `BASELINE_CORPUS_LITERAL_RECORDS` 21716→22937 (`scripts/verify-baselines.env`). | Raised the baseline floors in a separate, reviewable commit (DoD item 7) with `--show-actuals`-shaped citations rather than folding them into the feature commit. Ran DoD-8 (`monster:Ankheg`, `spell:Ablative Barrier`) immediately once the gate freed `driver.sh` — see `progress.md` §7 for the real PASS/FAIL result. | *(no ruling needed)* |
| 102 | 2026-08-16 | SD31-E6-F5-004 | NOTE | **Rows 90/91/92 RESOLVED this cycle.** Row 90 (the 50-record — re-derived corpus-wide to the true **39** — equipment mis-citation): `equipment_gap::find_citation` now tries every strategy (`find_by_key_field`, `find_exact_first_column`, `.COPY=` fallback) against equipment-shaped candidate files (basename containing `"equip"`) FIRST, only falling back to non-equipment-shaped files if nothing there resolves — the exact remedy row 90 itself named. Verified corpus-wide before relying on the predicate: every one of the ~1,900 already-correctly-resolved equipment/equipment_modifier citations in the shipped corpus already lands in an "equip"-named file, so narrowing the search order changes nothing for them. TDD (RED confirmed pre-fix, GREEN post-fix; `find_citation_prefers_an_equipment_shaped_file_over_a_proficiency_file_with_a_coincidental_key_match` + a fallback-still-works sibling test). Re-cited all 39 affected records for real: deleted the 39 wrong-citation JSON files (`write_json`'s no-clobber guard otherwise blocks a re-run from correcting them) and re-ran `gen_cache_hand_authored_equipment` against the real pinned oracle — all 39 now cite their real equipment row (worked example: `catapult_standard.json` now cites `uc_equip_arms_armor.lst:168`, matching the hand-authored table's own long-correct citation comment). Row 92's `KNOWN_KEY_MISMATCH_DEBT` allowlist shrunk from 10 entries to **0** — re-ran `ingested_record_keys_match_their_cited_line` in isolation, green with the empty list, confirming the debt is genuinely paid, not hidden. Row 91 (the systemic `compare_tokens` typed-field gap): `corpus_literal_sweep::compare_tokens` extended with a `cost_gp<->COST:` / `weight_lbs<->WT:` typed-field cross-check, independent of `raw_tokens` (closes exactly the tautology row 91 named — `raw_tokens` and `cost_gp` are populated from different sources in `cache_gen::equipment_gap`/`hand_authored_equipment`). MUTATION-PROVEN twice: (a) unit-level — removing the two `compare_typed_numeric_field` calls in a scratch copy makes exactly the 3 new-check tests go RED, restored; (b) corpus-level — ran the real `corpus_literal_sweep` binary against the full pinned oracle BEFORE fixing the underlying data and it found exactly **1 real, previously-invisible corpus-fidelity defect** (`data/corpus/beastiary/equipment/poison_black_smear.json`: `cost_gp=0.0` shipped, but the real corpus row `b1_equip_general.lst:7` carries no `COST:` token at all — a hand-transcription error in `rules_tables::beastiary1::equipment_data.rs`, `0` gp is a stated price, not "unstated"). description/name were named in row 91's remedy note too but NOT implemented this cycle — scoped to `cost_gp`/`weight_lbs` only (see row 95 for what remains, not silently dropped). | Landed all three fixes with tests, re-cited the 39 real records, hand-corrected the 1 corpus-fidelity defect the new check found (`cost_gp: Some(0.0) -> None` in both the shipped JSON and its source Rust table — did NOT re-run `gen_cache_beastiary` to regenerate it, since that binary unconditionally overwrites its WHOLE book's cache with no no-clobber guard and a stale schema that drops `raw_tokens` entirely; confirmed by a caught-and-reverted near-miss touching 46 unrelated monster records' `wiring_class` before landing the safer hand-edit). `corpus_literal_sweep` CLEAN (0 findings) after. Full receipt: `progress.md` `SD31-E6-F5-004`. | *(none needed — both rows' own remedies are discharged; row 103 names what row 91's `description`/`name` scope still owes)* |
| 103 | 2026-08-16 | SD31-E6-F5-004 | NOTE | **Two follow-ons this cycle intentionally left open, named precisely rather than silently dropped.** (1) Row 91's remedy also named `description<->DESC:`/`SPROP:` union and `name/key<->` first column — not implemented this cycle. `description` in particular needs a real per-kind reconstruction rule (equipment's own convention, confirmed against the Catapult (Standard)/Astral Armor shapes this cycle worked, is "join every `SPROP:` token's value with `"; "`, in corpus-row order" — verified by hand against 2 records, not proven at scale or mutation-tested), and a wrong reconstruction rule would itself manufacture false findings across other kinds' differently-shaped description conventions (spell/race_trait/monster_ability each build `description` differently) — a properly-scoped TDD pass of its own, not a same-cycle add-on. (2) The card's own item 4 ("extend `book_routing` to the ~962/~228 not-started books") is **not the real remaining lever, corrected here**: `equipment_gap_tables.rs` (the Rust data source `book_routing` maps INTO) has rows for only the same 9 books already routed (CRB/APG/ACG/ARG/UC/UI/UM/UPSI/UW, `grep -oE 'book: "[A-Z_]+"' src/rules_core/rules_tables/equipment_gap_tables.rs \| sort -u`) — extending `book_routing`'s match arms alone adds routing for books with ZERO underlying rows to route, a no-op. Re-derived the real not-started-by-book split (`python3` one-liner over `docs/work-inventory.json`'s `doneness_verdict`): `equipment` 962 not-started, top books `inner_sea_gods` 150, `occult_adventures` 119, `adventurers_guide` 115, `horror_adventures` 115, `mythic_adventures` 110, `inner_sea_combat` 72, `inner_sea_races` 72, `inner_sea_world_guide` 47, `monster_codex` 45, `inner_sea_temples` 43, `inner_sea_intrigue` 39, plus 5 bestiary/BotD books under 10 each; confirmed NONE of these 11 books has ANY `rules_tables/<book>/equipment*.rs` module at all (`find` over each book's `rules_tables/<book>/` dir for `*equip*`, zero hits corpus-wide) — the real remaining work is a genuine new per-book hand-transcription ingest (the "book onboarding tax is per-file" the dispatch itself named), not a routing-table edit. | Did not attempt a rushed new-book ingest under this cycle's own remaining time budget (PI-screening a brand-new book correctly is the highest-risk shape of work in this program per the dispatch's own wave-4 PI-failure warning, and rushing it is worse than leaving it correctly scoped for a dedicated cycle). Corrected the dispatch's own premise in this row rather than silently doing a no-op `book_routing` edit that would look like progress but move nothing. | *(no ruling needed — a well-scoped follow-up: the next `epic-6-ingest-lanes` F5 cycle should pick ONE not-started book (`inner_sea_gods` is largest at 150) and hand-transcribe its real `equip_*.lst`/`equipmods.lst` rows into a new `rules_tables/inner_sea_gods/equipment_data.rs`, following the `beastiary1`/`ultimate_magic` precedent, with both SD-30 PI contracts wired from the start)* |
| 104 | 2026-08-16 | SD31-E6-F7-001 | NOTE | **Found a real cross-lane join-key bug blocking 34 `companion` units from `done`, and a likely-larger latent hazard.** `enrich_companion_raw_tokens.rs` (this cycle, new binary) enriched all 99 `static`+`grounded` `companion` units' `raw_tokens`; `corpus_literal_sweep` verified 65 of them clean (`sweep_verified` now contains their `(book, file, line)` triple), but `v06_work_inventory::apply_done_rung_stamps`'s `Static` branch still left the other 34 at `held`. Traced one record deep: `core_essentials:companion:familiar_octopus` — its corpus JSON physically lives at `pathfinder/paizo/roleplaying_game/core_essentials/ce_races_familiar_apg.lst`, and `corpus_literal_sweep`'s `sweep_verified` correctly records the triple `(book: "core_essentials", file: "ce_races_familiar_apg.lst", line: 9)` (confirmed present in `/tmp/sweep-sd31-companion-feat-after-enrich.json`'s `verified` array). But the SAME unit's `book` field in `docs/work-inventory.json` is `"advanced_players_guide"` — correctly re-attributed by `SD31-ATTRIB-001`'s `resolve_true_book_for_core_essentials` (this file genuinely declares `SOURCELONG:Advanced Player's Guide`, not a misattribution). `apply_done_rung_stamps`'s `Static` arm (`src/bin/v06_work_inventory.rs:4433-4444`) joins `sweep_verified` on `item.unit.book.clone()` — the RE-ATTRIBUTED reporting field — instead of `item.unit.source_book.clone()`, the field the SAME file's own `CorpusUnit::source_book` doc comment (`:469-475`) explicitly names as "the only field safe to pass to [a] physical [...] lookup", and which TWO OTHER call sites in this same file (`engine_book_for` at `:3591`/`:5415`) already correctly use for exactly this reason. Re-derived corpus-wide, book-agnostic to kind: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(len([u for u in d['units'] if u.get('wiring_class')=='static' and u.get('status') in ('grounded','ingested-magnitude','text-complete') and u.get('source_file','').startswith('ce_')]))"` -> **34**, all `companion` (`{'bestiary':30,'advanced_players_guide':2,'ultimate_magic':2}` by re-attributed `book`) — the ONLY kind currently sourcing `static`+`grounded` units from a `core_essentials`-housed, header-re-attributed root `ce_*.lst` file. A future book-onboarding cycle for another kind under `core_essentials/ce_*.lst` would hit the identical bug. | Did not fix — `v06_work_inventory.rs`'s `apply_done_rung_stamps` is lane 1's file, out of this card's territory. Reported precisely with the exact line numbers and a one-line-per-branch fix (`item.unit.book.clone()` -> `item.unit.source_book.clone()` in BOTH the `Static` and `Derived` arms of `apply_done_rung_stamps`, mirroring the existing `engine_book_for` call sites) so the next cycle touching that file can verify and land it directly rather than re-diagnosing. | *(no ruling needed — a precisely-scoped, high-confidence one-line-per-branch fix for whoever next owns `v06_work_inventory.rs`; would move these 34 `companion` units to `done` immediately, no new ingest required, already `literal-verified`-eligible)* |
| 105 | 2026-08-16 | SD31-E6-F7-001 | NOTE | **Render-readiness report for lane 1's `Kind::Companion` prose done-bar rung, per this cycle's dispatch instruction.** Re-derived `SD31-D7-PROSE-001`'s named ~223-unit target fresh at this tip (unchanged by this cycle's own raw_tokens work, which only touches `static`-wiring-class units and does not overlap): `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print(len([u for u in d['units'] if u['kind']=='companion' and u.get('magnitude_token_count')==0 and u.get('status')=='grounded']))"` -> **223** (178 `display`, 43 `derived`, 2 `ambiguous`). All 223 resolve to real `record_type: "ability"` corpus rows (0 are top-level `"creature"` records, which carry no rendered `description` field at all on `CompanionCatalogEntryDto` — the rung must key on the ABILITY sub-record, not the owning creature). Cross-checked every one of the 223 against its real corpus JSON: **201 carry a real, non-null `data.description`; 9 carry `data.description: null` but real, non-empty `data.description_variants` (the row's every `DESC:` token is conditional — Ultimate Wilderness's shape, `decisions.md §61.1`); 13 carry NEITHER** (genuinely nothing for a player to see — e.g. `advanced_players_guide:companion:eidolon_skills`, a bare `TYPE:SkillChoice` reference row with no `DESC:` token anywhere in its corpus record — these 13 are correctly `held`, not a rung gap). **Render path certified sound for all 210 ready units, corpus-wide, not sampled**: `apps/desktop/src-tauri/src/companion_catalog.rs`'s `serve_ability_description`/`serve_desc_variant` both panic on any leaked PCGen syntax, and `build_companion_catalog()` (which every test below calls) walks every registered book's every ability; the pre-existing `no_served_description_leaks_pcgen_syntax` test already certified the 201-`description` half, and this cycle added `no_served_description_variant_leaks_pcgen_syntax` (commit, this cycle) to close the previously-untested 9-unit `description_variants`-only half the same way (both green, `cargo test --locked companion_catalog::`). **The one gap a rung built on `description` alone would introduce**: 9 of the 223 (4%) would be silently under-claimed the same shape as the equipment/spell 247-unit gap row 70 already named for `.COPY=` inheritance — a `Kind::Companion` rung must check `description.is_some() OR !description_variants.is_empty()`, mirroring `serve_desc_variants`' own promotion logic, not `description` alone. DoD-8: `advanced_players_guide:companion:eidolon_link`'s real corpus description screenshotted rendering on the live Companion Catalog screen this cycle (see this cycle's own `progress.md` receipt for path). | Reported the full per-unit readiness breakdown and the render-path certification to lane 1 (the `Kind::Companion` rung is `v06_work_inventory.rs`, out of this card's file territory) rather than attempting the rung myself. | *(no ruling needed — lane 1 can build the rung directly against these figures: 210 ready via `description.is_some() \|\| !description_variants.is_empty()`, 13 correctly excluded, 5 of the 223 resolve through the pre-existing `bestiary`/`beastiary` book-directory alias — already handled, not a new gap)* |
| 106 | 2026-08-16 | SD31-E6-F7-001 | NOTE | **Traced `feat`'s `static`+`grounded` `held` population (15 units, e.g. `core_rulebook:feat:acrobatic`) one record deep — a structurally different gap from `companion`/`monster_ability`'s.** Those two kinds' `static`+`grounded` populations are individually-ingested `data/corpus/<book>/<kind>/*.json` records simply missing `raw_tokens` (this cycle's own lever for `companion`; `SD31-E6-F9-001`'s precedent for `monster_ability`). `feat` is different: `data/corpus/core_rulebook/feat/` **does not exist at all** (`find data/corpus -maxdepth 2 -type d -iname feat` -> only `advanced_race_guide/feat` and `pathfinder_unchained/feat`, both already-dumped `feat_gap_tables`-derived residue, already wired live into `feats_all::all_feat_tables()`/`feat_catalog.rs` — re-checked this cycle, NOT a stale lead). `core_rulebook`'s feats (and most other books') are served straight from a hand-authored `rules_tables` Rust table with no corresponding `data/corpus/**/feat/*.json` record for `corpus_literal_sweep` to examine — there is no file to add `raw_tokens` to. Reaching `literal-verified` for this population needs a NEW cache_gen dump (a `cache_gen::feat` module analogous to `cache_gen::ultimate_equipment`/`cache_gen::equipment_gap`, corpus-wide across every hand-authored feat book, potentially touching a large fraction of the kind's ~2,600 units) — a properly-scoped ingest project of its own, not a same-cycle extension of the 15-unit population it would start from. | Did not attempt — real scope is materially larger than the 15 units that motivated the trace, and a rushed partial cache_gen module risks exactly the kind of half-ingested state the "no stubs" doctrine forbids. Logged with precise scope and the correct precedent shape to follow. | *(no ruling needed — a well-scoped follow-up for a future `epic-6-ingest-lanes` F8 cycle: build `cache_gen::feat` on the `cache_gen::ultimate_equipment` precedent, dump every hand-authored book's feat rows to `data/corpus/<book>/feat/*.json` with real `raw_tokens`, then re-run `corpus_literal_sweep`)* |
| 107 | 2026-08-16 | SD31-W6-INTEGRATE-001 | NOTE | **Rows 69/87/95's flat-magnitude question, re-sized at the wave-6-merged tip per the standing "size it, don't fix it" instruction -- the CONFIRMED finding that the original receipt's "conservative default" was applied to 1 unit while the shape rides on hundreds.** Re-derived fresh (not quoted from the prior wave's finding): of the **937** `monster_ability` units currently `text-complete` (20 fewer than the original 947 -- this integration cycle's own `monster_ability_desc_leaks_unresolved_argument` fix, row 104's sibling, removed the character-specific-argument shape from this population first), **645 (68.8%)** state a numeric magnitude in their raw `DESC:` token (`(?<![A-Za-z%])[+-]?\d` over each unit's own cited `.lst` row, matched by `(source_file, source_line)`, 937/937 resolved, 0 misses). Of the **308** `equipment`/`equipment_modifier` units currently `text-complete` that resolve to a real `data/corpus/**/equipment/*.json` citation (311 total, 3 unmatched -- a small residual join gap, not investigated further this cycle), **179 (58.1%)** carry the same shape in their recovered `data.description`. Neither population was touched, excluded, or promoted differently because of this count -- the ruling stays exactly as open as rows 69/87/95 left it; this row exists only so the operator's answer, whenever it lands, arrives knowing its true blast radius (roughly 824 units total across both kinds, not the single named unit the conservative default actually excluded). | Sized, not decided -- `wiring_class.rs`'s `prose_scaling_phrases` detector (the actual general fix either reading would drive) remains untouched, out of this integration cycle's scope same as every prior cycle that has hit this question. | *(needs the SAME operator ruling rows 69/87/95 already ask for -- now sized: ~824 units total ride on the answer, all currently `done`)* |
| 108 | 2026-08-16 | SD31-W6-INTEGRATE-001 | NOTE | **SUPERSEDED WITHIN THIS SAME CYCLE — the deferral below was wrong, and this row records why, rather than being silently deleted.** Originally logged as "deliberate design tradeoff, named rather than rushed" — a decision NOT to widen `render_pcgen_desc` to drop `%<KEYWORD>` tokens, reasoning that the equipment catalog's live `%CHOICE` exposure was a "genuine live-user regression risk" too large to take unreviewed. That framing was refuted minutes later by this cycle's own first full-gate run: `apps/desktop/src-tauri/src/equipment_catalog.rs`'s PRE-EXISTING pinned test `no_catalog_serves_a_description_carrying_raw_pcgen_syntax` went RED the moment `leaked_pcgen_syntax` (row 104's sibling fix) was widened to detect `%<KEYWORD>` — proving the exposure was never hypothetical: it is a live, currently-shipping defect (6 real records, not 5 — `Masterwork Tool: %CHOICE circumstance Bonus` was a 6th this row's own earlier count missed) that a pinned test ALREADY treats as gate-blocking. Leaving it unfixed was not an option once the gate was red for the right reason. **Fixed for real, doctrinally consistent option (a) from this row's own remedy**: widened `render_pcgen_desc`'s substitution loop to drop an unresolved `%<KEYWORD>` the same no-fabrication way it already drops an unresolved `%N` (no `PcgenDisplayValues` slot exists for a chargen-time player selection like a bloodline choice, so there is no resolution path to attempt). Checked against all four render consumers' own corpora before landing, per this row's own stated bar: full lib suite 1894/1894 unchanged, desktop suite 448/448 (was 446 — the two equipment_catalog tests that had been failing now pass), zero regressions in monster/companion/spell. The equipment-lane's own pinned "54-leak" fixture test was updated to 58 (the widened detector also now counts 4 real, always-present ACG `%CHOICE` occurrences in the RAW tables it never counted before). **Also fixed a resulting regression in this cycle's own `corpus_json_description_leaks_pcgen_syntax`** (row 104's sibling): once `render_pcgen_desc` cleanly drops `%CHOICE`, checking only `leaked_pcgen_syntax(&rendered.text)` no longer catches it — the text is now CLEAN but INCOMPLETE (a hole in the sentence), the exact Decision-7 condition-3 shape row 104's `monster_ability_desc_leaks_unresolved_argument` already refuses for its own kind. Added a second condition, `!rendered.dropped_args.is_empty()`, so the equipment rung stays consistent with the monster_ability rung's own discipline — re-ran the guarded regen after landing this: board figure unchanged (9,488/24.63%, byte-identical to the pre-fix regen except `generated_at`), confirming the two fixes net to zero doneness-credit movement, as they should. | Fixed at the root rather than left deferred, once the gate proved the exposure was real rather than hypothetical. 5 new `pcgen_desc` tests (confirmed RED before the fix), the equipment-lane's own pinned fixture updated, and the equipment verdict rung's own refusal condition widened to match. | *(none needed — discharged; the render path and the promotion rung now agree, and both are proven against the live desktop test suite, not just the corpus binaries)* |
| 123 | 2026-08-16 | SD31-E6-F8-001 | NOTE | **`mythic_adventures` `feat` (566 not-started, dispatch's own §10-cited population) is structurally blocked the same way `class` (row 124) is — no `RuleSetId` variant exists for the book at all, so it cannot close via ordinary per-book ingest.** Re-derived: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x.get('kind')=='feat' and x.get('book')=='mythic_adventures']; print(len(u), set(x.get('evidence') for x in u))"` → **566, {'no_compiled_rule_set_for_book'}** — every single one, not a citation/attribution gap. `grep -n "enum RuleSetId" -A 200 src/rules_core/rules_tables/mod.rs` confirms no `MythicAdventures`/`Ma` variant exists, and `grep -n COMPILED_RULE_SETS src/bin/v06_work_inventory.rs` confirms the compiled-rule-set registry (the thing a new variant would need adding to) lives entirely in `v06_work_inventory.rs` — lane 3, out of every `epic-6-ingest-lanes` F-card's file territory, this one included. Onboarding the book (a new `RuleSetId`, a `rules_tables/mythic_adventures/feat_tables.rs`, wiring into `feats_all::hand_authored_feat_tables()`, and the `COMPILED_RULE_SETS`/`corpus_dir_for`/`rule_set_id` triad in `v06_work_inventory.rs`) is genuine per-book ingest work, same shape as `SD31-E6-F5-002`'s `inner_sea_gods` recommendation for equipment — but needs an operator-authorized lane-3 write, not something this card can do unilaterally. **The dispatch's own §10 warning is confirmed live and quantified for whoever does this next:** `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); U=d['units']; f=[u for u in U if u.get('kind')=='feat']; ma=set(u.get('corpus_key') for u in f if u.get('book')=='mythic_adventures'); crb=set(u.get('corpus_key') for u in f if u.get('book')=='core_rulebook'); print(len(ma & crb))"` → **96** `(kind=feat, corpus_key)` collisions between `mythic_adventures` and `core_rulebook` (e.g. `Acrobatic`, `Alertness`, `Arcane Strike`), matching Decision 10's amendment's cited "95 objects" figure almost exactly. Per that amendment, `pathfinder_unchained` and `mythic_adventures` default to **variant, never supersession** — whoever builds the Supersession Register (a DIFFERENT epic/lane) must blanket-exclude these 96 pairs, and whoever ingests the mythic feats must ship them as their own distinct records (already naturally true today: `id` embeds `book`, so `mythic_adventures:feat:acrobatic` and `core_rulebook:feat:acrobatic` are already separate unit ids with no merge risk at the ingest layer — the risk is entirely in a future supersession-pairing pass, not in this ingest). | Did not attempt to onboard the book (would require an out-of-territory `v06_work_inventory.rs` edit) and did not ingest any of the 566 units. Reported the structural blocker and pre-quantified the §10 collision set so whoever does the onboarding does not have to re-derive it under gate pressure. | *(no ruling needed on the blocker — a future cycle with lane-3 write authority should onboard `mythic_adventures` as its own `RuleSetId` on the `inner_sea_gods`/`ultimate_magic` book-onboarding precedent, and the 96-pair collision list above should ship WITH that cycle's receipt so the eventual Supersession Register lane inherits it rather than re-deriving it)* |
| 124 | 2026-08-16 | SD31-E6-F8-001 | NOTE | **`class` (F10) traced end to end, per the dispatch's own "trace one unit before anything else" instruction — the kind is structurally blocked from independent ingest, correcting `epic-breakdown.md`'s SD31-E6-F10 acceptance text ("real per-book ingest; no probe or fixture blocks this kind").** Re-derived (`python3` over `docs/work-inventory.json` via `pf1e_dashboard_producer.doneness_verdict`): **158 of 185 `class` units not-done** (146 `visible: true`, 12 `visible: false`). Traced one of each: `advanced_class_guide:class:arcanist` is `done` (`status: grounded`, `evidence: class_probe_observed_computed_delta_on_the_rendered_snapshot`); `advanced_class_guide:class:ex_warpriest` is `not-ingested` (`evidence: class_absent_from_ClassId_ALL_and_book_class_id_enums`). Reading `src/bin/v06_work_inventory.rs`'s `Kind::Class` arm (line ~4613 `class_probe_observed_computed_delta_on_the_rendered_snapshot`/`ClassId::ALL`/`ApgClassId::ALL`/`AcgClassId::ALL` checks): a class reaches `done` ONLY by (a) being registered in a per-book `ClassId`-shaped enum AND (b) having a full BAB/save/HD/spellcasting chassis wired in `pilot_compute.rs` so the probe observes "a computed delta on the rendered snapshot" — both mechanisms live entirely in `v06_work_inventory.rs` (lane 3) and `pilot_compute.rs` (lane 4), which every `epic-6-ingest-lanes` card (not just this one) is explicitly barred from touching. **This is not a per-book ingest gap the way `equipment`/`feat` are** — it is the SAME instrument Epic 3/4/5's per-class chassis sweep already builds one class at a time for `class_feature`; a `class` unit's own record and its `class_feature` siblings are separately-tracked kinds riding the identical chassis-wiring mechanism, so `class`-kind closure is a byproduct of Epic 5 landing a class, not an independently workable F10 lever. Separately, of the 158 not-done: **27 are `bestiary`-book `ce_classes_race.lst`/`b1_classes_race.lst` rows** (`Aberration`, `Animal`, `Construct`, `Humanoid`, `Outsider (Fort/Ref)`, etc.) — PF1's per-creature-TYPE monster-advancement HD-progression tables, a genuinely different game object from a player-selectable base class, `visible: true` in the corpus despite never appearing in a chargen class picker; and **12 are `visible: false`** (`Ex-Warpriest`, `cr_feats.lst`-style PCGen-internal bookkeeping shadow-classes, `TYPE:...Internal`/`VISIBLE:NO` on their own corpus row) that no player ever selects. Neither population was excluded or specially handled — both stay `class`-kind, not-done, in the denominator; flagged here as a scope question for whoever eventually builds the chassis-wiring capability, not proposed for removal. Verifying command: `python3 -c "import json,sys,collections; sys.path.insert(0,'scripts/observer'); import pf1e_dashboard_producer as P; d=json.load(open('docs/work-inventory.json')); U=[u for u in d['units'] if u.get('book') not in P.EXCLUDED_BOOKS]; cls=[u for u in U if u.get('kind')=='class']; v=lambda u: P.doneness_verdict(u.get('wiring_class'),u.get('status'),u.get('kind')); nd=[u for u in cls if v(u)!='done']; print(len(cls), len(nd), sum(1 for u in nd if u.get('visible')), sum(1 for u in nd if not u.get('visible')))"` → `185 158 146 12`. `retro.py correction` emitted against `epic-breakdown.md`'s SD31-E6-F10 acceptance text. | Did not attempt a class ingest (would require editing `v06_work_inventory.rs`/`pilot_compute.rs`, both explicitly out of this card's — and every F-card's — file territory) and did not propose a Structural Exclusion Register entry (the 158 are not impossible, they are gated on capability work already tracked elsewhere in the package). Reported the structural finding instead of a partial/fake ingest. | *(no ruling needed on the blocker itself — a future cycle with lane-3/lane-4 write authority should treat `class`-kind closure as a checkbox on Epic 5's per-class chassis-sweep cycles, not a separate F10 ingest lane; the 27 monster-HD-progression and 12 `VISIBLE:NO` populations may eventually want their own operator ruling on whether they belong in the `class`-kind denominator at all, similar to the `core_essentials`/Bestiary-monster-count precedents, but this cycle did not propose removing either)* |
| 109 | 2026-08-16 | SD31-W6-INTEGRATE-001 | NOTE | **DoD-8 for the 34-unit companion join-key fix (row 104), plus a narrow `verify-on-screen.sh` timing gap found while capturing it.** Captured `core_essentials:companion:cat` (a real unit the join fix unlocked) rendering live on the Companion Catalog screen -- `Ability score adjustments (corpus BONUS:STAT tokens): STR -8, DEX +4, CON -2, INT -8, WIS +2, CHA -4`, byte-matching the corpus row's own six `BONUS:STAT` tokens. The script's own automated run reported a false FAILED first (`companion-cat.FAILED.verify.md`, kept as evidence): its hub-click landed correctly (confirmed by a direct screenshot immediately after) but the marker-read ran before the Companion Catalog screen's own async `Loading catalog...` state resolved -- a load-timing race, NOT `row 93`'s coordinate-drift shape (that row is about `SEARCH_Y` landing on the wrong element entirely for `race_trait`; this is the right element, read too early). Worked around by driving `driver.sh` directly with a short wait between the hub click and the next action. | Captured DoD-8 manually rather than leaving it undischarged or citing the script's own false-negative as blocking; logged the new, narrower timing-race shape separately from row 93 rather than conflating two different root causes under one coordinate-bug row. | *(no ruling needed -- a future desktop-driver cycle recalibrating row 93's `SEARCH_Y` table should also add a short post-navigation settle delay before the marker read, which would fix both shapes at once)* |
| 110 | 2026-08-16 | SD31-D7-PROSE-003 | NOTE | **CONFIRMED live regression, caught by the guarded regen's own before/after diff before it landed — a REAL bug the new `class_feature`/`companion` rungs introduced, fixed the same cycle, not left for a future one.** Extending the prose done-bar rung to `Kind::ClassFeature` initially promoted a `grounded` verdict to `text-complete` unconditionally on `text_only && has_real_description`, mirroring `SD31-D7-PROSE-002`'s `monster_ability` shape. The first guarded regen found **1 unit demoted off `done`**: `advanced_class_guide:class_feature:bloodrager_raging` (`wiring_class: computed`, `wiring_class_reason: pre_guard` — a bare `PRE:` guard alone, with ZERO magnitude tokens, is enough to classify `computed`) reached `grounded` via `explanation_id_observed_in_a_real_computation` and was already `done` (`doneness_verdict` maps `computed`+`grounded` straight to `done`); the unconditional promotion rewrote it to `text-complete`, which `computed` has no special case for and which falls through to `in-progress` — a silent regression the mandate's own "zero units regressed off done" check exists to catch. **Root cause is general, not class_feature-specific**: `grounded`→`text-complete` is only a genuine improvement for `wiring_class == "display"` (where `grounded` caps at `held` but `text-complete` reaches `done`); for `computed`, `grounded` ALREADY reaches `done` on its own, so promoting past it is strictly worse. `static`/`derived`/`ambiguous` are neutral (`grounded` and `text-complete` both map to `held`). **Fixed by threading `wc_class` into `classify()`** (a new, required parameter — every call site in the file updated, 25+ sites) and gating the promotion in all THREE sites this cycle's rung touches (`class_feature` ×2, `companion` ×1) on `wc_class == "display"` via a new `is_display_wiring_class_for_promotion` helper. Re-ran the guarded regen after the fix: `moved away from done: 0`, confirmed. Two new regression tests pin the exact reproduced case (`a_computed_wiring_class_grounded_feature_is_never_promoted_even_when_otherwise_qualifying` and its companion sibling), both red before the fix, green after. **NOT fixed this cycle, logged as a live, unresolved hazard**: this exact shape (`wiring_class: computed` + `status: text-complete`) already exists on the PRE-EXISTING, untouched `feat`/`spell` promotion branches (`ultimate_wilderness:feat:exotic_heritage`, 3 `advanced_players_guide:spell:evolution_surge*` units) — re-derived: `python3 -c "import json; d=json.load(open('docs/work-inventory.json')); print([u['id'] for u in d['units'] if u.get('wiring_class')=='computed' and u.get('status')=='text-complete'])"`. Investigated one record deep before concluding it is NOT the same bug: `Kind::Feat`'s `text_only` promotion branch is reached ONLY after `feat_effect_wired.contains(key)` already returned false (an early `return` inside that `if`), so it can never intercept an existing `grounded` evidence path the way `class_feature`/`companion`'s DID — these 4 units were never `grounded` via any other path, so `computed`+`text-complete`→`in-progress` is their correct, non-regressed verdict, not an under-claim. Logged only so a future cycle re-deriving this population does not re-investigate the same question from scratch. | Fixed the two new promotion sites this cycle owns; traced the pre-existing `feat`/`spell` shape one record deep and confirmed it is a DIFFERENT, non-regressed pattern rather than assuming it needed the same fix. | *(no ruling needed — a general-purpose correctness fix, not a scope or doneness-bar question; `wc_class` is now available to `classify()` for any future rung that needs it)* |
| 111 | 2026-08-16 | SD31-D7-PROSE-003 | NOTE | **Decision 7's PROXY WARNING, discharged against this cycle's own promoted population, not skipped.** Hand-checked the FULL, untruncated corpus `DESC:` text of all 43 `class_feature` units this cycle's rung would otherwise promote (not a stratified sample — the whole population, since it is small enough to read in full) and found **11** that state a real, flat, non-scaling numeric bonus, penalty, range, resource cost or duration in prose only — the identical shape row 87's own worked example names (`duergar_stability`'s "+4 racial bonus to CMD"): `advanced_players_guide:class_feature:cave_druid_wild_empathy` ("-4 penalty on her wild empathy check"), `core_rulebook:class_feature:barbarian_indomitable_will` / `pathfinder_unchained:class_feature:unchained_barbarian_indomitable_will` ("+4 bonus on Will saves"), `core_rulebook:class_feature:bard_well_versed` ("+4 bonus on saving throws"), `core_rulebook:class_feature:monk_empty_body` ("1 minute", "3 points for your Ki pool"), `core_rulebook:class_feature:monk_still_mind` / `pathfinder_unchained:class_feature:unchained_monk_still_mind` ("+2 bonus on saving throws"), `core_rulebook:class_feature:paladin_aura_of_faith` ("within 10 feet"), `core_rulebook:class_feature:paladin_aura_of_justice` ("two uses", "within 10 feet", "1 minute"), `core_rulebook:class_feature:ranger_swift_tracker` ("-5 penalty", "-10 penalty"), `pathfinder_unchained:class_feature:unchained_barbarian_tireless_rage` ("within 1 minute"). The other 32 genuinely state nothing but level-gate references ("At 4th level...") or bare feat/ability grants with no accompanying magnitude (`bloodrager_eschew_materials`, `sorcerer_eschew_materials`, `wizard_scribe_scroll`) — real prose-only, nothing-to-compute records under EITHER reading of the open question. Excluded the 11 via `CLASS_FEATURE_FLAT_MAGNITUDE_PENDING_RULING` (`src/bin/v06_work_inventory.rs`), the conservative default the card's own dispatch instruction named, pending the SAME operator ruling rows 69/87/95/107 already ask for. `companion`'s 165-unit promoted population was ALSO hand-checked this cycle (full closure, not sampled) and found genuinely clean — zero flat-magnitude-in-prose hits — so `COMPANION_FLAT_MAGNITUDE_PENDING_RULING` stays empty, verified rather than assumed. | Excluded the 11 conservatively rather than deciding the open interpretive question; did not touch `wiring_class.rs`'s `prose_scaling_phrases` detector (the actual general fix either reading would drive, and a sibling lane's file). | *(needs the SAME operator ruling rows 69/87/95/107 already ask for — now sized with 11 more named `class_feature` units; clearing `CLASS_FEATURE_FLAT_MAGNITUDE_PENDING_RULING` is a one-line change once it lands)* |
| 112 | 2026-08-16 | SD31-D9-DISSOLVE-001 | NOTE | **Decision 9 discharged: `core_essentials` re-attribution completed for the 516-unit gap `SD31-ATTRIB-002` (row 98) found but did not fix (out of that cycle's file territory).** `resolve_true_book_for_core_essentials` (`v06_work_inventory.rs`) is now source-line-aware: it tracks which of `ce_abilities_race.lst`'s 11 mid-file `SOURCELONG:<Book>` directive lines most recently preceded each row, instead of only checking a file's first 5 lines. Re-derived corpus-wide: `core_essentials` residual **644 → 128** (`monster_ability` 378→9, `race_trait` 258→111, `race` 8→8 unchanged — the 8 ambiguous races were already correctly left unattributed by prior cycles). Newly-resolved 516, by book: `bestiary` 263, `bestiary_2` 206, `bestiary_3` 41, `bestiary_4` 2, `bestiary_5` 1, `bestiary_6` 3 — matching row 98's own prediction exactly. `corpus_literal_sweep.rs`'s `short_book_of` synced in the same commit (previously disclaimed root-level `ce_*.lst` resolution entirely). Guarded regen: **0 doneness-verdict transitions** (pure relabel, same 0-transition proof every prior book-attribution fix in this program has produced), `corpus_literal_sweep` stays CLEAN (0 findings, 23,859 records examined). **A first draft shipped a real regression, caught before commit**: an unrecognized directive (`SOURCELONG:Universal Rules`) silently inherited the PRECEDING recognized directive's book instead of resetting to unattributed — fixed and mutation-proven both directions (`an_unrecognized_directive_resets_tracking_rather_than_inheriting_the_prior_book`; also proved the fix's absence regresses the real-corpus residual to 986 via a separate mutation). The remaining 129-unit residual (measured via the new real-corpus ratchet gate below, one more than the full pipeline's 128 because that gate's own `.MOD`-rescue pass isn't replicated) is genuinely unattributable: 23 rows precede the file's first `SOURCELONG:` line (book-agnostic PCGen internal bookkeeping per the file's own top comment), 6 carry `SOURCELONG:Universal Rules` (PCGen's own internal designation, not a Paizo book), and the 8 races two or more in-scope books natively declare contribute the rest. **The "books outside the roster" watch the dispatch named (Ironfang Invasion, Blood of the Moon, Universal Rules) resolved to a non-issue within this card's scope** (`monster_ability`/`race_trait`/`race` kinds only): none of the 128/129 residual units cite Ironfang Invasion or Blood of the Moon at all, and the 6 Universal Rules rows correctly stay unattributed rather than being guessed onto a real book. Decision 9's own census counted these book names across ALL of `core_essentials/` (every kind), so they may still surface in `equipment`/`feat`/other-kind residue outside this card's file territory — flagged for whichever lane owns those kinds, not investigated here. **The un-producible gate**: `core_essentials_book_attribution_tests::core_essentials_real_corpus_residual_never_grows_past_its_pinned_baseline` walks the REAL pinned oracle (not a scratch fixture) and pins the residual at 129 as a ratchet — mutation-proven to fail (residual jumped to 986) when the SOURCELONG-resolution mechanism was disabled, and to genuinely NOT catch a different, subtler regression class (the Universal-Rules-inheritance bug, which SHRINKS the residual rather than growing it — that class is caught by the scratch-fixture mutation proof instead, not the real-corpus ratchet; documented so a future cycle does not over-trust either gate alone). | Landed the fix, TDD throughout (12 new scratch tests + 1 real-corpus ratchet test, all failing red before their fix, all green after), re-derived every figure against the real corpus rather than trusting the dispatch's stale 644/53/four-races framing, and mutation-proved both the fix and the gate. | *(no ruling needed — discharged. The 129-unit residual is the honest floor; a future cycle could shave it further only by getting an operator ruling on the 8 genuinely-ambiguous races, which this card's own dispatch already anticipated as a possible permanent state: "A race that still cannot be resolved is left unattributed and said so.")* |
| 113 | 2026-08-16 | SD31-D9-DISSOLVE-001 | RULING-NEEDED | **Decision 10's own worked-example list contains a factual error, corrected here rather than silently followed.** The ruling names "Changeling (now bestiary_4, also ARG)" as a race that should move to `advanced_race_guide` under "most recent publish wins." Re-derived against the pinned oracle: `advanced_race_guide/advanced_race_guide.pcc`'s own `SOURCEDATE:2012-06` is OLDER than `bestiary_4/_bestiary_4_for_players.pcc`'s own `SOURCEDATE:2013-10` — Bestiary 4 is chronologically the NEWER of the two printings, the opposite of what the worked example implies. Under Decision 10's own binding rule ("Publication order comes from the .pcc headers' SOURCEDATE: — use it; never date a book from memory"), Bestiary 4's own 5 ARG-reprinted races (Changeling, Kitsune, Nagaji, Samsaran, Wayang) were therefore deliberately EXCLUDED from `RACE_NEWEST_PRINTING` and stay attributed to `bestiary_4` — moving them to ARG would be the exact reverse of what the ruling's own SOURCEDATE-ordering instruction requires. The most likely explanation: ARG's own `.lst` files (a living PCGen data file, continuously maintained) reference these 5 races' content because PCGen contributors extended ARG's Race Builder framework to cover them AFTER Bestiary 4 published, not because the physical 2012 ARG book itself reprinted 2013 content — i.e. "a row exists in `arg_*.lst`" is a PCGen-data-maintenance signal, not proof the physical book printed it, for this specific sub-case. **32 races (7 Core Rulebook + 11 Bestiary 1 + 7 Bestiary 2 + 5 Bestiary 3 + 2 Inner Sea World Guide — every one of ARG's reprints that IS strictly older than ARG by SOURCEDATE) were moved to `advanced_race_guide` as the ruling intends**; `advanced_race_guide` race now reads 33 (32 moved + its own pre-existing `Race Builder` scaffold unit, `not-started`), `core_rulebook` race now reads 0 (all 7 moved) — re-derived, not the operator's earlier "50 of 103" figure (which this cycle could not reproduce exactly; see row 112). | Applied Decision 10 exactly as its own binding SOURCEDATE-ordering rule specifies, over its own inconsistent worked example, and flagged the discrepancy rather than silently following either the wrong example or my own reading. Mutation-proof + pinned test (`bestiary_4_races_are_not_in_the_newest_printing_table`) guards against either direction of this being silently reversed later. | *(needs operator confirmation: is the Changeling/Bestiary-4 example in decisions.md a drafting error to correct in that doc, or is there a different, non-SOURCEDATE signal the operator intended for that specific sub-case that this cycle's strict-SOURCEDATE reading missed? Either answer leaves the 32-race move as landed; only the 5 Bestiary-4-native races' fate is open.)* |
| 114 | 2026-08-16 | SD31-D9-DISSOLVE-001 | NOTE | **Decision 10's own "50 of 103" figure could not be reproduced exactly; this cycle's independent re-derivation found 32, and the gap is explained, not silently absorbed.** The ruling's own text says the signal was "scanning [the ARG per-race-row signal] across all books," without naming the exact script. This cycle's own derivation used the already-established, gate-tested 37-race ARG-reprint roster (`decisions.md §25.2`'s table, cross-checked against `arg_races.lst`'s own 37 `.MOD` lines by a prior cycle) filtered to strict SOURCEDATE ordering (Bestiary 4's 5 excluded per row 113) — yielding 32, not 50. The operator's own worked examples for the 50-figure name exactly the same races this cycle's 32-set already covers (Aasimar/Dhampir/Fetchling/Grippli/Drow/Duergar/Goblin/Hobgoblin, all Core Rulebook/Bestiary-1/Bestiary-2) plus the one Bestiary-4 case row 113 disputes — none of the operator's own examples name a race OUTSIDE the 37-race ARG roster this cycle already scanned. The 18-unit gap (50 vs 32) most likely comes from either (a) counting Bestiary 4's other 4 races the ARG roster doesn't reprint at all (kasatha/trox/wyrwood/wyvaran — genuinely Bestiary-4-native, no ARG pair exists) as if they had one, or (b) a broader signal than this cycle used (e.g. matching on ANY shared ability/feat/equipment name rather than the specific 37-race `.pcc`-section roster) that would need the SAME variant-guard care Decision 10's own amendment already demands for `pathfinder_unchained`/`mythic_adventures`. Not chased further this cycle — the 32-race set is fully evidenced per-race (37-race roster ∩ SOURCEDATE-older-than-ARG), and widening the signal without that same rigor risks exactly the kind of unproven relabel Decision 1(a) forbids. | Landed the fully-evidenced 32-race subset rather than blindly reproducing an unverifiable 50-figure, and named the gap precisely instead of quietly matching the operator's number. | *(no ruling strictly needed — the 32-race move stands on its own evidence — but a future cycle with more time could re-run the operator's own original scan methodology, if it can be recovered, to check for the remaining ~18 and apply the same evidence bar)* |
| 115 | 2026-08-16 | SD31-D10-REGISTER-001 | NOTE | **Independent confirmation, from the Supersession Register cycle, that row 98's 644-unit/516-further-resolvable finding is STILL unfixed at this cycle's tip -- not a new defect, a re-derivation landing on the same one.** Card sequencing (Decision 10, "you are not first") required excluding every `book == "core_essentials"` unit from supersession pairing; re-deriving that population fresh against the wave-6-merged `docs/work-inventory.json` (tip `2ae22bdae`) found **644** (`monster_ability` 378, `race_trait` 258, `race` 8), byte-identical to row 98's own figure and to `SD31-ATTRIB-002`'s HEAD-`5d0cd1595` measurement -- confirming `resolve_true_book_for_core_essentials()`'s first-5-lines `SOURCELONG:` scan (row 98's full root-cause: `ce_abilities_race.lst`'s 11 mid-file directive lines, 516 of 545 further-resolvable via nearest-preceding-directive) has not been fixed by any cycle in between. Traced the card's own worked example, `core_essentials:monster_ability:kyton_unnerving_gaze`, independently: still `book: "core_essentials"`, still unresolved, matching row 98's shape exactly. | Excluded all 644 from this cycle's supersession pairing (per the card's own sequencing instruction) rather than re-diagnosing past row 98's already-precise root cause and remedy. | *(no new ruling needed -- row 98 already names the exact fix: make `resolve_true_book_for_core_essentials` source-line-aware and sync `corpus_literal_sweep.rs`'s `short_book_of` in the same commit; this row exists only to confirm it is STILL open and to note the Supersession Register's "deferred to post-dissolution pass" pairs (`SUPERSESSION-REGISTER.md` §1) are waiting on it)* |
| 116 | 2026-08-16 | SD31-D10-REGISTER-001 | NOTE | **The Supersession Register's denominator reduction (135 units, 38,521 -> 38,386) is built and gate-proven but NOT applied to the live pipeline -- here is the exact change needed, so it does not have to be re-derived.** `docs/release/SD-31-corpus-closure-grind/artifacts/SUPERSESSION-REGISTER.json` names 117 proven-duplicate objects (135 superseded units, field-level evidence re-derived from the pinned oracle, gate-checked). Applying it to the mandate denominator needs `v06_work_inventory.rs` (or its consumer, `scripts/observer/pf1e_dashboard_producer.py`) to build an `EXCLUDED_UNIT_IDS` set from the register's `objects[].superseded[].id` values and skip those units from every corpus-wide denominator/rollup computation, the same way `EXCLUDED_BOOKS` already skips a whole book -- additive, no `done`/`held`/verdict logic touched. Re-checked against the real `doneness_verdict()` before filing this row (a first pass wrongly checked raw `status` alone and is corrected in the register's own §8, `retro.py correction`): **36 of the 135 superseded units are currently `done`** (34 `equipment`, 1 `companion`, 1 `monster`) -- applying the register moves BOTH sides, numerator 9,488->9,452 and denominator 38,521->38,386, and the mandate headline moves 24.6307%->24.6236% (down slightly, not up, since the superseded population's own done-rate is a touch above the board average). Every one of those 36 keeps its `done` credit on the SURVIVING side of its pair -- only the superseded duplicate stops being counted. | Reported the precise change and left `v06_work_inventory.rs` untouched -- its doneness/rung path is lane 1's file territory, out of this card's write scope (`SUPERSESSION-REGISTER.md/.json`, the register's own gate, and this row are this card's actual deliverable). | *(no ruling needed -- a mechanical follow-up for lane 1 or a future integration cycle: add `EXCLUDED_UNIT_IDS`, wire it alongside `EXCLUDED_BOOKS` in every denominator computation, re-run the guarded regen, and re-derive the mandate headline against the new 38,386 denominator)* |
| 117 | 2026-08-16 | SD31-E4-F1-003 | NOTE | **`SD31-E3-F1-001`'s clearance table's `named_raw: 0` for Ninja is wrong -- corrected this cycle, per this card's own "verify rows as you consume them" instruction.** The clearance table's evidence method (`grep -oE 'Ninja_Archetype_[A-Za-z0-9]+' .../ultimate_combat/uc_abilities_class.lst`) searched one file only and found nothing, but Ninja has exactly one real archetype in the 23-book scope: `Ninja Archetype ~ Scout`, whose source row lives in a NESTED subdirectory (`ultimate_combat/support/uc_abilities_class_apg.lst:5`) the single-file grep never reached -- the identical class of miss row 1 already names for `wiring_class::CorpusLines::line()`'s single-level directory join, now confirmed to also silently understate a clearance-table evidence figure, not only a `wiring_class` verdict. Found via `docs/work-inventory.json`'s own already-ingested `corpus_key: "Ninja Archetype ~ Scout"` record (from `SD31-E5-F1-001`'s 21-book corpus dump), then verified against the raw `.lst` row directly. **A second Ninja archetype exists in a DIFFERENT book entirely and was NOT wired this cycle**: `inner_sea_intrigue:class_feature:ninja_archetype_frozen_shadow` (`isi_abilities_class.lst`) -- out of this card's tractable scope for one cycle (Inner Sea Intrigue's own `archetype_tables.rs` does not exist yet, a separate book-onboarding lift), named rather than silently missed. Samurai's own `named_raw: 0` was re-checked (targeted nested-directory + corpus-wide `corpus_key` search for "Samurai Archetype") and confirmed genuinely zero -- not the same miss shape. | Wired Scout for real this cycle (`archetype_tables.rs` + `pilot_compute.rs` supersession); named Frozen Shadow rather than attempting a second book's onboarding in the same cycle. | *(a future cycle sizing Epic 3's clearance table more broadly should re-run the nested-directory-aware search this row used across every book's own archetype grep, not just Ultimate Combat -- the miss shape is structural to the evidence method, not book-specific)* |
| 118 | 2026-08-16 | SD31-E4-F1-003 | BLOCKER | **Row 96's `modelled_class_books()` gap and row 97's `CLASS_OPTIONS` gap both reproduce identically for Ninja, confirming they are structural to every non-CRB/APG/ACG class this epic wires, not Gunslinger-specific.** `src/bin/v06_work_inventory.rs`'s `modelled_class_books()` (lane 1's file, out of this card's territory) still names only `ClassId`/`ApgClassId`/`AcgClassId` -- confirmed by direct re-read this cycle, unchanged since row 96. Consequence: Ninja's real corpus `class_feature` records (Sneak Attack, Ki Pool, Ninja Trick, No Trace, Uncanny Dodge/Improved Uncanny Dodge -- all genuinely computed by this cycle's own new wiring, proven via `build_pilot_headless_receipt`) cannot reach `done` OR `held` on the board; `class_feature_owner` returns `None` before the `explanation_id` suffix check (row 78) is ever reached. Board delta this cycle: predicted and confirmed **0/0** (measured below), the same outcome row 96 predicted for every future non-CRB/APG/ACG class until it is fixed. Separately, `apps/desktop/src/characterHub/characterHubModel.ts`'s `CLASS_OPTIONS` (frontend, out of this card's territory) carries no `ninja` entry (`grep -rn ninja apps/desktop/src/` -> 0 hits outside this cycle's own new Rust files), so DoD-8's full on-screen character-sheet proof is blocked one level before the archetype-picker gap, identically to row 97's own finding for Gunslinger -- not attempted live this cycle since the source read already answers it unambiguously (same reasoning row 97 gave). Reachability of the computation itself is proven via 9 new `build_pilot_headless_receipt`-based tests instead, per SD31-E4-F1's own named acceptance standard. | Wired the real chassis and supersession anyway (per this card's own "your deliverable is real wired classes, do not chase board movement" instruction) rather than declining the class because its board credit is blocked. Reported both blockers precisely rather than editing across file-territory boundaries. | *(same dispatch decision row 96/97 already ask for -- this row exists so the operator/lane-1 owner sees the blocker is now confirmed to recur on every new class this epic wires, raising the priority of fixing it once rather than re-discovering it per class)* |
| 119 | 2026-08-16 | SD31-E6-F2-005 | NOTE | **Traced ONE `spell` `held` unit end to end before doing any ingest work, per this card's own instruction.** `advanced_class_guide:spell:adhesive_blood` (`wiring_class=derived`, `status=ingested-magnitude`) is short of the `fixture-verified` rung `derived_evaluator_fixture_check` would need to grant, because its corpus row states `DURATION:(CASTERLEVEL) minutes` — a real caster-level-scaled formula `src/rules_core/derived_evaluator_fixture_check.rs` has **zero evaluator seams for `kind=spell`** to check (only `monster`'s narrow SLA-caster-level rule exists). This independently reproduces row 76's own finding at scale (91%, 1,127/1,240, of the pre-cycle `held` population is this exact shape) — confirmed rather than assumed. **Two structural blockers found this cycle that a `epic-6-ingest-lanes` F2 card cannot cross under this wave's file-territory grant, both traced one level past where prior receipts left them:** (1) Chaining ANY new book into `spell_resolver::spell_catalog_rows()` requires a matching one-line registration in `v06_work_inventory.rs`'s `spell_book_slug_for` (a closed-set lookup that panics on an unmapped `SPELL_BOOK_*` code) — every prior spell-lane widening cycle (E6-F2-002/003/004) touched this file; this cycle's dispatch explicitly forbids it (lane 3). No further book can be chained into the catalog from this card without either that edit or a dispatch change. (2) The `wiring_class==computed` spell population's `ingested-magnitude`→`grounded` lever (120 units, would reach `done` directly) runs through `probe_spell_effect_wiring`'s fixed pilot fixture — ran `v06_work_inventory --spell-probe`: **733 of 1,701 catalog keys (43%) refuse `no_casting_class_has_it`**, and only `core_rulebook` shows any `wired` outcome at all (623) — the probe's own fixture character, not `class_spell_levels.rs`'s per-class table coverage, is what caps this population; broadening it is `pilot_compute.rs` territory (lane 4), also forbidden this cycle. | Did not attempt either fix (both are real cross-territory blockers, not a stub-avoidance excuse — no partial workaround was written). Pivoted the cycle's spell work to the lever genuinely inside this card's file grant instead (row 111). | *(needs a dispatch decision for a future F2 cycle: either grant `v06_work_inventory.rs`'s `spell_book_slug_for`/`pilot_compute.rs`'s fixture as additive-list exceptions the way `cache_gen/mod.rs` already is, or route the remaining 19-book chain and the fixture-breadth fix through whichever lane already holds those files)* |
| 120 | 2026-08-16 | SD31-E6-F2-005 | NOTE | **The genuinely-workable `spell` `static` lever this cycle DID reach: 87 pre-cycle `wiring_class=static`+`status=ingested-magnitude` units (`occult_adventures` 40, `ultimate_magic` 26, `ultimate_combat` 11, `ultimate_intrigue` 10) had no `data/corpus/<book>/spell/*.json` cache at all — row 77's finding, re-derived and generalized past the 2 books it named to all 4 (re-checked `ultimate_intrigue` too: it has NO spell cache either, despite being one of the original chained books; `find data/corpus/ultimate_intrigue -maxdepth 1` shows only `LICENSE.json`/`class_feature`/`equipment` before this cycle).** Built `cache_gen::spell_lane_dump` (+`src/bin/gen_cache_spell_lane_dump.rs`), the same "dump the already-compiled table, recover a real citation, never re-derive a value" generator shape `cache_gen::ultimate_equipment`/`cache_gen::acg::generate_spells` already established — 660 records written (101+269+144+146), 0 unresolved citations, 0 dropped for `NAMEISPI:YES`/blacklist (re-derived: all 4 books' own `*_spells.lst` carry 0 `NAMEISPI:`/`DESCISPI:` tokens). Widened `enrich_spell_raw_tokens.rs`'s `TARGET_BOOKS` 5→8 (own file, spell lane) and ran it: 660/660 enriched, 0 citation misses. `corpus_literal_sweep`: CLEAN, 24,519 examined (was 23,859, +660). Guarded regen, measured (`docs/work-inventory.json` restored after, per the wave rule): board `done` **9,488 → 9,581 (+93)**, 24.63% → 24.87%; `spell` `done` **159 → 252 (+93)**, `held` **1,526 → 1,433 (-93)** — zero movement elsewhere, zero regressions. Updated the 4 books' `LICENSE.json` `records_processed`/`screening_method_note` (979→1123, 1119→1388, 1195→1341, 749→850), re-verified against `tests/sd27_book_license_record_counts.rs` (6/6 pass) — a real "count change needs a sweep" catch this cycle made on itself before committing. `declared_pi_shipping_audit`: CLEAN. | Landed. 4 new tests in `spell_lane_dump.rs` (one runs the real generator against the pinned oracle and asserts 0 unresolved citations — not a synthetic fixture standing in for the real corpus). | *(none needed — discharged this cycle)* |
| 121 | 2026-08-16 | SD31-E6-F2-005 | NOTE | **`race_trait`'s workable ingest pool under the currently-open chassis batches (24 races + Skinwalker chassis/standard-tier only) re-derived at effectively ZERO net-new safely-ingestable units, not the old 553 figure.** Filtered the 2,968 not-started `race_trait` units to those whose `corpus_key`'s leading race name is one of the 24 in-scope races: **70 units**, across `advanced_players_guide` (49), `advanced_class_guide` (5), `advanced_race_guide` (8), `bestiary` (4), `horror_adventures` (1), `inner_sea_races` (3) — every one individually traced, not batch-assumed: (a) the 49 `advanced_players_guide` rows are `OPEN-ISSUES.md` row 23's own already-investigated-and-REVERTED finding — byte-identical reprints of `advanced_race_guide`'s own already-ingested rows (`race_resolver`'s global-by-KEY map would panic on the duplicate), correctly left un-ingested, not a gap; (b) the `bestiary` 4 (`core_essentials:race_trait:aasimar`/`aasimar_default`/`tiefling`/`tiefling_default`) are structural non-content/summary rows — the bare `Aasimar`/`Tiefling` grantor row carries no `TYPE:` field at all (`ingest_race_traits.rs`'s own row-shape filter correctly skips it), and the `~ Default` row is a subrace-selector base-case that lives in a file (`<race>_abilities_race.lst`) no current `BookSource` reads (only the sibling `_subrace.lst` is registered) — real but narrow (4 units total across 2 races), and adding the base file risks re-declaring the 9 standard traits ALREADY shipped per race from a much older ingest path under a different KEY scheme (the exact APG-shaped collision hazard); (c) `advanced_race_guide`'s 8 (`Drow`/`Dwarf`/`Elf`/`Gnome`/`Grippli`/`Halfling`/`Orc`, plus 1 `Half-Elf ~ Drow Blooded`) are `CATEGORY:Adoptive Parentage` rows (`arg_abilities_race.lst:291-297`) — a different feature family (background/parentage flavor traits), not racial traits of the row's own named race, correctly excluded by the same no-`TYPE:Racial Trait` filter; (d) `horror_adventures`'s 1 (`Half-Elf ~ Starchild`, `support/ha_abilities_race_oa.lst`) is an ALREADY-DOCUMENTED deliberate exclusion, confirmed by running `scripts/classify_race_trait_rows.py` against it fresh (`in-scope rows 1 \| alternate 1` — needs no new mechanism) AND by the existing comment directly above the `horror_adventures` `BookSource` in `ingest_race_traits.rs`: the file's PCC load is gated `PRECAMPAIGN:1,INCLUDES=Occult Adventures` (a per-CAMPAIGN-selection runtime condition the ingest mechanism does not model, not a book-availability gap — Occult Adventures IS now ingested by this same wave, which does not change the PRECAMPAIGN semantics); (e) `inner_sea_races`'s 3 residual rows in an ALREADY-registered `BookSource` file: 2 (`Elf ~ Sovyrian-Born`, `Svirfneblin ~ Stalwart Watcher`-lineage) carry `DESCISPI:YES`/`NAMEISPI:` declarations and are correctly excluded by the ingest's own PI screen; the 3rd (`Human ~ Tribalistic Languages`) is a genuine 1-unit residual not traced further (out of budget this cycle, named rather than silently dropped). `scripts/screen_pcc_load_gates.py` run corpus-wide fresh: `remaining units screened: 0 \| TOTAL remaining units excluded by a PCC load gate: 0` — confirms no OTHER hidden PCC-conditional hazard is waiting among the candidates above. **The remaining 2,898 of 2,968 not-started units belong to races Epic 1's chassis batches have not yet covered** — this is the re-derivation the card asked for: the workable pool is a function of chassis output, and today that function evaluates to essentially nothing beyond what prior rounds already closed. | Did not ingest any of the 70 (all correctly excluded, already-excluded, or too narrow/risky to justify this cycle against the collision precedent). Ran both required pre-cycle screens fresh rather than trusting the prior "553 workable" figure. | *(no ruling needed — the honest re-derivation IS the deliverable; a future Epic 1 chassis batch is what reopens this population, not a `epic-6-ingest-lanes` F4 cycle)* |
| 122 | 2026-08-16 | SD31-E6-F2-005 | NOTE | **`monster_codex`'s 2-unit `race_trait` residual (`Standard Goblin`/`Oversized Goblin`, `mc_abilities_race.lst`, already an ingested `BookSource`) is the SAME bare-grantor-row shape row 112(b) names for Aasimar/Tiefling** — Goblin IS in-scope but these 2 rows carry no `KEY:`/`TYPE:Racial Trait` shape the parser recognizes as content (matches `cache_gen::acg`'s own doc-comment precedent for exactly these two record names, cited there for an unrelated reason). Not ingested this cycle; folded into the same future-work note as row 112(b) rather than opened as a separate finding. | Traced, not fixed — 2 units, same shape and same risk profile as row 112(b). | *(no ruling needed — bundle with row 112(b)'s future fix)* |
| 125 | 2026-08-16 | SD31-W7-INTEGRATE-001 | RULING-NEEDED | **Fixed both confirmed CONFIRMED prose done-bar over-claims from wave-6/7's adversarial review, without resolving the underlying open interpretive question (rows 69/87/95/107).** (1) `SD31-D7-PROSE-003`'s own full-closure hand-check of 43 promoted `class_feature` units missed 2: `advanced_class_guide:class_feature:bloodrager_indomitable_will` ("+4 bonus on Will saves... stacks with all other modifiers" — near-identical to the already-excluded `core_rulebook:class_feature:barbarian_indomitable_will`, same commit, opposite verdict) and `core_rulebook:class_feature:ranger_improved_quarry` ("+4 insight bonus"). Both added to `CLASS_FEATURE_FLAT_MAGNITUDE_PENDING_RULING`. (2) `SD31-E6-F8-001` banked 11 `ce_feats.lst` display units without discharging the PROXY WARNING; hand-checked all 11 against their whole corpus rows (byte-exact DESC+BENEFIT from the pinned oracle) and found 7 state a real flat magnitude (Ability Focus, Awesome Blow, Empower Spell-Like Ability x2, Hover, Snatch, Wingover) while 4 genuinely do not (Craft Construct, Flyby Attack, Quicken Spell-Like Ability x2). New `FEAT_FLAT_MAGNITUDE_PENDING_RULING` const, same shape as the sibling gates. | Fixed both without resolving the open question; conservative exclusion in both directions, matching this program's standing default. | *(same operator ruling rows 69/87/95/107 already ask for; this row exists so a check-in sees the wave-7 fix landed rather than re-deriving it)* |
| 126 | 2026-08-16 | SD31-W7-INTEGRATE-001 | NOTE | **Both confirmed SD-30 PI contract gaps closed on the production write paths merged this wave.** `enrich_spell_raw_tokens.rs` (writes `raw_tokens` on 660 spell records) previously ran neither contract 52.3 nor 53.5 over the field it writes; now runs both per-field (blacklist union declared-PI redaction; a NAMEISPI:YES row hard-stops before any raw_tokens write). `gen_feat_gap_tables.rs` previously called only contract 52.3 (over the whole generated file); now also reads contract 53.5's declared-PI reader per row in `parse_lst`, dropping a name-PI record and redacting a description-PI one. Independently re-verified before and after: 0 NAMEISPI:YES/DESCISPI:YES declarations and 0 blacklist hits across all affected records — nothing shipped changes; the gap was in the guard, not in today's output. `declared_pi_shipping_audit` re-run: CLEAN. | Fixed both, TDD, mutation-proven (temporarily disabling each new redact branch produced a real red before the fix). | *(no ruling needed — discharged)* |
| 127 | 2026-08-16 | SD31-W7-INTEGRATE-001 | NOTE | **The Supersession Register's gate defect (dead oracle re-derivation) and its one bad entry (`companion` corpus_key "1") are both fixed and mutation-proven; the register is now genuinely gate-verified, still PROPOSED and NOT wired into the live denominator.** See `progress.md`'s `SD31-W7-INTEGRATE-001` receipt and `SUPERSESSION-REGISTER.md` §11 for the full account. Regenerated: 117→116 objects, `count_removed` 135→134, proposed denominator 38,521→38,387 if applied. The gate now re-derives every entry from the pinned oracle for real (previously it silently passed on a `None == None` coincidence whenever re-derivation failed) and refuses a bare-integer `corpus_key` structurally. Applying it (wiring an `EXCLUDED_UNIT_IDS` set into the live doneness computation) was deliberately NOT done this cycle — named precisely as the next step in `followups`. | Fixed the gate and the bad entry; did not apply the register to the live denominator (a separate, dedicated change, not squeezed in under wave pressure). | *(no ruling needed to fix the gate; applying the register is a standing-rule action per `decisions.md §10`, not an operator-ruling question — next cycle can proceed directly)* |
| 128 | 2026-08-16 | SD31-W7-INTEGRATE-001 | NOTE | **`corpus_literal_sweep`'s coverage gap (confirmed by wave-7 review): it verifies `raw_tokens`/`cost_gp`/`weight_lbs` but never `description`/`school`/`level`, so a wrong shipped `description` is structurally undetectable by the check that gates the `literal-verified` done rung for every `static` unit, `spell` included.** Empirically demonstrated (no writes): 2,186 of 16,817 records carrying both a `description` and a `DESC` raw_token ship a `description` that is NOT byte-identical to that token's value, while the sweep reports 0 findings, CLEAN. Positive control confirms the existing numeric extension (row 91's own fix) CAN fail (a planted `COST` mutation goes red), so this is a real, scoped coverage gap, not a broken check. Not fixed this cycle (explicitly not a merge blocker — this wave's 660 new spell records were hand-verified against the oracle in the adversarial review instead) — logged as real, sizeable owed work for whichever lane next touches `corpus_literal_sweep.rs`. | Not fixed — verified the gap is real and scoped, verified the sibling numeric check still can fail, and did not claim the string-field extension as done. | *(no ruling needed — an engineering remedy: extend `corpus_literal_sweep::parse_transcription`/`compare_tokens` to typed string fields using `pcgen_desc::split_prose_and_args`'s existing `\|`-argument-tail rule, and mutation-test the new arm the same way `cost_gp` is tested)* |
| 129 | 2026-08-16 | SD31-D7-PROSE-004 | NOTE | **Rows 69/87/95/107's flat-vs-scaling question ANSWERED by `decisions.md` §7 REFINED (universal vs conditional, not flat vs scaling) — full resolution recorded in this file's "Needs an operator ruling" section above; this row is the append-only audit trail entry.** Built `closure_states_universal_sheet_modifier` (`v06_work_inventory.rs`), retired all four `*_FLAT_MAGNITUDE_PENDING_RULING` name-lists, threaded a `universal_sheet_modifier: bool` parameter through `classify()` and every one of its 36 call sites, and applied the discriminator to `Kind::Feat`/`Kind::MonsterAbility`/`Kind::ClassFeature`/`Kind::Companion`/`Kind::RaceTrait` uniformly. **Two-stage validation, both stages evidenced:** (1) 32-case hand-labelled accuracy test (`universal_vs_conditional_discriminator_accuracy_tests`, 30 real corpus records + 2 edge cases, 0 misses) built BEFORE the corpus-wide run, per Decision 1(e)'s "accuracy, not movement" bar; (2) running the retired-list replacement against the FULL corpus (not just the hand-labelled sample) surfaced **5 false positives** the first draft's broader `UNIVERSAL_MODIFIER_CUES` list produced (`guardian_of_the_wild`, `critical_focus`, `orc_weapon_expertise_killer`, `timely_coordination`, `greater_blind_fight` — each genuinely conditional, confirmed against the pinned oracle) — narrowed the cue list to `"size bonus"` alone (the one phrase every true positive shares and no false positive contains), re-ran, and the corpus-wide diff came back EXACTLY the expected 27 units (21 promoted, 6 demoted), zero unexpected movement either direction. **A second correction: this cycle's OWN dispatch brief mis-stated `goblin_size` as "(grants nothing)"** — the real shipped `bestiary:race_trait:goblin_size` row states the identical universal size-bonus text as the other 5 confirmed-universal race traits; verified by direct `cat` of the corpus JSON before acting, `retro.py correction` emitted. Board moved 9,780 → 9,795 done (25.3887% → 25.4277%), zero denominator change. DoD-8: `artifacts/SD31-D7-PROSE-004/item8/` — Monk 20's **Still Mind**/**Empty Body** rows, both newly-promoted `class_feature` units, real corpus `DESC:` text rendering on-screen, byte-matched by direct file read. | Built the discriminator, validated it twice (hand-labelled sample, then full-corpus false-positive sweep), fixed what the second validation found rather than shipping the first draft, corrected the brief's own error rather than propagating it. | *(no ruling needed — discharged; §7 REFINED is recorded verbatim in `decisions.md`)* |
| 130 | 2026-08-16 | SD31-E4-F1-004 | BLOCKER | **Rows 96/97/118's structural blockers reproduce identically for Samurai, UC's third and final real class, confirming the pattern is structural to the whole book, not per-class.** `SAMURAI_CLASS_ID`/`UcClassId::Samurai` now has real `pilot_compute.rs` wiring (base BAB/save chassis from `class_samurai.rs`, plus Challenge uses/day+damage bonus, Resolve uses/day, and Bonus Feat count — no archetype-supersession branch, because a fresh full-oracle `grep -rn "Samurai Archetype"` re-confirms `named_raw: 0` is genuine, not a missed nested-directory hit the way Ninja's Scout was). Two blockers, both re-confirmed by direct re-read this cycle, unchanged: (1) `src/bin/v06_work_inventory.rs`'s `modelled_class_books()` (lane 1's file) still names only `ClassId`/`ApgClassId`/`AcgClassId`, so Samurai's real corpus `class_feature` records cannot reach `done` or `held` on the board regardless of how complete the wiring is — board delta this cycle: predicted and confirmed **0/0**. (2) `grep -rn samurai apps/desktop/src/` returns zero hits outside this cycle's own new Rust files — `characterHubModel.ts`'s `CLASS_OPTIONS` (frontend, out of this card's territory) carries no `samurai` entry, so DoD-8's full on-screen character-sheet proof is blocked at character-creation, before the archetype-picker gap even matters. Not attempted live via `driver.sh` for this reason (source read already answers it unambiguously, same reasoning rows 97/118 gave). Reachability of the computation itself is proven via 8 new `build_pilot_headless_receipt`-based tests instead (chassis + all 4 class-feature slots + a no-archetype-superseded-claim self-check), per SD31-E4-F1's own named acceptance standard. **All three of UC's real classes (Gunslinger, Ninja, Samurai) are now fully chassis-wired and waiting on the single row-96 `modelled_class_books()` fix to reach the board at all.** | Wired the real chassis and class features anyway (per this card's own "your deliverable is real wired classes, do not chase board movement" instruction) rather than declining the class because its board credit is blocked. Also corrected a stale claim discovered en route (unrelated to Samurai): `explain_gnome_race_seam`/`explain_halfling_race_seam`'s `trait_bundle.size` explanation records still said "no size-modifier term exists anywhere in this engine's combat baseline for any race yet" — false since SD-27 (decisions.md §28) wired `combat_size_modifiers` generically for all 24 in-scope races; corrected both records to cite the real mechanism and to honestly name Stealth as the one genuinely-still-unapplied piece (no Stealth skill total exists anywhere in this engine). | *(same dispatch decision rows 96/97/118 already ask for — this row exists so the operator/lane-1 owner sees UC's entire class roster is now blocked on exactly one fix, which should raise its priority)* |
| 131 | 2026-08-16 | SD31-E6-F4-002 | RULING-DISCHARGED | **§7 REFINED (the operator's universal-vs-conditional ruling) applied to row 87's exact 11-unit population, re-derived one record deep against the WHOLE corpus row, not an abridged quote.** 6 of the 11 are universal-modifier-shaped and should COMPUTE, not stay text: `gnome_size`, `goblin_size`, `grippli_size`, `halfling_size`, `kobold_size`, `svirfneblin_size` — each grants a flat, always-on +1 AC / +1 attack / -1 CMB / -1 CMD / +4 Stealth Size bonus with no gating condition (verbatim DESC re-read for every one; this corrects the dispatch's own stated "5 must compute" to the true **6** — a race_trait-scoped re-derivation, not a trust of the summary figure). The other 5 are genuinely conditional and correctly stay `display`/text under the refined ruling: `duergar_stability` (manoeuvre-type condition: only vs bull rush/trip), `dwarf_hatred` + `svirfneblin_hatred` (target-subtype condition — `dwarf_hatred` is the operator's own named worked example), `fetchling_shadow_blending` (environmental-state condition: dim light only), `half_orc_stoic` (effect-type condition: emotion/fear saves only). **Of the 6 universal Size units, the AC/attack/CMB/CMD quarter is ALREADY wired to a real production compute path** — `rules_core::size::SizeCategory::armor_class_size_modifier`/`special_size_modifier`, called from `pilot_compute.rs:6912-6913` and folded into `combat_maneuver_bonus`/`combat_maneuver_defense`, driven by `race_resolver::RACE_SIZES` (which already marks all 6 races `Small`, cross-verified against the corpus by `tests/sd27_race_size_resolution.rs`) — so these 5 sheet effects are genuinely computed and reachable today, just misclassified `wiring_class: display` (`signals: ["display:no_magnitude_token"]`) because the classifier reads only the LST row's own tokens (`TEMPLATE:SIZE_S`, no literal `BONUS:` chain) and cannot see the size.rs/pilot_compute.rs cross-reference. **A genuine, un-wired gap confirmed the SAME way**: `SizeCategory::stealth_size_modifier()` (`size.rs:233`) is defined and unit-tested but has **zero production callers anywhere in `src/` or `apps/`** (`grep -rn stealth_size_modifier` outside `size.rs` itself returns nothing) — the Stealth quarter of every Small race's size trait, all 6 of them, is the Decision-8 "code path nothing ships" shape and needs the same wiring `spell_like_ability_caster_level()` got (row 44/Decision 8), not a text-bar. | Did not touch `wiring_class.rs` (a sibling lane's file this wave, per the standing convention row 69/95 already established) to force these 6 to `computed` — that would be the same "widen the cell without the description-and-render requirement" anti-gaming shape Decision 7's own §7-blocker section (`decisions.md`, "the structural blocker the ruling now runs into") forbids. Did not touch `pilot_compute.rs` (lane 2's file) to wire `stealth_size_modifier` in. Did not force the 6 through the prose done-bar mechanism either — the refined ruling is explicit that a universal modifier must compute, not merely print, so routing them through `SD31-D7-PROSE-001`'s rung would itself be the violation. Left all 11 units' `done`/`held` state exactly as row 87 found it; this row answers the interpretive question with evidence, it does not bank credit. | *(two concrete, bounded follow-ons, neither this cycle's file territory: (a) `wiring_class.rs`'s owner extends the classifier to recognize a `TEMPLATE:SIZE_<code>` token cross-referenced against `race_resolver::RACE_SIZES` as a real computed-and-reachable signal for exactly these 6 race_trait records — not a blanket rule, a named, provable case; (b) `pilot_compute.rs`'s owner wires `SizeCategory::stealth_size_modifier()` into the Stealth skill-check computation the same way `special_size_modifier` already feeds CMB/CMD, then DoD-8s a Small character's sheet showing the +4 Stealth line. Neither is a race-chassis-lane fix; both are named here so the next cycle that owns the file does not have to re-derive which 6 units or which quarter of the bonus is missing.)* |
| 132 | 2026-08-16 | SD31-E6-F4-002 | NOTE | **The chassis-blind `race_trait` population re-derived fresh at this cycle's own start-of-cycle HEAD: 2,785, not the dispatch-cited 2,576** (`evidence=='race_trait_race_not_modelled'` over `docs/work-inventory.json`) — the "corrected three times: 2,894 → 2,689 → 2,576" figure the dispatch quoted was itself already stale by the time this cycle started; `retro.py correction` filed. Landed the next chassis batch: `ingest_races.rs`'s `IN_SCOPE_RACES` widened 25 → 31 (Advanced Race Guide's Catfolk, Kitsune, Ratfolk, Strix, Suli, Wayang — confirmed via `arg_races.lst`'s own `<Race>.MOD ... TYPE:Featured\|Uncommon SOURCEPAGE:p.<n>` rows, the same page-citation signal Decision 9's `core_essentials` re-attribution used), attributed to `advanced_race_guide` per Decision 10's Catfolk worked example (newest printing wins) rather than a fabricated book label. All 6 races share the identical flat `<dir>_races.lst`/`<dir>_abilities_race.lst`/`<dir>_abilities_globalvar.lst` shape as the Bestiary 2/5 batches — no `_subrace.lst` file anywhere, no new mechanism needed. 6 new `race` chassis + 58 new `race_trait` standard-tier records landed (13-17 raw rows per race, filtered by the existing `is_standard_racial_trait` gate to 9-11 real records each), accounting for 93 of the 2,785 chassis-blind units directly (Catfolk 16, Kitsune 13, Ratfolk 20, Strix 17, Suli 15, Wayang 12). | Did NOT widen `ingest_race_traits.rs`'s own separate `IN_SCOPE_RACES` (24 races, gates ARG's *alternate*-trait ingestion) to include these 6 — that book's `arg_abilities_race.lst` genuinely carries further in-scope rows for them (e.g. `Catfolk ~ Cat's Claws`, `~ Clever Cat`, `~ Climber`, `~ Curiosity`, `~ Nimble Faller`, `~ Scent`, confirmed present and currently silently filtered as "skipped per out-of-scope race" by that binary's own run log) but pulling them in is genuinely separate scope from this cycle's chassis batch, not a config toggle to flip alongside it under wave pressure. Found and fixed a real cross-binary hazard instead of shipping it silently: both `ingest_races.rs` and `ingest_race_traits.rs` used to do a coarse whole-directory `remove_dir_all`/`count_json` over `<book>/race_trait/`, safe only because every book was exclusively owned by one binary; sharing `advanced_race_guide` for the first time made that assumption false (a run of either binary would delete or miscount the other's files). Fixed both binaries with a scoped, per-race-slug clear/count instead of a blanket directory wipe -- verified live: ran `ingest_race_traits.rs -- advanced_race_guide` AFTER `ingest_races.rs` and confirmed Catfolk's 9 files and Dwarf's 10 pre-existing ARG alternates both survive. | *(named follow-on, not a ruling: a future `epic-6-ingest-lanes` F3/F4 cycle can widen `ingest_race_traits.rs`'s `IN_SCOPE_RACES` to include these 6 races and pick up their real, already-registered ARG/Inner-Sea-Races/etc. alternate-trait content -- no new BookSource or mechanism needed, purely a config widening plus the same scoped-clear discipline this cycle already built.)* |
| 133 | 2026-08-16 | SD31-E6-F4-002 | NOTE | **`v06_work_inventory.rs`'s independent book re-derivation re-attributes all 58 of this cycle's new race_trait records away from `advanced_race_guide` to each race's ORIGINAL first-print book — the doneness COUNT is unaffected (verified +58 exactly, none of the target books are `EXCLUDED_BOOKS`), only the reported `book`/`id` label is.** Guarded regen measured board `done` 9,780 → 9,838 (+58), `race_trait` `done` 630 → 688 (+58, all 6 races' 5-9 standard-tier `computed`/`grounded` units, verbatim ids like `bestiary_3:race_trait:catfolk_ability_scores`). Traced one record deep: this repo's `data/corpus/advanced_race_guide/race_trait/catfolk/catfolk_ability_scores.json` genuinely has `"book"` unset/directory-derived as `advanced_race_guide` (correct per this cycle's own Decision-10 attribution, §2 of `progress.md`'s `SD31-E6-F4-002` receipt) — no `bestiary_3` directory exists anywhere on disk (`ls data/corpus/bestiary_3/race_trait/catfolk/` → does not exist). The relabelling happens entirely inside `v06_work_inventory.rs`'s own re-derivation, which reads each record's raw `.lst` `SOURCELONG` (the same `resolve_true_book_for_core_essentials`-shaped mechanism `SD31-E6-F3-002`/Decision 9 built for `core_essentials`) and reports the race's FIRST/original print book rather than the reprint this cycle attributed to per Decision 10's own worked example ("Catfolk exists in Bestiary and ARG — that's a duplicate, most recent publish wins... the unit therefore belongs to ARG"): Catfolk/Ratfolk/Suli → `bestiary_3`, Kitsune/Wayang → `bestiary_4`, Strix → `inner_sea_world_guide` — all real, correctly-read `SOURCELONG` values, just the WRONG side of Decision 10's "newest printing wins" rule, because the mechanism that reads `SOURCELONG` predates Decision 10 and does not yet know a book can legitimately claim content whose physical `.lst` row cites an older book by design (ARG's own `.MOD ... TYPE:Featured SOURCEPAGE:p.<n>` republication marker — the exact signal this cycle used to attribute correctly — is a different token the re-derivation logic never reads). | Did not touch `v06_work_inventory.rs` (explicitly out of this card's file territory) to special-case these 6 races. Did not change this cycle's own corpus JSON attribution to match the stale re-derivation instead — that would be reverting a Decision-10-correct attribution to satisfy an instrument that has not caught up with the ruling, exactly the "recover a drop by weakening the check" shape forbidden, just inverted (here it would be widening the WRONG bucket to match a stale label rather than fixing the label). Re-verified the doneness NUMBER is unaffected by the mislabel (`inner_sea_world_guide`/`bestiary_3`/`bestiary_4` are all non-excluded books, so nothing is silently dropped from the denominator) before treating this as informational rather than blocking. | *(named, bounded follow-on for `v06_work_inventory.rs`'s owner: extend the `core_essentials`-shaped re-derivation to also check each race's OWN `.pcc`/`.lst` for an ARG (or other later book) `.MOD ... TYPE:Featured|Uncommon` republication marker, matching the exact signal this cycle's `ingest_races.rs` batch used, and let that outrank a bare `SOURCELONG` when both are present -- otherwise every future Decision-10-attributed reprint will report under its ORIGINAL book here, not the one the corpus JSON and this package's own denominator accounting actually credit it to.)* |
| 134 | 2026-08-16 | SD31-E6-F6-001 | NOTE | **`gen_equipment_gap_tables.rs`'s parser never implemented `.COPY=` inheritance — every `.COPY=<name>` row (648 corpus-wide across the 9 gap-lane books, e.g. `Special Ability ~ Answering ~ Weapon.COPY=Answering`) parsed as if it stated ONLY its own line's tokens, so a genuinely-inherited `description`/`cost_gp`/`weight_lbs` real value the corpus states on the BASE row (found by the identical `KEY:`-or-bare-name identity PCGen itself resolves `.COPY=` against) shipped as `None` — the DATA-side root cause `SD31-E6-F6-001`'s dispatch named as "the recovery." Fixed via a new `collect_base_fields`/inheritance pass in `gen_equipment_gap_tables.rs` (TDD, mutation-proven RED-then-GREEN, 7 new tests): 392 records recovered a real `description`, 209 a real `cost_gp`, 3 a real `weight_lbs` — zero fabricated (every value traced to a real corpus token one hop away, at most one hop, a `.COPY=` row is never itself used as another's base). This SAME mechanism, applied generally rather than as the prior cycle's 8-row hand patch, reproduces (and generalizes) the pre-existing `SD31-E6-F5-002` ACG cost-correction note that this fix's regen now makes obsolete (removed from the regenerated file's doc comment — the correction is now automatic, not a manual footnote). Re-shipped to `data/corpus/`: 455 of 459 changed keys matched an on-disk record and were re-cited (4 legitimate non-writes: 2 disabled-`#`-line exclusions, 2 pre-existing `write_json` slug-collision skips, `equipment_gap.rs`'s own documented no-clobber-over-a-richer-record guard — named, not forced). **Figures below are the FINAL, post-row-132/133-fix measurement, not an intermediate one** (an earlier intermediate regen, before rows 132/133's own fixes landed, measured +334/board 26.2558% — superseded, not separately banked). Guarded regen, measured: board `done` **9,780 → 10,119 (+339)**, 25.3887% → 26.2688%; `equipment_modifier` `done` **228 → 566 (+338)**, 14.43% → 35.82%; `equipment` `done` **4,513 → 4,514 (+1)**, 72.6965% → 72.7126%; zero units moved away from `done`. | Landed, TDD, mutation-proven. `docs/work-inventory.json` restored per the wave rule, not committed. | *(none needed — discharged this cycle; rows 130/131/132/133 name what this fix's own regen surfaced and fixed downstream)* |
| 135 | 2026-08-16 | SD31-E6-F6-001 | NOTE | **Regenerating `data/corpus/` from the fixed table without re-running `enrich_equipment_raw_tokens` first silently dropped `raw_tokens`/`raw_bonus_chains` on 455 records** — caught by the guarded regen's own stamp-loss guard refusing to write (3 named first-offenders: `core_rulebook:equipment_modifier:material_dragonhide`, 2 `ultimate_wilderness:equipment:hunter_s_stand_*` records), traced one record deep before acting (per the standing rule): `equipment_gap.rs`'s base JSON schema never carried `raw_tokens` at all — that field is added by a SEPARATE, book-agnostic post-processing pass (`enrich_equipment_raw_tokens.rs`) that a prior cycle had already run against the pre-existing files; deleting-then-regenerating without re-running the enrichment pass reverted 455 records to their pre-enrichment thin state (real data preserved elsewhere, not lost from the corpus, but not this cycle's own regen output). Fixed by re-running `enrich_equipment_raw_tokens` (book-agnostic, safe to re-run: 455 enriched, 0 citation misses, 0 merged-entry mismatches); guarded regen then wrote clean. | Fixed by re-running the correct existing tool, not by hand-patching the 455 files or by loosening the stamp-loss guard. | *(no ruling needed — a future equipment-lane cycle regenerating from `equipment_gap_tables.rs` should always re-run `enrich_equipment_raw_tokens` afterward; the guarded regen's stamp-loss check is what catches a skip, and it worked exactly as designed here)* |
| 137 | 2026-08-16 | SD31-E6-F6-001 | NOTE | **The `.COPY=` inheritance fix (row 129) exposed a THIRD real defect, also fixed this cycle: `raw_tokens` — not just the typed `cost_gp`/`weight_lbs` fields — is a provenance surface other callers check directly, and inheritance had left it thin.** `tests/sd27_equipment_modifier_price_matches_corpus_cost_token.rs`'s `every_newly_reachable_record_is_priced_by_its_own_corpus_cost_token` derives its OWN independent "does this record's shipped `raw_tokens` actually state a COST?" check straight from the JSON (not via `corpus_literal_sweep`), and flagged `ACG:Exclusionary_AMF` (`cost_gp: Some(3750.0)`, correctly inherited) as apparent fabrication because its `raw_tokens` (populated by `enrich_equipment_raw_tokens.rs`, a SEPARATE binary from the generator) still carried only the `.COPY=` row's own thin `VISIBLE:NO` token, with no knowledge of `.COPY=` inheritance at all. Root-caused and fixed at the SAME layer, not by loosening the test: `enrich_equipment_raw_tokens.rs` gained the identical `copy_base_identity`/`find_copy_base` pair (3 new tests), folding a resolved base record's own on-its-own-line tokens into `raw_tokens` too — the SAME byte-present-on-its-own-line guard already protecting the cited line extended to the base line, so an inherited token that ISN'T genuinely on the base row still refuses to ship rather than fabricating. Re-ran corpus-wide (stripped `raw_tokens`/`raw_bonus_chains` from the same 455 files first, forcing re-enrichment): 455 enriched, 0 citation misses, 0 merged-entry mismatches. `raw_tokens` is now a true, self-contained provenance record for every `.COPY=` gap row, not just its typed fields. | Fixed at the source (the enrichment binary), TDD, tests pass without touching the SD27 test's own fabrication-safety logic. | *(none needed — discharged; the general lesson, named for a future `.COPY=`-adjacent cycle: a corpus-real inherited VALUE needs its PROVENANCE inherited too, everywhere that provenance is independently checked — `corpus_literal_sweep`'s closure, `raw_tokens` itself, and any future caller reading `raw_tokens` directly, not just the typed field)* |
| 138 | 2026-08-16 | SD31-E6-F6-001 | NOTE | **The `.COPY=` inheritance fix (row 129) found a REAL rendering-safety gap on its FIRST real application: 1 of 392 recovered descriptions (`CRB:equipment_modifier:IntItemBase`) still leaked raw PCGen syntax to the player after rendering.** `IntItemBase`'s base row's `SPROP:` states 4 BARE (unnumbered) `%` placeholders followed by a 4-argument `|`-delimited tail naming which `BONUS:VAR` each substitutes (`"Intelligence %, Wisdom %, Charisma %, Ego Score %\|IntItemStatINT\|IntItemStatWIS\|IntItemStatCHA\|IntelligentItemEgo"`) — a corpus shape `pcgen_desc::max_arg_reference`'s NUMBERED-reference detection does not recognize (it only counts `%N` digit references), so `split_prose_and_args`'s tail-stripping never fires and the raw `\|`-chain survives rendering intact. Caught by `apps/desktop`'s own `no_catalog_serves_a_description_carrying_raw_pcgen_syntax` test, which is exactly what it exists for. **Fixed at the SOURCE, not the render path** (out of this card's file territory; `pcgen_desc.rs` is shared, high-blast-radius infrastructure a fix there would need its own dedicated, carefully-scoped cycle): `gen_equipment_gap_tables.rs` gained `safe_description()`, running every candidate description (own-line or inherited) through the SAME `render_pcgen_desc`/`leaked_pcgen_syntax` the desktop test uses, before shipping — refusing (shipping `None`) rather than shipping broken syntax, matching `v06_work_inventory.rs`'s own established precedent for the identical judgment call (`corpus_json_description_leaks_pcgen_syntax`). **Empirically verified, not assumed, that this does NOT over-refuse**: `dropped_args.is_empty()` was deliberately NOT added as a second condition — a bare `%CHOICE` with no `\|` tail (68 of the 69 `%`/`\|`-carrying recovered descriptions) renders to CLEAN, readable text with `dropped_args: ["CHOICE"]` but `leaked_pcgen_syntax: None`, and refusing on `dropped_args` alone would have wrongly discarded 68 real recoveries to fix 1 real defect — checked by adding a scratch test BEFORE committing to the stricter check and finding it failed, then correcting to match production's own actual `leaked_pcgen_syntax`-only behavior. Exactly 1 record's description changed (re-verified by an independent before/after diff of the whole regenerated table): `IntItemBase` `Some(...)` → `None`. Re-shipped (1 file deleted+regenerated, re-enriched), `corpus_literal_sweep` re-run CLEAN, `apps/desktop` full test suite (455 tests) now 455/455 passing including both leak-detection tests. Updated 3 downstream pinned counts this cycle's own delta changed, each re-derived fresh rather than adjusted by arithmetic: `equipment_catalog.rs`'s per-book `description_coverage_is_pinned_per_book` (CRB 2022→2219, APG 349→368, ACG 264→312, ARG 194→205, UI 41→48, UPSI 311→406, UC 88→102, total 3844→4235), `character_hub.rs`'s `every_offered_modifier_row_charges_the_price_the_picker_displayed` (`priced_non_crb` 137→181, unrelated to the leak fix — this is `cost_gp`'s own inheritance recovery, re-derived to confirm it matches the earlier `left: 181` panic value exactly), and `sd27_equipment_modifier_price_matches_corpus_cost_token.rs`'s `(checked_numeric, checked_formula, checked_absent)` tuple (433,1,140)→(447,1,126), total unchanged at 574. | Fixed at the source with a mutation-tested, empirically-verified-narrow gate; corrected 3 downstream pinned counts, each freshly re-derived. | *(a future cycle with `pcgen_desc.rs` in its file grant could widen `max_arg_reference`/`split_prose_and_args` to recognize a bare-`%`-run-with-multi-argument-tail shape generally, which would let `IntItemBase`'s real description ship correctly rendered instead of being refused — named as owed work, not urgent: refusing is honest and safe, the alternative is strictly better but riskier under this card's own time budget)* |
| 136 | 2026-08-16 | SD31-E6-F6-001 | NOTE | **The `.COPY=` inheritance fix (row 129) exposed two further real defects, both fixed this cycle, neither a symptom of the fix being wrong.** (1) `corpus_literal_sweep::token_closure` had no concept of `.COPY=` inheritance at all — a `.COPY=` row's own cited-line closure never included its base row's tokens, so a genuinely-inherited, corpus-real `cost_gp`/`weight_lbs` (e.g. `BOWSTR`'s real `cost_gp: 0` per `cr_equipmods.lst:34`'s `COST:0`) read as unprovable by the very check that gates the `static` done rung — not because the value was wrong, but because the closure never looked at the row that states it. Fixed by widening `token_closure` with a new `copy_base_row: Option<&str>` parameter (mirrors the existing `.MOD`-row handling), resolved in `src/bin/corpus_literal_sweep.rs` via a new `Sweep::copy_base_row`/`copy_base_identity` pair using the IDENTICAL `KEY:`-or-bare-name resolution `gen_equipment_gap_tables.rs`'s own inheritance uses (so the two agree on what "the base" means, by construction). TDD + mutation-proven (temporarily disabling the merge made exactly the "with resolved base" test go RED, the "without" test stayed green as designed). 6 other pre-existing `token_closure` call sites (`enrich_spell_raw_tokens.rs` x2, `enrich_monster_raw_tokens.rs`, `repair_spell_citations.rs`, `enrich_companion_raw_tokens.rs`, `enrich_monster_ability_raw_tokens.rs`) needed a mechanical `, None` argument added — none of their own behavior changed. `corpus_literal_sweep` re-run CLEAN after (24,519 examined, 0 findings), confirmed with a fresh, guaranteed-uncached build (the file was `touch`ed immediately before the run) after an earlier same-session scare where a background build may have raced a mutation-test edit. (2) `equipment_resolver.rs`'s pinned `the_two_lookups_agree_on_every_catalog_key_but_the_one_pinned_collision` test's hardcoded expected list needed updating: 14 of its 28 pinned entries (`Adamantine (Ammo)`, `Alchemical Silver`, `BRACE`, `CLOTH`, `Cold Iron`, `DISARM`, `LEATHER`, `MONK`, `Mithral (Light Armor)`, `Mithral (Shield)`, `NONLETHAL`, `STEEL`, `TRIP`, `WOOD`) no longer genuinely disagree between `equipment_catalog_row_by_key` and `equipment_cost_gp_headless_resolve` — both now return the SAME real, corpus-true price (row 129's inheritance recovered it), so the coincidental KEY/NAME collision that used to hide a real ambiguity is resolved, not merely pinned around. Each of the 14 individually re-verified via a scratch print (`by_key.cost_gp == headless`, both `Some(<identical value>)`) before removal — not assumed from the count shrinking. Initially misread the `assert_eq!` panic's `left`/`right` fields backwards (assumed `left`=expected, `right`=actual; it is the reverse) and nearly reported this as a regression before re-deriving with a debug print — `retro.py correction` emitted. | Fixed both, TDD/mutation-proven where applicable, corrected own mid-cycle misreading before it reached the receipt. | *(none needed — discharged; both fixes are properly scoped generalizations of an existing, already-established precedent in each file, not new doctrine)* |
| 139 | 2026-08-16 | SD31-E6-F2-006 | NOTE | **Traced ONE `spell` `held` unit end to end before ingesting, per this card's own instruction — reproduced and quantified row 119's finding precisely rather than re-deriving from scratch.** `advanced_class_guide:spell:adhesive_blood` (`derived`/`ingested-magnitude`, `DURATION:(CASTERLEVEL) minutes`, `acg_spells.lst:8`) is short of `derived_evaluator_fixture_check`'s `fixture-verified` rung because that checker had zero `kind=spell` evaluator seams. Re-derived the population fresh at this cycle's tip: `derived`+held `spell` is **1,356 units** (884 `ingested-magnitude` + 472 `grounded`), 1,152 tagged `prose_expr` + 199 `range_keyword` + 5 `prose_formula_segment`. **Built the seam for the largest honestly-groundable sub-family**: `parse_caster_level_linear_duration()` (`src/rules_core/derived_evaluator_fixture_check.rs`) parses a corpus `DURATION:` token's `(CASTERLEVEL)`/`(CASTERLEVEL*N)` PF1 caster-level-LINEAR formula into its own parameters (multiplier + literal trailing unit) — a genuine, corpus-only derivation, deliberately NOT a resolved final duration: a spell's actual duration depends on the CASTING CHARACTER's caster level, which no corpus row states (unlike a monster's own HD), so resolving to one number would repeat the ability-score-scaling fabrication `SD31-E6-F1-002` already correctly refused. Confirmed this is the engine's own existing policy, not a new one: `pcgen_desc::render_pcgen_desc` already drops an unresolved `CASTERLEVEL` argument tail rather than guess. **898 fixture entries** landed (`tests/fixtures/rules_core/derived-evaluator-fixtures.json`'s new `spell_entries` array), generated by `scripts/derive_spell_caster_level_duration_fixtures.py` — reads ONLY the pinned upstream PCGen `.lst` bytes at each candidate's `(book, source_file, source_line)` (never `data/corpus/`, which the Rust evaluator itself reads and must therefore stay independent of, matching the equipment fixture's own documented independence guarantee), re-hashes each file itself (never copies `data/corpus`'s stored sha256), and uses a standalone Python regex, never the Rust parser. Corpus-wide shape census (re-derived, not assumed): of 1161 `DURATION`+`CASTERLEVEL` tokens, **1046 match the simple linear shape**; 115 carry `min(`/`max(`/an additive term/an "instantaneous or..." alternation and are refused, never guessed (worked examples: `(ConjurationNaturalistsCharmBonus+(CASTERLEVEL)) minutes [D]`, `Concentration, up to (CASTERLEVEL) rounds`). Of the 1046, 898 resolve against `data/corpus/`'s own `raw_tokens` (6 candidates — e.g. `advanced_players_guide:spell:fester` — are pre-`.lst`-pipeline `web_second_source` records with no `raw_tokens` at all, filtered out; `retro.py rework` logged for the generator's own self-correction here); the remaining ~142 are outside this cycle's `--limit`-free full run's `skipped_no_duration_field`/other-field-shape buckets, effectively zero residue for THIS exact shape. **Wired a real production consumer, not left resting on the fixture alone (Decision 8)**: `SpellCatalogEntryDto.duration: Option<String>` (`apps/desktop/src-tauri/src/spell_catalog.rs`), computed via `all_spell_caster_level_durations()` (new pub fn, same file territory) over the live `data/corpus/<book>/spell/` cache, served over the real `list_spell_catalog`/`list_spells` Tauri commands, rendered on `SpellCatalogScreen.tsx` as `Duration: <text>` only when present (`apps/desktop/src/boundary/loadSpellCatalog.ts` DTO widened; 4 pre-existing test fixtures across `spellCatalogRuntime.ts`/`itemPickerFilter.test.ts`/`spellPickerOffering.test.ts`/`spellsTabModel.test.ts` updated with `duration: null`, count-change-needs-a-sweep grepped). Guarded regen, measured: board `done` **9,780 → 10,678 (+898)**, 25.39% → 27.72%; `spell` `done` **252 → 1,150 (+898)**, `held` **1,433 → 535 (-898)** — zero movement elsewhere, zero regressions (`docs/work-inventory.json` restored after measurement, per the wave rule). **Remaining named scope, not closed by this cycle**: the 199 `range_keyword` units (RANGE:Close/Medium/Long, a different token family, same "no fixed corpus value" shape) and the 5 `prose_formula_segment` units are untouched; a future F2 cycle can extend `derived_evaluator_fixture_check`'s spell seam to those, and/or extend the linear-DURATION seam past the ~142-unit residue this run's own `skipped_complex`/`skipped_no_duration_field` counts named (re-run the generator with `--limit` removed and diff against a fresh `docs/work-inventory.json` regen to size it exactly, since the held population itself will have moved). The SAME `derived`-held shape is very likely present for `class_feature`/`feat`/`monster_ability`/`companion` kinds too (all show up in Decision 7's own `magnitude_token_count==0` census as carrying `derived` records) — this card's file territory is spell-only, so those are named, not sized or touched. | Built the seam + a sizeable first fixture batch (898, not the theoretical max) + a real consumer, matching Decision 8's "wire it" precedent up front rather than repeating the pattern that required a follow-up cycle for `SD31-E6-F1-002`'s monster seam. Did not attempt the character-dependent live-number version (would be fabrication) or the `range_keyword`/`prose_formula_segment` families (different token shape, out of this cycle's budget after the DURATION family). | *(no ruling needed — the remaining ~204-unit residue and the sibling-kind opportunity are both engineering scope for a future F2/F9/F7/F8 cycle, not an operator decision)* |
| 140 | 2026-08-16 | SD31-ATTRIB-003 | RULING-NEEDED | **Building the per-race citation evidence table the operator asked for (CRB-7-vs-ARG, Bestiary-4-exception) surfaced a THIRD race-attribution book the existing `RACE_NEWEST_PRINTING` logic never considered: `campaign_setting/inner_sea_races` (`SOURCELONG:Inner Sea Races`, `SOURCEDATE:2015-09`) — newer than the Advanced Race Guide (2012-06) and older than only Bestiary 5/6/Ultimate Wilderness.** `isr_abilities_race.lst` supplies full base-trait (Ability Scores/Type/Size/Speed/Vision) `.MOD` citations, with real page numbers, for **all 43** `RACE_TRUE_BOOK` races — but `isr_races.lst` itself is 100% `.MOD` rows and carries **zero** base `RACE:` declarations (`grep -c "^RACE:" isr_races.lst` → 0), the same "cites, never declares" shape `core_essentials`'s own root files have (Decision 9) — except Inner Sea Races IS a real, independently-published Paizo sourcebook, not a PCGen-internal packaging construct, so Decision 9's "not a book" answer does not mechanically apply to it. Applied literally, "newest `SOURCEDATE` among genuine base-trait citers" would move every CRB/Bestiary-1/2/3/(most of)ISWG race to Inner Sea Races instead of ARG. Full evidence: `artifacts/SD31-ATTRIB-003-race-evidence.md` §1-§2. | Did not change `RACE_TRUE_BOOK`/`RACE_NEWEST_PRINTING` (out of this card's file territory and the operator has not ruled) — built the full per-race table showing every race's citation evidence both including and excluding Inner Sea Races, so the ruling can be made against evidence. | *(needs an operator ruling: does a citation-only compendium like Inner Sea Races count as a "printing" under Decision 10's "newest publish wins," or does it stay excluded the same way `core_essentials`'s own citation-only root files are — same "declares vs merely cites" test Decision 9 already applies, extended to a real published book instead of a PCGen packaging construct)* |
| 141 | 2026-08-16 | SD31-W8-INTEGRATE-001 | RULING-NEEDED | **PRECEDENCE-1 FIX: wave-8 adversarial review CONFIRMED `site/dashboard/units/*.json` (new in `sd31/attrib-evidence-003`) published 261 units' names verbatim where the unit's OWN cited corpus row declares `NAMEISPI:YES` — a publicly-deployed exposure (`deploy-site.yml` pushes `site/**` to Cloudflare Pages on push to `main`) that `declared_pi_shipping_audit` structurally cannot see (it scans `data/corpus/` only).** Fixed the minimum the precedence bar requires THIS wave: `site/dashboard/units/` is **not committed** to `tranche/11` at all (removed from the merge before this integration cycle's commit; confirmed absent from the working tree and from every commit on this branch — `git log --all -- site/dashboard/units/` after this wave shows no reachable commit adding it back). The two secondary `site-dashboard-check`/`publish-site-dashboard.sh --check` bugs the same review confirmed (non-repo-derived `usage`/`retrospective` subtrees making `--check` deterministically red on an aged tree; `cp -r` without `-p` making the shard-cache seed hit unconditionally) are BOTH fixed in this cycle, TDD-proven (a case added to `scripts/tests/test_publish_site_dashboard.sh` fails red without the `usage`/`retrospective` drop and passes green with it; the `cp -r -p` fix is applied but not yet independently self-test-proven — the fake producer has no shard-cache model, named below as owed coverage). **NOT fixed this wave, and it is the reason for the `RULING-NEEDED` severity, not `NOTE`:** the root PI gap in `scripts/observer/pf1e_dashboard_producer.py`'s `build_unit_shards()` itself — it has no NAMEISPI:YES cross-reference against the pinned oracle at all, so the NEXT cycle that runs `./scripts/publish-site-dashboard.sh` (without `--check`) and `git add`s `site/dashboard/units/` reintroduces the identical 261-unit exposure. Sizing the real fix (not attempted this cycle — cross-referencing 38,521 units' `(source_file, source_line)` against every `NAMEISPI:YES` declaration in the ~200-file pinned oracle tree in Python, at the scale `declared_pi_shipping_audit.rs` already does in Rust, is a properly-scoped build of its own, not a same-cycle patch) is this row's open question. | Removed the exposure from disk (the hard PRECEDENCE-1 bar) and fixed 2 of the 3 confirmed infrastructure bugs the same review found; did not build the producer-side name-PI screen — flagged rather than rushed, per the same "no stub" discipline that governs everything else in this package. | *(needs a build decision, not strictly an operator ruling: (a) port `declared_product_identity`'s oracle-cross-reference logic from Rust to `pf1e_dashboard_producer.py`'s `build_unit_shards()`, excluding any unit whose cited row declares `NAMEISPI:YES` from the shard the same way `data/corpus/` already excludes it, then extend `declared_pi_shipping_audit` to take `site/dashboard/units/` as a second scan root so this class is gated rather than trusted on the next commit; (b) until (a) lands, no cycle should `git add site/dashboard/units/` under any circumstance)* |
| 142 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **Widening `leaked_pcgen_syntax` (row 141's sibling fix, part of the same wave-8 review remediation) surfaced ONE pre-existing, previously-invisible hand-authored leak, unrelated to any of the five merged lanes.** `src/rules_core/rules_tables/acg/equipment_data/magic_items.rs`'s "Gloves of Marking" reads `"...must save (Will DC %) or be shaken for 1 minute."` — a literal unfilled `DC %` placeholder in hand-typed Rust source, not a corpus-JSON field. `apps/desktop/src-tauri/src/equipment_catalog.rs`'s pinned `the_raw_percent_escape_stops_at_the_catalog_boundary` fixture updated 58→59 with the new `("ACG","MagicItems")→1` entry, same precedent `SD31-W6-INTEGRATE-001` already set for a moving pinned-leak count. Did not fabricate the missing DC — the source Paizo text is not available to this repo to re-derive it from, and this program's no-stub doctrine forbids inventing it. | Updated the pinned fixture to the true count; did not invent a DC value. | *(engineering follow-up, not an operator ruling: either source the real DC from a licensed reference and fix the literal string, or leave the record excluded from any prose done-bar until it can be)* |
| 143 | 2026-08-16 | SD31-W8-INTEGRATE-001 | RULING-NEEDED | **NOT FIXED this wave (named, sized, deliberately not rushed): `UNIVERSAL_MODIFIER_CUES` is still narrowed to the single literal phrase `"size bonus"`.** Wave-8 adversarial review CONFIRMED this has effectively zero recall outside that one phrase: over 13,203 `wiring_class=display` units across the five kinds the discriminator gates, 10,797 have recoverable prose, and it calls only 21 (0.19%) universal while passing 10,776 (99.81%) eligible for the text done-bar — including real shipped `done` records that state unconditional modifiers to computed values without the string "size bonus" appearing anywhere (`bestiary_4:monster_ability:flying_polyp_deflecting_winds` "+5 deflection bonus to its Armor Class and a +4 resistance bonus on Reflex saving throws"; `bestiary_4:monster_ability:mi_go_deceptive` "+4 racial bonus on Bluff and Disguise checks"; `bestiary_4:monster_ability:ningyo_group_grappler` "+2 racial bonus on combat maneuver checks"). Partial mitigation already in place, not new this wave: all such counterexamples found so far are `monster_ability`, a kind the app computes no AC/skill totals for at all (`monster_catalog.rs`'s DTO carries CR/size/speed/type only), so today's practical harm is bounded; `race_trait`/`class_feature` (the PC-facing kinds) showed no misclassification in the same review. | Did not widen the cue list this cycle — the correct fix is a sentence-scoped matcher (bonus phrase + the sentence carrying it contains no conditional cue and no negation), not a broader substring list, and it needs its own accuracy validation before moving any count, per Decision 1(e) — properly scoped follow-up work, not a same-cycle patch alongside five lane merges and three other fixes. | *(engineering follow-up, not strictly an operator ruling: add hand-labelled UNIVERSAL cases that do NOT contain "size bonus" to `universal_vs_conditional_discriminator_accuracy_tests` FIRST — watch them fail — then widen the cue list with a sentence-scoped matcher, then re-run the corpus-wide false-positive sweep `SD31-D7-PROSE-004` already built. Also correct the function's doc comment, which currently claims a false "not proven universal" verdict "costs nothing" — under this ruling it is exactly what banks a unit as done-on-text, so the asymmetry runs the other way)* |
| 144 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **NOT FIXED this wave: `enrich_equipment_raw_tokens.rs` (widened by `sd31/equipmod-e6f6-001` to fold `.COPY=` base-row tokens into shipped `raw_tokens`) calls neither SD-30 PI contract (`§52.3` blacklist sweep, `§53.5` declared-PI reader).** Contract gap, not a leak — re-verified this wave: all 7 equipmod source `.lst` files carry 0 `NAMEISPI:YES`/`DESCISPI:YES`, and a full 54-term `PI_BLACKLIST_TERMS` sweep over all 455 changed equipment records returned 0 new hits, so nothing PI-bearing has shipped through this path yet. Must be closed before a book with declared equipment-modifier PI reaches this path. | Not fixed — re-confirmed the gap is real and re-confirmed no current exposure; wiring the two contracts into a binary this cycle did not author is scoped follow-up, not a same-cycle patch. | *(engineering follow-up, not an operator ruling: wire `pi_screening::declared_product_identity` + the blacklist sweep into `enrich_equipment_raw_tokens.rs`'s write path, mirroring `enrich_spell_raw_tokens.rs`'s own redaction test, per SD31-W7-INTEGRATE-001's precedent for the sibling spell binary)* |
| 145 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **NOT FIXED this wave (pre-existing, owned by neither of wave-8's merged lanes): `cache_gen::class_feature::generate` applies the SD-30 PI contracts to NAME and DESCRIPTION only — `raw_tokens: tokens` is written as an unscreened, byte-for-byte transcription (`class_feature.rs:409/435`, `row_tokens` at `:345`).** Re-verified this wave: the 381 currently-shipped PI-REDACTED class_feature records ARE clean on disk (a DATA-only fix, commit `6b62df0dd`, redacted the already-generated records) but the GENERATOR itself was last touched at the earlier `f35a845a7` and was not fixed — regenerating the class_feature cache today would re-ship all 381 declared-PI `DESC:` strings verbatim into `raw_tokens`, caught only downstream by `declared_pi_shipping_audit` (which does check `raw_tokens` directly and is currently PASS/CLEAN). | Not fixed — confirmed the gap is real, confirmed the downstream audit is the only thing standing between this and a SAFETY-CRITICAL regen, did not touch a generator neither merged lane authored this wave. | *(engineering follow-up, not an operator ruling: redact the DESC entry inside `tokens` before constructing `CacheRecord` — union of declared `DESCISPI:` and the `§52.3` blacklist scan, mirroring `enrich_spell_raw_tokens.rs`'s own redaction test — and add a generator-level test that a `DESCISPI:YES` row's `raw_tokens` DESC ships as the marker)* |
| 146 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **NOT FIXED this wave: `v06_corpus_trap_report --audit` has zero coverage of `equipment`/`equipment_modifier`/`race_trait`/`class_feature`/`feat` — its 1,191-finding baseline is composed entirely of `monster` (839), `spell` (243), `companion` (84) and `monster_ability` (25).** Confirmed by tallying the report's own defect-line record paths and by re-pointing one `equipment_modifier` citation from its `.COPY=` line to its resolved base line and re-running: still exactly 1,191, no new finding, because the audit never reaches the record at all. This means DoD item 3 ("trap report unchanged") was VACUOUSLY true for every equipment/race_trait/class_feature/feat lane this whole package, not evidence those kinds are clean. | Not fixed — the audit's cache walk is out of every wave-8 lane's file territory; re-confirmed the coverage gap rather than citing the unchanged count as though it meant something for these kinds. | *(engineering follow-up, not an operator ruling: extend `v06_corpus_trap_report`'s cache walk to the equipment/equipment_modifier and race_trait/class_feature/feat caches, or stop citing its unchanged count as DoD-3 evidence for kinds it structurally cannot see)* |
| 147 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **Receipt corrections for the record, both CONFIRMED by wave-8 adversarial review, neither fixed by re-editing history:** (1) `SD31-D7-PROSE-004`'s receipt attributed its own +32 root-full test delta as "only ~13 of this cycle's own, the rest from concurrently-landing sibling cycles" — false; `git show`-diffed test-function counts at both ends of that cycle's own single commit show +37 in the ONE file it touched, and no sibling commit exists between its parent and its tip, so the whole +32 (with a small residual to trace) is this cycle's own. (2) `SD31-E4-F1-004`'s Samurai doc-comment claim of a full-oracle-tree grep returning "exactly two hits, both structural" does not reproduce (17 hits, 5 real archetype records, all in out-of-scope `player_companion` books) — FIXED in this wave's integration commit `06fd706b8` (doc comment and diagnostic both corrected with the real count and a forward-scope note), unlike the two findings above which remain open. | Corrected in the receipt trail here rather than editing the original cycles' commits; #2 additionally fixed at the source in this wave's own commit. | *(no ruling needed — record-keeping; #1's stale `BASELINE_ROOT_FULL_TESTS` should be raised to the measured actual by whichever cycle next touches `scripts/verify-baselines.env`, with the corrected attribution)* |
| 148 | 2026-08-16 | SD31-W8-INTEGRATE-001 | NOTE | **DoD item 3: `v06_corpus_trap_report --audit`'s wiring-class-mismatch baseline moved 1,191 -> 1,225 (+34), fully traced, zero unexplained.** Exact set diff against the SD31-W6-INTEGRATE-001 baseline log (`comm -13`, both sorted, byte-exact line match): 0 findings resolved, 34 new, and EVERY ONE of the 34 is a `.COPY=` record (32 `ultimate_equipment:ue_equip_arms_armor.lst` equipment records — e.g. `Barding (Agile Breastplate/Large)`, stored `static` vs freshly `computed:pre_guard` — plus 2 `advanced_players_guide:apg_spells.lst` spell records — `Elemental Aura (Cold Only)`/`Corruption Resistance (Evil)`, both real `<base>.COPY=<name>` rows verified against the pinned oracle). This is the DIRECT, EXPECTED consequence of this cycle's own `wiring_class::build_copy_base_index` fix (`4fc65df96`, closing the GAMED equipment_modifier gap): the LIVE classifier now correctly resolves `.COPY=` base rows corpus-wide (not equipment_modifier-scoped — the fix touches every kind's closure), but the mandate forbids a wholesale `data/corpus/` regen this cycle, so the 34 affected records' STORED `wiring_class` field on disk stays at its pre-fix value, and the trap report (which compares stored-vs-fresh) correctly flags the drift it exists to catch. Not a regression in board honesty — the freshly-computed side is now MORE correct, not less; the stored side is stale metadata a targeted regen would fix. | Did not regenerate `data/corpus/` (wholesale forbidden this cycle; a targeted 34-record regen is a scoped follow-up, not attempted here to avoid touching corpus JSON under wave pressure). Traced every single one of the 34 by exact set diff rather than estimating from category totals. | *(engineering follow-up, not an operator ruling: re-run the `ue_equip_arms_armor`/`apg_spells` per-book cache generators — both already carry the `copy_base_index` fix via `cache_gen/mod.rs`'s shared `WiringClassIndex` — for ONLY these 34 records' books, not a wholesale corpus regen, then re-run the trap report and confirm it returns to 1,191)* |
| 149 | 2026-08-16 | SD31-W8-INTEGRATE-001 | RULING-NEEDED | **PRECEDENCE-1, NEW FINDING (self-discovered while performing the mandated site-dashboard publish step, NOT part of wave-8 adversarial review's confirmed list, and PRE-EXISTING -- verified present byte-for-byte in `HEAD` before this cycle touched anything): `site/dashboard/PF1e-dashboard.json`'s `manifests`/`content_state`/`matrix` sections ship real, declared-`NAMEISPI:YES` class/prestige-class/feat/spellbook/magic-item names as plain roadmap/task-tracking text, on the SAME publicly-deployed surface (`deploy-site.yml`, `site/**` -> Cloudflare Pages) row 141 already flagged for the `units/` shards.** Cross-referenced the pinned oracle's full `NAMEISPI:YES` declaration set (1,957 names) against the committed file with a word-boundary match (not a bare substring, to exclude collisions like "Nex" inside "Nexus"/"Next"): 56 names survive the boundary check, including unambiguous prestige-class/feat identities that also appear as `"id"`/`"name"` manifest-entry pairs (`{"id": "aldori_swordlord", "name": "Aldori Swordlord", "book": "Adventurer's Guide", ...}`, `{"id": "magaambyan_arcanist", "name": "Magaambyan Arcanist", ...}`, `{"id": "rivethun_emissary", "name": "Rivethun Emissary", ...}`), plus a large `prestige_classes`/spell/feat/magic-item roster array carrying `"Aldori Alacrity"`, `"Aldori Artistry (Sunder/Trip/Disarm/Reposition/Steal)"`, six `"Trlbal Scars (...)"` variants (the raw oracle's own typo for "Tribal", preserved verbatim), `"Shobhad Longrifle"`, `"Otyugh Hide"`, `"Scepter of Shibaxet"` and others. Source traced: this content originates from `docs/work-inventory.json`'s own `corpus_key`/`name` fields for classes/records the board tracks as NOT-YET-INGESTED, surfaced by the producer's manifest/roadmap-building logic as a public "what's coming next" list — structurally different from the `units/` shard exposure (a public TODO naming upcoming work, not a shipped game-mechanics record), but the SAME governing rule applies verbatim: "a key/name cannot be redacted... its mere presence on disk IS the violation" (`declared_pi_shipping_audit.rs`, `decisions.md §50.3`). Not fully sized: the 56-name word-boundary count still needs per-name `(source_file, source_line)` resolution the same way row 141's shard analysis did (some of the 56, e.g. `"Signifer"`/`"Skald"`/`"Erastil"`/`"Shackles"`/`"Darklands"`, may be coincidental matches against a DIFFERENT declared row sharing that string — "a shared name never implies a shared thing" — and were not individually resolved this cycle). | Did NOT withhold this wave's `site/dashboard/PF1e-dashboard.json` refresh — the exposure is pre-existing (already public from prior waves' commits, unaffected by whether this cycle refreshes the file) and withholding would leave the public feed silently reporting stale board figures without closing anything. Did not attempt a same-cycle fix — scrubbing names from `manifests`/roadmap content is a design decision (what should a public "what's coming next" list show?) that needs an operator ruling, not a unilateral field removal that could silently break the roadmap feature under wave pressure. | *(needs an operator ruling, PRECEDENCE-1: should the producer's manifest/roadmap builder (a) drop the `name` field for any not-yet-ingested record whose corpus row declares `NAMEISPI:YES`, keeping only the `id`/book/status shape, matching how `data/corpus/` and the row-141 shard fix both treat name-PI, or (b) something else? Until ruled, `scripts/observer/pf1e_dashboard_producer.py`'s manifest/roadmap-building functions are the file to extend with the same oracle cross-reference row 141 already asks for, applied to this second surface too)* |
| 150 | 2026-08-17 | SD31-E4-F1-005 | NOTE | **`site-dashboard-check` (`scripts/publish-site-dashboard.sh --check`) spuriously fails from ANY worktree whose checkout path differs from the one that last published `site/dashboard/PF1e-dashboard.json`, even with ZERO real content drift.** Root cause, proven by a scrub-diff of the committed feed against a fresh regen (using the real `--check` scripts scrub logic verbatim -- seed-then-overwrite the same path, strip `generated_at`/`generated_by` recursively, drop `usage`/`retrospective`, strip the `[engine content dump <ts>]` suffix): the ONLY surviving difference is `/unit_index/source_document`, an ABSOLUTE path (`$REPO_ROOT/docs/work-inventory.json`, itself resolved from `${BASH_SOURCE[0]}`'s own directory in `publish-site-dashboard.sh`) that is never stamp-stripped, so it always differs between two different checkouts even when their `docs/work-inventory.json` content is byte-identical. Reproduced on `SD31-E4-F1-005`'s own worktree (`.claude/worktrees/wf_071aed05-f44-1`) against the feed last published from wave 8's primary-checkout run: committed `source_document` reads `/home/ubuntu/workspace/repos/codex/docs/work-inventory.json`, fresh regen from this worktree reads `/home/ubuntu/workspace/repos/codex/.claude/worktrees/wf_071aed05-f44-1/docs/work-inventory.json` -- everything else byte-identical after the real scrub. Since every dispatched cycle in this package runs in its own `git worktree add` tree (`AGENTS.md`'s own concurrency rules), this stage is structurally guaranteed to spuriously fail for every non-primary-checkout cycle unless it happens to run from the exact same absolute path the feed was last published from -- confirmed NOT caused by, or worsened by, this cycle's own two commits (the diff above is the same with or without them; not re-run against `HEAD~2` directly, but logically guaranteed regardless: the producer only reads `docs/work-inventory.json` (byte-identical at every commit this cycle made, confirmed via `git diff --stat a9426b760..HEAD -- docs/work-inventory.json` returning empty) to build `unit_index`, and `source_document` is a pure function of `$REPO_ROOT` (this worktree's own path), never of source code content). | Did not "fix" it by running `./scripts/publish-site-dashboard.sh` for real from this worktree -- that would commit a feed whose `source_document` bakes in THIS worktree's own path, which would then spuriously fail for the NEXT cycle that is not dispatched to this exact same worktree, merely relocating the defect rather than closing it. Left `site/dashboard/PF1e-dashboard.json` untouched; `VERIFY_EXIT` from this cycle's own gate run will show `site-dashboard-check` red for this reason alone, named here so it is not mistaken for a regression this cycle caused. | *(no ruling needed -- a fix, not a decision: `scripts/publish-site-dashboard.sh`'s `--check` mode should scrub `unit_index.source_document` the same way it already scrubs `generated_at`/`generated_by` -- either drop it from the comparison entirely, matching the `usage`/`retrospective` treatment, or normalize it to a path relative to `$REPO_ROOT` before comparing. Out of `SD31-E4-F1-005`'s own file territory -- `scripts/publish-site-dashboard.sh` and `scripts/observer/pf1e_dashboard_producer.py` are lane-2/observer-tooling files, not `pilot_compute.rs`/`archetype_resolver.rs`/`rules_tables/*`)* |
