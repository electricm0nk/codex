---
canonical: true
purpose: Patterns that may apply beyond where they were found. A sweep closes only when the corpus
  has been checked and the remaining count is stated — never when one instance is fixed.
---

# Sweeps

**The rule:** fixing the instance you found is not closing the sweep. State the corpus-wide count.

| # | Sweep | Found | Status | Corpus-wide count | Notes |
|---|---|---|---|---|---|
| S1 | **The Monk case — data present, dispatch missing.** Monk's chassis table was complete for months; only the string→ClassId mapping in `table_class_id` was absent. **How many of the 157 not-done classes are in this same state?** | code comment 2026-07-29 | **CLOSED wave 27 — swept, answer is ZERO** | **0 of 157** | Pattern fully exhausted by earlier waves: all 34 classes that have a real chassis table anywhere (CRB 11, APG 6, ACG 10, PU 4, UC 3) already dispatch. Verified by grepping every `rules_tables/<book>/` for orphaned ClassId/class_table infrastructure — none exists. **This is what a closed sweep looks like: a corpus-wide count, not a fixed instance.** |
| S2 | **Generalise S1 beyond classes.** If a class could be computable-but-unreachable, so could a race, a companion, a monster. Wherever a table and a dispatch are separate, the same gap can exist. | derived from S1, 2026-08-21 | PARTIAL — wave 28 checked the 3 kinds that carry an `IdEnum`+table split (class/race/monster: 19 enum hits, 11 `Pf1SchoolId` copies) and found 0 new gaps there, but a wave-28 lane's own S1-adjacent finding refutes "the gap cannot occur outside 3 kinds by construction": `derived_evaluator_fixture_check.rs` has zero references to `SPELL_BOOK_B6`/bestiary_6 while `spell_resolver.rs` defines `pub const SPELL_BOOK_B6` — a table/consumer drift with **no `IdEnum` involved at all**. Do NOT close S2 on the enum-architecture argument; broaden it to "any source-of-truth table vs. consumer drift," not only enum-mediated ones. **Wave 30**: the two kinds the brief named as unchecked, done. **Companion CLOSED at 0** — `companion_chassis::COMPANION_BOOKS` is read by every consumer (`gen_book_cache.rs`, `companion_catalog.rs`, `v06_work_inventory.rs`, `corpus_ingest_diagnostic.rs`) and `companion_catalog.rs` already carries a locking test asserting every table entry reaches the catalog (`grep -n "without reaching the catalog fails here" apps/desktop/src-tauri/src/companion_catalog.rs` — line 909; **corrected on adversarial review**: the original citation quoted the comment text without its literal backticks around `COMPANION_BOOKS`, which does not match; the semantic claim, a real locking test asserting every table entry reaches the catalog, holds) — the Monk shape cannot recur here undetected. **Equipment: the enum/table-dispatch shape does not apply** — no `EquipmentId`-style enum+table+dispatch layer exists (checked: no equipment id enum defined anywhere in `rules_tables`); a corpus record IS the artifact once written, there is no intermediate dispatch an id could go missing from. Equipment's real table-vs-consumer risk is the cross-generator `remove_dir_all` collision S6/`defects.md` D9 already found — cross-referenced, not double-counted. | 0 new gaps in the 3 enum-bearing kinds; ≥1 non-enum drift found (dormant, 0 live units today); companion CLOSED 0; equipment N/A-by-architecture, its real risk filed under S6/D9 | Wave 28 lane originally marked this CLOSED; reverted to PARTIAL on adversarial review (`VISIBILITY-sweeps.md`, `THE-BOX.md` §2.6/§6). Race/monster not re-derived wave 30, standing at wave 28's figures. |
| S3 | **Race-trait key matcher.** `modelled_race_of_race_trait()` requires the key segment before the first `~` to EXACTLY equal a bare race name, so a compound key ("Elf Shaman Hex Range Choice ~ Chant") reports `race_trait_race_not_modelled` though the race is named. Wave 22 fixed hyphen/space normalisation; the compound-key case is still open. **Wave 30: the corpus-wide count is derived for the first time** — `evidence == "race_trait_race_not_modelled"` on the live inventory returns 2,472 not-done `race_trait` units (`docs/release/SD-31-corpus-closure-grind/artifacts/w30-coverage-table.json` group `W30-RT-NOTMODELLED`, reproduced by `python3 scripts/coverage_ledger.py --groups <that file>`). Not root-caused or fixed this wave — a compound-key normalisation is a RECLASSIFICATION project, out of this wave's no-banking scope. | wave 19 | OPEN — re-named in waves 23, 24, 25, 26, 27, 30 without closure; corpus-wide count now known | **2,472 of 2,472 `race_trait_race_not_modelled` units, corpus-wide, wave 30** (ACG share was previously cited as 137; not re-split by book this wave) | The canonical example of why this directory exists. Seven waves of naming it, six without a corpus-wide count. Fixing it is a RECLASSIFICATION, not a doneness gain — report separately. |
| S4 | **Scope-carrying tokens with nowhere to carry the scope.** Wave 17 shipped a widening for Amulet of Mighty Fists whose type had no field for "natural attacks only", while the live consumer applied the bonus to every weapon. Caught and reverted; re-opened at wave 18 because the re-land guarded one consumer path and missed the one the app actually calls. | waves 17/18 | PARTIALLY SWEPT | unknown | Sweep question: how many other emitted types assert a scope the consumer cannot honour? Never asked corpus-wide. |
| S5 | **Generators emitting unscreened fields.** `cache_gen::class_feature` screened `data.description` for declared PI but shipped `raw_tokens`' own `DESC` entry completely unscreened — it had shipped clean by luck, not construction. | wave 19 | PARTIAL — wave 28 checked 5 `raw_tokens`-producing `enrich_*.rs` binaries, all 5 screen correctly (one field-generically, stronger than the original fix). **NOT a closed universe**: at least 4 more binaries emit `raw_tokens` and were not checked (`ingest_races.rs`, `ingest_race_traits.rs`, `ingest_pu_classes.rs`, `gen_book_cache.rs`); a first pass shows each references screening machinery, but this is not verified the way the 5 were. | 5 of ≥9 binaries checked, 0 unscreened found so far | Wave 28's own filed report called this "encouraging, 5/5" — corrected here: the universe was never established, so "5/5" cannot mean "closed." |
| S6 | **Self-erasing fixture generators.** Three of four generators in wave 15 selected units by a status that stamping then overwrites, so a second run wrote an empty fixture and would have silently withdrawn every banked unit at the next regen. No gate caught it. | wave 15 | **CLOSED for all 10 Python generators (wave 29). PARTIAL for the Rust binary residual (wave 30, lane 4) — one genuinely new instance found and live-reproduced, one lead investigated and refuted, the rest of the population triaged.** See `VISIBILITY-sweeps-2.md` §1 for the full per-binary table and every reproducible command; summary below. Python-generator history unchanged from wave 29: `derive_derived_evaluator_fixtures.py` lost 2,110 rows across 8 families on a single run from the committed state (rebuild-from-scratch + hardcoded `"monster_"` allowlist); fixed by preserving every top-level key BY EXCLUSION, plus an independently-necessary `HELD_STATUSES` fix, both mutation-proven; all 9 sibling Python generators re-run once each from the committed baseline, none shrink any family. **Rust residual, wave 30**: of 29 `enrich_*`/`gen_*`/`ingest_*` binaries (exact count from `ls src/bin/ \| grep -E '^(cache_gen\|enrich_\|gen_\|ingest_)'`, re-counted twice against a first-draft miscount of 27; `cache_gen` is a module, not a binary), the 5 `enrich_*` binaries are SAFE BY CONSTRUCTION (per-file read-modify-write over a generic `serde_json::Value`, with an explicit `AlreadyEnriched` idempotency guard — architecturally cannot drop a sibling field or re-derive-and-lose). `gen_book_cache.rs` holds 4 generator functions sharing one file: `gen_monster_book` (monster/monster_ability) already carries the exact `out_path.exists()`-skip fix (`SD31-E6-F9-005`) that this shape needs; `gen_pathfinder_unchained` (feat/equipment) was FIRST filed as safe on directory-disjointness grounds alone — **corrected on adversarial review (wave 30 reviewer 8): it is vulnerable on the OTHER axis, raw_tokens self-erasure.** `grep -n "out_path.exists()" src/bin/gen_book_cache.rs` shows its `gen_pathfinder_unchained()` function (line 616) has zero exists-guards, identical in shape to `gen_advanced_race_guide`; all 42 of 42 `data/corpus/pathfinder_unchained/equipment` records carry `raw_tokens` (written by `enrich_equipment_raw_tokens.rs`, which lists `pathfinder_unchained` among its books), and `gen_book_cache.rs` never writes `raw_tokens` anywhere (3 mentions in the file, all comments) — so a run strips it from all 42 without re-supplying it. Directory-disjointness only protects against the cross-generator-collision axis; it says nothing about self-erasure. Not live-reproduced (budget), but code-read confirmed the same way `gen_companion_book`/`gen_core_rulebook_cache` were below; **`gen_advanced_race_guide` (spell/equipment/feat), `gen_pathfinder_unchained` (feat/equipment, corrected above), and `gen_companion_book` (companion) have NO such guard** (`grep -n "out_path.exists()" src/bin/gen_book_cache.rs` returns exactly 2 hits, both inside `gen_monster_book`) — the fix that protects two of the file's four functions was never extended to the other two. **Live-reproduced for `gen_advanced_race_guide`**: ran it against the real committed corpus in the isolated worktree (git status clean before, reverted with `git checkout --` after, `docs/work-inventory.json` hash unchanged throughout) — all 93 `advanced_race_guide` spell records lost `raw_tokens` (100% of the book's spell population; one spot-check: 88 lines → 11 lines, `raw_tokens` count 1 → 0), and 15 real, populated equipment records (e.g. `equipmods/cruel.json`, a shipped weapon special ability with real description/raw_tokens) were permanently deleted, not renamed — 0 untracked files after, count matched the generator's own "200/200 written" against 215 committed. Root cause: `gen_equipment_gap_tables.rs` and `cache_gen::equipment_gap` ("ARG" mapping) also write into the same directory; `gen_advanced_race_guide`'s unconditional `remove_dir_all` destroys their output too. **`gen_companion_book` (927 of 927 companion records, 100% of the kind, across 16 `CompanionBookSpec` books) and `gen_core_rulebook_cache` (a separate binary, D1-adjacent; 664 of 664 core_rulebook spell records) are confirmed vulnerable by the identical code-read method — zero exists-guards — but NOT live-tested, given their size; stated as high-confidence-by-code-read, not proven live, deliberately.** A parallel lead — `gen_cache_beastiary`'s hardcoded `web_second_source` citation for "Rag Armor (Dark Creeper)" — looked like a third D1 instance on static read; checked live via `repair_lst_provenance --check` (non-destructive dry run) and REFUTED: the record is permanently un-narrowable (`repair_lst_provenance` itself refuses it, "no corpus row matches this record's identity"), so nothing is being reverted. The same dry run also shows `advanced_players_guide`/`core_rulebook` currently have **zero pending narrows** (372/2,993 "already cited", 0 "narrowed") — D1's 412-narrowed-citations describes content already sitting in the corpus today, at risk from a future rerun of those two generators, not a currently-pending repair. The `ingest_races.rs`/`ingest_race_traits.rs`/`ingest_apg_race_traits.rs` family (3, not the originally-filed 4) carry an in-line doc comment naming this exact hazard and the incident (`SD-31-E6-F4-002`/`-003`) that already fixed it for them; re-confirmed safe, not new territory. **Corrected on adversarial review**: `ingest_pu_classes.rs` does NOT carry that comment (0 references, vs 6/17/2 in the other three) and its `remove_dir_all` at line 901 is an unconditional whole-directory wipe of `pathfinder_unchained/{class,class_feature}`, not the scoped/per-content-field shape the other three use — its SAFE verdict survives on DIFFERENT, real grounds (it writes `raw_tokens` itself — 11 references — and `cache_gen::class_feature.rs` explicitly excludes `pathfinder_unchained` from its own write path, so neither the self-erasure nor the collision axis applies), but the evidence originally cited for it was wrong. Filed as `defects.md` D9. | **10 of 10 Python generators confirmed non-shrinking (wave 29). Rust: population 29 (not 27 — see the population column note in the left cell).** **Corrected on adversarial review: 9 of 29 binaries confirmed safe or refuted (5 `enrich_*` + `ingest_races.rs`/`ingest_race_traits.rs`/`ingest_apg_race_traits.rs` — 3, not 4, of the `ingest_*` family; `ingest_pu_classes.rs` moved to the vulnerable-by-code-read column, see the Notes cell — + `gen_cache_beastiary`); 3 of 29 confirmed-or-high-confidence to contain a vulnerable write path (`gen_book_cache` — 3 of its 4 internal functions, `gen_advanced_race_guide`/`gen_companion_book`/`gen_pathfinder_unchained` — and `gen_core_rulebook_cache`); 17 not reached** (10 `gen_*`: `gen_cache_acg`, `gen_cache_apg` equipment-citation aspect already known via D1, `gen_cache_class_feature`, `gen_cache_equipment_gap`, `gen_cache_hand_authored_equipment`, `gen_cache_spell_lane_dump`, `gen_cache_ultimate_equipment`, `gen_class_feature_grants`, `gen_equipment_gap_tables`, `gen_feat_gap_tables`; 7 `ingest_*`: `ingest_adventurers_guide_spells`, `ingest_class_spell_levels_arg`, `ingest_inner_sea_gods_spells`, `ingest_occult_adventures_spells`, `ingest_ultimate_combat_spells`, `ingest_ultimate_magic_spells`, `ingest_ultimate_wilderness_spells` — their write-vs-merge pattern and cross-generator directory overlap were not traced this wave; 10+2+17=29 ✓). | **Still open, narrowed**: 17 Rust binaries genuinely unassessed (listed in full in `VISIBILITY-sweeps-2.md` §1.9); the fix for the 2 confirmed-vulnerable binaries (extend `SD31-E6-F9-005`'s `out_path.exists()`-skip from `gen_monster_book` to its own 2 siblings inside `gen_book_cache.rs`, plus `gen_core_rulebook_cache`) is scoped and ready for a code-writing wave — see `defects.md` D9. Not carried forward as an unmeasured residual anymore; carried forward as a named, sized, partially-fixed defect. |
| S7 | **Bar checks that count without checking identity.** Wave 16 review found a companion bar check asserting *how many* abilities but never *which*. | wave 16 | PARTIAL — wave 28 checked ~8 sites in `pilot_compute/*.rs` + 3 named grounding files, 0 recurrences found. **Wave 30: the narrowed sub-question CLOSED at 0.** `grep -rn "assert_eq!(.*\.len()" --include=*.rs src apps/desktop/src-tauri/src` returns 494 sites this wave's exact command (differs from wave 28's cited 311 — different literal pattern/scope, both reproducible from their own stated commands). A scanner tracking each site's nearest enclosing `fn` and whether it was `#[test]`-decorated found: 487 of 494 sit inside `#[test]` functions (fixture assertions, not gates); of the 7 production sites, exactly 1 sits inside a `ground_*`/`compute_*`/`classify_*`-named function (`pilot_compute::compute_pu_class_chassis`), and reading it in context shows it is a documented single-class-only precondition guard, not a content-blind doneness gate — not the wave-16 shape. The other 6 production sites are test-harness helpers or genuine ingest-completeness assertions (`assert_eq!(written, rows.len(), ...)`, an S6-adjacent protection, not an S7 defect). | 0 of 494 (proxy-and-name-filtered; full detail `VISIBILITY-sweeps-2.md` §4) | **Narrowed sub-question closed.** Broader S7 (any "counts without checking identity" shape not literally `assert_eq!(_.len())` inside a matching-named function) remains open — this method cannot see an `if x.len() >= N` gate or one hiding in a differently-named helper. |
| S8 | **`ClassFeatureData.class` read from key text.** Ships `class: "Sigilus"` where the true granting class is Magus, across ~12,247 records. Wave 22 fixed the same defect for grant data; this field was out of its scope. | wave 22 | **MEASURED wave 28, not yet closed** — first real corpus-wide number: **8,210 of 11,502 (71.4%)** class_feature records with `data.class` set carry a non-dispatched-class value. Sub-split: 2,682 are `"<RealClass> <Suffix>"`-shaped (fixable via a `GROUP_PREFIX→CLASS` lookup table, tool-shaped — see `THE-BOX.md` §4); 532 are real classes outside the 34-dispatch universe (Magus/Medium/Psychic/Vigilante/Shifter — needs the class itself modelled, not a field remap); 4,996 unresolved by this wave's prefix heuristic. 979 records have `data.class == None` — defect or intentional, not investigated. **Wave 30: re-derived, the 11,502 anchor reproduces exactly** (`total − none_class = 12417 − 915 = 11502`); the `None` sub-count drifted 979 → 915 (−64), not investigated — plausibly wave 29's `adventurers_guide` onboarding adding new records, flagged not explained. The 71.4% breakdown itself was not re-run. | **8,210 of 11,502 (71.4%)**, anchor re-confirmed wave 30 | Needs `data/corpus` regen once the remap table lands — sequence AFTER `blocked.md` B4/B5 land (Magus/Vigilante/Shifter overlap those rulings). |
| S9 | **The Ninja/Samurai shape — complete chassis blocked by one missing downstream-table row.** How many other units corpus-wide are one table row from working? | wave 27 | **PARTIAL, not closeable at 0 as first filed** — wave 28 checked only the 34 dispatched `class` units (answer: 0/34 are S9-shaped beyond the already-known Ninja/Samurai) and a related equipment_modifier probe-blind-spot lead (162 units claimed, **REJECTED on review** — closes 0, the probe's `applied_modifiers` is always empty so the targeted field can never resolve; see `THE-BOX.md` §2.5). The sweep's own question is corpus-wide across every kind, not just the 34 classes; that question is still open. **Wave 30**: re-derived the class-scoped anchor rather than trusting it — `grep -c "ClassWeaponProficiency {" src/rules_core/rules_tables/crb/weapon_tables.rs` returns 34, not 32 as first filed — **corrected on adversarial review**: the pattern also matches the struct definition (line 422) and a test-helper signature (line 598); the real table-row count is 32, confirmed via a Python line-range count restricted to the `CLASS_WEAPON_PROFICIENCIES` array body (lines between the `const` declaration and its closing `];`), which returns 32 — the semantic claim (32 of 34 dispatched classes have a row, the 2 missing are the known Ninja/Samurai pair) holds, the originally-cited grep command did not isolate it. Searched for other `ClassId`-keyed lookup tables sharing this architecture (`grep -rn "\.class_id == class_id\|\.iter().find(|.*class_id"` across `rules_core`/`apps/desktop/src-tauri/src`, 10 hits) — the only other class-lookup table, `CLASS_META`, is intentionally CRB-scoped (12 entries for the 11-12 CRB base classes, not a 34-class shared table), not a gap. No corpus-wide (non-class-kind) sub-question reached. | 0 of 34 classes (class-scoped sub-result only), re-confirmed wave 30 | Wave 28's own `todo/sweeps.md` edit marked this CLOSED at "0/34"; reverted here — the corpus-wide question was never asked. Wave 30 re-derived the class anchor (still 0/34) and checked for sibling class-keyed tables (none found narrower than 34); did not reach companion/monster/race/spell/equipment tables — genuinely large search, not attempted this wave. |
| S10 | **(new, wave 28) Natural-attack STR-damage refusal's true corpus-wide scope.** `monster` held's 253 units were censused at wave-17's OPEN-ISSUES row 310 as 176 WEAPONPROF DAMAGE STR + 49 VAR BonusDamage + 11 literal-constant + remainder — but a raw grep for the same token returns 410 hits corpus-wide, not 176. | wave 28 | NOT STARTED | 410 raw grep hits vs. 176 monster-kind-scoped; which non-monster records carry it and whether they are already done through another route is unknown | Sweep question: does the STR-damage refusal recur outside `monster`, and if so is it already resolved there? |
| S11 | **(new, wave 29) `type_facet`-substring proxies both over- and under-count against the real corpus row.** THE-BOX §2.1 F2's own reproduction filters `class_feature` units on `type_facet` containing "bonusfeat" (case/space-insensitive) to size the feat-cross-reference bridge at 431. Re-deriving directly from `data/corpus/*/class_feature/**/*.json`'s real `raw_tokens` (not the `type_facet` string) instead: 65 genuine matches carry a `type_facet` that never contains "bonusfeat" at all (`Golden Legionnaire ~ Swift Aid`'s is `GoldenLegionnaireSwiftAid`; `Superior Discernment ~ Sharp Senses`'s is `LanternBearerDiscernment`) — the census's proxy misses them entirely — while 25 of the proxy's own 431 fail a real correctness check once the actual token shape is read (22 carry a second mechanical token the proxy's own `type_facet` match can't see, 2 target a compound self-reference, 1 matches a feat with no description to lend). Net: 471 real, verified matches, not 431 — see `class_feature_feat_bridge.rs`'s own doc comment and `retro.py` correction `1787346410974-wave29-lane4-f8b81a`. | wave 29 (lane 4) | NOT STARTED beyond this one instance | 1 of however many `type_facet`-substring filters this wave's six census lanes used (class_feature G1's 817 pool names, unmeasurable bucket C's 958 group prefixes, and race_trait's G1/G4/G5 triage all filter on facet/group text the same way) checked; corpus-wide reach across the OTHER five unknown | Sweep question: how many of THE-BOX's own filed counts are `type_facet`/group-text substring proxies that would move, in either direction, if re-derived from the real `raw_tokens` instead? The §4 triage tool (class_feature_row_triage, proposed but not built) would answer this for free if it reads raw_tokens rather than the facet string. |
| S12 | **(new, wave 30) `class_feature_group_names_no_class_at_all` evidence-string honesty.** The evidence code's own text claims "no class at all", but a corpus-wide, non-sampled check of each matched record's raw `TYPE` token (exact first-segment match against all 34 dispatched classes, not a substring scan) finds a real class name on many of them. | wave 30 (lane 6 Part C) | **MEASURED, not yet closed.** Corpus-wide: 1,377 of 12,114 matched not-done `class_feature` units (11.4%) carry an unambiguous TYPE-token class signature contradicting their own `data.class`/group-name field, across 5 evidence codes — **corrected on adversarial review: the 5-code table sums to 1,321, not 1,377; the extra 56 hits (54 under `no_compiled_rule_set_for_book`) sit in codes the published table did not list.** Within `class_feature_group_names_no_class_at_all` specifically (this sweep's own evidence code, 2,551 raw / 1,971 matched-with-corpus-record): 471 of 1,971 (23.9%) show the signature — independently reproduced exactly by wave-30 reviewer 7. Nothing reclassified; this is an evidence-string-honesty finding, not a doneness claim — the 471 (and the wider 1,377) may still lack a real explanation/consumer once the correct owner is identified. | 471 of 1,971 within this evidence code; 1,321 of 12,114 (not 1,377) across all `class_feature` not-started | Command: `os.walk('data/corpus')` scan matching each record's `TYPE:` token against `ClassId::ALL ∪ ApgClassId::ALL ∪ AcgClassId::ALL ∪ UcClassId::ALL ∪ PuClassId::ALL` (34 total), full script `VISIBILITY-cross-kind.md` §4. Whether reclassifying any of these 1,321–1,377 units would change their VERDICT (not just their evidence string) is unmeasured. |
| S13 | **(new, wave 30) Does the R4 non-`.PC`-token race contamination (43 units, `blocked.md` B12) reach the 35 already-`done` race units?** Named by wave-30 lane 2 as unrun/out-of-population; answered in one command by wave-30 reviewer 7 using the same discriminator the lane already established (`type_facet` IS the verbatim raw `TYPE` token, `v06_work_inventory.rs:2090`). | wave 30 (lane 2, answered by reviewer 7) | **CLOSED, corpus-wide count is 0.** All 35 `done` race units carry a `.PC` segment in `type_facet`; 0 do not. Of the 60 not-done race units, 16 carry `.PC` and 44 do not (matches R1+R2+R3 = 16 vs R4+R5 = 44 exactly). | **0 of 35** | Command: `python3` filter over `docs/work-inventory.json`, `kind=='race'`, `doneness_verdict=='done'`, checking `'.PC' in (u.get('type_facet') or '')` — 35/35 True. Filed here rather than left open, per the wave's own standing rule against naming a one-command check as unrun. |
| S14 | **(new, wave 30) `.COPY=`-alias (`origin=="copy"`) population, cross-kind.** `THE-BOX.md`'s lane-reported "up to 861" (equipment + equipment_modifier) reproduces exactly for those 2 kinds, but the same `origin` field carries not-done units in 5 more kinds. | wave 30 (lane 6 Part B.4) | **MEASURED.** Corpus-wide not-done total is 966 across 7 kinds (equipment 451 + equipment_modifier 410 unmeasurable/in-progress/not-started splits summing to 861, plus race_trait 51, companion 25, spell 15, race 12, monster 2 = 105 more), not 861 across 2. Reproduced exactly by wave-30 reviewer 7. | **966 of 24,914**, 861 of which were already named | Command: `python3` filter over `docs/work-inventory.json`, not-done units, `origin=='copy'`, grouped by `kind`. Whether this population overlaps `ce_*` (S-below)/`visible==false`/lane-2's 141 was checked by reviewer 7: 19 units overlap lane 2's 141 (7 visible==false, 12 origin==copy — see S16). |
| S15 | **(new, wave 30) Do G6's two cheap-fix mechanisms (allowlist-word gap, dot-namespace miss) recur outside the 29 named `class_feature_no_dedicated_magnitude_id_matched_the_record_slug` units?** | wave 30 (lane 3) | NOT STARTED | 5 of 29 units are the cheap subset (4 allowlist-word-away, 1 dot-namespace-miss — see `levers.md` L18); whether the same two shapes recur corpus-wide outside this 29-unit evidence code is unchecked | Sweep question: is `CLASS_FEATURE_ID_MAGNITUDE_SUFFIXES` missing other common words, and does the dot-namespace miss (Summoner ~ Eidolon's 10 sub-ids invisible to the trailing-underscore-only matcher) recur for any other class's own sub-namespaced feature (e.g. a future Cavalier ~ Mount id family)? |
| S16 | **(new, wave 30) Do the Part-B cross-kind populations (book-onboarding-gate/`ce_*`/`visible==false`/`origin==copy`) overlap each other?** Lane 6 disclosed this as unchecked. | wave 30 (lane 6, answered by reviewer 7) | **MEASURED.** The four populations (422 + 566 + 921 + 966 = 2,875 memberships) cover only 2,249 distinct units — 626 double-memberships. 19 of those 2,249 also sit inside lane 2's 141-unit never-examined census (7 visible==false, 12 origin==copy, 0 book-gate, 0 `ce_*`). | **626 double-memberships among 2,249 distinct units**; 19 overlap lane 2's 141 | Set arithmetic over the four filters in `blocked.md` B7/B8 and this file's S14, reproduced by wave-30 reviewer 7. Not further investigated — which SPECIFIC pairs of populations drive the 626 overlap is unstated. |

## Wave 27's replacement finding — the 157 classified by real shape

S1 came back zero, but the census that answered it is the more useful artifact. The 157 not-done
`class` units are not one problem; they are five, and only one is cheap:

| Shape | Count | What it actually needs |
|---|---:|---|
| Prestige classes (58 CRB/APG + 19 Ultimate Psionics) | **77** | An entry-requirement gating mechanism that **does not exist anywhere in the codebase**. Independently confirmed by a sibling lane's CRB-10 finding. Not a wiring task. |
| Structurally not player classes | **48** | 33 Monster creature-type HD progressions, 7 Monster.Companion (incl. Eidolon), 3 Psionic power-list menus, 3 untyped edge records, 2 Vigilante identity records. **Operator ruling requested** — see `blocked.md` B4. |
| Real base classes with zero table | **≥20** (corrected wave 28, was 18) | Antipaladin, the 6 Occult Adventures classes, the 10 Ultimate Psionics classes, **plus Magus (ultimate_magic) and Vigilante/Shifter (ultimate_intrigue/ultimate_wilderness) — all three independently confirmed wave 28 via the same `class_absent_from_ClassId_ALL_and_book_class_id_enums` evidence tag, only Magus was previously named.** Net-new table construction. |
| Books with no compiled rule set | **28** | adventurers_guide 25, inner_sea_magic 3. From-scratch book work. |
| CRB NPC classes | **5** | Adept, Aristocrat, Commoner, Expert, Warrior. Real but untabled. |
| **Near-miss: Ninja + Samurai** | **2** | Complete, tested, correctly-dispatched chassis already. Blocked ONLY by a missing `CLASS_WEAPON_PROFICIENCIES` row in `weapon_tables.rs` — a file outside the lane's grant. **Cheapest known units in the program.** |

**S9 (new sweep):** the Ninja/Samurai shape — complete chassis blocked by one missing row in a
downstream table. How many other units corpus-wide are one table row from working? Nobody has asked.
See the S9 row above (table, wave-28 status) — the class-scoped sub-question is answered (0/34
beyond Ninja/Samurai), the corpus-wide question is not.

## Wave 28's replacement finding — does a render catalog no verdict trusts recur beyond class_feature?

Wave 28's class_feature census found F1: a generic render catalog (`class_feature_descriptions.rs`)
that is already built, already PI-screened, already leak-guarded — but `classify()`'s owner-
resolution logic doesn't trust its output as proof-of-holds, requiring an explanation-id join or
pool-catalog membership instead. **Whether this same shape recurs for `race_trait`/`monster_ability`
— both of which have their own `SD31-D7-PROSE-001`/`002` render surfaces — was proposed as a new
sweep candidate but not checked corpus-wide this wave.** Not yet added as a numbered sweep; needs a
first measurement before it earns one.

