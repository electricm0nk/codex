---
canonical: true
purpose: Known-wrong things, with blast radius. Distinct from sweeps: a defect is one broken thing;
  a sweep is a question about how widely a pattern applies.
---

# Defects

| # | Defect | Found | Blast radius | Status |
|---|---|---|---|---|
| D1 | **Equipment cache generators revert narrowed citations.** Re-running `cache_gen::apg::generate_equipment` or `gen_core_rulebook_cache` silently reverts 412 narrowed citations and withdraws up to 346 units. Standing mitigation: re-run `repair_lst_provenance` after either generator, before committing. | wave 17 (OPEN-ISSUES 264) | up to 346 units | DETECTION LANDED (`sd31_lst_provenance_repair_is_durable.rs`); the two generators themselves are still not fixed |
| D2 | **`classlevel()` does not verify its class-name argument.** `formula_interpreter.rs` documents that no consumer may bank a value through a `classlevel()`-bearing formula until this is resolved. **Reach measured wave 31**: a wave-31 lane first filed 2,340 not-done units / 616 distinct names, using a regex that scanned the whole token VALUE (including the field a formula WRITES to, not just what it READS) over a join that only found a corpus record for 53.6% of not-done units. **Corrected on adversarial review, using a complete `(book, source_file, source_line)` join restricted to the formula field only: 1,957 not-done units, 523 distinct variable names.** Still the single largest identified cross-cutting binding gap once the accumulator mechanism (`levers.md` L23) is separated from per-unit consumer wiring — corrected downward, not eliminated. | wave 25b (named), wave 31 (reach measured, then corrected) | **1,957 not-done units, 523 distinct names** (corrected from a first-filed 2,340/616) | OPEN — standing precondition, respected by wave 26; reach now measured and corrected, still not fixed |
| D3 | **Four formula shapes unreadable.** 431 of 2,671 corpus formulas (16.1%) refuse: PREVARGTEQ-embedded conditional addends (a different PCGen subsystem, BonusObj — the largest bucket), boolean-to-int coercion of a bare comparison, the `&&` operator, and `skillinfo(...)`. | wave 25b | unknown unit count behind them | IN FLIGHT (wave 26 uncovered-shapes lane) |
| D4 | **Anti-fabrication gates exclude seven classes.** Wizard, Bard, Paladin, Cleric, Sorcerer sit behind nine gates; Druid and Monk behind `is_druid_pillar_id`/`is_monk_pillar_id` LevelUpPlan allowlists that silently drop real ids. | waves 20/23 (OPEN-ISSUES 330/338) | caps the grant-fact consumer | OPEN — deferred across waves 21, 22, 23, 24 |
| D5 | **A silent drop is its own defect.** If the LevelUpPlan id-prefix allowlists discard real ids, what else have they been discarding? Never measured. | wave 24 dispatch | unknown | NOT STARTED |
| D6 | **equipment_modifier description-closure extraction bug.** 111 of 136 (82%) `unmeasurable`/"declared, no description" equipment_modifier rows actually carry real `SPROP:`/`DESC:`/`BENEFIT:` text on their pinned-oracle line — confirmed by a FULL (non-sampled) oracle sweep of all 136, not a spot check. Includes sibling "Ghost Touch" records where only 1 of 3 identical-shaped variants reaches text-complete. | wave 28 (`VISIBILITY-unmeasurable.md`) | 111 confirmed misclassified this wave; root cause not traced (needs closure-construction code read, out of a measurement-only wave's scope) | OPEN — not yet root-caused |
| D7 | **Self-erasing fixture generator — FIXED, root cause was ONE thing, not two.** `scripts/derive_derived_evaluator_fixtures.py` destroyed 2,110 fixture entries across 8 families on its first run from the committed state (S6). The write step rebuilt the whole fixture document from scratch every run and preserved only keys matching a hardcoded `"monster_"` prefix — that alone accounts for all 2,110 (94 `entries` + 2,016 across the other 7 families). `HELD_STATUSES` missing `fixture-verified` is a real, separate, genuine bug (it silently excludes freshly-verified `derived` units from ever being re-derived), but restores **zero** of the 94 `entries` rows on its own — wave 29 lane 1's report and this row both originally claimed `literal-verified` restored 83 of the 94; that is false. `literal-verified` only stamps `wiring_class == static` units (`v06_work_inventory.rs::apply_done_rung_stamps`), and `derive_derived_evaluator_fixtures.py` only ever selects `wiring_class == "derived"`, so a `derived`+`literal-verified` unit cannot exist and `literal-verified` in `HELD_STATUSES` is unreachable dead configuration. The 83 static-reclassified rows are restored solely by the carry-forward merge described below, not by the status tuple. | wave 15 (named), wave 28 (measured), wave 29 lane 1 (fixed, self-corrected by wave 29 integration after adversarial review) | 2,110 fixture entries, 8 families, confirmed by running the generator ONCE from the committed state (a twice-run diff is blind to this bug — see S6) | **CLOSED, wave 29 lane 1 + wave 29 integration correction.** Fix has two independent parts. (1) The write step now preserves every sibling top-level key by EXCLUDING this script's own keys rather than allowlisting a `"monster_"` prefix — this is the part that actually restores all 8 families, and merges (never deletes) `entries` rows a live selection no longer covers, which is what restores the 83 reclassified-to-static rows. (2) `HELD_STATUSES` now also carries `fixture-verified` (a real, independently-necessary fix — without it, a `derived` unit newly verified since the last run would stay invisible forever — but this fix alone, isolated, still produces ZERO fresh `entries` on a run from the committed state; mutation-proven by wave 29 integration: reverting `HELD_STATUSES` to its original 3-tuple leaves all 5 pre-existing tests green and the fixture byte-identical, because the carry-forward merge masks a fully-silent generator). Integration added a direct guard for this: `main()` now asserts `"fixture-verified" in HELD_STATUSES` and additionally the corpus-driven test `at_least_one_entry_is_freshly_derived_this_run_not_merely_carried_forward` (fails if the live selection ever again produces zero fresh rows against a non-empty inventory). `literal-verified` removed from `HELD_STATUSES` (was dead code; see Defect column). Reproduced against the real pre-fix commit (`283850c13`) in `scripts/tests/test_derive_derived_evaluator_fixtures.py::SandboxReproductionTests` — RED on the buggy code (8-family, 2,110-row shrink detected), GREEN on the fix (0 violations, output byte-identical to committed). All 10 of 10 `derive_*_fixtures.py` generators independently re-verified non-shrinking via a real once-run-vs-committed check (not a twice-run-diff — see S6). |
| D8 | **B6 spell coverage-test drift (dormant).** `derived_evaluator_fixture_check.rs` has zero references to `SPELL_BOOK_B6`/bestiary_6 while `spell_resolver.rs` defines `pub const SPELL_BOOK_B6` — the exact wave-19 "silent drop" shape recurring. Zero live production impact today (nothing currently depends on the missing check), but the coverage test built to prevent this class of bug hardcodes its own comparison list instead of deriving from `spell_resolver::SPELL_BOOK_*`, so it cannot catch the NEXT occurrence either. | wave 28 (both adversarial reviews, independently) | 0 live units affected today; the defect is in the guard itself, not in banked content | OPEN |
| D9 | **`gen_book_cache.rs`'s `gen_advanced_race_guide()`/`gen_companion_book()`/`gen_pathfinder_unchained()`, and the separate binary `gen_core_rulebook_cache.rs`, unconditionally wipe-and-regenerate their owned subdirectories with no per-record exists-guard — the S6 self-erasure shape, generalized beyond the Python fixture generators to the Rust corpus binaries.** `gen_monster_book`, in the SAME FILE as the other three, was already fixed for this exact shape (`SD31-E6-F9-005`, an `out_path.exists()`-then-skip guard); the fix was never extended to its own siblings. **Live-reproduced for `gen_advanced_race_guide`** (isolated worktree, real committed corpus, cleanly reverted — `git status` and `docs/work-inventory.json` md5 confirmed unchanged after): one run wiped `raw_tokens` from all 93 `advanced_race_guide` spell records (100% of the book's spell population — a kind, `spell`, D1 never named) and permanently deleted 15 real, populated equipment records (e.g. a shipped "Cruel" weapon special ability with real description/`raw_tokens`) belonging to `gen_equipment_gap_tables.rs`/`cache_gen::equipment_gap`'s own write into the same directory — not renamed, not recoverable by re-running `enrich_equipment_raw_tokens` (that tool enriches existing files, it cannot recreate deleted ones). `gen_companion_book` (927 of 927 companion records, 100% of the whole kind, across 16 `CompanionBookSpec` books) and `gen_core_rulebook_cache` (664 of 664 core_rulebook spell records; already named in D1 for its equipment-citation aspect only) are confirmed vulnerable by the identical code-read method — `grep -n "out_path.exists()"` returns zero hits for either function/file — but were NOT live-tested, deliberately, given their size (companion is the single largest at-risk population found this wave; core_rulebook is the corpus's largest, most central book). **Added on adversarial review (wave 30 reviewer 8, CONFIRMED): a 4th unguarded function was missed by the original lane, which had classified it SAFE on directory-disjointness grounds alone without checking the self-erasure axis.** `gen_pathfinder_unchained()` (line 616) has the identical zero-exists-guard shape; all 42 of 42 `data/corpus/pathfinder_unchained/equipment` records carry `raw_tokens` (written by `enrich_equipment_raw_tokens.rs`) that a run would strip, since `gen_book_cache.rs` never writes `raw_tokens` anywhere in the file (3 mentions, all comments). Not live-tested (budget), confirmed by code-read the same way `gen_companion_book`/`gen_core_rulebook_cache` were. | wave 30 (lane 4, sweep residual S6; 4th function added wave 30 reviewer 8) | 93 spell + 15 equipment confirmed live (`advanced_race_guide`); up to 927 companion + 664 core_rulebook spell + 42 pathfinder_unchained equipment by code-read (not yet proven live) | OPEN — not yet root-caused into a fix, though the fix shape is already known and shipped for `gen_monster_book` in the same file: extend `out_path.exists()`-then-skip to the 3 remaining unguarded functions in `gen_book_cache.rs` (`gen_advanced_race_guide`, `gen_companion_book`, `gen_pathfinder_unchained`) plus the separate `gen_core_rulebook_cache.rs` binary. Full evidence and every reproducible command: `artifacts/VISIBILITY-sweeps-2.md` §1 (as corrected by wave-30 reviewer 8, see `sweeps.md` S6). |
| D10 | **"Weapon and Armor Proficiency" reversed-key false positive inside class_feature's G1 option-pool population.** 13 units of the `class_feature_option_pool_record_not_held_by_engine` population match a pool-membership heuristic keyed on a reversed/substring form of the group name, misclassifying a non-pool record as a pool member. | wave 30 (lane 3), reproduced by wave-30 reviewer 7 | 13 units within the 3,064-unit G1 population (`docs/release/SD-31-corpus-closure-grind/artifacts/w30-coverage-table.json` group `W30-CF-G1`) | OPEN — named and counted this wave, not root-caused into a code fix (measurement-only wave). |
| D11 | **CLOSED, wave 30 — G3 formula-readability regex-proxy correction, superseded by a real parser run.** Wave 28's `THE-BOX.md` filed G3's formula-readability split (554 refused/882 readable/1,690 unclassified) from a regex proxy over `type_facet` text. Wave 30 lane 3 built a temporary probe calling the REAL `formula_interpreter::{extract_formula_field,recognises_shape}` directly against every unit's actual `BONUS`/`DEFINE` `raw_tokens`. | wave 28 (proxy filed), wave 30 lane 3 (real parser run) | 3,320 units (the corrected G3 population, `evidence == "no_explanation_id_and_no_diagnostic_names_this_feature"`) | **CLOSED — the wrong (proxy) number is fully replaced with a verified one, not because underlying production code was fixed.** Lane 3's run: 2,286 readable / 3 refused / 864 no-formula-token / 167 no-corpus-record. **Independently rebuilt from scratch by wave-30 reviewer 8** (own probe, same public functions, own worktree): 2,287 readable / 3 refused / 2,290 has-formula-token — a 1-unit difference from lane 3's 2,286/2,289, attributed to an undisclosed cross-book join fallback in one of the two probes, not investigated further (both numbers are real parser runs, not proxies; the practical conclusion — the vast majority of G3 IS formula-readable, only 3 records genuinely refuse — holds under both). |
| D12 | **`granted_via_archetype` (`data/class_feature_grants`) is single-hop only and cannot see a multi-hop archetype chain.** An archetype row that grants a sub-row which itself grants a base-class-named feature is invisible to the field's own CATEGORY-text derivation. | wave 30 (lane 3), end-to-end reproduced by wave-30 reviewer 8 | 4 preclass-gated/`false`/key-group-equals-class facts exist corpus-wide (Cleric ~ Channel Energy, Gunslinger ~ Gun Training, Druid ~ Wild Shape, Paladin ~ Smite Evil); 3 of the 4 disagree in level with their base-class fact (3 vs 1, 6 vs 4, 2 vs 1) and are today protected only by that incidental level mismatch, not by structure; Gunslinger has no competing fact at all | OPEN — not yet root-caused into a fix. 0 live fabrication today (checked all 4 instances individually against the pinned oracle), but the protection is not structural — a future corpus edit that removed the incidental level mismatch would silently fabricate. |
| D13 | **`wiring_class`-vs-`status` classifier blind spot: 12 CRB flag-shaped `class_feature` units are `display`+`grounded` and never re-examined once stamped `held`.** Evasion, Improved Evasion, Timeless Body, Woodland Stride, Quarry, Improved Uncanny Dodge and siblings are text-only flags whose full token closure the classifier's own doc comment (`pf1e_dashboard_producer.py`) names as missing, not built. | wave 30 (lane 2, the 141 never-examined units — H2) | 12 `class_feature` units (`wiring_class=='display' and status=='grounded'`, corroborated by `evidence == "monster_ability_held_and_corpus_record_carries_real_description"`-shaped siblings elsewhere in the corpus, not yet swept) | OPEN — new finding, not previously named anywhere in `todo/` before wave 30. Needs the "wiring-class classifier that checks the full token closure GE-01 defines" `pf1e_dashboard_producer.py`'s own doc comment names as missing. |
| D14 | **(new, wave 31) `extract_formula_field` mis-extracts the 3-field `<SUBTAG>\|<formula>\|TYPE=<X>` token shape.** e.g. `BONUS:COMBAT\|1\|TYPE=NaturalArmor` extracts the literal text `TYPE=NaturalArmor` as if it were the formula, instead of `1`. Safe today only because the mis-extracted text happens to fail to parse as a formula, so it refuses rather than silently computing garbage — but the shape is real and the safety is incidental, not structural. | wave 31 (compute-shape clustering lane, "other-named-variable-expression" family) | 4 tokens corpus-wide, confirmed exact | OPEN — new finding, not root-caused into a fix (measurement-only wave) |
