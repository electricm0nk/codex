# SD-31 Forward-Scope Register

Successor work depending on this package's output. Format follows
`SD-30-class-feature-archetype-bundle/forward-scope-register.md`'s convention (one row per item, named
owner, no unowned tidiness entries).

| ID | Item | Owner |
|---|---|---|
| G1.1 | ~~The ~2,894-unit chassis-blocked `race_trait` remainder and `race`'s 103 units stay outside this package's ceiling until a sibling package builds the chassis.~~ **RETIRED 2026-08-15 (`decisions.md §2`): this is no longer forward scope.** The race chassis is Epic 1 of this package and gates Epic 6-F3/F4 internally. Nothing to hand forward. | — (in-package, Epic 1) |
| G1.2 | ~~Any `class_feature` `unknown`-bucket unit characterized as "genuinely unreachable" is a named finding routed to a sibling package's verdict-path epic.~~ **RETIRED 2026-08-15 (`decisions.md §2`): no longer forward scope.** The verdict-path build is Epic 2 of this package and runs *before* the characterization (Epic 3-F4) and disposal (Epic 5-F3) seeds that consume it. A unit still unreachable after Epic 2 goes to the Structural Exclusion Register with operator sign-off (`decisions.md §3`), not to a successor. | — (in-package, Epic 2) |
| G1.3 | ~~`equipment`/`equipment_modifier`/`companion`/`feat`/`monster_ability` ingest lanes — real `not-started` residue exists (per `SD-30-.../decisions.md §44`'s own "not in this epic" note) but was not prioritized into a card by SD-30's original Epic 10 fold, and this package inherited that same scoping. A future card can open under this package's Epic 6 without a new operator ruling. **These kinds are in the 100 % denominator even though no card claims them — Epic 0's audit surfaces them and Epic 9 cannot close over them silently** (`risks-and-open-questions.md` open question 1).~~ **CARDS OPENED 2026-08-15 (launch-readiness remediation Step 2, blocker B2, operator ruling "open cards for the six unowned kinds"): see Epic 6 F5-F10** (`epic-breakdown.md`) — `equipment` (F5), `equipment_modifier` (F6), `companion` (F7), `feat` (F8), `monster_ability` (F9), and a **sixth kind, `class` (F10, 158 not-done units)**, named nowhere in this package before this correction. Also opened: Epic 6-F11 (held static/derived residual, 5,273 units) and Epic 5-F4 (the 36 `deferred-with-reason` units). No longer an unclaimed forward-scope item. | Epic 6 F5-F11, Epic 5-F4 (in-package, see `epic-breakdown.md`) |
| G1.4 | The per-class PI-blacklist sweep this package's Epic 5 (`class_feature` chassis sweep, ex-SD-31 Epic 3, ex-SD-30 Epic 6) must call before writing any generated table: `codex::rules_core::pi_table_sweep::screen_generated_table` (shared `pi_screening::PI_BLACKLIST_TERMS`), already built, already production-wired (two live callers, `gen_feat_gap_tables.rs`/`gen_equipment_gap_tables.rs`), proven against real `class_feature` content by `SD30-E3-F1-001`. Six-step invocation contract: `SD-30-.../decisions.md §52.3`; do not re-derive or fork the term list. | This package's Epic 5 (chassis-sweep ingest binary) |
| G1.5 | The declared-PI reader (`pi_screening::{declared_product_identity, classify_optional_field_declared}`) this package's Epic 5 must call, for the 6 books `SD-30-.../decisions.md §39.2` measured real `NAMEISPI:YES`/`DESCISPI:YES` exposure in (`adventurers_guide` 276, `inner_sea_magic` 67, `inner_sea_world_guide` 49, `inner_sea_intrigue` 45, `book_of_the_damned_volume_2` 18, `inner_sea_combat` 9 source rows) — already wired into this repo's one existing `class_feature` production writer (`src/bin/ingest_pu_classes.rs`) by `SD30-E3-F2-001`, proven against real-shaped rows (its own book carries zero live declared-PI tokens today). A sibling check to G1.4's blacklist sweep, never a substitute. Six-step invocation contract: `SD-30-.../decisions.md §53.5`. | This package's Epic 5 (chassis-sweep ingest binary) |
| G1.6 | The declared-PI reader, hand-applied at BOTH `scripts/transcribe_monster_tables.py` (drop `NAMEISPI:YES`, redact `DESCISPI:YES` on `MonsterAbilityRecord.description`/`description_variables`) and `scripts/transcribe_companion_tables.py` (same, plus `CompanionAbilityRecord.description_variants`, and a new `NAMEISPI:YES` creature-half drop that script had never had at all) — the Pipeline-B Rust-literal-table shape's counterpart to G1.5's JSON-record shape, by `SD30-E3-F3-001`. Dormant today: all 6 registered monster books and all 17 registered companion books carry zero declared-PI source tokens, re-confirmed, so this fires the first time `epic-6-ingest-lanes` F1 (or a future companion card under this register's own `G1.3`) onboards a book that does. Proven against synthetic rows replaying each script's real `transcribe()` function (15/15 checks). Contract: `SD-30-.../forward-scope-register.md` `C1.6`, restated `SD-30-.../progress.md` cycle `SD30-E3-F3-001` §8. | `epic-6-ingest-lanes` F1 (`monster`); this register's own `G1.3` (`companion`, unclaimed) |
| G1.7 | The `class_feature` PI regression gate, `tests/sd30_declared_product_identity_in_shipped_class_features.rs` (`SD30-E3-F4-001`) — a permanent `cargo test` suite, wired into `scripts/verify.sh`'s existing `root-full` stage via its own `expected_test_suites()` auto-discovery (no new stage, nothing to invoke). It reads shipped `data/corpus/*/class_feature/**.json` and fails `root-full` the moment any record ships a `NAMEISPI:YES`-declared name (must be dropped) or a `DESCISPI:YES`-declared description unredacted (must carry `pi_marker: "redacted"` / `description: REDACTED_PI_MARKER`). Live-proven: a `NAMEISPI:YES` row planted as a scratch copy in the real `pathfinder_unchained/class_feature/` tree turned the suite RED, removing it turned it GREEN again (`SD-30-.../progress.md`, cycle `SD30-E3-F4-001`). `epic-5-chassis-sweep`'s first cycle for any of the 22 not-yet-ingested `class_feature` books does not need to invoke anything new — this gate fires automatically inside the standing `verify.sh` run every cycle already performs; route any red result through `G1.5`'s declared-PI reader, not around this gate. Contract/detail: `SD-30-.../forward-scope-register.md` `C1.7`. | `epic-5-chassis-sweep` (passive; fires automatically, no card action needed unless it goes red) |
| G1.8 | Any race the Epic 1 chassis design (F1) finds cannot be chassis-built within that epic's bound (e.g. a genuinely bespoke racial subsystem) is a **named finding**, not silently dropped — and after `decisions.md §3`, not a unilateral deferral either: it is a proposal to the Structural Exclusion Register requiring operator sign-off. Absorbed from `SD-32-engine-capability-builds/forward-scope-register.md` `H1.3` (`decisions.md §2`). Its siblings `H1.1`/`H1.2` (the race-chassis and verdict-path handoffs to this package) are **retired, not absorbed** — they described cross-package handoffs that the merge made internal, and they are recorded here as `G1.1`/`G1.2` above. | Unassigned pending Epic 1-F1's actual finding — do not pre-assign a successor for a population not yet named |

No item above is left without a stated home, per this program's standing "unowned deferral is not a
valid disposition" discipline (`SD-30-.../decisions.md §27`).
