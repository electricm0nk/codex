# SD-32 Gate 1 — canonical shape-family vocabulary and reconciliation

Generated by `scripts/family_vocabulary_reconcile.py` — see that module's docstring for the full method. Card `family-vocabulary-reconciliation` (`decisions.md` §12a). Re-derive: `python3 scripts/family_vocabulary_reconcile.py --output-json artifacts/gate-1-shape-closure/family-vocabulary.json --output-md artifacts/gate-1-shape-closure/family-vocabulary.md`.

**Canonical vocabulary is `scripts/shape_ledger.py`'s `FAMILIES` list (F1-F10, priority order) plus its F0/F8 extension families — 34631 not-done units, unclassified_count 0.** This is the one place a family's id, name, rule, proof width, count, and re-derive command are defined; every other document (engine module docs, AT-32-* criteria, kanban card titles, `epic-breakdown.md`) cites this table rather than restating it.

## 0. Join-status split (decisions.md §14b — read this before the family table)

**The family rollup below (and F0 in particular) is not the honest coverage figure on its own.** F0 ("no formula content") conflates two different populations its own proof-width text distinguishes: a unit whose `(book, source_file, source_line)` join found **no corpus record at all** (`no_record`), and a unit whose join found a record that simply **carries no `DEFINE`/`BONUS` token** (`no_formula_tokens`). Only `matched` rests on a real corpus record. Absence of a record is absence of evidence, not evidence the unit has no formula — do not read `unclassified_count: 0` as "100% of this population is understood".

| join_status | count | share | meaning |
|---|---:|---:|---|
| `matched` | 11426 | 33.0% | rests on a real corpus record |
| `no_formula_tokens` | 23073 | 66.6% | record found, carries no DEFINE/BONUS token |
| `no_record` | 132 | 0.4% | join found no corpus record at all — this is Gate 3's `no_record` closure-budget invariant |

Re-derive: `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --output /tmp/l.json && python3 -c "import json,collections; r=json.load(open('/tmp/l.json'))['rows']; print(collections.Counter(x['join_status'] for x in r))"`.

## 1. Canonical family table

| id | label | count | proof width (truncated) |
|---|---|---:|---|
| F9 | Skill-rank-derived (skillinfo/TOTALRANK) | 57 | Covers any formula segment containing the `skillinfo(` call or a bare `TOTALRANK` reference. Does not cover skill-rank use inside a `.MOD`-c… |
| F6 | classlevel(...)-derived | 391 | Covers any formula segment calling `classlevel(`. Known corpus defect (MEASURE-TWICE.md §3, `defects.md` D2): `classlevel()` does not verify… |
| F10 | Level-threshold step-count (summed >= indicators) | 4 | Narrow heuristic: >=2 `if(` calls AND >=2 numeric `>=` comparisons AND a top-level `+` in the same formula segment. Does not cover a step-co… |
| F5 | Clamped/capped per-level scaling (min/max/floor/ceil around a level expr) | 586 | Covers `min(`/`max(`/`floor(`/`ceil(` wrapping a formula segment that also contains a `<Word>LVL` term. Does not cover a clamp wrapping a na… |
| F2 | Per-level scaling (<Class>LVL bare or arithmetic) | 2337 | Covers any formula segment containing a `<Word>LVL` token, bare or inside arithmetic. Does not distinguish a modelled class's own LVL variab… |
| F7 | Conditional-step (if/boolean toggle) | 12 | Covers any formula segment containing an `if(` call not already claimed by F9/F6/F10 above. Does not distinguish a genuine boolean toggle fr… |
| F3 | Ability-modifier-derived (STR/DEX/CON/INT/WIS/CHA) | 650 | Covers a formula segment that is a bare ability abbreviation (optionally signed/offset) or contains one as a token. Does not cover an abilit… |
| F4 | Named-counter/pool variable (plain identifier reference) | 1073 | Covers a formula segment that is exactly one bare identifier (no arithmetic, no literal) and is not an ability abbreviation -- the shape `bo… |
| F1 | Flat-constant magnitude (bare literal) | 6032 | Covers a formula segment that is exactly a signed/unsigned integer or decimal literal, optionally with a trailing `%`. Does not cover a lite… |
| F0 | No formula content (no DEFINE/BONUS token found for this unit) | 23289 | Covers a unit whose (book, source_file, source_line) join finds either no corpus record, or a corpus record whose data.raw_tokens carries no… |
| F8 | Other named-variable expression (residual) | 200 | Covers any non-empty formula segment that matched none of F1/F2/F3/F4/F5/F6/F7/F9/F10 above -- e.g. multi-term arithmetic mixing several ide… |

Re-derive command: `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --output artifacts/gate-1-shape-closure/ledger.json`.

## 2. Mapping to SD-31's `MEASURE-TWICE.md` §3 vocabulary

MT's ten families (`docs/release/SD-31-corpus-closure-grind/artifacts/MEASURE-TWICE.md §3`) partitioned only the 4948-unit formula-bearing subset of the 24,914 not-done units, using an unrecorded priority order ("a unit's PRIMARY family is assigned by priority order" — MT never states what that order was, and its own walk was explicitly not committed as a script, per MEASURE-TWICE.md §7). `shape_ledger.py`'s priority order (F9 > F6 > F10 > F5 > F2 > F7 > F3 > F4 > F1) is the first codification, stated and auditable in `FAMILIES`' own list order.

**Ruling: `shape_ledger.py`'s vocabulary and counts are canonical.** It is the only one of the two that (a) is committed and re-runnable rather than a one-time hand-walk, (b) covers the full 24,914-unit not-done population rather than only the 4,948-unit formula-bearing subset (F0 below is the finding this produces), and (c) states an auditable priority order. Where a per-family count differs (F2, F3, F7 most visibly), the difference is not a defect in either count on its own — MT's counts were correctly reproduced against MT's own (unrecorded) priority order and are retained here as a historical cross-check; `shape_ledger.py`'s counts are the ones every AT-32-* criterion and engine-coverage claim must cite going forward.

| id | canonical count | MT count | delta | canonical label | MT label |
|---|---:|---:|---:|---|---|
| F9 | 57 | 17 | 40 | Skill-rank-derived (skillinfo/TOTALRANK) | Skill-rank-derived (skillinfo TOTALRANK) |
| F6 | 391 | 211 | 180 | classlevel(...)-derived | classlevel(...)-derived |
| F10 | 4 | 7 | -3 | Level-threshold step-count (summed >= indicators) | Level-threshold step-count (sum of level>=N indicator terms) |
| F5 | 586 | 368 | 218 | Clamped/capped per-level scaling (min/max/floor/ceil around a level expr) | Clamped/capped per-level scaling (min/max/floor wrapping a level expr) |
| F2 | 2337 | 1140 | 1197 | Per-level scaling (<Class>LVL bare or arithmetic) | Per-level scaling (<Class>LVL bare/arithmetic) |
| F7 | 12 | 54 | -42 | Conditional-step (if/boolean toggle) | Conditional-step (if/boolean toggle) |
| F3 | 650 | 804 | -154 | Ability-modifier-derived (STR/DEX/CON/INT/WIS/CHA) | Ability-modifier-derived (STR/DEX/CON/INT/WIS/CHA in formula) |
| F4 | 1073 | 563 | 510 | Named-counter/pool variable (plain identifier reference) | Named-counter/pool variable (e.g. a class's per-day-uses tracker) |
| F1 | 6032 | 1747 | 4285 | Flat-constant magnitude (bare literal) | Flat-constant magnitude (bare literal, e.g. +2 damage) |
| F0 | 23289 | — | — | No formula content (no DEFINE/BONUS token found for this unit) | *(no MT counterpart — see note below)* |
| F8 | 200 | 37 | 163 | Other named-variable expression (residual) | Other named-variable expression (residual) |

**F0:** No MT counterpart -- MT's ten families only ever partitioned the 4,948-unit formula-bearing subset; a unit with no DEFINE/BONUS token was never assigned to any MT family at all. F0 is the honest naming of that gap, not a new distinction MT lacked.

## 3. Engine-coverage reconciliation (card 7)

Card 7 (`gate-2-engines-f10-binding`, since retitled per this card's own propagation step) cited **"77.2% of custom identifiers (893/1,156)"** as the reach of `bonus_stack_reader.rs`'s generalised producer-chain mechanism, attributing that figure to "F10" — which the canonical table above shows is wrong: **F10 is a 3-unit level-threshold step-count family, unrelated to the binding layer.** F4's own proof-width text (canonical table above) names F4 — "named-counter/pool variable" — as "the shape `bonus_stack_reader.rs`'s binding-layer pattern targets".

Independently re-derived (corpus-wide, `_walk_corpus_tokens` + canonical F4 predicate from `shape_ledger.classify_formula`, not a citation of the 007/008 cycle receipts' own numbers): **614 of 689 (89.1%) distinct F4-shaped bare-identifier strings are resolvable** via `bonus_stack_reader.rs`'s producer-chain mechanism (the identifier is the TARGET of a `DEFINE` or `BONUS:VAR` write somewhere else in the corpus).

This population (689 distinct identifiers) is close to but not identical to MT §3.1's cited 1,156 and card 7/8's cited 1,156/4,736 — the three are different denominators over related but distinct populations: MT's 1,156 was a hand-walk (not re-run here byte-for-byte); the corpus-wide `bonus_stack_reader` run (`artifacts/gate-2-engines/bonus_stack_reader.corpus-wide.json`) scanned **4,736** distinct BONUS:VAR TARGET variables (a broader population — every write target, not only F4-shaped bare-identifier VALUE references); this script's **689** is the population of distinct identifier STRINGS that appear as the VALUE of an F4-shaped formula segment, i.e. the canonical-F4-scoped reading of "custom identifier". All three are legitimate, differently-scoped counts; none of them may be quoted as a bare total without naming which of the three it is (`decisions.md` §12c).

First 10 unresolved F4-shaped identifiers (no producer found anywhere in corpus — either a genuinely one-off value, or the join missed a producer this predicate doesn't see): `AdaptiveWarriorEnhancementCount`, `AegisPrimeStat`, `ArcanistEnergyExploitDC`, `ArcanistLvl`, `BloodragerBloodlineFeatCount`, `BodySnatcherEnhancementCount`, `BoonCompanionBonus_AnimalCompanion`, `CHASCORE`, `CL`, `CONSCORE`

