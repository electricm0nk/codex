# Cycle reSaveCharacter-cycle — Epic 7 (Tauri Command-Surface Repair) / Criterion 7.3

- **Card ID:** `t_PENDING` (placeholder — backfilled in a follow-up commit once the real `hermes kanban` card ID is known, per `loop-instruction.md §3`)
- **Commit SHA:** `PENDING` (backfilled once committed)
- **Files touched:**
  - `apps/desktop/src-tauri/src/characterHub/reSaveCharacter.rs` (new)
  - `apps/desktop/src-tauri/src/characterHub/mod.rs` (1-line addition: `pub mod reSaveCharacter;` + doc-comment update)
  - `apps/desktop/src-tauri/src/main.rs` (2-line addition: import + `invoke_handler!` registration for `re_save_character`)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_7/reSaveCharacter_cycle_receipt.md` (this file)
  - `docs/release/SD-24-beta-readiness-and-multiclass/progress.md` (Status matrix, TODO→DONE, DISCOVERED, Cycle log)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (before and after; no self-heal needed)
- **Wired-integration audit result:** 1 hit, unchanged before/after this cycle's own diff — the pre-existing, already-documented `Plant Growth` spell-description "hack" false positive (real PF1 SRD text: "creatures must hack or force a way through"), located in `src/rules_core/rules_tables/crb/spell_list.rs` (an Epic 6 file this cycle never touches). Confirmed by `grep`-locating the exact diff hunk before writing this receipt — the hit is not in any file this cycle's granted scope covers (`reSaveCharacter.rs`/`mod.rs`/`main.rs` contribute 0 new hits). Matches the identical finding `appendToCharacter_cycle_receipt.md` (criterion 7.1) already re-confirmed unchanged.
- **Acceptance criterion (verbatim, `epic-breakdown.md` Criterion 7.3):** "`reSaveCharacter` Tauri command. Files touched: `apps/desktop/src-tauri/src/characterHub/reSaveCharacter.rs`. RED: re-saving an existing character at the same path fails (write conflict). GREEN: re-saving preserves revision_id (`{id}.rev.N` counter increments; duracon 2026-07-18 18:20:41 hardcoded the hardcoded `.rev.1` is replaced)."
- **Status:** complete
- **Notes:**
  - **Plan-vs-reality correction #1 (test-file path, `## DISCOVERED` already forwarded by 7.1, applied here):** `content-unit-inventory.md` §4.1 and `acceptance-and-verification.md` row 37 both name a standalone integration-test target (`tests/sd24_characterhub_resave.rs`, run via `cargo test --locked --test sd24_characterhub_resave`). As criterion 7.1's own cycle already found and forwarded for 7.2/7.3 specifically, no `apps/desktop/src-tauri/tests/` directory exists anywhere in this crate — every Tauri command here (`character_hub.rs`, `equipment_catalog.rs`, `spell_catalog.rs`, `characterHub/appendToCharacter.rs`) is tested via an inline `#[cfg(test)] mod tests` in its own file. Followed that same established convention: 3 tests live in `characterHub/reSaveCharacter.rs`'s own `#[cfg(test)] mod tests`. No new `## DISCOVERED` entry needed for this correction — it re-confirms the one 7.1 already forwarded.
  - **Plan-vs-reality correction #2 (request-contract ambiguity, new `## DISCOVERED` this cycle):** `technical-design.md` §3.2 sketches this command's request shape as `{ characterId, character: CharacterSnapshot }`, implying the caller pushes an entire re-edited character payload. No `CharacterSnapshot` wire type exists in this crate — the closest namesake, `PilotSnapshotDto`, is the *derived* stat snapshot (BAB/saves/AC/skills) already returned by `create_character`/`load_saved_character`/etc., not an editable `CharacterInput` mirror. `codex::rules_core::character_input::CharacterInput` itself derives no `serde` impls at all (the `codex` crate is intentionally dependency-free). Building a full serde-mirrored `CharacterInput` DTO to accept arbitrary client-authored edits is materially larger than this cycle's granted file-touch scope (`reSaveCharacter.rs` + one `main.rs` line) and duplicates work criteria 7.1/7.2 already do more narrowly (batch equipment append; recompute). Implemented the load-bearing part of the acceptance text instead: `ReSaveCharacterRequest { characterId, expectedRevisionId, savedAt }` — the revision-conflict guard and the `{id}.rev.N` counter replacing the hardcoded `.rev.1`, operating on the character already persisted at `characterId`. Full documented in the source file's own module doc comment. See `## DISCOVERED` below for the forward.
  - **Reuse over duplication:** reuses `character_hub::resolve_character_root` (already `pub(crate)` since criterion 7.1) for the Tauri-command wrapper; does **not** reuse `mutate_saved_character_at_root` since this command performs no `CharacterInput` mutation and no recompute (there is nothing new to compute — the persisted build was already `Computed` when it was last saved) — it only manages the envelope's revision metadata, so calling straight through `SavedCharacterStore::load`/`save` is the correct, non-duplicative seam.
  - **Failure-mode scope:** implemented exactly the `revision_conflict` failure mode `technical-design.md` §3.3 names for this command ("returns `{ success: false, error: "revision_conflict" }` if the local revision_id doesn't match the canonical revision_id"). The missing-character case (`SavedCharacterStore::load` failing) surfaces as a real propagated `Err`, matching `appendToCharacter`'s own precedent for an error case not named in this criterion's own explicit failure-mode spec.
- **RED → GREEN evidence:**
  - RED: `characterHub/reSaveCharacter.rs`'s test module (3 tests) was written calling `re_save_character_at_root`. To reproduce a genuine RED live (rather than merely asserting one), the implementation function was temporarily renamed to `re_save_character_at_root_not_yet_implemented` (mirroring the `git stash`-based RED-reproduction precedent Epic 4's `apg-class-audit`/`acg-class-audit` cycles already used) and `cargo test --bin codex-desktop re_save_character` was run: **5 compile errors**, all `error[E0425]: cannot find function 're_save_character_at_root' in this scope` (3 call sites in the test module + 2 in the doc-comment code the compiler still parsed as call-site references) — confirmed failing for the intended reason (missing implementation, not a typo/setup bug).
  - GREEN: restored `re_save_character_at_root` (envelope load → revision-id equality check → `{id}.rev.N` bump via `next_revision_id` → re-save) + the `#[tauri::command] re_save_character` wrapper + `main.rs`/`mod.rs` registration. `cargo test --bin codex-desktop re_save_character` → `test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 117 filtered out`.
  - Full crate suite: `cargo test --locked` (`apps/desktop/src-tauri/`) → `test result: ok. 120 passed; 0 failed` (117 pre-existing + 3 new, 0 regressions).
- **Discovery forwards:**
  - `2026-07-21T05:00:00Z | epic-7 | 7.3 | epic-7-request-contract-correction | technical-design.md §3.2 sketches reSaveCharacter's request as { characterId, character: CharacterSnapshot }, implying a full re-edited character payload in the request body. No CharacterSnapshot wire type exists in this crate; the closest namesake, PilotSnapshotDto, is the derived-stats snapshot already returned by create_character/load_saved_character, not an editable CharacterInput mirror, and CharacterInput itself derives no serde impls (codex crate is intentionally dependency-free). Implemented the request as { characterId, expectedRevisionId, savedAt } instead — the revision-conflict guard + {id}.rev.N counter the criterion's own RED/GREEN text actually describes — operating on the character already persisted at characterId, reusing appendToCharacter/add_spell_selection/level_up_character for content edits. | suggested: if an operator wants a genuine client-side "edit everything, then push" re-save workflow, that needs its own design + a full serde-mirrored CharacterInput DTO surface — scope it as a dedicated follow-on cycle/criterion, not folded into 7.3's file-touch budget.
  - `2026-07-21T05:05:00Z | epic-7 | 7.3 | epic-7-scope-note | mutate_saved_character_at_root (character_hub.rs) and every command routed through it (level_up_character, add_equipment_selection, add_spell_selection, appendToCharacter) preserve whatever revision_id was already on disk across a mutation — none of them advance it either, so technical-design.md §3.1's "hardcoded .rev.1" framing is really two separate gaps: (a) creation-time paths (create_character, clone_character, seed_default_character_if_needed, import_character) hardcode .rev.1 at construction, and (b) mutation-time paths never advance revision_id at all. This cycle's reSaveCharacter closes the new-command surface the acceptance text asks for (an explicit re-save with a real, incrementing counter); it does not retrofit revision-advancing behavior into character_hub.rs's own mutation commands, since that file is outside this cycle's granted file-touch scope. | suggested: an operator-scoped follow-on could fold revision-advancing directly into mutate_saved_character_at_root itself so every mutation command advances revision_id, not just an explicit reSaveCharacter call — out of this cycle's scope to decide unilaterally.
- **Next-cycle plan:** Criterion 7.3 is now `complete`. Per Epic 7's deterministic order, criterion 7.4 (Add Weapon/Armor/Spell onClick wired to real corpus data, `apps/desktop/src/characterHub/**/*.tsx`) is next — a frontend (TS/TSX) cycle, disjoint file-touch set from this cycle and from 7.1/7.2/7.3's `src-tauri` files, per `loop-instruction.md §2.4`'s file-touch partition table. 7.5 (`compose_character_input` loadout hardcoding) remains last in Epic 7's deterministic order.
