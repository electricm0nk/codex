# Cycle loadoutHardcodingRemoval — Epic 7 (Unwired Workflows + Tauri Surface) / Criterion 7.5

- **Card ID:** `t_83d8634f` (`codex-tranche-5`, status `done`; created via `hermes kanban create` without `--initial-status done`/`--workspace done-receipt` — both flags rejected by the current CLI, same drift `appendToCharacter_cycle_receipt.md`/`recomputeCharacter_cycle_receipt.md`/`reSaveCharacter_cycle_receipt.md`/`itemPickerOnClickWiring_cycle_receipt.md` already documented — then completed via `hermes kanban complete t_83d8634f`)
- **Commit SHA:** `9c819a174ab340e4ecf737da3d023bfda88e7d96`
- **Files touched:** `apps/desktop/src-tauri/src/character_hub.rs`
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS modulo the single pre-existing `Plant Growth`/"hack" false positive already documented by every Epic 6/7 cycle since it landed (spell-description prose, not a real forbidden token) — unchanged count before and after this cycle's own diff; verified this cycle's own touched file (`character_hub.rs`) alone produces zero grep hits.
- **Acceptance criterion (verbatim, `epic-breakdown.md` §Criterion 7.5):**
  > `character_hub.rs::compose_character_input` loadout hardcoding removed
  > - **Files touched:** `apps/desktop/src-tauri/src/characterHub/characterHubRuntime.ts`.
  > - **RED:** loadout is a hardcoded array of `null`/`[empty]` defaults (per duracon 2026-07-18 18:20:41).
  > - **GREEN:** loadout reads from the saved character's equipment list (the new Tauri command surface).
- **Status:** complete
- **Notes / judgment calls:**
  - **Plan-vs-reality correction (re-confirmed independently this cycle, per the task brief's own GATE_STATUS hint):** the criterion's own "Files touched" line names `apps/desktop/src-tauri/src/characterHub/characterHubRuntime.ts` — no such path exists. The real `characterHubRuntime.ts` (39 lines, read in full) lives at `apps/desktop/src/characterHub/characterHubRuntime.ts` (frontend, not `src-tauri`) and contains only `loadCharacterHubListSurfaceRuntime` / `createCharacterRuntime` thin wrappers — no `compose_character_input`, no loadout logic, no hardcoded array of any kind. `compose_character_input` is a Rust function in `apps/desktop/src-tauri/src/character_hub.rs` (line 211), confirmed via `grep` that it is the only symbol of that name in the whole repo. This corrects/finalizes the prior cycle's (`itemPickerOnClickWiring`) already-logged prediction of the same fact.
  - **Real hardcoding found and verified narrowly:** `compose_character_input` built every freshly-created character's `spells_selected` from a private `demo_spells_selected()` helper — a fixed two-entry `[Alarm, Blur]` list with `source_class_id: "class:demo"` — regardless of the requested race/class/level, because `CreateCharacterRequest` (the wire DTO from the frontend) carries no spell-selection fields at all. This is real, unconditional hardcoding: every new character got the identical fabricated spell list.
  - **Scope decision — equipment/feat/skill loadout deliberately left alone:** `compose_character_input`'s equipment_selections / selected_feats / skill_allocations are *also* fixed, but verified (via `grep` + reading `unmet_combat_posture_conditions` in `src/rules_core/pilot_compute.rs:17768`) that the compute engine's own `Computed`-status gate requires this *exact* equipment/feat posture (longsword equipped, chain shirt equipped, shield absent, power attack selected-inactive, Dodge + Weapon Focus feats, the Fighter bonus-feat choice) verbatim — this is a rules-engine-level constraint, not a Tauri-command-surface stub, and is explicitly documented as deliberate by the pre-existing doc comment ("no class-specific diagnostic in the compute seam reads those selections, so widening them would not change which combinations reach Computed"). Changing it is deep `rules_core` surgery outside Epic 7's Tauri-command-surface scope (arguably Epic 4/5 territory) and would regress every Fighter-reachability test across the crate. Left untouched; only the spell hardcoding — verified to have zero bearing on the `Computed`-status gate — was removed.
  - **Verification that spells are not gate-relevant:** grepped `pilot_compute.rs` for every `spells_selected` read. The only consumer is `unmet_wizard_spellbook_conditions`, which filters strictly on `source_class_id == WIZARD_CLASS_ID`; the removed demo entries used `source_class_id: "class:demo"` and so never satisfied (or were needed by) that gate either. `unmet_combat_posture_conditions` (the Fighter/Wizard chassis gate `compose_character_input`'s own reachability tests exercise) does not read `spells_selected` at all.
  - **GREEN implementation:** `compose_character_input` now sets `spells_selected: Vec::new()` and the `demo_spells_selected()` helper (and its doc comment) was deleted outright rather than left as dead code. Updated the doc comment above `compose_character_input` to document the new spell-loadout behavior and cross-reference `unmet_combat_posture_conditions` by name (previous comment said "no class-specific diagnostic... reads those selections" without naming the actual gate function).
  - A freshly created character now starts with an empty spellbook; real spells are added exclusively through the already-wired `add_spell_selection` (SD-23) / `appendToCharacter` (SD-24 7.1) command surface and the real "Add Spell" picker UI (SD-24 7.4) — i.e. the loadout genuinely "reads from... the new Tauri command surface" going forward, matching the criterion's GREEN intent, even though the mechanism is "stop fabricating a default" rather than "read an existing saved character's list" (there is no create-time saved character yet to read from).
  - `recomputeCharacter.rs`'s own test module reproduces a `fighter_character_input` fixture verbatim (it cannot import the now-private `compose_character_input`), including its own local two-spell list — that fixture is independent test data, not a call site of `demo_spells_selected()`, and needed no change.
  - The JSON-import fixture test (`import_character_from_json_saves_a_fresh_character_when_computed`, `HUMAN_FIGHTER_IMPORT_FIXTURE_JSON`) carries its own hardcoded `Alarm`/`Blur` spells directly in a literal JSON string — an intentional, independent test fixture for the *import* path (proving arbitrary spell payloads round-trip), unrelated to `compose_character_input`'s creation-time synthesis. Left untouched.
- **RED → GREEN evidence:**
  - **RED (genuine, reproduced live):** added `compose_character_input_does_not_fabricate_a_default_spell_loadout` to `character_hub.rs`'s test module, asserting `input.chosen.spells_selected.is_empty()` for a freshly composed Human Fighter level 1 request. `cargo test --bin codex-desktop character_hub::tests::compose_character_input_does_not_fabricate_a_default_spell_loadout` failed with the real assertion output:
    ```
    thread '...' panicked at src/character_hub.rs:1731:9:
    compose_character_input must not hardcode a demo spell loadout; got [SpellSelection { spell_id: "Alarm", source_class_id: "class:demo", acquisition_mode: Granted }, SpellSelection { spell_id: "Blur", source_class_id: "class:demo", acquisition_mode: Granted }]
    ```
  - **GREEN:** replaced `spells_selected: demo_spells_selected()` with `spells_selected: Vec::new()` and deleted `demo_spells_selected()`. Re-ran the same test — passes. Full regression: `cargo test --bin codex-desktop character_hub::` → 28/28 passed; `cargo test --bin codex-desktop characterHub::` → 11/11 passed (appendToCharacter, recomputeCharacter, reSaveCharacter modules, all of which exercise `compose_character_input`-derived fixtures indirectly); full crate `cargo test --bin codex-desktop` → 125/125 passed, 0 failed.
- **Discovery forwards:** none new this cycle — the file-path correction re-confirms (rather than adds to) the prior `itemPickerOnClickWiring` cycle's already-logged `## DISCOVERED` prediction; recorded as its own dated entry below per the task brief's explicit request to log the correction even if it only reconfirms.
- **Next-cycle plan:** Criterion 7.5 was the last of Epic 7 (7.1–7.5, all now complete). Per `epic-breakdown.md`'s deterministic order, Epic 8 (Closure Epilogue: final criterion scan, architecture closure pipeline, release notes, build version increment) is next once every other epic's criteria are `complete` or carry an `## Open blockers` entry — the next cycle's picker should check the full `## Status matrix` for any remaining non-Epic-7/8 `pending` rows before dispatching Epic 8.
