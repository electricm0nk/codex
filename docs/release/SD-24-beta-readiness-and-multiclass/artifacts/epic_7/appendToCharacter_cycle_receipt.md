# Cycle appendToCharacter-cycle — Epic 7 (Tauri Command-Surface Repair) / Criterion 7.1

- **Card ID:** t_pending7f1a (placeholder — backfill with the real `codex-tranche-5` card ID per §2.3 step 10)
- **Commit SHA:** `ed6406f`
- **Files touched:**
  - `apps/desktop/src-tauri/src/characterHub/mod.rs` (new)
  - `apps/desktop/src-tauri/src/characterHub/appendToCharacter.rs` (new)
  - `apps/desktop/src-tauri/src/character_hub.rs` (2 visibility-only edits: `resolve_character_root` and `mutate_saved_character_at_root` bumped `fn` → `pub(crate) fn`, each with a doc-comment explaining why; no behavior change)
  - `apps/desktop/src-tauri/src/main.rs` (registered the `characterHub` submodule + the `append_to_character` command in `invoke_handler!`)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_7/appendToCharacter_cycle_receipt.md` (this file)
  - `docs/release/SD-24-beta-readiness-and-multiclass/progress.md` (Status matrix, TODO→DONE, DISCOVERED, Cycle log)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (before and after; no self-heal needed)
- **Wired-integration audit result:** 1 hit both before and after — the pre-existing, already-documented `Plant Growth` spell-description "hack" false positive from Epic 6's content-completion work (real SRD prose: "creatures must hack or force a way through"), unchanged by this cycle's diff. Not introduced by, or related to, this cycle. See "Notes" below for the precedent this matches.
- **Acceptance criterion (verbatim, `epic-breakdown.md` Criterion 7.1):** "`appendToCharacter` Tauri command. Files touched: new `apps/desktop/src-tauri/src/characterHub/appendToCharacter.rs`, plus the IPC adapter and call sites. RED: adding equipment to a saved character returns a `success: true` that lies (per the duracon 2026-07-18 18:20:41 sentinel). GREEN: appending equipment actually appends it; the saved character round-trips through load+append+recompute+re-save."
- **Status:** complete
- **Notes:**
  - **Plan-vs-reality correction #1 (test-file path, recorded as `## DISCOVERED`):** `content-unit-inventory.md` and `acceptance-and-verification.md` both name a standalone integration-test target (`tests/sd24_characterhub_append.rs`, run via `cargo test --locked --test sd24_characterhub_append`). No `apps/desktop/src-tauri/tests/` directory exists in this repo, and every other Tauri command in `character_hub.rs` (and `equipment_catalog.rs`, `spell_catalog.rs`, etc.) tests itself via an inline `#[cfg(test)] mod tests` in the same file — there is no precedent anywhere in `apps/desktop/src-tauri` for a `tests/*.rs` integration-test crate. Followed the established inline-test convention instead (tests live in `characterHub/appendToCharacter.rs`'s own `#[cfg(test)] mod tests`). This also avoids a real identifier-discipline hazard: a new file literally named `sd24_characterhub_append.rs` under the audited `apps/desktop/src-tauri/**/*.rs` pathspec would put the bundle tag `sd24_characterhub_append` in the diff's own file-header line, tripping the identifier audit's `sd24_` pattern on the very file the plan asked for (root-crate `tests/sd24_*.rs` files are exempt only because the audit pathspec is `src/**/*.rs`, not `tests/**/*.rs` — this crate's tests live under `apps/desktop/src-tauri/**/*.rs`, which *is* covered).
  - **Plan-vs-reality correction #2 (existing surface, recorded as `## DISCOVERED`):** `technical-design.md` §3.1 frames the entire iterative-mutation command surface as missing ("`create_character`/`list_saved_characters`/`load_saved_character` only compose-and-save once... No command exists to mutate/re-save an existing character"). That framing predates SD-23, which already landed `level_up_character`, `add_equipment_selection`, and `add_spell_selection` (all load → mutate → recompute → re-save, all wired) — confirmed via `git log -- character_hub.rs` (commits `e74f3fa`, `f203df8`). The real, still-open gap this cycle closes is narrower than the design doc implies: (a) no command appends a *batch* of items in one round trip, and (b) no command validates an `item_id` against the real CRB equipment corpus before mutating — `add_equipment_selection` will push an entry for any string, corpus-real or not, which is exactly the "success: true that lies" duracon sentinel this criterion's RED targets, just at the single-item granularity rather than character-creation. `appendToCharacter` closes both gaps for equipment.
  - **Reuse over duplication:** rather than re-deriving the load → mutate → recompute → re-save invariant a second time in the new module, `resolve_character_root` and `mutate_saved_character_at_root` (both previously private to `character_hub.rs`) were bumped to `pub(crate)` with a one-line doc-comment each explaining the new caller. This is the only touch to `character_hub.rs` — no behavior changed, no existing test's assertions changed, and the file's own `SAVED_CHARACTER_MUTATION_OPERATIONS` table (which documents `character_hub.rs`'s own three ops) was deliberately left untouched since `append_to_character` lives in its own module and is a distinct addition, not a fourth row in that table.
  - **`hack` false-positive precedent:** matches the exact shape Epic 6's `crb-field-completion-cycle` (equipment/spell content completion) already documented and self-healed — real PF1 SRD text using "hack" as an ordinary English verb, not a stub marker. No new occurrence was introduced by this cycle; the count is unchanged (1, both before and after).
  - **`characterHub` / `appendToCharacter` naming:** kept exactly as `technical-design.md` §3.2 and `content-unit-inventory.md` specify (camelCase module/file name, matching the wire-level command name), even though every other module in this crate is snake_case. Added `#[allow(non_snake_case)]` on both `mod` declarations (`main.rs`'s `mod characterHub;` and `characterHub/mod.rs`'s `pub mod appendToCharacter;`) to keep the build warning-free without renaming the plan's chosen identifiers.
  - **Failure-mode scope:** implemented the `equipment_not_found` failure mode exactly as `technical-design.md` §3.3 specifies (validated against the real CRB `equipment_tables()` corpus, all-or-nothing batch semantics — one unknown id rejects the whole batch, no partial append). Did not fabricate the `character_not_found` / `revision_conflict` error-tag strings `technical-design.md` §3.3 documents for `recomputeCharacter`/`reSaveCharacter` (criteria 7.2/7.3, out of this cycle's scope) — a nonexistent-character load failure surfaces the real `SavedCharacterStore::load` error message via `Result::Err`, matching every existing `character_hub.rs` command's own error-propagation convention (e.g. `add_equipment_selection`), rather than inventing a machine-parseable tag no other command in this crate uses.
- **RED → GREEN evidence:**
  - RED: `characterHub/appendToCharacter.rs`'s test module (4 tests) was written first, calling `append_to_character_at_root` before it existed. `cargo test --bin codex-desktop append_to_character` failed to compile with 4× `error[E0425]: cannot find function 'append_to_character_at_root' in this scope` (one per call site) — confirmed failing for the intended reason (missing implementation, not a typo/setup bug).
  - GREEN: implemented `append_to_character_at_root` (corpus-validate → `mutate_saved_character_at_root` → map response) + the `#[tauri::command] append_to_character` wrapper. `cargo test --bin codex-desktop append_to_character` → `test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured; 113 filtered out`.
  - Full crate suite: `cargo test --locked` (`apps/desktop/src-tauri/`) → `test result: ok. 117 passed; 0 failed` (113 pre-existing + 4 new, 0 regressions).
  - Root-workspace `cargo test --locked --tests` was attempted but hit an environment-level linker crash (`ld terminated with signal 7 [Bus error]`) on two unrelated pre-existing test binaries (`sd22_acg_class_warpriest_resolves`, `sd24_equipment_field_completion` — neither touches any file this cycle changed). Root cause confirmed via `df -h`: the shared build volume (`/`) was at 100% (`96G used / 2.0M avail`), almost certainly from concurrent `/loop /batch` worktrees' own `target/` directories competing for the same disk. This cycle's diff does not touch the root `codex` crate at all (only `apps/desktop/src-tauri/**`), so the crate-scoped `apps/desktop/src-tauri` suite above is the correct, complete regression check for this change; the root-workspace run is recorded here as an environment condition, not a code regression, and is not self-healable from this cycle's granted write scope (cleaning up other worktrees' disk usage is out of scope and risks clobbering concurrent cycles' in-flight builds).
- **Discovery forwards:**
  - `2026-07-21T02:00:00Z | epic-7 | 7.1 | epic-7-path-correction | content-unit-inventory.md and acceptance-and-verification.md both name a standalone tests/sd24_characterhub_append.rs integration-test target and a --test sd24_characterhub_append verification command. No apps/desktop/src-tauri/tests/ directory exists; every existing Tauri command in this crate (character_hub.rs, equipment_catalog.rs, spell_catalog.rs, ...) is tested via an inline #[cfg(test)] mod tests in the same file. A literal tests/sd24_characterhub_append.rs file would also put the sd24_ bundle tag in the diff's own file-header line under the audited apps/desktop/src-tauri/**/*.rs pathspec, tripping the identifier-discipline audit on the very file the plan names. This cycle used the inline-test convention instead (characterHub/appendToCharacter.rs's own #[cfg(test)] mod tests, 4 tests). | suggested: criteria 7.2/7.3's cycles (recomputeCharacter, reSaveCharacter) should do the same — inline tests in their own characterHub/*.rs file — and acceptance-and-verification.md's verification-command column should be corrected to cargo test --bin codex-desktop <fn_name_substring> in a follow-up documentation-only cycle.`
  - `2026-07-21T02:05:00Z | epic-7 | 7.1 | epic-7-scope-narrowing | technical-design.md §3.1 frames the entire load/mutate/recompute/re-save command surface as missing, but SD-23 (commits e74f3fa, f203df8) already landed level_up_character, add_equipment_selection, and add_spell_selection with exactly that shape, all wired. The real remaining gap for 7.1 was narrower: batch-append + real-corpus validation (add_equipment_selection accepts any item_id string, corpus-real or not). Confirmed via git log -- character_hub.rs before writing any code. | suggested: no action needed against 7.1 (closed this cycle); flag for whoever eventually revises technical-design.md that §3.1's "gap" framing is stale post-SD-23 and should be narrowed for accuracy in a documentation-only follow-up.`
  - `2026-07-21T02:10:00Z | epic-7 | 7.1 | epic-7-env-blocker | Root-workspace cargo test --locked --tests hit a linker Bus-error crash on two test binaries unrelated to this cycle's diff, root-caused to the shared build volume (/) being at 100% disk usage (df -h: 96G used / 2.0M avail), likely from concurrent /loop /batch worktrees' own target/ directories. Not self-healable from this cycle's file-touch scope (cleaning other worktrees' disk usage is out of scope and risks clobbering in-flight concurrent builds). This cycle's own crate-scoped suite (apps/desktop/src-tauri, 117/117) is unaffected and is the correct regression check for this diff. | suggested: operator-level infra action — prune stale worktree target/ directories or provision more disk before Epic 7's remaining cycles (7.2-7.5) run root-workspace-wide test suites; otherwise those cycles will hit the same environment condition and should record it the same way rather than treating it as a code regression.`
- **Next-cycle plan:** Criterion 7.1 is now `complete`. Criterion 7.2 (`recomputeCharacter`) is next in Epic 7's deterministic order — same `characterHub/` submodule, same reuse-via-`pub(crate)` pattern against `character_hub.rs`'s shared helpers, same inline-test convention. `recomputeCharacter` needs no new mutation (recompute-only against the as-saved `CharacterInput`), and per `technical-design.md` §3.3 must return `{ success: false, error: "character_not_found" }` for an invalid `characterId` — unlike this cycle's `appendToCharacter`, that literal tag is in this criterion's own explicit failure-mode spec (7.2's own RED text), so it should be implemented as a real, distinguishable error case rather than a raw propagated `SavedCharacterStore::load` message.
