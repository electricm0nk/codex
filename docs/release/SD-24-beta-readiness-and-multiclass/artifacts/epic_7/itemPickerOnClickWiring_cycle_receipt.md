# Cycle itemPickerOnClickWiring-cycle — Epic 7 / Criterion 7.4

- **Card ID:** `t_7dd7a1b9` (`codex-tranche-5`, `done`; created via `hermes kanban create` without `--initial-status done`/`--workspace done-receipt` — both flags rejected by the current CLI, same drift `appendToCharacter_cycle_receipt.md`/`recomputeCharacter_cycle_receipt.md`/`reSaveCharacter_cycle_receipt.md` already documented — then completed via `hermes kanban complete t_7dd7a1b9`)
- **Commit SHA:** `3ac287d169c5e44791643aaadbdb0ec8f8a4f007`
- **Files touched:**
  - `apps/desktop/src/characterHub/CharacterSheet.tsx` (extracted `buildItemPickerConfig` pure dispatch function; render body now calls it instead of an inline ternary chain)
  - `apps/desktop/src/characterHub/buildItemPickerConfig.test.ts` (new — 5 test functions / 9 assertions)
  - `docs/release/SD-24-beta-readiness-and-multiclass/progress.md` (status matrix, cycle log, DONE/TODO move, DISCOVERED entries)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (both baseline and final diff against `${BASE_BRANCH}` = `09e43c3`)
- **Wired-integration audit result:** the one pre-existing `Plant Growth`/"hack" false positive already documented by prior Epic 6/7 cycles (`src/rules_core/rules_tables/crb/spell_list.rs`) — unchanged count before and after this cycle's own diff; no new violation from this cycle's files.
- **Acceptance criterion (verbatim, `epic-breakdown.md` 7.4):** "Add Weapon / Add Armor / Add Spell onClick wired to real corpus data — Files touched: `apps/desktop/src/characterHub/**/*.tsx` for picker modal flows + onClick handlers. RED: clicking 'Add Weapon' does nothing (`onClick={() => {}}` per the wired-integration audit pattern). GREEN: the picker modal opens, queries real corpus data, and the chosen item appends to the character's loadout via the new Tauri command surface."
- **Status:** complete
- **Notes (judgment calls):**
  - **Plan-vs-reality correction (major):** the criterion's own RED text (`onClick={() => {}}`) does not describe this repo's current state. `git log --oneline -- CharacterSheet.tsx ItemPickerModal.tsx` shows the Add Weapon/Add Armor/Add Spell `onClick` handlers, the shared `ItemPickerModal`, and the real `listEquipment`/`listSpells` + `addEquipmentSelection`/`addSpellSelection` wiring were already landed by SD-23 (commit `885bbf9`, "feat(desktop): equipment/spell picker modal, closes out Epic 5 (SD-23)") — before SD-24 even launched. Manually walked the flow read-only: `WeaponsTab`'s "Add Weapon" button → `setItemPickerOpen('weapon')` → `ItemPickerModal` opens → `loadEntries` calls the real `listEquipment` boundary (Tauri `invoke('list_equipment', ...)`, not a mock) narrowed to the real `ArmsArmor` category → on select, `handleAddEquipment` calls the real `addEquipmentSelection` boundary (a genuine mutate/recompute/re-save round trip per criterion 7.1's own cycle-log finding that this command pre-dated SD-24) → `onDetailRefreshed` updates the sheet. Same shape for Add Armor (`GearTab`) and Add Spell (`SpellsTab`, `addSpellSelection`). This is not the stub the criterion's RED text names.
  - **Why this cycle is not a no-op despite the above:** the wiring existed with **zero test coverage** locking it in — the dispatch logic (which title / which corpus category / which loader / which mutate-handler per `itemPickerOpen` kind) lived inline inside `CharacterSheet`'s render body, unexported and untestable, so nothing in this repo's test suite would fail if a future edit silently regressed it back to a no-op `onClick={() => {}}`. This cycle did genuine TDD work: extracted the dispatch as a pure, exported `buildItemPickerConfig` function (same "pure logic split out of the component for unit-testability" pattern already used for `itemPickerFilter.ts` and `characterSheetRefresh.ts`, since this repo has no jsdom/testing-library to render `CharacterSheet` itself), wrote a failing test against the not-yet-exported function first (RED — `SyntaxError: does not provide an export named 'buildItemPickerConfig'`), then implemented the extraction (GREEN). The render body's runtime behavior is unchanged (byte-identical category/title/handler wiring), so this is a refactor-with-new-test-coverage, not a behavior change — appropriate given the behavior was already correct.
  - **Did NOT route the picker's mutation through the new `appendToCharacter` (7.1) command.** `technical-design.md` §3.2 names `appendToCharacter` as a *batch* append with corpus-id validation (`itemsToAppend: Vec<ItemRef>`), a materially different shape from the picker's single-item select-and-add. Criterion 7.1's own cycle-log entry already establishes that `add_equipment_selection`/`add_spell_selection` (the commands the picker already calls) are the pre-existing, correct single-item load→mutate→recompute→re-save commands SD-23 built — `appendToCharacter` is additive batch capability, not a replacement for these. Re-routing the picker onto `appendToCharacter` would be a scope-widening behavior change this cycle's acceptance text does not require (the picker already "appends to the character's loadout via [a] Tauri command surface" — just not the newest-added one), so left as-is; noted in `## DISCOVERED` for an operator call.
- **Discovery forwards:**
  1. `2026-07-21T (this cycle)` | epic-7 | criterion-7.4 | epic-7-plan-correction | Criterion 7.4's RED text (`onClick={() => {}}`) is stale — the Add Weapon/Add Armor/Add Spell wiring was already fully landed by SD-23 commit `885bbf9`, predating SD-24's launch. No follow-up needed; recorded for traceability so a future audit doesn't re-flag this as an open gap. | n/a (informational)
  2. `2026-07-21T (this cycle)` | epic-7 | criterion-7.4 | epic-3-candidate | `CharacterSheet.tsx`'s top-menu "Open" / "Save" / "Clone" items (`menuItems` array, near line 767) are genuine no-op handlers (`onSelect: () => {}`) — real accidental stub debt, just not inside this criterion's Add-Weapon/Add-Armor/Add-Spell scope. Did not remediate here (out of file-touch/criterion scope for 7.4; the dual-audit gate's token grep does not flag bare `() => {}` patterns, only literal STUB/MOCK/placeholder/etc. tokens, so it passed clean without catching this). | suggested: Epic 3 remediation backlog (empty-event-handler check) or a new Epic 7 follow-on criterion.
  3. `2026-07-21T (this cycle)` | epic-7 | criterion-7.4 | epic-7-scope-note | The picker's single-item mutation path deliberately still calls `addEquipmentSelection`/`addSpellSelection` (SD-23-era), not the newer 7.1 `appendToCharacter` batch command — see Notes above. If the operator wants every character-mutating UI affordance funneled through the new Epic 7 command surface for consistency, that is a deliberate follow-on decision, not a defect in this cycle. | suggested: operator decision, no epic assignment needed unless elected.
- **Next-cycle plan:** Criterion 7.5 (`character_hub.rs::compose_character_input` loadout hardcoding removed) is last in Epic 7's deterministic order. Per criterion 7.1's own prior-cycle finding, the real hardcoded-loadout reference is `demo_spells_selected()` in `apps/desktop/src-tauri/src/character_hub.rs`, not `characterHubRuntime.ts:compose_character_input` (no such function exists in the TS runtime file; `compose_character_input` is a backend Rust symbol) — 7.5's own cycle should re-verify current state and log its own `## DISCOVERED` correction if the plan and reality still disagree, per this bundle's established pattern.
