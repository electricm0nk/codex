# Cycle acg-class-audit — Epic 4 (Per-Class Coverage Audit + Plan) / Criterion 4.3

- **Card ID:** `t_3fd42108`
- **Commit SHA:** `2e074ce` (feat commit) + `1901230` (progress.md update)
- **Files touched:**
  - `src/rules_core/rules_tables/acg/mod.rs` (added `AcgClassId::ALL`/`name()`, `AcgClassCoverage`, `class_coverage`, `coverage_report`)
  - `tests/sd24_acg_class_coverage_audit.rs` (new)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/per-class-coverage-matrix.md` (ACG section, replacing the "pending" placeholder)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/acg_{arcanist,bloodrager,brawler,hunter,investigator,shaman,skald,slayer,swashbuckler,warpriest}_coverage.md` (new, 10 files)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/acg-class-audit_cycle_receipt.md` (this file)
  - `docs/release/SD-24-beta-readiness-and-multiclass/progress.md` (Status matrix, TODO→DONE, DISCOVERED, Cycle log)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (before and after)
- **Wired-integration audit result:** OK_NO_TOKENS (before and after — no self-heal needed this cycle)
- **Acceptance criterion (verbatim, epic-breakdown.md):** "Criterion 4.3 — Per-class audit: ACG classes (Alchemist-side, Arcanist, Bloodrager, Brawler, Hunter, Investigator, Shaman, Skald, Swashbuckler, Warpriest). Cycle artifact: extension of `./artifacts/epic_4/per-class-coverage-matrix.md`. Files touched: per-class `class_*.rs` files in `src/rules_core/rules_tables/acg/`."
- **Status:** complete
- **Notes:**
  - **Roster correction:** criterion 4.3's own header text lists "Alchemist-side" as one of the ten ACG classes and omits "Slayer." Both are stale, per `rules_tables::acg::mod.rs`'s own pre-existing SD-22 roster-correction doc comment: there is no real `CLASS:Alchemist` record anywhere in `acg_classes.lst` (confirmed again this cycle by direct grep — Alchemist is APG-only content, ACG never republishes a distinct Alchemist chassis), and Slayer *does* have a real record. This audit covers the real, corrected 10-class roster: Arcanist, Bloodrager, Brawler, Hunter, Investigator, Shaman, Skald, Slayer, Swashbuckler, Warpriest.
  - **Plan-vs-reality correction:** this cycle's dispatch brief named `src/rules_core/rules_tables/acg/class_tables.rs` as a target file. That path does not exist — ACG (like APG, unlike CRB) has one file per class (`class_arcanist.rs` ... `class_warpriest.rs`), with the shared book-level shape (`ClassTableRow`, `AcgClassId`, `class_chassis_resolve`) in `acg/mod.rs`, which is where this cycle's new coverage-audit code was added.
  - **Mid-cycle course correction (recorded here for transparency):** this cycle's first attempt built and landed real production wiring — ten new `src/rules_core/level_up/acg_<name>.rs` modules composing each class's chassis table into `compute_level_up_grants`, plus the dispatch registration in `level_up.rs` — reasoning (at the time) that the chassis pillar was real, non-fabricated, already-tested data that was simply unreachable. Before committing, a `git fetch` surfaced the sibling criterion 4.2 (APG)'s already-landed, already-merged cycle (`f25dc7b`/`f5f6f46`/`657daad`), which explicitly chose **not** to remediate the identical finding for APG, reasoning that `epic-breakdown.md`'s own Epic 4 purpose statement ("audit... output the coverage matrix") and `acceptance-and-verification.md` CG-03 scope 4.1–4.3 as an *audit*, not a remediation, and that "APG/ACG classes are not fully wired" is the exact condition `loop-instruction.md §4.2`'s hard-stop row already anticipates and pre-resolves via the 4.5 deferral — no blocker, no in-cycle fix expected. To keep the bundle's roll-up matrix internally consistent (an operator reading it should not see APG audited-only and ACG partially-remediated for the identical structural gap), this cycle **reverted** the production `level_up::acg_*` wiring and instead mirrored 4.2's exact audit-only shape (`AcgClassId::ALL`/`name()`, `AcgClassCoverage`, `class_coverage`, `coverage_report`, and the matching three-finding standing test), scoring ACG identically to how APG was scored. This is the "fix gaps TDD-style" instruction interpreted the same way 4.2's cycle interpreted it: the "gap" fixed here is the absence of an executable, regression-tested coverage report — not the absence of the narrative class features themselves, which remain an honest, correctly-diagnosed, deferred gap.
  - This cycle does not implement any of the ten classes' missing named features (Arcane Exploit, Bloodline, Studied Combat, Spirit, Raging Song, Panache/Deeds, Blessings, ...) — that would be a multi-week, high-fabrication-risk undertaking spanning ~250 combined feature records across ten classes, explicitly out of this criterion's scope (criterion 4.4 is the separate remediation-*plan* enumeration).
  - **Audit findings, RED → GREEN verified:**
    1. Chassis (BAB + saves): fully wired for all ten classes (20/20 levels), independently re-verified correct against the real PCGen corpus's `acg_classes.lst` `BONUS:COMBAT|BASEAB`/`BONUS:SAVE` tokens for every class (no defect found).
    2. Named class features: 0 wired for every class against a corpus-derived expected count per class (Arcanist 9, Bloodrager 19, Brawler 14, Hunter 21, Investigator 95, Shaman 10, Skald 20, Slayer 15, Swashbuckler 29, Warpriest 18 — methodology: distinct `KEY:<Class> ~ ...` records in `acg_abilities_class.lst`, PCGen corpus commit `7f818006e371188e5717fd18d74d18a420747fc6`, 2026-06-17, documented in `AcgClassCoverage`'s doc comment with the exact reproduction grep).
    3. `pilot_compute.rs`'s live `compute_class_chassis` dispatch does not recognize any ACG class — proven empirically by driving a real `CharacterInput` for each of the ten classes through `compute_pilot_base_chassis` and confirming the honest, claim-blocking `class_chassis.unsupported` diagnostic fires with `base_attack_bonus: 0`, never a fabricated number.
    4. No `level_up::<class>` module exists for any ACG class.
  - This audit's conclusion (ACG classes are chassis-only, zero live integration — the identical shape found for APG) is the precise condition `loop-instruction.md §4.2`'s hard-stop row already anticipates and pre-resolves. No blocker raised; criterion 4.5 formalizes the already-decided deferral, jointly for APG and ACG.
- **RED → GREEN evidence:**
  - RED: `tests/sd24_acg_class_coverage_audit.rs` did not compile before this cycle's `acg/mod.rs` additions (`AcgClassId::ALL`, `class_coverage`, `coverage_report` did not exist) — reproduced live via `git stash push -- src/rules_core/rules_tables/acg/mod.rs && cargo test --locked --test sd24_acg_class_coverage_audit`, which failed with 4 compile errors (`E0432` unresolved imports for `class_coverage`/`coverage_report`; `E0599` no `ALL`/`name` on `AcgClassId`), then `git stash pop` to restore.
  - GREEN: `cargo test --locked --test sd24_acg_class_coverage_audit` → 3 passed, 0 failed.
  - Full suite: `cargo test --locked --tests` (root) — 433 test binaries executed ("test result: ok" count), 0 failed (0 "FAILED" occurrences). Includes the 3 new `sd24_acg_class_coverage_audit` tests.
  - `cargo test --locked` (`apps/desktop/src-tauri/`) — re-run fresh against the final tree (this cycle touches no `apps/desktop` files, but the shared `codex` lib crate changed): 113 passed, 0 failed — unchanged.
  - Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`, clean before and after, no self-heal needed.
- **Discovery forwards:**
  - `2026-07-21T00:20:00Z | epic-4 | 4.3 | plan-vs-reality | task brief named a non-existent src/rules_core/rules_tables/acg/class_tables.rs; ACG's real shape is ten per-class files (class_arcanist.rs et al.) plus the shared acg/mod.rs, identical to APG's own already-documented correction | no new epic/criterion — documentation correction only, folded into this cycle's own artifacts`
  - `2026-07-21T00:20:00Z | epic-4 | 4.3 | roster-correction | criterion 4.3's header text lists "Alchemist-side" (not real ACG content) and omits "Slayer" (real ACG content) — same defect shape already resolved for APG's Gunslinger/Magus in SD-22 | suggested: correct epic-breakdown.md's criterion 4.3 header text in a documentation-only follow-up cycle`
  - `2026-07-21T00:20:00Z | epic-4 | 4.3 | remediation-scope | Named class features (Arcane Exploit, Bloodline, Studied Combat, Spirit, Raging Song, Panache/Deeds, Blessings, ...) have no grounded source anywhere in this repo for any ACG class; widening this requires a new pilot_compute.rs chassis-grounding slice per class | suggested: criterion 4.4's remediation-plan enumeration, one class at a time, future bundle(s) per criterion 4.5's deferral`
- **Next-cycle plan:** Criterion 4.3 is now `complete` in `progress.md`'s Status matrix, DONE section, and Cycle log. All of Epic 4's per-class audits (4.1 CRB Fighter+Wizard scope, 4.2 APG, 4.3 ACG) are now landed. Criterion 4.4 (remediation plan enumeration) and 4.5 (APG/ACG multiclass deferral decision) can now consume the full coverage-matrix output.
