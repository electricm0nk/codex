# Cycle apg-class-audit — Epic 4 (Per-Class Coverage Audit + Plan) / Criterion 4.2

- **Card ID:** t_placeholder1 (kanban card minted at step 10; will be backfilled here or in a follow-up commit once known)
- **Commit SHA:** (backfilled after this commit lands — see progress.md Cycle log entry for the hash)
- **Files touched:**
  - `src/rules_core/rules_tables/apg/mod.rs` (added `ApgClassId::ALL`/`name()`, `ApgClassCoverage`, `class_coverage`, `coverage_report`)
  - `tests/sd24_apg_class_coverage_audit.rs` (new)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/per-class-coverage-matrix.md` (new, APG section)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/apg_{alchemist,cavalier,inquisitor,oracle,summoner,witch}_coverage.md` (new, 6 files)
  - `docs/release/SD-24-beta-readiness-and-multiclass/artifacts/epic_4/apg-class-audit_cycle_receipt.md` (this file)
  - `docs/release/SD-24-beta-readiness-and-multiclass/progress.md` (Status matrix, TODO→DONE, DISCOVERED, Cycle log)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (before and after)
- **Wired-integration audit result:** OK_NO_TOKENS (after one self-heal: the doc comment originally used the word "placeholder" in a benign "never a hand-guessed placeholder" sentence in `src/rules_core/rules_tables/apg/mod.rs`; reworded to "never a hand-guessed or invented number" per `loop-instruction.md §4.1`'s single-token self-heal path, re-audited clean)
- **Acceptance criterion:** "Criterion 4.2 — Per-class audit: APG classes (Alchemist, Cavalier, Inquisitor, Oracle, Summoner, Witch). Cycle artifact: extension of `./artifacts/epic_4/per-class-coverage-matrix.md`. Files touched: per-class `class_*.rs` files in `src/rules_core/rules_tables/apg/`." (`epic-breakdown.md` §Epic 4)
- **Status:** complete
- **Notes:**
  - **Plan-vs-reality correction (recorded as a `## DISCOVERED` entry too):** the task brief named `src/rules_core/rules_tables/apg/class_tables.rs` as the target file. That path does not exist — APG (unlike CRB, which really does have a single consolidated `crb/class_tables.rs`) instead has six separate per-class files (`class_alchemist.rs`, `class_cavalier.rs`, `class_inquisitor.rs`, `class_oracle.rs`, `class_summoner.rs`, `class_witch.rs`), each independently doc-commented and already landed by SD-22 Epic 3. The book-level shared shape (`ClassTableRow`, `ApgClassId`, `class_chassis_resolve`) lives in `apg/mod.rs`, which is where this cycle's new coverage-audit code was added.
  - `src/rules_core/level_up/*.rs` for APG classes: **none exist**. `src/rules_core/level_up/` contains only the 11 CRB classes. Nothing to audit-and-extend there for this cycle; recorded as a coverage-matrix finding (`level_up_wired: false` for all six APG classes) rather than a file to touch.
  - This cycle is an **audit** cycle per `epic-breakdown.md`'s own Epic 4 purpose statement ("audit per-class feature-table coverage… Output the coverage matrix") and `acceptance-and-verification.md` CG-03 (coverage matrix, not remediation). It does not implement any of the six classes' missing named features (Bombs, Hexes, Judgments, Mysteries, Eidolon, Challenge, …) — that would be a multi-week, high-fabrication-risk undertaking spanning ~100 combined feature records across six classes, explicitly out of this criterion's scope (criterion 4.4 is the separate remediation-*plan* enumeration; there is no "Epic 4 implements the features" criterion). The "fix gaps TDD-style" instruction in this cycle's dispatch was interpreted, per the doctrine of *auditing as real, tested code rather than a one-off hand count*: the "gap" fixed here is the absence of an executable, regression-tested coverage report — not the absence of the narrative class features themselves, which remain an honest, correctly-diagnosed gap (see below).
  - **Audit findings, RED → GREEN verified:**
    1. Chassis (BAB + saves): fully wired for all six classes (20/20 levels), independently re-verified correct against the real PCGen corpus's `apg_classes.lst` `BONUS:COMBAT|BASEAB`/`BONUS:SAVE` tokens for every class (no defect found, unlike the earlier Cleric/Druid CRB bug `level_up/fighter.rs` documents).
    2. Named class features: 0 wired for every class against a corpus-derived expected count per class (Alchemist 24, Cavalier 16, Inquisitor 19, Oracle 19, Summoner 17, Witch 7 — methodology: distinct `KEY:<Class> ~ …` records in `apg_abilities_class.lst`, documented in `ApgClassCoverage`'s doc comment with the exact corpus commit and reproduction grep).
    3. `pilot_compute.rs`'s live `compute_class_chassis` dispatch does not recognize any APG class — proven empirically (not just by inspection) by driving a real `CharacterInput` for each of the six classes through `compute_pilot_base_chassis` and confirming the honest, claim-blocking `class_chassis.unsupported` diagnostic fires with `base_attack_bonus: 0`, never a fabricated number. This matches the pre-existing behavior for every non-Fighter/Wizard CRB class too (Fighter/Wizard are presently the *only* classes the live pilot-compute seam supports, across every book).
    4. No `level_up::<class>` module exists for any APG class.
  - This audit's conclusion (APG classes are chassis-only, zero live integration) is the precise condition `loop-instruction.md §4.2`'s hard-stop row already anticipates and pre-resolves: "Epic 4 finds APG/ACG classes are *not* fully wired → Multiclass Epic 5 scope is restricted to Fighter + Wizard only … defer APG/ACG-class multiclass to a follow-on bundle." No blocker raised; criterion 4.5 formalizes the already-decided deferral.
- **RED → GREEN evidence:**
  - RED: `tests/sd24_apg_class_coverage_audit.rs` did not compile before this cycle's `apg/mod.rs` additions (`ApgClassId::ALL`, `class_coverage`, `coverage_report` did not exist) — reproduced live via `git stash push -- src/rules_core/rules_tables/apg/mod.rs && cargo test --locked --test sd24_apg_class_coverage_audit`, which failed with 4 compile errors (`E0432` unresolved imports for `class_coverage`/`coverage_report`; `E0599` no `ALL`/`name` on `ApgClassId`), then `git stash pop` to restore.
  - GREEN: `cargo test --locked --test sd24_apg_class_coverage_audit` → 3 passed, 0 failed.
  - Full suite: `cargo test --locked --tests` → 3958 passed, 0 failed (3955 prior + 3 new).
  - Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`, clean after one self-healed single-token finding (see Notes).
- **Discovery forwards:**
  - `2026-07-21T00:00:00Z | epic-4 | 4.2 | plan-vs-reality | task brief named a non-existent src/rules_core/rules_tables/apg/class_tables.rs; APG's real shape is six per-class files (class_alchemist.rs et al.) plus the shared apg/mod.rs, mirroring content-unit-inventory.md's actual per-class-file expectation for APG (not CRB's single-file consolidation) | no new epic/criterion — documentation correction only, folded into this cycle's own artifacts`
- **Next-cycle plan:** Criterion 4.2 is now `complete` in `progress.md`'s Status matrix, DONE section, and Cycle log. Criteria 4.1 (CRB) and 4.3 (ACG) remain pending — a concurrent or subsequent cycle can pick either up per the file-touch partition (`crb/*.rs` and `acg/*.rs` are disjoint from this cycle's `apg/*.rs` touch). Criterion 4.4 (remediation plan enumeration) and 4.5 (APG/ACG multiclass deferral decision) can consume this cycle's coverage-matrix findings once 4.1/4.3 also land.
