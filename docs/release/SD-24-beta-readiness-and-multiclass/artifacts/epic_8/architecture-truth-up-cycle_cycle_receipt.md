# Cycle architecture-truth-up-cycle — Epic 8 (Closure Epilogue) / Criterion 8.2

- **Card ID:** t_a1b2c3d4 (placeholder — backfilled after kanban mint, see step 8 below)
- **Commit SHA:** (recorded post-commit in progress.md; see this receipt's own commit)
- **Files touched:** `docs/architecture/README.md`, `docs/architecture/overview.md`,
  `docs/architecture/desktop-app.md`, `docs/architecture/status.md`,
  `docs/architecture/rules-data-tables.md`, `docs/architecture/rules-engine.md`,
  `docs/architecture/persistence.md`, `docs/architecture/release-pipeline.md`,
  `docs/architecture/testing.md`, `docs/architecture/update-and-feedback.md`,
  `docs/architecture/conventions.md`, `docs/architecture/support-state-matrix.md`
  (header-only refresh, no content drift found), plus this receipt and
  `docs/release/SD-24-beta-readiness-and-multiclass/progress.md`.
  `docs/architecture/corpus-ingest.md` and `docs/architecture/homebrew-and-oracle.md`
  were reviewed and left untouched — their source dirs (`src/pcgen_import/`,
  `src/homebrew_authoring/`, `src/oracle_validation/`) carry zero diff since
  the SD-23 closure base (`09e43c3`), confirmed via `git diff --stat`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (full `09e43c3...HEAD` diff, unchanged from 8.1's baseline — this cycle's own changes are docs-only and outside the audited source globs)
- **Wired-integration audit result:** only the two pre-documented benign false positives (7.5's anti-fabrication doc comments in `character_hub.rs`; the `Plant Growth` CRB spell corpus text containing the ordinary word "hack") — identical to 8.1's finding; no new violation
- **Acceptance criterion (verbatim, `epic-breakdown.md` Criterion 8.2):**
  "Architecture closure pipeline (truth-up + graphify + PR + merge)."
  Files touched per the plan: `../../architecture/` (read-only survey),
  `./progress.md` (state), `./receipts.md` (cycle artifacts). Steps in
  order: (1) run `architecture-truth-up` script, (2) run `graphify-update`
  script, (3) open the `tranche/5-2 → develop` PR, (4) run
  `merge-conflict-resolution` pre-flight/post-PR, (5) stop the loop.
- **Status:** DISCOVERED-forked — this cycle completed **only sub-step 1
  (architecture-docs truth-up)**, the portion this cycle's granted write
  scope (`docs/architecture/**`) actually covers. Sub-steps 2-4 (graphify,
  PR open, merge-conflict-resolution) are explicitly out of this cycle's
  file-touch grant and are forwarded to a follow-on cycle — see Discovery
  forwards below. Criterion 8.2 as a whole is **not yet fully closed**;
  the Status matrix row below reflects `in-progress` rather than
  `complete` for that reason.
- **Notes / plan-vs-reality corrections (recorded in `progress.md` §DISCOVERED too):**
  1. **`architecture_truth_up.py` (`~/.hermes/profiles/god-emporer/skills/devops/architecture-truth-up/scripts/`) has a dead parser.** Its `parse_source_dirs_index` regex requires literal `||` table-cell delimiters (`r"\|\|\s*\[...\]\(\./...\)\s*\|[^|]+\|\s*\`...\`\s*\|\|"`), but `docs/architecture/README.md`'s real index table uses standard single-pipe Markdown rows. Running the regex directly against the real file (`python3 -c "..."`, reproduced in this cycle's shell history) matches **zero rows**, so `map_paths_to_docs` always returns an empty mapping regardless of diff content — the script would unconditionally report "no architecture impact" and write a false-negative receipt even against this cycle's substantial real diff. This is an out-of-repo tooling defect (the script lives under the operator's `~/.hermes/` profile, not this repo) that this cycle's write scope (`docs/architecture/**` in this repo) cannot fix. **Worked around by performing the truth-up manually**, following `README.md` §Maintenance contract's own procedure (diff review → path-to-doc mapping via the index table, read by a human/agent rather than the buggy regex → in-place edits → the two verification one-liners) rather than trusting the script's (silently wrong) output.
  2. **`receipts.md` does not exist yet** in this bundle's folder (`docs/release/SD-24-beta-readiness-and-multiclass/`), and the script refuses to run without one (`err("receipts.md does not exist...")`, exit 2) — consistent with never having been run for this bundle. Creating it is in scope for whichever cycle actually runs sub-steps 2-4 (this cycle did not create it, since doing so without a working script behind it would be a hollow gesture, and `receipts.md` is not under `docs/architecture/**`).
  3. **Real, substantive truth-up performed** (not a no-op pass): corrected 8 identifier-rename staleness hits (`sd19_equipment_catalog.rs`→`equipment_catalog.rs`, `sd19_spell_catalog.rs`→`spell_catalog.rs`, `sd19_class_catalog.rs`→`class_catalog.rs`, `sd19_race_catalog.rs`→`race_catalog.rs`, `sd19_corpus.rs`→`corpus_fixtures.rs`, `sd16_browser_handoff.rs`→`browser_handoff.rs`, `resources/sd19_corpus_fixtures/`→`resources/corpus_fixtures/`, criterion 1.1); documented the 3 new Epic-7 Tauri commands (`append_to_character`, `recompute_character`, `re_save_character`) including the **newly-discovered fact that none of them has any frontend caller** (zero `boundary/*.ts` wrapper, zero `invoke()` call site — confirmed by repo-wide grep); corrected the Tauri command-inventory count from a stale 26 to the real 36 (the extra 8 — `level_up_character`, `add_equipment_selection`, `add_spell_selection`, `delete_character`, `export_character`, `import_character`, `list_equipment`, `list_spells` — predate SD-24 but were never reflected in this doc, a pre-existing SD-23-era gap corrected here per the "current state" mandate); corrected the boundary-file list from 17 to the real 26 files for the same reason; documented SD-24 Epic 5's Fighter+Wizard multiclass base-chassis dispatch (`compute_multiclass_base_chassis`, `good_saves_for`, `wizard_level_in_mix`) in `rules-engine.md`, being explicit that it grounds the base-chassis/explanation layer only and does not by itself reach `HeadlessReceiptStatus::Computed`; added a new "Equipment/spell content completeness" section to `rules-data-tables.md` tabulating the real cost/weight/description population ceilings SD-24 Epic 6 landed per book (CRB/APG/ACG); corrected `persistence.md`'s command list and documented the real, inconsistent revision-id-advancement behavior (`re_save_character` is the only mutation path that advances `revision_id`; the rest keep the on-disk value forever); fixed a stale `0.5.96` version citation in `release-pipeline.md` (real current version, confirmed via grep, is `0.5.97` across `package.json`/`tauri.conf.json`/`Cargo.toml`); refreshed `testing.md`'s file count (426→441) and inline-test-module file list (12→15, including the new `characterHub/` submodule files).
  4. **Verification one-liners: RED confirmed, then GREEN.** Ran the maintenance contract's cited-path check against the **pre-edit** committed doc content (`git show HEAD:docs/architecture/desktop-app.md` etc.) and reproduced two real missing-path hits (`apps/desktop/src-tauri/src/sd16_browser_handoff.rs`, `apps/desktop/src-tauri/src/sd19_corpus.rs` — both renamed away by this bundle's own criterion 1.1, proving the doc set had genuinely drifted). Re-ran the identical check against the post-edit working tree across all 14 docs: zero missing paths. Relative-link check: zero broken links, both before and after (no link topology changed).
- **Discovery forwards:**
  - `2026-07-21T17:30:00Z | epic-8 | criterion-8.2 | epic-8-tooling-defect | ~/.hermes/profiles/god-emporer/skills/devops/architecture-truth-up/scripts/architecture_truth_up.py's parse_source_dirs_index regex requires literal "||" table delimiters that do not match docs/architecture/README.md's real single-pipe Markdown table -- the script always returns an empty docs_touched mapping and would silently under-report architecture impact against any real diff. Out-of-repo (operator's ~/.hermes profile), cannot be fixed from this cycle's granted scope. | suggested: operator fixes the regex (or the README table format) directly; not a follow-on cycle in this repo since the script lives outside it.
  - `2026-07-21T17:30:00Z | epic-7 | criterion-7.1/7.2/7.3 | epic-7-unwired-commands | append_to_character, recompute_character, and re_save_character (SD-24 Epic 7's three new Tauri commands) are registered and tested but have zero frontend callers -- no boundary/*.ts wrapper, no invoke() call site anywhere in apps/desktop/src. Epic 7's own cycles were file-touch-scoped to src-tauri only per loop-instruction.md §2.4, so wiring a frontend caller was out of each cycle's granted scope, and no later Epic 7 cycle closed the gap. | suggested: a follow-on Epic 7 (or Epic 8 remediation) cycle adds the three boundary/*.ts wrappers and wires at least one real UI affordance to each, matching the pattern criterion 7.4 already proved for Add Weapon/Add Armor/Add Spell.
  - `2026-07-21T17:30:00Z | epic-8 | criterion-8.2 | epic-8-scope-narrowing | This cycle's granted write scope (docs/architecture/** per the dispatching orchestrator's explicit file-ownership grant) covers only sub-step 1 of criterion 8.2's own 5-sub-step acceptance text (truth-up). Sub-steps 2-4 (graphify-update, opening the tranche/5-2 -> develop PR, merge-conflict-resolution) remain undone. | suggested: a follow-on Epic 8 cycle with write scope covering repo-root git/PR operations (and, if fixed, the graphify script) closes the remaining sub-steps and flips 8.2 to complete.
- **Next-cycle plan:** A follow-on Epic 8 cycle should (a) create `docs/release/SD-24-beta-readiness-and-multiclass/receipts.md` if it doesn't yet exist, (b) run (or manually perform the equivalent of) the graphify-update sub-step and append its receipt, (c) open the `tranche/5-2 → develop` PR via `gh pr create`, (d) run merge-conflict-resolution pre-flight (rebase) and post-PR (mergeable-state) checks, resolving any conflicts, then (e) flip criterion 8.2 to `complete` in the Status matrix once all 5 sub-steps have a receipt. Only after 8.2 is `complete` should criterion 8.3 (release notes) and 8.4 (build version increment) fire, per Epic 8's deterministic order.
