# Cycle fighter-wizard-multiclass-dispatch-cycle — Epic 5 (Multiclass Stacking Real and Full) / Criterion 5.1

- **Card ID:** t_5c0184e5 (placeholder pending real kanban ID — backfilled in step 8)
- **Commit SHA:** (backfilled after commit — see this file's own git history / progress.md `## DONE`)
- **Files touched:**
  - `src/rules_core/pilot_compute.rs`
  - `src/rules_core/level_up/fighter.rs`
  - `src/rules_core/level_up/wizard.rs`
  - `tests/sd24_multiclass_fighter_wizard_split.rs` (new)
  - `tests/sd24_multiclass_fighter_lv10.rs` (new)
  - `tests/sd24_multiclass_wizard_lv10.rs` (new)
  - `tests/fixtures/rules_core/pf1_human_fighter4_wizard1_sd24_multiclass_split_input.txt` (new)
  - `tests/fixtures/rules_core/pf1_human_fighter9_wizard1_sd24_multiclass_lv10_input.txt` (new)
  - `tests/fixtures/rules_core/pf1_human_wizard9_fighter1_sd24_multiclass_lv10_input.txt` (new)
  - `tests/sd13_wizard_level{2..10}_progression.rs` (9 files, mechanical negative-control fix — see Notes)
  - `tests/sd18_wizard_level{11..20}_widening.rs` (10 files, mechanical negative-control fix — see Notes)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (before and after)
- **Wired-integration audit result:** `OK_NO_TOKENS` (before and after)
- **Acceptance criterion (verbatim, `epic-breakdown.md` criterion 5.1):**
  > Files touched: `src/rules_core/pilot_compute.rs`, `src/rules_core/rules_tables/crb/class_fighter.rs`, `class_wizard.rs`.
  > RED: advancement from level 1 → 10 fails at level 5 (the multiclass split-class transition level) for the Fighter-side or Wizard-side.
  > GREEN: advancement from level 1 → 10 succeeds; BAB stacking follows canonical PF1 best-progression; saves use best-fractional-progression.
- **Status:** complete
- **Notes:**
  - **Plan-vs-reality path correction (already known going in, per the loop dispatcher's own briefing):** `class_fighter.rs`/`class_wizard.rs` do not exist. Per-class BAB/save table data lives in the shared `rules_tables::crb::class_tables`; the actual per-class `LevelUpPlan` composer files are `src/rules_core/level_up/fighter.rs` / `level_up/wizard.rs` (SD-20 Epic 7), which is the file set this cycle was granted and used.
  - **Real gap found (this cycle's own audit before writing any test):** `pilot_compute.rs`'s multiclass BAB/save chassis stacking (`compute_multiclass_base_chassis`, `is_supported_multiclass_mix`, `fighter_level_in_mix`) was **already landed** by a prior SD (SD-21 E7.28/E7.29/E7.30) — it already correctly sums BAB and stacks saves via the sum-fractions-then-round-down-once rule for a Fighter+Wizard mix, and Fighter's own class features already kept firing inside a supported mix via `fighter_level_in_mix`. The genuinely open gap was **symmetric but Wizard-side**: (1) `pilot_compute.rs`'s `explain_wizard_level1_prepared_spell_baseline` (Wizard's level-1 prepared arcane spell-bearing recognition, Scribe Scroll, school specialization choice, specialist bonus slot ladder, the two Evocation school powers, and the SD-21 E6b.2 prepared-spellbook posture) was still gated on `supported_wizard_level` — single-class-only — so every one of those facts silently vanished the instant Wizard joined a multiclass mix; and (2) `level_up::fighter::compute_fighter_level_up_grants` / `level_up::wizard::compute_wizard_level_up_grants` (the `LevelUpPlan` grant model) were BOTH still gated on an exact single-class match, so `level_up::compute_level_up_grants`'s own top dispatcher (unchanged, outside this cycle's file-touch grant) fell through to its `_ => LevelUpPlan::default()` arm for any 2-class mix — an honestly-empty-but-wrong-for-this-input `LevelUpPlan` at exactly the "split-class transition level" the criterion's own RED text names.
  - **Fix:** (a) added `wizard_level_in_mix` to `pilot_compute.rs`, mirroring the pre-existing `fighter_level_in_mix` exactly, and re-gated `explain_wizard_level1_prepared_spell_baseline` on it instead of `supported_wizard_level`. (b) Widened `level_up::fighter`'s and `level_up::wizard`'s own entry gates from "exact single-class match" to "solo, OR a length-2 Fighter+Wizard mix" (`is_fighter_or_supported_fighter_wizard_mix` / `is_wizard_or_supported_fighter_wizard_mix`), mirroring `pilot_compute.rs`'s own `is_supported_multiclass_mix` boundary. No other change was needed inside either `level_up` file: both already read `from_level`/`to_level` purely from the caller's parameters (never from `character.chosen.class_levels`'s own level number), so once the entry gate widened, real per-class grant data composed correctly using each class's own from/to sub-level supplied by the caller. `level_up.rs`'s own top dispatcher (`compute_level_up_grants`) was deliberately left untouched: reconstructing a fully general multiclass "from" state from a single character snapshot + two plain level numbers is ambiguous (which of the two classes changed this step is not recoverable from that shape alone), so this cycle's tests call `level_up::fighter::compute_fighter_level_up_grants` / `level_up::wizard::compute_wizard_level_up_grants` directly (both already `pub`), each supplying that class's own unambiguous sub-level from/to pair — the same isolate-per-class technique `compute_multiclass_base_chassis` already established for the chassis pillar.
  - **Mechanical fallout fix (19 pre-existing test files, outside this cycle's originally granted file set — recorded as `## DISCOVERED`):** `tests/sd13_wizard_level{2..10}_progression.rs` and `tests/sd18_wizard_level{11..20}_widening.rs` each carried a `multiclass_wizard_levelN_is_not_promoted_by_this_slice` negative control pairing Wizard with **Fighter** as the "definitely still unsupported" second class. That pairing is no longer unsupported — it is precisely what this cycle promotes — so all 19 broke. Fixed by swapping the injected second class from `class:fighter:1` to `class:rogue:1` (Rogue is still genuinely unsupported for multiclass), mirroring the Fighter-side negative controls' own pre-existing precedent (e.g. `sd18_fighter_level20_widening.rs` already used Rogue for the identical reason). A short doc comment was added above each fixed test explaining the correction. No assertion logic changed — only which class fills the "unsupported partner" role.
  - No `level_up.rs` (the parent dispatch module) changes were needed or made; it stays outside this cycle's file-touch set.
- **Discovery forwards:**
  - `2026-07-21T00:40:00Z` | epic-5 | criterion-5.1 | epic-5-follow-on | `level_up::compute_level_up_grants`'s top-level dispatcher (`src/rules_core/level_up.rs`, NOT in this cycle's granted file set) still returns an honestly-empty `LevelUpPlan::default()` for any 2+-class `class_levels` mix, including the now-supported Fighter+Wizard combination — it never routes to `fighter::compute_fighter_level_up_grants` / `wizard::compute_wizard_level_up_grants` for a mix. This cycle worked around it by having tests call each per-class function directly (each already `pub`), which is unambiguous and correct per-class, but any real caller (a future Tauri command surface, Epic 7's `recomputeCharacter`) that only calls the single top-level `compute_level_up_grants` entry point would still see an empty plan for a multiclass character. A real fix needs a genuinely new signature or an explicit two-classes-worth-of-sub-levels parameter shape (not attempted this cycle — reconstructing an unambiguous "from" state per class from a single plain `from_level`/`to_level` pair is not decidable in general, only in the specific split-level scenarios this cycle's tests construct). | suggested: a follow-on Epic 5 or Epic 7 cycle that either (a) widens `compute_level_up_grants`'s signature to accept explicit per-class from/to sub-levels for a multiclass caller, or (b) adds a dedicated `compute_multiclass_level_up_grants` entry point in `level_up.rs` composing both per-class functions, once the real Tauri command surface (Epic 7) needs to drive a multiclass level-up end to end.
- **Next-cycle plan:** Criterion 5.2 (Deterministic test surface: 30 character-advancement cycles, `tests/sd24_multiclass_deterministic.rs`) per `## TODO` deterministic order — can build directly on this cycle's fixtures (`pf1_human_fighter9_wizard1_...`, `pf1_human_wizard9_fighter1_...`) and the now-widened `level_up::fighter`/`level_up::wizard` entry gates.
