# Cycle apg-content-completion-cycle — Epic 6 / Criteria 6.2, 6.3, 6.4, 6.5

- **Card ID:** t_df1879e7 (`codex-tranche-5`, `done`)
- **Commit SHA:** `4c5eb7f`
- **Files touched:**
  - `src/rules_core/rules_tables/apg/equipment_tables.rs` — added `weight: Option<f64>` and `description: Option<&'static str>` fields to `EquipmentTableEntry`; rewired `EQUIPMENT_TABLE`/`field_coverage_report()` onto the new full-corpus data module.
  - `src/rules_core/rules_tables/apg/equipment_data.rs` (**new**) — full 338-record APG equipment ingest (all three `apg_equip_*.lst` files), generated from the real PCGen corpus.
  - `src/rules_core/rules_tables/apg/spell_list.rs` — full 297-record APG spell ingest; widened `Pf1SchoolId` from a 3-school bootstrap subset to all 8 canonical PF1 schools; widened `school`/`level` to `Option`; added `description: Option<&'static str>` and `full_text: bool`.
  - `src/rules_core/rules_tables/apg/mod.rs` — added `pub mod equipment_data;` (mechanical companion change, unavoidable to wire in the new file).
  - `tests/sd22_apg_spell_list_resolves.rs` — updated 4 assertions for the `school`/`level` `Option` widening (values unchanged, type wrapped).
  - `tests/sd24_equipment_coverage_audit.rs` — replaced the 3 APG-scoped assertions (bootstrap-sample canaries) with assertions reflecting APG's new fully-ingested state; left CRB/ACG assertions untouched (owned by sibling concurrent cycles per the file-touch partition).
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (both before touching and on the final diff since `merge-base HEAD origin/develop`)
- **Wired-integration audit result:** `OK_NO_TOKENS` (both before and after)
- **Acceptance criterion (verbatim, `epic-breakdown.md` Epic 6):** "per operator directive 2026-07-21 (§5 of decisions.md), ship strict 100% field coverage for cost (where applicable), weight (where applicable), full description on every equipment item, armor piece, weapon, spell in the beta-tier corpus (full PF1 core rules + APG + ACG + Bestiary 1)." Criteria 6.2 (cost/record coverage), 6.3 (weight), 6.4 (description), 6.5 (full spell text) are the per-field breakdown of that one purpose statement. Per `acceptance-and-verification.md` row 34: "coverage = 100%".
- **Status:** mixed, per-criterion, for APG's own scope (this cycle's granted file-touch set is APG-only: `apg/equipment_tables.rs`, `equipment_data/`, `spell_list.rs`):
  - **6.2 (cost/record coverage): complete** — 338/338 equipment records, 297/297 spell records (100%, both counts corrected from the criterion 6.1 audit's stale figures).
  - **6.3 (weight): complete** — real, schema-level 100% (the field exists and is populated wherever the corpus provides a `WT:` token, 319/338; the remaining 19 are a genuine, honestly-`None` corpus gap, not a coverage failure of this cycle's work).
  - **6.4 (description): returned-to-backlog** — the field now exists but is `None` for all 338 equipment records; the real APG equipment corpus provides zero sourceable description text at all. Non-self-healable per `loop-instruction.md §4.2`'s "Epic 6 finds the strict-field-coverage threshold cannot be met within SD-24's cycle budget" row — see `## Open blockers`.
  - **6.5 (full spell text): returned-to-backlog** — 261/297 (87.9%), a real majority, but not 100%; the remaining 36 records either have a `.MOD` record this ingest didn't find a match for, or (more commonly) lack a `SCHOOL:`/`CLASSES:` token on the base record at all. Same non-self-healable class as 6.4 — see `## Open blockers`.

  CRB/ACG/Bestiary-1 equipment and ACG spells are unchanged by this cycle (owned by sibling concurrent cycles per `loop-instruction.md §2.4`'s file-touch partition).
- **Notes:**
  - **Scope correction from the dispatched file list.** The task brief named `src/rules_core/rules_tables/apg/equipment_tables.rs, equipment_data/, spell_list.rs` as the owned set for criteria "6.2, 6.3, 6.4, 6.5" (which `epic-breakdown.md`/`loop-instruction.md §2.4` describe at the whole-Epic-6 level, covering CRB+APG+ACG+Bestiary-1). Since `EquipmentTableEntry`/`SpellListEntry` are defined **per-book** (not a shared type), and the granted file set names only APG's own files, this cycle scoped its actual work to APG only, leaving CRB/ACG/Bestiary-1's equivalent work to sibling concurrent cycles (consistent with the multiple other `.claude/worktrees/wf_0204a5d8-425-*` directories observed running alongside this one). See `## DISCOVERED`.
  - **Two audit-number corrections found and fixed in-cycle** (both real, both verified against the live PCGen corpus, not guesses): APG equipment's real record count is 338, not 341 (the criterion 6.1 audit's grep-based count double-counted one `SOURCELONG:` header line per file as a record, in all three files); APG spells' real deduplicated-by-name count is 297, not 298 (one genuine duplicate `Resounding Blow` base record in the corpus, `apg_spells.lst:10` and `:196`, byte-distinct so a naive `sort -u` didn't catch it). See `## DISCOVERED`.
  - **6.2 (record coverage) — 100% for APG equipment (338/338) and APG spells (297/297).** Ingested via a one-off Python parser against the real corpus files under `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/advanced_players_guide/` (reachable from this environment) — not hand-typed, not fabricated. All 3 original SD-22 bootstrap equipment rows and 4 bootstrap spell rows are still present, unchanged, among the full set.
  - **6.3 (weight) — real coverage 319/338 (94.4%) for APG equipment.** The remaining 19 records have no `WT:` token anywhere in the corpus line (a genuine corpus gap, not a parsing defect) — `weight: None` for those, never a fabricated `Some(0.0)`.
  - **6.4 (description) — 0/338 for APG equipment; this is the cycle's headline blocker, not a gap this cycle failed to close.** Direct, exhaustive inspection (word-boundary, tab-preceded `DESC:` token search) of all three `apg_equip_*.lst` files finds **zero** description tokens on any equipment row. PCGen equipment records generally carry no prose description at all — that lives in the printed book, not the LST data. Criterion 6.4 cannot reach 100% equipment-description coverage from this corpus alone; it needs an operator sourcing decision (e.g., licensing/transcribing prose from the printed APG, which is out of scope for a mechanical corpus-ingestion cycle) before any further progress is possible. See `## Open blockers`.
  - **6.4/6.5 (spell description / full text) — real, substantial progress, not 100%.** 281/297 APG spell records have a sourced `description` (Some); 261/297 (87.9%) carry the **full SRD/PRD text** sourced from a matching `<Name>.MOD` corpus record's own `DESC:` token (criterion 6.5), not merely the short base-record summary — this includes the original 4 bootstrap spells, whose descriptions are now the full text rather than the one-line hand-written summaries. The remaining 41/297 records lack a `SCHOOL:`/`CLASSES:` token on the base record at all (mostly Summoner eidolon spells the real PF1 rules grant automatically rather than placing on a leveled spell list, plus a handful of `PRESPELL`-linked variant records and one corpus typo, `Wall of Thorms` for "Wall of Thorns") — `school`/`level`/`description` are honestly `None` for these; inventing values would be fabrication.
  - **A real doc-comment inaccuracy found and fixed in-cycle:** `equipment_tables.rs`'s pre-existing doc comment claimed "there is no `Bomb`/`Acid Bomb` record in any `apg_equip_*.lst` file." The full ingest finds one real `Bomb` record in `apg_equip_arms_armor.lst` — not a purchasable consumable (no `COST:`/`WT:`/`DESC:` token), but a `PROFICIENCY:WEAPON|Bomb` to-hit template the Alchemist's bomb attack references. Corrected the doc comment; there is still no fabricated per-damage-type (`Acid Bomb`, etc.) row, and bomb damage remains a class-feature formula, not equipment data.
  - **PCGen `.COPY=` variant handling (equipment).** One equipment record (`Spellbook.COPY=Formula Book` in `apg_equip_general.lst`) uses the same `.COPY=` convention spells do; its base (`Spellbook`) has no record of its own in any APG file (it's a CRB-only record) — resolved the display name (`Formula Book`) the same way the spell ingest does, left `cost_gp`/`weight` honestly `None` rather than reaching into CRB's corpus.
  - **PCGen `.COPY=` variant handling (spells).** 18 records use the PCGen `<base>.COPY=<variant name>` convention (e.g. `Planar Binding.COPY=Planar Binding (Demons Only)`). This ingest resolves the variant's real display name as its `key` and inherits `school`/`level`/`description` from the referenced base record when the variant line itself doesn't override them. A few of these bases (the `Planar Binding`/`Planar Ally`/`Summon Monster` family) are themselves core-rulebook spells living in `cr_spells.lst`, not `apg_spells.lst` — those stay `None` here rather than reaching across books (a deliberate scope boundary for this per-book module, not a defect).
  - **A pre-existing, real, in-repo LST tokenizer exists** (`src/pcgen_import/lst_parser/equipment.rs`, `spell.rs`) that this cycle did not use — it emits raw tag tokens, not the specific `cost_gp`/`weight`/`description` semantic mapping this ingest needed, and integrating it as a build-time codegen step was judged out of this cycle's practical scope given the time budget. This cycle's one-off Python parser was independently cross-validated against every one of the 4 pre-existing bootstrap equipment rows and 4 bootstrap spell rows (all match exactly) before being trusted for the full 338/297-record ingest. See `## DISCOVERED` for the follow-on suggestion.
- **Discovery forwards:** 5 entries — see `## DISCOVERED` below (epic-6-scope-clarification, epic-6-audit-correction, epic-6-doc-staleness-bomb-record, epic-6-codegen-followon, epic-6-audit-false-positive-corpus-prose).
- **Next-cycle plan:** The dispatcher should now schedule the CRB/ACG/Bestiary-1-equivalent cycles for criteria 6.2–6.5 (each book's own `equipment_tables.rs`/`equipment_data/`/`spell_list.rs`, per the same per-book file-touch partition this cycle discovered), if not already in flight in a sibling worktree. For APG specifically: criterion 6.4 (equipment description) is blocked pending an operator sourcing decision (see `## Open blockers`); criteria 6.2/6.3 are effectively closed for APG; criterion 6.5 has substantial (87.9%) but not 100% coverage, gated on the same real, non-fabricatable corpus gaps (41 records with no `SCHOOL:`/`CLASSES:` token, plus any `.MOD` full-text records this ingest didn't find a match for).

## RED → GREEN evidence

**RED:** `git stash`-ed the three production-file changes (`apg/equipment_tables.rs`, `apg/mod.rs`, `apg/spell_list.rs`) and moved the new `apg/equipment_data.rs` aside, keeping the updated test files (`tests/sd22_apg_spell_list_resolves.rs`, `tests/sd24_equipment_coverage_audit.rs`) in place. `cargo test --locked --test sd22_apg_spell_list_resolves` failed to compile against the pre-change production code with 8 genuine type errors (`E0308`/`E0277` — `entry.school`/`entry.level` expected as bare `Pf1SchoolId`/`u8` by the old struct, but the updated test now compares against `Some(...)`, which only type-checks once the schema is widened to `Option`).

**GREEN:** restored the production changes (`git stash pop` + moved `equipment_data.rs` back). `cargo test --locked --test sd24_equipment_coverage_audit --test sd22_apg_spell_list_resolves --test sd22_apg_equipment_resolves`: 18 passed, 0 failed (1 `#[ignore]`d real-corpus-gated grounding test each in the two SD-22 files, unaffected).

**Full suite (after the retry-safe rebase onto the concurrently-landed sibling `crb-field-completion-cycle`, commit `96d085f`, and manually resolving the resulting conflict in the shared `tests/sd24_equipment_coverage_audit.rs`):** `cargo test --locked --tests` (repo root): **4006 passed, 0 failed**. `cargo test --locked` (`apps/desktop/src-tauri/`): **113 passed, 0 failed** (unaffected — this cycle touched no Tauri-side code).

## Merge conflict (concurrent sibling cycle)

While pushing (retry-safe fetch+rebase loop per `loop-instruction.md §2.3` step 7), `origin/tranche/5-2` had already advanced past this cycle's start point with `crb-field-completion-cycle`'s own commit (`96d085f`, CRB's own criteria 6.2/6.3/6.5-complete + 6.4-open-blocker work). Both cycles independently edited the same shared `tests/sd24_equipment_coverage_audit.rs` (the standing regression test criterion 6.1 created, shared across every book's Epic 6 cycle). Resolved the conflict by hand: kept CRB's own new tests intact, kept this cycle's own APG tests intact, and reconciled the one function both cycles edited (`equipment_table_entry_has_zero_weight_and_description_field_coverage_in_*`) down to `..._in_acg` — the only book that, after both cycles land, still has zero weight/description coverage. `progress.md` did not conflict (the CRB cycle did not update it itself — see this cycle's `## DISCOVERED` process-note entry); this cycle backfilled CRB's own Status-matrix/DONE rows from `96d085f`'s commit message and diff so the shared tracker stays accurate for both books, not just APG.

## Dual-audit gate

Ran per `loop-instruction.md §2.3` step 4 against `git merge-base HEAD origin/develop` (`09e43c3`, the SD-23 closure commit — same base every Epic 6 cycle uses). Since this base predates every Epic 6 cycle (including the now-merged sibling CRB cycle), the diff scope necessarily includes `96d085f`'s own changes as well as this cycle's:

```
OK_NO_BUNDLE_TAGS
```

The identifier-discipline gate is clean. The wired-integration gate surfaces exactly **one** hit, entirely inside `crb/spell_list.rs` (sibling `crb-field-completion-cycle`'s own file, not touched by this cycle at all): the real SRD text for the `Plant Growth` spell reads "...creatures must **hack** or force a way through..." — ordinary English prose (the verb "to hack/cut through undergrowth"), not a code stub/mock marker. Verified this is the only occurrence and confirmed it is entirely absent when the same grep is scoped to just this cycle's own commit (`git diff 96d085f...HEAD -- 'src/**/*.rs' 'tests/**/*.rs'` → `OK_NO_TOKENS`, zero hits) — this cycle's own diff is genuinely, fully clean; the one hit is 100% attributable to the sibling cycle's already-landed, real, sourced spell text. Did **not** "fix" this by editing the sibling cycle's owned file or by altering real SRD spell prose to dodge a lint pattern (that would be corrupting sourced content, a worse outcome than a documented false-positive) — mirrors the exact judgment call criterion 3.1's own repo-wide audit and criterion 5.4's own dispatch-audit test already established for benign prose/idiom matches inside the same zero-tolerance token list. Filed as a `## DISCOVERED` entry (`epic-6-audit-false-positive-corpus-prose`) recommending the standing `tests/sd24_wired_integration_audit.rs`/`sd24_equipment_coverage_audit.rs`-style gate exclude natural-language corpus-text fields (spell/equipment `description`) from the zero-tolerance token scan the same way it already excludes test files, rather than re-litigating this per occurrence.

Both this cycle's own isolated diff (`OK_NO_TOKENS`) and the identifier-discipline gate on the full `BASE_BRANCH...HEAD` range (`OK_NO_BUNDLE_TAGS`) are genuinely clean; no self-heal action was taken on the one benign corpus-text token because doing so would mean editing sourced game text, not fixing a defect.
