# SD-24 — Progress

> **Operating method:** see `./scope-draft.md` and `./loop-instruction.md`. This file is created on cycle 0 of Epic 2 with the deterministic seed of 35 criteria. Cycle picker reads `## TODO` + `## DISCOVERED` per the deterministic-seeded-then-dynamic model.

This file is the bundle's runtime state. The loop's `progress.md` is the canonical cycle-log + status matrix; the kanban board is the durable receipt; the per-cycle `artifacts/<epic>/<cycle>_cycle_receipt.md` is the per-cycle truth.

`## TODO` — the deterministic seed + dynamic entries, in dispatch-priority order. The picker reads this first.

`## DONE` — completed criteria, with commit SHA and cycle-id. Append-only.

`## DISCOVERED` — cycle-found items outside the deterministic list. Each entry has `<ISO-8601> | <epic-of-origin> | <criterion-of-origin> | <priority-bump-tag> | <description> | <suggested-epic-and-criterion>`. Priority-bump items go to the front of `## TODO` on operator call.

`## Status matrix` — per-criterion state in epic+criterion-number order (1.1, 1.2, 2.1, ..., 8.4). Update on every cycle's Step 9.

`## Cycle log` — append-only cycle entries (per `./loop-instruction.md §3` schema).

`## Open blockers` — non-self-healable items requiring operator intervention.

## Status matrix (placeholder — populated by cycle 0 of Epic 2)

| Criterion | State | Cycle ID | Commit SHA | Notes |
|---|---|---|---|---|
| 1.1 Source-code identifier audit | complete | identifier-audit-cycle | 3c3882a | Corrected-pattern audit found + remediated 6 sd16_/sd19_ modules, a resource dir, 13 SD19_* consts; see ## DISCOVERED |
| 1.2 Per-cycle tests pass | complete | identifier-audit-cycle | 3c3882a | cargo test --locked --tests (root) + cargo test --locked (src-tauri) both 0 failures |
| 2.1 board reachable | complete | operator-pre-launch-cycle | d6cddac | `hermes kanban boards` shows codex-tranche-5 |
| 2.2 branch pushed | complete | operator-pre-launch-cycle | d6cddac | ls-remote resolves, matches local HEAD |
| 2.3 SD-23 closure PR merged | complete | operator-pre-launch-cycle | d6cddac | Tier-1 launch gate CONFIRMED: 09e43c3 (PR #329) is HEAD of origin/develop |
| 2.4 working tree clean | complete | operator-pre-launch-cycle | d6cddac | git status --porcelain empty |
| 2.5 doctrines loaded | complete | operator-pre-launch-cycle | d6cddac | gap re-confirmed non-blocking per ./missing_skills.md; dual-audit grep gate is the operative check and is clean |
| 3.1 Wired-Integration Audit | complete | wired-integration-audit-cycle | 8756340 | Repo-wide four-check audit; 1 genuine finding (GE-07 pilot-shell-snapshot scaffold), deferred; see ## DISCOVERED |
| 3.2 forbidden tokens remediation | complete | wired-integration-audit-cycle | 8756340 | Zero-tolerance tokens (STUB/MOCK/todo/fixme/hack) clean repo-wide; the one `placeholder` finding deferred per risks-and-open-questions.md §5 |
| 3.3 noop handlers remediation | complete | wired-integration-audit-cycle | 8756340 | 0 findings repo-wide; nothing to remediate |
| 3.4 mock leaks + "Would …" remediation | complete | wired-integration-audit-cycle | 8756340 | 0 findings repo-wide; nothing to remediate |
| 4.1 per-class audit CRB | complete (Fighter+Wizard scope) | fighter-wizard-audit-cycle | 66f9be8 | Fighter: 0 gaps. Wizard: 1 gap found + fixed (`class_spell.wizard.*` dropped from LevelUpPlan); see ## DISCOVERED for remaining 9-class follow-on |
| 4.2 per-class audit APG | complete | apg-class-audit | f25dc7b | All 6 real APG classes: chassis 20/20 wired + verified correct; 0 named features wired for any class; pilot_compute.rs integrates none; no level_up module for any; see per-class-coverage-matrix.md APG section + ## DISCOVERED |
| 4.3 per-class audit ACG | complete | acg-class-audit | 2e074ce | All 10 real ACG classes (Arcanist, Bloodrager, Brawler, Hunter, Investigator, Shaman, Skald, Slayer, Swashbuckler, Warpriest — corrected roster): chassis 20/20 wired + verified correct; 0 named features wired for any class; pilot_compute.rs integrates none; no level_up module for any; see per-class-coverage-matrix.md ACG section + ## DISCOVERED |
| 4.4 remediation plan | complete | remediation-and-deferral-cycle | ac3b130 | Enumerated all class gaps from 4.1-4.3 with fix cycle-ids (Wizard: remediated in-cycle by 4.1; 16 APG/ACG classes: no cycle-id assigned, deferred); see `./artifacts/epic_4/remediation-plan.md` |
| 4.5 APG/ACG multiclass deferral | complete | remediation-and-deferral-cycle | ac3b130 | Formal decision record: APG/ACG-class multiclass deferred to follow-on bundle (Fighter+Wizard-only Epic 5 unaffected); see `./artifacts/epic_4/apg-acg-multiclass-deferred.md` and `risks-and-open-questions.md §5` |
| 5.1 F+W multiclass dispatch | complete | fighter-wizard-multiclass-dispatch-cycle | 0068818 | Wizard-side gap fixed (`wizard_level_in_mix` mirroring pre-existing `fighter_level_in_mix`; `level_up::fighter`/`level_up::wizard` entry gates widened to the mix); BAB/save chassis stacking was already landed by a prior SD. See `## DISCOVERED` for the top-dispatcher follow-on. |
| 5.2 30 character-advancement cycles | complete | fighter-wizard-multiclass-deterministic-cycle | e8e97c2 | 30-cycle deterministic walk (10 solo-Fighter + 10 solo-Wizard + 10 split-advance) in `tests/sd24_multiclass_deterministic.rs`; per-cycle input/output in `artifacts/epic_5/multiclass-fixture.md` |
| 5.3 integration test | complete | integration-test-cycle | b503c47 | New `tests/sd24_multiclass_integration.rs` cross-checks the multiclass dispatch against `class_tables()`'s ingested content (not a hand-typed formula twin); added `class_tables::good_saves_for` and removed `pilot_compute.rs`'s duplicate good/poor save table |
| 5.4 multiclass four-check audit | complete | multiclass-dispatch-four-check-audit | 79162c1 | New standing regression test `tests/sd24_multiclass_dispatch_audit.rs` scopes the four-check audit to exactly the multiclass dispatch production files (`pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, `class_tables.rs`); genuine RED surfaced 19 pre-existing "packet placeholder" anti-fabrication-idiom hits, bucketed clean per criterion 3.1's own precedent. See `## DISCOVERED` for the 3rd occurrence of the task-brief-drift pattern. |
| 5.5 APG/ACG multiclass deferred | complete | apg-acg-multiclass-deferred | bb7dd74 | Epic-5-side echo of criterion 4.5's decision; confirmed (by inspection of all four Epic 5 commits) that no APG/ACG class touch landed anywhere in Epic 5's actual delivery. See `## DISCOVERED` for the 4th occurrence of the task-brief-drift pattern (Epic 5 now closed). |
| 6.1 Equipment coverage audit | complete | equipment-coverage-audit-cycle | b8b9d40+f037d80 | CRB equipment/spells: 100%/96.6% record coverage; APG/ACG equipment/spells: bootstrap-only (3/341, 3/221, 4/298, 4/145); Bestiary1: 0/7, no module; EquipmentTableEntry has no weight/description field in any book; no spell has full SRD/PRD text; card t_5c240f6b; see ## DISCOVERED |
| 6.2 Equipment content completion: cost (CRB) | complete | crb-field-completion-cycle | 96d085f | Re-audit found no real gap: CRB equipment (2977/2977) and spells (652/652) were already 100% record-complete; 6.1's "675/96.6%" spell figure was a measurement error. ACG/Bestiary-1 still open (separate cycles) |
| 6.2 Equipment content completion: cost (APG) | complete | apg-content-completion-cycle | (pending — see Cycle log) | APG equipment 338/338 (100%, corrected from 341) + APG spells 297/297 (100%, corrected from 298) record coverage. ACG/Bestiary-1 still open |
| 6.3 Equipment content completion: weight (CRB) | complete | crb-field-completion-cycle | 96d085f | `weight_lbs: Option<f64>` added + populated to the honest "where applicable" ceiling: 2011/2977 (67.5%); equipmods have 0 real WT: tokens (not a gap). ACG/Bestiary-1 still open |
| 6.3 Equipment content completion: weight (APG) | complete | apg-content-completion-cycle | (pending — see Cycle log) | `weight: Option<f64>` added to APG `EquipmentTableEntry`; 319/338 (94.4%) populated from real `WT:` tokens, 19 real corpus gaps honestly `None`. ACG/Bestiary-1 still open |
| 6.4 Equipment content completion: description (CRB) | returned-to-backlog | crb-field-completion-cycle | 96d085f | `description: Option<&'static str>` added + populated from real corpus DESC: tokens: 1821/2977 (61.2%) honest ceiling — literal 100% unreachable without fabrication; see ## Open blockers |
| 6.4 Equipment content completion: description (APG) | returned-to-backlog | apg-content-completion-cycle | (pending — see Cycle log) | `description: Option<&'static str>` added, but 0/338 populated: real APG equipment corpus has zero `DESC:` tokens on any equipment row at all — an even harder ceiling than CRB's; see ## Open blockers |
| 6.5 Spell content completion: full text (CRB) | complete | crb-field-completion-cycle | 96d085f | Every present CRB spell (652/652) now carries the fullest corpus text (.MOD full text 623/652, base-record complete-for-variant text 29/652) instead of the pre-cycle first-sentence truncation. ACG still open |
| 6.5 Spell content completion: full text (APG) | returned-to-backlog (87.9%) | apg-content-completion-cycle | (pending — see Cycle log) | 261/297 (87.9%) carry full SRD/PRD text sourced from matching `.MOD` corpus records; 41/297 lack a `SCHOOL:`/`CLASSES:` token at all (real corpus gap) so cannot reach 100%. ACG still open |
| 7.1 appendToCharacter Tauri command | not-started | — | — | gated on 5/6 |
| 7.2 recomputeCharacter Tauri command | not-started | — | — | — |
| 7.3 reSaveCharacter Tauri command | not-started | — | — | — |
| 7.4 Add Weapon/Armor/Spell onClick | not-started | — | — | — |
| 7.5 loadout hardcoding removed | not-started | — | — | — |
| 8.1 Final criterion scan | not-started | — | — | fires LAST |
| 8.2 Architecture closure pipeline | not-started | — | — | fires LAST |
| 8.3 Release notes | not-started | — | — | fires LAST |
| 8.4 Build version increment | not-started | — | — | fires LAST |

## TODO (deterministic seed; populated by cycle 0 of Epic 2)

- 6.2, 6.3 (Epic 6, ACG/Bestiary-1 remainder — CRB + APG portions complete, see ## DONE)
- 6.5 (Epic 6, ACG/Bestiary-1 remainder — CRB complete at 100%; APG at 87.9% real-corpus ceiling, see ## Open blockers)
- 6.4 (Epic 6, description field — returned-to-backlog for both CRB and APG, both hit real corpus ceilings; ACG/Bestiary-1 unassessed; see ## Open blockers)
- 7.1, 7.2, 7.3, 7.4, 7.5 (Epic 7, unwired workflows + Tauri surface)
- 8.1, 8.2, 8.3, 8.4 (Epic 8, closure epilogue)

## DONE

- 1.1 Source-code identifier audit — cycle `identifier-audit-cycle`, commit `3c3882a`, receipt `./artifacts/epic_1/identifier-audit-cycle_receipt.md`
- 1.2 Per-cycle tests pass — cycle `identifier-audit-cycle`, commit `3c3882a`, receipt `./artifacts/epic_1/identifier-audit-cycle_receipt.md`
- 2.1 board reachable — cycle `operator-pre-launch-cycle`, receipt `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
- 2.2 branch pushed — cycle `operator-pre-launch-cycle`, receipt `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
- 2.3 SD-23 closure PR merged (Tier-1 launch gate CONFIRMED) — cycle `operator-pre-launch-cycle`, receipt `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
- 2.4 working tree clean — cycle `operator-pre-launch-cycle`, receipt `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
- 2.5 doctrines loaded (known non-blocking skills gap re-confirmed, see `./missing_skills.md`) — cycle `operator-pre-launch-cycle`, receipt `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
- 3.1 Wired-Integration Audit (read-only sweep, codebase-wide) — cycle `wired-integration-audit-cycle`, receipt `./artifacts/epic_3/wired-integration-audit.md`
- 3.2 forbidden tokens remediation (zero-tolerance clean; 1 finding deferred) — cycle `wired-integration-audit-cycle`, receipt `./artifacts/epic_3/wired-integration-audit.md`
- 3.3 noop handlers remediation (0 findings) — cycle `wired-integration-audit-cycle`, receipt `./artifacts/epic_3/wired-integration-audit.md`
- 3.4 mock leaks + "Would …" remediation (0 findings) — cycle `wired-integration-audit-cycle`, receipt `./artifacts/epic_3/wired-integration-audit.md`
- 4.1 per-class audit CRB (Fighter+Wizard scope — this cycle's granted file-touch set per `loop-instruction.md §2.4`) — cycle `fighter-wizard-audit-cycle`, receipt `./artifacts/epic_4/fighter-wizard-audit-cycle_receipt.md`
- 4.2 per-class audit APG (all 6 real APG classes: chassis fully wired + verified correct; 0 named features wired; 0 live pilot_compute integration; 0 level_up modules) — cycle `apg-class-audit`, receipt `./artifacts/epic_4/apg-class-audit_cycle_receipt.md`
- 4.3 per-class audit ACG (all 10 real ACG classes, corrected roster: chassis fully wired + verified correct; 0 named features wired; 0 live pilot_compute integration; 0 level_up modules) — cycle `acg-class-audit`, receipt `./artifacts/epic_4/acg-class-audit_cycle_receipt.md`
- 4.4 remediation plan enumeration (per-class `class_features_wired`/`expected` gap list with fix cycle-id, consuming 4.1-4.3) — cycle `remediation-and-deferral-cycle`, receipt `./artifacts/epic_4/remediation-and-deferral-cycle_receipt.md`, artifact `./artifacts/epic_4/remediation-plan.md`
- 4.5 APG/ACG-class multiclass scope decision (formal deferral record: Epic 5 Fighter+Wizard-only, unaffected; APG/ACG multiclass deferred to follow-on bundle) — cycle `remediation-and-deferral-cycle`, receipt `./artifacts/epic_4/remediation-and-deferral-cycle_receipt.md`, artifact `./artifacts/epic_4/apg-acg-multiclass-deferred.md`
- 5.1 F+W multiclass dispatch (Wizard-side gap fixed: `wizard_level_in_mix` + widened `level_up::fighter`/`level_up::wizard` entry gates; BAB/save chassis stacking pre-existing) — cycle `fighter-wizard-multiclass-dispatch-cycle`, commit `0068818`, receipt `./artifacts/epic_5/fighter-wizard-multiclass-dispatch-cycle_cycle_receipt.md`
- 5.2 30 character-advancement cycles (deterministic walk: 10 solo-Fighter + 10 solo-Wizard + 10 split-advance, `tests/sd24_multiclass_deterministic.rs`) — cycle `fighter-wizard-multiclass-deterministic-cycle`, commit `e8e97c2`, receipt `./artifacts/epic_5/fighter-wizard-multiclass-deterministic-cycle_cycle_receipt.md`, artifact `./artifacts/epic_5/multiclass-fixture.md`
- 5.3 integration test consumes ingested content (`tests/sd24_multiclass_integration.rs` cross-checks the multiclass dispatch against `class_tables()`'s ingested content; added `class_tables::good_saves_for` accessor, refactored `pilot_compute.rs`'s `multiclass_good_saves` to delegate to it instead of a hardcoded duplicate) — cycle `integration-test-cycle`, commit `b503c47`, receipt `./artifacts/epic_5/integration-test-cycle_receipt.md`
- 5.4 multiclass dispatch passes the four-check audit (new standing regression test `tests/sd24_multiclass_dispatch_audit.rs` scopes the four-check audit to the multiclass dispatch production files; genuine RED surfaced 19 pre-existing "packet placeholder" anti-fabrication-idiom hits, bucketed clean per criterion 3.1's precedent) — cycle `multiclass-dispatch-four-check-audit`, commit `79162c1`, receipt `./artifacts/epic_5/multiclass-dispatch-four-check-audit_cycle_receipt.md`
- 5.5 APG/ACG multiclass deferred (Epic-5-side echo of criterion 4.5's decision; documentary-only, no production/test change; confirmed by commit inspection that no APG/ACG touch landed anywhere in Epic 5's delivery — Epic 5 is now fully complete) — cycle `apg-acg-multiclass-deferred`, commit `bb7dd74`, receipt `./artifacts/epic_5/apg-acg-multiclass-deferred_cycle_receipt.md`, artifact `./artifacts/epic_5/apg-acg-multiclass-deferred.md`
- 6.1 Equipment coverage audit (read-only; CRB equipment 100% record-ingested (2977/2977), CRB spells 96.6% (652/675); APG/ACG equipment+spells still at original SD-22 bootstrap samples; Bestiary 1 has zero equipment ingestion, no module exists; `EquipmentTableEntry` has no `weight`/`description` field in any book; no spell in any book carries full SRD/PRD text) — cycle `equipment-coverage-audit-cycle`, commit `b8b9d40`, receipt `./artifacts/epic_6/equipment-coverage-audit_cycle_receipt.md`, artifact `./artifacts/epic_6/equipment-coverage-matrix.md`
- 6.2 Equipment content completion: cost, CRB scope (re-audit found no real record-coverage gap: CRB equipment 2977/2977 and spells 652/652 were already 100% complete; 6.1's "675 real/96.6%" spell figure was a measurement error, corrected — see `## DISCOVERED`; ACG/Bestiary-1 record-coverage remainder is unstarted, separate follow-on cycles) — cycle `crb-field-completion-cycle`, commit `96d085f`, receipt `./artifacts/epic_6/crb-field-completion-cycle_cycle_receipt.md`, artifact `./artifacts/epic_6/content-completion-log.md`
- 6.2 Equipment/spell content completion: cost/record coverage, APG scope (338/338 equipment records [corrected from 341], 297/297 spell records [corrected from 298], both 100%) — cycle `apg-content-completion-cycle`, receipt `./artifacts/epic_6/apg-content-completion-cycle_receipt.md`. ACG/Bestiary-1 equivalents not covered by either cycle (see `## DISCOVERED` epic-6-scope-clarification).
- 6.3 Equipment content completion: weight, CRB scope (`weight_lbs: Option<f64>` added to `EquipmentTableEntry`, populated from the real corpus `WT:` token to the honest "where applicable" ceiling: 2011/2977 = 67.5%, `cr_equipmods.lst` genuinely has 0 `WT:` tokens; ACG/Bestiary-1 unstarted) — cycle `crb-field-completion-cycle`, commit `96d085f`, receipt `./artifacts/epic_6/crb-field-completion-cycle_cycle_receipt.md`, artifact `./artifacts/epic_6/content-completion-log.md`
- 6.3 Equipment content completion: weight, APG scope (`weight: Option<f64>` field added, populated 319/338 [94.4%] from real `WT:` tokens, 19 real corpus gaps honestly `None`) — cycle `apg-content-completion-cycle`, receipt `./artifacts/epic_6/apg-content-completion-cycle_receipt.md`. ACG unchanged.
- 6.5 Spell content completion: full text, CRB scope (every present CRB spell, 652/652, now carries the fullest corpus text — 623/652 from the `<Name>.MOD` record's full `DESC:`, 29/652 from the base record's own complete-for-that-variant text — replacing the pre-cycle first-sentence truncation; ACG unstarted) — cycle `crb-field-completion-cycle`, commit `96d085f`, receipt `./artifacts/epic_6/crb-field-completion-cycle_cycle_receipt.md`, artifact `./artifacts/epic_6/content-completion-log.md`
- 6.5 Spell content completion: full text, APG scope, returned-to-backlog at 87.9% (`full_text: bool` field added; 261/297 [87.9%] carry full SRD/PRD text sourced from matching `.MOD` corpus records; remaining 36 lack a matching `.MOD` record or a `SCHOOL:`/`CLASSES:` token at all, a real corpus gap) — cycle `apg-content-completion-cycle`, receipt `./artifacts/epic_6/apg-content-completion-cycle_receipt.md`; see `## Open blockers`. ACG unstarted.

(Criterion 6.4, equipment description, is intentionally NOT listed here as "done" for either CRB or APG — both hit a real, non-self-healable corpus ceiling short of the strict-100% bar; tracked in `## TODO` and `## Open blockers` instead, per `crb-field-completion-cycle`'s own established convention of only moving a criterion-row to `## DONE` once it reaches its threshold or a formal deferral, not merely once a cycle has run against it.)

## DISCOVERED

- `2026-07-21T00:00:00Z` | epic-1 | criterion-1.1 | governance-pattern-bug | The canonical identifier-discipline grep in `epic-breakdown.md`/`loop-instruction.md §2.3 step 4` (`` \b(sd(16|19|22|23|24)_|SD(16|19|22|23|24)_|Sd(16|19|22|23|24)|t_[0-9a-f]{8,})\b `` ) has a trailing-`\b` boundary bug: `\b` never matches between an underscore and a following word character, so the pattern silently fails to catch any bundle-tag identifier that is followed by more identifier characters (e.g. `sd19_class_catalog`) and only matches a bare standalone token like `sd24_` on its own. Live proof: the literal command returned 0 hits against the pre-remediation repo while 6 production Rust modules, a bundled Tauri resource directory, and 13 constants were genuinely bundle-tag-prefixed. A corrected pattern (drop the trailing `\b`) is now load-bearing in `tests/sd24_identifier_discipline_audit.rs`. | suggested: harden `governance/identifier-discipline.md`'s canonical pattern (or `epic-breakdown.md`/`loop-instruction.md`'s embedded command) in a follow-up governance-doc cycle — out of this cycle's granted write scope.
- `2026-07-21T00:00:00Z` | epic-3 | criterion-3.1 | epic-3-originated | Codebase-wide four-check wired-integration audit found one genuine, pre-existing finding: `apps/desktop/src-tauri/src/main.rs`'s `load_pilot_shell_snapshot` Tauri command + `apps/desktop/src/boundary/loadPilotShellSnapshot.ts` unconditionally return hardcoded fixture data (`case_id: "ge07-e1-scaffold-placeholder"`) regardless of real character state — fixture-only data in a production command path. Legacy GE-07 scaffolding (predates the `SD-<N>` bundle-tag convention), consumed only by the SD-11 internal tester workbench which already labels it honestly. Recorded in `risks-and-open-questions.md §5 Deferrals` (not the Stubs Registry — accidental finds without operator-verbatim justification route to risks-and-open-questions.md per the registry's own rule) rather than remediated in-cycle, since real remediation needs an operator design decision (what a "headless-core-backed" pilot shell snapshot computes, from what input contract) outside an audit-and-mechanical-remediation cycle's granted scope. A standing regression test (`tests/sd24_wired_integration_audit.rs`) tolerates only this named finding. | suggested: epic-3-or-later Wired Integration Cleanup follow-on, or an operator-pinned design cycle for the GE-07 pilot-shell-snapshot feature.
- `2026-07-21T00:10:00Z` | epic-4 | criterion-4.1 | epic-4-path-correction | `content-unit-inventory.md` §3.1 names the per-class wired-feature-count location as `src/rules_core/rules_tables/<book>/class_<name>.rs` (e.g. a hypothetical `class_fighter.rs`/`class_wizard.rs`). No such per-class file exists anywhere in the repo. Real per-class wiring is split across `rules_tables/crb/class_tables.rs` (generic BAB/save table, shared by all 11 CRB classes) and `level_up/<class>.rs` (the class-specific `LevelUpPlan` composer, one file per class, landed by SD-20 Epic 7). This cycle's audit (and `loop-instruction.md §2.4`'s own file-touch partition table) is scored against the real modules. | suggested: correct `content-unit-inventory.md` §3.1's row template in a documentation-only follow-up cycle.
- `2026-07-21T00:15:00Z` | epic-4 | criterion-4.1 | epic-4-follow-on | Fighter + Wizard audited this cycle (the pair Epic 5's multiclass work is scoped to); the other 9 CRB classes (Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger) each already have a landed `level_up/<class>.rs` module (SD-20 Epic 7) that has not yet had this same class_spell.\*-vs-class_feature.\*-prefix coverage audit applied. The Wizard gap found this cycle (a later `pilot_compute.rs` grounding landing after its `level_up` module and never being added to that module's explanation-id filter) is a plausible recurring bug class — Sorcerer/Cleric/Bard all have their own multi-source spell-baseline groundings that could have the identical shape. Not gating Epic 5 (Fighter+Wizard-only multiclass scope), so LOW priority. | suggested: follow-on Epic 4 cycle(s), one `level_up/<class>.rs` file at a time per the file-touch partition, if full-11-class breadth is later required by the operator.
- `2026-07-21T00:20:00Z` | epic-4 | criterion-4.2 | epic-4-path-correction | Task brief named `src/rules_core/rules_tables/apg/class_tables.rs` as the audit target. No such file exists — unlike CRB (which really does consolidate into a single `crb/class_tables.rs`), APG has six separate per-class files (`class_alchemist.rs`, `class_cavalier.rs`, `class_inquisitor.rs`, `class_oracle.rs`, `class_summoner.rs`, `class_witch.rs`), each already landed by SD-22 Epic 3, plus the shared book-level shape (`ClassTableRow`, `ApgClassId`, `class_chassis_resolve`) in `apg/mod.rs`. This cycle's new coverage-audit code (`ApgClassId::ALL`, `class_coverage`, `coverage_report`) was added to `apg/mod.rs`. | suggested: correct `content-unit-inventory.md` §3.1's implicit assumption that every book uses the same per-class-file-vs-consolidated-file shape, in a documentation-only follow-up cycle (same family as the epic-4-path-correction entry above for CRB).
- `2026-07-21T00:25:00Z` | epic-4 | criterion-4.2 | epic-4-follow-on | All six APG classes are chassis-only: zero named class features wired (Alchemist 0/24, Cavalier 0/16, Inquisitor 0/19, Oracle 0/19, Summoner 0/17, Witch 0/7 — corpus-derived expected counts, methodology in `rules_tables::apg::mod`'s `ApgClassCoverage` doc comment), zero live `pilot_compute.rs` integration (proven empirically via the honest `class_chassis.unsupported` diagnostic, not a fabricated gap), and zero `level_up::<class>` modules. This is the exact condition `loop-instruction.md §4.2`'s hard-stop row already anticipates and pre-resolves (defer APG/ACG-class multiclass to a follow-on bundle); no blocker raised. Feeds directly into criterion 4.4 (remediation plan) and 4.5 (APG/ACG multiclass deferral decision). | suggested: criterion 4.4 (remediation plan enumeration) should fold this cycle's per-class gap lists in; criterion 4.5 formalizes the deferral this finding requires.
- `2026-07-21T00:30:00Z` | epic-4 | criterion-4.3 | epic-4-path-correction | Task brief named `src/rules_core/rules_tables/acg/class_tables.rs` as the audit target. No such file exists — like APG, ACG has one file per class (`class_arcanist.rs` ... `class_warpriest.rs`), each already landed by SD-22 Epic 4, plus the shared book-level shape (`ClassTableRow`, `AcgClassId`, `class_chassis_resolve`) in `acg/mod.rs`. This cycle's new coverage-audit code (`AcgClassId::ALL`, `class_coverage`, `coverage_report`) was added to `acg/mod.rs`, mirroring 4.2's identical correction and fix shape. | suggested: fold into the same documentation-only follow-up cycle already suggested for 4.1/4.2's path corrections.
- `2026-07-21T00:30:00Z` | epic-4 | criterion-4.3 | roster-correction | `epic-breakdown.md`'s criterion 4.3 header text lists "Alchemist-side" as one of the ten ACG classes and omits "Slayer." Both stale: no real `CLASS:Alchemist` record exists in `acg_classes.lst` (Alchemist is APG-only content), and Slayer does have a real record — the identical roster-defect shape SD-22 already resolved for APG's Gunslinger/Magus, and already documented in `rules_tables::acg::mod.rs`'s own doc comment predating this cycle. This audit scored against the real, corrected 10-class roster. | suggested: correct `epic-breakdown.md`'s criterion 4.3 header text in the same documentation-only follow-up cycle as the path-correction items above.
- `2026-07-21T00:35:00Z` | epic-4 | criterion-4.3 | mid-cycle-course-correction | This cycle initially built and (pre-commit) reverted real production wiring — ten new `level_up::acg_<name>.rs` modules composing each class's real, already-tested chassis table into `compute_level_up_grants` — reasoning that unreachable-but-real data was a fixable gap. A pre-commit `git fetch` surfaced the sibling criterion 4.2 (APG) cycle's already-landed, audit-only precedent (chose not to remediate the identical finding, citing `epic-breakdown.md`'s own Epic 4 purpose statement and `acceptance-and-verification.md` CG-03 as scoping 4.1–4.3 to audit, not remediation). To keep the bundle's roll-up matrix internally consistent across the two sibling criteria, this cycle reverted the production wiring and mirrored 4.2's exact audit-only shape instead. No production behavior changed as a result of this cycle; only the coverage-report API and its tests/docs. | suggested: if a future bundle decides to wire the chassis-only pillar for APG/ACG classes (a genuinely small, honest, non-fabricated change), do it for both books in the same cycle for consistency, not one book at a time.
- `2026-07-21T00:35:00Z` | epic-4 | criterion-4.3 | epic-4-follow-on | All ten ACG classes are chassis-only: zero named class features wired (Arcanist 0/9, Bloodrager 0/19, Brawler 0/14, Hunter 0/21, Investigator 0/95, Shaman 0/10, Skald 0/20, Slayer 0/15, Swashbuckler 0/29, Warpriest 0/18 — corpus-derived expected counts, methodology in `rules_tables::acg::mod`'s `AcgClassCoverage` doc comment), zero live `pilot_compute.rs` integration, and zero `level_up::<class>` modules. Identical shape to APG's 4.2 finding. Feeds directly into criterion 4.4 (remediation plan) and 4.5 (APG/ACG multiclass deferral decision), jointly with APG. | suggested: criterion 4.4 should fold both APG's and ACG's per-class gap lists in together; criterion 4.5 formalizes one joint deferral decision for both books.
- `2026-07-21T00:40:00Z` | epic-5 | criterion-5.1 | epic-5-follow-on | `level_up::compute_level_up_grants`'s top-level dispatcher (`src/rules_core/level_up.rs`, outside this cycle's granted file-touch set) still returns an honestly-empty `LevelUpPlan::default()` for any 2+-class `class_levels` mix, including the now-supported Fighter+Wizard combination — it never routes to `fighter::compute_fighter_level_up_grants` / `wizard::compute_wizard_level_up_grants` for a mix. This cycle's tests call each per-class function directly (both already `pub`), which is unambiguous and correct per-class, but any real caller that only calls the single top-level `compute_level_up_grants` entry point would still see an empty plan for a multiclass character. Reconstructing an unambiguous "from" state per class from a single plain `from_level`/`to_level` pair is not decidable in general (which of the two classes changed this step is not recoverable from that shape alone). | suggested: a follow-on Epic 5 or Epic 7 cycle that either widens `compute_level_up_grants`'s signature to accept explicit per-class from/to sub-levels, or adds a dedicated `compute_multiclass_level_up_grants` entry point in `level_up.rs` composing both per-class functions, once the real Tauri command surface (Epic 7) needs to drive a multiclass level-up end to end.
- `2026-07-21T01:00:00Z` | epic-5 | criterion-5.2 | epic-5-task-brief-drift | This cycle's invocation carried forward criterion 5.1's own granted file set verbatim (`pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, plus 5.1's 3 already-landed test files) rather than criterion 5.2's real target named by `epic-breakdown.md`'s own criterion 5.2 row and `content-unit-inventory.md`: `tests/sd24_multiclass_deterministic.rs`. Treated the epic-breakdown/content-unit-inventory file target as ground truth per the harness's stale-plan-vs-reality correction policy; the 3 source files were only transiently reverted-then-restored to construct this cycle's RED demonstration, never permanently changed. | suggested: if a future cycle's invocation text is templated from the prior cycle's own "Files/area you own" block, regenerate it from the current criterion's own `epic-breakdown.md` row instead of copying the previous cycle's block forward.
- `2026-07-21T01:15:00Z` | epic-5 | criterion-5.3 | epic-5-task-brief-drift-recurrence | This cycle's invocation again carried forward criterion 5.1's own granted file set verbatim (`pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, plus 5.1's 3 test files) rather than criterion 5.3's real target — this is the exact drift the prior cycle's `epic-5-task-brief-drift` entry already flagged and predicted would recur if invocation file lists are templated from the previous cycle's block instead of regenerated from the current criterion's own `epic-breakdown.md` row. Corrected against `epic-breakdown.md`'s criterion 5.3 row (cycle artifact `./artifacts/epic_5/integration-test-cycle_receipt.md`) and `acceptance-and-verification.md`'s own verification command (`cargo test --locked --test sd24_multiclass_integration`), both naming a brand-new `tests/sd24_multiclass_integration.rs`. `pilot_compute.rs` happened to be genuinely in-scope this cycle (a real duplicate-data defect was found and fixed there), but the granted test-file trio was not touched at all. | suggested: the dispatcher/invocation-generation step should read each cycle's own file set fresh from `epic-breakdown.md` + `content-unit-inventory.md` at dispatch time rather than reusing a template block across cycles — this is now a repeated (2x) pattern, not a one-off.
- `2026-07-21T01:30:00Z` | epic-5 | criterion-5.4 | epic-5-task-brief-drift-3rd-occurrence | This cycle's invocation carried forward criterion 5.1's own granted file set verbatim a **third** time (`pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, plus 5.1's 3 test files) rather than a file set appropriate to criterion 5.4's own ask ("Multiclass dispatch passes the four-check audit — dual-audit gate output captured"). Corrected against `epic-breakdown.md`'s criterion 5.4 row and `acceptance-and-verification.md`'s row ("5.4 multiclass dispatch four-check audit \| per-cycle artifact \| Dual-audit gate"), and against the prior cycle's own explicit next-cycle plan. No production file was modified this cycle (5.4 is audit-only); the new artifact is a standing regression test, `tests/sd24_multiclass_dispatch_audit.rs`, outside the stale granted set. This is now a **3x-repeated** occurrence of the identical drift, not a one-off or even a "recurrence" — the pattern spans every Epic-5 cycle after 5.1. | suggested: strong signal the invocation-generation step is templating the Epic-5 file-set block verbatim across all of Epic 5's cycles rather than reading each criterion's own row; regenerate per-cycle file grants from `epic-breakdown.md` + `content-unit-inventory.md` at dispatch time, or (if Epic 5 is otherwise closing after 5.5) treat this as closed learning for the next epic's cycles rather than a live blocker.
- `2026-07-21T01:45:00Z` | epic-5 | criterion-5.5 | epic-5-task-brief-drift-4th-occurrence-epic-5-closes | This cycle's invocation carried forward criterion 5.1's own granted file set verbatim a **fourth** time (`pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, plus 5.1's 3 test files) rather than a file set appropriate to criterion 5.5's own documentary ask ("APG/ACG-class multiclass deferral — cycle artifact `./artifacts/epic_5/apg-acg-multiclass-deferred.md`"). Corrected against `epic-breakdown.md`'s criterion 5.5 row and `acceptance-and-verification.md`'s row 32 ("n/a (deferral artifact)"). No production or test file was touched this cycle — the criterion is documentary-only, matching criterion 4.5's own precedent. This is now a **4x-repeated** occurrence, spanning every Epic-5 cycle after 5.1 (4 of the epic's 5 criteria) — but Epic 5 is now fully complete (5.1-5.5 all `complete`), so this is retrospective closed learning rather than a live blocker within this epic. | suggested: closed learning for Epic 5; if the invocation-generation mechanism is shared across epics, apply the same fix (regenerate per-cycle file grants from `epic-breakdown.md` + `content-unit-inventory.md` at dispatch time) before Epic 6 dispatches, to avoid the identical drift recurring there.
- `2026-07-21T02:00:00Z` | epic-6 | criterion-6.1 | epic-6-path-correction | Task brief named a non-existent `src/rules_core/rules_tables/beastiary1/equipment_tables.rs`. No equipment concept exists anywhere in the `beastiary1` module (monster-stat-block-only, 41 monsters across 8 SD-22 Epic 5 subsets); the real corpus does have 7 active Bestiary-1-specific equipment records (`b1_equip_general.lst` x2, `b1_equip_arms_armor.lst` x3, `b1_equip_magic_items.lst` x2) that have never been ingested. No file created this cycle (audit-only scope; fabricating rows without an ingest design decision would violate the no-stub doctrine). | suggested: epic-6 remediation cycle (6.2 family) creates a new, small `beastiary1/equipment_tables.rs` ingesting all 7 real records.
- `2026-07-21T02:05:00Z` | epic-6 | criterion-6.1 | epic-6-schema-gap | `EquipmentTableEntry` (shared shape across crb/apg/acg) has no `weight` field and no `description` field at all — criteria 6.3 and 6.4 are schema-change-then-populate work, not per-row fill-in work, in every book that has an equipment table. | suggested: 6.3's cycle should land the `weight` field (all three books, one PR) before any per-row weight population; 6.4 likewise for `description`, and needs its own sourcing-methodology decision first since PF1 equipment LST records generally carry no `DESC:` token the way spells do.
- `2026-07-21T02:10:00Z` | epic-6 | criterion-6.1 | epic-6-schema-gap | No spell in any book (652 CRB + 4 APG + 4 ACG = 660 present records) carries full SRD/PRD text — every present description is the corpus's short `DESC:` summary. The real corpus's full text lives on a separate `<Name>.MOD` record with its own longer `DESC:` token this repo's spell ingester has never read. Criterion 6.5 is therefore a re-ingest of every already-landed spell row, not purely additive work on newly-ingested ones. | suggested: 6.5's cycle designs a `.MOD`-record full-text extraction path in the LST spell parser before any full-text population can start; scope explicitly includes the 660 already-present rows.
- `2026-07-21T02:15:00Z` | epic-6 | criterion-6.1 | epic-6-doc-staleness | `crb/equipment_tables.rs`'s own module doc comment ("Bootstrap coverage: one representative item per category... Exhaustive per-category coverage is the loop's job") is stale — the real data has been 100% record-complete (2977/2977) since a later SD-17 `KEY:`-merge-dedup pass, contradicting its own doc comment. Low priority (does not affect correctness), but worth a documentation-only follow-up alongside the other stale-doc-comment corrections already queued from Epic 4. | suggested: fold into the same documentation-only follow-up cycle already suggested for Epic 4's path corrections. (Corrected in-cycle by `crb-field-completion-cycle`, which rewrote this doc comment; see the criterion-6.1-correction entry below.)
- `2026-07-21T02:30:00Z` | epic-6 | criterion-6.2/6.3/6.4/6.5 | epic-6-scope-clarification | This cycle's granted file-touch set (`apg/equipment_tables.rs`, `equipment_data/`, `spell_list.rs`) names only APG's own files, but the dispatched criteria (6.2-6.5) are epic-level, spanning CRB+APG+ACG+Bestiary-1 per `epic-breakdown.md`'s Epic 6 purpose statement. Since `EquipmentTableEntry`/`SpellListEntry` are separate types defined per-book (confirmed by inspection: no shared struct), this cycle scoped its actual work to APG only, leaving CRB/ACG/Bestiary-1 equivalents to sibling concurrent cycles (multiple other `.claude/worktrees/wf_0204a5d8-425-*` directories were observed running alongside this one, consistent with `/batch`'s file-touch-partition concurrency model — confirmed live: the CRB sibling did indeed land concurrently as `crb-field-completion-cycle`, commit `96d085f`). | suggested: future Epic 6 cycle dispatches should name the target book explicitly in the criterion label (e.g. "6.2 (APG)", "6.2 (ACG)") rather than relying on the file-touch set alone to disambiguate scope.
- `2026-07-21T03:00:00Z` | epic-6 | criterion-6.2 | epic-6-audit-correction | Criterion 6.1's own "CRB spells 652/675 = 96.6%" record-coverage figure was a measurement error, not a real gap. The `675` came from `grep -vE '\\.MOD' | awk -F'\\t' '{print $1}' | sort -u | wc -l` against `cr_spells.lst`, which (a) counted 12 `.COPY=`-suffixed rows as distinct spell names instead of merging them into their base spell (the same merge rule equipment records already apply — verified: equipment's own 2977 total record-matches 100% against the Rust data using the merge rule, so the omission was spell-specific), and (b) counted 10 `TEMPBONUS:`/`TEMPVALUE:` sub-choice records (e.g. `Resist Energy (Acid)`, `Desecrate (Standard)` — real corpus rows, but game-engine choice-mechanic sub-options with no `SCHOOL:`/`CLASSES:` token, not independent spells) plus 1 `SOURCELONG:` header line as spell names. The real, level-and-school-bearing spell count is 652, exactly matching the pre-existing `SPELL_LIST` ingest — CRB spell record coverage was already 100% entering this cycle. Verified via an independent Python corpus parser (kept in scratchpad, not shipped) whose merge rule mirrors the production `.COPY=`-strip logic; cross-checked by matching all 652 existing `SPELL_LIST` keys 1:1 against the corrected corpus set with zero mismatches. | suggested: correct `epic-breakdown.md`/`content-unit-inventory.md`'s own citations of "675" if any exist, in the same documentation-only follow-up cycle already queued for Epic 4/Epic 6's other path/doc corrections.
- `2026-07-21T02:35:00Z` | epic-6 | criterion-6.2 | epic-6-audit-correction | The criterion 6.1 audit's documented APG equipment record count (341 = 94+76+171) and APG spell count (298) both have a small, real off-by-N error — the same class of `SOURCELONG:`-header-line/near-duplicate-record miscount as the CRB spell-count correction above, independently found. Equipment: each of the three `apg_equip_*.lst` files carries exactly one `SOURCELONG:...` header line that is not itself an equipment record but is not a `#`-comment or blank line either — the audit's `grep -vE '^\\s*#|^\\s*$' | wc -l`-style count double-counted it as a record in all three files, so the real count is 338 (93+75+170), not 341. Spells: the real corpus has one genuine duplicate base record (`Resounding Blow`, `apg_spells.lst:10` and `:196`, the second an errata-reprint with a wider `CLASSES:` token) that a naive `sort -u`-on-name-column dedup should have caught but the two lines are byte-distinct (differing whitespace) so it didn't — real deduplicated-by-name count is 297, not 298. Both corrections are reflected in this cycle's `field_coverage_report()`/`spell_coverage_report()` `records_expected` values and the corresponding `tests/sd24_equipment_coverage_audit.rs` assertions. | suggested: re-verify the identical `SOURCELONG:`-header-miscount hazard against ACG's/Bestiary-1's own equipment-file counts and ACG's own spell-file count, in whichever sibling cycle owns those files next — the same audit methodology was applied uniformly across all books in the 6.1 cycle, so the same class of error may recur there too (CRB and APG have both now independently confirmed it does).
- `2026-07-21T03:00:00Z` | epic-6 | criterion-6.4 | epic-6-open-blocker | `decisions.md §5` requires "a full description... for all of these things" for equipment/armor/weapons with no "where applicable" carve-out (unlike cost/weight, which both have that qualifier). CRB equipment's `description` field, populated this cycle from the real corpus `DESC:` token, reaches an honest ceiling of 1821/2977 (61.2%) — the remaining ~39% (mostly slot-type markers in `cr_equip_general.lst`/`cr_equip_arms_armor.lst` such as `Belt`, `Ring`, `Wand`, `(Base)` weapon/armor templates, and 656/658 `cr_equipmods.lst` modifier records) carry no `DESC:` token in the real PCGen LST corpus at all. Per the no-stub-mvp doctrine, no description text was fabricated to close this gap. This is filed as an `## Open blockers` entry (not self-healable) per `loop-instruction.md §4.2`'s own anticipated row ("Epic 6 finds the strict-field-coverage threshold cannot be met within SD-24's cycle budget"). | suggested: operator decides between (1) accepting the LST-derived ceiling as criterion 6.4's closure bar, (2) authorizing a second-source ingestion pass against the printed CRB book text (out of this bundle's PCGen-LST-corpus scope), or (3) relaxing criterion 6.4's wording to carry the same "where applicable" qualifier cost/weight already have.
- `2026-07-21T02:40:00Z` | epic-6 | criterion-6.4 | epic-6-schema-gap-confirmed-not-closeable | Adding the `description: Option<&'static str>` field to APG's `EquipmentTableEntry` (this cycle) and populating it from the real corpus finds **zero** `DESC:` tokens on any of APG's 338 equipment records (word-boundary, tab-preceded search across all three `apg_equip_*.lst` files) — an even harder ceiling than CRB's own 61.2% (see the CRB `epic-6-open-blocker` entry above), confirming the 6.1 audit's suspicion that PF1 equipment records generally carry no prose description in the LST data at all (equipment flavor text lives in the printed book's prose). This is a hard, non-self-healable ceiling on criterion 6.4's equipment-description coverage per `loop-instruction.md §4.2`'s "strict-field-coverage threshold cannot be met within SD-24's cycle budget" row — not something a future cycle can close by trying harder at parsing, only by an operator sourcing decision (transcribing/licensing prose from the printed book, or relaxing the threshold). | suggested: operator decision needed — see `## Open blockers`. ACG's/Bestiary-1's equipment corpora should be checked for the same zero/low-DESC-token pattern (plausible, since it's a PCGen-wide equipment-authoring convention, not a book-specific gap) and folded into the same consolidated blocker rather than re-discovered independently.
- `2026-07-21T02:45:00Z` | epic-6 | criterion-6.4 | epic-6-doc-staleness | `apg/equipment_tables.rs`'s pre-existing doc comment claimed "there is no `Bomb`/`Acid Bomb` record in any `apg_equip_*.lst` file" (predates this cycle). The full-corpus ingest finds this only half true: `apg_equip_arms_armor.lst` does carry one real `Bomb` record — not a purchasable consumable (no `COST:`/`WT:`/`DESC:` token), but a `PROFICIENCY:WEAPON|Bomb` to-hit template the Alchemist's bomb attack references. Corrected in-cycle; there is still no fabricated per-damage-type (`Acid Bomb`, etc.) row, and bomb damage remains a class-feature formula. | suggested: none — already remediated in this cycle's own commit.
- `2026-07-21T02:50:00Z` | epic-6 | criterion-6.2 | epic-6-codegen-followon | Both this cycle and the sibling `crb-field-completion-cycle` (and, per its receipt, the prior 6.1 cycle) independently used their own one-off ad-hoc scripts against the real PCGen corpus to generate Rust source, rather than the pre-existing, real, in-repo LST tokenizer at `src/pcgen_import/lst_parser/equipment.rs`/`spell.rs` (SD-17 Slice B-5, already used elsewhere in the codebase for equipment-effects parity tests). That tokenizer emits raw tag tokens, not the `cost_gp`/`weight`/`description` semantic mapping this ingest needed, and wiring it as a build-time codegen step was judged out of this cycle's practical time budget. Two independently-written scripts for the same class of problem is itself a signal: as Epic 6 does more of this per-book, per-field ingest work, a shared, tested, `pcgen_import`-backed codegen path (parse -> semantic-map -> emit Rust literals, as a `tests/`-gated generator or a `build.rs`) would reduce the risk of each cycle's own ad-hoc script silently diverging in behavior (e.g. differing `.COPY=`/`.MOD` handling, differing float-literal formatting, differing entity-decoding). | suggested: a dedicated Epic 6 (or Epic 8 closure-epilogue) cycle to build this shared codegen path once, before further per-book equipment/spell ingest cycles multiply the number of independent ad-hoc scripts.
- `2026-07-21T03:00:00Z` | epic-6 | criterion-6.5 | epic-6-audit-false-positive | This cycle's real, full-text CRB spell ingestion (`Plant Growth`: "creatures must **hack** or force a way through") collided with `tests/sd24_wired_integration_audit.rs`'s zero-tolerance forbidden-token check (`hack` is one of the six zero-tolerance tokens). Self-healed per `loop-instruction.md §4.1` (single-token collision, benign on review): added one narrow, named bucket exclusion matching this exact corpus phrase to the standing test, mirroring the file's own pre-existing `placeholder`-bucketing pattern; the raw (unbucketed) grep from `loop-instruction.md §2.3` step 4 still shows this one line as a literal hit, expected and documented, the same shape criterion 3.1's own precedent already established for raw-grep false positives on reviewed, benign content. No token was fabricated or removed from real spell text. The concurrently-running APG cycle independently hit and confirmed the identical false-positive class in its own audit run (same "hack"/`Plant Growth` line, since it's the same already-landed CRB text, plus confirmed its own diff is otherwise 100% clean) rather than re-discovering a new instance. | suggested: if more full-text spell/equipment ingestion lands in follow-on Epic 6 cycles (APG/ACG), re-scan for the same collision class (real PF1 rules text incidentally containing `STUB`/`MOCK`/`todo`/`fixme`/`hack` as an ordinary English word) before assuming a raw grep hit is a real stub; consider hardening the standing test to exclude corpus-text fields from the zero-tolerance scan entirely, the same way it already excludes test files.

## Cycle log

- **cycle_id:** identifier-audit-cycle (2026-07-21T00:00:00Z)
  **criterion:** 1.1 + 1.2 (Epic 1)
  **summary:** Ran the repo-wide identifier-discipline scan per criterion 1.1's own verbatim command; discovered the command's trailing-\b regex has a boundary bug that produces a false-clean 0-hit result. Ran a corrected (boundary-fixed) pattern instead, which found real bundle-tag leaks: 6 production Rust modules (`sd16_browser_handoff`, `sd19_class_catalog`, `sd19_corpus`, `sd19_equipment_catalog`, `sd19_race_catalog`, `sd19_spell_catalog`), a bundled Tauri resource directory (`resources/sd19_corpus_fixtures/`), and 13 `SD19_*_TEST` constants in `support_state_matrix.rs`. Remediated all of them (renamed files/modules/consts/resource dir, updated every call site, doc-comment citation, and `tauri.conf.json` path). Added `tests/sd24_identifier_discipline_audit.rs` as a standing regression test using the corrected pattern. RED→GREEN demonstrated live via `git stash`: stashed the remediation, confirmed the new test fails listing the real pre-remediation leaks, restored the remediation, confirmed the test (and the full `cargo test --locked --tests` root suite, and `cargo test --locked` in `apps/desktop/src-tauri`) all pass with 0 failures. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) are clean on the final diff.
  **commit:** `3c3882a`
  **receipt:** `./artifacts/epic_1/identifier-audit-cycle_receipt.md`
  **discovered:** 1 entry (governance-pattern-bug, see `## DISCOVERED`)
  **next:** Epic 2 (Operator Pre-Launch, criteria 2.1–2.5) is next in the deterministic seed.

- **cycle_id:** operator-pre-launch-cycle (2026-07-21)
  **criterion:** 2.1 + 2.2 + 2.3 + 2.4 + 2.5 (Epic 2, Operator Pre-Launch)
  **summary:** Re-verified all 5 items of `loop-instruction.md §1`'s pre-launch checklist plus `acceptance-and-verification.md`'s Epic-2 verification commands, live, right now (this is a re-confirmation gate mid-bundle, not the original first-launch check). Results: (2.1) `hermes kanban boards` lists `codex-tranche-5` (done=43) — PASS. (2.2) `git ls-remote origin tranche/5-2` resolves to `e433a63...`, matching local HEAD after a no-op fetch+rebase — PASS. (2.3, **Tier-1 launch gate**) `git fetch origin develop` then `git log origin/develop --oneline | head -5` shows `09e43c3` (SD-23 closure, PR #329) as the literal HEAD of `origin/develop` — **CONFIRMED, bundle is NOT blocked** — PASS. (2.4) `git status --porcelain` empty — PASS. (2.5) re-checked `hermes skills list`: `identifier-discipline` / `wired-integration-discipline` / `kanban-claude-code-execution-receipt` are still not installed as invocable skills — this reconfirms the gap already tracked non-blocking in `./missing_skills.md` (2026-07-21); the operative gate (the embedded dual-audit grep in `loop-instruction.md §2.3` step 4) was re-run against the diff since `merge-base HEAD origin/develop` and is clean (`OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`) — PASS with known caveat. Also cross-checked pre-launch item 7 (build counter): `develop`'s `package.json`/`tauri.conf.json` both still `0.5.97`, no drift from `decisions.md §3`'s captured value. No production/test files touched (verification-only cycle); no TDD RED/GREEN applicable.
  **commit:** `d6cddac`
  **receipt:** `./artifacts/epic_2/operator-pre-launch-cycle_receipt.md`
  **discovered:** 0 new entries (a minor stale-CLI-form nit in `acceptance-and-verification.md` line 22 — `hermes skills --profile god-emporer --list` errors; correct form is `hermes skills list` — noted in the receipt's Notes but not filed as a fresh `## DISCOVERED` entry since it doesn't change any criterion's disposition)
  **next:** Epic 3 (Wired-Integration Audit + Remediation, criterion 3.1 — read-only sweep) is next in the deterministic seed.

- **cycle_id:** wired-integration-audit-cycle (2026-07-21)
  **criterion:** 3.1 + 3.2 + 3.3 + 3.4 (Epic 3, Wired-Integration Audit + Remediation)
  **summary:** Ran the full four-check wired-integration audit codebase-wide (per `acceptance-and-verification.md` CG-02's "covers the entire codebase" requirement, broader than the per-cycle diff-scoped gate) against `apps/desktop/`, `apps/desktop/src-tauri/`, `src/` via `git grep` (tracked files only, so `src-tauri/target/` build artifacts are naturally excluded). Checks 2 (no-op onClick handlers), 3 (mock leaks), and 4 ("Would ..." strings) are 0 hits, repo-wide -- nothing to remediate for 3.3/3.4. Check 1's zero-tolerance alternatives (STUB/MOCK/not yet implemented/todo/fixme/hack) are also 0 hits repo-wide (confirmed with a supplementary case-insensitive TODO/FIXME sweep too). Check 1's noisy `placeholder` alternative produced 71 raw hits; manual per-hit review classified 70 as benign (HTML/JSX input-placeholder-text affordances, CSS `::placeholder`, or ordinary engineering prose using "placeholder" as a sentinel-value concept) and 1 genuine finding: `load_pilot_shell_snapshot` (Tauri command in `main.rs` + `loadPilotShellSnapshot.ts`) unconditionally returns hardcoded GE-07-era fixture data regardless of real state -- fixture-only data in a production command path. This is legacy scaffolding predating the SD-N convention, consumed only by the SD-11 internal tester workbench (which already labels it honestly), and its real fix needs an operator design decision outside this audit-and-mechanical-remediation cycle's scope -- deferred to `risks-and-open-questions.md §5 Deferrals` (not the Stubs Registry, since that requires operator-verbatim justification this accidental find doesn't have) per the self-heal path in `loop-instruction.md §4.1`. Wrote a standing regression test `tests/sd24_wired_integration_audit.rs` (5 tests: zero-tolerance tokens, placeholder-bucketed, no-op handlers, mock leaks, would-strings) that tolerates only the one named/documented finding and fails on anything new. RED->GREEN demonstrated live: disabled the documented-deferred-finding exclusion bucket, ran the test, it failed listing the real finding's 7 lines plus 9 previously-unbucketed `pilot_compute.rs` anti-fabrication-explanation lines (a legitimate design pattern using "packet placeholder" as an anti-fabrication assurance, not a stub); re-enabled the bucket and added a 4th bucket for the `pilot_compute.rs` pattern, ran again -- 5 passed, 0 failed. Full suite re-run: `cargo test --locked --tests` (root) 3955 passed/0 failed; `cargo test --locked` (`apps/desktop/src-tauri/`) 113 passed/0 failed. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) on the diff since `merge-base HEAD origin/develop` are clean (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`).
  **commit:** `8756340`
  **receipt:** `./artifacts/epic_3/wired-integration-audit.md`
  **discovered:** 1 entry (epic-3-originated, GE-07 pilot-shell-snapshot scaffold deferral, see `## DISCOVERED`)
  **next:** Epic 4 (Per-Class Coverage Audit + Plan, criterion 4.1) is next in the deterministic seed.

- **cycle_id:** fighter-wizard-audit-cycle (2026-07-21)
  **criterion:** 4.1 (Epic 4, Per-Class Coverage Audit — Fighter + Wizard scope)
  **summary:** This cycle's granted file-touch set (`src/rules_core/rules_tables/crb/class_tables.rs`, `src/rules_core/level_up/fighter.rs`, `src/rules_core/level_up/wizard.rs` — matching `loop-instruction.md §2.4`'s own partition table, which names only these two per-class files for Epic 4, not all 11 CRB classes epic-breakdown.md's criterion 4.1 header lists) bounds this cycle's audit to Fighter + Wizard, the two classes Epic 5's multiclass work is scoped to (operator directive 2026-07-21). Audited each class's real per-class-feature wiring (`level_up/<class>.rs`'s `LevelUpPlan` composer) against every named class-feature pillar `pilot_compute.rs` itself grounds for that class. **Fighter: 0 gaps** — every grounded pillar (Bravery, 10 Bonus Feat slots, 4 Armor Training ranks, Weapon Training + 3 chosen groups, Armor Mastery, Weapon Mastery) is already surfaced. **Wizard: 1 genuine gap found** — `level_up/wizard.rs`'s explanation filter (`class_chassis.wizard.` prefix) predates SD-21 E6b.2's `ground_wizard_prepared_spellbook` grounding and was never widened to also capture the `class_spell.wizard.*` family (spellbook contents, daily preparation, base/Intelligence-bonus/total spells-per-day per spell level 0-3), so every one of those real facts was silently dropped from the `LevelUpPlan` whenever a wizard's bounded prepared-spellbook posture (levels 1-3, canonical Evocation specialization) was fully met. RED: `tests/sd24_wizard_level_up_spell_coverage.rs` drove `compute_wizard_level_up_grants` from level 2 (posture unmet) to level 3 (posture met) for a populated-spellbook Human Wizard — failed, `plan.automatic_features` contained only the generic table-column and pre-existing chassis grants, no `class_spell.wizard.*`-sourced grant. GREEN: added `WIZARD_SPELL_EXPLANATION_PREFIX = "class_spell.wizard."` to the filter (and to `friendly_name`'s prefix-trim chain); corrected the module's now-stale doc comment claiming this posture was permanently ungrounded. Regression: `tests/sd21_wizard_prepared_spellbook.rs` (5 tests) still passes unchanged. Full suite: `cargo test --locked --tests` (root) 431 test binaries, 0 failed; `cargo test --locked` (`apps/desktop/src-tauri/`) — see receipt for final count. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) clean before and after on the diff since `merge-base HEAD origin/develop`. Wrote per-class coverage artifacts (`class_fighter_coverage.md`, `class_wizard_coverage.md`) and the epic-level roll-up (`per-class-coverage-matrix.md`).
  **commit:** `66f9be8`
  **receipt:** `./artifacts/epic_4/fighter-wizard-audit-cycle_receipt.md`
  **discovered:** 2 entries (epic-4-path-correction, epic-4-follow-on — see `## DISCOVERED`)
  **next:** Epic 4 criteria 4.2 (APG classes) / 4.3 (ACG classes) per `## TODO` priority order, or Epic 5 criterion 5.1 (Fighter+Wizard multiclass dispatch) since its Fighter+Wizard coverage-matrix dependency is now satisfied.

- **cycle_id:** apg-class-audit (2026-07-21)
  **criterion:** 4.2 (Epic 4, Per-Class Coverage Audit — APG classes)
  **summary:** Audited all six real APG classes (Alchemist, Cavalier, Inquisitor, Oracle, Summoner, Witch — Gunslinger/Magus are not real APG content per SD-22's own corrected roster) for wiring coverage. Task brief named a non-existent `apg/class_tables.rs`; APG's real shape is six per-class files plus `apg/mod.rs`'s shared types (see `## DISCOVERED`). **Chassis (BAB/saves): fully wired, 20/20 levels for every class, independently re-verified correct** against the real PCGen corpus (`apg_classes.lst`'s `BONUS:COMBAT|BASEAB`/`BONUS:SAVE` tokens, corpus commit `7f818006e371188e5717fd18d74d18a420747fc6`) — no defects found. **Named class features: 0/24 (Alchemist), 0/16 (Cavalier), 0/19 (Inquisitor), 0/19 (Oracle), 0/17 (Summoner), 0/7 (Witch)** — expected counts are a corpus-derived, reproducible count of distinct `KEY:<Class> ~ ...` records in `apg_abilities_class.lst` (methodology documented in code). **`pilot_compute.rs`'s live `compute_class_chassis` dispatch recognizes no APG class** — proven empirically (not just by inspection) by driving a real `CharacterInput` for each class through `compute_pilot_base_chassis` and confirming the honest, claim-blocking `class_chassis.unsupported` diagnostic fires with `base_attack_bonus: 0`, matching the pre-existing behavior for every non-Fighter/Wizard CRB class too. **No `level_up::<class>` module exists for any APG class.** Added `ApgClassId::ALL`/`name()`, `ApgClassCoverage`, `class_coverage()`, `coverage_report()` to `rules_tables::apg::mod` so the audit is executable, regression-tested code, not a one-off hand count. RED: `tests/sd24_apg_class_coverage_audit.rs` did not compile before these additions (reproduced live via `git stash` on `apg/mod.rs`: 4 compile errors, unresolved `class_coverage`/`coverage_report` imports and missing `ApgClassId::ALL`/`name`). GREEN: 3 passed, 0 failed after restoring; full suite `cargo test --locked --tests` → 3958 passed, 0 failed (3955 prior + 3 new). Dual-audit gate: one self-healed single-token finding (benign "placeholder" in a doc comment, reworded per `loop-instruction.md §4.1`), then clean (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`). Merged this cycle's APG section into the shared `per-class-coverage-matrix.md` (which the concurrent `fighter-wizard-audit-cycle` had already created for CRB) rather than overwriting it, resolving an add/add rebase conflict by hand. Wrote 6 per-class artifacts (`apg_<name>_coverage.md`) plus this cycle's own receipt.
  **commit:** `f25dc7b`
  **receipt:** `./artifacts/epic_4/apg-class-audit_cycle_receipt.md`
  **discovered:** 2 entries (epic-4-path-correction, epic-4-follow-on — see `## DISCOVERED`)
  **next:** Criterion 4.3 (ACG classes, disjoint `acg/*.rs` file-touch) or 4.4 (remediation plan enumeration, consuming this cycle's + the Fighter/Wizard cycle's coverage findings) per `## TODO` priority order.

- **cycle_id:** acg-class-audit (2026-07-21)
  **criterion:** 4.3 (Epic 4, Per-Class Coverage Audit — ACG classes)
  **summary:** Audited all ten real ACG classes (Arcanist, Bloodrager, Brawler, Hunter, Investigator, Shaman, Skald, Slayer, Swashbuckler, Warpriest — corrected roster; criterion 4.3's own header text stales "Alchemist-side" in and "Slayer" out, both wrong per `rules_tables::acg::mod.rs`'s own pre-existing SD-22 correction) for wiring coverage, mirroring criterion 4.2's audit-only methodology exactly. Task brief named a non-existent `acg/class_tables.rs`; ACG's real shape is ten per-class files plus `acg/mod.rs`'s shared types (see `## DISCOVERED`). **Chassis (BAB/saves): fully wired, 20/20 levels for every class, independently re-verified correct** against the real PCGen corpus (`acg_classes.lst`'s `BONUS:COMBAT|BASEAB`/`BONUS:SAVE` tokens, corpus commit `7f818006e371188e5717fd18d74d18a420747fc6`) — no defects found. **Named class features: 0/9 (Arcanist), 0/19 (Bloodrager), 0/14 (Brawler), 0/21 (Hunter), 0/95 (Investigator), 0/10 (Shaman), 0/20 (Skald), 0/15 (Slayer), 0/29 (Swashbuckler), 0/18 (Warpriest)** — expected counts are a corpus-derived, reproducible count of distinct `KEY:<Class> ~ ...` records in `acg_abilities_class.lst`. **`pilot_compute.rs`'s live `compute_class_chassis` dispatch recognizes no ACG class** — proven empirically by driving a real `CharacterInput` for each class through `compute_pilot_base_chassis` and confirming the honest, claim-blocking `class_chassis.unsupported` diagnostic fires with `base_attack_bonus: 0`. **No `level_up::<class>` module exists for any ACG class.** Added `AcgClassId::ALL`/`name()`, `AcgClassCoverage`, `class_coverage()`, `coverage_report()` to `rules_tables::acg::mod`, mirroring APG's SD-24 additions exactly. **Mid-cycle course correction:** this cycle first built and pre-commit reverted real production `level_up::acg_<name>.rs` wiring (chassis-pillar-only, honest, non-fabricated) after a `git fetch` surfaced that the already-landed sibling 4.2 cycle deliberately chose audit-only remediation for the identical finding — reverted to keep the bundle's roll-up matrix internally consistent between the two sibling criteria (see `## DISCOVERED`). RED: `tests/sd24_acg_class_coverage_audit.rs` did not compile before the `acg/mod.rs` additions (reproduced live via `git stash` on `acg/mod.rs`: 4 compile errors, unresolved `class_coverage`/`coverage_report` imports and missing `AcgClassId::ALL`/`name`). GREEN: 3 passed, 0 failed after restoring. Full suite `cargo test --locked --tests` (root) and `cargo test --locked` (`apps/desktop/src-tauri/`) both re-run clean (see this cycle's receipt for exact final counts). Dual-audit gate clean before and after (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`), no self-heal needed. Appended this cycle's ACG section to the shared `per-class-coverage-matrix.md` (replacing its "pending" placeholder) and wrote 10 per-class artifacts (`acg_<name>_coverage.md`) plus this cycle's own receipt.
  **commit:** `2e074ce` (feat) + `1901230` (progress.md)
  **receipt:** `./artifacts/epic_4/acg-class-audit_cycle_receipt.md`
  **discovered:** 4 entries (epic-4-path-correction, roster-correction, mid-cycle-course-correction, epic-4-follow-on — see `## DISCOVERED`)
  **next:** All of Epic 4's per-class audits (4.1 CRB Fighter+Wizard scope, 4.2 APG, 4.3 ACG) are now landed. Criterion 4.4 (remediation plan enumeration) and 4.5 (APG/ACG multiclass deferral decision) can now consume the full coverage-matrix output.

- **cycle_id:** remediation-and-deferral-cycle (2026-07-21)
  **criterion:** 4.4 + 4.5 (Epic 4, Remediation Plan + APG/ACG Multiclass Deferral)
  **summary:** Documentation-only planning cycle consuming the audit findings already landed by 4.1 (`fighter-wizard-audit-cycle`, `66f9be8`), 4.2 (`apg-class-audit`, `f25dc7b`), and 4.3 (`acg-class-audit`, `2e074ce`); per `acceptance-and-verification.md` rows 26-27 both criteria's verification is "n/a (planning artifact)" so no code/tests/TDD RED-GREEN cycle applies. **4.4:** wrote `./artifacts/epic_4/remediation-plan.md` enumerating, per class, `class_features_wired` vs `class_features_expected` and the fix cycle-id: Fighter 10/10 (no gap); Wizard 12/12 after the in-cycle fix (`class_spell.wizard.*` family, remediated by `fighter-wizard-audit-cycle`, no carry-forward); the 9 unaudited CRB classes (out of this bundle's granted file-touch scope) forwarded as a LOW-priority, non-gating follow-on; all 6 APG classes (0/24, 0/16, 0/19, 0/19, 0/17, 0/7) and all 10 ACG classes (0/9, 0/19, 0/14, 0/21, 0/95, 0/10, 0/20, 0/15, 0/29, 0/18) have zero fix cycle-id assigned in SD-24 — deferred per criterion 4.5. **4.5:** wrote `./artifacts/epic_4/apg-acg-multiclass-deferred.md`, the formal decision record confirming (with 4.2/4.3's audit evidence) that all 16 real APG/ACG classes are chassis-only (fully wired BAB/saves, 0 named features, 0 live `pilot_compute.rs` integration, 0 `level_up` modules) — the exact precondition `loop-instruction.md §4.2`'s hard-stop row anticipated — so Epic 5's Fighter+Wizard-only multiclass scope (operator directive 2026-07-21, `decisions.md §4`) stands confirmed and unblocked (Fighter+Wizard's own 4.1 coverage is complete and gap-free). Formalized the pre-existing generic `risks-and-open-questions.md §5` APG/ACG-multiclass deferral bullet with this cycle's concrete evidence and cross-references to both new artifacts, closing `§2`'s corresponding anticipated-hard-stop row. Dual-audit gate: `OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`, both before and after — no source-code files touched this cycle (planning/docs only).
  **commit:** `ac3b130`
  **receipt:** `./artifacts/epic_4/remediation-and-deferral-cycle_receipt.md`
  **discovered:** 0 new entries (consolidates existing `epic-4-follow-on`/`epic-4-path-correction` entries already in `## DISCOVERED`; no new plan-vs-reality gaps found)
  **next:** Epic 4 (all 5 criteria) is now fully complete. Next dispatch per `## TODO` deterministic order: Epic 5 criterion 5.1 (Fighter+Wizard multiclass dispatch), whose Epic-4 coverage-matrix dependency is now fully satisfied and formally closed.

- **cycle_id:** fighter-wizard-multiclass-dispatch-cycle (2026-07-21)
  **criterion:** 5.1 (Epic 5, Multiclass Stacking Real and Full — Fighter+Wizard multiclass dispatch)
  **summary:** Confirmed Epic 4's per-class coverage matrix is genuinely committed (hard-stop precondition per `loop-instruction.md §2.2`/§2.4) before proceeding. Task brief named `class_fighter.rs`/`class_wizard.rs` (which do not exist); this cycle's granted file set instead correctly named the real per-class `LevelUpPlan` composer modules, `level_up/fighter.rs`/`level_up/wizard.rs`, alongside `pilot_compute.rs`. Own-audit before writing any test found: `pilot_compute.rs`'s multiclass BAB/save chassis stacking (`compute_multiclass_base_chassis`, sum-fractions-then-round-down-once) and Fighter's own per-class explanations inside a mix (`fighter_level_in_mix`) were **already landed** by a prior SD (SD-21 E7.28/29/30) — not this cycle's gap. The real, still-open gap was Wizard-side and symmetric: `explain_wizard_level1_prepared_spell_baseline` was gated on single-class-only `supported_wizard_level`, and both `level_up::fighter`/`level_up::wizard`'s own entry gates required an exact single-class match, so `level_up::compute_level_up_grants`'s top dispatcher fell through to an empty default `LevelUpPlan` for any 2-class mix — exactly the "split-class transition level" failure the criterion's RED text names. RED: `tests/sd24_multiclass_fighter_wizard_split.rs`, `sd24_multiclass_fighter_lv10.rs`, `sd24_multiclass_wizard_lv10.rs` (16 tests total) written first, confirmed failing for the intended reason live via `git stash` on the 3 source files (Wizard's own spell-baseline explanations absent from a Fighter4/Wizard1 mix; `level_up::wizard`'s own `LevelUpPlan` empty at the split; `level_up::fighter`'s own solo-Wizard-dominant walk also failing the same way) before any fix landed. GREEN: added `wizard_level_in_mix` to `pilot_compute.rs` (mirroring `fighter_level_in_mix` exactly) and re-gated `explain_wizard_level1_prepared_spell_baseline` on it; widened `level_up::fighter`/`level_up::wizard`'s own entry gates to accept a supported Fighter+Wizard mix (`is_fighter_or_supported_fighter_wizard_mix` / `is_wizard_or_supported_fighter_wizard_mix`) — no other change needed inside either file, since both already read `from_level`/`to_level` from the caller rather than deriving them from `class_levels`'s own level number. All 16 new tests pass; 3 new fixtures added (Fighter4/Wizard1 split, Fighter9/Wizard1 and Wizard9/Fighter1 level-10 splits). **Regression fallout (mechanical, self-healed per `loop-instruction.md §4.1`):** 19 pre-existing wizard progression tests (`sd13_wizard_level{2..10}_progression.rs`, `sd18_wizard_level{11..20}_widening.rs`) broke because their multiclass negative control paired Wizard with Fighter (now genuinely supported) as the "still unsupported" partner — swapped to Rogue, mirroring the Fighter-side tests' own pre-existing precedent (e.g. `sd18_fighter_level20_widening.rs` already used Rogue). No assertion logic changed in those 19 files, only the injected partner class + an explanatory comment. Full suite: `cargo test --locked --tests --no-fail-fast` (root) 436 test binaries, 0 failed; `cargo test --locked --tests --no-fail-fast` (`apps/desktop/src-tauri/`) 113 passed, 0 failed. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) clean before and after on the diff since `merge-base HEAD origin/develop` (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`).
  **commit:** `0068818`
  **receipt:** `./artifacts/epic_5/fighter-wizard-multiclass-dispatch-cycle_cycle_receipt.md`
  **discovered:** 1 entry (epic-5-follow-on — `level_up.rs`'s top dispatcher still does not route a multiclass mix; see `## DISCOVERED`)
  **next:** Criterion 5.2 (Deterministic test surface: 30 character-advancement cycles, `tests/sd24_multiclass_deterministic.rs`) per `## TODO` deterministic order.

- **cycle_id:** fighter-wizard-multiclass-deterministic-cycle (2026-07-21)
  **criterion:** 5.2 (Epic 5, Multiclass Stacking Real and Full — Deterministic test surface: 30 character-advancement cycles)
  **summary:** Confirmed Epic 4's per-class coverage matrix and criterion 5.1's Fighter+Wizard dispatch fix (`0068818`) are both genuinely committed before proceeding. This cycle's invocation carried forward 5.1's own granted file set verbatim rather than 5.2's real target; corrected against `epic-breakdown.md`'s own criterion 5.2 row and `content-unit-inventory.md` (both name `tests/sd24_multiclass_deterministic.rs`), recorded as a `## DISCOVERED` entry rather than silently deviating. Built the 30-cycle deterministic walk `technical-design.md §2.2` specifies: 10 solo-Fighter cycles (level 1-10), 10 solo-Wizard cycles (level 1-10), and 10 split-advance cycles (5 Fighter-side + 5 Wizard-side, total level 6-10 — the split step itself, total level 5, is criterion 5.1's own test surface and is not re-counted). All 30 points reuse the two existing level-10 fixtures' own posture via the same isolated-clone-and-mutate-`class_levels` technique 5.1's own `*_lv10.rs` tests already established -- no new fixture files needed. Every cycle asserts against canonical PF1 formulas computed independently in the test file (Fighter full BAB/good Fort/poor Ref+Will; Wizard half BAB/poor Fort+Ref/good Will; multiclass mix sums each class's own unrounded fractional save value and floors once for the total), cross-checked against a standalone Python script before being written into `artifacts/epic_5/multiclass-fixture.md`'s per-cycle table -- 100% agreement. RED: temporarily reverted `pilot_compute.rs`/`level_up/fighter.rs`/`level_up/wizard.rs` to their pre-5.1-fix state (`git checkout c3330b6 --`, the commit immediately preceding 5.1's fix commit `0068818`) and ran the new test: the two solo-class cycles passed unaffected, but both mix cycles (Fighter-side and Wizard-side split-advance) FAILED, each panicking on `class_chassis.spell_baseline.wizard` silently absent from the mix -- reproducing 5.1's exact defect across the whole split-advance walk, not just its two single-point endpoints. GREEN: restored the fix (`git checkout HEAD --`), re-ran: `4 passed; 0 failed`. Confirmed the 3 source files are byte-identical to `HEAD` after restore (`git status --porcelain` shows only the new test + artifact files). Full regression: `cargo test --locked --tests --no-fail-fast` (root) 437 test binaries / 3982 passed / 0 failed; same (`apps/desktop/src-tauri/`) 113 passed / 0 failed. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) clean before and after on the diff since `merge-base HEAD origin/develop` (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`) -- the new test file's own `sd24_` filename identifier sits outside the audited glob set, matching every prior Epic-5 test file's own precedent.
  **commit:** `e8e97c2`
  **receipt:** `./artifacts/epic_5/fighter-wizard-multiclass-deterministic-cycle_cycle_receipt.md`
  **discovered:** 1 entry (epic-5-task-brief-drift — this cycle's invocation carried forward 5.1's file set instead of 5.2's own; see `## DISCOVERED`)
  **next:** Criterion 5.3 (Integration test consumes ingested content, `./artifacts/epic_5/integration-test-cycle_receipt.md`) per `## TODO` deterministic order.

- **cycle_id:** integration-test-cycle (2026-07-21)
  **criterion:** 5.3 (Epic 5, Multiclass Stacking Real and Full — Integration test consumes ingested content)
  **summary:** Confirmed Epic 4's per-class coverage matrix and 5.1/5.2's own commits are genuinely committed before proceeding. This cycle's invocation again carried forward criterion 5.1's own granted file set verbatim instead of 5.3's own target — the same drift 5.2's cycle already flagged and predicted would recur; recorded a `epic-5-task-brief-drift-recurrence` `## DISCOVERED` entry. Corrected against `epic-breakdown.md`'s own criterion 5.3 row (cycle artifact `./artifacts/epic_5/integration-test-cycle_receipt.md`) and `acceptance-and-verification.md`'s named verification command (`cargo test --locked --test sd24_multiclass_integration`). Interpreted "integration test consumes ingested content" as distinct from 5.2's hand-typed-formula oracle: this cycle's new `tests/sd24_multiclass_integration.rs` derives its expected BAB/save values directly from `rules_tables::crb::class_tables::class_tables()` (the codebase's own established "ingested content" table, "SD-19's foundation" per every `level_up::<class>.rs` doc comment) rather than re-deriving the PF1 formula a second time, so the test catches production drift from the single ingested source of truth, not just drift from a hand-typed twin that could drift in lockstep. Building the oracle surfaced a genuine, if dormant, production defect-risk: `pilot_compute.rs`'s `multiclass_good_saves` hardcoded a second, independently-maintained copy of Fighter/Wizard's good/poor save classification, justified only by a doc-comment cross-reference to `class_tables.rs` rather than actually reading it (which had no public accessor for that classification). Added `pub fn good_saves_for(class_id: ClassId) -> Option<(bool, bool, bool)>` to `class_tables.rs` and refactored `multiclass_good_saves` to delegate to it, removing the duplicate — consistent with this codebase's own stated preference (every `level_up::<class>.rs` doc comment) for composing `class_tables()` directly rather than re-declaring its data. RED: the new test file's `use ...::good_saves_for` failed to compile (`error[E0432]: unresolved import`) before the accessor was added — a genuine compile-time RED, the same style Epic 4's 4.2/4.3 cycles already established. GREEN: after adding `good_saves_for` and the `multiclass_good_saves` refactor, `cargo test --locked --test sd24_multiclass_integration` → `5 passed; 0 failed`; all four pre-existing Epic 5 test files (`sd24_multiclass_fighter_lv10.rs`, `sd24_multiclass_wizard_lv10.rs`, `sd24_multiclass_fighter_wizard_split.rs`, `sd24_multiclass_deterministic.rs`) re-run clean against the refactor with no behavior change. Full regression: `cargo test --locked --tests --no-fail-fast` (root) 438 test binaries / 3987 passed / 0 failed (3982 prior + 5 new); same (`apps/desktop/src-tauri/`) 113 passed / 0 failed. Both dual-audit gates (`identifier-discipline`, `wired-integration-discipline`) clean before and after on the diff since `merge-base HEAD origin/develop` (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`).
  **commit:** `b503c47`
  **receipt:** `./artifacts/epic_5/integration-test-cycle_receipt.md`
  **card:** `t_75b0fb36` (`codex-tranche-5`, `done`)
  **discovered:** 1 entry (epic-5-task-brief-drift-recurrence — see `## DISCOVERED`)
  **next:** Criterion 5.4 (Multiclass dispatch passes the four-check audit — dual-audit gate output captured) per `## TODO` deterministic order.

- **cycle_id:** multiclass-dispatch-four-check-audit (2026-07-21)
  **criterion:** 5.4 (Epic 5, Multiclass Stacking Real and Full — Multiclass dispatch passes the four-check audit)
  **summary:** Confirmed Epic 4's per-class coverage matrix for Fighter+Wizard is genuinely committed (`4.1 complete`, commit `66f9be8`) before proceeding — hard-stop precondition per `loop-instruction.md §2.2`. This cycle's invocation again carried forward criterion 5.1's own granted file set verbatim, a **third** occurrence of the drift 5.2's and 5.3's cycles already flagged; recorded `epic-5-task-brief-drift-3rd-occurrence`. Corrected against `epic-breakdown.md`'s own criterion 5.4 row ("Multiclass dispatch passes the four-check audit") and `acceptance-and-verification.md`'s row (verification method: "Dual-audit gate"). Ran the generic per-cycle dual-audit gate (identifier-discipline + wired-integration four-check, scoped to `${BASE_BRANCH}...HEAD` where `BASE_BRANCH=09e43c3` the SD-23 closure commit) — clean both before and after this cycle's change (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`). Interpreted 5.4's own narrower ask as the four-check audit run *specifically* against the multiclass dispatch production surface (the exact 4 files 5.1/5.3 touched: `pilot_compute.rs`, `level_up/fighter.rs`, `level_up/wizard.rs`, `class_tables.rs`), captured as a durable `cargo test` regression guard (mirroring criterion 3.1's precedent of turning an audit into standing test coverage) rather than a one-off shell grep. RED (genuine, organic, not manufactured): wrote the new `tests/sd24_multiclass_dispatch_audit.rs`'s `placeholder` check first with **no exclusion filter at all**, ran it against the real files — it failed immediately, surfacing 19 real hits in `pilot_compute.rs`. Manual review classified all 19 as benign: 18 are the pre-existing "undocumented packet placeholder" anti-fabrication idiom already reviewed and bucketed as "Bucket D" in `tests/sd24_wired_integration_audit.rs` (criterion 3.1's repo-wide audit — not a new finding, the same pattern resurfacing under a narrower grep), and 1 is ordinary `//`-comment prose. GREEN: added the two matching exclusion buckets; re-ran: `5 passed; 0 failed`. Filter-honesty re-check: temporarily forced both buckets to always-exclude-nothing and re-ran — reproduced the identical 19-hit RED, proving the buckets are not a rubber stamp; restored the real filter logic (confirmed byte-identical via diff) and re-ran clean. No production file (`pilot_compute.rs`/`level_up/fighter.rs`/`level_up/wizard.rs`) was modified this cycle — 5.4 is audit-only. Full regression: `cargo test --locked --tests --no-fail-fast` (root) 439 test binaries / 3992 passed / 0 failed (3987 prior + 5 new); `cargo test --locked` (`apps/desktop/src-tauri/`) 113 passed / 0 failed.
  **commit:** `79162c1`
  **receipt:** `./artifacts/epic_5/multiclass-dispatch-four-check-audit_cycle_receipt.md`
  **card:** `t_0cca4c3d` (`codex-tranche-5`, `done`)
  **discovered:** 1 entry (epic-5-task-brief-drift-3rd-occurrence — see `## DISCOVERED`)
  **next:** Criterion 5.5 (APG/ACG-class multiclass deferral, `./artifacts/epic_5/apg-acg-multiclass-deferred.md`) per `## TODO` deterministic order — the last criterion in Epic 5, documentary-only scope.

- **cycle_id:** apg-acg-multiclass-deferred (2026-07-21)
  **criterion:** 5.5 (Epic 5, Multiclass Stacking Real and Full — APG/ACG-class multiclass deferral, deferred-work-item)
  **summary:** Confirmed Epic 4's per-class coverage matrix and criteria 5.1-5.4's own commits are genuinely committed before proceeding. This cycle's invocation again carried forward criterion 5.1's own granted file set verbatim — a **fourth** occurrence of the drift 5.2/5.3/5.4's cycles already flagged; recorded `epic-5-task-brief-drift-4th-occurrence-epic-5-closes`. Corrected against `epic-breakdown.md`'s own criterion 5.5 row (cycle artifact `./artifacts/epic_5/apg-acg-multiclass-deferred.md`, "Documents the deferral with reference to Epic 4's coverage report") and `acceptance-and-verification.md`'s row 32 (verification method: "n/a (deferral artifact)"). Per that "n/a" designation, this is a documentary-only cycle mirroring criterion 4.5's own precedent (`remediation-and-deferral-cycle`, commit `ac3b130`) exactly: no source code, no tests, no TDD RED/GREEN cycle applies. Wrote `apg-acg-multiclass-deferred.md` as the Epic-5-side echo of criterion 4.5's canonical decision record (`../epic_4/apg-acg-multiclass-deferred.md`, which itself names criterion 5.5 as its expected consumer). Confirmed — by reviewing each of Epic 5's four landed commits (`0068818`, the 5.2 deterministic-cycle commit `e8e97c2`, `b503c47`, `79162c1`) — that no APG/ACG class, `level_up::apg_*`/`level_up::acg_*` module, or `ApgClassId`/`AcgClassId` chassis-dispatch branch was introduced anywhere in Epic 5's actual delivery: the Fighter+Wizard-only deferral held end to end. Dual-audit gate: no source files touched this cycle, so both greps against `${BASE_BRANCH}...HEAD` (`BASE_BRANCH=09e43c3`) are trivially clean (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`) both before and after, matching 4.5's own precedent cycle's identical no-source-touch result.
  **commit:** `bb7dd74`
  **receipt:** `./artifacts/epic_5/apg-acg-multiclass-deferred_cycle_receipt.md`
  **card:** `t_2dbe8a8f` (`codex-tranche-5`, `done`)
  **discovered:** 1 entry (epic-5-task-brief-drift-4th-occurrence-epic-5-closes — see `## DISCOVERED`)
  **next:** Epic 5 (all 5 criteria: 5.1-5.5) is now fully complete. Per `## TODO` deterministic order, next dispatch is Epic 6 criterion 6.1 (Equipment coverage audit, read-only).

- **cycle_id:** equipment-coverage-audit-cycle (2026-07-21)
  **criterion:** 6.1 (Epic 6, Equipment/Armor/Spells Content Completion — Equipment coverage audit, read-only)
  **summary:** Audited the full PF1 CRB + APG + ACG + Bestiary 1 corpus for equipment/armor/spell field completeness (cost, weight, description, full spell text), scored against the real `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/` corpus (reachable). Task brief named `src/rules_core/rules_tables/{crb,apg,acg,beastiary1}/equipment_tables.rs`; `crb`/`apg`/`acg` exist as named, but **`beastiary1/equipment_tables.rs` does not exist at all** — the `beastiary1` module carries only monster stat blocks, no equipment concept (see `## DISCOVERED`). **Record-coverage findings:** CRB equipment is **100% record-ingested** (2977/2977: 310 arms/armor + 453 general + 1556 magic items + 658 equipmods), contradicting `equipment_tables.rs`'s own stale "one representative item per category" doc comment (a documentation gap, not a data gap). CRB spells are 96.6% ingested (652/675). APG equipment (3/341) and spells (4/298), and ACG equipment (3/221) and spells (4/145), are all still at their original SD-22 bootstrap samples. Bestiary 1 equipment: 0/7 real records, no module. **Field-coverage findings (the headline result):** `EquipmentTableEntry` (identical shape across all three books that have one) has exactly 4 fields — `key`, `category`, `name`, `cost_gp` — **no `weight` field and no `description` field exist at all**, in any book; this is a schema-level gap, not a per-row gap, meaning criteria 6.3/6.4 need a schema change before any per-row population. Every present `SpellListEntry.description` (660 records total) is populated but is only the corpus's short one-sentence `DESC:` summary — **no spell in any book carries the full SRD/PRD text**, which lives on a separate, never-ingested `<Name>.MOD` corpus record with its own longer `DESC:` token (verified directly: Alarm base record 25 chars vs. its `.MOD` record's ~900-char full text). Added `EquipmentFieldCoverage`/`field_coverage_report()` to each book's `equipment_tables.rs` and `SpellFieldCoverage`/`spell_coverage_report()` to each book's `spell_list.rs`, making every claim above executable, regression-tested code (mirroring criteria 4.1-4.3's `coverage_report()` pattern) rather than a one-off hand count — no equipment row or spell text was added, edited, or fabricated. RED: `tests/sd24_equipment_coverage_audit.rs` (written first) failed to compile with 9 `E0425` errors (the six `field_coverage_report`/`spell_coverage_report` functions did not yet exist). GREEN: after adding the six functions, `cargo test --locked --test sd24_equipment_coverage_audit` → 6 passed, 0 failed. Full suite: `cargo test --locked --tests` (root) → 3998 passed, 0 failed (440 test binaries, 3992 prior + 6 new); `cargo test --locked` (`apps/desktop/src-tauri/`) → 113 passed, 0 failed (unaffected). Dual-audit gate clean before and after (`OK_NO_BUNDLE_TAGS`/`OK_NO_TOKENS`), no self-heal needed. Wrote `./artifacts/epic_6/equipment-coverage-matrix.md` (the per-corpus gap list steering criteria 6.2-6.5) and this cycle's receipt.
  **commit:** `b8b9d40` (feat) + `f037d80` (progress.md)
  **receipt:** `./artifacts/epic_6/equipment-coverage-audit_cycle_receipt.md`
  **card:** `t_5c240f6b` (`codex-tranche-5`, `done`)
  **discovered:** 4 entries (epic-6-path-correction, epic-6-schema-gap x2, epic-6-doc-staleness — see `## DISCOVERED`)
  **next:** Criteria 6.2 (cost field / record-coverage completion for APG/ACG/Bestiary-1), 6.3 (weight field — schema change first), 6.4 (description field — schema change + sourcing-methodology decision), 6.5 (full spell text — `.MOD`-record ingest path) are now unblocked, consuming `./artifacts/epic_6/equipment-coverage-matrix.md`'s per-corpus gap list. Suggested order: 6.2 before 6.3/6.4 (both need the widened `EquipmentTableEntry` schema) before 6.5 (needs a new `.MOD`-record parser path).

- **cycle_id:** crb-field-completion-cycle (2026-07-21)
  **criterion:** 6.2 + 6.3 + 6.4 + 6.5 (Epic 6, Equipment/Armor/Spells Content Completion — CRB-only file-touch scope: `src/rules_core/rules_tables/crb/{equipment_tables.rs,equipment_data/,spell_list.rs}`)
  **summary:** Re-audited both of criterion 6.1's CRB gaps before doing any remediation work and found neither was real: CRB equipment record coverage was already 100% (2977/2977, unchanged, re-confirmed); CRB spell record coverage was **also** already 100% (652/652) — 6.1's "675 real/96.6%" figure was a measurement error (a naive `awk '{print $1}' | sort -u` count that double-counted 12 `.COPY=`-suffixed rows instead of merging them into their base spell, and counted 10 `TEMPBONUS:`/`TEMPVALUE:` sub-choice records with no `SCHOOL:`/`CLASSES:` token — not independent spells — plus 1 `SOURCELONG:` header line, as spell names). So **criterion 6.2 required zero remediation for CRB.** **6.3 (weight):** added `weight_lbs: Option<f64>` to CRB's `EquipmentTableEntry`, populated per-record from the real corpus `WT:` token (matched 100% by `KEY:` against a live PCGen-corpus parse): General 379/453, Arms/Armor 137/310, Magic Items 1495/1556, Equipmods 0/658 (equipment *modifiers* have zero `WT:` tokens anywhere in the real corpus — a genuine "where applicable" absence, matching `decisions.md §5`'s own cost/weight carve-out). **6.4 (description):** added `description: Option<&'static str>` similarly, populated from the corpus `DESC:` token (PCGen-entity-decoded: `&nl;`→newline, `&lbracket;`/`&rbracket;`→`[`/`]`, `&pipe;`→`|`): General 116/453, Arms/Armor 147/310, Magic Items 1556/1556 (100%), Equipmods 2/658 — total 1821/2977 (61.2%). Unlike cost/weight, `decisions.md §5` requires "a full description... for all of these things" with **no** "where applicable" qualifier — the real CRB corpus genuinely lacks a `DESC:` token for ~39% of equipment rows (mostly generic slot-type markers like `Belt`/`Ring`/`Wand` and equipment-modifier bookkeeping records), so literal 100% is unreachable without fabricating text. Filed as an `## Open blockers` entry rather than forced to "complete" (see below). **6.5 (full spell text):** no schema change needed; replaced every present CRB spell's truncated-to-first-sentence `description` with the fullest corpus text available — 623/652 (95.6%) from the `<Name>.MOD` record's full `DESC:` (stripping a trailing PCGen `|PRERULE:1,DisplayFullSpell` display-rule qualifier present on 622/623, verified as a directive, not spell text), 29/652 (4.4%, narrow spell variants like `Align Weapon (Chaos Only)` with no separate `.MOD` record) from the base record's own complete-for-that-variant text. `full_text_verified` now 652/652 (100%) — this criterion reaches a clean, honest 100% for CRB. Data regeneration used a one-time Python script (kept in scratchpad, not shipped — matching the existing data files' own "generated programmatically... regenerate if the corpus changes" doc-comment convention) that parses the real corpus and re-emits each `.rs` array, preserving every existing `key`/`category`/`name`/`cost_gp` value verbatim. **RED (genuine, compile-time):** the moment the four `equipment_data/*.rs` files were regenerated to reference the not-yet-existent `weight_lbs`/`description` fields, `cargo check --lib` failed with 5954 `E0560` errors; the new `tests/sd24_equipment_field_completion.rs` also failed to compile identically. **GREEN:** after adding both fields to `EquipmentTableEntry` and updating `field_coverage_report()` to compute real (non-hardcoded) counts, both the new test (6 passed) and the updated `tests/sd24_equipment_coverage_audit.rs` (CRB-specific assertions corrected; APG/ACG assertions untouched — 7 passed) went GREEN. **Two self-healed regressions (both genuine, expected consequences of the 6.5 full-text change, per `loop-instruction.md §4.1`):** (1) `tests/sd20_spellbook_parity.rs`'s fixture hardcoded `Shield`'s pre-cycle truncated text — updated the fixture. (2) `tests/sd24_wired_integration_audit.rs`'s zero-tolerance token check flagged `Plant Growth`'s real SRD text ("creatures must **hack** or force a way through", ordinary English, not a stub) — added one narrow, named bucket exclusion mirroring the file's own `placeholder`-bucketing precedent. Full suite: `cargo test --locked --tests` (root) 441 test binaries / 4005 passed / 0 failed; `cargo test --locked` (`apps/desktop/src-tauri/`) 113 passed / 0 failed. Dual-audit gate (working tree, since the audit was run before commit): `OK_NO_BUNDLE_TAGS`; wired-integration raw grep shows the one self-healed `Plant Growth`/"hack" finding (documented, not fabricated, not a stub) rather than a clean `OK_NO_TOKENS` — same shape as criterion 3.1's own precedent for reviewed, benign raw-grep hits on real content.
  **commit:** `96d085f` (feat) + `688b3e1` (progress.md)
  **receipt:** `./artifacts/epic_6/crb-field-completion-cycle_cycle_receipt.md`
  **card:** `t_0d4f6f29` (`codex-tranche-5`, `done`)
  **discovered:** 4 entries (epic-6-audit-correction, epic-6-open-blocker, epic-6-audit-false-positive — see `## DISCOVERED`; plus an in-cycle correction to the pre-existing epic-6-doc-staleness entry)
  **next:** Follow-on cycles (disjoint file-touch from this cycle) should pick up APG equipment/spell record-coverage (338/341 equipment + 294/298 spell records remaining) and ACG (218/221 equipment + 141/145 spell records remaining), then their own weight/description schema population, then Bestiary 1's from-scratch equipment module (7 records). The CRB `description` field-coverage Open Blocker needs an operator decision before Epic 6 can be marked fully closed. (Note: the APG portion of this "next" plan landed concurrently — see the `apg-content-completion-cycle` entry immediately below, which this cycle's own commit predates by minutes.)

- **cycle_id:** apg-content-completion-cycle (2026-07-21)
  **criterion:** 6.2 + 6.3 + 6.4 + 6.5 (Epic 6, Equipment/Armor/Spells Content Completion — **APG scope only**, see discovered scope-clarification below)
  **summary:** Granted file-touch set (`apg/equipment_tables.rs`, `equipment_data/`, `spell_list.rs`) names only APG's own files; since `EquipmentTableEntry`/`SpellListEntry` are separate per-book types (not shared), scoped this cycle's actual work to APG, leaving CRB/ACG/Bestiary-1's equivalent work to sibling concurrent cycles (multiple other `.claude/worktrees/wf_0204a5d8-425-*` were observed running in parallel — confirmed live: `crb-field-completion-cycle` landed CRB's own equivalent concurrently, see the cycle-log entry immediately above). Built a one-off Python parser against the real corpus (`~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/advanced_players_guide/`, reachable from this environment), cross-validated against all 7 pre-existing bootstrap rows (3 equipment + 4 spell) before trusting it for the full ingest. **6.2:** ingested all 338 real APG equipment records (94+76+171 minus 3 `SOURCELONG:` header-line miscounts the 6.1 audit's grep-based methodology double-counted — corrected 341 -> 338) and all 297 real APG spell records (298 minus 1 genuine duplicate `Resounding Blow` base record the audit's dedup missed — corrected 298 -> 297); both 100% record coverage. **6.3:** added `weight: Option<f64>` to `EquipmentTableEntry`, populated 319/338 (94.4%) from the corpus `WT:` token; 19 records have no `WT:` token at all (real, honest `None`). **6.4:** added `description: Option<&'static str>` to `EquipmentTableEntry`; populated 0/338 — direct, exhaustive inspection of all three `apg_equip_*.lst` files finds zero `DESC:` tokens on any equipment row at all, a real corpus limitation (equipment prose lives in the printed book, not the LST data), not a parsing gap. Filed as a non-self-healable `## Open blockers` item per `loop-instruction.md §4.2`. **6.5:** widened `Pf1SchoolId` from a 3-school bootstrap subset to the 8 canonical PF1 schools; widened `school`/`level` to `Option` (real corpus gaps: 41/297 records carry no `SCHOOL:`/`CLASSES:` token at all, mostly Summoner eidolon spells the real rules grant automatically rather than placing on a leveled list); added `description: Option<&'static str>` (281/297 sourced) and `full_text: bool` (261/297 = 87.9% carry the real full SRD/PRD text from a matching `<Name>.MOD` corpus record, not just the short base-record summary) — a real majority but not 100%, also filed as an Open blocker. Handled 18 (spell) + 1 (equipment) PCGen `.COPY=` variant records by resolving the variant's real display name and inheriting fields from the referenced base record where the base is in-book; left cross-book bases (a few `Planar Binding`/`Planar Ally`/`Summon Monster` variants whose base is a CRB spell) honestly unresolved rather than reaching across books. Found and fixed one doc-comment inaccuracy in-cycle: `apg/equipment_tables.rs` claimed no `Bomb` record exists in any APG equipment file; the full ingest finds one real (non-purchasable, to-hit-template) `Bomb` record. RED: `git stash`-ed the 3 production files (keeping the updated test files) — `cargo test --locked --test sd22_apg_spell_list_resolves` failed to compile with 8 genuine `E0308`/`E0277` type errors (the updated tests compare `entry.school`/`entry.level` against `Some(...)`, which only type-checks once the schema is widened to `Option`). GREEN: restored the production changes — `cargo test --locked --test sd24_equipment_coverage_audit --test sd22_apg_spell_list_resolves --test sd22_apg_equipment_resolves` → 18 passed, 0 failed. **Push-time merge conflict:** the retry-safe fetch+rebase found `origin/tranche/5-2` had already advanced with this exact sibling `crb-field-completion-cycle` commit (`96d085f` + its own `progress.md` update `688b3e1`), which independently edited the same shared `tests/sd24_equipment_coverage_audit.rs` and this same `progress.md`; resolved both by hand (kept both cycles' tests/entries, reconciled the one shared canary test down to an ACG-only "zero coverage" function, and merged the Status matrix/DONE/DISCOVERED/Open-blockers sections to carry both books' rows side by side rather than either cycle's edit silently overwriting the other's). Full suite after rebase+resolution: `cargo test --locked --tests` (root) 4006 passed, 0 failed; `cargo test --locked` (`apps/desktop/src-tauri/`) 113 passed, 0 failed. Both dual-audit gates clean on this cycle's own isolated diff (`git diff 96d085f...HEAD`); the wired-integration gate on the full `BASE_BRANCH...HEAD` range shows the one already-self-healed-and-documented `Plant Growth`/"hack" finding from the sibling CRB cycle (see `epic-6-audit-false-positive` in `## DISCOVERED`), not anything from this cycle's own diff.
  **commit:** `4c5eb7f`
  **receipt:** `./artifacts/epic_6/apg-content-completion-cycle_receipt.md`
  **card:** `t_df1879e7` (`codex-tranche-5`, `done`)
  **discovered:** 6 entries (epic-6-scope-clarification, epic-6-audit-correction, epic-6-schema-gap-confirmed-not-closeable, epic-6-doc-staleness [Bomb record], epic-6-codegen-followon, epic-6-audit-false-positive corroboration — see `## DISCOVERED`)
  **next:** ACG's and Bestiary-1's own equivalents of criteria 6.2-6.5 are the only remaining unstarted work in this epic (each book's own `equipment_tables.rs`/`equipment_data/`/`spell_list.rs`). CRB and APG are both now landed: 6.2/6.3 closed for both books; 6.5 closed for CRB (100%), substantial-but-not-100% for APG (87.9%); 6.4 (equipment description) is returned-to-backlog for both CRB (61.2% ceiling) and APG (0% ceiling) — see `## Open blockers` for the consolidated decision the operator needs to make.

## Open blockers

- **Epic 6 criterion 6.4 (equipment `description` field, CRB + APG) — strict-100% threshold unreachable from the real corpus without fabrication, in both books landed so far.**
  **Origin:** `crb-field-completion-cycle` (2026-07-21) + `apg-content-completion-cycle` (2026-07-21), criteria 6.2-6.5.
  **Finding:** `decisions.md §5` requires "a full description... for all of these things" for equipment/armor/weapons with no "where applicable" carve-out (unlike cost/weight, which both have one).
  - CRB equipment's `description` field, populated from the real corpus `DESC:` token, reaches 1821/2977 (61.2%) — the honest ceiling the PCGen LST corpus supports. The remaining 1156 rows (mostly slot-type markers in `cr_equip_general.lst`/`cr_equip_arms_armor.lst` such as `Belt`, `Ring`, `Wand`, and `(Base)` weapon/armor templates, plus 656/658 `cr_equipmods.lst` modifier records) carry no `DESC:` token in the real corpus at all.
  - APG equipment's `description` field reaches an even harder ceiling: 0/338 (0%) — direct, exhaustive inspection of all three `apg_equip_*.lst` files finds zero `DESC:` tokens on any equipment row whatsoever. PCGen equipment records generally carry no prose description at all; that text lives in the printed book, not the LST data.
  - Per the no-stub-mvp doctrine, no description text was fabricated to close either gap.
  **Options for the operator:**
  1. Accept each book's LST-derived ceiling (61.2% CRB, 0% APG so far; ACG/Bestiary-1 TBD once their own cycles land) as criterion 6.4's closure bar per book — i.e. redefine "full description" as "the fullest description the ingested PCGen LST corpus provides."
  2. Authorize a second-source ingestion pass against the printed rulebook text for each book (out of this bundle's PCGen-LST-corpus scope; likely a follow-on bundle's content-source dependency, and a licensing question).
  3. Relax criterion 6.4's wording to carry a "where applicable" qualifier matching cost/weight's own carve-out (many equipment-modifier, slot-marker, and — per APG's finding — potentially entire-book equipment categories have no independent "description" concept in the source corpus at all, the same way they have no independent cost/weight in some cases).
  **Blocking status:** does NOT block 6.2/6.3 (both complete for CRB + APG) or 6.5 (complete for CRB at 100%; substantial-but-not-100% for APG at 87.9%, its own separate real-corpus-gap reasons — see the `epic-6-schema-gap-confirmed-not-closeable`/APG-spell-full-text entries in `## DISCOVERED`) or either cycle's own closure. Blocks only Epic 6's full closure claim on criterion 6.4 specifically (both books so far, and presumably ACG/Bestiary-1 once assessed), and only pending this operator decision — the mechanical/schema/population work is otherwise done for both CRB and APG.
- **Epic 6 criterion 6.5 (APG spell full text) — 87.9% (261/297), a real majority but not 100%, for reasons distinct from the CRB/APG description ceiling above.**
  **Origin:** `apg-content-completion-cycle` (2026-07-21).
  **Finding:** The remaining 36 of 297 APG spell records are split between (i) records with no `SCHOOL:`/`CLASSES:` token at all on the base record (41 total minus overlap with the description gap — mostly Summoner eidolon spells the real PF1 rules grant automatically rather than placing on a leveled spell list, plus a handful of `PRESPELL`-linked variants and one corpus typo, `Wall of Thorms` for "Wall of Thorns") and (ii) records whose base line has no matching `<Name>.MOD` full-text record in the corpus. Neither is fabricatable.
  **Options for the operator:** accept 87.9% as the practical ceiling for APG spell full-text coverage (contrast: CRB reached 100%, so this is not a universal ceiling — some books' corpora are simply more complete than others), or scope a follow-on cycle to hand-verify/hand-transcribe the remaining ~36 spells' full text against a licensed SRD/PRD source (same licensing consideration as the 6.4 blocker above).
  **Blocking status:** does not block 6.2/6.3/6.4's own disposition. Blocks only Epic 6's full closure claim on criterion 6.5 for APG specifically.


---

*Per `./loop-instruction.md §2.3 step 9`: the cycle picker updates this file in place on every cycle. Do not rewrite from scratch; each cycle appends to its own section.*
