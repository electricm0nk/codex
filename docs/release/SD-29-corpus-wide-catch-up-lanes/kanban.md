# SD-29 — Local-file Work Queue (replaces Hermes board `codex-tranche-9`)

Per operator directive 2026-08-01, the Hermes board is retired. SD-29's
work queue is a local-file Markdown table. The supervisor reads this file
at top of each cycle tick to identify the next ready card; the
file-touch partition ensures only one cycle claims a card at a time.

**Re-cut 2026-08-10 (`decisions.md §37`).** Cards are now lane-scoped
(kind, or merged-kind-pair), not per-book. A lane epic's cycle-batches
fan out per book internally — see `epic-breakdown.md` for each lane's
per-book unit counts.

**Re-scoped corpus-wide 2026-08-10 (`decisions.md §38`).** Every lane below now fans out across
all 37 in-scope books, not the retired seven-book set. Epic 4 (Tier 1, proven-path) is split into
three cards since it now covers six kinds at corpus scale; Epics 5-7 (Tier 2, mechanism-gated)
each pilot on one small book before extending corpus-wide.

> ## ✅ BUNDLE CLOSED — 2026-08-13 (`decisions.md §70`, `sd29-closure-r3`, card `epic-11-closure-run3`)
>
> Every lane is at a **measured** ceiling: `companion`, `monster`/`monster_ability` and `race_trait`
> are all `DRY` with **0 workable units**, re-derived at the closing tip by the checked-in row
> classifiers (`§70.1`). `docs/architecture/` is refreshed to current-state truth, `release-notes.md`
> §Known issues is rewritten into real shipped issues plus owned structurally-unreachable residue,
> every Decision-27 deferral carries a named owner (and the one ceiling that has none says so rather
> than being given a false one, `§70.3`), the gate ran FULL with its exit code captured directly, and
> DoD item 8 passed on all three reopened lanes on screen.
>
> **PR #360 is UPDATED, not replaced, and NOT merged. The operator merges it.**
>
> The reopen below is preserved verbatim. It is the reason the bundle shipped 4,208 of its 4,208
> grounded-unit gain at all.

> ## 🔴 BUNDLE REOPENED — 2026-08-11 (operator directive, `decisions.md §42`) — RESOLVED, see above
>
> **Operator, verbatim:** "this is part of sd-29's scope. sd-29 isn't done. let's get after it."
>
> SD-29 was closed on 2026-08-11 (`73f1421f`, `ac217788`) with **three lanes unfinished**. That
> closure is **rescinded**. Cards **8, 9, 10, 11, 12** (monster extend ingest; race-trait pilot and
> extend ingest; the whole companion lane) are back to `READY` and are **SD-29 scope** — not SD-30's,
> not `successor-forward-scope-register.md`'s, not any successor bundle's. Cards **15** and **16**
> (`epic-10-review`, `epic-11-closure`) are reopened with them, because both ran against a bundle
> state that is no longer final.
>
> **PR #360 (`tranche/9` → `develop`) stays OPEN and unmerged** until real closure. *(Real closure
> arrived 2026-08-13; the PR is updated in place and still awaits the operator's merge.)*
>
> **Prior receipts are preserved, not erased.** Every reopened row keeps its `progress.md` receipt
> and its landed half; only the *status* is corrected. Where a card delivered one half (a chassis, a
> classifier fix) and left another undelivered (the ingest), the row now reads `READY` for the
> undelivered half — an undelivered half does not inherit the delivered half's status.

## Status legend

- `READY` — not yet claimed. Cycle can pick up once every `Depends-on` card is `COMPLETE`.
- `IN-FLIGHT` — claimed by a cycle, in progress. Other cycles must wait.
- `BLOCKED` — cycle claims the block, captures the gap, surfaces in `progress.md` as a blocker.
- `COMPLETE` — cycle receipt in `progress.md` closes the card.
- `DECISION-BLOCKED` — the card required a decision the playbook routes to the operator; under
  UNATTENDED MODE (`loop-instruction.md` item 4) the cycle ruled it itself, took the safe default,
  and recorded the ruling and its evidence in `progress.md`. A terminal state, not a wait: the card
  is closed for this bundle and downstream cards treat it as settled. Do not re-dispatch it.
  **Narrowed 2026-08-11 (`decisions.md §42`), because this legend was misapplied to close the
  bundle early:** `DECISION-BLOCKED` is terminal only for a card that genuinely needed an operator
  decision and got one. It is **not** a disposal chute for work that was simply never dispatched.
  A card whose ingest half was never attempted is `READY`, not `DECISION-BLOCKED`, regardless of
  what its other half delivered.

**Dispatch tiebreak:** next card = lowest `Order` among `READY` cards whose
every `Depends-on` card is `COMPLETE`. A card whose `Depends-on` is not
fully `COMPLETE` is not eligible regardless of `Order` or `Status`.

## Cards (one row per lane epic cycle-batch), in dispatch order

| Order | ID | Status | Lane / Scope | Cycle-type | Depends-on | Claimed-by | Claimed-at | Cycle-id |
|---|----|--------|------|-----------|------------|------------|------------|----------|
| 1 | `epic-1-identifier` | COMPLETE | Identifier Cleanup | identifier-discipline audit pass | none | sd29-e1-identifier | 2026-08-10T00:00:00Z | SD29-E1-F1-001 |
| 2 | `epic-2-prelaunch` | COMPLETE | Operator Pre-Launch | corpus-wide (37-book) cycle-0 trap-report + work-inventory | `epic-1-identifier` | sd29-e2-prelaunch | 2026-08-10T23:56:00Z | SD29-E2-F1-001 |
| 2.5 | `epic-1b-naming-sweep` | COMPLETE | Function-Based Naming Sweep | corpus-source rename sweep: SD-NN + GE-NN tags out of file names, directory names, and identifiers (operator directive 2026-08-11) | `epic-1-identifier` | sd29-e1b-naming | 2026-08-11T00:00:00Z | SD29-E1B-F1-001 |
| 3 | `epic-3-provenance` | COMPLETE | Provenance Gate | PI-screening wired into each lane's extraction step; license-matrix citation for OGL/attribution, corpus-wide | `epic-2-prelaunch` | sd29-e3-provenance | 2026-08-11T00:00:00Z | SD29-E3-F1-001 |
| 4 | `epic-4-proven-equip-mod` | COMPLETE | Proven-Path Lanes — equipment + equipment_modifier | corpus-wide, 1,144 + 812 remaining units (equipment corrected from 1,163 by Epic 2 — the old figure counted `beginner_box`'s 19 excluded units; see `corpus-shape-37-books.md` §3) | `epic-3-provenance` | sd29-e4-equip | 2026-08-11T00:00:00Z | SD29-E4-F1-001 |
| 5 | `epic-4-proven-spell` | COMPLETE | Proven-Path Lanes — spell | corpus-wide, **1,561** remaining units (was 1,754: 192 ARG+UI units were already ingested AND already on screen, but a two-list divergence between `build_spell_catalog` (5 books) and `v06_work_inventory`'s `spell_levels` (3 books) reported them `not-ingested` — closed by this card's cycle; see `progress.md`). Residual splits 622 in rule-set-bearing books / 939 in books with no compiled rule set | `epic-3-provenance` | sd29-e4-spell | 2026-08-11T00:00:00Z | SD29-E4-F1-001 |
| 6 | `epic-4-proven-feat-race-class` | COMPLETE — settled by `epic-10-review` 2026-08-11: its actor never obtained an exit code (turn budget ran out mid-`root-full`) and left the row `IN-FLIGHT` with the lane's 83 feat rows landed but two downstream count pins RED. Those pins are fixed (`b4cff429`) and the lane's whole diff is now covered by a green full gate (`verify.sh` exit `0`, `root-full` 6170/543). Not re-dispatched: the work is landed and gate-verified, only the receipt was missing | Proven-Path Lanes — feat + race + class | corpus-wide, 1,348 + 96 + 158 remaining units (feat: the prior 1,350 counted the kind's 2 `deferred-with-reason` units as remaining — predicate difference, not an arithmetic error; see `corpus-shape-37-books.md` §3) | `epic-3-provenance` | sd29-e4-frc | 2026-08-11T02:40:00Z | SD29-E4-F2-001 |
| 7 | `epic-5-monster-lane-pilot` | COMPLETE | Monster / Monster-Ability Chassis Lane — pilot | Bonus Bestiary end-to-end (14 monster + 17 monster_ability — both re-derived from the corpus, both now `grounded`) | `epic-3-provenance` | sd29-e5-monster-pilot | 2026-08-11T00:00:00Z | SD29-E5-F1-001 |
| 8 | `epic-5-monster-lane-extend` | **DRY — ingest work complete** (round 11, the FINAL PASS, landed 2026-08-13 by `sd29-monster-final-r1`, gate GREEN on RUN 1 — `./scripts/verify.sh` FULL, `VERIFY_EXIT=0`, **14 of 14 stages, no red at any point in the cycle**: `root-lib` **1,752**, `root-full` **6,320 across 544 suites with all 525 `tests/*.rs` suites executed**, `desktop` 445, `reach` 27, `clippy` 0 errors, `pi-sweep` 10/10, `class-dump` 31/31. Item 8 PASS on BOTH artifacts, machine-verdicted: `monster / Hive Queen`, whose extracted line `Speed 50 ft., climb 20 ft. · HA p.236 · Hit dice Aberration:15` proves the new `HA` wire code, the page citation and the two-mode speed on one screen; and `monster / Hive Warrior`, whose `Rending Mandibles — Special Attack (Ex)p.237` proves a chassis ABILITY reaching the screen with its own facet, delivery and page. A THIRD run''s FAILED verdict file is committed rather than discarded — it lost its launch to the companion lane''s merge landing under a live tauri watcher, which yields a new standing rule: **do not run `verify-on-screen.sh` across a merge into its own worktree**, sibling to the existing never-concurrent-with-`verify.sh` rule. Commits `568f0756` (the 9-unit ingest + 7 registration points), `a1ef3a8e` (two classifier fixes + the new PCC-gate screen), `43ea54a1` (Decision 68 + register C1.5), `7a0b7530` (baseline ratchet + retro events), `b9403983` (merge of origin through companion round 9); pushed to `origin/tranche/9`. Full detail in receipt `## Cycle SD29-E5-F2-012`, `decisions.md §68`.) **Round 11 took `horror_adventures` — 9 units, and the lane's REAL workable remainder is now 0.** Grounded `monster` 1,239 → **1,242**, `monster_ability` 1,623 → **1,629**; corpus-wide grounded 4,638 → **4,647**. Raw remaining 1,515 → **1,506**, closing exactly. **`§64`'s REAL ceiling of 10 was reproduced EXACTLY at cycle start and then CORRECTED DOWN to 9 — the first time this lane''s ceiling moved for a reason other than an ingest.** `occult_adventures:monster:kami_shikigami` was never workable: `_occult_adventures.pcc:75` loads its file under `!PRECAMPAIGN:1,INCLUDES=Bestiary 3`, a **NEGATED** gate this repo fails because round 5 registered Bestiary 3; the file''s own header declares `SOURCELONG:Bestiary 3`/`SOURCESHORT:B3`, and `rules_tables::bestiary_3::monster_data` has SHIPPED that creature since round 5. Ingesting it would have written a second record for one creature under a second book label. **`§64` made "read the PCC LOAD LINE" standing and it has been used correctly several times — always in the POSITIVE direction. The negated form is excluded by a book this repo DOES have, so it gets MORE likely to fire as the repo ingests more books, and it only became false when round 5 landed.** Checked in as `scripts/screen_pcc_load_gates.py`, which screens both directions over every kind and finds **719** units corpus-wide excluded by a load gate. **It made two false confident claims before it made a true one** (it read ONE `.pcc` per directory when a PCGen directory declares several campaigns, and treated `PRECAMPAIGN:1,<a>,<b>` as a sequence when the leading count makes it a disjunction) — caught ONLY by running it against four cases whose answers three other documents already record, all four of which it now reproduces exactly. **A second instrument defect, in the lane''s own classifier:** its zero-monster line bucketed on REMAINING monsters while its parenthetical said "no monster in the book to own them", so **grounding this round''s 3 monsters made the figure RISE, 866/13 → 931/14**. Fixed to `monsters_all`; `§64`''s published `866 across 13` splits exactly into **703 across 10 structural** + 163 exhausted. This is the line `loop-instruction.md`''s zero-monster HARD STOP keys on. **§68.4: `ABILITY:Internal|AUTOMATIC|` has a SECOND, LIVE use** — attack composition (`ha_races.lst:4` prices one Claw and names Bite and Tail Slap only in the bundle; a `NATURALATTACKS:`-only reader serves 1 attack where the corpus states 3) — independent of the deferred ownership hop, and load-bearing on 2 of this round''s 3 monsters. **§68.5: `§63.4`''s shape reappeared in the file that documents it** — the diagnostic''s `horror_adventures` row still called `companion_book_counts` after the book stopped being companion-only; moved to the merged helper. **WHAT REMAINS IS NOT WORKLOAD.** 66 classifier-reachable units and **0 REAL**: `bestiary` 58 (instrument over-report, `§60.2`), `inner_sea_bestiary` 7 (PI cascade, `§57.2`), `occult_adventures` 1 (`§68.1`). **`§64.1`''s 229-unit bundle class is UNCHANGED by this round''s ingest — re-scanned after the 9 landed, still 229 across the same six books — and is now OWNED:** `successor-forward-scope-register.md` **C1.5**, owner **SD-31**, carrying the traversal (with `scan_monster_ability_bundle_rows.py::scan_book` named as the reference to port rather than re-derive), the six-book split, a five-step change list ending in the count-pin sweep, the two tests designed to go red, and the command pair that splits the class from the workable set. Under Decision 27 an unowned deferral is not a valid disposition; it now has one. `horror_adventures` contributes **0** to that class, reproducing `§64`''s own prediction for the book. Prior status: **READY — round 11** (round 10 landed 2026-08-13 by `sd29-monster-r12`, gate GREEN on run 3 of 3 — `./scripts/verify.sh` full, `VERIFY_EXIT=0`, all 14 stages — `root-lib` 1,748, `root-full` **6,316 across 544 suites with all 525 `tests/*.rs` suites executed**, `desktop` 445, `reach` 27, `clippy` 0 errors, `pi-sweep` 10/10. Item 8 PASS, clipboard-extracted and machine-verdicted: `monster / Psicrystal`, which proves THREE things at once — the new book on screen, the `CR 0` fix (§64.3), and a chassis ability rendering — and `monster / Astral Swarm`. Commits `612004df` (ingest + twelve registration points + the `CR:0` fix + the checked-in scan), `eeafe60a` (the twelfth registration point), `de010f2e` (merge of origin through companion round 8), `3b0a414c` (inventory regenerated on the merged tree); pushed to `origin/tranche/9` and verified BY CONTENT: `git cat-file -p origin/tranche/9:src/rules_core/rules_tables/ultimate_psionics/monster_data.rs` piped to `grep -c 'MonsterStatBlock {'` → **21**, to `grep -c 'MonsterAbilityRecord {'` → **13**. Full detail in receipt `## Cycle SD29-E5-F2-011`, `decisions.md §64`). **Runs 1 and 2 are recorded rather than hidden:** run 1 was ABANDONED BY ME mid-`root-full` because I had edited two `.rs` doc comments while it ran and would not cite a result whose input I could not name; run 2 finished `VERIFY_EXIT=1` on TWO reds, both attributed — `frontend-test` was a real defect of this diff (the twelfth registration point, fixed in `eeafe60a`) and `driver-selftest` was a CONCURRENT AGENT's live app on display :80, proven by the pid it returned being a long-lived process rather than the test's own decoy, and green again on run 3. Baselines updated from run 3's own measurements: `BASELINE_ROOT_LIB_TESTS` 1,738 → 1,748, `BASELINE_ROOT_FULL_TESTS` 6,306 → 6,316. **Round 10 took `ultimate_psionics` — 34 of the 44 REAL remainder, 77%, exactly the classifier's reachable figure for the book, and every one of its 21 corpus monster rows ships.** Grounded `monster` 1,218 → **1,239**, `monster_ability` 1,610 → **1,623**. Raw remaining `monster` 31 + `monster_ability` 1,484 = **1,515**; `1549 − 34 = 1515` closes exactly, and **§62's 44 REAL ceiling was reproduced EXACTLY at cycle start — confirmed, not corrected — and is now 10**, the second consecutive round where the ceiling moved by exactly the units ingested. **It is the cheapest registration this lane has done and the first with NO new `RuleSetId`, no new corpus directory and no new work-inventory book entry** — `RuleSetId::Upsi` has served this book's feats/equipment/archetypes since SD-28 E29. **§64.1 IS THE ROUND'S HEADLINE AND IT MOVES THE CEILING UP BY 229.** `§62.4` recorded an ownership class — a monster row naming a `CATEGORY:Internal` bundle row which names the abilities, a hop neither ownership pass follows — and measured it at **16**, in the one file it was reading. Round 10 ran the scan `§62.4` asked for, as its FIRST action, before any ingest: the class is **229 orphan rows across six books, five of them already registered** (`bestiary` 63, `inner_sea_gods` 79, `bestiary_4` 61, `bestiary_2` 15, `bestiary_3` 9, `ultimate_psionics` 2). The scan reproduces §62.4's 16 EXACTLY as the `support/` subset of inner_sea_gods' real 79, which is what makes it a correction rather than a competing claim. **The scan is CHECKED IN** as `scripts/scan_monster_ability_bundle_rows.py` — it imports the lane's own classifier and reuses `classify_book`'s orphan set rather than defining `orphan` twice — because `§45.1`'s finding is that an ephemeral path is not a citation. Deliberately NOT closed this round: widening an ownership pass changes what five registered books ship, and it is pinned by an executing test (`ultimate_psionics::monster_tests::no_internal_bundle_ability_ships_yet`). **§64.3 is a player-visible defect eleven books could not produce:** `up_races.lst:47` states `CR:0` for Psicrystal, the first zero challenge rating this catalog serves, and the frontend rendered it `CR 1/Infinity` (`Math.round(1/0)`). TWO Rust guards went red on a CORRECT transcription because both asserted `challenge_rating > 0.0` — a property accidental to the eleven books registered when they were written. Neither was weakened: the catalog test now asserts the token↔value correspondence (a parsed zero is admissible exactly when the token is literally `"0"`), and the response test pins the zero-CR key set the way it already pins the two B3 rows with no `SOURCEPAGE:`. **§64.4 fixes `§63.4`'s shape prospectively**: `ultimate_psionics_counts()` now CHAINS `chassis_book_counts` instead of adding two literals, so the Corpus Ingest panel cannot drop a family the way it dropped Ultimate Wilderness's 327 companion records. **Round 11's queue, from ONE command** (`python3 scripts/classify_monster_ability_rows.py`, raw remaining 1,515 / classifier reachable 75 / REAL **10**): `horror_adventures` 74 remaining / **9 REAL**, `RuleSetId::Ha` exists, both files at the book root, and its 4 `Internal` bundle rows reach 0 orphans so §64.1 does not apply there; `occult_adventures` 4 / **1**, a ONE-monster book needing a whole new `RuleSetId` for one record, reachable at all only because of §62.2's resolver. `bestiary` (58 classifier-reachable, 0 REAL) and `inner_sea_bestiary` (7, 0) stay reachable-exhausted; `ultimate_psionics` joins them. **Thirteen books now hold 866 orphan abilities and zero remaining monsters** (up from 12/800). **A successor must not read this as "10 left": for the first time in this lane the mechanism work (229) is larger than the ingest work (10), and it is ONE mechanism, scanned, counted and checked in.** Card stays READY. Prior status: **READY — round 10** (round 9 landed 2026-08-13 by `sd29-monster-r11`, gate GREEN on RUN 1 — `./scripts/verify.sh` full, `VERIFY_EXIT=0`, 14 of 14 stages, no red at any point in the cycle: `root-lib` 1,743, `root-full` **6,311 across 544 suites with all 525 `tests/*.rs` suites executed** (6,303 → 6,311), `desktop` 445, `reach` 27, `clippy` 0 errors, `pi-sweep` 10 hits / 10 baseline rows — the load-bearing stage for a book of deities. Item 8 PASS on BOTH artifacts, clipboard-extracted and machine-verdicted: `monster / The First Blade`, whose corpus row lives in `support/isg_races_b4.lst` and therefore proves the round's whole new chain on screen, and `monster / Arcanotheign`, proving a chassis ability renders. Full detail in receipt `## Cycle SD29-E5-F2-010`, `decisions.md §62`). Commit `1c7d8ef9` (ingest, the file-resolution widening on both sides, all eleven registration points, the re-transcription of nine books); pushed to `origin/tranche/9` and verified BY CONTENT: `git cat-file -p origin/tranche/9:src/rules_core/rules_tables/inner_sea_gods/monster_data.rs` piped to `grep -c 'MonsterStatBlock {'` → **39**, to `grep -c 'MonsterAbilityRecord {'` → **77**. **Round 9 took `inner_sea_gods` — 116 of the 160 REAL remainder, 73%, exactly the classifier's reachable figure for the book.** Every one of its 39 corpus monster rows ships, a first for this lane. Grounded `monster` 1,179 → **1,218**, `monster_ability` 1,533 → **1,610**. Raw remaining `monster` 52 + `monster_ability` 1,497 = **1,549**; `1665 − 116 = 1549` closes exactly, and **§60's 160 REAL ceiling was reproduced EXACTLY at cycle start — confirmed, not corrected — and is now 44**, the first round in this lane where the ceiling moved by exactly the units ingested. **§62.1: the blocker `§58.5` named and `§60.7` carried forward was real about the corpus and wrong about what bound.** All 77 shipped abilities come from `isg_abilities_races.lst` and zero from the support file, so the singular `abilities_lst` it blamed would have worked; what actually blocked the book is 3 MONSTER rows under `support/isg_races_b4.lst` against an inventory `source_file` that is a BARE BASENAME. `resolve_book_file` (written once on each side, refusing both not-found and found-twice; zero duplicate `.lst` basenames across all fourteen books this lane has considered) is the fix, and the generator carries the resolved sub-path into each record's `path` citation. The ability-side widening (`MonsterAbilityRecord::source_file`, plural `abilities_lsts`) landed too and is recorded as NOT load-bearing for this book. **§62.3: the re-transcription of all nine prior books is additive, proven — 1,533 insertions, 0 deletions, zero non-`source_file` diff lines, and 1,533 is exactly the pre-round grounded `monster_ability` count.** **§62.4 is a scope finding that moves the ceiling UP, the first time this lane's instrument has erred in the expensive direction:** 16 `support/isg_abilities_races_b4.lst` abilities have a real owner the corpus states through a `CATEGORY:Internal` `Race Traits ~` bundle row (`support/isg_races_b4.lst:6` → `support/isg_abilities_races_b4.lst:8`) that neither ownership pass follows — deliberately not closed in the same gate that widened file resolution, and pinned by an executing test so the round that does close it is told this arithmetic is stale. **Round 10's queue, from ONE command** (`python3 scripts/classify_monster_ability_rows.py`, raw remaining 1,549 / classifier reachable 109 / REAL **44**): `ultimate_psionics` 100 remaining / **34 REAL — 77% of what the classifier still calls reachable**, `RuleSetId::Upsi` exists and both files sit at the book root, so no resolution work and no new rule set; `horror_adventures` 74 / **9**, `RuleSetId::Ha` exists; `occult_adventures` 4 / **1**, a ONE-monster book needing a whole new `RuleSetId`, whose single monster row is `support/oa_races_b3.lst` and is therefore reachable at all only because of THIS round's resolver. `inner_sea_gods` is now 84 remaining / 0 classifier-reachable / **≥16 REAL** by §62.4. `bestiary` (58 reachable, **0** REAL) and `inner_sea_bestiary` (7, **0**) stay reachable-exhausted. **Twelve books now hold 800 orphan abilities and zero monsters** (up from 11/716 — `inner_sea_gods` joined that set the moment its last monster grounded). **Three books and 44 units, and not one needs a mechanism this repo lacks: the whole remaining cost is registration.** Card stays READY. | Monster / Monster-Ability Chassis Lane — extend | corpus-wide, every remaining book. **Denominators re-derived 2026-08-12 at the end of round 3 by the same command rounds 1 and 2 recorded (sum `not-ingested` + `not-started` for `monster` and `monster_ability` across every book whose `scope` is not `out_of_scope`): BEFORE `monster` 1,199 + `monster_ability` 3,034 = 4,233 — round 2's closing figure, reproduced EXACTLY before being moved. AFTER `monster` 1,190 + `monster_ability` 3,020 = **4,210 remaining**, `units_ingested` = 23.** Grounded is now `monster` **80** and `monster_ability` **87**. **The dispatch brief's "monster ~305, monster_ability ~852, grounded 62 and 20" is wrong for the SECOND round running** — `§46.1` corrected the identical pair for round 2 and the round-3 brief repeated it verbatim; that pair is near `bestiary`'s book subtotal (284/523), not the corpus-wide figure. **Cost, round 3:** the 8 registration points rounds 1-2 recorded, plus a `source_file` field on `MonsterStatBlock` (which touches all four already-shipped books' tables by exactly one line each) and a two-screen transcriber. **Round 2 recorded:** corpus-wide, every remaining book. **Denominators re-derived 2026-08-12 at the end of round 2 over the merged tree, by the same command round 1 recorded (sum `not-ingested` + `not-started` for `monster` and `monster_ability` across every book whose `scope` is not `out_of_scope`): BEFORE `monster` 1,208 + `monster_ability` 3,087 = 4,295. AFTER `monster` 1,199 + `monster_ability` 3,034 = **4,233 remaining**, `units_ingested` = 62.** Grounded is now `monster` **71** (bestiary 46 + bonus_bestiary 14 + monster_codex 2 + BotD1 5 + BotD2 4) and `monster_ability` **73** (bonus_bestiary 17 + monster_codex 3 + BotD1 36 + BotD2 17); the pre-round figures 62 and 20 were correct and were reproduced before being moved. **4,233 is NOT the lane's workload** (`decisions.md §46.1`): 1,327 of it is orphan `monster_ability` rows, structurally unreachable through the chassis, so the genuinely ingestable remainder is **2,906**. The dispatch brief's "monster ~305, monster_ability ~852" was wrong and is corrected here with its command — that pair is near `bestiary`'s book subtotal, not the corpus-wide figure. **Cost:** a book with no orphans costs a `RuleSetId` + `BOOK_SOURCES`-equivalent rows in 8 places (transcriber `BOOKS`, `monster_chassis::MONSTER_BOOKS`, `gen_book_cache::MONSTER_BOOK_SPECS`, a wire code + display name, two `reach_gate` arms + a `CORPUS_BOOK_IDS` row, a diagnostic row, a frontend label) plus a scope flip whose collateral was measured for the first time: 49 units for Volume 1, 233 for Volume 2 | corpus-wide, every remaining book. **Denominators re-derived 2026-08-11T19:26Z by round 1, confirming the prior figure and then moving it: BEFORE `monster` 1,210 remaining + `monster_ability` 3,090 remaining = 4,300; AFTER round 1 **`monster` 1,208 + `monster_ability` 3,087 = 4,295 remaining**. Grounded monsters are `bestiary` 46 (SD-22) + `bonus_bestiary` 14 (pilot) + `monster_codex` 2 (round 1); grounded monster_ability is `bonus_bestiary` 17 + `monster_codex` 3. The prior "1,224 monster" figure in this row was wrong — corrected to 1,270 total. The prior "1,224 monster" figure in this row was wrong — corrected to 1,270 total. Grounded monsters are `bestiary` 46 (SD-22) + `bonus_bestiary` 14 (this bundle's pilot); grounded monster_ability is `bonus_bestiary` 17, all pilot.** **Cost, from the pilot's receipt: do NOT extrapolate a per-unit rate** — essentially all of the pilot's cost was the once-per-*kind* chassis (new `RuleSetId`, rules-table module, generator arm, wire DTO, `CORPUS_KIND_NAMES` entry, reach claims, diagnostic row, frontend path, and 8 whole-catalog assertions re-scoped to their own book). The next book inherits all of it. Expect to fund a published-text dice-grounding decision: 13 of the pilot book's 14 named natural attacks carry no dice in the corpus. | `epic-5-monster-lane-pilot` | sd29-monster-r11 (round 9 landed; round 10 unclaimed) | 2026-08-13T05:40:00Z | SD29-E5-F2-010 (round 9 of a loop-until-dry lane) |
| 9 | `epic-6-race-trait-lane-pilot` | **COMPLETE** (2026-08-11, `decisions.md §43`) — both halves are now delivered. The classifier defect fix stood already; this cycle **re-pinned the pilot book to `monster_codex`** and landed the ingest end-to-end: 5 records at `data/corpus/monster_codex/race_trait/`, a real reach claim `("monster_codex", "race_traits")`, and the retirement of the standing `beastiary1/race_traits` finding from BOTH `OPEN_FINDINGS` and `UNREACHED_RECORD_FINDINGS` (DoD item 6). Prior receipts preserved | Race-Trait Lane — pilot | classifier defect fix (DONE) + `monster_codex` pilot ingest (DONE) | `epic-3-provenance` | sd29-racetrait-repin | 2026-08-11T16:30:00Z | SD29-E6-F1-002 |
| 10 | `epic-6-race-trait-lane-extend` | **DRY — round 5 landed 2026-08-12 and CONFIRMED round 4's ruling by reproducing it exactly before touching anything; the workable remainder is still 3 units, all needing a race-variant chassis that is not this card.** **Round 5** (`sd29-racetrait-r6`, commits `3fb8d539`, `b8fa449f`, `fffd74e5` plus three merges of the concurrent monster lane, branch tip `99df2e36`; **full gate `VERIFY_EXIT=0`, all 14 stages green, `root-full` 6259 passed across 544 suites with all 525 `tests/*.rs` suites executed**; two item-8 PASS artifacts taken on the MERGED tip; receipt `progress.md` `## Cycle — epic-6-race-trait-lane-extend, ROUND 5`, `decisions.md §53` — number RESERVED at claim time rather than renumbered on merge, the fix this board asked for after §47 and §49 both collided): **round 5 did not ingest, because there is nothing left to ingest. It fixed a Product-Identity defect in what the lane had already shipped, and the defect is corpus-wide.** `pi_screening::PI_BLACKLIST_TERMS` is a 55-term heuristic and PCGen declares the answer per record in `NAMEISPI:YES`/`DESCISPI:YES`, tokens every Pipeline A ingest path parses into `raw_tokens` and none of them reads. **26 shipped `race_trait` records declare `DESCISPI:YES`; the blacklist redacted 18 by coincidence and PUBLISHED the other 8** (`Kodar Mountains`, `Earthfall`, `Ekujae`, `Gogpodda`, `Omesta`, `Droskar`, `Abaddon`, `Inner Sea` — none on the list). **One declares `NAMEISPI:YES` and shipped a Product Identity NAME**: `Elf ~ Sovyrian-Born`, the record card 8 reported into this lane's territory. A name cannot be redacted, so the row is DROPPED — the same ruling `§50` reached independently for ISWG's five monster rows, now written ONCE in `pi_screening` instead of twice in hand-built tables. Nine pinned counts moved with it (alternates 283→282, race corpus 516→515, menu rows 456→455, Elf 28→27, `character_hub` accepts 189→188, ISR reach 72/71→71/70, `race_trait` grounded 514→513), each re-derived rather than decremented, and the orphan-flag assertion deliberately did NOT move because both of the row's flags are still claimed. **Round 5's second output is that `§8b` was misattributed for three rounds** — the round-1 receipt inferred "the IPC round trip happened" from the right panel updating, but that sentence is rendered from `selected.length` (local React state) and `blocked.size` (0 when `selection` is null), so it is exactly what renders when the resolve has NOT answered. The backend candidate is now dead by test (`plagueborn_really_suppresses_both_standard_traits_its_flags_name_so_8b_is_not_a_backend_gap`: 9→8 applied, both flagged traits suppressed, lock-outs non-empty); what survives is a screenshot-timing reading, which round 6 must reproduce live before writing any fix. **Scope finding for a successor bundle:** the declaration gap is corpus-wide and large — `grep -rho 'NAMEISPI:YES\|DESCISPI:YES' --include='*.lst' <paizo tree>` → **2,530 + 1,190**, across 106 spell/feat/equipment files — and it CANNOT be closed by widening this round's shipped-record gate, because `feat` (204), `spell` (1,185), `companion` (135), `monster` (80), `monster_ability` (87) and 602 `equipment` records ship with NO `raw_tokens` at all, so the declaration is invisible in the shipped file. Each ingest path must read it. Round 5 did NOT widen the gate, deliberately: turning it on for kinds whose ingest paths cannot satisfy it would land a red gate on three other lanes' live work. Prior status **DRY — round 4 landed 2026-08-12 and the lane's ingestable remainder is 3 units, none of them race-trait work.** **Round 4** (`sd29-racetrait-r4`, commits `9176f869`, `c8416f33`, merge `499b75e8`, receipt `progress.md` `## Cycle — epic-6-race-trait-lane-extend, ROUND 4`, `decisions.md §49` — written as §48 and renumbered in merge, because the companion lane claimed §48 concurrently; see §49's own header note): **Core Essentials' Aasimar and Tiefling heritages ingested end-to-end — 64 records, ALL 64 reaching a player, no `OPEN_FINDINGS` entry** (16 heritage selectors a player picks plus the 48 `<Race> Racial Trait`-typed replacement rows they grant). **Round 4's most reusable output is that `§47.8`'s verdict on this book was wrong: it needed NO new mechanism.** `§47.8` recorded the 48 as requiring a `PREABILITY`-grant mechanism, and the rows do carry a positive `PREABILITY` gate `race_resolver::classify` cannot read — but PCGen states the same transaction a second time in `core_essentials/races/<race>/<race>_abilities_globalvar_subrace.lst` as `ABILITY:<Race> Racial Trait|AUTOMATIC|<key>|PREVAREQ:<flag>,0`, the third grant shape `link_automatic_grants` has resolved since SD-27. Cost: one ~40-line ingest-side reader, zero resolver changes. **This is `§45.1` one level out** — classifying the rows of ONE FILE is not classifying the corpus; the file that made them ordinary was three directories away. Round 4 also fixed three defects its own work found: (a) **giving `core_essentials` a `RuleSetId` demoted 155 unchanged Core Rulebook and Bestiary 1 records** from `grounded` to `race_trait_record_loaded_but_never_applies` — `v06_work_inventory` required the corpus probe's observed book to EQUAL the unit's own book's rule set, and this is the one book whose rows are filed under a different book; caught because `race_trait` grounded went 450 → 359 in the run that ADDED 64 records, i.e. by re-deriving the denominator AFTER the ingest as well as before it; **the hazard is live for any lane whose units are stored under a book other than their owner**; (b) **16 heritages had no mutual-exclusion guard** (they carry no `PREMULT`, so a player could take two heritages of one race and collect both ability-score bonuses) — the picker now reads the `PREVAREQ:<flag>,0` spelling and the unguarded-set pin did NOT move; (c) **all 64 rows carry a placeholder `SOURCEPAGE`** (`p.xx` on Tiefling's 40, `xx` on Aasimar's 24) and `xx` would have rendered beside the trait as a real page — dropped at ingest by exact match, and no earlier book carries a placeholder at all. Plus `RuleSetId::Ce`, 16 `ALTERNATE_TRAIT_REPLACE_FLAGS` rows adding exactly 2 distinct flags to the corpus's 91, `BookSource.lst_relatives` as a LIST (two rows sharing one `corpus_book` would have erased each other, a hazard Horror Adventures dodged by ingesting only one of its two files), and `scripts/race_trait_ceiling.py` checked in. **Two item-8 PASS artifacts on the merged tree**, one proving the heritages render and one proving `§47.4`'s PI redaction holds on the shipped surface for a second book. **A round 5 on this card can only build a race-variant chassis.** **`§8b`'s browse-screen render bug is still open and still owned by this lane** — round 4 did not reach it either. Prior status **COMPLETE FOR INGEST (round 3 landed 2026-08-12) — the lane is DRY for no-new-mechanism work, and 51 mechanism-blocked units remain, named below. This is NOT a claim that those 51 are done.** **Round 3** (`sd29-racetrait-r3`, commit `bd98b9fe`, receipt `progress.md` `## Cycle — epic-6-race-trait-lane-extend, ROUND 3`, `decisions.md §47` — written as §46 and renumbered in merge `HEAD`, because the monster lane claimed §46 concurrently; see §47's own header note): **Horror Adventures ingested end-to-end — 43 records, ALL 43 reaching a player, the first book in this lane with no shortfall and therefore no `OPEN_FINDINGS` entry** (its two non-selectable rows are granted by name from `Halfling ~ Deep Jungle`, the completed form of the transaction ISR's `Human ~ Tribalistic Languages` leaves half-finished). Plus a `RuleSetId::Ha`, 41 `ALTERNATE_TRAIT_REPLACE_FLAGS` rows adding exactly ONE new distinct flag to the corpus's 90, and a reach claim asserting a plain `Reach::Surfaced`. **Round 3's three most valuable outputs are not the ingest.** (a) **Round 2 left `origin/tranche/9` RED and its receipt recorded the gate as 'in flight'** — eight assertions across three files, every one attributable to ISR rather than HA (HA contributes 0 Aasimar alternates and 0 changed-description entries; the deltas are exactly ISR's 72/68). All moved with their reasons, none relaxed. (b) **The PI screen was live and ineffective on the shipped surface**: `pi_screening` redacts `data.description` but `render_description` renders from the record's raw `DESC:` tokens, so all 12 of ISR's redacted records were rendering the Golarion place names the screen removed into the Race Traits panel; fixed at the source and pinned over all 12. (c) **Five alternates across two books were offered and moved nothing** — the engine's alternate-trait save wiring was a single hardcoded ARG constant; it is now a table, and three ISR skill bonuses missing from the skill table are added. **A round 4 on this card cannot make progress by ingesting.** **`§8b`'s browse-screen render bug is still open and still owned by this lane** — round 3 did not reach it either. Prior status **READY (round 3)**; before that IN-FLIGHT (round 2). Prior receipts preserved. **Round 2 landed 2026-08-11** (`sd29-racetrait-r2`, commits `4264739c`/`c8e2d6ad`, receipt `progress.md` `## Cycle — epic-6-race-trait-lane-extend, ROUND 2`, `decisions.md §45`): **Inner Sea Races ingested end-to-end — 72 records, 71 grounded and reaching a player** — plus a `RuleSetId::Isr` variant, 68 `ALTERNATE_TRAIT_REPLACE_FLAGS` rows, a reach claim, and an `OPEN_FINDINGS`/`UNREACHED_RECORD_FINDINGS` pair for the one record that cannot be surfaced (`Human ~ Tribalistic Languages`, an upstream data gap, evidenced as such). **Round 2's most reusable output is a correction to round 1's successor queue** (`decisions.md §45.1`): `§44.4` ranked four books by the work-inventory's *evidence token* when the question was what the corpus *rows* are, and got the order exactly backwards. `inner_sea_races` (68 of 72 rows) and `horror_adventures` (42 of 44) are ordinary replace-flag alternates needing **no new mechanism**; `core_essentials`' 48 are `PREABILITY`-gated subrace traits and `bestiary`'s 3 are `Unclassified` Drow Noble variant traits — **both mechanism-blocked**, and ingesting either as-is would have shipped the exact stub class `§44.2` describes. **Round 3 starts at `horror_adventures`' 44** — round 2's shape, repeated. Round 2 also closed a FOURTH instance of the stale-hardcoded-book-roots defect (in the ingest binary's own leak guard) and a latent `.MOD`-row ingest hazard that only race scope had been preventing. **`§8b`'s browse-screen render bug is still open and still owned by this lane** — round 2 did not reach it. Prior status **IN-FLIGHT (round 2)**, before that **READY (round 2)** — this is a **loop-until-dry** lane; a round delivers as much gate-verified work as its turn holds and a successor resumes from its receipt. **Round 1 landed 2026-08-11** (`sd29-racetrait-r1`, commits `4d362e2e`/`577d8e55`, receipt `progress.md` `## Cycle — epic-6-race-trait-lane-extend, ROUND 1`, `decisions.md §44`): `§43.5`'s probe repair (race_trait grounded **21 → 336**), the `beastiary`/`bestiary` book-spelling defect that was silently under-reporting 108 Bestiary 1 records, a live picker-vs-engine stub the Epic 6 pilot shipped for Monster Codex's 4 alternates, and APG's `Half-Orc ~ Plagueborn` (closing `decisions.md §39`'s deferral). **Round 2 starts at `core_essentials`' 48 and `bestiary`'s 3** — no new mechanism, chassis already loaded, and the probe repair means they ground the moment they land. Round 1's gate went RED first and is recorded that way (`progress.md` §6b): 3 root-full failures and 3 clippy warnings, every one attributed to this lane, all fixed at the source, ceiling not raised. **Round 2 also owns one finding round 1's on-screen pass caught** (`progress.md` §8b): the Race Traits browse screen's standard-trait column does not recompute when an alternate is selected — a render bug, evidenced as such, with the engine's own suppression proven green. Prior status `READY` (reopened by `epic-12-reopen`, `decisions.md §42`); the companion mis-classification fix IS landed and stays landed. The race-chassis ceiling (`crb::race_traits()` models exactly 7 races) is a real structural finding and SD-29 now owns confronting it — it is not a licence to close the lane. Prior receipts preserved | Race-Trait Lane — extend | corpus-wide, **26** books. **Denominators re-derived at the end of ROUND 2 (2026-08-11) from `docs/work-inventory.json`: `race_trait` 3,447 total / **407 grounded** / **3,040 remaining** by status (`not-ingested` 1,583 + `not-started` 1,457). Round 2 reproduced round 1's 3,447/336/3,111 exactly before moving it, so the two rounds' figures are commensurable.** **3,111 is not the lane's workload** (`decisions.md §44.4`): only **553** of the 3,447 name one of the 18 races the product models, and a race trait whose race has no chassis is unreachable by construction — `RaceCorpus::resolve` returns `None` without one. **Superseded by round 4 (2026-08-12), re-derived with the now-checked-in `scripts/race_trait_ceiling.py` and reproducing `§44.4`/`§47.1`'s 553 EXACTLY before widening it: the honest ceiling is **571**, because 18 `TYPE:<Race> Subrace` heritage-selector rows (all in `core_essentials`) are `race_trait`-kinded units in `docs/work-inventory.json` just like the 553 — they were in the lane's DENOMINATOR and out of its CEILING, the one combination that makes a lane look further from done than it is. Within the 571: **514 grounded / 57 remaining**, and only **3** of the 57 are workable (`bestiary`'s Drow Noble, needing a race-variant chassis). The other 54 are evidenced non-gap: APG 49 (ARG key collisions, `§39`), `core_essentials` 2 (`Aasimar ~ Default` / `Tiefling ~ Default`, the no-heritage baseline the engine's no-selection state already is, `§49.2`), HA 1 (`Half-Elf ~ Starchild`, `PRECAMPAIGN`-gated on Occult Adventures, `§47.2`), ISR 1 (`Human ~ Tribalistic Languages`, `§45.4`), MC 1 (`Oversized Goblin`, `§43`). Chassis-blocked residue is **2,876** (3,447 − 571), superseding `§44.4`'s 2,894 by those same 18 rows rather than correcting it. Corpus-wide `race_trait` grounded **450 → 514**, `units_ingested` 64. **Scope finding OUTSIDE this bundle:** `scripts/race_trait_ceiling.py --whole-tree` returns 897; of the 326 rows beyond the bundle's 38 book directories, **291 are ordinary no-new-mechanism alternate racial traits for the 18 modelled races** in twelve `player_companion/` books (`blood_of_fiends` 102, `blood_of_angels` 101, `blood_of_shadows` 19, …) and two `campaign_setting/` books — the largest known block of cheap race-trait work anywhere, not in `corpus-work-channels.md §10.2`'s 37 and therefore not this bundle's to take. Round 3's superseded text: re-derived by the same join and reproducing round 2's 553 / 407 exactly before moving it: within the 553, **450 grounded / 103 remaining**, and **not one of the 103 is ordinary ingest**. 51 are workable-with-a-new-mechanism (`core_essentials` 48 needing a `PREABILITY`-grant mechanism AND the 16 non-`race_trait`-typed subrace selector rows; `bestiary` 3 needing a race-variant chassis) and 52 are deliberately not gap (APG 49, ISR 1, MC 1, and HA's 1 `PRECAMPAIGN`-gated Occult Adventures row, `decisions.md §47.2`). Round 2's figure of **95** was correct when written; `horror_adventures`' 44 was the only mechanism-free entry in it and round 3 took 43 of them, leaving the OA-gated 1.** Round 2's superseded text: the genuinely ingestable remainder is now **95** (was 167 before round 2 took `inner_sea_races`' 72), and its ORDER is the corrected one: `horror_adventures` 44 (**no new mechanism**), `core_essentials` 48 (needs a `PREABILITY`-grant mechanism AND the 16 non-`race_trait`-typed subrace selector rows), `bestiary` 3 (needs a race-variant chassis). THREE residuals are deliberately not gap: APG's 49 ARG-key collisions (`§39`), Monster Codex's `Oversized Goblin`, and round 2's `Human ~ Tribalistic Languages` — each mechanism- or upstream-blocked with its finding recorded. The pre-round-1 figures this row carried — 21 grounded / 3,426 remaining — were correct when written and reproduced exactly before being superseded | `epic-6-race-trait-lane-pilot` | sd29-racetrait-r6 | 2026-08-12T17:30:00Z | SD29-E6-F2-006 (round 5 of a loop-until-dry lane; **DRY** — 3 workable units left, all needing a race chassis; round 5 shipped no ingest and a corpus-wide PI fix. Prior: SD29-E6-F2-005, round 4) |
| 11 | `epic-7-companion-lane-pilot` | **COMPLETE (round 1 landed 2026-08-12)** — the pilot's whole deliverable is a MECHANISM and it is built, gate-verified and pushed. `sd29-companion-r4`, commit `bac2f569`, receipt `progress.md` `## Cycle — epic-7-companion-lane, ROUND 1`, `decisions.md §48`. **The pinned pilot book `inner_sea_combat` was RE-CONFIRMED, not trusted** — the exact check the race-trait lane skipped: `python3 scripts/classify_companion_rows.py inner_sea_combat` → 4 creature rows + 6 ability rows = the 10 units the inventory counts, **ORPHAN 0**. All 10 ingested, grounded and verified on screen. **What shipped beyond the pilot book:** `companion_chassis.rs` (record types + a `COMPANION_BOOKS` registry every consumer iterates), `scripts/classify_companion_rows.py` (the corpus-ROW classifier `decisions.md §45.1` requires, CHECKED IN rather than left in a scratchpad), `scripts/transcribe_companion_tables.py`, a `gen_book_cache companion:<book>` generator writing ONE `data/corpus/<book>/companion/` directory for both structural shapes, `v06_work_inventory` grounding of `Kind::Companion` through the registry, `RuleSetId::Isc`/`::Isi`, `companion_catalog.rs` + `list_companion_catalog` + `CompanionCatalogScreen.tsx` + a hub link, and per-book `reach_gate` claims. **The Pets tab was never this surface** and `monster_catalog.rs` already said so: it renders `pilot_compute`'s two hand-grounded species (Wolf, Horse) whose values are Rust constants, and can never show a Griffon or a Clockwork Spy. **Item 8 caught a real near-miss**: the harness's new `companion` family needed its `SEARCH_Y` calibrated live at 247 — 285, the value every other single-chip-row family uses, lands below this screen's search box, and the filtered-count gate refused the run rather than screenshotting an unfiltered list. Prior status **READY** (reopened 2026-08-11 by `epic-12-reopen`, `decisions.md §42`); before that its round-1 cycle refused at Cycle-mechanics step 1c (`preflight-disk` EXIT=1, 91% used, twice) and round 2 died with its workflow having produced no commits. Both prior receipts preserved | Companion Lane — pilot | mechanism-build + a pilot book's ingest. **`inner_sea_combat` (10 units) needs re-confirming before dispatch** — the Epic 6 companion mis-classification fix moved 13 units into `companion` corpus-wide, so per-book companion counts have shifted since this row was written | `epic-3-provenance` | sd29-companion-r4 | 2026-08-12T13:00:00Z | SD29-E7-F1-002 |
| 12 | `epic-7-companion-lane-extend` | **DRY (round 9 landed 2026-08-13 — the lane's FINAL PASS)** — `sd29-companion-final-r1`, commits `d8422c30` (four books ingested end to end + the `PREABILITY` gate widening + the `#[path]` retirement + five test pins), `3ce4a1d4` (work-inventory regenerated on the merged tip), `8da9a31c` (the count-pin sweep: APG left the LICENSE.json exemption set), `231ee0d5` (the root-full floor had been counting 7 tests twice); **full gate `VERIFY_EXIT=0`, all 14 stages green at `231ee0d5` — `root-full` 6,313 across 544 suites with all 525 `tests/*.rs` suites executed**; item-8 **4 PASS**, one per book registered (`artifacts/SD29-E7-F2-010/item8/{um-giant-leech,arg-puffball,botd1-imp,apg-eidolon}.png`); earlier text: `3ce4a1d4` (work-inventory regenerated on the merged tip); receipt `progress.md` `## Cycle — epic-7-companion-lane, ROUND 9 (SD29-E7-F2-010)`, `decisions.md §69`. **Round 9 ingested the last four books with any reachable companion row — Ultimate Magic (32), Advanced Race Guide (14), Advanced Player's Guide (4) and Book of the Damned Volume 1 (2) — 52 units, all 52 grounded, NO new `RuleSetId` for any of them and ZERO units of any other kind moved** (structural status diff vs HEAD: exactly eight changes, two per book). Companion grounded **870 → 922**. **THE LANE IS DRY AND THE LAST UNIT IS CHASSIS-BLOCKED, NOT WORKABLE.** Re-derived two independent ways on the merged tip: `python3 scripts/classify_companion_rows.py` corpus-wide → `1696 units / 773 excluded / 923 reachable`, and `923 − 922 grounded = 1`; the per-book classifier-intersection derivation names the same single row, `core_essentials` `Pseudodragon ~ Tail`. **That row is not workable and the classifier over-reports it** — it is OWNED, so an ownership classifier calls it reachable, but its corpus line carries no `TYPE:`, no `DESC:` and no `BONUS:`, only `SOURCEPAGE:` and `ASPECT:ReachAttack|5 ft.`, so the transcriber's empty-payload screen (`§63.3`) correctly drops it; transcribed, the card a player opens reads "Tail" over a page number, and **the reach gate would have counted it as reached**. Remedy is a chassis widening (model PCGen's `ASPECT:`), measured on this tip at **34 of the 922 grounded companion rows carrying an `ASPECT:` that is dropped today — 33 diminished by the omission, exactly 1 emptied by it** — named, NOT taken, and routed to `successor-forward-scope-register.md` **C1.6**. Same proxy-defect class as `§68`'s two. **There is no next book**: all 16 books with any reachable companion row are registered, so a future companion round is a CHASSIS round, never a book round. **`§69.3` — a new `DESC:` gate kind, widened in BOTH directions**: Ultimate Magic's three vermin companions gate their text on `PREABILITY:1,CATEGORY=Special Ability,Companion Advancement (…)` and its `!PREABILITY` negation; `serve_desc_condition` refused both as designed, and rendering only the positive one would have left the "after" text on screen under no condition at all. **`§69.4` — two `#[path]` duplicates retired in `gen_book_cache`**: a `#[path]`-included `mod.rs` resolves `super::` against the BINARY's crate root, where there is no `companion_chassis`, so ARG's new `mod companion_data;` compiled in the library and failed in the generator; the binary now reaches ARG through the library crate exactly as it already reached `pathfinder_unchained`. **`§69.2` — the 393 rows that do not ship are ONE finding**: 361 are the summoner's evolution pool and the bladebound magus's black blade, both hanging off a CLASS FEATURE rather than any creature row — round 8's Core Rulebook finding, three more times. Prior status **READY (round 9)**; round 8 landed 2026-08-13 (`sd29-companion-r12`, commits `240c1865` (ingest + the class-row screen + the `MODULE_DIR` fix + two test pins), `736dfd3d` (decision §65 + a classifier label that had stopped being true), `bcbb12f1` (per-agent vite dev port + the frontend test roster + item-8 artifacts); **full gate `VERIFY_EXIT=0`, all 14 stages green — re-run on the MERGED tree at `75e2711f` after the monster lane pushed Ultimate Psionics mid-cycle, `root-full` 6316 passed across 544 suites with all 525 `tests/*.rs` suites executed** (6312 pre-merge; the +4 are the monster lane's). All four of this cycle's registration points re-verified BY CONTENT on the merged tree, and the inventory regenerated there changed only `generated_at` — the two lanes are genuinely file-disjoint; item-8 PASS artifact `artifacts/SD29-E7-F2-009/item8/crb-companion-crocodile.png`; receipt `progress.md` `## Cycle — epic-7-companion-lane-extend, ROUND 8 (SD29-E7-F2-009)`, `decisions.md §65` — number RESERVED at claim time per §53, leaving §64 for the concurrent monster lane). **Round 8 ingested Core Rulebook — 84 of its 170 companion units (38 creature rows, 46 ability rows), all 84 grounded, no new `RuleSetId` and ZERO units of any other kind moved.** Companion grounded **786 → 870**. **The book had sat unstarted through seven rounds behind a REFUSAL, not a missing mechanism**: the transcriber raised `SystemExit` on any book carrying `*_classes_companion.lst` rows, and `core_rulebook` was the lane's largest remaining book. `§65.1` widened it to the same drop-and-name screen the `.COPY=` rows already used — the deliberate answer being DROP-AND-NAME, because a PCGen monster class is a hit-dice progression and modelling it is a NEW RECORD TYPE, declared and NOT taken. The widening changed no book's shipped output: the classifier had always counted those rows as excluded. **`§65.3` — a second module that would have compiled, passed its own tests and been reachable from nothing:** `MODULE_DIR` mapped only `bestiary → beastiary1`, and this book's corpus id is `core_rulebook` while its engine module is `crb`, so an unmapped run would have written a SECOND `companion_data.rs` for a book that already has one. Caught by reading the transcriber's own comment before running it — the gate cannot see an unreferenced module. `advanced_players_guide → apg` added in the same edit. **`§65.2` — the sixth unmodelled-facet shape** (`TYPE:NaturalAttack.NaturalAttackSecondary.Secondary`), which SHIPS where `§63.3`'s row was dropped: that row carried no `TYPE:`, no `DESC:` and no `BONUS:`; this one carries all three plus `SOURCEPAGE:p.301`. **An unmodelled FACET is not an empty RECORD.** **`§65.4` — the first registered book whose shortfall is ONE finding rather than per-row accidents**: the 84 orphans are the generic `Animal Companion ~ …` / `Animal Trick ~ …` rows, orphaned precisely BECAUSE they hang off the Animal Companion CLASS — the same missing record type as the 2 class rows, from the other side. **TWO CROSS-LANE ITEMS A ROUND-9 DISPATCH MUST CARRY.** (1) **`§65.7`, a harness defect that produced two FALSE item-8 failures and could equally have produced a false PASS:** `vite.config.ts` pinned port 1420 with `strictPort:true` for EVERY agent, so this cycle's vite could not bind and `tauri dev` attached to a SIBLING worktree's dev server — this tree's Rust backend painted by another tree's frontend, every record present, the screenshot evidence about a tree the agent does not control. The prior mitigation was `kill -9` on whoever held 1420, i.e. clobber the sibling, so both outcomes were wrong. Fixed at the source: `driver.sh` derives `CODEX_DEV_PORT` from the same `DISPLAY_NUM` that already uniquifies the display, `vite.config.ts` reads it, `tauri dev` gets a matching `--config build.devUrl`; `default` keeps 1420 so a solo run is unchanged. **The second-order lesson: `RUN_DESKTOP_AGENT` was set correctly and the collision happened anyway, in a resource the guard was never wired to — a concurrency guard protects the resources it enumerates, not the concept of 'this agent's app'.** (2) **DoD item 3 is RED bundle-wide and is NOT this lane's**: `cargo run --locked --bin v06_corpus_trap_report -- --audit` exits **2** on three `[wiring-class-mismatch]` defects, all `inner_sea_gods` `monster/` records landed by the monster lane in `1c7d8ef9`, which PREDATES companion round 7 — and round 7's receipt does not mention item 3 at all. Zero defects are in companion territory and this cycle touches 0 `inner_sea_gods` files; `scripts/verify.sh` does not run `--audit`, which is how a green gate hid it. Reported, not fixed — fixing it means editing the concurrently-running monster lane's live records. `§65.8`: the frontend `SERVED_BOOK_CODES` roster gained `CRB` — the suite passed 99/99 with the book shipping and UNLABELLED, because the roster held twelve codes and `CRB` was not one; third instance of `§44.2`'s stale-roster defect class in this bundle. **The lane is NOT dry.** Prior status **READY (round 8)** — round 7 landed 2026-08-13 (`sd29-companion-r11`, commits `fb873737` (ingest + three mechanisms + one test), `380db42d` (the NINTH registration point + a stale-upward wire pin), `a8f720d6` (merge + the §62→§63 renumber, the monster lane having claimed §62 concurrently), `2bbbeff0` (inventory regenerated on the merged tree), `17121fdb` (two unmodelled `TYPE:` shapes); **full gate `VERIFY_EXIT=0`, all 14 stages green on run 3, `root-full` 6312 passed across 544 suites with all 525 `tests/*.rs` suites executed**; item-8 PASS artifact `artifacts/SD29-E7-F2-008/item8/ce-companion-stirge.png`; receipt `progress.md` `## Cycle — epic-7-companion-lane-extend, ROUND 7`, `decisions.md §63`). **Round 7 ingested Core Essentials — 102 of its 145 companion units (58 creature rows, 44 ability rows), all 102 grounded, and ZERO units of any other kind moved.** Companion grounded **684 → 786**. Verified BY CONTENT on origin: `git cat-file -p origin/tranche/9:src/rules_core/rules_tables/core_essentials/companion_data.rs | grep -c 'CompanionRecord {'` → **58**, `| grep -c 'CompanionAbilityRecord {'` → **44**. **The round's dispatched hazard was FALSE and checking it was the point:** the row said this book "needs a NEW `RuleSetId`" — `RuleSetId::Ce` was already compiled by the race-trait lane in `§49` (`grep -n 'Ce\b' src/rules_core/rules_tables/mod.rs` → line 150), so registration cost no scope flip at all. **`§63.1` is the round's headline mechanism and its shape is the transferable half:** `§59.2` built the `.COPY=`/`.MOD` delta screen one round earlier and ran it over ABILITY rows alone, because Bestiary 4 — the book that forced it — carries the shape only there. This is the first companion book whose CREATURE rows carry it, 22 of them (`ce_races_familiar_cr.lst:33` reads `Bat.COPY=Bat (Celestial)` and carries `OUTPUTNAME:`, `TEMPLATE:`, `KIT:` — no `SIZE:`, no `MOVE:`, no `MONSTERCLASS:`; the eleven CRB familiars each appear twice, `(Celestial)` and `(Fiendish)`). **A guard written for the book that provoked it holds for that book and quietly does nothing for the next one.** The RED was an EXISTING test — `every_registered_creature_states_its_monster_class_token_verbatim` went red on all 22 at once — and the screen is placed where the creature set is BUILT, not beside the ability screen, because `creature_display` (shape 5, keyed on `OUTPUTNAME:`, and `Bat (Celestial)`/`Bat (Fiendish)` BOTH say `OUTPUTNAME:Bat`) and `creature_species` (shape 3) are derived from it and are both actively wrong with a delta row present. Widening changed **not one byte** of the eleven registered books. **`§59.2`'s `mod_only` half, shipped as "stated, not exercised" and naming this book, is now EXERCISED** (the 4 `Universal Monster Rule ~ …` overlays). **`§63.2` is a PI COMPLIANCE defect this round would otherwise have shipped:** all four `LICENSE.json` writers hardcoded `"records_redacted": 0` beside a `records_processed` derived from disk. `core_essentials` was ingested first by the race-trait lane, which redacted **9** of 64 records, so the generator run silently rewrote 9 → 0 while all nine `[redacted PI]` markers stayed on disk — a book-wide claim that nothing was redacted, published over the evidence that nine things were. `count_on_disk_redactions` now derives it from the same walk as the numerator; the file carries `records_redacted: 9`, `records_processed: 166`. **`§63.3`: 102 ship against 103 reachable, and the one-row gap is a FINDING, not a miscount.** `Pseudodragon ~ Tail` is OWNED (the classifier is right) and carries no `TYPE:`, no `DESC:` and no `BONUS:` — only a `SOURCEPAGE:` and an `ASPECT:`. Across all twelve registered books it is the ONLY such row, of 394 grounded ability rows checked. **Reachability is a fact about ownership; shippability is a fact about the record type**, and this is the first book where they differ. The screen is `reach_gate::companions_reach`'s own payload predicate **minus `source_page`** — so this row would have PASSED the reach gate while showing a player nothing. **`§63.4` corrects a figure every companion receipt since round 1 has carried: the lane's per-book cost is NINE surfaces, not eight.** `corpus_ingest_diagnostic`'s `book_status` row was never counted, and this is the first round that could have found it — the first companion book whose `rules_tables/` directory did not already exist for another family's sake. Reading that failure found a second the test cannot see: `ultimate_wilderness_counts()` inserts only `feats`, so **round 6's 327 companion records had been absent from the Corpus Ingest panel since the day they landed**. Both fixed. Prior status **READY (round 7)** — this is a **loop-until-dry** lane; a round delivers as much gate-verified work as its turn holds and a successor resumes from its receipt. Prior receipts preserved. **READY (round 7)** — round 6 landed 2026-08-12 (`sd29-companion-r10`, commits `ae3dad3a` (ingest + three mechanisms + five tests), `cea8fa9c` (the §60→§61 renumber, the monster lane having claimed §60 concurrently) and `2799bc08` (three stale-upward baselines); **full gate `VERIFY_EXIT=0`, all 14 stages green, `root-full` 6296 passed across 544 suites**; item-8 PASS artifact `artifacts/SD29-E7-F2-007/item8/uw-companion-spitting-cobra.png`; receipt `progress.md` `## Cycle — epic-7-companion-lane-extend, ROUND 6`, `decisions.md §61`). **Round 6 ingested Ultimate Wilderness — 327 of its 575 companion units (169 creature rows, 158 ability rows), all 327 grounded, no new `RuleSetId` and zero units of any other kind moved** (`RuleSetId::Uw` was compiled by SD-28 Epic 26). Companion grounded **357 → 684**. Its 169 creature rows are **more than every previously registered companion book combined** (166). **Round 6 is the first round in four where reading the rows the classifier was about to throw away did NOT move the ceiling up** — it found the opposite, and that is the round's most reusable output. `§61.2`: of the 247 orphans, **30** are the book's `CATEGORY:Archetype` companion/familiar archetype rows, **119** are ability rows namespaced under those archetypes' DISPLAY names (ownership shape 5 exactly, except the owner is an archetype rather than a creature), and **72** are the generic option groups `Animal Trick ~ …` (39) and `Animal Companion Feat ~ …` (33) that attach to ANY animal companion. That is a real ownership relation the corpus states and this round deliberately did NOT take it: an archetype has no `SIZE:`, `MOVE:` or `MONSTERCLASS:`, so `CompanionRecord` cannot hold one and `CompanionCatalogScreen` has no section that would show one — widening the predicate would have made 149 rows 'reachable' in the classifier and shipped them under a creature they do not belong to, the stub class `§44.2` describes arriving by the exact route `§45.1` exists to prevent. **They are also NOT an `OPEN_FINDINGS` entry, and the transcriber's own generated boilerplate had been claiming otherwise since round 4** — that list is keyed by (book, FAMILY) and `unsurfaced_families_are_exactly_the_recorded_findings` fails an entry for a family that DOES reach a player; the sentence was unfalsifiable while every registered book had zero orphans, and this is the first book that could falsify it. Fixed at the source. **The mechanism round 6 built is conditional `DESC:` variants** (`§61.1`): **22** of the book's ability rows — `Poison`, `Constrict`, `Breath Weapon`, `Spray`, `Camouflage`, `Saber-Toothed Bite`, the abilities that make a companion a companion — state their rules text between 2 and 9 times, each token gated on a different `PREVARGTEQ:`/`PREVARLT:`/`PREALIGN:` predicate, and `parse_desc` had refused that shape outright since round 1 ('refuses to pick one by position. Widen it deliberately') without the refusal ever firing. All tokens now ship, none is evaluated, each keeps its own gate AND its own `%N` argument list verbatim in `CompanionAbilityRecord::description_variants`; `description` stays the single UNGATED token when there is exactly one, so every previously shipped record is byte-identical. All 8 shipped UW rows of this shape have `description: None`, so `reach_gate::companions_reach`'s payload predicate was widened too — without it a Spitting Cobra's poison would have judged as identity-only while the screen renders two paragraphs under it. **8 shipped vs 22 in the file is the finding, not a discrepancy**: the other 14 are archetype rows. **`§61.3` is the round's largest defect and it was program-wide, not lane-local**: `pcgen_desc::render_pcgen_desc_with_values` documented `%%` as 'never an argument reference' and emitted `%` then the digit, while `leaked_pcgen_syntax` rejects the resulting `%N` — two shipped functions that cannot both be right, latent because **no ingested record carried the shape** (`grep -rl '%%[0-9]' data/corpus/` → 0 before this round; 4 tokens in the whole Paizo tree, all reading `… must make a DC %%1 Fortitude save …|<DC variable>`, an upstream escaping typo). The narrow reading ships — `%%N` is an argument ONLY when argument N exists — and it needed a change in TWO places: `max_arg_reference` skipped past `%%` without counting the digit, so 'does argument N exist' was answered by a function that had already discarded the question and the new renderer branch was unreachable. **The test was RED for exactly that reason before it was green**, which is the only reason the second half was found rather than shipped as a no-op. A second, quieter defect surfaced with it: `companion_catalog::serve_ability_description` had been handing the renderer the `DESC:` prose ALONE (the transcriber splits a token into `description` and `description_variables` and the wire only read the first) — inert for every registered book, decisive here. `§61.4`: **15 rows spell `TYPE:SpecialQuaility`**, one transposition from the modelled `SpecialQuality`, and are deliberately NOT corrected into the facet — `type_segments` carries the corpus's own spelling to the screen, and both the chassis and wire tests pin the count AND the spelling so a successor must delete an assertion deliberately. Unmodelled-facet records 5 → 20, wire rows 7 → 121. `§61.5`: a `PRECAMPAIGN` gate checked and correctly NOT counted (`support/uw_abilities_companion_pu.lst` is gated on Pathfinder Unchained, which IS ingested; all 17 of its rows are orphans anyway). Round 6 also fixed a generator defect it caused: `transcribe_companion_tables.main` opened its output file BEFORE transcribing, so a refused run left an EMPTY generated module behind — nothing in the gate would have caught it, because an unreferenced module compiles fine. Prior status **READY (round 6)** — this is a **loop-until-dry** lane; a round delivers as much gate-verified work as its turn holds and a successor resumes from its receipt. Prior receipts preserved. Prior status: **READY (round 6)** — round 5 landed 2026-08-12 (`sd29-companion-r9`, commit `2481e31e` (ingest + both mechanisms + four tests); receipt `progress.md` `## Cycle — epic-7-companion-lane-extend, ROUND 5`, `decisions.md §59`). **Round 5 ingested Bestiary 4 — 78 of its 80 companion units (34 creature rows, 44 ability rows), all 78 grounded, ZERO orphans dropped, no new `RuleSetId` and zero units of any other kind moved** (`RuleSetId::B4` was already compiled by the monster lane's `52da4bc3`, the same free registration `bestiary` and `bestiary_3` had). Companion grounded **279 → 357**. **For the THIRD consecutive round the orphan instrument was found to be UNDER-claiming, and again the discovery came from reading the rows the classifier was about to throw away** (`§45.1` as amended by `§56.1`). `§56.3` predicted this book would be the first to exercise `§50`'s orphan-drop disposition; it did not, because all five of its orphans are owned. `§59.1` **ownership shape 6, RELAY ROWS**: `b4_races_companion.lst:22` `Familiar (Giant Flea)` never names `Flea (Giant) ~ Disease` — it names `Racial Traits ~ Flea (Giant)`, a `CATEGORY:Internal` row at `b4_abilities_companion.lst:56`, and THAT row carries the `ABILITY:Special Ability|AUTOMATIC|Flea (Giant) ~ Disease|…` token. Shape 4 is exactly this closure and cannot see it, because **the middle row is not an inventory unit** — `v06_work_inventory` does not count `CATEGORY:Internal` rows. The first hop is also read under ANY `ABILITY:<Category>|AUTOMATIC|` category (the creature's own token says `Internal`, not `Special Ability`); shape 1 keeps its narrower predicate, so nothing that was a non-owner becomes an owner by a looser read of an existing link. **The token was already being parsed**: `transcribe_companion_tables.parse_natural_attacks` has read `ABILITY:Internal|AUTOMATIC|` since round 1 and explicitly skips the entries containing ` ~ ` — precisely the relays. Corpus-wide shape 6 recovers **15** units (5 `bestiary_4`, 10 `core_essentials`) and changes **not one byte** of the nine already-registered books: all nine were regenerated and `git status --porcelain` listed none of their `companion_data.rs` files. **`§59.2` is the round's second mechanism and it moves the ceiling DOWN — the first correction in four rounds that does.** `gen_book_cache`'s `verified_citation_line` REFUSED the book on its first run: `b4_abilities_companion.lst:99` reads `CATEGORY=Special Ability|Change Shape.COPY=Pooka ~ Change Shape`, not the record's name. Bestiary 4 is the first companion book carrying `.COPY=` rows, and the monster lane has screened them since Bestiary 2 for a reason that transfers unchanged: a `<Base>.COPY=<Variant>` row states a DELTA on a base record elsewhere, so transcribed verbatim it ships a card with an `ASPECT` and nothing else. The companion transcriber now screens `origin in (copy, mod_only)` and scrubs the dropped keys from their owners' `ability_keys` so the chassis link stays closed both ways; the `mod_only` half is **stated, not exercised** (`core_essentials` 4 and `ultimate_wilderness` 1 will be the first to hit it). **The ceiling was also being computed as a SUM of its exclusions when the sets overlap** — 735 orphans + 2 `PRECAMPAIGN` + 7 class + 30 delta = 774, but exactly one row is both an orphan and a delta, so **773** distinct rows are excluded. `§51.1` and `§54.2` each fixed a missing TERM; this fixes the arithmetic. Prior status **READY (round 5)** — this is a **loop-until-dry** lane; a round delivers as much gate-verified work as its turn holds and a successor resumes from its receipt. Prior receipts preserved | Companion Lane — extend | corpus-wide, **5** books remaining. **Denominators re-derived 2026-08-13 at the end of round 7 with the commands recorded in the receipt: `companion` 1,696 total / **786 grounded** / 910 remaining by status. 910 is NOT the lane's workload.** The REAL ceiling is **922** — round 6's 923 minus the ONE row `§63.3` drops, the first correction that moves it DOWN for a reason other than arithmetic — so the honest remainder is **136**. `922 − 786 = 136` and the five remaining books' per-book reachable counts sum to 136 independently (`python3 scripts/classify_companion_rows.py core_rulebook ultimate_magic advanced_race_guide advanced_players_guide book_of_the_damned_volume_1` → 615 units / 479 excluded / **136 reachable**); the two derivations close exactly. Ranked by reachable count: `core_rulebook` 170 / 86 / **84**, `ultimate_magic` 170 / 138 / **32**, `advanced_race_guide` 32 / 18 / **14**, `advanced_players_guide` 212 / 208 / **4**, `book_of_the_damned_volume_1` 31 / 29 / **2**. **Three hazards for round 8:** (a) **`core_rulebook` at 84 is the only book left above 40** and is the obvious next take; it, `ultimate_magic` and `book_of_the_damned_volume_1` still carry the 7 `*_classes_companion.lst` class rows the transcriber refuses BY NAME (it raises rather than dropping silently), and `core_rulebook` needs a `RuleSetId` check of its own — do not assume either way, `§63` is the second consecutive round whose dispatched `RuleSetId` hazard was false; (b) **`advanced_players_guide` (4 reachable of 212) and `book_of_the_damned_volume_1` (2 of 31) remain FLOORS, not queued work** — a round taking either pays a full book's registration cost, now known to be NINE surfaces, for a handful of records and should say so up front; (c) **the two things this lane knows about and cannot hold are both NEW RECORD TYPES, not wider predicates**: Ultimate Wilderness's 149-row archetype block (`§61.2`) and the `ASPECT:` token (`§63.3`), which no chassis in this program models and which **27 of the 394 grounded companion ability rows carry and drop today** — only one of them is emptied by the omission, the other 26 merely diminished. A round taking either should declare it up front rather than discover it at the ceiling table. The prior figures **923 ceiling / 684 grounded / 239 honest remainder** were correct when written and were REPRODUCED EXACTLY by round 7 before this book's ingest superseded them. corpus-wide, **6** books remaining. **Denominators re-derived 2026-08-12 at the end of round 6 with the commands recorded in the receipt: `companion` 1,696 total / **684 grounded** / 1,012 remaining by status. 1,012 is NOT the lane's workload.** The REAL ceiling is **923** — unchanged by this round, the first in four that did not move it, and `§61.2` is why — so the honest remainder is **239**. `923 − 684 = 239` and the six remaining books' per-book reachable counts sum to 239 independently (`python3 scripts/classify_companion_rows.py core_essentials core_rulebook ultimate_magic advanced_race_guide advanced_players_guide book_of_the_damned_volume_1` → 760 units / 521 excluded / **239 reachable**); the two derivations close exactly. Ranked by reachable count: `core_essentials` 145 / 42 / **103**, `core_rulebook` 170 / 86 / **84**, `ultimate_magic` 170 / 138 / **32**, `advanced_race_guide` 32 / 18 / **14**, `advanced_players_guide` 212 / 208 / **4**, `book_of_the_damned_volume_1` 31 / 29 / **2**. **Three hazards for round 7:** (a) **`core_essentials` is the largest and cheapest real book left** — 6 companion `.lst` files, 22 `.COPY=` rows, 4 `mod_only` rows, and it is the book that will first exercise the `mod_only` half of `§59.2`'s delta screen, still **stated, not exercised**; it needs a NEW `RuleSetId` and **you must check rather than assume**, because the race-trait lane added `RuleSetId::Ce` in `§49`; (b) `core_rulebook`, `ultimate_magic` and `book_of_the_damned_volume_1` still carry the 7 `*_classes_companion.lst` class rows the chassis refuses by name; (c) **`advanced_players_guide` (4 reachable of 212) and `book_of_the_damned_volume_1` (2 of 31) remain FLOORS, not queued work**. **The largest thing this lane now knows about and cannot take is the ARCHETYPE block** — 149 Ultimate Wilderness rows plus whatever the other five books carry of the same shape. It is a NEW RECORD TYPE (`CompanionArchetypeRecord`) plus a screen section, not a wider ownership predicate, and a round that takes it should declare that up front rather than discover it at the ceiling table. The prior figures **923 ceiling / 357 grounded / 566 honest remainder** were correct when written and were REPRODUCED EXACTLY by round 6 before this book's ingest superseded them. Round 5's superseded text: corpus-wide, **7** books remaining. **Denominators re-derived 2026-08-12 at the end of round 5 with the commands recorded in the receipt: `companion` 1,696 total / **357 grounded** / 1,339 remaining by status. 1,339 is NOT the lane's workload.** The REAL ceiling is **923** — 1,696 minus the **UNION** of 735 orphan ability rows, 2 `PRECAMPAIGN`-gated rows, 7 `*_classes_companion.lst` class rows and 30 `.COPY=`/`.MOD` delta rows (773 distinct, not 774) — so the honest remainder is **566**. `923 - 357 = 566` and the seven remaining books' per-book reachable counts sum to 566 independently; the two derivations close exactly, and the nine GROUNDED books' reachable counts sum to **357**, the grounded figure to the unit. Ranked by reachable count, every figure from round 5's own classifier run (`python3 scripts/classify_companion_rows.py`): `ultimate_wilderness` 575 units / 248 excluded / **327 reachable** (the largest single block left), `core_essentials` 145 / 42 / **103**, `core_rulebook` 170 / 86 / **84**, `ultimate_magic` 170 / 138 / **32**, `advanced_race_guide` 32 / 18 / **14**, `advanced_players_guide` 212 / 208 / **4**, `book_of_the_damned_volume_1` 31 / 29 / **2**. **Three hazards for round 6:** (a) **`core_essentials` is the book shape 6 most changed** (26 orphans → 16) and the one that will first exercise the `mod_only` half of `§59.2`'s screen — 6 companion `.lst` files, 22 `.COPY=` rows, 4 `mod_only` rows, and it needs a NEW `RuleSetId` (nothing in `src/` compiles it today — check before writing it); (b) **`ultimate_wilderness` is the largest block left at 327 and needs no new `RuleSetId`** (`RuleSetId::Uw`, SD-28 Epic 26), but carries **169** creature rows, more than every registered book combined; (c) `core_rulebook`, `ultimate_magic` and `book_of_the_damned_volume_1` still carry the 7 `*_classes_companion.lst` rows the chassis refuses outright — the transcriber raises on them by name rather than dropping them silently. **`advanced_players_guide` (4 reachable of 212) and `book_of_the_damned_volume_1` (2 of 31) are effectively FLOORS, not queued work**, the same reachable-exhausted shape `§57` recorded for `bestiary_4`'s monster half; a round taking either pays a full book's registration cost for a handful of records and should say so up front. The prior figures **937 ceiling / 279 grounded / 658 honest remainder** were correct when written and were REPRODUCED EXACTLY by round 5 before shapes 6 and the delta screen superseded them | `epic-7-companion-lane-pilot` | sd29-companion-r12 | 2026-08-13T03:30:00Z | SD29-E7-F2-008 (round 7 of a loop-until-dry lane; **136** reachable units remain across 5 books, `core_rulebook` at 84 is the largest, the per-book registration cost is NINE surfaces not eight, and the two blocked blocks — the 149-row archetype block and the `ASPECT:` token — are both new record types) |
| 13 | `epic-9-version` | COMPLETE | Build Version Numbering | first concrete value `0.9.<build>` | `epic-1-identifier` | sd29-e9-version | 2026-08-11T00:00:00Z | SD29-E9-F1-001 |
| 14 | `epic-8-toolkit` | DECISION-BLOCKED | DM Toolkit extension | RULED 2026-08-11 under UNATTENDED MODE item 4: does NOT land inside SD-29; surfaces as the Class 3 retrofit C3.1. Criterion (`epic-breakdown.md` Epic 8 / `loop-instruction.md` "Epic ordering") is "a lane cycle needed the consumer surface to satisfy its reach claim" — unmet: both of Epic 5's pilot reach claims assess the already-shipped `list_monster_catalog`, zero assess a toolkit surface | `epic-5-monster-lane-pilot` | sd29-e8-toolkit | 2026-08-11T00:00:00Z | SD29-E8-F1-001 |
| 15 | `epic-10-review` | **COMPLETE** — run 2 executed 2026-08-13 against the pushed tip `de303fd0`, branch point `a1295856`, 246 commits / 4,632 files / +305,921−15,513. Gate PRECONDITION met on the review's own tree: `./scripts/verify.sh` FULL **exit 0**, 14/14 stages, `root-full` 6316/544 with all 525 `tests/*.rs` suites executed, `reach` 27. **Five findings, triaged per `decisions.md §27`: two fixed-in-bundle (`4d22ecbb`), three deferred with a named owner (`successor-forward-scope-register.md` C1.4).** Full receipt `progress.md` `## Epic 10 — Bundle Code Review, RUN 2`; ruling `decisions.md §66`. **One condition Epic 11 must not soften:** `§27`'s bundle-scope `wired-integration-audit.sh` run is NOT clean (13 `placeholder` hits, all hand-classified as non-stubs, remedy named in C1.4b) — any closure statement claiming it clean repeats run 1's error | Bundle Code Review | full-bundle diff review vs. branch point (`decisions.md §27`), re-run against the reopened bundle's final diff | `epic-4-proven-equip-mod`, `epic-4-proven-spell`, `epic-4-proven-feat-race-class`, `epic-5-monster-lane-extend`, `epic-6-race-trait-lane-pilot`, `epic-6-race-trait-lane-extend`, `epic-7-companion-lane-pilot`, `epic-7-companion-lane-extend`, `epic-9-version`, `epic-8-toolkit` (COMPLETE or `decision-blocked`) | sd29-review-r2 | 2026-08-13T00:00:00Z | SD29-E10-F2-001 |
| 16 | `epic-11-closure` | **COMPLETE — SD-29 IS CLOSED.** **RUN 3 executed 2026-08-13 (`sd29-closure-r3`, card `epic-11-closure-run3`, receipt `progress.md` `## Cycle SD29-E11-F1-003`).** Run 2 refused to close with 63 workable units outstanding; run 3 re-derived the same three splits with the same checked-in classifiers after cards 8 and 12 landed their final passes, and found **0 WORKABLE units in every lane** — `companion` 1 classifier-reachable row, `ASPECT:`-blocked (C1.6); `monster`+`monster_ability` 66 classifier-reachable, all non-workable (54 cross-table owners + 4 `.MOD` overlays + 7 PI + 1 negated-PCC-gate row), plus 229 mechanism-blocked (C1.5); `race_trait` 58 inside its 571-row ceiling, each with a recorded finding, and 2,876 chassis-blocked. Closure work landed: `docs/architecture/` refreshed to current-state truth (`c584318b`), `release-notes.md` "Known issues" rewritten into real shipped issues + owned residue, all four Decision-27 deferrals confirmed to carry a named owner, gate run FULL with the exit code captured directly, DoD item 8 PASS on all three reopened lanes at the closing tip, and **PR #360 UPDATED in place — not replaced, and NOT merged; the operator merges.** Nothing stranded: `git branch -a --no-merged tranche/9` returns 2 refs, both accounted for by content |AUTOMATIC|` bundle hop. `race_trait` is **DRY** and card 10's ruling was reproduced exactly (ceiling 571, `{'grounded': 513, 'not-ingested': 58}`, the 58 all carrying recorded findings). **The raw remainders (826 + 1,515 + 2,934 = 5,275) are NOT the workload** — reading them as such is the error in one direction and run 1's disposal is the error in the other. Run 2 therefore did NOT refresh `docs/architecture/`, did NOT rewrite `release-notes.md`'s "Known issues" into shipped issues, and did NOT touch PR #360 — all three are closure edits that would have made the documents describe a state the bundle has not reached, which is exactly how run 1's documents came to disagree with its board. Run 2 also recovered the shared checkout's base (diverged 2 ahead / 143 behind; merged not reset, `0ddfc126`, pushed before any other work) and resolved a duplicate-`§46` collision by keeping both decisions and renumbering the unpushed one to `§67`. Gate on the merged tip: `./scripts/verify.sh` FULL, **`VERIFY_EXIT=0`**, 14/14 stages, `root-full` 6,316 across 544 suites with all 525 `tests/*.rs` suites executed. **Run 3's precondition is now MET: cards 8, 10 and 12 are all `DRY` as of 2026-08-13** — the monster lane's round 11 (`sd29-monster-final-r1`) and the companion lane's round 9 (`sd29-companion-final-r1`, `decisions.md §69`) each closed their lane out and each recorded its residual as chassis-blocked with a named successor owner (C1.5, C1.6). **Run 3 must still re-derive both rulings rather than transcribe them**, which is the whole lesson of run 1. Original wording: run 3 fires only after cards 8 and 12 reach their own ceilings; `progress.md` `## Cycle SD29-E11-F1-002` §6 lists what a real closure still owes, including `decisions.md §66`'s unsoftened condition that the bundle-scope `wired-integration-audit.sh` run is NOT clean. Prior: **READY** (reopened 2026-08-11 by `epic-12-reopen`, `decisions.md §42`) — the 2026-08-11 closure (`ac217788`) was **premature and is rescinded by operator directive**. It closed the bundle with three lanes unfinished and disposed of them outward. **PR #360 (`tranche/9` → `develop`) stays OPEN and unmerged**; the operator merges it at real closure. Receipts preserved: `progress.md` `## Cycle SD29-E11-F1-001` (rescinded) and `## Cycle SD29-E11-F1-002` (run 2, not-closed) | Closure Epilogue | closure rollup + tranche promotion PR (#360 already open — do NOT open a second one) | all cards above (COMPLETE or `decision-blocked`) | `sd29-closure-r3` | 2026-08-13T11:00:00Z | SD29-E11-F1-003 (run 3, CLOSED) |
| 18 | `epic-13-desktop-driver-fix` | COMPLETE | Desktop Driver Fix — make Definition-of-done item 8 (on-screen verification) satisfiable again | diagnose + fix `apps/desktop/.claude/skills/run-desktop/driver.sh`; add its self-test to the gate; backfill on-screen checks for the families run 1 shipped without item 8 | none (tooling; blocks item 8 for every other card) | sd29-driver-fix | 2026-08-11T12:30:00Z | SD29-E13-F1-001 |
| 17 | `epic-12-reopen` | COMPLETE | Reopen Correction — make the package's own documents tell the truth after the premature closure | doc-only: record `decisions.md §42`, reset cards 8-12 and 15-16, correct `progress.md` + `release-notes.md`, re-derive the four lane denominators, emit the correction event | none (supersedes `epic-11-closure`) | sd29-reopen | 2026-08-11T15:00:00Z | SD29-E12-F1-001 |

> **Cycle-id collision, recorded not silently fixed (2026-08-11).** Cards `epic-4-proven-equip-mod`
> and `epic-4-proven-spell` both minted `SD29-E4-F1-001`: the two lanes ran concurrently in
> isolated worktrees and neither could see the other's claim before pushing. The ids are therefore
> **not** unique in this bundle — disambiguate a receipt by its card id and `Claimed-by` actor
> (`sd29-e4-equip` vs `sd29-e4-spell`), both of which are unique, not by cycle-id alone. Neither
> receipt is rewritten, because the id is already committed in each lane's commit messages and
> `progress.md` heading. A future concurrent split of one epic should suffix the lane
> (`SD29-E4-F1-001-equip` / `-spell`) at claim time.

> **Epic 6 pilot book carries zero race traits — operator re-pin needed (2026-08-11,
> `sd29-e6-racetrait-pilot`).** Card `epic-6-race-trait-lane-pilot` split on delivery. The
> **classifier defect fix is COMPLETE** (`corpus-work-channels.md` §9.3's name-coincidence defect;
> grounded race traits 44 → 21, see `progress.md`). The **per-book pilot ingest is
> `decision-blocked`**: all 9 of `inner_sea_intrigue`'s `race_trait`-kinded units come from
> `isi_abilities_race_companion.lst` and are Clockwork Familiar / Clockwork Spy *construct-companion*
> abilities, not racial traits of any player race — `file_kind()` types that file `race_trait` by
> filename. The unit count (9) was right; the kind was not. This is loop-instruction.md's named hard
> stop ("a book's derived shape contradicts its recorded ingest subtype — the cycle reports; the
> operator re-pins the book list"), so no pilot book was substituted unilaterally.
>
> **Re-pin candidates**, smallest first, re-derived from `docs/work-inventory.json` excluding
> `*companion*.lst` sources: `ultimate_intrigue` (3), `ultimate_magic` (3), `inner_sea_bestiary` (4),
> `ultimate_combat` (4), `monster_codex` (14), `bestiary` (21). `monster_codex` is the recommended
> pick — it is the book DoD item 6 already expects to retire the standing
> `beastiary1/race_traits` `OPEN_FINDINGS` entry, so the pilot and that retirement land together.
>
> **RE-PIN LANDED, 2026-08-11 (`sd29-racetrait-repin`, card 9, `decisions.md §43`).** The pilot
> book is **`monster_codex`**. The candidate figures above were re-derived independently and
> reproduce exactly, with two additions the note omitted (`book_of_the_damned_volume_1`/`_2` at
> **1** unit each, not 2). The decisive reason for the pick is stronger than the one recorded above:
> reading the candidates' actual unit keys rather than their counts, **five of the six carry the
> same defect that disqualified `inner_sea_intrigue`** — eidolon rows, `Racial SLA ~ …` rows,
> favoured-class rows, and monster racial abilities (Drow Noble, Rust Monster, Treant, Unicorn,
> `Template ~ +2 <Stat>`) under a `_abilities_race` filename. `monster_codex` is the only candidate
> carrying genuine **player-race** alternate racial traits, so it is not the best pilot but the only
> viable one. Card 10 is now eligible.
>
> The dispatch brief's claim that `inner_sea_bestiary` is out of scope is itself a transcription of
> a passage `loop-instruction.md` strikes through and corrects; the book is one of the 37. Nothing
> turned on it — it was not picked — but the correction is recorded (`decisions.md §43.2`).
>
> **RESOLVED IN PART, 2026-08-11 (`sd29-e6-racetrait-extend`, card 10).** The re-pin is no longer
> needed to unblock the *classifier* half: `file_kind()` now types an `_abilities_race*` basename
> that also carries a `companion`/`familiar` marker as `Companion`, so the 9 Clockwork units left
> `race_trait` for `companion` where they belong and `inner_sea_intrigue` no longer appears as a
> race-trait book at all. Corpus-wide the fix moved **9** units (`inner_sea_intrigue`, all 9) and
> added **13** companion units (`inner_sea_intrigue` 9 + `bestiary_4` 4 from
> `b4_abilities_race_ce_companion.lst`, whose rows survive the companion path that the race-trait
> row exclusion had been dropping). The **pilot's re-pin is still outstanding**, but its purpose is
> now narrower: a per-book ingest exemplar, not a classifier probe.
>
> **Card 10's own ingest half was recorded `decision-blocked` on "the engine models exactly 7 races
> (CRB's hardcoded `race_traits()`)". CORRECTED 2026-08-11 (`decisions.md §43.5`): that is true of
> ONE INSTRUMENT and false of the product.** The player surface —
> `race_resolver::load_race_corpus` → `race_trait_picker` → `list_alternate_racial_traits` — models
> **18** races read off disk at runtime (CRB 7 + B1 11; re-derived with
> `ls -d data/corpus/{core_rulebook,beastiary,advanced_race_guide}/race_trait/*/ | xargs -n1 basename | sort -u | wc -l`
> → 18). Duergar and Goblin are both in it, which is why card 9's pilot records reach a player and
> carry a passing reach claim. What is pinned to 7 is **`v06_work_inventory`'s grounding probe**:
> `race_names` from `RaceId::ALL`, `race_trait_ids` solely from `crb::race_traits()`. So ARG's 156,
> B1's 108, APG's 1 and the pilot's 5 all report `race_trait_race_not_modelled` while reaching a
> player. **Card 10's real first task is repairing that probe**, not waiting on a race chassis. Its
> blast radius (several hundred units' status across the dashboard) is why it was recorded with its
> evidence rather than attempted inside a pilot cycle.

## Cycle claims (cycle-supervisor protocol)

When a cycle claims a card:

1. Edit the card's `Status` to `IN-FLIGHT`.
2. Edit `Claimed-by` to the cycle's harness identifier.
3. Edit `Claimed-at` to the cycle's ISO-8601 timestamp.
4. Edit `Cycle-id` to the cycle's audit ID (e.g., `SD29-E4-F1-001`).
5. Append the cycle's per-cycle facts to `progress.md` (write to
   `progress.md` after writing the kanban claim; the supervisor reads
   progress.md to verify the prior cycle complete before claiming the
   next).
6. On cycle completion, edit `Status` to `COMPLETE` and append the
   completion receipt to `progress.md`.

## Operator override slot

Operator may add or remove cards directly by editing this file. Cycle
dispatch honors the post-edit state.

## Resolution to operator directives

This file is the load-bearing replacement for the Hermes `codex-tranche-9`
board (operator-confirmed 2026-08-01). When a Hermes board card is
referenced from prior doctrine (`decisions.md`, `scope-draft.md`,
`loop-instruction.md`, etc.), the reference resolves to a `kanban.md`
card id at the time of cycle dispatch.
