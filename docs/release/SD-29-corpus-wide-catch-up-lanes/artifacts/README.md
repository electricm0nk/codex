# SD-29 artifacts

Per-cycle artifact sink for the SD-29 corpus-wide catch-up lanes. Created at the
2026-08-10 pre-launch readiness audit — this directory is the path every trap-report
requirement cites (`technical-requirements.md` TR-29-003, `acceptance-and-verification.md`
AT-29-003, `epic-breakdown.md` Epics 2/5), and it must exist before the first cycle fires.

What lands here as cycles run:

- `<book>-cycle0-trap-report.md` — Epic 2's corpus-wide pre-flight output, one per book.
- `<book>-trap-report.md` — per-cycle `v06_corpus_trap_report` output for the active book.
- Per-lane PI-screening sweep outputs (Epic 3 provenance gate evidence).

Empty (this README aside) until SD-29 launches on `tranche/9`.
