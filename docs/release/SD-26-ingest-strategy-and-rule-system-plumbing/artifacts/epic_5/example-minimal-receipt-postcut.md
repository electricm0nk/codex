# Cycle example-postcut-human_wizard — Epic 5 minimal-receipt shape demo

- **Card ID:** n/a (illustrative — not a real dispatched cycle)
- **Class/case:** `pf1_human_wizard_level1` (`tests/fixtures/rules_core/`)
- **Commit SHA:** n/a
- **RED:** `cargo test --locked --test <per-class-test>` fails for the intended reason (chassis/table absent).
- **GREEN:** `cargo test --locked --test <per-class-test>` passes; `cargo test --locked --lib` no regressions.
- **Dual-audit:** OK_NO_BUNDLE_TAGS / OK_NO_TOKENS
- **duration_seconds:** <int>
