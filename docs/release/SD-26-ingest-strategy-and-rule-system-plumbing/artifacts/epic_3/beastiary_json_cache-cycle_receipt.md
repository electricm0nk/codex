# Cycle beastiary_json_cache — Epic 3 (JSON Cache Build) / Criterion 3.4

- **Card ID:** `t_4bd46141` (board `codex-tranche-5`, assignee `operator`, completed)
- **Commit SHA:** (stamped in the commit that follows this receipt in the same push)
- **Files touched:**
  - `src/rules_core/rules_tables/beastiary1/mod.rs` (adds `impl MonsterId { pub const ALL: &'static [MonsterId] = &[...] }` + 2 new unit tests — `decisions.md §11.6` in-scope fix)
  - `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs` (replaces the hand-maintained 41-entry `ALL_BESTIARY1_MONSTERS` workaround list with `MonsterId::ALL`, removing the duplication)
  - `src/rules_core/cache_gen/mod.rs` (single-line addition: `pub mod beastiary1;`)
  - `src/rules_core/cache_gen/beastiary1.rs` (new — Shape B schema types + Bestiary 1 generator)
  - `src/bin/gen_cache_beastiary.rs` (new — one-off CLI entry point that ran the generation)
  - `tests/sd26_cache_beastiary.rs` (new — acceptance test, 7 tests)
  - `data/corpus/beastiary/monster/*.json` (41 files, generated output)
  - `data/corpus/beastiary/equipment/*.json` (4 files, generated output)
  - `data/corpus/beastiary/.gitkeep` (removed — superseded by real generated content)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — ran against `BASE_BRANCH=$(git merge-base HEAD origin/develop)` combined with `git diff --cached` (this cycle's new files were untracked until staged; both diffs checked together, clean).
- **Wired-integration audit result:** `OK_NO_TOKENS` — same combined-diff method, scoped to `apps/desktop/**/*.ts*` + `apps/desktop/src-tauri/**/*.rs` + `src/**/*.rs`.
- **Acceptance criterion (verbatim, epic-breakdown.md Epic 3 / Criterion 3.4):** "`data/corpus/beastiary/` — Bestiary 1 content. `isolation: 'worktree'`." Per-monster/per-equipment JSON files, generated by dumping `src/rules_core/rules_tables/beastiary1/`'s current runtime state. Verification per `acceptance-and-verification.md` row 3.4: `cargo test --locked --test sd26_cache_beastiary`.
- **Status:** complete
- **RED → GREEN evidence:** RED — `tests/sd26_cache_beastiary.rs` did not exist before this cycle; ran immediately after authoring it (before the generator existed / before any cache files were written): **7 failed, 0 passed**, every failure `read_dir data/corpus/beastiary/{monster,equipment}: No such file or directory` (the correct failure mode for an absent cache — not a compile error, not an unrelated crash). GREEN — built `src/rules_core/rules_tables/beastiary1/mod.rs`'s new `MonsterId::ALL` constant, `src/rules_core/cache_gen/beastiary1.rs` + `src/bin/gen_cache_beastiary.rs`, ran `cargo run --locked --bin gen_cache_beastiary` against the real local PCGen corpus checkout (`~/workspace/repos/pcgen/data`, `PCGEN_CORPUS_ROOT` default), which reported `41 monsters, 4 equipment records; ingested_at=...` with **zero unresolved citations**, then re-ran the test: **7 passed, 0 failed** (one intermediate fix: the test's initial `speed_ft > 0` assertion was too strict — Vargouille's real corpus row has no `Walk` speed token at all (`MOVE:Fly,30` only), so `speed_ft: 0` is a real, honest corpus value, not a generator bug; relaxed the assertion to check field presence, not positivity). Also ran `cargo test --locked --lib`: **162 passed, 0 failed** (159 pre-cycle + 3 new: 2 `beastiary1::mod`'s `MonsterId::ALL` unit tests + 1 `cache_gen::beastiary1::tests::slugify_handles_parens_and_spaces`), `cargo test --locked --test sd24_equipment_coverage_audit`: **9 passed, 0 failed** (confirms the untouched `rules_tables::beastiary1::equipment_tables`/`equipment_data` this generator only *reads* is unchanged), `cargo test --locked --test sd26_identifier_discipline_audit`: **1 passed, 0 failed** (standing regression guard over `scripts/`+`data/`, relevant since this cycle adds 45 new files under `data/corpus/`) — no regressions. `cargo build --locked --workspace`: clean, and `cargo check --locked` inside `apps/desktop/src-tauri` (the crate hosting the edited `corpus_ingest_diagnostic.rs`): clean compile, only 5 pre-existing unrelated warnings (`SavedCharacterMutationOp`/`ClassLevelDelta`/`RuleSystemAdapter` dead-code, none touched by this cycle).
- **Real coverage numbers achieved — INDEPENDENTLY RE-VERIFIED this cycle against the live corpus, not assumed:**
  - **Monsters: 41/41** (`MonsterId::ALL`, the SD-22 corrected roster across subsets 01–08), each with `completeness: chassis_only` — matches `beastiary1::MonsterStatBlock`'s own doc comment: AC/HP/saves are PCGen-computed, not literal row tokens, deliberately out of this schema's scope (the same "deliberately bounded, not a partial-data gap" semantics `cache_gen::acg` already uses for its own class chassis records). Every one of the 41 real `b1_races.lst` line citations was independently re-confirmed this cycle by direct corpus lookup (a live first-column tab-delimited match against the monster's own display name, not a hand-typed table) — spot-checked 8 of the 41 directly by `sed -n '<line>p'` against the real file before trusting the generator's own output (Ghoul:200, Gnoll:212, Ankheg:18, Hell Hound:230, Yellow Musk Creeper:430, Rat Swarm:334, Iron Cobra:249, Dark Creeper:89 — all matched the corresponding module doc-comment citations exactly).
  - **Equipment: 4/4 (100%)** — independently re-counted directly against the live corpus this cycle (`b1_equip_general.lst` grep: 1 real record; `b1_equip_arms_armor.lst`: 2; `b1_equip_magic_items.lst`: 1), confirming `decisions.md §11.4`'s own correction stands: the real count is **4**, not the ~7 a prior SD-25 cycle-doc estimate assumed. 3/4 (`Poison (Black Smear)`, `Aklys`, `Heartstone (Night Hag)`) source `description` from the corpus's `SPROP:` token (register A10 — a real, checkable LST token, just not `DESC:`; this generator's citation lookup correctly recognizes and cites this as `lst_token`, not misclassifying it). 1/4 (`Rag Armor (Dark Creeper)`) has neither `DESC:` nor `SPROP:` in the real corpus (independently re-confirmed by direct grep this cycle) and is cited `web_second_source`, carrying forward SD-25's own real, dated web pass verbatim (`https://www.d20pfsrd.com/bestiary/monster-listings/humanoids/dark-creeper/` + `https://www.aonprd.com/MonsterDisplay.aspx?ItemName=Dark+Creeper`, fetched 2026-07-22) — never re-derived or re-fetched, per `decisions.md §11.3`.
  - **No spell-list concept exists for this book** (register A13, `equipment_tables.rs`'s own doc comment, independently re-confirmed this cycle: no `b1_spells.lst`/`*spell*`-named file exists under `pathfinder/paizo/roleplaying_game/bestiary/`) — correctly N/A, not an unclaimed gap; this generator writes only `monster/` and `equipment/` directories, no `spell/`.
- **Notes / judgment calls:**
  - **`decisions.md §11.6` in-scope code fix landed as part of this cycle, not deferred:** `beastiary1::mod.rs`'s `MonsterId` enum had no public `ALL`/count constant, unlike `ClassId::ALL`/`ApgClassId::ALL`/`AcgClassId::ALL` on the other three books. Added `impl MonsterId { pub const ALL: &'static [MonsterId] = &[...41 variants...]; }` directly to `beastiary1/mod.rs`, with 2 new unit tests (`all_has_exactly_the_41_real_monsters_with_no_duplicates`, `every_all_entry_resolves_a_real_stat_block_for_bestiary1`). This generator (`cache_gen::beastiary1`) uses the real constant directly — it does not duplicate the workaround a third time. Additionally updated `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs`'s own duplicate 41-entry `ALL_BESTIARY1_MONSTERS` list to `const ALL_BESTIARY1_MONSTERS: &[MonsterId] = MonsterId::ALL;`, removing the second copy entirely (verified this compiles clean via `cargo check --locked` inside `apps/desktop/src-tauri`, since that crate is a separate Cargo package from the library this cycle's primary file-touch grant targets — a small, disjoint, additive one-line-body change, not a re-architecture of that file).
  - **Every citation this cycle uses a live corpus lookup (tab-delimited first-column match), not a hand-copied line-number table** — mirrors `cache_gen::apg`/`cache_gen::acg`'s own `find_exact_first_column` helper. This is more robust against any future corpus edit than hardcoding, and the generator's own `unresolved_citations` report (empty this run) would surface any lookup failure rather than silently defaulting to a fabricated line.
  - **Monster `completeness` uses `chassis_only`, matching `cache_gen::acg`'s own semantics for the same reason** (a schema deliberately bounded to literal-token fields, not a book with missing/partial data) — this is a naming-consistency judgment call, not a coverage claim; every field this schema tracks is populated for every one of the 41 records (verified in `tests/sd26_cache_beastiary.rs`'s `monster_cache_has_all_41_real_monsters_with_chassis_data`).
  - **`speed_ft: 0` for Vargouille is a real, honest corpus value, not a bug.** `b1_races.lst:401`'s real row carries `MOVE:Fly,30` only — no `Walk` speed token at all — and the pre-existing (untouched-by-this-cycle) `monster_subset_06::vargouille()` function correctly transcribes `speed_ft: 0` for that reason (fly-only creature; per `beastiary1::MonsterStatBlock`'s own scope boundary, only the walk-speed convention is captured, matching every sibling monster function's pattern). This cycle's own test initially asserted `speed_ft > 0` for every record, which is wrong; caught and fixed during the GREEN pass (see RED→GREEN evidence above) rather than shipping a test that would silently mask a real future regression by never actually exercising the zero case.
  - **The generator (`src/bin/gen_cache_beastiary.rs` + `cache_gen/beastiary1.rs`) is checked into the repo, not run-once-and-discarded**, matching `cache_gen::apg`/`cache_gen::acg`'s own reproducibility precedent (`decisions.md §11.3`'s "regenerate if the corpus changes" posture).
  - **`sha256` computed via shelling out to the system `sha256sum`**, matching `cache_gen::apg`/`cache_gen::acg`'s established pattern (no `sha2` crate dependency in this workspace's library crate).
  - **File-touch scope stayed effectively disjoint from sibling E3 cycles** (3.1 `core_rulebook` — not yet started at commit time, 3.2 `advanced_players_guide` — already landed, commit `b705e91`, 3.3 `advanced_class_guide` — already landed, commit `c42af42`) per `loop-instruction.md §3`'s "Disjoint per-book" framing: the only shared-file edits were the single-line `pub mod beastiary1;` addition to `src/rules_core/cache_gen/mod.rs` (additive, does not touch the pre-existing `pub mod acg;`/`pub mod apg;` lines) and the `MonsterId::ALL`-adoption edit to `corpus_ingest_diagnostic.rs` (a file no sibling E3 cycle touches, per the file-touch verification table).
- **Discovery forwards:** none blocking. One observation for Epic 6's final closure scan: Bestiary 1's equipment ceiling (4/4, 100%) and monster-chassis ceiling (41/41) are both now cache-verified, joining ACG (10/10 classes, 144/144 spells, 264/269 equipment) as books with a fully independently re-measured-this-pass ceiling; CRB (in progress) and APG (331/338, 284/297/285/297, already landed commit `b705e91`) round out the 4-book set once 3.1 lands.
- **Next-cycle plan:** sibling criterion 3.1 (`core_rulebook`) remains the only not-yet-started E3 book cache at this cycle's commit time; disjoint file-touch, no dependency on this cycle's output. A future cycle wanting to widen Bestiary 1's equipment scope (e.g. `.MOD` companion records from later splatbooks, per `equipment_tables.rs`'s own doc comment) would need a fresh corpus-intake pass first — out of this cycle's dump-the-Rust-module charter (`decisions.md §11.3`).
