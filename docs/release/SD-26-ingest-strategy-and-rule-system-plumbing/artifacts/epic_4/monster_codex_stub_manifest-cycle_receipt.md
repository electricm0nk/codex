# Cycle monster_codex_stub_manifest — Epic 4 / Criterion 4.13

- **Card ID:** (see kanban step, below)
- **Commit SHA:** (filled in after push — see `progress.md` for the landed SHA)
- **Files touched:**
  - `data/stubs/monster_codex.json` (new — per-book stub manifest)
  - `docs/governance/wired-integration-stubs-registry.md` (added entry #0014; updated the reserved-entries footer note)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** Epic 4, Criterion 4.13 — Book stub manifest for "monster_codex". Replicates the `book_stub` kind pattern (defined by 4.1, pilot `advanced_race_guide`, entry #0003) for this future-state PF1 book: `data/stubs/monster_codex.json` + a numbered registry entry following #0003-#0013's exact template.
- **Status:** complete
- **Notes:**
  - **Rebase-time state check.** Re-fetched + rebased onto `origin/tranche/5-4` before touching the shared registry file. At that point the registry already had entries #0003-#0013 landed (through criterion 4.11, `horror_adventures`); `monster_codex` was not yet claimed anywhere (`data/stubs/monster_codex.json` did not exist, no registry entry referenced it). Used the next free number on disk, #0014, per the brief's explicit instruction rather than assuming a number in advance.
  - **Book identity — confirmed real, not guessed.** Verified against `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/monster_codex/_monster_codex.pcc`: `SOURCELONG:Monster Codex`, `SOURCESHORT:MC`, `SOURCEDATE:2014-11`, `PUBNAMELONG:Paizo Inc.`, `CAMPAIGN:Monster Codex (Player Options Only)`. This is Paizo's real Pathfinder Player Companion "Monster Codex" (November 2014) — player-facing options (races, kits, feats, spells, equipment) built around the concept of playing/encountering intelligent, organized monster bands (per the PCC's own `DESC:` field: "Menagerie of Mayhem! ... no single creature entry can truly cover every vicious champion, unholy priest, or savage sorcerer in a band of organized and intelligent monsters"). `book_name: "Monster Codex"` matches the title-only naming convention used by all prior `book_stub` entries (e.g. `"Horror Adventures"`, `"Core Essentials"` — no publisher/subtitle prefix).
  - **JSON manifest shape.** Copied `data/stubs/advanced_race_guide.json`'s exact shape with no deviation: `{book_id, book_name, planned_resolution_bundle, content_kind_counts: null, registered_at}`. `registered_at` stamped with the real wall-clock UTC time this cycle ran (`2026-07-23T02:43:30Z`), not a placeholder. `planned_resolution_bundle: "SD-27+ (unscheduled)"` matches the value all prior `book_stub` entries (#0003-#0013) landed with — the open-ended deferral posture from `risks-and-open-questions.md §5`, not the un-reconciled `decisions.md §10` default (`"SD-27"`) that 4.1's receipt flagged as a still-open discrepancy for the operator.
  - **Registry entry.** Followed #0013 (`horror_adventures`)'s structural template field-for-field: `Book / manifest path`, `What's missing` (with the `content_kind_counts: null` vs. `0` honesty rationale carried over verbatim in spirit), `Justification` (same operator-verbatim citation, same timestamp — the directive covers all 21 future-state books identically), `Audit-grep impact: None`, `Bundle-of-record` (criterion 4.13, this cycle), `Remediation cycle` (`SD-27+ (unscheduled)`, matching the manifest), `Status: Registered stub 2026-07-23`. Updated the trailing "(Entries 00NN-000n reserved...)" footer note to move `monster_codex` from the "remaining" list into the "done by 4.13" parenthetical and decrement the reserved-entry count from 10 to 9.
- **Discovery forwards:** none new. (The `decisions.md §10` `planned_resolution_bundle` discrepancy remains open from 4.1's original `## DISCOVERED` entry; not re-forwarded here since it's already tracked and this cycle's value matches the already-landed convention.)
- **Next-cycle plan:** Criteria 4.12, 4.14-4.22 continue picking the next unclaimed book from `content-unit-inventory.md §2.2`'s 21-book list (`mythic_adventures`, `occult_adventures`, `pathfinder_unchained`, `ultimate_campaign`, `ultimate_combat`, `ultimate_equipment`, `ultimate_intrigue`, `ultimate_magic`, `ultimate_wilderness` remain unclaimed as of this cycle's rebase), each writing `data/stubs/<book_id>.json` + the next-numbered registry entry (#0015+) following this cycle's exact template.
