# Cycle ultimate_wilderness_stub_manifest — Epic 4 / Criterion 4.22

- **Card ID:** (see kanban step, below)
- **Commit SHA:** (filled in after push — see `progress.md` for the landed SHA)
- **Files touched:**
  - `data/stubs/ultimate_wilderness.json` (new — per-book stub manifest)
  - `docs/governance/wired-integration-stubs-registry.md` (added entry #0023 following #0003-#0022's `book_stub` template exactly; replaced the stale "1 remaining book" reservation footer with a closure note — all 21 future-state books are now registered, and criterion 4.12 is confirmed orphaned/no-op per `content-unit-inventory.md`'s count-discrepancy note)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** Epic 4, Criterion 4.22 — Book stub manifest for `ultimate_wilderness` (one of the 21 future-state-book cycles per `epic-breakdown.md` "Criteria 4.2..4.22 — One cycle per future-state book"): writes `data/stubs/ultimate_wilderness.json` + a Stubs Registry entry, following the `book_stub` kind and pilot shape criterion 4.1 established.
- **Status:** complete
- **Notes:**
  - **Book identity confirmed against real source.** `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_wilderness/_ultimate_wilderness.pcc` confirms this is a real Paizo PF1 sourcebook: `SOURCELONG:Ultimate Wilderness`, `SOURCESHORT:UW`, `SOURCEDATE:2017-11`, `BOOKTYPE:Supplement`, `GAMEMODE:Pathfinder|Pathfinder_PFS`. `book_name` is set to the real title "Ultimate Wilderness" (not guessed), and the registry entry's "What's missing" prose cites the real content the book covers (shifter base class, druidic sects/rituals, new archetypes, wilderness-survival options) per the `.pcc`'s own `DESC:` field — no fabricated detail.
  - **Criterion/book mapping resolved per brief.** The registry's prior reservation footer (left by the sibling cycle that landed `ultimate_magic` as #0022/criterion 4.21) flagged two still-open criterion slots (4.12, 4.22) for one remaining book (`ultimate_wilderness`), noting 4.12 is likely an orphaned criterion number from the bundle's original 22-book miscount (`content-unit-inventory.md §2.2`'s "Pre-existing count discrepancy" note). This cycle's brief explicitly assigns criterion 4.22 to `ultimate_wilderness`, resolving that ambiguity — landed under 4.22, leaving 4.12 confirmed as the orphaned/no-op number (no 22nd book exists to register against it). Updated the registry's reservation footer to record this closure explicitly for the record, since this cycle happens to be the one that completes the full 21-book set (rather than leaving a stale "N remaining" note that the next reader would have to re-derive from scratch).
  - **JSON manifest shape.** Followed the pilot's exact shape from `data/stubs/advanced_race_guide.json` and all subsequent entries, no deviation: `{book_id, book_name, planned_resolution_bundle, content_kind_counts: null, registered_at: <ISO-8601>}`. Validated via direct JSON parse (see Verification below).
  - **`planned_resolution_bundle` value.** Used `"SD-27+ (unscheduled)"`, matching the pilot's corrected value and every subsequent entry (#0003-#0022), not `decisions.md §10`'s stale `"SD-27"` default — same resolved discrepancy the pilot cycle flagged.
  - **Re-fetch before shared-file edit.** Re-fetched + rebased immediately before editing the registry (per the concurrent-write protocol); found the sibling `ultimate_magic` cycle had already landed as #0022/criterion 4.21 in that window, confirming the next free entry number was #0023 and the next-to-register book was `ultimate_wilderness` at criterion 4.22 — consistent with this cycle's assigned brief, no renumbering or book-pivot needed.
- **Discovery forwards:** none new. (The `decisions.md §10` / `"SD-27"` vs `"SD-27+ (unscheduled)"` discrepancy was already forwarded by the 4.1 pilot cycle.)
- **Next-cycle plan:** All 21 future-state books (`advanced_race_guide` through `ultimate_wilderness`) are now registered (#0003-#0023). Epic 4's dynamic per-book fan-out is complete; remaining Epic 4 follow-up (if any) is confirming criterion 4.12 closes as a no-op per operator confirmation, then Epic 4 as a whole can be marked complete in `progress.md` once all sibling cycles' rows are reconciled.
