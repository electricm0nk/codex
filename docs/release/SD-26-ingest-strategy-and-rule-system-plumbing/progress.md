# SD-26 — Progress

> **Operating method:** see `./scope-draft.md` and `scripts/workflow-dispatch.sh`. This file is created on cycle 0 of Epic 2 with the deterministic seed. The orchestrator reads `## TODO` + `## DISCOVERED` and dispatches the highest-priority unclaimed item.

## Status matrix (placeholder)

| Criterion | State | Cycle ID | Commit SHA | Notes |
|---|---|---|---|---|
| 1.1 Source-code identifier audit | complete | epic1-1.1-identifier-audit | 74d9402 | Tree already clean (RED returned 0 hits, per SD-24's prior remediation); extended standing regression guard to scripts/+data/ (see receipt) |
| 2.1 comparator | complete | epic2-2.1-comparator | 744cd71 | `compare()` + `NormalizedOutput`/`ComparisonResult` land in `src/oracle_validation/comparator.rs`; see receipt |
| 2.2 normalization | complete | epic2-2.2-normalization | a87bc0d | `normalize()`/`normalize_dimension_value()` rule engine (trim-then-integer-coercion) lands in `src/oracle_validation/normalization.rs`, producing `comparator.rs`'s `NormalizedOutput`; see receipt |
| 2.3 parity_report | complete | epic2-2.3-parity_report | 7566d06 | `render_parity_report()`/`write_parity_report()`/`default_parity_report_dir()` land in `src/oracle_validation/parity_report.rs`, rendering a real `parity_report_<case-id>.md` from `comparator.rs`'s `ComparisonResult` + `normalization.rs`'s rule set; see receipt |
| 2.4 pcgen_runner | complete | epic2-2.4-pcgen_runner | 20ab8c9 | `run_pcgen_character()`/`PcgenRunOutput` land in `src/oracle_validation/pcgen_runner.rs`, wrapping `scripts/pcgen-run-character.sh` + `scripts/pcgen-normalize-output.py`; `to_normalized_output()` bridges directly into `comparator.rs`'s `NormalizedOutput` (normalizer already emits typed values); see receipt |
| 2.5 verification cycle | complete | epic2-2.5-pilot_case_verification (+ followup) | 0a8ba63 (+ followup commit, see Cycle log) | Full pipeline run for real end-to-end against the pilot case. Original cycle: no real same-character `.pcg` existed, ran against substitute `pf_Paladin.pcg`, 0/9 dimensions matched (expected — different character). **Followup cycle**: a real near-complete precursor `.pcg` was located (GE-05 provisional evidence) and completed to the exact pilot spec (Dodge + Weapon Focus (Longsword) feats, Climb/Intimidate/Swim rank-1 skills, Chain Shirt + Longsword equipped, no shield), then re-run through the real pipeline. Result: 7/9 dimensions genuinely match (including `character.identity` now matching for real), but `skill.selected_modifier.{climb,swim}` genuinely mismatch (PCGen 6, Codex 5) — a real Codex bug (`pilot_compute::compute_ability_modifiers` never folds the chosen Human `+2 Strength` racial bonus into `AbilityModifiers.strength`). `current_claim_status` correctly stays `not_yet_grounded` (real mismatch, not force-upgraded); see receipt + DISCOVERED |
| 3.1 core_rulebook cache | complete | epic3-3.1-core_rulebook_json_cache | 13108c6 | 3326 records (11 class + 652 spell + 2663 deduplicated equipment) generated by dumping `rules_tables::crb`'s current state; real ceilings re-verified and matched exactly: equipment description 2021/2977 raw (67.9%), 67 `lst_corrected_ingest` + 117 `lst_inherited_copy` match decisions.md §11.2 exactly; equipmods.rs's 314/658 duplicate-key defect de-duplicated at cache-write time, forwarded as DISCOVERED; see receipt |
| 3.2 advanced_players_guide cache | complete | epic3-3.2-apg_json_cache | b705e91 | 641 records (6 class + 297 spell + 338 equipment) generated by dumping `rules_tables::apg`'s current state; real ceilings re-verified and matched exactly: equipment description 331/338 (97.9%), spell full_text 284/297 (95.6%), spell description 285/297; see receipt |
| 3.3 advanced_class_guide cache | complete | epic3-3.3-acg_json_cache | c42af42 | 423 records (10 class + 144 spell + 269 equipment) generated by dumping `rules_tables::acg`'s current state; ACG's real ceiling was UNKNOWN before this cycle (not covered by SD-25) — independently measured: classes 10/10 full chassis, spell description+full_text 144/144 (100%), equipment description 264/269 (98.1%, sourced from `SPROP:` not `DESC:`); see receipt |
| 3.4 beastiary cache | complete | epic3-3.4-beastiary_json_cache | 228a077 | 45 records (41 monster + 4 equipment) generated by dumping `rules_tables::beastiary1`'s current state; real ceilings re-verified and matched exactly: monsters 41/41 (`MonsterId::ALL`, newly added), equipment description 4/4 (100%, corrected record count per decisions.md §11.4); see receipt |
| 4.1 research epic | complete | epic4-4.1-research_book_stub_kind | d6a7c61 | Defined `book_stub` kind in `docs/governance/wired-integration-stubs-registry.md` (adapted #0001/#0002's field shape: File/line→Book/manifest path, Stub pattern→What's missing); landed pilot entry #0003 + `data/stubs/advanced_race_guide.json` proving the shape end-to-end; see receipt + DISCOVERED |
| 4.3 adventurers_guide stub manifest | complete | adventurers_guide_stub_manifest | 6f820ee | `data/stubs/adventurers_guide.json` + registry entry #0004 for Paizo's PF1 supplement "Adventurer's Guide" (2017), following #0003's pilot template exactly; book identity confirmed against real LST files in the PCGen checkout; see receipt |
| 4.2 beginner_box stub manifest | complete | beginner_box_stub_manifest | 24fb3fb | `data/stubs/beginner_box.json` + registry entry #0005 for Paizo's PF1 supplement "Beginner Box" (2011), following #0003/#0004's pilot template exactly; book identity confirmed against real LST/PCC files in the PCGen checkout; dispatch brief originally named `advanced_race_guide` (already done by 4.1) then `adventurers_guide` (landed mid-cycle by a concurrent sibling as 4.3) before re-verifying live registry state and landing on the next genuinely unclaimed book; see receipt |
| 4.5 bestiary_2 stub manifest | complete | bestiary_2_stub_manifest | 9a3c1bd | `data/stubs/bestiary_2.json` + registry entry #0006 for Paizo's PF1 supplement "Bestiary 2" (Dec 2010), following #0003/#0004/#0005's pilot template exactly; book identity confirmed against real `bestiary_2.pcc`/LST files in the PCGen checkout; see receipt |
| 4.4 bestiary_3 stub manifest | complete | bestiary_3_stub_manifest | f174225 | `data/stubs/bestiary_3.json` + registry entry #0007 for Paizo's PF1 supplement "Bestiary 3" (Jan 2012), following #0003-#0006's pilot template exactly; book identity confirmed against real `bestiary_3.pcc`/LST files in the PCGen checkout; dispatch brief originally named `beginner_box` (already done by 4.2) then `bestiary_2` (landed mid-cycle by a concurrent sibling as 4.5) before re-verifying live registry state a second time and landing on the next genuinely unclaimed book; see receipt |
| 4.6 bestiary_4 stub manifest | complete | (landed by a concurrent sibling cycle, not this one) | c97c9a4 | `data/stubs/bestiary_4.json` + registry entry #0008 for Paizo's PF1 supplement "Bestiary 4" (Oct 2013), confirmed on disk by this cycle (4.7) at rebase time; see `artifacts/epic_4/bestiary_4_stub_manifest-cycle_receipt.md` |
| 4.7 bestiary_5 stub manifest | complete | bestiary_5_stub_manifest | 262bbcd | `data/stubs/bestiary_5.json` + registry entry #0009 for Paizo's PF1 supplement "Bestiary 5" (Dec 2015), following #0003-#0008's pilot template exactly; book identity confirmed against real `_bestiary_5.pcc`/LST files in the PCGen checkout; dispatch brief originally named `bestiary_4` for criterion 4.7, but that book was already registered by a concurrent sibling (criterion 4.6, #0008) before this cycle's first rebase — pivoted to `bestiary_5` (the next unclaimed book per the canonical 21-book list); see receipt |
| 4.8 bestiary_6 stub manifest | complete | bestiary_6_stub_manifest | b7ab111 | `data/stubs/bestiary_6.json` + registry entry #0010 for Paizo's PF1 supplement "Bestiary 6" (May 2017), following #0003-#0009's pilot template exactly; book identity confirmed against real `_bestiary_6.pcc`/LST files in the PCGen checkout; dispatch brief originally named `bestiary_3` for criterion 4.6 (both already claimed live), first pivoted to `bestiary_5` and implemented it locally, then hit a same-book collision with a concurrent sibling who independently landed `bestiary_5` first (`#0009`, commit `69a3f86`, timestamp seconds ahead) — discarded the unpushed local commit and re-derived the next genuinely unclaimed book (`bestiary_6`); see receipt |
| 4.9 bonus_bestiary stub manifest | complete | bonus_bestiary_stub_manifest | bdaf39b | `data/stubs/bonus_bestiary.json` + registry entry #0011 for Paizo's PF1 supplement "Bonus Bestiary" (June 2009), following #0003-#0010's pilot template exactly; book identity confirmed against real `_bonus_bestiary.pcc` in the PCGen checkout; dispatch brief originally named `bestiary_5` for criterion 4.8, but that book (#0009, criterion 4.7) and the "criterion 4.8" label itself (`bestiary_6`, #0010) were both already claimed live before this cycle's first rebase — derived the next genuinely unclaimed book from the canonical 21-book list and landed against `bonus_bestiary` instead; see receipt |
| 4.10 core_essentials stub manifest | complete | core_essentials_stub_manifest | cf7b84f | `data/stubs/core_essentials.json` + registry entry #0012 for Paizo's PF1 sourcebook "Core Essentials" (Aug 2009), following #0003-#0011's pilot template exactly; book identity confirmed against real `_core_essentials.pcc` in the PCGen checkout; dispatch brief originally named `bestiary_6` for criterion 4.9 (already claimed live, #0010/4.8), pivoted to `bonus_bestiary` and wrote it locally, but the pre-registry-edit rebase itself failed (untracked-file collision) because a concurrent sibling had already landed `bonus_bestiary` first (#0011, commit `bdaf39b`) — discarded the unpushed local file and re-derived the next genuinely unclaimed book (`core_essentials`); see receipt |
| 4.11 horror_adventures stub manifest | complete | horror_adventures_stub_manifest | 9cda689 (+ footer-fix commit `617eb61`) | `data/stubs/horror_adventures.json` + registry entry #0013 for Paizo's PF1 sourcebook "Horror Adventures" (Aug 2016), following #0003-#0012's pilot template exactly; book identity confirmed against real `horror_adventures.pcc` in the PCGen checkout; dispatch brief originally named `bonus_bestiary` for criterion 4.10, but that book (#0011, criterion 4.9) and the "criterion 4.10" label itself (`core_essentials`, #0012) were both already claimed live before this cycle's first rebase — derived the next genuinely unclaimed book from the canonical 21-book list and landed against `horror_adventures` instead; see receipt |
| 4.13 monster_codex stub manifest | complete | monster_codex_stub_manifest | 621cdad | `data/stubs/monster_codex.json` + registry entry #0014 for Paizo's PF1 sourcebook "Pathfinder Player Companion: Monster Codex" (Nov 2014), following #0003-#0013's pilot template exactly; book identity confirmed against real `_monster_codex.pcc` in the PCGen checkout; see receipt |
| 4.14 mythic_adventures stub manifest | complete | mythic_adventures_stub_manifest | 7575aa8 | `data/stubs/mythic_adventures.json` + registry entry #0015 for Paizo's PF1 sourcebook "Pathfinder Roleplaying Game: Mythic Adventures" (Aug 2013), following #0003-#0014's pilot template exactly; book identity confirmed against real `_mythic_adventures.pcc` in the PCGen checkout; see receipt |
| 4.15 occult_adventures stub manifest | complete | occult_adventures_stub_manifest | f31d833 | `data/stubs/occult_adventures.json` + registry entry #0016 for Paizo's PF1 sourcebook "Pathfinder Roleplaying Game: Occult Adventures" (July 2015), following #0003-#0015's pilot template exactly; book identity confirmed against real `_occult_adventures.pcc` in the PCGen checkout; see receipt |
| 4.16 pathfinder_unchained stub manifest | complete | pathfinder_unchained_stub_manifest | a61305f | `data/stubs/pathfinder_unchained.json` + registry entry #0017 for Paizo's PF1 sourcebook "Pathfinder Unchained" (Apr 2015), following #0003-#0016's pilot template exactly; book identity confirmed against real `_pathfinder_unchained.pcc` in the PCGen checkout; rebase-time collision with a concurrent sibling landing `occult_adventures` as #0016 first — renumbered this cycle's entry to #0017, no content lost; see receipt |
| 4.17 ultimate_campaign stub manifest | complete | ultimate_campaign_stub_manifest | a7df999 | `data/stubs/ultimate_campaign.json` + registry entry #0018 for Paizo's PF1 sourcebook "Ultimate Campaign" (May 2013), following #0003-#0017's pilot template exactly; book identity confirmed against real `_ultimate_campaign.pcc` in the PCGen checkout; see receipt |
| 4.18 ultimate_combat stub manifest | complete | ultimate_combat_stub_manifest | d1fa4b7 | `data/stubs/ultimate_combat.json` + registry entry #0019 for Paizo's PF1 sourcebook "Ultimate Combat" (Jan 2011), following #0003-#0018's pilot template exactly; book identity confirmed against real `_ultimate_combat.pcc` in the PCGen checkout; see receipt |
| 4.19 ultimate_equipment stub manifest | complete | ultimate_equipment_stub_manifest | ee142b1 | `data/stubs/ultimate_equipment.json` + registry entry #0020 for Paizo's PF1 sourcebook "Ultimate Equipment" (Aug 2012), following #0003-#0019's pilot template exactly; book identity confirmed against real `ultimate_equipment.pcc` in the PCGen checkout; see receipt |
| 4.20 ultimate_intrigue stub manifest | complete | ultimate_intrigue_stub_manifest | d509f33 | `data/stubs/ultimate_intrigue.json` + registry entry #0021 for Paizo's PF1 sourcebook "Ultimate Intrigue" (Apr 2016), following #0003-#0020's pilot template exactly; book identity confirmed against real `_ultimate_intrigue.pcc` in the PCGen checkout; see receipt |
| 4.21 ultimate_magic stub manifest | complete | ultimate_magic_stub_manifest | 06222fa | `data/stubs/ultimate_magic.json` + registry entry #0022 for Paizo's PF1 sourcebook "Ultimate Magic" (May 2011), following #0003-#0021's pilot template exactly; book identity confirmed against real `_ultimate_magic.pcc` in the PCGen checkout; see receipt |
| 4.22 ultimate_wilderness stub manifest | complete | ultimate_wilderness_stub_manifest | 72dccac | `data/stubs/ultimate_wilderness.json` + registry entry #0023 for Paizo's PF1 sourcebook "Ultimate Wilderness" (Nov 2017), following #0003-#0022's pilot template exactly; book identity confirmed against real `_ultimate_wilderness.pcc` in the PCGen checkout. This completes the full 21-book future-state set (#0003-#0023: `advanced_race_guide` done by 4.1, `adventurers_guide` done by 4.3, `beginner_box` done by 4.2, `bestiary_2` done by 4.5, `bestiary_3` done by 4.4, `bestiary_4` done by 4.6, `bestiary_5` done by 4.7, `bestiary_6` done by 4.8, `bonus_bestiary` done by 4.9, `core_essentials` done by 4.10, `horror_adventures` done by 4.11, `monster_codex` done by 4.13, `mythic_adventures` done by 4.14, `occult_adventures` done by 4.15, `pathfinder_unchained` done by 4.16, `ultimate_campaign` done by 4.17, `ultimate_combat` done by 4.18, `ultimate_equipment` done by 4.19, `ultimate_intrigue` done by 4.20, `ultimate_magic` done by 4.21, `ultimate_wilderness` done by 4.22); see receipt |
| 4.12 per-book (orphaned) | no-op | — | — | Confirmed orphaned per `content-unit-inventory.md`'s "Pre-existing count discrepancy" note (the bundle's original 22-book/4.2..4.23 miscount, corrected to 21-book/4.2..4.22) — no 22nd book exists to register against this criterion number; all 21 future-state books are now covered by 4.1-4.11 + 4.13-4.22. Closing as a documented no-op rather than leaving it open indefinitely. |
| 5.1 doctrine-cost audit | complete | epic5-5.1-doctrine_cost_floor | (pending push) | Measured: pre-cut ~40min (SD-22 Alchemist receipt) vs post-cut ~6.3min real floor; see `artifacts/epic_5/per-class-cycle-floor-measurement.md` |
| 6.1 Final criterion scan | complete | final-criterion-scan | (this cycle's commit) | 38/38 criteria accounted for (17 declarative + 21 dynamic); verdict **complete-with-flagged-gaps**; `cargo test --workspace --locked` 4124 passed/0 failed; `## DISCOVERED` queue 7/10 (under hard-stop); 4 newly-found kanban/receipt paper-trail gaps (criteria 1.1, 2.1, 4.6, 5.1) + the 2 already-known real gaps (CG-03 ability-modifier bug; `planned_resolution_bundle` decisions.md-vs-landed discrepancy) all documented, none silently resolved; see `artifacts/epic_6/closure-readiness-report.md` + `artifacts/epic_6/final-criterion-scan-cycle_receipt.md` |
| 6.2 Architecture closure pipeline | complete | architecture-closure | (this cycle's commit) | Truth-up (manual, per known-broken `architecture_truth_up.py` single-pipe-index bug): updated `docs/architecture/{homebrew-and-oracle,status,overview,rules-data-tables}.md` — the oracle comparator/normalization/parity-report/PCGen-runner (E2) landed but 3 docs still called them "deferred/absent/does-not-exist-yet"; also documented the E3 JSON corpus cache + E4 `book_stub` future-state registry. Both README verification one-liners clean (0 missing paths, 0 broken links). Graphify: `graphify cluster-only` ran green (exit 0) — 12243 nodes, 29197 edges, 632 communities, backup dir `2026-07-23/`. Adversarial re-verify of 6.1's scan: **CONFIRMED** (21 stub JSON + 21 registry entries; 4 E3 caches with real counts 3326/641/423/45; E2 tests live — comparator 4, normalization 7, parity_report 10, pcgen_runner 6, pilot_case 2). Dual-audit: OK_NO_BUNDLE_TAGS / OK_NO_TOKENS. See `artifacts/epic_6/architecture-closure-cycle_receipt.md` |
| 6.3 Release notes | complete | release-notes-cycle | (pending push) | All 7 REQUIRED_NOTES_SECTIONS populated with real content (Summary, User-Visible Changes, Defects Fixed, Operational Notes, Verification Evidence, Known Issues, Update Eligibility); no placeholders remain; CG-03 blocker documented in Known Issues; 7 DISCOVERED + 1 open blocker cited; see `artifacts/epic_6/release-notes-cycle_receipt.md` |
| 6.4 Build version (→ 0.5.99) | complete | epic6-6.4-version-bump | f1575cc | Bumped version from 0.5.98 to 0.5.99 in 4 files (package.json, tauri.conf.json, Cargo.toml, Cargo.lock); both dual-audit gates clean (OK_NO_BUNDLE_TAGS / OK_NO_TOKENS); see receipt |
| 6.5 PR + merge | PR opened, awaiting operator merge | closure-pr | (this cycle's commit) | Fetch+rebase confirmed already up to date with `origin/tranche/5-4`. Develop-divergence check: `origin/develop` showed 2 commits `tranche/5-4` didn't have by SHA (#336/#337), but content-diffed byte-identical to 2 commits already on `tranche/5-4` under different SHAs (independently landed via the same upstream PRs); `git merge-tree --write-tree tranche/5-4 origin/develop` confirmed a no-op merge (tree hash identical to `tranche/5-4`'s own) — **no rebase against develop needed.** Dual-audit re-run on full `merge-base(develop)...HEAD` diff: `OK_NO_BUNDLE_TAGS`, `OK_NO_TOKENS` (1 negation-form doc-comment, non-violating). Opened PR https://github.com/electricm0nk/codex/pull/338 (`tranche/5-4` → `develop`), state `OPEN`, `mergeable: MERGEABLE`. **Not merged/approved** per standing repo convention and this cycle's explicit non-negotiable instruction — awaiting operator (Todd Hintzmann) review and merge. PR body calls out CG-03 (ability-modifier bug) and the `planned_resolution_bundle` `"SD-27"` vs `"SD-27+ (unscheduled)"` discrepancy explicitly for the operator. See `artifacts/epic_6/pr_merge-cycle_receipt.md`. **Bundle-close step (final kanban done-receipt + this row's terminal state) is explicitly operator-gated, post-merge — not performed by this cycle.** |

## TODO (deterministic seed)
None — 6.5 (terminal criterion) dequeued this cycle: PR #338 opened from `tranche/5-4` into `develop`, not merged (operator-gated). All of SD-26's automated dispatch work is complete; only the operator's merge of PR #338 (plus the operator-judgment items documented in `closure-readiness-report.md` §3-§4) remain. (6.4 dequeued — version bump to 0.5.99 complete; 6.3 dequeued — release notes complete; 6.2 dequeued — architecture closure pipeline complete; 4.12 and 4.22 dequeued — both resolved; 6.1 dequeued — complete; see status matrix above for all)

## DONE
6.5 (this cycle's commit — `tranche/5-4 → develop` PR opened: https://github.com/electricm0nk/codex/pull/338, state OPEN, not merged; see `artifacts/epic_6/pr_merge-cycle_receipt.md`)
6.4 (this cycle's commit — version bump to 0.5.99; bumped package.json / tauri.conf.json / Cargo.toml / Cargo.lock; see `artifacts/epic_6/version-bump-cycle_receipt.md`)
6.2 (this cycle's commit — architecture closure pipeline: manual truth-up of `docs/architecture/{homebrew-and-oracle,status,overview,rules-data-tables}.md` reflecting E2 oracle harness + E3 JSON cache + E4 book_stub registry; graphify cluster-only re-cluster green; 6.1's closure scan adversarially re-verified CONFIRMED; see `artifacts/epic_6/architecture-closure-cycle_receipt.md`)
6.1 (this cycle's commit — final criterion scan; 38/38 criteria accounted for; verdict complete-with-flagged-gaps; see `artifacts/epic_6/closure-readiness-report.md`)
4.21 (commit 06222fa — ultimate_magic stub manifest, #0022 registry entry)
4.20 (commit d509f33 — ultimate_intrigue stub manifest, #0021 registry entry)
4.19 (commit ee142b1 — ultimate_equipment stub manifest, #0020 registry entry)
4.18 (commit d1fa4b7 — ultimate_combat stub manifest, #0019 registry entry)
4.17 (commit a7df999 — ultimate_campaign stub manifest, #0018 registry entry)
4.16 (commit a61305f — pathfinder_unchained stub manifest, #0017 registry entry)
4.15 (commit f31d833 — occult_adventures stub manifest, #0016 registry entry)
4.14 (commit 7575aa8 — mythic_adventures stub manifest, #0015 registry entry)
4.13 (commit 621cdad — monster_codex stub manifest, #0014 registry entry)
4.11 (commit 9cda689 — horror_adventures stub manifest, #0013 registry entry)
4.10 (commit cf7b84f — core_essentials stub manifest, #0012 registry entry)
4.9 (commit bdaf39b — bonus_bestiary stub manifest, #0011 registry entry)
4.8 (commit b7ab111 — bestiary_6 stub manifest, #0010 registry entry)
4.7 (commit 262bbcd — bestiary_5 stub manifest, #0009 registry entry)
4.6 (commit c97c9a4 — bestiary_4 stub manifest, #0008 registry entry; landed by a concurrent sibling cycle)
4.4 (commit f174225 — bestiary_3 stub manifest, #0007 registry entry)
4.5 (commit 9a3c1bd — bestiary_2 stub manifest, #0006 registry entry)
4.2 (commit 24fb3fb — beginner_box stub manifest, #0005 registry entry)
4.3 (commit 6f820ee — adventurers_guide stub manifest, #0004 registry entry)
4.1 (commit d6a7c61 — book_stub kind defined, #0003 pilot entry + advanced_race_guide.json)
1.1 (commit 74d9402)
2.1 (commit 744cd71)
2.2 (commit a87bc0d)
2.4 (commit 20ab8c9)
2.3 (commit 7566d06)
2.5 (commit 0a8ba63 — verification cycle ran for real; upgrade correctly withheld, see DISCOVERED)
3.2 (commit b705e91 — APG JSON cache, 641 records)
3.3 (commit c42af42 — ACG JSON cache, 423 records; real ceiling independently measured for the first time, see DISCOVERED)
3.4 (commit 228a077 — Bestiary 1 JSON cache, 45 records; adds MonsterId::ALL constant per decisions.md §11.6, see DISCOVERED)
3.1 (commit 13108c6 — core_rulebook JSON cache, 3326 records; equipmods.rs duplicate-key defect de-duplicated at cache-write time, see DISCOVERED)

## DISCOVERED
- **`decisions.md §10`'s `planned_resolution_bundle` default (`"SD-27"`) conflicts with this cycle's brief instruction (`"SD-27+ (unscheduled)"`) (E4.1).** `decisions.md §10` pins the field to the literal `"SD-27"` as an "operator-pinned default (operator may override)." This cycle's brief explicitly instructed `"SD-27+ (unscheduled)"`, citing `risks-and-open-questions.md §5`'s open-ended deferral posture ("concrete rule-system implementations land in SD-27+", not a commitment to a specific numbered bundle). Followed the brief (more specific/recent, and avoids fabricating a specific bundle number that isn't actually scheduled). Landed in `data/stubs/advanced_race_guide.json` and registry entry #0003. **Needs an operator call before criteria 4.2-4.22 run at scale:** either correct `decisions.md §10` to `"SD-27+ (unscheduled)"` to match what's now live, or confirm `"SD-27"` is genuinely intended and correct this pilot entry + `decisions.md §10` the other way. Not blocking 4.2-4.22 (they should follow this pilot's landed value for consistency unless overridden) but flagged for Epic 6's closure scan (6.1) if not resolved sooner. See `artifacts/epic_4/research_book_stub_kind-cycle_receipt.md`.
- **ACG's real per-field completion ceiling was independently measured for the first time this cycle (E3.3)** — `risks-and-open-questions.md` Q4 flagged it as genuinely unknown (ACG was already SD-24-complete at SD-26 authoring time and was not covered by SD-25's corpus-intake pass, unlike CRB/APG/Bestiary-1). Measured directly from the compiled `rules_tables::acg` module: 10/10 classes with full 20-level chassis; spell `description`+`full_text` 144/144 (100% — better than CRB's 67.9% or APG's 95.6%/97.9%, because ACG's own `acg_spells.lst` base record already carries the full text on its own `DESC:` token, unlike CRB/APG's `.MOD`-record convention); equipment `description` 264/269 (98.1%, sourced from the corpus's `SPROP:` token since `acg_equip.lst`/`acg_equipmods.lst` carry zero `DESC:` tokens). Worth surfacing at Epic 6's final closure scan (6.1) as a genuine cross-book completeness data point: ACG's higher ceiling reflects SD-24's own more thorough original ingest, not a lower real corpus complexity. Not blocking; informational only. See `artifacts/epic_3/acg_json_cache-cycle_receipt.md`.
- **RESOLVED (followup cycle, see `artifacts/epic_2/pilot_case_oracle_checked-followup-cycle_receipt.md`): a real PCGen-native `.pcg` character file for the exact pilot build now exists and is wired into the pipeline.** A real near-complete precursor was located outside this repo — `programs/codex/requirements/GE-05-oracle-validation-and-parity-harness/artifacts/pf1-crb-human-fighter-level1-provisional-ge05-e1-f2.pcg`, proven to load in the real PCGen engine by GE-05's own runtime-output receipt (`BUILD SUCCESSFUL`, exit 0, matching SHA-256 already cited in the golden fixture's `legacy_raw_output_sha256`). It carried the correct campaign/gamemode/race/class/level/ability-scores/human-bonus/Power-Attack already; this cycle completed it (Dodge + Weapon Focus (Longsword) feats, Climb/Intimidate/Swim rank-1 skill allocations, Chain Shirt + Longsword equipped/active, no shield — the exact `pf1_human_fighter_level1_ge06_deterministic_input.txt` contract) and renamed its `CHARACTERNAME` to the pilot's own `case_id` so `character.identity` carries a genuine same-character signal. `tests/sd26_pilot_case_verification.rs` now points at this completed file instead of the substitute `pf_Paladin.pcg`. Original blocker text (superseded, kept for history): "No real PCGen-native `.pcg` character file exists anywhere (either repo) for the exact pilot build... Epic 2 Criterion 2.5's full-pipeline verification cycle... found 0/9 dimensions matching, including a `character.identity` mismatch." That specific blocker on `CG-03` is closed. **A new, real, structural blocker on `CG-03` was discovered by this followup cycle instead — see the next `## DISCOVERED` entry below.**
- **NEW (followup cycle): `pilot_compute::compute_ability_modifiers` never applies the chosen Human `+2 Strength` racial ability bonus before deriving `AbilityModifiers`, causing a genuine `skill.selected_modifier.{climb,swim}` parity mismatch.** Running the real pipeline against the now-completed, genuinely same-character pilot `.pcg` (see the resolved item above) produced a real, informative comparison: 7 of 9 selected parity dimensions genuinely agree (`character.identity`, `combat.baseline_melee_attack_bonus`, `defense.baseline_armor_class`, all three `defense.total_save.*`, `skill.selected_modifier.intimidate`), but `skill.selected_modifier.climb` and `skill.selected_modifier.swim` disagree (PCGen: 6, Codex: 5). Root cause, traced in `src/rules_core/pilot_compute.rs`: `compute_ability_modifiers` derives `AbilityModifiers.strength` directly from the chosen raw ability *score* (16 -> modifier +3); `explain_human_pilot_race_seam`'s `race.human.ability_bonus_target` explanation *narrates* the chosen Human ability-bonus selection but never actually adds it to the score/modifier before that point, so the effective (bonus-applied) score of 18 (modifier +4) is never computed anywhere. `combat.baseline_melee_attack_bonus` coincidentally still matches PCGen's total (Codex: BAB+1 + STR+3 + Weapon Focus+1 = 5; PCGen: BAB+1 + STR+4 + 0 = 5 — different, non-equivalent arithmetic landing on the same number), masking the same underlying bug there. `skill.selected_modifier.climb`/`swim` have no such compensating term, so the missing `+1` surfaces directly. Blocks `CG-03` (`current_claim_status=oracle_checked`) until `compute_ability_modifiers` (or its caller) is fixed to fold chosen racial ability-score bonuses into the modifiers it returns. Not self-healable inline (out of this cycle's `src/oracle_validation/` file-touch scope; the fix belongs to `src/rules_core/pilot_compute.rs`, a different module's ownership). Forward to a `rules_core` fix cycle, then re-run `tests/sd26_pilot_case_verification.rs` to confirm 9/9 and genuinely earn the `oracle_checked` upgrade.
- **`equipmods.rs`'s 314/658 duplicate-`key` shell-record defect (E3.1, `decisions.md §11.6`) remains unfixed at its source.** Independently re-confirmed exactly: 658 raw entries, 344 truly-unique keys, 314 near-empty self-referential shells (`name == key`, `cost_gp: None`). This cycle de-duplicates at cache-write time only (per explicit instruction — fixing `equipmods.rs` itself would break several existing SD-24 tests that hard-code its `658`/`2977` totals, a larger and separate fix out of this cycle's scope): `data/corpus/core_rulebook/equipment/equipmods/` correctly has 344 records, one per real unique key, preferring the non-shell (`name != key`) entry in each duplicate pair. One duplicate pair (`Special Ability ~ Ghost Touch ~ Armor`) had the identical real web-sourced description on *both* sides — confirmed harmless, not a data-loss case. Not blocking; a future Epic 1/5/6 cycle should fix `equipmods.rs` at the source and correct its hard-coded totals. See `artifacts/epic_3/core_rulebook_json_cache-cycle_receipt.md`.
- **`beastiary1::mod.rs`'s `MonsterId` enum had no public `ALL`/count constant (E3.4, `decisions.md §11.6`)** — unlike `ClassId::ALL`/`ApgClassId::ALL`/`AcgClassId::ALL` on the other three books. `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs` had been hand-maintaining its own duplicate 41-entry workaround list (`ALL_BESTIARY1_MONSTERS`) to cope. This cycle added the real `MonsterId::ALL` constant to `beastiary1/mod.rs` and updated `corpus_ingest_diagnostic.rs` to read it directly, removing the duplicate list entirely. Also confirmed (independently, by direct corpus grep) that Bestiary 1's real equipment record count is 4, not the ~7 a prior SD-25 cycle-doc estimate assumed — matches `decisions.md §11.4`'s own correction note exactly. Not blocking; informational + a landed fix. See `artifacts/epic_3/beastiary_json_cache-cycle_receipt.md`.
- **RESOLVED (criterion 4.22 cycle): Criterion-to-book count mismatch in Epic 4's remaining work (E4.21).** After landing `ultimate_magic` (criterion 4.21, registry entry #0022), only 1 of the 21 future-state books remained unregistered (`ultimate_wilderness`), but 2 criterion numbers remained open (`4.12`, `4.22`) — one too many for the one remaining book. `content-unit-inventory.md`'s "Pre-existing count discrepancy" section already documents that the bundle's original 22-book/22-criterion scoping was corrected to 21 books, but the correction evidently did not fully renumber criteria: `4.12` had no book mapped to it across any of entries `#0003`-`#0022`. **Resolved by this cycle's explicit task brief, which assigned `ultimate_wilderness` to criterion 4.22** (registry entry #0023, the 21st and final future-state book) — confirming `4.12` is the orphaned/no-op criterion number from the original 22-book miscount, with no 22nd book to register against it. All 21 future-state books are now registered (#0003-#0023); `progress.md`'s criterion table records `4.12` as an explicit no-op row rather than leaving it silently open. See `artifacts/epic_4/ultimate_wilderness_stub_manifest-cycle_receipt.md`.

## Cycle log

| Cycle ID | Criterion | Commit SHA | Result |
|---|---|---|---|
| architecture-closure | 6.2 Architecture closure pipeline | (this cycle's commit) | complete — dual role per template §2 (truth-up pipeline + adversarial re-verify of 6.1). **Truth-up (manual):** the documented `architecture_truth_up.py` single-pipe-index parse bug makes it falsely report "no impact", so mapped changed paths to docs by hand. E2's oracle harness (comparator/normalization/parity_report/pcgen_runner) had landed, but `homebrew-and-oracle.md` still quoted the old "these are deferred/out-of-scope" mod-doc, `status.md` said "the in-crate comparator and report-writer are absent", and `overview.md` said "that comparator does not exist yet" — all now false. Rewrote the `oracle_validation/` section of `homebrew-and-oracle.md` (full descriptions of all four new modules + the pilot-case CG-03 mismatch), regraduated `status.md`'s Oracle-parity row (harness real; passing verdict still blocked on CG-03), fixed `overview.md`'s prose + mermaid node, and documented the E3 JSON corpus cache + E4 `book_stub` registry in `status.md`/`overview.md`/`rules-data-tables.md` (new "JSON corpus cache" section). Both README verification one-liners clean (0 missing paths, 0 broken links); `Last verified` refreshed on all four. **Graphify:** no bundle `receipts.md` exists, so ran the underlying `graphify cluster-only` directly — green (exit 0): 12243 nodes, 29197 edges, 632 communities, backup dir `2026-07-23/`, `graph.json`/`GRAPH_REPORT.md` refreshed; added `/graphify-out` to `.gitignore` (regenerated build output). **Adversarial re-verify of 6.1's scan: CONFIRMED** — 21 `data/stubs/*.json` + 21 registry `book_stub` entries (match); 4 E3 caches present with real record counts (core_rulebook 3326, apg 641, acg 423, beastiary 45); E2's 5 files exist and their tests pass live (sd26_comparator 4, sd26_normalization 7, sd26_parity_report 10, sd26_pcgen_runner 6, sd26_pilot_case_verification 2 incl. the genuine CG-03 skill mismatch). No discrepancy with 6.1's substance found. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| final-criterion-scan | 6.1 Final criterion scan | (this cycle's commit) | complete — modeled on SD-25's Epic 8 closure scan (same structure: per-criterion terminal-state table cross-checked against 3 independent sources, `## DISCOVERED` spot-check, verdict). 38/38 SD-26 criteria (17 declarative + 21 dynamic) accounted for, 0 missing. Found and flagged 4 new kanban/receipt paper-trail gaps (criteria 1.1, 2.1 cite non-existent Card IDs while real, correctly-titled `done` cards exist under different IDs; 15 of 21 E4 book-stub receipts left `Card ID:` as an unresolved placeholder despite real matching `done` cards, cosmetic only; criterion 4.6 has no kanban card at all despite real, verified landed work; criterion 5.1's receipt skips the standard schema entirely, has no kanban card, and `progress.md`'s own 5.1 commit-SHA cell was stale `(pending push)` though the commit is confirmed live) — none silently fixed, all documented in `artifacts/epic_6/closure-readiness-report.md` §3 for operator judgment, matching SD-25's own precedent of documenting rather than editing other epics' artifacts. Confirmed, not re-litigated, the 2 already-known real gaps named in the dispatch brief: CG-03's pilot ability-modifier bug (re-read `pilot_compute.rs` directly, still open) and the `planned_resolution_bundle` `decisions.md §10` (`"SD-27"`) vs all 21 landed E4 entries (`"SD-27+ (unscheduled)"`) discrepancy (re-verified via direct grep, flagged for operator resolution, not picked either way). `## DISCOVERED` queue spot-check: 7 live entries, under the 10-entry hard-stop, all independently re-verified live. `cargo test --workspace --locked` run live: 4124 passed, 0 failed, across 468 test binaries. Verdict: **complete-with-flagged-gaps** (honest per CG-01's own "complete or have a real blocker" wording, not forced to all-green). Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Minted + completed hermes kanban receipt card `t_8ba2b809` on `codex-tranche-5` (assignee `operator`). |
| epic1-1.1-identifier-audit | 1.1 Source-code identifier audit | 74d9402 | complete — audited tree already clean (SD-24 prior remediation); added `tests/sd26_identifier_discipline_audit.rs` extending the standing regression guard to `scripts/` + `data/` (previously uncovered, ahead of Epic 3/4 populating `data/corpus/`+`data/stubs/`). RED/GREEN proven via temporary synthetic-leak injection since no real leak existed to remediate. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| epic2-2.1-comparator | 2.1 comparator | 744cd71 | complete — implemented `compare(canon_pcg: &NormalizedOutput, codex: &SelectedParityDimensions) -> ComparisonResult` in `src/oracle_validation/comparator.rs`; defined `NormalizedOutput`/`NormalizedDimensionValue` mirroring `SelectedDimension`'s shape (normalization.rs/2.2 not yet built). Authored `tests/sd26_comparator.rs` (did not exist) covering agreement, value mismatch, and both one-sided-dimension cases against the real GE06 pilot receipt. RED (module missing) -> GREEN (4/4 tests pass) -> `cargo test --locked --lib` 157/157 pass. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| epic2-2.2-normalization | 2.2 normalization | a87bc0d | complete — implemented `normalize(raw: &RawPcgenOutput, rules: &[NormalizationRule]) -> NormalizedOutput` (+ `normalize_dimension_value`) in `src/oracle_validation/normalization.rs`, authoring `RawDimensionValue`/`RawPcgenOutput` (2.4's expected producer shape, not yet built) and the two `technical-design.md §2.2` default rules (`trailing-whitespace-strip`, `integer-coercion`, applied in that order). Authored `tests/sd26_normalization.rs` (did not exist), including an end-to-end test composing `normalize()` output straight into `comparator::compare()`. RED (module missing) -> GREEN (7/7 tests pass) -> `cargo test --locked --test sd26_comparator` 4/4 + `cargo test --locked --lib` 157/157 pass, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| epic2-2.4-pcgen_runner | 2.4 pcgen_runner | 20ab8c9 | complete — implemented `run_pcgen_character(character_pcg: &Path, options: &PcgenRunOptions) -> Result<PcgenRunOutput, PcgenRunnerError>` in `src/oracle_validation/pcgen_runner.rs`, wrapping the real `scripts/pcgen-run-character.sh` (PCGen Gradle invocation) + `scripts/pcgen-normalize-output.py` (typed-JSON normalizer) pair. Judgment call: since the normalizer script already emits typed `value_string`/`value_i16` dimensions (not raw text), `PcgenRunOutput::to_normalized_output()` bridges directly into `comparator.rs`'s `NormalizedOutput` rather than round-tripping through `normalization.rs`'s `RawPcgenOutput` — see receipt for full reasoning. Authored `tests/sd26_pcgen_runner.rs` (did not exist), including a real end-to-end PCGen invocation against the same substitute `pf_Paladin.pcg` fixture SD-25's `pcgen_runner_smoke.rs` uses. RED (module missing) -> GREEN (6/6 tests pass) -> `cargo test --locked --lib` 157/157, `sd26_comparator` 4/4, `sd26_normalization` 7/7, `pcgen_runner_smoke` 2/2, identifier-discipline audits 1/1 each pass, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Minted + completed hermes kanban receipt card `t_7ad1a31b` on `codex-tranche-5` (assignee `operator`). |
| epic2-2.3-parity_report | 2.3 parity_report | 7566d06 | complete — implemented `render_parity_report(case_id: &str, comparison: &ComparisonResult, normalization_rules_used: &[NormalizationRule]) -> String` + `write_parity_report(output_dir: &Path, ...) -> io::Result<PathBuf>` + `default_parity_report_dir() -> PathBuf` in `src/oracle_validation/parity_report.rs`, rendering `technical-design.md §2.3`'s Summary / Per-Dimension Comparison / Normalization Rules Used / Discovered Deltas Markdown shape from a real `ComparisonResult` (2.1) + rule set (2.2), and writing it to `artifacts/oracle_validation/parity_report_<case-id>.md` per `scope-draft.md §1.2`. Pure renderer: does not itself run the comparator or normalization engine. Authored `tests/sd26_parity_report.rs` (did not exist, 10 tests) covering the case-id heading, summary counts, per-dimension table rows (matched + mismatched), normalization-rules listing, discovered-deltas listing (incl. one-sided `MissingFromCodex`/`MissingFromPcgen` reasons rendered as legible English), pass/fail result line, and a real on-disk write/read round trip. RED (module missing) -> GREEN (10/10 tests pass) -> `cargo test --locked --lib` 157/157, `sd26_comparator` 4/4, `sd26_normalization` 7/7, `sd26_pcgen_runner` 6/6, identifier-discipline audit 1/1 pass, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Verification per acceptance-and-verification.md row 2.3 ("Manual review + cargo test"): cargo test proves renderer/writer mechanics; manual review (deferred to 2.5, once a real pilot-case report exists) should confirm the rendered Markdown reads well to a human and that `artifacts/oracle_validation/` is an acceptable durable location. Minted + completed hermes kanban receipt card `t_c9b7b0b4` on `codex-tranche-5` (assignee `operator`). |
| epic2-2.5-pilot_case_verification | 2.5 verification cycle | 0a8ba63 | complete — ran the full comparator pipeline (2.1 comparator + 2.2 normalization/carry + 2.3 parity_report + 2.4 pcgen_runner) for real, end to end, against the real pilot case: real Codex-computed `SelectedParityDimensions` (via `rules_core::pilot_compute`), a real PCGen engine run (via the substitute `pf_Paladin.pcg` fixture — no real `.pcg` exists for this exact pilot build in either repo), the real `compare()`, and a real written `artifacts/oracle_validation/parity_report_pf1-crb-human-fighter-level1.md`. Result: 0/9 dimensions matched, including a `character.identity` mismatch proving this is not a genuine same-character parity check. Per the criterion's own instruction, did NOT force `current_claim_status` to `oracle_checked`; it stays `not_yet_grounded` (asserted both as the typed `ClaimTier` and against the raw on-disk fixture text). Authored `tests/sd26_pilot_case_verification.rs` (did not exist, 2 tests). `cargo test --locked --lib` 157/157, `sd26_comparator` 4/4, `sd26_normalization` 7/7, `sd26_parity_report` 10/10, `sd26_pcgen_runner` 6/6, identifier-discipline audit 1/1 pass, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Forwarded a `## DISCOVERED` blocker: no real matching `.pcg` exists for the pilot build, blocking `CG-03` until one is hand-authored (out of this epic's file-touch scope, a different repo) or another legitimate same-character oracle source is found. Minted + completed hermes kanban receipt card `t_b7bb32bf` on `codex-tranche-5` (assignee `operator`). |
| epic2-2.5-pilot_case_verification_followup | 2.5 verification cycle (followup) | (see this cycle's commit) | complete — located a real, genuine, near-complete precursor `.pcg` outside this repo (GE-05 provisional runtime evidence, proven to load in the real PCGen engine), completed it to the exact `pf1_human_fighter_level1_ge06_deterministic_input.txt` contract (Dodge + Weapon Focus (Longsword) feats via real `cr_feats.lst`-confirmed `KEY:` names, Climb/Intimidate/Swim rank-1 `SKILL:...CLASSBOUGHT:` allocations, Chain Shirt + Longsword `EQUIPNAME:`/`EQUIPSET:Armor`/`EQUIPSET:Primary Hand` equipped/active, no shield), and renamed `CHARACTERNAME` to the pilot `case_id` so `character.identity` carries a genuine same-character signal. Re-ran the real pipeline (`tests/sd26_pilot_case_verification.rs` now points at the completed file, not `pf_Paladin.pcg`). Result: 7/9 dimensions genuinely match (`character.identity`, `combat.baseline_melee_attack_bonus`, `defense.baseline_armor_class`, `defense.total_save.{fortitude,reflex,will}`, `skill.selected_modifier.intimidate`); 2/9 genuinely mismatch (`skill.selected_modifier.climb`/`swim`, PCGen 6 vs Codex 5) — traced to a real bug: `pilot_compute::compute_ability_modifiers` never applies the chosen Human `+2 Strength` racial bonus before deriving `AbilityModifiers.strength`. Per the criterion's own instruction, did NOT force `current_claim_status` to `oracle_checked` (real mismatches remain); stays `not_yet_grounded` (asserted both as the typed `ClaimTier` and against the raw on-disk fixture text). `cargo test --locked --lib` 162/162, `sd26_comparator` 4/4, `sd26_normalization` 7/7, `sd26_parity_report` 10/10, `sd26_pcgen_runner` 6/6, `sd26_identifier_discipline_audit` 1/1, `sd26_pilot_case_verification` 2/2 pass, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Forwarded a new `## DISCOVERED` blocker (the ability-modifier bug above) superseding the old "`.pcg` doesn't exist" blocker, which is now resolved. See `artifacts/epic_2/pilot_case_oracle_checked-followup-cycle_receipt.md`. |
| epic3-3.2-apg_json_cache | 3.2 advanced_players_guide cache | b705e91 | complete — new `src/rules_core/cache_gen/apg.rs` generator (`generate(corpus_root, out_dir, ingested_at, fetched_at_web) -> GenerationReport`) DUMPS `rules_tables::apg`'s current runtime state (`ApgClassId::ALL` + each class's `class_table()`, `spell_list::SPELL_LIST`, `equipment_tables::EQUIPMENT_TABLE`) to `data/corpus/advanced_players_guide/{class,spell,equipment}/*.json` per `decisions.md §11.3` — never re-parses raw LST for values, only for real, checkable line citations (via a corpus grep helper) of values already known correct from the compiled module. Shape B `source` uses the `decisions.md §11.2` discriminated union: 298 `lst_token`, 337 `web_second_source` (331 equipment description + 6 spell), 3 `same_book_fallback` (Threefold Aspect sub-forms), 3 `lst_corrected_ingest` (same-line `.MOD`-concatenation fix records). 641/641 records generated with 0 unresolved citations against the real PCGen corpus. Real ceilings re-verified directly from the compiled module before generation (not trusted blindly from `decisions.md`) and matched exactly: 6/6 classes, equipment description 331/338 (97.9%), spell full_text 284/297 (95.6%), spell description 285/297. Authored `tests/sd26_cache_apg.rs` (did not exist, 7 tests). RED (cache dirs absent) -> 7 failed for the correct reason -> GREEN (7/7 pass). `cargo test --locked --lib` 158/158 (+1 new unit test), `sd24_equipment_coverage_audit` 9/9, `sd26_identifier_discipline_audit` 1/1, `cargo build --workspace` clean — no regressions (full `cargo test --locked --workspace` twice hit external PCGen-Gradle-invocation timeouts unrelated to this cycle's files, per running-process inspection during the run). Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Judgment calls (documented in receipt + module doc comment): `source` is keyed off each record's most informative field (chassis for classes, `description` for spell/equipment), not a literal per-field breakdown; equipment `web_second_source.url` uses a two-tier precision (`exact_item_page` for ~35 individually-named SD-25-receipt items, `category_aggregate_page` for the remainder) since `equipment_data.rs` stores no per-record URL and redoing SD-25's web research is outside this cycle's charter; `fetched_at` uses the real SD-25 sourcing-commit date as a documented proxy. |
| epic3-3.3-acg_json_cache | 3.3 advanced_class_guide cache | c42af42 | complete — new `src/rules_core/cache_gen/acg.rs` generator (`generate(corpus_root, out_dir, ingested_at) -> GenerationReport`) DUMPS `rules_tables::acg`'s current runtime state (`AcgClassId::ALL` + each class's `class_table()`, `spell_list::SPELL_LIST`, `equipment_tables::equipment_tables()`) to `data/corpus/advanced_class_guide/{class,spell,equipment}/*.json` per `decisions.md §11.3`. ACG's real ceiling was UNKNOWN before this cycle (not covered by SD-25's corpus-intake pass, per `risks-and-open-questions.md` Q4) — independently measured directly from the compiled module, not assumed: 10/10 classes with full 20-level chassis; spell description+full_text 144/144 (100% — ACG's own `DESC:` token already carries full text on the base record, unlike CRB/APG's `.MOD`-record convention); equipment description 264/269 (98.1%, sourced from the corpus `SPROP:` token since `acg_equip.lst`/`acg_equipmods.lst` carry zero `DESC:` tokens). 423/423 records generated with 0 unresolved citations against the real PCGen corpus; every record is `source.kind = lst_token` (no web-sourcing or fallback needed for this book — every populated value traces to a real, checkable corpus line). Equipmods citation lookup disambiguates repeated display names (e.g. "Sneaky") by the real `KEY:` field, not first-column name match. Authored `tests/sd26_cache_acg.rs` (did not exist, 9 tests). RED (cache dirs absent) -> 9 failed for the correct reason -> GREEN (9/9 pass). `cargo test --locked --lib` 159/159 (+1 new unit test), `sd24_acg_class_coverage_audit` 3/3, `sd26_identifier_discipline_audit` 1/1, `cargo build --locked --workspace` clean — no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Minted + completed hermes kanban receipt card `t_11f1e8c0` on `codex-tranche-5` (assignee `operator`). See receipt for full judgment calls (all-`lst_token` honesty rationale, equipmods KEY:-disambiguation, the real "Fake Rapier" corpus oddity dumped verbatim). |
| epic3-3.4-beastiary_json_cache | 3.4 beastiary cache | 228a077 | complete — new `src/rules_core/cache_gen/beastiary1.rs` generator (`generate(corpus_root, out_dir, ingested_at, fetched_at_web) -> GenerationReport`) DUMPS `rules_tables::beastiary1`'s current runtime state (`MonsterId::ALL` + `monster_resolve()`, `equipment_tables::EQUIPMENT_TABLE`) to `data/corpus/beastiary/{monster,equipment}/*.json` per `decisions.md §11.3`. Landed the `decisions.md §11.6` in-scope fix as part of this cycle: added `MonsterId::ALL` to `beastiary1/mod.rs` (this book had no public `ALL`/count constant, unlike the other 3 books) and updated `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs` to read the real constant instead of its own hand-maintained duplicate 41-entry list. Real ceilings independently re-verified directly against the live corpus, not assumed: monsters 41/41 (`MonsterId::ALL`, the SD-22 corrected roster, `completeness: chassis_only` — AC/HP/saves deliberately out of scope); equipment 4/4 (100%) — confirmed the real record count is 4, not the ~7 a prior SD-25 estimate assumed (`decisions.md §11.4`'s own correction, independently re-confirmed by direct corpus grep this cycle). 3/4 equipment records cite `lst_token` via the corpus's `SPROP:` token (register A10); 1 (`Rag Armor (Dark Creeper)`) carries SD-25's own `web_second_source` pass forward verbatim. No spell-list concept exists for this book (register A13, re-confirmed) — generator writes only `monster/`+`equipment/`. 45/45 records generated with 0 unresolved citations. Authored `tests/sd26_cache_beastiary.rs` (did not exist, 7 tests). RED (cache dirs absent) -> 7 failed for the correct reason -> GREEN (7/7 pass, after fixing an over-strict `speed_ft > 0` test assertion — Vargouille's real corpus row has no Walk-speed token, `speed_ft: 0` is honest, not a bug). `cargo test --locked --lib` 162/162 (+3 new unit tests), `sd24_equipment_coverage_audit` 9/9, `sd26_identifier_discipline_audit` 1/1, `cargo build --locked --workspace` clean, `cargo check --locked` in `apps/desktop/src-tauri` clean (5 pre-existing unrelated warnings only) — no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Minted + completed hermes kanban receipt card `t_4bd46141` on `codex-tranche-5` (assignee `operator`). See receipt for full judgment calls. |

| epic3-3.1-core_rulebook_json_cache | 3.1 core_rulebook cache | 13108c6 (feat commit `ecc00b8`) | complete — new `src/rules_core/rules_tables/crb/json_cache.rs` (Shape B discriminated-union record types, kept local to the CRB module to stay file-disjoint from the 3 sibling `isolation: 'worktree'` cycles) + `src/bin/sd26_gen_core_rulebook_cache.rs` (one-off codegen tool) DUMP `rules_tables::crb`'s current runtime state (`ClassId::ALL`+`class_tables()`+`good_saves_for()`, `spell_list::SPELL_LIST`, `equipment_tables::equipment_tables()`) to `data/corpus/core_rulebook/{class,spell,equipment}/*.json` per `decisions.md §11.3` — reads the real local PCGen corpus checkout only for per-record citation metadata (path/SHA-256/line), never for `data` field values. 3326 records generated (11 class + 652 spell + 2663 deduplicated equipment) with **zero unattributable records** (100% real, checkable `source` citations, no fabrication). `source.kind` distribution independently counted and reconciled against `decisions.md §11.2`: 67 `lst_corrected_ingest` (exact match), 117 `lst_inherited_copy` (exact match), 82 `web_second_source` (vs. cited 83 — reconciled: one duplicate-key equipmods pair carried the identical web-sourced text on both sides, collapsed by de-duplication, not a regression), 3060 `lst_token`. Equipment description ceiling re-verified directly via `field_coverage_report()`: 2021/2977 (67.9%) on the raw table, exactly matching `decisions.md §11.4`; 2020/2663 (75.9%) on this cache's own deduplicated set. `equipmods.rs`'s 314/658 duplicate-key defect (`decisions.md §11.6`) de-duplicated at cache-write time (preferring non-shell `name != key` entries), source module left unfixed and forwarded as `## DISCOVERED` per this cycle's explicit instruction. Authored `tests/sd26_cache_core_rulebook.rs` (did not exist, 6 tests). RED (cache dirs absent, verified by moving the freshly-generated cache aside and re-running) → 6 failed for the correct reason → GREEN (6/6 pass). `cargo test --locked --lib` 162/162, full `cargo test --locked` workspace-wide (every integration test file, including `sd26_pcgen_runner` 6/6 real PCGen engine invocation and `sd26_pilot_case_verification` 2/2) — all green, no regressions. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Minted + completed hermes kanban receipt card `t_cce2e62e` on `codex-tranche-5` (assignee `operator`). Judgment calls (documented in receipt): equipment directory layout uses the real `EquipmentCategory` enum (`arms_armor`/`general`/`magic_items`/`equipmods`) rather than `technical-design.md §3.2`'s illustrative `{weapon,armor,shield,wondrous,consumable}` scheme (the real data has no such subtype breakdown); `completeness` for equipment is derived from field presence (`full` when `description.is_some()`, else `chassis_plus_extract`/`chassis_only`); class BAB/save formula strings re-derived from `class_tables()`'s public per-level numeric output (module-private `BabProgression`/`CLASS_META` were not directly accessible). |
| epic4-4.1-research_book_stub_kind | 4.1 research epic | d6a7c61 | complete — defined the `book_stub` registry entry kind in `docs/governance/wired-integration-stubs-registry.md`, adapting #0001/#0002's seven-field code-stub shape to a data-completeness-gap shape (`File / line`→`Book / manifest path`; `Stub pattern`→`What's missing`; other five fields carried over with `book_stub`-specific guidance). Landed pilot entry #0003 (`advanced_race_guide`) citing `README.md §3`'s operator-verbatim quote (2026-07-21 17:39:26, "in-scope books no stubs, future-state books knowingly stub") and `data/stubs/advanced_race_guide.json` following `content-unit-inventory.md §2.1`'s shape exactly (`{book_id, book_name, planned_resolution_bundle, content_kind_counts: null, registered_at}`), no deviation. Validated JSON via schema assertion. Judgment call: used `"SD-27+ (unscheduled)"` for `planned_resolution_bundle` per this cycle's explicit brief instruction, which conflicts with `decisions.md §10`'s pinned `"SD-27"` default — forwarded as `## DISCOVERED` for operator resolution before 4.2-4.22 scale out. No cargo test suite (markdown/JSON authoring criterion, per `epic-breakdown.md`); verification was JSON-schema + structural-pattern grep against #0001/#0002. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| adventurers_guide_stub_manifest | 4.3 adventurers_guide stub manifest | 6f820ee | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for the next future-state book: `data/stubs/adventurers_guide.json` (`{book_id: "adventurers_guide", book_name: "Adventurer's Guide", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T00:46:07Z"}`) + registry entry #0004 following #0003's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/adventurers_guide/_adventurers_guide.pcc` confirms `SOURCELONG:Adventurer's Guide`, a real Paizo PF1 supplement (`SOURCEDATE:2017-06`) with real LST content files (classes/races/spells/feats/abilities/equipment/templates/companion mods) none of which is ingested into `data/corpus/adventurers_guide/`. Entry number (#0004) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading, not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as 4.1); verification was JSON-schema shape assertion + structural-pattern comparison against #0003. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| beginner_box_stub_manifest | 4.2 beginner_box stub manifest | 180a6b0 (+ SHA-stamp commit 24fb3fb) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for the next genuinely-unclaimed future-state book: `data/stubs/beginner_box.json` (`{book_id: "beginner_box", book_name: "Beginner Box", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T00:52:00Z"}`) + registry entry #0005 following #0003/#0004's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/beginner_box/`'s `.pcc` confirms `SOURCELONG:Beginner Box`, a real Paizo PF1 supplement (`SOURCEDATE:2011-10`) with real LST content files (`bbox_equip_arms_armor.lst`, `bbox_equip_magic_items.lst`), none of which is ingested into `data/corpus/beginner_box/`. **This cycle's dispatch brief named `advanced_race_guide` for criterion 4.2**, but that book was already registered by criterion 4.1 as its own pilot (#0003) — both 4.1's receipt and this bundle's own `progress.md` said so explicitly before this cycle started. Pivoted to `adventurers_guide` (the book 4.1's receipt names as next), implemented it fully, then hit a rebase conflict: a concurrent sibling cycle had independently reached the same conclusion and landed `adventurers_guide` first (#0004, commit `6f820ee`, their own criterion label "4.3"). Aborted the rebase, discarded the local `adventurers_guide` commit, re-verified the live registry + `data/stubs/` for the actual next unclaimed book, and landed against `beginner_box` instead. Entry number (#0005) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading, not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as 4.1/4.3); verification was JSON-schema shape assertion + structural-pattern comparison against #0003/#0004. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full two-attempt narrative. |
| bestiary_2_stub_manifest | 4.5 bestiary_2 stub manifest | 9a3c1bd | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `bestiary_2`: `data/stubs/bestiary_2.json` (`{book_id: "bestiary_2", book_name: "Bestiary 2", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T00:58:51Z"}`) + registry entry #0006 following #0003/#0004/#0005's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary_2/bestiary_2.pcc` confirms `CAMPAIGN:Bestiary 2`, `SOURCELONG:Bestiary 2`, a real Paizo PF1 supplement (`SOURCEDATE:2010-12`) with real LST content files (`b2_races.lst`, `b2_templates.lst`, `b2_abilities_race.lst`, `b2_equip_general.lst`, etc.), none of which is ingested into `data/corpus/bestiary_2/`. Re-verified live registry + `data/stubs/` state at cycle-start before writing (confirmed `bestiary_2` still unclaimed by any sibling cycle). Entry number (#0006) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0005`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as 4.1/4.2/4.3); verification was JSON-schema shape assertion + structural-pattern comparison against #0003/#0004/#0005. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| bestiary_3_stub_manifest | 4.4 bestiary_3 stub manifest | f174225 | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `bestiary_3`: `data/stubs/bestiary_3.json` (`{book_id: "bestiary_3", book_name: "Bestiary 3", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T01:04:12Z"}`) + registry entry #0007 following #0003-#0006's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary_3/bestiary_3.pcc` confirms `SOURCELONG:Bestiary 3`, `SOURCESHORT:B3`, a real Paizo PF1 supplement (`SOURCEDATE:2012-01`); `data/corpus/bestiary_3/` does not exist. **This cycle's dispatch brief named `beginner_box` for criterion 4.4**, but that book was already registered by a concurrent sibling (criterion 4.2, #0005, commit `180a6b0`) before this cycle's first rebase. Pivoted to `bestiary_2` (the next unclaimed book per the canonical 21-book list), implemented it fully and committed locally, then on the pre-push re-fetch found a second concurrent sibling had independently landed `bestiary_2` first (criterion 4.5, #0006, commit `9a3c1bd`). Discarded both local `bestiary_2` commits (`git reset --hard origin/tranche/5-4`, safe — neither had been pushed), re-derived the next unclaimed book (`bestiary_3`), and landed against it instead. Entry number (#0007) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0006`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion + structural-pattern comparison against #0003-#0006. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full two-collision narrative. |
| bestiary_5_stub_manifest | 4.7 bestiary_5 stub manifest | 262bbcd (+ SHA-stamp commit, same cycle) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `bestiary_5`: `data/stubs/bestiary_5.json` (`{book_id: "bestiary_5", book_name: "Bestiary 5", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:15:04Z"}`) + registry entry #0009 following #0003-#0008's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary_5/_bestiary_5.pcc` (and `_bestiary_5_for_players.pcc`) confirm `SOURCELONG:Bestiary 5`, `SOURCESHORT:B5`, a real Paizo PF1 supplement (`SOURCEDATE:2015-12`, `SOURCEWEB:http://paizo.com/products/btpy9g9x`); `data/corpus/bestiary_5/` does not exist. **This cycle's dispatch brief named `bestiary_4` for criterion 4.7**, but that book was already registered by a concurrent sibling (criterion 4.6, #0008, commit `c97c9a4`) before this cycle's first rebase — confirmed by both the on-disk `data/stubs/bestiary_4.json` and the pre-existing `artifacts/epic_4/bestiary_4_stub_manifest-cycle_receipt.md` (a `Write` attempt to that exact receipt path failed because the file already existed). Re-verified live registry + `data/stubs/` state a second time after a fresh rebase; `bestiary_4` remained claimed. Derived the next genuinely unclaimed book from the canonical 21-book list (`content-unit-inventory.md` line 43) — `bestiary_5` — and landed against it instead, naming the receipt `bestiary_5_stub_manifest-cycle_receipt.md` after the book actually landed. Entry number (#0009) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0008`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion (key-set/order diff against the pilot) + structural-pattern comparison against #0003-#0008. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full pivot narrative. |
| bestiary_6_stub_manifest | 4.8 bestiary_6 stub manifest | b7ab111 (+ SHA-stamp commit `3824fa0`) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `bestiary_6`: `data/stubs/bestiary_6.json` (`{book_id: "bestiary_6", book_name: "Bestiary 6", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:19:40Z"}`) + registry entry #0010 following #0003-#0009's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary_6/_bestiary_6.pcc` confirms `SOURCELONG:Bestiary 6`, `SOURCESHORT:B6`, a real Paizo PF1 supplement (`SOURCEDATE:2017-05`); `data/corpus/bestiary_6/` does not exist. **This cycle's dispatch brief named `bestiary_3` for criterion 4.6**, but by the time this cycle's worktree started, both `bestiary_3` (#0007, criterion 4.4) and the "criterion 4.6" label itself (`bestiary_4`, #0008) were already claimed on `origin/tranche/5-4`. Re-derived the canonical 21-book list and pivoted to `bestiary_5` (next unclaimed) first, implementing and committing it locally as criterion 4.7/entry #0009 — but on the pre-push re-fetch found a concurrent sibling had independently landed the identical book (`bestiary_5`, commit `69a3f86`, entry #0009, `registered_at` timestamp 5 seconds ahead), a genuine add/add content collision on both the manifest and the registry entry, not a mergeable diff. `git rebase --abort` + `git reset --hard origin/tranche/5-4` (safe, the local commit had never been pushed), re-derived the next unclaimed book a second time (`bestiary_6`), and landed against it instead as criterion 4.8/entry #0010. Entry number (#0010) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0009`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion + structural-pattern comparison against #0003/#0009. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full two-collision narrative. |
| bonus_bestiary_stub_manifest | 4.9 bonus_bestiary stub manifest | bdaf39b (+ SHA-stamp commit `392763d`) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `bonus_bestiary`: `data/stubs/bonus_bestiary.json` (`{book_id: "bonus_bestiary", book_name: "Bonus Bestiary", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:24:12Z"}`) + registry entry #0011 following #0003-#0010's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bonus_bestiary/_bonus_bestiary.pcc` confirms `CAMPAIGN:Bonus Bestiary`, `SOURCELONG:Bonus Bestiary`, `SOURCESHORT:BB`, a real Paizo PF1 supplement (`SOURCEDATE:2009-06`); `data/corpus/bonus_bestiary/` does not exist. **This cycle's dispatch brief named `bestiary_5` for criterion 4.8**, but by the time this cycle's worktree started, both `bestiary_5` (#0009, criterion 4.7, commit `69a3f86`) and the "criterion 4.8" label itself (`bestiary_6`, #0010, commit `b7ab111`) were already claimed live on `origin/tranche/5-4`, confirmed on disk before writing anything. Re-derived the canonical 21-book list (`content-unit-inventory.md §2.2`) and, cross-checking against every book already present in `data/stubs/`, landed on `bonus_bestiary` — the next genuinely unclaimed book — as criterion 4.9/entry #0011, with no collisions on push (single fetch+rebase+push attempt succeeded). Entry number (#0011) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0010`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion (key-set/order diff against `bestiary_5.json`) + structural-pattern comparison against #0003-#0010. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full pivot narrative. |
| core_essentials_stub_manifest | 4.10 core_essentials stub manifest | cf7b84f | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `core_essentials`: `data/stubs/core_essentials.json` (`{book_id: "core_essentials", book_name: "Core Essentials", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:28:25Z"}`) + registry entry #0012 following #0003-#0011's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/core_essentials/_core_essentials.pcc` confirms `CAMPAIGN:Core Essentials`, `PUBNAMELONG:Paizo Inc.`, a real Paizo PF1 free rules-reference sourcebook (`#SOURCEDATE:2009-08`, most metadata lines `#`-commented in the upstream PCGen data but still legible); `data/corpus/core_essentials/` does not exist. **This cycle's dispatch brief named `bestiary_6` for criterion 4.9**, but that book was already registered by a concurrent sibling (criterion 4.8, #0010, commit `b7ab111`) before this cycle's first rebase. Pivoted to `bonus_bestiary` (the next unclaimed book per the canonical 21-book list) and wrote `data/stubs/bonus_bestiary.json` locally, but the next `git rebase origin/tranche/5-4` itself hard-failed ("untracked working tree files would be overwritten by checkout") because a concurrent sibling had independently landed the identical `bonus_bestiary` first (#0011, commit `bdaf39b`, `registered_at` earlier). Deleted the local untracked file, re-ran the rebase cleanly, re-derived the next unclaimed book a second time (`core_essentials`), and landed against it instead as criterion 4.10/entry #0012. Entry number (#0012) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0011`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion + structural-pattern comparison against #0003/#0011. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Push succeeded on first attempt (collision caught at rebase, before commit). See receipt for full pivot narrative. |
| horror_adventures_stub_manifest | 4.11 horror_adventures stub manifest | 9cda689 (+ footer-fix commit `617eb61`) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `horror_adventures`: `data/stubs/horror_adventures.json` (`{book_id: "horror_adventures", book_name: "Horror Adventures", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:33:17Z"}`) + registry entry #0013 following #0003-#0012's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/horror_adventures/_horror_adventures.pcc` confirms `CAMPAIGN:Horror Adventures`, `SOURCELONG:Horror Adventures`, `SOURCESHORT:HA`, a real Paizo PF1 supplement (`SOURCEDATE:2016-08`); `data/corpus/horror_adventures/` does not exist. **This cycle's dispatch brief named `bonus_bestiary` for criterion 4.10**, but by the time this cycle's worktree rebased, both `bonus_bestiary` (#0011, criterion 4.9, commit `bdaf39b`) and the "criterion 4.10" label itself (`core_essentials`, #0012, commit `cf7b84f`) were already claimed live on `origin/tranche/5-4`, confirmed on disk before writing anything. Re-derived the canonical 21-book list (`content-unit-inventory.md §2.2`) and, cross-checking against every book already present in `data/stubs/`, landed on `horror_adventures` — the next genuinely unclaimed book in list order — as criterion 4.11/entry #0013, with no collisions on either push attempt (registry entry, then a small footer-consistency fix to the reserved-entries note). Entry number (#0013) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0012`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was JSON-schema shape assertion (key-set/order check via `python3 -c "import json; ..."`) + structural-pattern comparison against #0003/#0012. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full pivot narrative. |
| monster_codex_stub_manifest | 4.13 monster_codex stub manifest | 621cdad | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `monster_codex`, dispatched explicitly for this book/criterion pair: `data/stubs/monster_codex.json` (`{book_id: "monster_codex", book_name: "Monster Codex", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:43:30Z"}`) + registry entry #0014 following #0003-#0013's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/monster_codex/_monster_codex.pcc` confirms `CAMPAIGN:Monster Codex (Player Options Only)`, `SOURCELONG:Monster Codex`, `SOURCESHORT:MC`, a real Paizo Pathfinder Player Companion (`SOURCEDATE:2014-11`); `data/corpus/monster_codex/` does not exist. Re-verified live registry + `data/stubs/` state at cycle-start before writing (confirmed `monster_codex` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for both the code commit and this `progress.md` update. Entry number (#0014) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0013`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was `python3 -c "import json; ..."` shape assertion + `cargo test --locked --test sd26_identifier_discipline_audit` (1/1 pass) + structural-pattern comparison against #0003-#0013. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| mythic_adventures_stub_manifest | 4.14 mythic_adventures stub manifest | 7575aa8 | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `mythic_adventures`, dispatched explicitly for this book/criterion pair: `data/stubs/mythic_adventures.json` (`{book_id: "mythic_adventures", book_name: "Mythic Adventures", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:46:04Z"}`) + registry entry #0015 following #0003-#0014's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/mythic_adventures/_mythic_adventures.pcc` confirms `CAMPAIGN:Mythic Adventures`, `SOURCELONG:Mythic Adventures`, `SOURCESHORT:MA`, a real Paizo PF1 hardcover supplement (`SOURCEDATE:2013-08`); `data/corpus/mythic_adventures/` does not exist. Re-verified live registry + `data/stubs/` state at cycle-start before writing (confirmed the highest existing registry heading was `#0014` and `mythic_adventures` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for the code commit. Entry number (#0015) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0014`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was `python3 -c "import json; ..."` shape assertion + structural-pattern comparison against #0003-#0014. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| occult_adventures_stub_manifest | 4.15 occult_adventures stub manifest | f31d833 | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `occult_adventures`, dispatched explicitly for this book/criterion pair: `data/stubs/occult_adventures.json` (`{book_id: "occult_adventures", book_name: "Occult Adventures", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:52:17Z"}`) + registry entry #0016 following #0003-#0015's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/occult_adventures/_occult_adventures.pcc` confirms `CAMPAIGN:Occult Adventures`, `SOURCELONG:Occult Adventures`, `SOURCESHORT:OA`, a real Paizo PF1 hardcover supplement (`SOURCEDATE:2015-07`; introduces the kineticist/medium/mesmerist/occultist/psychic/spiritualist base classes and the psychic magic system); `data/corpus/occult_adventures/` does not exist. Re-verified live registry state at cycle-start before writing (confirmed the highest existing registry heading was `#0015` and `occult_adventures` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for the code commit. Entry number (#0016) determined at edit time by re-fetch+rebase then grepping the registry on disk for the highest existing `### NNNN` heading (`#0015`), not assumed in advance. No cargo test suite (markdown/JSON authoring criterion, same as prior 4.x cycles); verification was `python3 -c "import json; ..."` shape assertion + structural-pattern comparison against #0003-#0015, plus a direct `grep` confirming both the manifest and registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| pathfinder_unchained_stub_manifest | 4.16 pathfinder_unchained stub manifest | a61305f | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `pathfinder_unchained`, dispatched explicitly for this book/criterion pair: `data/stubs/pathfinder_unchained.json` (`{book_id: "pathfinder_unchained", book_name: "Pathfinder Unchained", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T02:54:05Z"}`) + registry entry #0017 following #0003-#0016's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/pathfinder_unchained/_pathfinder_unchained.pcc` confirms `CAMPAIGN:Pathfinder Unchained`, `SOURCELONG:Pathfinder Unchained`, `SOURCESHORT:PU`, a real Paizo PF1 hardcover supplement (`SOURCEDATE:2015-04`, described as "an indispensable companion to the Pathfinder Roleplaying Game: Core Rulebook"), with real LST content files (`pu_abilities_class.lst`, `pu_abilities_race.lst`, `pu_equip.lst`, `pu_feats.lst`, `pu_spells.lst`, `pu_templates.lst`, etc.); `data/corpus/pathfinder_unchained/` does not exist. **Registry-number collision on the push-time rebase**: this cycle's first rebase found the highest existing registry heading at `#0015` and wrote its entry as `#0016`, but a concurrent sibling had independently landed `occult_adventures` as `#0016` (criterion 4.15) by the time of the push-time re-fetch+rebase, producing a genuine content conflict on the shared registry file. Resolved by keeping the sibling's `#0016` (`occult_adventures`) unchanged and renumbering this cycle's entry to `#0017`, then correcting the reserved-entries footer note; re-verified the resolved file (no conflict markers, 17 sequential `### NNNN` headings) before continuing the rebase and pushing. No cargo test suite required by the criterion (markdown/JSON authoring), but ran `cargo test --locked --test sd26_identifier_discipline_audit` (1/1 pass) as an extra check; verification was `python3 -c "import json; ..."` shape assertion (key-set/order match against the pilot) + structural-pattern comparison against #0003-#0016, plus a direct `grep` confirming both the manifest and the final #0017 registry entry landed. Dual-audit gate (re-run post-rebase, against the final diff): `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. See receipt for full collision-resolution narrative. |
| ultimate_campaign_stub_manifest | 4.17 ultimate_campaign stub manifest | a7df999 | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_campaign`, dispatched explicitly for this book/criterion pair: `data/stubs/ultimate_campaign.json` (`{book_id: "ultimate_campaign", book_name: "Ultimate Campaign", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:00:32Z"}`) + registry entry #0018 following #0003-#0017's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_campaign/_ultimate_campaign.pcc` confirms `CAMPAIGN:Ultimate Campaign`, `SOURCELONG:Ultimate Campaign`, `SOURCESHORT:UCA`, `PUBNAMELONG:Paizo Publishing` (`SOURCEDATE:2013-05`, described as covering downtime/kingdom-building/leadership/crafting systems "between adventures"), with real LST content files (`uca_abilities_drawbacks.lst`, `uca_abilities_traits.lst`, `uca_abilities_retraining*.lst`, `uca_abilitycategories.lst`, `uca_companionmods.lst`, `uca_feats.lst`); `data/corpus/ultimate_campaign/` does not exist. Re-verified live registry state at cycle-start before writing (confirmed the highest existing registry heading was `#0017` and `ultimate_campaign` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for both the code commit and this progress.md update. Mid-cycle self-correction: the manifest was first accidentally written to the main repo checkout's live working tree instead of this cycle's own worktree; caught and fixed before committing, no cross-cycle interference. No cargo test suite required by the criterion (markdown/JSON authoring), but ran `cargo test --locked --test sd26_identifier_discipline_audit` (1/1 pass) as an extra check; verification was `python3 -c "import json; ..."` shape assertion (key-set/order match against the pilot) + structural-pattern comparison against #0003-#0017, plus a direct `grep` confirming both the manifest and the #0018 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| ultimate_combat_stub_manifest | 4.18 ultimate_combat stub manifest | d1fa4b7 (+ SHA-stamp commit `c341b85`) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_combat`, dispatched explicitly for this book/criterion pair: `data/stubs/ultimate_combat.json` (`{book_id: "ultimate_combat", book_name: "Ultimate Combat", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:06:12Z"}`) + registry entry #0019 following #0003-#0018's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_combat/_ultimate_combat.pcc` confirms `CAMPAIGN:Ultimate Combat`, `SOURCELONG:Ultimate Combat`, `SOURCESHORT:UC`, `PUBNAMELONG:Paizo Inc.` (`SOURCEDATE:2011-01`, `PRECAMPAIGN` on the Advanced Player's Guide), with real LST content files (`uc_abilities.lst`, `uc_abilities_class.lst`, `uc_abilities_race.lst`, `uc_classes.lst`, `uc_equip_arms_armor.lst`, `uc_equip_general.lst`, `uc_equip_magic_items.lst`, `uc_equipmods.lst`, `uc_feats.lst`, `uc_kits.lst`, `uc_races.lst`, `uc_skills.lst`, `uc_spells.lst`, `uc_templates.lst`); `data/corpus/ultimate_combat/` does not exist. Re-verified live registry state at cycle-start before writing (confirmed the highest existing registry heading was `#0018` and `ultimate_combat` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for both the code commit and this progress.md update. No cargo test suite required by the criterion (markdown/JSON authoring), but ran the dual-audit gate against `${BASE_BRANCH}...HEAD` both pre-implementation and post-commit; verification was `python3 -c "import json; ..."` shape assertion (key-set/order match against the pilot) + structural-pattern comparison against #0003-#0018, plus a direct `grep` confirming both the manifest and the #0019 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| ultimate_equipment_stub_manifest | 4.19 ultimate_equipment stub manifest | ee142b1 (+ receipt commit `676986d`) | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_equipment`, dispatched explicitly for this book/criterion pair: `data/stubs/ultimate_equipment.json` (`{book_id: "ultimate_equipment", book_name: "Ultimate Equipment", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:11:08Z"}`) + registry entry #0020 following #0003-#0019's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_equipment/ultimate_equipment.pcc` confirms `CAMPAIGN:Ultimate Equipment`, `SOURCELONG:Ultimate Equipment`, `SOURCESHORT:UE`, `PUBNAMELONG:Paizo Publishing LLC` (`SOURCEDATE:2012-08`, `PRECAMPAIGN` on the Advanced Player's Guide), with real LST content files (`ue_abilities.lst`, `ue_abilitycategories.lst`, `ue_equip_arms_armor.lst`, `ue_equip_general.lst`, `ue_equip_magic_items.lst`, `ue_equipmods.lst`, `ue_kits.lst`, `ue_profs_armor.lst`, `ue_profs_weapon.lst`, `ue_skills.lst`, `ue_spells.lst`, `ue_templates.lst`); `data/corpus/ultimate_equipment/` does not exist. Re-verified live registry state at cycle-start before writing (confirmed the highest existing registry heading was `#0019` and `ultimate_equipment` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for both code commits and this progress.md update. No cargo test suite required by the criterion (markdown/JSON authoring); verification was `python3 -c "import json; ..."` shape assertion (key-set/order match against the pilot) + structural-pattern comparison against #0003-#0019, plus a direct `grep` confirming both the manifest and the #0020 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| ultimate_intrigue_stub_manifest | 4.20 ultimate_intrigue stub manifest | d509f33 | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_intrigue`, dispatched explicitly for this book/criterion pair: `data/stubs/ultimate_intrigue.json` (`{book_id: "ultimate_intrigue", book_name: "Ultimate Intrigue", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:14:54Z"}`) + registry entry #0021 following #0003-#0020's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_intrigue/_ultimate_intrigue.pcc` confirms `CAMPAIGN:Ultimate Intrigue`, `SOURCELONG:Ultimate Intrigue`, `SOURCESHORT:UI`, `PUBNAMELONG:Paizo Inc.` (`SOURCEDATE:2016-04`, `PRECAMPAIGN` on Core Rules + Advanced Player's Guide + Ultimate Magic + Ultimate Combat + Advanced Class Guide), with real LST content files (`ui_abilities_class.lst`, `ui_abilities_race.lst`, `ui_classes.lst`, `ui_companionmods.lst`, `ui_equip_arms_armor.lst`, `ui_equip_general.lst`, `ui_equip_magic_items.lst`, `ui_equipmods.lst`, `ui_feats.lst`, `ui_kits.lst`, `ui_profs_weapon.lst`, `ui_skills.lst`, `ui_spells.lst`, `ui_templates.lst`); `data/corpus/ultimate_intrigue/` does not exist. Re-verified live registry state at cycle-start before writing (confirmed the highest existing registry heading was `#0020` and `ultimate_intrigue` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for the code commit. No cargo test suite required by the criterion (markdown/JSON authoring); verification was `python3 -c "import json; ..."` shape assertion (key-set/order match against the pilot) + structural-pattern comparison against #0003-#0020, plus a direct `grep` confirming both the manifest and the #0021 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |
| ultimate_magic_stub_manifest | 4.21 ultimate_magic stub manifest | 06222fa | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_magic`, dispatched explicitly for this book/criterion pair: `data/stubs/ultimate_magic.json` (`{book_id: "ultimate_magic", book_name: "Ultimate Magic", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:19:03Z"}`) + registry entry #0022 following #0003-#0021's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_magic/_ultimate_magic.pcc` confirms `CAMPAIGN:Ultimate Magic`, `SOURCELONG:Ultimate Magic`, `SOURCESHORT:UM`, `PUBNAMELONG:Paizo Inc.` (`SOURCEDATE:2011-05`), with real LST content files (`um_classes.lst`, `um_feats.lst`, `um_spells.lst`, `um_abilities_class.lst`, `um_abilities_race.lst`, `um_abilities_companion.lst`, `um_domains.lst`, `um_equip_arms_armor.lst`, `um_equip_general.lst`, `um_kits.lst`, `um_templates.lst`, `um_races_companion.lst`); `data/corpus/ultimate_magic/` does not exist. Re-verified live registry state at cycle-start before writing (re-fetched/rebased twice, once before starting and once immediately before editing the shared registry file; confirmed the highest existing registry heading was `#0021` and `ultimate_magic` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for the code commit. No cargo test suite required by the criterion (markdown/JSON authoring); verification was `python3 -c "import json; ..."` shape assertion (key-set/value-shape match against the pilot) + structural-pattern comparison against #0003-#0021, plus a direct `grep` confirming both the manifest and the #0022 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. Forwarded a `## DISCOVERED` note: after this cycle only 1 book (`ultimate_wilderness`) remains unregistered but 2 criterion numbers (`4.12`, `4.22`) remain open — a leftover count mismatch from the bundle's original 22-book scoping, needs operator resolution before the final book_stub cycle. |
| ultimate_wilderness_stub_manifest | 4.22 ultimate_wilderness stub manifest | 72dccac | complete — replicated criterion 4.1's landed `book_stub` pattern (not redesigned) for `ultimate_wilderness`, dispatched explicitly for this book/criterion pair (task brief resolved the `4.12`/`4.22` ambiguity ultimate_magic's cycle flagged, assigning `ultimate_wilderness` to `4.22`): `data/stubs/ultimate_wilderness.json` (`{book_id: "ultimate_wilderness", book_name: "Ultimate Wilderness", planned_resolution_bundle: "SD-27+ (unscheduled)", content_kind_counts: null, registered_at: "2026-07-23T03:22:19Z"}`) + registry entry #0023 following #0003-#0022's field-by-field template exactly. Book identity verified against real source, not guessed: `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/ultimate_wilderness/_ultimate_wilderness.pcc` confirms `CAMPAIGN:Ultimate Wilderness`, `SOURCELONG:Ultimate Wilderness`, `SOURCESHORT:UW`, `PUBNAMELONG:Paizo Inc.` (`SOURCEDATE:2017-11`, introduces the shifter base class, druidic sects/rituals, and wilderness-survival character options per the `.pcc`'s own `DESC:` field), with real LST content files (`uw_classes.lst`, `uw_feats.lst`, `uw_spells.lst`, `uw_abilities_class.lst`, `uw_abilities_race.lst`, `uw_abilities_companion.lst`, `uw_domains.lst`, `uw_equip_arms_armor.lst`, `uw_equip_general.lst`, `uw_kits.lst`, `uw_templates.lst`, `uw_races.lst`); `data/corpus/ultimate_wilderness/` does not exist. Re-verified live registry state at cycle-start before writing (re-fetched/rebased before starting and again immediately before editing the shared registry file; confirmed the highest existing registry heading was `#0022` and `ultimate_wilderness` still unclaimed by any sibling cycle) — no collision, single fetch+rebase+push attempt succeeded for the code commit. This is the 21st and final future-state book, completing #0003-#0023; also updated the registry's reservation footer to record the closure and confirmed criterion 4.12 is an orphaned/no-op criterion number (`content-unit-inventory.md`'s "Pre-existing count discrepancy" note — the bundle's original 22-book miscount). No cargo test suite required by the criterion (markdown/JSON authoring); verification was `python3 -c "import json; ..."` shape assertion (key-set/value-shape match against the pilot) + structural-pattern comparison against #0003-#0022, plus a direct `grep` confirming both the manifest and the #0023 registry entry landed. Dual-audit gate: `OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`. |

## Open blockers
- **CG-03 (pilot case upgrade to `oracle_checked`)**: still genuinely blocked, but the blocker changed shape. The original blocker (no real matching `.pcg` existed) is **resolved** — a real, completed, genuinely same-character `.pcg` now exists and is wired into `tests/sd26_pilot_case_verification.rs`. The real pipeline run against it found 7/9 dimensions genuinely match and 2/9 (`skill.selected_modifier.climb`/`swim`) genuinely mismatch due to a real Codex bug in `pilot_compute::compute_ability_modifiers` (chosen racial ability-score bonuses are narrated but never applied). See `## DISCOVERED` above and `artifacts/epic_2/pilot_case_oracle_checked-followup-cycle_receipt.md`. Does not block Epic 3+.
- **`planned_resolution_bundle` value discrepancy (operator decision needed, per criterion 6.1's closure scan)**: `decisions.md §10` + `risks-and-open-questions.md §4` Q2 pin the default to `"SD-27"`, but all 21 landed E4 book_stub entries (`data/stubs/*.json`) instead carry `"SD-27+ (unscheduled)"` per an explicit, documented cycle-4.1-brief instruction. Both values are internally consistent across their own set of sources, but the two sets disagree with each other. Operator must either (a) correct `decisions.md §10`/Q2 to `"SD-27+ (unscheduled)"`, or (b) confirm `"SD-27"` and correct all 21 `data/stubs/*.json` entries + the corresponding registry prose to match. See `## DISCOVERED` (E4.1 entry) and `artifacts/epic_6/closure-readiness-report.md §4`. Does not block 6.2–6.5; should be resolved before any SD-27+ work assumes one value or the other.
- **4 kanban/receipt paper-trail gaps found by criterion 6.1's closure scan (all non-blocking, substance independently verified true in every case)**: (1) criteria 1.1 and 2.1's own receipts cite `Card ID:` values that do not exist on `codex-tranche-5`, while a different, real, correctly-titled `done` card exists for each; (2) 15 of the 21 Epic 4 book-stub receipts left their `Card ID:` field as an unresolved placeholder despite a real matching `done` card existing — cosmetic only; (3) criterion 4.6 (`bestiary_4`) has no kanban card anywhere on the board despite the work being real and landed; (4) criterion 5.1's receipt skips the standard receipt schema entirely, has no kanban card, and this file's own 5.1 commit-SHA cell was stale `(pending push)` though the commit is confirmed live. Full detail + disposition options in `artifacts/epic_6/closure-readiness-report.md §3`. None block 6.2–6.5.

---

*Per `loop-instruction.md §6 step 7`: the orchestrator updates this file in place via the concurrent-write protocol.*
