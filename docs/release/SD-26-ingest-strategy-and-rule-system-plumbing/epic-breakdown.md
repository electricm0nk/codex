# SD-26 — Epic Breakdown (6 epics / 17 declarative + 21 dynamic criteria = 38 total)

> **Operating method:** see `./scope-draft.md` — `scripts/workflow-dispatch.sh` (Workflow orchestrator). Bundle fires on `tranche/5-4`, kanban board `codex-tranche-5`. Cycle dispatch model is deterministic-seeded-then-dynamic.

## Execution lane split

E1 Identifier Cleanup is the governance base; fires FIRST. E2 Oracle-Harness Comparator reads SD-25's PCGen runner + writes the comparator + normalization + parity report. E3 + E4 carry the bulk of mechanical work (JSON cache for 4 in-scope books parallel + 21 future-state book stubs parallel). E5 cuts doctrine cost. E6 Closure Epilogue fires LAST.

## Epic 1 — Code-Side Identifier Cleanup (governance base; fires FIRST)

### Criterion 1.1 — Source-code identifier audit

- **Cycle artifact:** `./artifacts/epic_1/identifier-audit-cycle_receipt.md`
- **Cycle doc:** `./cycles/1_1.md`
- **RED:** `git grep -nE '\b(sd(16|19|22|23|24)_|SD(16|19|22|23|24)_|Sd(16|19|22|23|24)|t_[0-9a-f]{8,})\b' apps/desktop/ apps/desktop/src-tauri/ src/ scripts/ data/` returns ≥1 hit.
- **GREEN:** the renames land; the same `git grep` returns 0 hits.

## Epic 2 — Oracle-Harness Comparator (the missing piece)

### Criterion 2.1 — `src/oracle_validation/comparator.rs`

`compare(canon_pcg: &NormalizedOutput, codex: &SelectedDimensions) -> ComparisonResult`. Concurrency: serial.

### Criterion 2.2 — `src/oracle_validation/normalization.rs`

Small rule engine. Concurrency: serial.

### Criterion 2.3 — `src/oracle_validation/parity_report.rs`

Generates `parity_report_<case-id>.md`. Concurrency: serial.

### Criterion 2.4 — `src/oracle_validation/pcgen_runner.rs`

Rust-side wrapper. Concurrency: serial.

### Criterion 2.5 — Verification cycle for the pilot case

Runs the comparator against the pilot case; upgrades `current_claim_status` from `not_yet_grounded` to `oracle_checked`. Concurrency: serial.

## Epic 3 — JSON Cache Build (4 in-scope books; parallel per-book)

> **Read `decisions.md §11` + `technical-design.md §3.3` before authoring any of this epic's cycle docs.** SD-25's actual corpus-intake execution (post-dating this epic's original draft) found: the Shape B schema needs a discriminated `source` union (a real, large fraction of CRB/APG/Bestiary-1's completed fields have no LST-token provenance); the cache must be generated by dumping the completed Rust `rules_tables` modules, not by re-parsing raw LST (which would regress real, already-shipped completions); and CRB/APG do not reach 100% field coverage for genuine corpus reasons, not effort — this is measured, not hypothetical.

### Criterion 3.1 — `data/corpus/core_rulebook/`

`isolation: 'worktree'`. Per-class / per-spell / per-equipment JSON files.

### Criterion 3.2 — `data/corpus/advanced_players_guide/`

`isolation: 'worktree'`. APG content.

### Criterion 3.3 — `data/corpus/advanced_class_guide/`

`isolation: 'worktree'`. ACG content.

### Criterion 3.4 — `data/corpus/beastiary/`

`isolation: 'worktree'`. Bestiary 1 content.

## Epic 4 — Book Stub Manifest (21 future-state books; research epic + parallel)

### Criterion 4.1 — Research epic: `book_stub` kind in Stubs Registry

Defines the metadata fields; validates against an existing stub. Concurrency: serial.

### Criteria 4.2..4.22 — One cycle per future-state book

21 cycles, one each. Per cycle: writes `data/stubs/<book>.json` + Stubs Registry entry. Concurrency: `parallel: yes` after 4.1.

## Epic 5 — Doctrine-Cost Reduction

### Criterion 5.1 — Audit + cut per-class gate-cost

Single audit cycle. Output: `artifacts/epic_5/per-class-cycle-floor-measurement.md`. Concurrency: serial.

## Epic 6 — Closure Epilogue (subagent tiering per-criterion)

### Criterion 6.1 — Final criterion scan (Sonnet)

### Criterion 6.2 — Architecture closure pipeline (Opus; template §2 adversarial-verify)

### Criterion 6.3 — Release notes (Haiku)

### Criterion 6.4 — Build version increment (`0.5.99`, Haiku)

### Criterion 6.5 — PR + merge (Sonnet)

---

## Quick reference — 6 epics / 17 declarative + 21 dynamic criteria (38 total)

| Epic | Declarative criteria | Dynamic criteria | Concurrency |
|---|---|---|---|
| E1 Identifier Cleanup | 1 | 0 | serial |
| E2 Oracle-Harness Comparator | 5 | 0 | serial |
| E3 JSON Cache Build | 4 | 0 | parallel per-book |
| E4 Book Stub Manifest | 1 (research) | 21 (per-book) | serial research → parallel fan-out |
| E5 Doctrine-Cost Reduction | 1 | 0 | serial |
| E6 Closure Epilogue | 5 | 0 | serial; sub-step tiering |
| **Total** | **17** | **21 dynamic** | per-`parallel` row gets `isolation: worktree` |

Dynamic criteria (E4's 21) grow as the research epic output defines the per-book entry shape. The orchestrator script handles dynamic entries via `## DISCOVERED` priority-bump mechanism.
