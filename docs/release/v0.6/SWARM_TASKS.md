# Release-Swarm Task Table (v0.6 alpha)

> Single source of truth for the operator-facing task table on the dashboard.
> Each row is read 1:1 by the observer at `~/.hermes/profiles/god-emporer/skills/release-swarm-observer/scripts/observer.py`.
>
> Schema (operator-pinned 2026-07-23):
>   task     – ≤ 2-sentence description
>   owner    – one of {UI, Backend, QA}
>   rulebook – one of {CRB, B1, APG, ACG} or `-` (none / cross-cutting)
>   status   – one of {done, in progress, queued, blocked}
>   last_update – free-form (commit hash, date, or short note)
>
> The lead updates this file as work moves through the alpha-bar. The cron
> driver re-renders the dashboard every 60 seconds from this sidecar, so
> keeping it current is the cheapest possible visibility signal.
>
> **Rebuilt from scratch 2026-07-24.** The prior version of this file
> (seeded 2026-07-23) described work that never happened in this swarm —
> "Epic"-numbered rows ("campaign manager + Drive persistence," "Wizard
> single-class completion," etc.) that don't correspond to any real
> commit or task this session produced. Flagged as untrusted for most of
> the session rather than acted on; rebuilt here against the swarm's real
> tracking record (`risks-and-open-questions.md` items 1-27,
> `SWARM_STATUS.md`'s Happened log, and `git log`) instead of guessed or
> carried forward. Owner mapping note: this schema has no "lead" value;
> rows for lead-authored scoping/design docs use `QA` per the nearest
> real-work analog, with the actual author named in the row text.

| task | owner | rulebook | status | last_update |
|------|-------|----------|--------|-------------|
| 4-race ability-adjustment bug (Elf/Dwarf/Gnome/Halfling missing mental-stat component) | Backend | CRB | done | 9ec0e036 + 2f05dee4 |
| Create-character ability-score submission bug (calculated score shown but raw score sent) | UI | CRB | done | f2c616ed |
| Wizard spellbook bootstrap chain (school-choice seed, first-spell deadlock, slot-budget enforcement) | Backend | CRB | done | 3484b5d, b2a5eb6, 365b3a1a |
| Wizard spell-save-DC missing calculation | Backend | CRB | done | 3b39731 |
| Class-skill-modifier bug (Climb/Intimidate/Swim bonus applied regardless of actual class) | Backend | CRB | done | 93a0636d |
| Damage-reduction exposure (DTO + Defense tab render) | Backend/UI | CRB | done | f7ce289d, 26ac0704 |
| Recompute-DTO damage-reduction parity gap | Backend | CRB | done | af2d8b9f |
| Fighter feat-choice legality (race gate, multiclass gate, internal-consistency gate — 3 instances) | Backend | CRB | done | 32289cb4, 0eb9ea65, 68721ca0 |
| Class-dropdown honesty fix (human-diagnostics-only vs partial-human-only mislabeling) | UI | - | done | 34635157 |
| corpus_derived wire-serialization render-staleness bug | Backend | - | done | 498679d1 |
| Feat catalog exposure + Feats-tab picker + full feat-list display | Backend/UI | CRB | done | 89c3710, febf4d80, 1509124c, aa611ce1 |
| Money conversion + Money panel UI | Backend/UI | CRB | done | 67490ac, 59d5bc0a |
| Equipment purchase atomic money coupling | Backend | CRB | done | 29e67515 |
| Skill allocation persistence + SkillAllocationDialog wiring | Backend/UI | CRB | done | e0a0bda4, 75200fcb |
| Level-up HP + choices persistence + LevelUpDialog wiring | Backend/UI | CRB | done | 7694b227, e8e45976 |
| Bio schema + persistence command + Bio editor wiring | Backend/UI | - | done | 0ab784df, 94a3865 |
| Durability calc (max/current/temp HP, dying/unconscious/death thresholds) + Tauri commands | Backend | CRB | done | 0aeed25 |
| Durability/HP tab (Defense tab damage/heal UI) | UI | CRB | done | 75b083bd |
| Carry-capacity + encumbrance calculation | Backend | CRB | done | d475097 |
| Multiclass BAB/save widening (Fighter/Wizard/Rogue) | Backend | CRB | done | d20a5b9 + 8d814e8 |
| Multiclass classSummary parsing bug (comma vs slash separator, silent corruption) | Backend/UI | CRB | done | d03bc89d |
| Skill-allocation Wizard/Rogue class-skill grounding + cross-class rank-cap enforcement | Backend | CRB | done | 21f815c1 |
| characterProgression.ts test coverage (was zero, including for the classSummary bug) | UI | - | done | ca25bfef |
| LevelUpDialog feat-pick at feat-gaining levels (was silently absent) | UI | CRB | done | ddfc66bb |
| PCGen parity comparator: new combat.base_attack_bonus dimension | Backend | CRB | done | cda3bf1c + b8eff433 |
| Frontend test-coverage backlog (skillsModel, characterBio, spell-routing extraction, abilityScoreMethods, portraitImageProcessing) | UI | - | done | f905d656 |
| Item 25 follow-up: test-coverage self-audit of every boundary/*.ts file this swarm touched | UI | - | done | 0c602c72, lead-verified 77/77 + typecheck clean; 8 files closed (addFeatSelection/listFeats/loadCreateCharacter/loadSavedCharacterDetail/purchaseEquipment/recordAndPrepareSpellSelection/characterDurability/characterMoney) |
| Load-Character list stale cached row after a sheet mutation | UI | - | done | 2b13a23c |
| ItemPickerModal duplicate-catalog-key React reconciliation bug | UI | - | done | e50d7762 |
| Comprehensive live-UI smoke test across all 3 working classes | UI | - | done | e50d7762 (fix landed as part of this pass) |
| Frontend closure-readiness pass (alpha-bar UI check, doctrine sweep, lint/typecheck/build baseline) | UI | - | done | c014ffec, lead-verified 77/77 + typecheck clean; found+fixed one real pre-existing gap (WeaponsTab's dead Print button, risks item 30) |
| driver.sh cross-agent GUI display/state-file collision fix | QA/Backend (infra) | - | done | f6fe0df2 |
| Interim four-check wired-integration audit (doctrine compliance) | QA | - | done | c3b5fba8, lead re-verified f3676470 |
| Backend closure-readiness pass (full workspace test+build, DTO-consumption re-check, formal 4-check doctrine audit against full develop...tranche/6 diff) | Backend | - | done | no commit needed (genuinely clean); 4271/4271 root-crate tests, 204/204 desktop, 0 doctrine-audit hits; lead independently re-ran the doctrine grep checks (0 hits, matches) and the full workspace test suite |
| Starting wealth by class (operator-provided table, 11 CRB classes) | Backend | CRB | done | 0dbf67ad |
| Feat-effects engine, Toughness +3 HP slice (first real feat mechanical effect) | Backend | CRB | done | 53ddd1ce |
| Item 1 architecture-wall design/scoping pass (headless/corpus-aware split) | Backend | - | done | 4dadad51 (`item-1-architecture-wall-design.md`) |
| Item 1 shape (c): corpus-derived AC/ACP display section (non-claim-gated) | Backend | CRB | done | 08a829a1, verified 236/236 lib + 202/202 desktop + 4253/0 full workspace |
| Item 1 attack-bonus, bounded single-weapon slice (no schema change, honest-absent for 0/2+ weapons) | Backend | CRB | done | 845f860c, verified 242/242 lib + 202/202 desktop |
| Bundled desktop corpus fixture caps corpus_derived — backend half (surface unresolved_spell_ids/unresolved_equipment_item_ids instead of silent drop) | Backend | CRB | done | 647e52aa, verified 245/245 lib + 203/203 desktop |
| Render an honest "not shown — outside demo corpus" indicator for the new unresolved-selection lists | UI | CRB | done | 5406e335, live-verified against the exact original smoke-test finding |
| Wire shape (c)'s equipmentEffects (AC/ACP) into the frontend TS type and Defense tab | UI | CRB | done | d8528ce2, live-verified against a real Chain-Shirt-equipped Fighter and cross-checked on a Wizard |
| Defense tab doesn't refetch durability after a feat pick (render-staleness, same shape as corpus_derived bug) | UI | - | done | 7360fe4a, live-verified both feat-grant paths |
| Spells tab corpusDerived inconsistency for non-Human Wizards (found in smoke test) | Backend | CRB | done | root cause identified -- NOT a Wizard/race issue, see the bundled-corpus-fixture row above; superseded, not itself a separate task |
| Items 1+27 scoping plan (equipment-attachment schema + posture-gate widening) | Backend | CRB | done | 1f8dd2fa, `items-1-and-27-scoping.md`; operator resolved all 3 open questions (never block on unknown feat; replace the gate outright, not additive; skip the static category pre-filter) |
| Sub-task 1: `applied_modifiers` schema field on `EquipmentSelection` (32 real QA-owned test-fixture sites, not 42 -- backend's list overcounted; QA independently re-verified the real count) | Backend+QA | CRB | done | 797bd436 (backend's 25 non-QA sites) + eeaa876d (QA's 32 fixture sites); lead-verified 4280 total passed / 0 failed across 478 test blocks |
| Sub-task 2: per-weapon attack-bonus resolution via `applied_modifiers` (closes item 1's 2+-weapon gap) | Backend | CRB | done | 5b0c7243, lead-verified 263/263 lib + 204/204 desktop; real deliberate behavior change: equipmods now require explicit attachment, no more auto-attach-to-only-weapon |
| Sub-task 3: static category pre-filter data-authoring pass | - | CRB | skipped | operator decided 2026-07-24: redundant with sub-task 4, not doing it |
| Sub-task 4: corpus-aware combat-baseline + selected-skill pillars, armor/shield-only widening (item 27) | Backend | CRB | done | 6ef7f3ef, lead-verified 4292/4292 + 211/211; not yet wired to any command (sub-task 5) |
| Sub-task 5: command-layer wiring (create_character/level_up_character/etc.) | Backend | CRB | done | 350e6566, lead-verified 4292/4292 + 211/211; real regression found and fixed during re-verification (an over-strict "every equipped item must resolve" requirement broke tests with a harmless extra item) before landing, not after |
| Sub-task 6: frontend equipment-modifier-attachment UI | Backend+UI | CRB | done | 3a05539a (backend) + a768ad85 (frontend); lead-verified 4284/4284 lib + 211/211 desktop + 78/78 frontend + typecheck clean; live-verified against a real character |
| Feat-effects engine widened to 3 more feats (Great Fortitude/Iron Will/Lightning Reflexes) | Backend | CRB | done | f38e9f33, verified 254/254 lib + 203/203 desktop |
| Wizard non-Human spell-math (spell-save-DC + spellbook-ceiling absent for non-Human) | Backend | CRB | done | 7c2d2ad2, lead-verified 4275/4275 + 204/204; QA independently re-verified with a full function trace (258/258, 204/204) |
| Live verification of the EquipmentEffectsDto null-serialization fix against real 0-weapon and 2-weapon builds | QA | CRB | done | wire-level test + cross-referenced backend tests, 204/204; live-UI leg environment-blocked (recurring libEGL/DRI3, risks item 29) and structurally unreachable for the 0-weapon case (risks item 28) |
| Feat-effects widening (Great Fortitude/Iron Will/Lightning Reflexes) independent re-verification | QA | CRB | done | f38e9f33, clean, item 17 fully closed |
| Full class/race chassis breadth, CRB+APG+ACG (Bestiary has no classes) -- **MILESTONE: 11 of 27 classes genuinely reach Computed via the compute engine** (Arcanist, ACG, is the first non-CRB class ever to do so this segment), the original 10 CRB/product-reachable classes remain reachable through the real product UI (product-reachability gap fully closed, see below; Arcanist's own backend command path is now Computed-reachable too, `772298bc`+`25c5bcae`, and a real save-layer colon-convention bug frontend found via live-testing is now fixed and regression-tested -- frontend clear to re-add the `CLASS_OPTIONS` entry), Monk has 6 of 7 restricted-list feats closed (only Deflect Arrows remains), Skald/Bloodrager/Brawler/Hunter (ACG) plus Cavalier/Alchemist/Inquisitor (APG) each have one or more real abilities grounded but stay Blocked on other deferred features, Warpriest/Slayer/Oracle/Swashbuckler/Investigator (ACG/ACG/APG/ACG/ACG) each have a genuine FULL non-CRB build (real spellcasting-or-equivalent + a class-specific resource/feature + skills) and stay Blocked (Oracle permanently, proven via a dedicated test), only Shaman/Summoner/Witch remain fully untouched beyond chassis dispatch (Shaman/Witch already scoped and greenlit, Summoner held) | Backend | CRB/APG/ACG | in progress | All 11 CRB classes now have at least some real engine work landed, 10 of them fully Computed. Eight APG/ACG class-specific closures landed: Skald's Inspired Rage (`d2eb0798`) plus real spontaneous spellcasting (`7e7f6fbd`, reusing Bard's spell list and pure lookup functions directly -- byte-identical tables, confirmed NOT enough alone to reach full Computed since Skald lacks Bard's ~20-feature prior investment), Bloodrager's Bloodrage (`15560e62`), Brawler's AC Bonus (`19c792e1`, first always-on/no-gate shape), Hunter's Animal Companion (`21c5b13a`, first closure reusing an earlier closure's own code), Cavalier's Mount (`d256bc9c`, first-ever APG class-specific closure, cross-book proof the pattern generalizes), Alchemist's Mutagen (`101bf40d`, first closure combining choice-recognition and activation-gating). Monk's remaining-3-feats closure (`e45b622f`) landed after a full adversarial review found Improved Grapple could reuse an existing precedent (Dwarf Stability's CMD-bonus idiom) rather than needing a brand-new CMB/CMD pillar. All lead-verified against committed HEAD, zero workspace regressions. The cheap-win scan across all 16 APG/ACG classes is complete: 7 closed (6 original + Inquisitor's Judgment, `22abe6e5`, narrowed to Justice-only via a legitimate re-narrowing of an earlier "not cheap" verdict -- lead caught and backend fixed a real pre-commit bug where the over-budget check was missing from a second, separate helper function), 9 explicitly ruled not-cheap. **MILESTONE: product-reachability gap fully closed** -- backend's Path A (`9bafe303`, mirroring the already-shipped Wizard silent-canonical-default pattern, ~30-45 lines, no wire-contract change) plus frontend's `characterHubModel.ts` update (`adf57cfb`, real per-class levelOptions live-verified through the dev build) mean all 10 Computed classes are now reachable through the real product for the first time. Full writeup in risks-and-open-questions.md item 8. Path B (a real interactive picker) deliberately deferred as future work. **Arcanist's full ACG build (`ce73a598`) landed as an 8th closure and the first genuine full-scope (not single-ability) build outside CRB** -- real independently-verified prepared spellcasting (a genuinely different table from Wizard's own, caught and corrected mid-scoping before assuming byte-identity), Arcane Reservoir, and class-skill wiring all real; stays honestly Blocked on `exploits_deferred` alone even with a valid spellbook, confirmed via the actual `HeadlessReceiptStatus::Blocked` test. Backend attempted the Wizard-style `compose_character_input` spell-seed follow-on and correctly caught + reverted it before committing: unlike Wizard's own genuinely-conditional remaining diagnostic, Arcanist's `exploits_deferred` is unconditionally claim-blocking, so seeding a spell has zero product-visible effect -- reaching `Computed` needs Exploits itself addressed, not just a seed. Lead re-verified against committed HEAD: 443/443 lib, coverage audit 3/3, all 8 Arcanist tests individually. **Warpriest's full ACG build (`03bb35d3`) landed as a 9th closure**, the second genuine full-scope build outside CRB -- real independently-verified spellcasting (matches Cleric exactly at levels 1-2, genuinely diverges from level 3 despite sharing `SPELLLIST:1|Cleric`), Blessings, Sacred Weapon, and Destruction Blessing's Destructive Attacks all real; also caught and fixed a genuine class-skill-bonus widening bug (`selected_skill_class_skill_bonus_applies` was missing Warpriest despite its corpus list including Climb/Intimidate/Swim -- mirror image of a historical Wizard false-positive). Lead re-verified against committed HEAD: 451/451 lib, coverage audit 3/3 ("six" closures), all 8 Warpriest tests individually, skill-bonus regression tests clean. **Slayer's full ACG build (`77910256`) landed as a 10th closure**, the third genuine full-scope build outside CRB and the first with zero spellcasting scope (confirmed non-caster) -- four real, independently-verified flat formulas (Sneak Attack dice, Trap Sense, Trapfinding, Track), honestly counted as `named_features_wired = 4` (not folded down like Arcanist/Warpriest's 2, since none of the four share an underlying mechanism -- a real, deliberate methodology distinction, not an inconsistency), grounded as standalone records with no total-integration, mirroring Barbarian's/Rogue's own Trap Sense precedent; also caught a third instance of the class-skill-bonus widening bug (Climb/Intimidate/Swim). A second comparative-scoping doc across the 7 then-remaining untouched classes also caught and corrected a real error in the earlier comparison: Shaman's Spirit Animal is a Familiar, not an Animal Companion, making Shaman genuinely pricier than first estimated. Lead re-verified against committed HEAD: 457/457 lib, coverage audit 3/3 ("seven" closures), all 6 Slayer tests individually. **MILESTONE: Arcanist's Metamagic Knowledge Exploit closure (`54d8048b`) landed as a follow-on to its own full build, reaching genuine `Computed` for the first time among all ACG/APG classes** -- the second full-build comparison across the 6 then-remaining untouched classes came up empty (every one has a real, concrete complexity blocker), so backend pivoted to Arcanist's own permanent Exploits blocker instead. Real finding: of Arcanist's 46 Exploits, Metamagic Knowledge is genuinely a one-time bonus-feat grant (no Reservoir cost, unlike the other 45), validated via an already-built, unrelated SD-20 Epic 3 module (`feat_prereqs::metamagic`) -- genuine reuse, not new logic. Recognizing it narrows `exploits_deferred` enough to clear the last claim-blocking diagnostic. Lead independently verified every claim before greenlighting (the 46-count, the reservoir-cost contrast on 4 sample Exploits, the module's real bounded scope, the exact two-diagnostic dispatch, Empower Spell's real zero-prerequisite corpus record) given the milestone size, then re-ran the actual milestone test against the real committed HEAD: `HeadlessReceiptStatus::Computed` with zero claim-blocking diagnostics, confirmed for real. All 14 Arcanist tests, coverage audit 3/3 (unchanged at "seven" closures -- Metamagic Knowledge is a sub-list entry under the already-counted Exploits slot, same non-bump reasoning as Destruction Blessing under Warpriest's Blessings), full lib 463/463. Explicitly named as engine-Computed only, not yet product-reachable (no UI picker for `choice:arcanist_metamagic_knowledge`, the same Path A gap Sorcerer/Cleric/Druid/Wizard needed closed) -- a separate follow-on, not conflated with this milestone. **Backend closed the backend half of that follow-on the same day (`772298bc`, Path A)**: `compose_character_input`/`apply_level_up` now seed both the starter spell and the Metamagic Knowledge choice (Arcanist needed both sites, like Wizard, unlike Sorcerer/Cleric/Druid's single-site fix); new milestone test `arcanist_level1_reaches_computed_from_compose_character_input_alone` proves a freshly composed Arcanist reaches `Computed` with zero caller-added anything. Lead re-verified against the real committed HEAD: both new tests, full desktop suite 214/214. **But the lead independently checked the actual `CreateCharacterForm` before accepting "product-reachable" and found a second, still-open gap**: unlike Sorcerer/Cleric/Druid (already listed in `characterHubModel.ts`'s `CLASS_OPTIONS` as `headless-only` before their own Path A), Arcanist has ZERO entry in `CLASS_OPTIONS` -- the real Create Character form's class dropdown is a plain `<select>` populated only from that array, no free-text option, so a real user still cannot select Arcanist in the shipped app today. Closing this needs a `characterHubModel.ts` update adding an Arcanist entry, mirroring `adf57cfb`'s own update for the other three. **Frontend's own attempt to close this live-tested first (same rigor as Sorcerer/Cleric/Druid) and found a real, load-bearing bug instead of a missing UI entry**: a fresh Human Arcanist 1 hit a raw save-time error, not a graceful `Blocked` diagnostic -- `EMPOWER_SPELL_METAMAGIC_SELECTION = "Empower Spell"` has zero colons, but `local_store.rs`'s save-layer validation requires 2+ colon-segments per `selection_id` to round-trip through its fixture grammar. The same gap category as item 23's earlier finding, recurring in a new, load-bearing spot. Lead independently confirmed the bug was real, then caught a flaw in its own first-pass fix suggestion (a naive rename would break the real catalog lookup and silently regress Arcanist back to `Blocked`) before backend acted on it. **Backend landed the real fix in `25c5bcae`, together with Oracle's own full APG closure**: `arcanist_metamagic_knowledge_feat_name` is a genuine, generic `metamagic:<slug>` -> real-feat-name translation (works for any real CRB Metamagic feat, not just Empower Spell, verified against all 9 real CRB Metamagic feats' own "Word Spell" shape), applied before the catalog lookup; a new end-to-end regression test (`create_character_at_root_saves_a_fresh_arcanist_without_a_colon_convention_error`) exercises the real save command and was verified to actually catch the original bug (reverted, confirmed red, restored, confirmed green). **Oracle's own full APG build landed as the 8th ACG/APG closure and the first genuine full build in APG**: real spontaneous known-spell casting (`oracle_spells_known_table`/`unmet_oracle_known_spell_conditions`/`ground_oracle_known_spells`, mirroring Sorcerer's own spontaneous shape since Oracle is `MEMORIZE:NO` unlike Cleric/Wizard/Arcanist/Warpriest's prepared shape, reusing Cleric's own spell-list content for the shared `SPELLLIST:2|Cleric|Oracle` portion), Mystery (Life Mystery's Healing Hands, flat `+4 Heal`) and Curse (Clouded Vision, flat 30-ft vision cap) both narrowed the same three-branch way Warpriest's Blessing was. Unlike Arcanist, Oracle stays permanently Blocked on `other_features_deferred` -- proven via a dedicated test asserting Blocked even with Mystery+Curse+known-spell all recognized, not assumed. `named_features_wired = 2` (Mystery + Curse slots). Lead independently verified everything against the real committed HEAD before accepting either piece: the translation function's logic and its correctness against all 9 real CRB Metamagic feats, the real save-path regression test (ran it directly, confirmed it exercises `create_character_at_root` and asserts `Saved`), all 15 Arcanist tests, all 14 Oracle-related tests (including the permanently-Blocked proof), both SD-24 coverage audits, full lib 474/474, full desktop suite 215/215. Frontend is now clear to re-attempt the `CLASS_OPTIONS` addition. **Swashbuckler's full ACG build (`d267fe79`) landed as a 9th closure**, a non-caster (zero spellcasting scope, same cheap structural shape as Slayer) -- Panache (flat `max(1,CHA)` pool), Charmed Life (activation-gated save bonus, granted starting 2nd level, `((level-2)/4)+3` uses/day), and Nimble (flat AC dodge bonus) all real. Two honest evidentiary findings, named rather than glossed over: Panache's own base-class corpus record carries no literal `BONUS:VAR` token at all (DESC-text-sourced, cross-validated against the Inspired Blade archetype's own formula which adds an INT term on top of the same base -- confirming the base really is `max(1,CHA)`, a different, weaker evidentiary path than every other formula this segment has grounded); Charmed Life's real grant level (2nd, not 1st) confirmed via web search, resolving the lead's own flagged open question about negative-operand truncation by making it moot (the feature doesn't exist below level 2, so the formula is never evaluated there) -- the lead independently re-confirmed this same grant level directly in the corpus's own per-level feature-grant table (`acg_classes.lst`'s `2	ABILITY:...Charmed Life` row). Fourth instance of the class-skill-bonus widening fix. `named_features_wired = 3` (Panache/Charmed Life/Nimble). Lead re-verified against the real committed HEAD: 8 Swashbuckler tests, coverage audit 3/3 ("eight" closures), full lib 482/482, desktop 215/215 (unchanged), full workspace sweep clean. **Investigator's full ACG build (`46eb6c3d`) landed as a 10th closure via the scout-scoped pipeline** -- prepared-caster shape (Wizard/Arcanist's simpler shape, not Oracle's spontaneous one) but spellcasting deliberately deferred (reuses the unbuilt Alchemist formula list, confirmed zero such list exists, 104 real raw records as source); Trapfinding/Trap Sense/Inspiration pool-size all real, the swapped-floors hazard scout flagged correctly avoided (verified independently against Investigator's own corpus record, not copied from Slayer). Investigator forced the first genuine architectural change to a shared function this segment: its 2-of-3 partial class-skill match (Climb+Intimidate yes, Swim no) required splitting `selected_skill_class_skill_bonus_applies` into three independent per-skill functions -- confirmed behavior-preserving for all 6 prior classes via the full regression suite. Backend caught and fixed a real regression in an existing QA-owned test before shipping (stale assertion text from the old shared-scalar phrasing). Lead re-verified against the real committed HEAD: 488/488 lib, coverage audit 3/3 ("nine" closures), all 6 Investigator tests, the 9-test skill-bonus regression suite, desktop 215/215 unchanged. **Scout's re-scoping of the last 3 untouched classes corrected the standing "all blocked on a subsystem" verdict**: Shaman (Life Spirit's Healer's Touch, a direct Oracle twin) and Witch (Ward hex, flat self-scoped) both have real MVPs independent of their Familiar, greenlit as the next two targets; Summoner remains genuinely subsystem-blocked (Eidolon), held. Lead caught one factual error in that doc (Animal Companion code does exist and is reused by Druid/Hunter; Shaman's Spirit Animal stays deferred because it's a Familiar specifically, a different subsystem) and one in scout's follow-on Eidolon-sizing background research (104 real Evolution records, not 353 -- a broad grep pattern had swept in 102 parallel Temp Evolution records). **Scout also found a real, active, shipped bug via a systematic formula-audit of already-landed closures**: Warpriest's Sacred Weapon dice-count formula uses the wrong sibling PREBASESIZE branch's divisor (`/20` instead of the Medium/Longsword branch's `/15`), giving the wrong dice count at levels 15-19 -- low-moderate severity (a flat informational record, not integrated into any total), escalated to backend, not yet fixed. Only Deflect Arrows, Warpriest's/Slayer's/Oracle's/Swashbuckler's/Investigator's own remaining-feature blockers, the Sacred Weapon fix, and Shaman/Summoner/Witch (3 classes, not 4) remain in this bucket. Detailed per-class/race table maintained in SWARM_REPORT.md |
| Starting wealth recognition for the 10 non-CRB classes (real count confirmed against the live d20pfsrd source, "12" was a prose typo) | Backend | APG/ACG | done | 4576fe5f, lead-verified 276/276 + 212/212; wealth-only recognition, NOT a substitute for the real chassis work these classes still need (see the full-breadth row above) |
| EquipmentEffectsDto's 3 optional fields serialize None as literal `null`, not an omitted key -- renders as garbled "+null"/"null%" for the common case (0 or 2+ weapons) | Backend | CRB | done | 874df6db, lead-verified 204/204 desktop; frontend needed no change (TS type was already `?:`, not `\| null`) |
