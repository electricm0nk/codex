v0.6 Alpha Release Swarm — Status
Branch: tranche/6 (from origin/develop @ 5b1bad5)
Source of truth: docs/release/v0.6/release-swarm.md
Update model: (a)-(c) below are hand-written by the lead after every real
event (task completion, blocker, correction) -- this is the narrative
record. The "Auto Heartbeat" block at the very end of this file is a
separate, mechanical, cron-driven refresh (every 5 min, via
~/workspace/swarm-status-heartbeat.py) that proves liveness and flags
staleness even if the lead goes quiet; it never touches anything above it,
and the lead never hand-edits it.

Agent Status convention (operator directive, 2026-07-24): every write to
this file includes a per-agent status line using one of: working (doing
X), idle (queue clear, nothing assigned), waiting-on-<agent> (blocked on
another agent's in-flight work), paused (quota/outage, with reset time
if known). This replaces the looser free-text "Happening now" prose below
where the two overlap -- (a) still carries the narrative detail, this
block is the at-a-glance state.

**SECURITY CONCERN RAISED THEN RESOLVED, 2026-07-24 (lead).** A teammate
message arrived claiming `teammate_id="operator"`, directing the lead to
author `SWARM_TASKS.md` and citing an uncommitted `§8.4.1` section of
`release-swarm.md` as authority. The lead correctly treated this as
suspicious given the process (an unrecognized identity on the teammate
channel, backed by an uncommitted edit to a trusted doc) and reverted the
addition without complying, then flagged it plainly to the operator
rather than deciding unilaterally. **Resolved, not an attack**: the
operator confirmed directly (through the real user channel) that this
was legitimate — they're working with a separate collaborator agent on
the `/swarm/dashboard.html` tooling, and that agent authored both the
`§8.4.1` section and the "operator" teammate message on the operator's
behalf. The caution was the right call given the information available
at the time (an unattributed identity plus a tampered-looking doc edit
is a real red-flag pattern), and surfacing rather than silently
complying or silently refusing was the correct way to resolve genuine
ambiguity about who's asking. Net effect: `SWARM_TASKS.md` ownership is
now confirmed as a real, standing lead responsibility (see below); the
reverted `§8.4.1` section may need re-adding by the operator's
collaborator agent since the lead's revert discarded it (asked the
operator whether to restore it or leave it to them).

**`SWARM_TASKS.md` rebuilt and reconciled (lead, `ff705d93`).** The prior
seed content didn't match this swarm's real work (fabricated-looking
"Epic"-numbered rows) — rather than stamp real commit hashes onto false
task descriptions (which would itself be dishonest reporting regardless
of the request's legitimacy), rebuilt the table from scratch against the
swarm's real tracking record (`risks-and-open-questions.md` items 1-27,
this file's own Happened log, `git log`). ~40 rows, real commit hashes,
honest `done`/`in progress`/`queued`/`blocked` statuses including the
genuinely-blocked architecture items. Lead authoring commitment adopted
going forward: update this file on every meaningful status flip,
alongside the existing Agent Status convention.

- Frontend's comprehensive smoke test complete. Live-drove all 3 working
  classes (Fighter/Human, Fighter/Dwarf, Wizard/Elf, Rogue/Half-Elf) end
  to end -- creation, level-up (including the item-23 feat-pick path),
  every populated tab, Add Weapon/Add Spell/Add Feat, skill allocation,
  HP damage/heal. **One real bug found and fixed (e50d7762)**:
  ItemPickerModal.tsx used a bare `entry.key` as its React key; the real
  feat corpus has a genuine duplicate ("Combat Expertise" appears twice,
  same key, differing internal effect formula), which confused React's
  reconciler across re-filters -- searching showed phantom/stale rows
  while the count text stayed correct. Fixed with a compound key
  (`${entry.key}::${index}`), live-verified. The underlying grant flow
  was never actually broken (Toughness persisted and showed correctly
  even while the picker rendering glitched). **One real, unresolved
  finding, not fixed, needs backend investigation**: the Spells tab
  renders from `corpusDerived`, not `spellsSelected`, and doesn't
  reliably reflect what's actually persisted for non-Human Wizards --
  a brand-new Elf Wizard showed "No corpus-reachable spells" despite 3
  real, disk-confirmed spells surviving a full reload; a different,
  older Elf Wizard showed 1-of-3. Not a clean "never works" pattern --
  inconsistent. May be the same root cause as the already-tracked item C
  /item 18 (Wizard non-Human spell math absence) surfacing through
  `corpusDerived`, or a separate, narrower issue -- frontend correctly
  didn't guess at the backend mechanism (outside their ownership) and
  flagged with exact repro steps instead. Everything else: clean pass,
  no other correctness issues. 69/69 suite green throughout.

- **SIGNIFICANT FINDING (QA, 2026-07-24)**: root-caused the Spells tab
  inconsistency, and it's bigger than the Spells tab. Traced to
  `corpus_fixture_bundle()` (`apps/desktop/src-tauri/src/
  corpus_fixtures.rs`) — the desktop app's corpus-aware layer
  (`compute_pilot_with_corpus`) is bundled with a deliberately tiny,
  hardcoded 2-spell/2-equipment-record fixture (the doc comment itself
  says "not a general corpus provider, exhaustive corpus coverage is out
  of scope"). Any spell/item a user actually picks that isn't one of
  those exact 2 bundled records per category silently drops out of
  `corpus_derived` (school_coverage, equipped_items grounding, and now
  shape (c)'s `equipment_effects` too) with zero diagnostic -- the
  underlying data (`spells_selected`/`equipment_selections`) persists
  correctly the whole time, confirmed by reading the saved envelope
  directly. **Not the same root cause as item 18** (which is a
  race-gated chassis-explanation path, unrelated) and **not
  race-specific at all** -- a Human Wizard picking the same spells would
  show the identical gap. Empirically reproduced and reverted cleanly
  (touched only `pf1_adapter.rs`, confirmed via `git status` and a clean
  `cargo build`, never went near backend's in-progress files). **Real
  scope implication for shape (c) specifically**: its ACP/AC value only
  looks complete today because the fixed-loadout's Chain Shirt/Longsword
  happen to be exactly the 2 bundled equipment records -- any other gear
  would silently show nothing, the same gap. Two fix shapes offered, not
  QA's call: bundle more real spells/equipment (bounded, mechanical) vs.
  surface an honest "not in demo corpus" indicator instead of silent
  omission (small UX fix). Queued for backend once the attack-bonus
  slice lands.

- **Defense-tab feat-add render-staleness fixed (frontend, `7360fe4a`).**
  New shared `refreshDurability()` helper called after a successful feat
  grant on both paths (`handleAddFeat`, `handleLevelUpFeatPick`) -- falls
  back to `null` on failure, same honest treatment as the initial load,
  never stale or fabricated. Live-verified both paths on a real character
  with the Defense tab already open: HP moved live (22/22 -> 25/25 via
  the Feats-tab picker, then 31 -> mid-transition via the level-up feat
  pick), no reselect needed. Also re-confirmed the earlier ItemPickerModal
  compound-key fix is holding up under continued use. 69/69 suite green,
  typecheck clean, no new test file (pure data-refresh wiring, no new
  pure logic). Frontend's queue is clear again.

- **"Outside demo corpus" indicator landed (frontend, `5406e335`).** New
  `UnresolvedNotice` component rendered in both SpellsTab and GearTab
  whenever backend's new `unresolvedSpellIds`/`unresolvedEquipmentItemIds`
  is non-empty -- minimal by design, just count + raw ids, no fabricated
  detail. Live-verified against the exact character from frontend's
  original smoke-test finding ("Smoke Wizard Elf"): Spells tab now reads
  "3 spells not shown (outside demo corpus)" instead of the misleading
  "No corpus-reachable spells selected yet," confirming backend's
  root-cause call was right all along -- never a Wizard/race issue.
  **Real gap flagged, not assumed covered**: shape (c)'s `equipmentEffects`
  field (AC/ACP) is also on the wire now but isn't wired into the frontend
  TS type at all yet -- frontend correctly didn't assume it was out of
  scope for this task and flagged it explicitly rather than silently
  leaving it uncovered. 69/69 suite green, typecheck clean.

## Agent Status (2026-07-24, ~13:50 ET)
| Agent | Status | Detail |
|---|---|---|
- **"Outside demo corpus" indicator independently verified, clean (QA,
  2026-07-24).** Confirmed both `SpellsTab` and `GearTab` are genuinely,
  independently wired (not one path reused), the minimal-detail claim
  holds exactly (literally count + raw joined ids, nothing else), and
  backend's field-population diff replaces the old silent `continue` with
  a real push in both resolve loops. Reproduced RED themselves (reverted
  the push, watched the exact `[]` vs `["Magic Missile"]` mismatch,
  restored). One thing noted, correctly not flagged as a bug: the list is
  deliberately not deduplicated (a spell picked Known+Prepared shows up
  twice) -- consistent with the "never hide information" discipline, not
  a defect. Ran targeted tests only (not a full workspace regression),
  explicitly to stay out of backend's concurrent edit -- good judgment.

- **Shape (c)'s equipmentEffects wired into the frontend (frontend,
  `d8528ce2`).** New "Equipment Effects" stat-tile row on the Defense
  tab, above the existing DR line -- same honest-absence discipline as
  the rest of the tab: `armorClassDelta`/`armorCheckPenaltyTotal` always
  render (a real 0 isn't treated as absent); `maxDexCap`/
  `spellFailureChance`/`attackBonusDelta` only render when genuinely
  present, with particular care taken to preserve `attackBonusDelta`'s
  documented `Some(0)`-vs-absent distinction (exactly one weapon with no
  enhancement is a real "+0"; zero or 2+ weapons stays honestly hidden).
  Live-verified against a real Chain-Shirt-equipped Fighter (AC+4/ACP-2/
  MaxDex4/SpellFail20%/AttackBonus+0, all matching real PF1 Chain Shirt
  stats) and cross-checked identical values show up for a Wizard too --
  correctly identified as the known pre-existing fixed-loadout
  simplification, not something newly introduced. 69/69 suite green,
  typecheck clean.

- **REAL BUG FOUND (QA, 2026-07-24) — `EquipmentEffectsDto`'s three
  optional fields never actually hide on absence, they render garbled
  "+null"/"null%" instead.** `maxDexCap`/`spellFailureChance`/
  `attackBonusDelta` lack `#[serde(skip_serializing_if =
  "Option::is_none")]` (unlike the precedented `damage_reduction` field
  in the same file, which has it), so a Rust `None` serializes as
  `"attackBonusDelta":null` -- key present, literal `null` -- not an
  omitted key. The frontend's `!== undefined` hide-checks are dead code
  for this case (`null !== undefined` is `true`), so the hide-branch
  never fires; confirmed the actual broken render strings directly:
  `fmt(null)` -> `"+null"`, `` `${null}%` `` -> `"null%"`. **Confirmed
  three independent ways**, not just reasoned about: a throwaway Rust
  serialization test (reverted after, `character_hub.rs` confirmed
  clean again), tracing `loadSavedCharacterDetail.ts`'s zero-transform
  raw `invoke()` call, and running the actual JS comparison/template
  logic in Node. **Why the earlier live-verification missed it**: the
  one character tested (Chain Shirt + exactly one weapon) happens to
  resolve `Some(...)` for all three fields, so the None/hide path was
  never exercised -- any character with zero or 2+ weapons (a fresh
  character, a Wizard, a dual-wielder -- the common case) would show
  "Attack Bonus: +null" instead of nothing. **Fix is small, mechanical,
  and precedented**: add the same `skip_serializing_if` attribute
  `damage_reduction` already carries. Not fixed -- backend's file,
  currently mid-feat-widening; QA correctly didn't interrupt, flagged
  for whoever picks it up next. Typecheck clean, 69/69 still pass (no
  existing test exercises the None path, exactly how it slipped
  through).

## Agent Status (2026-07-24, ~14:18 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | working | feat-effects widening landed and verified (f38e9f33, 254/254 lib + 203/203 desktop); already starting the null-serialization bug fix |
| frontend | idle | queue clear |
| qa | working | dispatched to verify the feat-effects widening |
| qa | idle | verified the outside-demo-corpus indicator clean (both tabs, RED reproduction); standing by for backend's or frontend's next landing |

(a) Happening now (refreshed 2026-07-24, ~06:40 ET, resumed after operator pause)
------------------------------------------
orchestrator (lead)  Sonnet   RESUMED after an operator-directed pause
                                (swarm was paused mid-cycle over a
                                stuck-in-a-loop concern; root cause found
                                and fixed -- a CronCreate job was still
                                firing the 20-min check-in prompt even
                                after the lead stopped issuing new
                                ScheduleWakeup calls; deleted). Resuming
                                with ScheduleWakeup only this time (single
                                mechanism, explicit stop between cycles),
                                not CronCreate, to avoid the same failure
                                mode recurring. Operator also provided:
                                the starting-wealth-by-class table
                                (item 7, cited to d20pfsrd.com) and
                                explicit go-aheads to un-defer items 17
                                (feat-effects engine) and 1 (the
                                architecture-wall math) onto the
                                actionable backlog.
frontend               Sonnet  re-engaging now; last known state was a
                                comprehensive live-UI smoke test,
                                interrupted by a quota limit before the
                                pause (resets 9:10am ET) -- may still be
                                unavailable, will check.
backend                Sonnet  re-engaging now with the 3-item queue the
                                operator just added (7, 17, 1, in that
                                order)
qa                     Sonnet  re-engaging now; will hold for something
                                real to verify once backend/frontend land
                                work, per standing practice

(b) Happened
------------
- tranche/6 cut from origin/develop @ 5b1bad5, docs committed (43f8d46), pushed.
- Launch-readiness review fixed 7 blocker-class defects in release-swarm.md
  (stack mismatch, wrong ownership paths, merge authority, version-bump
  ownership, scope sizing, observer status, doctrine contradiction).
- Wired-integration ceremony waiver recorded as operator override in
  docs/release/v0.6/risks-and-open-questions.md.
- Swarm launched (frontend/backend/qa spawned).
- Routing correction: lead must be reached via SendMessage "main", not
  "orchestrator" (wrong name in initial teammate briefs) -- corrected with
  all three teammates.
- Frontend investigation corrected two wave-1 task assumptions:
  * DetailsPanel already renders unconditionally in CharacterSheet.tsx (not
    an unwired-component gap). Real gap: bio fields are session-local
    useState, zero backend persistence, no bio field in ChosenCharacterState
    or character_input.rs. Tab-switch "Details"/"Bio" entries are dead
    duplicates resolving to a generic coming-soon placeholder -- removing
    them is itself a real fix for the no-stub-surface bar item.
  * A 185-record CRB feat catalog already exists server-side
    (src/rules_core/rules_tables/crb/feats.rs) with zero Tauri exposure --
    feat-picker task reframed as "expose + consume", not build-from-scratch.
  * Money/currency has no schema field anywhere in the engine -- only
    per-item cost_gp pricing exists. Confirmed biggest lift of the three.
  Frontend sent backend a consolidated ask (bio schema+command, feat
  list+add-selection command, money field+command) directly.
- BLOCKER FILED: frontend reported all 5 wave-1 tasks gate on backend work
  that doesn't exist yet (verified against schema/corpus before escalating --
  correctly refused to build throwaway UI shells against nonexistent
  commands, per no-stub doctrine). Landed one real fix in the meantime:
  removed dead Details/Bio TABS entries (typecheck clean, commit pending).
  Lead response: reprioritized backend's queue to land all 5 frontend-
  blocking commands before calc-accuracy work (BAB/save, equipment AC),
  and assigned frontend a non-blocking audit of the 4 remaining stub tabs
  (Defense/Pets/Actions/Overrides) for anything already wireable today.
- QA closed wave-1 tasks 1/2/4: PCGen baseline clean (pcgen_runner_smoke
  2/2, sd26_pcgen_runner 6/6); gap-list survey found carry capacity,
  encumbrance, and money conversion have ZERO production implementation
  in src/rules_core (not test gaps -- missing calculations), corroborating
  frontend's money-schema finding; SWARM_REPORT.md skeleton drafted with
  full per-calculation coverage table (commit 9ffe32f).
- Lead ruling: "durability" (bar item 4) = character survivability display
  (max/current/temp HP, nonlethal, dying/death thresholds), distinct from
  "level-up hit points" (the level-up increment calc). No item-hardness
  system in scope. Recorded in risks-and-open-questions.md item 4.
- QA follow-up: the money/carry-capacity/encumbrance gap is TWO layers --
  rules_core has no calc, AND the PCGen comparator (pcgen_runner.rs,
  pcgen-normalize-output.py) doesn't extract those fields from PCGen's
  export either. QA correctly stopped short of speculative design work;
  folded into backend's wave-2 scope as its own subtask.
- Backend landed skill-point Tauri command work in progress; version bump
  commit (0c614d9) triggered buildLabelFixtureFreshness.test.ts (stale
  "Codex 0.5.98-test" literal). Frontend fixed it plus 4 more dependent
  tests backend's flagged list missed (shared makeSurface.ts fixture) --
  full suite 62/62 green, typecheck clean. Pushed as 743c358, which also
  wired the Actions tab for real (characterProgression.ts's existing
  class-features data, zero backend dependency).
- Frontend's stub-tab audit (Defense/Pets/Actions/Overrides) closed:
  Actions now real. Defense's DR data exists server-side (pilot_compute.rs
  explanations) but isn't exposed through any DTO -- added to backend
  backlog, non-blocking, not part of the alpha-bar calc list. Overrides
  has zero hits anywhere in the character-build engine -- parked, unclear
  purpose, not inventing scope for it. Pets (animal companions) confirmed
  a genuine pre-existing non-goal ("named-but-unproven" in pilot_compute.rs,
  no stat-block engine) -- see new open question below on alpha-bar fit.
- QA's follow-up survey: durability (per lead's ruling) has almost no
  production surface either -- only an isolated level-1 fighter HP value,
  no aggregate max_hp/current_hp/temp_hp/dying-death fields anywhere. Same
  shape of gap as carry-capacity/money -- wave 2 is now 4 calc gaps, not 3.
  QA also located a real local PCGen checkout at
  /home/ubuntu/workspace/repos/pcgen/system/gameModes/Pathfinder/load.lst
  and pulled carry-capacity/encumbrance tables directly from it (not
  reconstructed from memory); durability thresholds from SRD; money-
  conversion ratios standard but not PCGen-source-verified (flagged).
  Starting-wealth-by-class formula not found in the class LST file QA
  checked -- correctly flagged unresolved rather than guessed. Spec
  appendix landed in SWARM_REPORT.md (abec13b).
- NEW OPEN QUESTION filed (risks-and-open-questions.md item 5): does the
  missing Pets/animal-companion mechanic create a real gap against alpha
  bar item 2 ("any class... from the four primary books") for Druid/
  Ranger/Hunter/Cavalier? Not a literal blocker per the bar's text, but
  flagged for explicit operator sign-off rather than silently dropped.
- Backend closed wave-1 task 2: set_skill_allocations command (e0a0bda),
  real TDD (5 new tests incl. a golden-path proof it's a true replace not
  a no-op), verification output posted. IMPORTANT FINDING, folded into
  risks item 1: the compute engine's Computed path only accepts ONE exact
  hardcoded posture (Climb/Intimidate/Swim rank 1, chain shirt equipped,
  nothing else) -- any other tester choice returns Blocked with
  diagnostics. Pre-existing, not introduced by the swarm, but sharper than
  the known "Fighter 1-3 only" framing and likely more central to hitting
  the alpha bar than individual picker/persistence work. Told backend to
  finish the frontend-unblocking queue first, then bring a scoped proposal
  before starting wave-2 calc-accuracy work on the assumption that
  persistence alone unlocks real play.
- Backend closed task 3: level_up_character extended (not a new command --
  correct call, one atomic mutation beats sequential partial-apply calls)
  to accept additionalChoices + optional skillAllocations, backward
  compatible via serde defaults. Surfaced a real pre-existing wire-format
  constraint (choiceSetId/selectionId colon-count rules enforced by
  SavedCharacterStore) without expanding scope to relax it. 151/151 tests
  green. CAUGHT: backend then said "moving to task 4, multiclass BAB/save
  stacking" -- that's the ORIGINAL wave-1 numbering, not the reprioritized
  queue. Corrected: next is bio schema+command (reprioritized task 3 of 5),
  frontend is still fully blocked on bio/feat/money, nothing changed that.
- Frontend closed task 4 (75200fc): SkillAllocationDialog wired to real
  set_skill_allocations, LIVE-VERIFIED against a real dev build (not just
  typecheck) -- confirmed both the success path (rev.1->rev.2 persisted)
  and the honest-Blocked path (rejected mutation correctly never persisted,
  engine's real diagnostic surfaced in UI). Correctly excluded backend's
  in-flight pilot_compute.rs from the commit. Picked up task 5 next
  (LevelUpDialog wiring) on its own initiative -- correct, doesn't need
  bio/feat/money, real available work.
- Frontend closed task 5 (e8e4597): LevelUpDialog wired through the real
  extended level_up_character request. Live-verified: Fighter 1->2, HP
  12->20, BAB/saves recomputed live, on-disk revision bumped rev.2->rev.3,
  choice:level_2_hit_points:hp:average persisted verbatim. Two honest scope
  calls, both named rather than silently dropped: no feat-pick-at-level-up
  UI (no list_feats yet, would be a fake dropdown = stub doctrine
  violation), and skillAllocations left omitted from the level-up call
  since the separate SkillAllocationDialog (task 4) already covers it --
  avoids double-implementing the same concern in two places. Frontend has
  now closed tasks 4 and 5 of wave-1; 1-3 (bio/feat/money) remain blocked
  on backend, who is currently paused (see below).
- SECOND QUEUE DEVIATION (more serious than the first): found backend's
  uncommitted pilot_compute.rs diff (325 lines) is real multiclass/BAB-save
  chassis-widening work (Fighter/Wizard -> all 11 CRB classes), doc-commented
  "v0.6 alpha swarm task 4" -- i.e. wave-2 work, done WITHOUT the scoped
  proposal I explicitly asked for before any widening work, and contrary to
  the queue correction two messages prior. Paused backend, asked what
  happened (message-delivery gap vs. proceeding anyway) before repeating the
  correction a third time. Told them: commit if at a clean tested stopping
  point (don't waste real work), then bio schema+command, no further
  wave-2/widening work without checking with me first. Awaiting reply.
- Backend committed task 5 (d475097, equipment AC audit + real new
  encumbrance.rs carry-capacity/encumbrance calc, cited to Archives of
  Nethys) WITHOUT replying to the hard stop above and without waiting for
  the commit-serialization ack. Lead treated this as a third deviation,
  issued a full stop, and escalated to the operator rather than repeat a
  correction -- per the threshold already logged in risks item 6.
- RESOLUTION -- ROOT CAUSE WAS A DELIVERY BUG, NOT BACKEND: backend reported
  zero inbound SendMessage content had reached it all session; every one of
  its reports was execution of its ORIGINAL wave-1 spawn brief (task 4 =
  BAB/save stacking, task 5 = equipment AC/carry-capacity, in that exact
  order) with no visibility into the reprioritization, the pause, or the
  hard stop. All of the lead's messages arrived simultaneously in a single
  batch, well after task 5 was already committed. Backend correctly held at
  the LAST instruction visible to it ("wait for a message from me before
  doing anything at all") rather than self-authorizing off older content in
  the same batch that could have read as permission -- exemplary handling,
  not a discipline failure. Verified fixed with an explicit ack-the-literal-
  sentence test; confirmed received. Backend RESUMED with a clean current
  instruction set. The d475097 commit stands -- legitimate work under a
  legitimate brief. Frontend and QA showed no equivalent gap in the same
  window, so this looks scoped to backend's inbox/session specifically.
  Full narrative + assessment in risks-and-open-questions.md item 6
  (rewritten from "not stopping on instruction" to "delivery bug").
- QA independently reached the same "don't commit against undecided
  production code" conclusion on its own, before being told -- finished the
  40-test catalogue fix for backend's multiclass widening (0 failures, 467
  result lines, every value independently verified against real computation
  output, one real cosmetic defect found and correctly left for backend
  rather than fixed out-of-lane) and held the commit pending resolution.
  Cleared to proceed now that backend's status is resolved.
- Backend closed the last reprioritized task, money conversion (67490ac):
  real conversion math (money.rs, standard 1pp=10gp=100sp=1000cp, flagged
  not-PCGen-verified same as QA's spec note), money.json sidecar (mirrors
  bio pattern), track-and-spend only -- correctly left starting-wealth
  auto-roll open since QA flagged that PCGen formula unresolved rather
  than invent one. 176/176 lib tests, 170/170 desktop suite. This closed
  ALL 3 of frontend's blocking commands -- checkpoint reached, both
  teammates told to hold rather than self-select wave-2 scope. Lead
  reported a full checkpoint to the operator (tasks landed, remaining
  alpha-bar gap, the still-open posture-narrowness and Pets questions).
- Operator asked about SWARM_STATUS.md update automation; lead built a
  mechanical cron heartbeat (~/workspace/swarm-status-heartbeat.py, every
  5 min, see file header) as a liveness/staleness guarantee independent of
  the lead's own diligence, then resumed the swarm. During the pause,
  both teammates finished real work:
  * Backend: fixed the cosmetic "Fighter"-hardcoded-in-explanation-text
    defect QA found, via a proper class_summary_label(input) helper
    (dynamic, not a blind strip -- an existing test asserted the literal
    "Fighter" string for real Fighter input, which a strip would have
    broken). Committed d1905ed, full suite clean (176/176, 3961/3961,
    170/170).
  * Frontend: closed all 3 remaining wave-1 tasks. Bio editor (94a3865,
    live-verified with a real close-and-reopen disk round-trip). Feat
    picker (febf4d8, reused ItemPickerModal pattern, live-verified against
    the real 185-entry catalog, "Showing 185 of 185", added Cleave and
    confirmed on-disk persistence) -- honestly flagged that the tab can't
    show a character's *existing* full feat list since selected_feats
    isn't exposed via load_saved_character (new backend backlog item,
    same shape as the Defense/DR gap). Money panel (59d5bc0, live-verified
    both the success path -- added 150gp, confirmed PP15 display and
    on-disk totalCopper -- and the honest-failure path -- tried to
    overspend, got the real backend error, balance unchanged) -- correctly
    left equipment-purchase auto-deduct unbuilt rather than risk a
    non-atomic partial-apply bug, flagged as a real bounded follow-on.
  All 5 of frontend's original wave-1 tasks are now done: bio (94a3865),
  feat picker (febf4d8), money panel (59d5bc0), skill persistence
  (75200fc), level-up persistence (e8e4597).
- Backend proposed the posture-narrowness widening (4 exactness gates
  found, not 3 -- including a previously undocumented Human-Wizard
  spellbook gate). AC slice greenlit initially, then DROPPED
  mid-implementation when backend found baseline_armor_class lives in the
  corpus-free headless compute layer and can't cheaply reach the already-
  correct per-item AC math, which needs corpus access -- a ~347-call-site
  architecture question, not a wiring job. Backend caught this before
  writing the change and stopped to ask. Flagged for the operator: the
  same headless/corpus-aware split likely blocks attack-bonus and
  skill-posture widening too -- possibly one architecture fix unlocks all
  three pillars rather than three separate slices. Full writeup in
  risks-and-open-questions.md item 1. Backend redirected to items 2-4
  (durability, money-conversion PCGen verification, comparator
  field-extraction), none of which share this problem.
- QA adopted money-conversion + encumbrance into the test catalogue (12
  independently-authored tests, cross-checked the PCGen carrying-capacity
  table a second time from a different angle than the original spec pass,
  caught a real fixture-authoring trap in their own draft -- corpus KEY
  tokens don't take a "(Base)" suffix for general items -- and documented
  it for future fixture writers). Refreshed gap-list survey: money/carry-
  capacity/encumbrance no longer gaps; durability is now the sole
  zero-production-surface calculation; BAB/save breadth still 3 of 11
  classes. Caught and fixed a stale "AC greenlit" line in its own
  SWARM_REPORT.md attestation after seeing the drop -- good self-check on
  the document that matters most for closure.
- MAJOR FINDING: frontend's multiclass audit (asked to verify alpha bar
  item 3 is reachable through the UI, not just the engine) found character
  creation is single-class-only by design (multiclassing is a level-up-
  time action, matches backend's docs) -- structurally correct. But
  LIVE-testing (not just reading code) surfaced a real, silent,
  launch-blocking bug: the backend persisted a Fighter-2-into-Rogue-1
  multiclass level-up perfectly (verified on disk: two real class_level
  entries, correct recomputed BAB/saves/HP), but the frontend rendered it
  as a garbled single pseudo-class with HP wrong by 17 points. Root cause:
  characterProgression.ts's parseHeldClasses() split the wire-format
  classSummary on '/' ; character_hub.rs actually joins on ',' -- verified
  directly against the Rust source. The wrong assumption silently
  corrupted HP, skill-point totals, caster level, the Progression rail,
  and weapon proficiency for ANY multiclass character while single-class
  characters (no separator needed) looked completely fine -- exactly why
  it went unnoticed until someone actually drove a real multiclass
  level-up end-to-end rather than trusting that the code paths existed.
  Fixed and live-verified (d03bc89): same flow now shows "Fighter 2 /
  Rogue 1", correct HP 27/27, correct Progression-rail entry. Confirms
  alpha bar item 3 is genuinely reachable -- this was a real bug, not a
  missing affordance. Frontend now attempting a full 6-level multiclass
  walkthrough to proactively surface anything else like this.
- Backend closed item 2, durability (0aeed25): compute_max_hp +
  classify_durability, hit-die size verified against the primary
  cr_classes.lst corpus source directly rather than trusting
  class_tables.rs's existing citation. load_character_durability/
  adjust_character_hp commands, hp.json sidecar. Scoped to single-class
  Fighter/Wizard/Rogue -- multiclass HP honestly deferred: CharacterClassLevel
  stores cumulative totals per class, not the order individual levels were
  taken in, so which level gets the maximized first-level die is genuinely
  ambiguous from that data shape, not guessed at. Temp HP and the
  favored-class-bonus HP choice also deferred per QA's spec flag. Not
  frontend-blocking (wasn't on frontend's original ask), so no UI wiring
  yet -- command shapes documented for whenever a Health tab gets built.
  189/189 lib, 3973/3973 full suite. Moving to item 3 (money-conversion
  PCGen-source verification).
- UNATTRIBUTED FILE: docs/release/v0.6/SWARM_TASKS.md appeared untracked
  in the shared checkout, not created by the lead or any known teammate.
  Content (Epic 1/2/3/6/7 labels, tasks like "campaign manager + Drive
  persistence," "Wizard single-class completion") doesn't match anything
  in this swarm's actual work, and its claim that the observer's cron
  reads it as an "operator-pinned schema" contradicts the verified
  behavior of observer.py (reads SWARM_STATUS.md + mailbox/task JSON only).
  Flagged to the operator, not trusted, not edited, not deleted. Backend
  initially assumed it was the lead's -- corrected.
- MILESTONE: frontend completed a full 6-level multiclass live walkthrough
  (no code changes needed -- pure verification of the d03bc89 fix), taking
  a character from Fighter2/Rogue1 (level 3) through 4 more real level-ups
  to character level 6 (ending Fighter4/Rogue2). Every step: real backend
  mutation (revision_id incremented each time, rev.8 by the end), real
  server-side recompute, hand-checked PF1 math matching exactly (e.g.
  BAB+5 at the end = Fighter4's full +4 plus Rogue2's 3/4-progression
  floor(2*0.75)=+1), zero errors, zero display glitches, Feats tab still
  correct with two classes held. This is the first genuinely end-to-end
  live proof that alpha bar item 3 (advance 6 levels, multiclass required)
  actually works today, not just that the underlying pieces are wired.
  One transient hiccup (caught backend's durability.rs mid-edit, same
  class of false start as earlier) -- frontend handled it by waiting via
  a background poll instead of raising another alarm, learned from the
  earlier pattern. Next: same rigor applied to spell-slot selection at
  spell-gaining levels (bar item 3's other named requirement).
- Backend closed item 3, money-conversion PCGen verification: searched the
  local PCGen checkout directly (.lst files, .java source, output-sheet
  templates) for a currency-denomination table -- found none anywhere.
  Nothing contradicts the 1pp=10gp=100sp=1000cp ratio, but nothing
  affirmatively pins it to a PCGen source either (unlike carry-capacity's
  load.lst). Conclusion, now backed by a direct second search rather than
  left on QA's original flag alone: this is universal D20/PF1 arithmetic
  PCGen likely tracks as a single gold-piece-equivalent total, never
  exposed as denomination data. No ratio change needed -- doc-only update
  to money.rs recording the verification.
- COMMIT-HYGIENE NOTE: 6cbee9c ended up containing both backend's item-3
  doc update and this file's milestone entry under a docs-only commit
  message -- lead error, not backend's: `git commit` with no pathspec
  commits everything currently staged, and backend had already staged
  money.rs when the lead ran a bare `git commit` right after `git add`ing
  only SWARM_STATUS.md. Content is correct and wanted either way; not
  fixed via force-push (too disruptive with three live writers on this
  branch for a purely cosmetic issue). Lead now checks `git diff --cached
  --stat` immediately before every commit, not just before `git add`.
- MAJOR FINDING: frontend's spell-slot walkthrough hit the Wizard
  spellbook gate before ever reaching the spell picker. Unlike the other
  posture gates (narrow but reachable via one exact combo), this one is
  STRUCTURALLY UNREACHABLE -- compose_character_input never seeds the
  required school-specialization choices for any class, and
  CreateCharacterForm has no arcane-school field at all, so no UI action
  can ever satisfy the gate. Verified live two ways (fresh creation,
  multiclass level-up) -- both correctly Blocked, neither persisted.
  Directly blocks bar item 3's "select spells" for the class most
  associated with spellcasting. Read code first, verified before
  reporting, correctly did not improvise a fix (real cross-team scope
  decision: new UI field + new backend param, vs. a hardcoded default).
  Lead decision: hardcode the default specialization server-side,
  mirroring Fighter's existing hardcoded-loadout precedent -- sufficient
  for the bar (item 3 requires spell selection, not school choice), small
  and bounded. A real arcane-school selector goes to backlog. Backend
  paused item 4 to take this first since it unblocks a literal bar
  requirement. Full writeup in risks-and-open-questions.md item 1.
- Backend closed item 4, comparator field-extraction (b726a36): 5 new
  PCGen dimensions parsed from base-xml.ftl (encumbrance thresholds/total,
  durability.max_hp, best-effort money.total_copper from free-text
  MISC.FUNDS). No Rust struct changes needed -- pure serde deserialization,
  confirmed the safe case rather than another blast-radius trap. Genuine,
  confirmed (not overlooked) gap: PCGen's own export template hardcodes
  current HP as an empty tag, structurally absent, can't be extracted
  without a PCGen template change (out of repo scope). Verified two ways:
  synthetic fixture AND the real end-to-end PCGen pipeline (8/8,
  including the genuine non-mocked Gradle run). This closes wave-2 items
  2-4 in full (durability 0aeed25, money-verification 6cbee9c, comparator
  b726a36), none of which hit the AC slice's architecture problem.
- Backend confirmed and started the Wizard fix (crossed-message timing
  only, no discipline issue -- correctly held rather than self-select
  when uncertain, exactly the right instinct). Frontend's caster survey
  (Cleric/Druid/Bard/Sorcerer all need whole subsystems, not a seeded
  choice) relayed to backend to avoid duplicate investigation.
- Wizard spellbook GATE fix landed (3484b5d) -- compose_character_input
  seeds Evocation/opposed-Necromancy/opposed-Transmutation choices,
  gated Wizard-only (mirrors the existing Fighter/Human conditional-seed
  pattern), 2 new tests (Wizard-only confirmed via a Fighter negative
  control; a real recorded+prepared spell now reaches Computed via
  direct Rust construction of CharacterInput).
  Confirmed independently: no other class has an equivalent bespoke gate
  to generalize from -- none of Cleric/Druid/Bard/Sorcerer/etc are even
  chassis-supported yet (only Fighter/Wizard/Rogue), so none can hit a
  gate like this regardless.
  CORRECTION (frontend, same day): the "Wizard is now the second class
  that reaches Computed" claim below was PREMATURE -- struck. Frontend's
  live testing found the compute-layer fix is necessary but not
  sufficient: (1) create_character only ever persists a proven-Computed
  build, so a fresh Wizard (empty spellbook) can never be saved in the
  first place -- bootstrap deadlock; (2) the Add Spell picker can't
  bootstrap it either, since unmet_wizard_spellbook_conditions needs one
  spell recorded as BOTH Known AND Prepared, but add_spell_selection only
  sets one mode per call and each call is independently reject-if-not-
  Computed -- neither a Known-only nor Prepared-only call can ever build
  toward the other; backend's own passing test only reaches Computed by
  constructing CharacterInput directly in Rust, bypassing the command
  layer entirely, which proves the compute logic but not UI-reachability;
  (3) SEPARATELY, apply_level_up (the multiclass-dip path) never got the
  fix at all -- only compose_character_input (creation) was touched, so
  multiclassing Wizard onto an existing character still hits the OLD
  diagnostic. Net: there is currently no UI path, creation or level-up,
  that produces a live saved Wizard character. Backend fixing the
  level-up-path gap (small, clear parity fix) now; the bootstrap
  deadlock -- the real bottleneck -- needs a scoped proposal before code,
  same diligence as the posture-narrowness work.
  Also surfaced (not part of this commit, correctly left for qa): item
  4's landed comparator fix exposed a stale assertion in
  tests/sd26_pilot_case_verification.rs (expected 1 mismatch, now sees 6
  -- the known CG-03 mismatch plus 5 real MissingFromCodex entries, since
  Codex's own SelectedParityDimensions::from_receipt was never wired to
  populate the new encumbrance/durability dimensions). Real upside in the
  failure output: PCGen's actual golden-fixture values match backend's
  encumbrance.rs/durability.rs formulas exactly -- genuine external
  validation, not just internal self-consistency. Backend greenlit to do
  the from_receipt wiring as a small, bounded follow-on directly
  completing item 4's comparator work; QA to update the test expectation
  once it lands, same sequencing as the earlier BAB/save catalogue work.
- Backend split the from_receipt wiring into 3 problems before writing
  code: durability (small, landed alongside), encumbrance (initially
  looked blocked by the same headless/corpus-aware wall as the AC slice
  -- independent 2nd confirmation the boundary is real), money (confirmed
  structural exclusion, not deferred -- money.json's balance is never
  derived from CharacterInput/PilotReceipt, nothing for any
  receipt-projection function to expose). RESOLVED for encumbrance:
  backend found contract::PilotReceipt is already corpus-aware and
  already carries a resolved EncumbranceComputation, so an additive
  SelectedParityDimensions::from_pilot_receipt (2298780) sidesteps the
  wall entirely for parity-testing purposes, without touching the harder
  Computed/Blocked gating problem (still a future epic for AC/attack-
  bonus/skills). from_receipt itself provably untouched, zero blast
  radius on QA's 3 existing callers. Corrected in risks doc.
  Money's exclusion is a real backlog item now (needs its own
  money.json-reading comparison path if ever wanted), not folded into
  the encumbrance/AC architecture deferral.
- Redirected backend back to the two still-open Wizard gaps (level-up
  path fix, then the bootstrap-deadlock scoped proposal) after it went
  to the from_receipt wiring first -- normal reprioritization, not a
  repeat of the earlier discipline pattern; backend was holding for
  direction, not self-selecting past an explicit stop.
- Backend closed the level-up-path gap (1aabdf8): apply_level_up now
  seeds the canonical choices in the new-class-entry branch only,
  verified with a dedicated test that it fires exactly once per
  character and doesn't re-seed on a later Wizard level-up. Both
  fixable Wizard gaps now closed; the bootstrap deadlock remains.
- QA independently switched sd26_pilot_case_verification.rs to
  from_pilot_receipt (900beee) rather than accept the 5 new dimensions
  as permanent gaps -- built a real corpus fixture (reusing the GE-06
  posture), ran the actual corpus-aware compute path, and got 13 of 14
  dimensions genuinely matching PCGen's real export values (max_hp=12,
  light/medium/heavy=100/200/300, total carried=29 -- exact matches, not
  presence checks). Independently confirmed backend's "zero blast
  radius" claim on from_receipt rather than trusting it. The one
  pre-existing combat.baseline_melee_attack_bonus mismatch (CG-03,
  already known) is untouched.
- Backend's bootstrap-deadlock proposal: independently re-derived the
  root cause before reading the lead's framing (verification, not
  agreement), confirmed it's purely a first-spell bootstrap problem --
  add_spell_selection itself needs no redesign, works fine for every
  spell after the first. Proposed a new atomic command,
  record_and_prepare_spell_selection, pushing both Known and Prepared
  entries in one mutate_saved_character_at_root call -- same additive-
  command pattern as the rest of the codebase. Greenlit over the
  alternative (widening add_spell_selection) since it matches this
  swarm's established add-don't-modify pattern. Wizard-scoped, general
  shape not built speculatively. Backend building it now; frontend to be
  looped in on the signature once it lands -- the UI needs a decision on
  how "the first spell" is presented before the picker flow is testable.
- record_and_prepare_spell_selection landed and pushed (6e12437). Real
  and correct fix for its target problem (level-up parity + the
  add-spell-selection second-call bootstrap). Backend proactively looped
  in frontend (command signature) and QA (Wizard spellbook test surface
  impact) without waiting to be told. Frontend testing the real
  spell-picker flow next -- the original ask from before this whole
  investigation chain started.
- Backend closed item 8, feats-tab full-list exposure (1509124):
  chosen.selected_feats now exposed through LoadSavedCharacterResponse,
  found and fixed all 3 build sites for the shared struct literal.
  Small, bounded, no surprises as expected. Proactively messaged
  frontend with the field shape.
- QA scoped a Wizard PCGen fixture task and correctly stopped before
  sinking effort into it: no real PCGen-loadable Wizard .pcg fixture
  exists anywhere accessible (the one that exists is wrong-ruleset,
  GAMEMODE:3e). The Fighter case took multiple GE-05/SD-26 cycles to
  get a genuinely correct one -- hand-authoring a new Wizard fixture
  carries real risk of producing something that doesn't load in PCGen,
  multi-cycle-shaped work. Deferred to backlog (risks item 11). Going
  with the lighter substitute instead: cross-checking Wizard's spell-
  slot/spells-known/save-DC formulas against PCGen's source LST data
  directly, same technique as the carrying-capacity/encumbrance work --
  real external validation without the fixture-authoring risk.
- Frontend found one more small gap while wiring the spell picker:
  load_saved_character doesn't expose spells_selected either (same
  shape as items 6/8), so it can't detect "is this the first spell" and
  routes all Wizard spell adds through record_and_prepare_spell_selection
  regardless -- a sound, documented-pattern-consistent workaround, not
  blocking. Backend correctly flagged it rather than acting on it
  unprompted; logged as risks item 9a, not prioritized.
- Backend closed the real Wizard spell-save-DC gap QA found (3b39731):
  10 + spell_level + Intelligence modifier, mirroring the identical
  pattern already used for Paladin/Ranger/Sorcerer/Bard. 3 new tests,
  195/195 lib green. Coordinated cleanly with QA on the single test
  break it caused (a stale negative-control allowlist) -- held the
  commit until QA's fix landed, same sequencing discipline as before.
- THIRD CORRECTION to a premature "resolved" claim (frontend, live-
  tested again): record_and_prepare_spell_selection is real and correct
  for what it does, but insufficient alone -- it requires an already-
  saved Wizard-holding character, and nothing in the command surface
  could produce that state (class-acquisition is itself gated on spell
  state; spell-acquisition needs an already-saved Wizard). A distinct,
  one-level-deeper chicken-and-egg from the one already fixed. Struck
  the earlier "MILESTONE"/"second class reaches Computed" framing again,
  logged the real gap in risks item 10, sent backend a scoping question
  with a plausible (not mandated) direction: seed one canonical starter
  spell atomically at class-acquisition, mirroring the school-choice
  pattern. Frontend's routing commit (d55a919) still correct and
  necessary regardless of what's found next.
- Backend's investigation confirmed the deadlock precisely (traced to
  SD-24 Criterion 7.5's now-false "starts empty" assumption, invalidated
  by SD-21 E6b's later spellbook gate) and proposed the exact seeding
  fix, verified against real budget math (1 of 3 cantrips consumed,
  comfortably under budget at every supported level) and confirmed not
  to touch the separate level 1-3 ceiling. Correctly declined to fold in
  the unrelated source_class_id validation gap (pre-existing across the
  whole spell-selection family, fixing it inconsistently on one function
  would be worse than leaving both alone). Greenlit without reservation.
- Starter-spell bootstrap fix landed and pushed (b2a5eb6): both seed
  points (compose_character_input, apply_level_up's new-class-entry
  branch) now seed one canonical starter spell. Existing deadlock-
  reproduction tests rewritten (not deleted) into standing regression
  guards; 3 new tests prove the actual fix. 187/187 desktop suite.
  Backend correctly did NOT declare this resolved itself this time --
  explicitly asked for frontend's live verification given the pattern.
  NOT marked resolved in risks doc either, pending that report.
- MILESTONE, GENUINELY CONFIRMED THIS TIME: frontend live-tested the
  starter-spell fix end-to-end. Created a fresh Human Wizard -- creation
  form reported "computed and saved" for the first time in this
  investigation. Real sheet loaded (HP 8/8, real Progression rail).
  Used the real Add Spell picker twice (Magic Missile, then Alarm),
  both genuinely persisted (rev.1->rev.2->rev.3), looked correct at
  "2/2" against the 1st-level budget (1 base + 1 specialist + 0
  Int-mod). CORRECTED BELOW: that "2/2" was never actually server-
  enforced -- see the real bug entry further down. This closes the
  bootstrap/class-acquisition layers of the Wizard investigation:
  class acquisition, then first-spell bootstrap, now confirmed with
  real UI-driven persistence, not just compute-layer proof. Wizard is
  the second genuinely playable class alongside Fighter. Marked
  RESOLVED in risks doc item 10, for real this time.
  One new minor finding along the way: post-mutation corpus_derived
  render staleness (Spells tab briefly shows empty after a successful
  add, corrects on reload). Backend investigated and ruled their side
  out with confidence (mutation path is fully synchronous, no caching,
  response provably fresh) -- narrowed to a frontend render/state-
  timing issue, passed back to frontend. Not urgent, data always
  correct on reload.
- Frontend also landed the feats-tab full-list wiring (aa611ce) against
  backend's selectedFeats field (1509124) -- live-verified, Aldric's
  Feats tab correctly showed all 5 feats (3 fixed-loadout + Cleave +
  newly-added Toughness), revision bumped on the new add. Risks item 8
  marked RESOLVED.
- REAL BUG FOUND (frontend, while chasing the cosmetic render-
  staleness issue): Wizard spell-slot budget enforcement never applies
  to any real spell. parse_wizard_spellbook_spell_id expects the
  bespoke seed-spell format ("evocation.0.light") but real corpus
  spell keys ("Magic Missile", "Alarm") have no dots, so the parser
  returns None for every real spell and the consumption sum silently
  drops them all -- the over-budget check can mathematically never
  fire against real spells. Verified live: added a 3rd 1st-level spell
  past the 2-slot budget, expected Blocked, got silent acceptance and
  persistence instead (rev.3->rev.4 on disk). This RETROACTIVELY
  CORRECTS the earlier "2/2, honestly enforced" milestone claim above
  -- it was UI-side arithmetic, never server-verified. Directly
  relevant to alpha bar item 4. Frontend correctly did not propose a
  fix (real ambiguity in repair shape, not their file). Backend
  investigating now; fix must include a live-verified over-budget test
  case, not just a parser unit test, per lead instruction.
- MILESTONE, GENUINELY FINAL THIS TIME: spell-slot budget fix landed
  (365b3a1a) and confirmed NOT the same architecture wall as AC/
  encumbrance -- SPELL_LIST (652 real CRB records) is compiled directly
  into the binary, already headless-accessible, unlike equipment.
  parse_wizard_spellbook_spell_id resolves real keys first, falls back
  to the synthetic convention for zero blast radius on QA's fixtures;
  WIZARD_STARTER_SPELL_ID upgraded from placeholder to a real spell
  ("Light") now that real resolution exists. Genuine RED->GREEN: backend
  disabled the fix, watched the test fail with the exact reported
  symptom, restored it, confirmed green. Frontend live-re-verified with
  a fresh character (avoiding the prior save's stale pre-fix artifact):
  Magic Missile + Alarm accepted at 2/2 (disk-confirmed), Grease as a
  third spell honestly Blocked with a real diagnostic, revision
  unchanged, nothing persisted -- verified against disk specifically
  because the still-open render-staleness bug makes accepted-vs-blocked
  look identical on screen. This closes the ENTIRE Wizard investigation:
  class acquisition, first-spell bootstrap, and now slot-budget
  enforcement, all live-verified end-to-end. Only the cosmetic render-
  staleness bug remains open in this whole area (backend's to pick up,
  not urgent).
- OPERATOR DIRECTIVE, 2026-07-23: fully autonomous, operatorless
  operation, potentially for days. Resolved the 3 remaining "operator's
  call" items with the lead's own judgment (tranche-6 CI needs no new
  workflow; book-stub labels out of scope for this swarm; Pets/companion
  gap is a documented non-blocker, real future feature). Cron backstop
  set (~every 20 min, 7-day window) to keep dispatching work even if the
  turn-based flow ever goes quiet. Deep prioritized queues loaded into
  all 3 teammates with explicit "don't wait for check-in between tasks"
  instructions. Full reasoning in risks-and-open-questions.md.
- Backend confirmed Rogue reaches Computed through the real UI with
  ZERO gap (0bb37521) -- unlike Wizard, no hidden posture/spellbook
  gate exists for Rogue anywhere; compose_character_input uses the
  identical fixed loadout as every non-Fighter/non-Wizard class, and
  the one class-conditional sub-check only adds a requirement for
  Fighter, never blocks others. Confirmed empirically with 2 new tests
  through the real command path (creation + multiclass dip). No fix
  needed -- task 4's BAB/save widening was already sufficient. Third
  class (after Fighter, Wizard) confirmed reachable, for free. Moved
  autonomously to item 2 (DR exposure) without waiting for check-in.
- QA delivered a comprehensive coverage resurvey (81785b6f) against
  everything that's changed this session -- durability, encumbrance,
  money, Wizard spellbook, spell save DC, Rogue widening.
- Backend landed DR exposure (f7ce289d), spells_selected exposure
  (034d1b84, unblocks frontend's earlier routing workaround), then
  moved to investigating the money-purchase-coupling transaction shape
  before building it. QA folded a real de-risking finding on the
  Wizard .pcg fixture task into risks item 11 (real working syntax
  templates found for every section needed; one real unknown flagged,
  not assumed) -- correctly routed through the lead rather than
  committing to a file outside its own lane, even under autonomous mode.
- QA closed the Rogue reachability catalogue-coverage task (c800d568)
  with good judgment on a real complication: backend's 2 proof tests
  live in the separate codex-desktop crate (apps/desktop/src-tauri),
  which tests/** can't target -- same crate-boundary shape as the
  spell-slot-budget fix and mutation-op registry QA correctly left
  alone before. Instead of skipping coverage, QA read
  compose_character_input's source directly, confirmed its fixed
  loadout is byte-for-byte the existing GE-06 deterministic fixture
  shape, and wrote an independently-authored rules_core-layer
  equivalent proving the identical claim as a permanent tests/** entry
  -- complementary to backend's proof, not a duplicate, now under QA's
  ownership/sign-off same as the BAB/save and DC work. Moved to the
  systematic Fighter/Wizard/Rogue gap sweep next.
- Backend closed money-purchase coupling (29e67515): new atomic
  purchase_equipment command, resolves real catalog cost headlessly (no
  corpus wall -- checked before building), pre-checks affordability,
  adds equipment + deducts cost together, honestly Blocked (never free)
  if unaffordable or cost unknown. Re-confirmed the render-staleness
  elimination holds for the two newest mutation commands too (same
  synchronous, no-caching code path) -- nothing further diagnosable
  from backend's side without frontend-specific render/state detail.
- MAJOR FIND, priority bug: QA's systematic gap sweep found
  compute_selected_skill_modifiers applies the Climb/Intimidate/Swim
  class-skill bonus (+3) unconditionally regardless of actual class.
  Written originally for Fighter (whose real class-skill list genuinely
  includes all three); the Computed/Blocked gate was later widened to
  any dispatch-supported class, but the +3 itself was never made
  class-aware. Rogue is coincidentally correct (its real list also
  includes all three); Wizard's real list includes none of them.
  VERIFIED CONCRETELY, not reasoned abstractly: built the actual Wizard
  posture, got climb=5 (real value should be 2, no class-skill bonus) --
  a silently wrong number in the explanations list, no diagnostic fires.
  Narrower and more urgent than the already-known deferred "class-skill
  recognition is Fighter-only" finding (that one is in skill_allocation.rs,
  doesn't gate anything reaching Computed) -- this is the narrow
  deterministic-posture function that DOES gate Computed/Blocked, live
  and wrong right now. Two smaller findings from the same sweep, lower
  severity, logged but not actioned: level-up HP exposure is
  inconsistent per-class (Fighter has a dedicated explanation, Wizard/
  Rogue don't, though durability.rs already computes the same correct
  value for all three) -- reads as inconsistent exposure, not a missing
  calc; bonus spells from high ability score confirmed present and
  correctly wired, no gap. Backend redirected to the bug ahead of
  render-staleness (CORRECTION BELOW: this framing was wrong).
- Both quota outages recovered cleanly. All 3 teammates resumed after
  the ~11pm ET reset and picked up exactly where they'd left off.
- QA's continued sweep of the adjacent narrow-posture-gate functions
  (Fighter bonus-feat sub-check, Weapon/Armor Training level fallback)
  came back clean -- both correctly guarded/semantically right, not
  bugs, closing out that thread with certainty rather than leaving it
  assumed-fine.
- MAJOR REPORT (frontend, class-dropdown honesty audit, went deeper
  than asked): confirmed all 11 CRB classes appear in the create-
  character dropdown; 8 (Paladin/Ranger/Sorcerer/Bard/Barbarian/Monk/
  Cleric/Druid) were labeled "(Human only, partial)", implying Human
  produces a computed build. FALSE for all 8, verified via source
  (each has its own "does not compute a supported chassis" doc
  comment) AND live (fresh Human Barbarian -> real named Blocked
  diagnostic, never Saved) -- these never reach Computed for ANY race
  including Human, they just get nicer diagnostics. Fixed: new
  ClassSupportLevel variant `human-diagnostics-only`, distinct from
  the genuinely-working `partial-human-only`, honest dropdown/
  description/fallback text. No stub/silent-failure anywhere -- the
  Blocked path itself was always fine, just the labeling overclaimed.
  BONUS, opposite direction: Wizard and Rogue (labeled
  `partial-human-only`) turn out to be mislabeled too -- confirmed via
  git archaeology (3484b5d never had a Human condition) AND live
  (fresh Elf Wizard 1 and fresh Elf Rogue 1 both reached Computed/
  Saved, disk-confirmed with race_id=race:elf). Reclassified both to
  `full`, same as Fighter. Real, free additional alpha-bar progress on
  item 2 ("any class or race") -- the "Human only" framing for two of
  our three working classes was a stale, never-verified assumption.
  Pushed 34635157, 62/62 suite green.
- CORRECTION (frontend, re-tested live against the current build):
  the render-staleness bug is NOT fixed -- the lead's earlier "already
  eliminated on backend's side" framing was wrong (backend's own
  elimination of possible causes was real, but "eliminated as a cause"
  got conflated with "the bug is gone," and nobody had re-tested live
  since). Root cause identified precisely: character_hub.rs's
  CreateCharacterResponse enum has #[serde(tag = "kind")] with no
  rename_all, so corpus_derived stays snake_case on the wire while
  frontend expects camelCase. Real trap flagged: a bare
  rename_all="camelCase" would ALSO lowercase the "Saved"/"Blocked"
  tag values, breaking every outcome.kind==='Saved' check across the
  frontend -- needs a per-field rename or rename_all_fields instead.
  Reopened, queued for backend next with this guidance attached so
  they don't walk into the trap.
- MILESTONE: class-skill-modifier bug FIXED (93a0636d), verified
  against the real PCGen corpus (cr_abilities_class.lst) before
  writing code, not memory -- confirmed Fighter/Rogue genuinely have
  Climb/Intimidate/Swim as class skills, Wizard has none. Now only
  applies the +3 when the character has a class that actually grants
  it; multiclass correctly gets it via PF1's real union rule (tested
  explicitly, not assumed). One real test break found and coordinated
  with QA before pushing. Reading the full render-staleness context
  before touching that fix, per the trap flagged earlier -- correct
  instinct given the "obvious" fix (bare rename_all) would break tag
  matching across the frontend.
- Frontend explained an idle-with-no-report gap when checked: purchase_
  equipment wiring is code-complete (typecheck clean, 62/62), held
  uncommitted pending live verification per standing practice, and hit
  the same transient shared-checkout build break as before (backend's
  skill-modifier commit landing mid-edit) -- correctly waited it out via
  a background poll rather than assume a regression, resumed once clear.
- REAL PARITY GAP found (QA, following up on the Elf Wizard/Rogue
  confirmation): traced PCGen's actual Elf race data through the real
  reference chain (not just the top-level citation-only line) and
  confirmed +2 Intelligence is the CRB-standard racial default, not the
  "alternate variant" Codex's own comment claims -- explain_elf_race_seam
  only grounds DEX/CON, missing INT entirely. Matters specifically
  because Elf Wizard (Intelligence-cast) is now confirmed reachable --
  a live parity break for exactly the combination just proven to work,
  not theoretical. Small, well-scoped fix expected (mirrors the existing
  DEX/CON pattern). Queued for backend after render-staleness. QA moving
  to check other grounded races for the same "comment says out-of-scope,
  real PCGen data says default" pattern.
- CROSS-AGENT GUI COLLISION fixed (lead, f6fe0df2): driver.sh shared one
  DISPLAY_NUM=99, one state file, one log file, and cmd_stop's process-kill
  matched every agent's codex process globally -- any agent's launch/stop
  could hijack or kill another agent's session (this is what frontend
  flagged and QA independently hit as "black webview"/dying process/
  vanishing state file). Fixed: RUN_DESKTOP_AGENT=<name> namespaces
  DISPLAY_NUM (frontend=96/backend=97/qa=98/default=99) plus state/log
  files, and cmd_stop's kill is scoped to the caller's own DISPLAY via
  /proc/<pid>/environ. Also fixed a latent bug where cmd_stop never
  sourced its own state file (dead TAURI_PID/XVFB_PID branch). QA stood
  down its own stray instance cleanly before the fix landed -- no harm.
- Doc-hygiene pass (lead): SWARM_STATUS's wave-1/wave-2 on-deck table had
  gone stale (didn't reflect durability/money-conversion/comparator/DR/
  money-purchase/spells_selected all landing) -- replaced with an accurate
  consolidated snapshot (026b9cd0). risks-and-open-questions.md items 6,
  9, 9a corrected from stale "backlog, non-blocking" to RESOLVED to match
  what actually shipped.
- Backend's scoping-only pass (no code) on whether attack-bonus/skill
  widening hits the same headless/corpus-aware wall as AC: YES for
  attack-bonus (enhancement bonuses need corpus-resolved EquipmentRecord,
  same as AC) and YES for armor-check-penalty (ACCHECK: token same story),
  but NO for class-skill-list recognition -- that's a labor-volume
  hand-authoring problem (GROUNDED_FIGHTER_CLASS_SKILLS precedent), not an
  architecture wall. Folded into risks item 1 (81103838). Since the
  class-skill-list gap is confirmed tractable, dispatched backend to
  ground Wizard's and Rogue's real class-skill lists against the PCGen
  corpus and widen skill_allocation.rs's recognition beyond Fighter-only
  -- in progress.
- QA's second SWARM_REPORT.md consolidation checkpoint (58da213d): 4-race
  ability-modifier gap fully closed end to end, the create-character
  submission bug, the 3-instance Fighter multiclass/race lookup-gap sweep,
  items 6/9/9a resolved. Bar-distance assessment unchanged in shape:
  multiclass breadth (3/11), class-chassis breadth (8/11 missing engines),
  posture narrowness, feat effects remain the real distance, all
  architecture-level.
- QA ran an INTERIM four-check wired-integration audit early (lead's
  request, since remaining bar-distance looks architecture-bounded rather
  than "more bugs to find") against the full combined diff
  (origin/develop...origin/tranche/6, 116 files, ~10.6k insertions):
  clean, zero real violations (c3b5fba8). Lead independently re-extracted
  and re-ran all four checks without reading QA's extraction first, per
  the doctrine's executed-by-QA/re-verified-by-lead requirement --
  confirmed identical result (f3676470). This is an interim checkpoint;
  the closure-time audit still runs separately against the final diff.
- Frontend's GUI sanity pass, first real live use of the fixed driver.sh
  (RUN_DESKTOP_AGENT=frontend, DISPLAY :96): clean first-try launch, no
  collisions. Fresh Dwarf Rogue ("Borin Sanity") -- deliberately a
  different race+class combo than the earlier capstone -- covering
  creation with correct Dwarf adjustments, money Add, money Spend (a
  genuinely different code path from purchase_equipment, first time
  tested), money Spend correctly Blocked on overspend with balance
  unchanged on disk, feat add, level-up to Rogue 2 with feat/money
  surviving intact. No bugs found. Also chased down and explained a
  stale Load-Character list-row observation as a session-scoped list
  cache (clears on app restart), not a real data bug. Dispatched next:
  wire the still-stub Defense tab to the real damage_reduction field
  backend exposed (f7ce289d) but nothing on the frontend side renders
  yet -- scoped narrowly to what's actually computed today, no
  speculative AC/save rendering.
- Dispatched QA next: independent tests/** catalogue coverage for the
  three recently-landed DTO-exposure fixes (DR, purchase_equipment
  atomicity, spells_selected) that currently only have backend's own
  inline tests -- same shape as the earlier BAB/save and Rogue
  reachability catalogue work.
- THIRD QUOTA OUTAGE (all 3 teammates, ~01:40-04:10 ET): resets 4:10am ET,
  same pattern as the two earlier ones. QA's finished-but-uncommitted
  Wizard/Rogue skill-allocation test verified and landed by the lead
  (d35521ec) to avoid losing it during the outage; QA's separate DR test
  (39fef863) had already landed cleanly. Logged and all 3 re-engaged with
  their paused tasks once past reset -- clean recovery again.
- Backend's systematic Fighter-only-grounding sweep (post-reset) came back
  clean, one near-miss (explain_rogue_level1_chassis) traced and correctly
  ruled out (no downstream consumer, cosmetic-only). Adopted as a real
  signal the swarm is down to architecture-level gaps -- logged as risks
  item 21.
- Backend consolidated the three architecture-level gaps (items 1/17/18)
  into a single scoping doc for the operator, docs/release/v0.6/
  future-epic-scoping.md (c30f9b04), with a pointer from the top of
  risks-and-open-questions.md. Side-by-side table (what's blocked/why/fix
  shape/size), explicit answer to "which gaps would bridging the headless/
  corpus-aware wall actually unlock" (attack-bonus + skill-ACP: yes;
  feat-effects + Wizard-non-Human spell math: no, independent problems),
  and a plain recommendation (B/C have no architecture prerequisite and
  can start anytime; A is the expensive, consequential piece worth its own
  design pass). Good, disciplined commit hygiene noted: checked
  `git diff --cached --stat` was empty before staging, confirmed only its
  own 2 files landed, left frontend's and QA's concurrent in-progress
  edits untouched.
- QA's fresh-eyes review of the lead's stewardship commit (d35521ec) found
  a real, independent gap: the pre-existing
  sd20_skill_allocation_class_skill.rs multiclass test used the bare
  string "wizard", not skill_allocation.rs's actual "class:wizard" id, so
  it never really exercised Wizard recognition in a multiclass union --
  correctly still a valid Fighter-plus-unrecognized-class test, just not
  the Wizard proof it looked like. Writing a proper fixture-driven
  multiclass test with the real class ids to close the gap; in progress.
- QA's verification-completeness sweep (self-directed, scanning the full
  develop..HEAD commit history for anything not yet independently
  checked): 5 clean confirmations landed today, each reading the actual
  code/diff rather than trusting the commit message -- the Defense-tab DR
  wiring (risks item 6), the durability status thresholds against real
  PF1 rules (risks item 4), all 11 classes' support-level labeling (risks
  item 15, not just the two already touched), the corpus_derived
  wire-serialization fix (risks item 10, including QA reproducing RED
  themselves by removing the fix and watching it fail before restoring
  it), and the 185-record feat catalog exposure (per-category counts
  re-grepped from the data files themselves: 50+110+8+17=185, plus
  confirming append-safety from the source side, not just the earlier
  empirical test). Continuing down the unverified-commit list
  (Bio/LevelUp persistence commands next).
- QA's sweep continued: independently verified `7694b227` (level-up
  hit-die/feat/skill persistence) -- traced the "exactly one colon"
  validator to its real source in `local_store.rs` and confirmed via
  `git log -S` it's genuinely pre-existing (dated 3 days before this
  swarm started, not invented to justify the fix); read the atomicity
  test directly and confirmed it reloads from disk (not just in-memory
  state) to prove the class-level bump, both choice entries, and all
  three skill allocations landed together in one mutation. 6th clean
  independent verification today. Moving to Bio persistence next.
- QA's sweep continued: independently verified `0ab784df` (bio field
  persistence) -- confirmed the "requires existing character" claim
  traces to a real `SavedCharacterStore::load` check (not the naive
  `root.exists()` the commit deliberately avoided), read the overwrite
  test directly (second save's value alone survives reload, no merge),
  confirmed the disclosed default-when-absent asymmetry in source, and
  correctly cross-attributed the stale-test side-fix to the earlier
  Rogue BAB/save widening rather than treating it as unexplained. 7th
  clean independent verification today (Feats+Bio+LevelUp persistence
  all now independently confirmed). Continuing down the unverified list.
- QA's sweep continued: independently verified `d03bc89d` (multiclass
  classSummary comma-separator fix) -- confirmed the fix itself is
  correct (ran `parseHeldClasses` directly, matches the commit's
  live-verified outcome), but found a real, concrete gap:
  characterProgression.ts has zero test coverage anywhere, including for
  the exact silent-multiclass-corruption bug just fixed there -- nothing
  guards against it recurring. Logged as risks item 22, routed to
  frontend once free (QA correctly didn't pause its own sweep or touch
  frontend's file). Then verified `e0a0bda4` (set_skill_allocations,
  genuine wholesale-replace confirmed via a reversed-order golden-path
  test) and `67490acb`'s Tauri-layer money persistence (rejects negative
  balance before any write, no duplicated/drifted conversion logic --
  calls the same pure function QA's own earlier test already covers).
  9 clean independent verifications today plus the 1 real gap found.
  QA has now covered essentially every backend Tauri persistence command;
  what's left unchecked is the 8 frontend UI-wiring commits themselves
  (does the UI call the right command with the right shape) -- harder to
  verify without live-driving the app. Greenlit to continue into that
  with RUN_DESKTOP_AGENT=qa now that driver.sh is fixed.
- Backend's consolidated full-workspace health check, run fresh rather
  than trusting the many per-commit green reports individually: `codex`
  crate full workspace suite 4239/1 (the 1 failure a wall-clock
  performance assertion squeezed by running 4 heavy checks concurrently,
  confirmed a contention flake by re-running alone: 25/25, 1.13s vs the
  2.34s threshold breach), 0 warnings; release build clean, 0 warnings,
  1m21s; desktop Tauri crate 197/197, 1 pre-existing unrelated warning;
  frontend typecheck clean, 63/63 tests green including against
  frontend's own in-progress edits. Flagged the flaky test honestly
  rather than silently omitting or silently calling it clean -- worth
  noting in the eventual attestation as a documented, re-verified
  non-issue. No code changes, pure verification.
- Items 22-26 (the last wave) all resolved today: characterProgression.ts
  test coverage, LevelUpDialog's feat-pick gap, a new PCGen BAB parity
  dimension, the systemic frontend-test-coverage pattern across 5
  modules, and the Load-list stale-row refresh -- every one independently
  re-verified by QA after landing, no exceptions.
- Lead wrote up the bounded-backlog-exhaustion assessment promised in
  open question 3: all 26 risk items resolved or correctly deferred, two
  clean backend self-scans in a row, a clean consolidated health check,
  QA's completeness sweep essentially exhaustive. Not claiming the alpha
  bar is met -- class/multiclass breadth and the three architecture gaps
  remain real distance -- but the bounded, same-session backlog is
  genuinely exhausted, not abandoned early.
- QA's final comprehensive SWARM_REPORT.md consolidation (34bf1ffa): a
  new "Current-state summary" section distinct from the four incremental
  checkpoints -- one coherent read of where things stand, the defects
  table brought fully current (added the wire-serialization bug as
  defect 10), the bar-distance assessment restated plainly, and a "what's
  genuinely left" section matching the lead's own assessment. Still
  explicitly not signing an attestation.
- Frontend running a comprehensive live-UI smoke test across the whole
  app (all 3 working classes, multiple races, several levels, every
  populated tab) as a genuine end-to-end sanity check broader than any
  single-feature verification pass today -- in progress.

(c) Consolidated status (refreshed 2026-07-24, ~06:15 ET — supersedes the
prior refresh, itself already superseded by everything since)
--------------------------------------------------------------------------
- Fighter, Wizard, and Rogue all reach `Computed`/`Saved` end-to-end
  through the shipped UI, for ANY race (not Human-only), confirmed through
  a full 6-level multiclass walkthrough and multiple independent level-up
  paths.
- Money, equipment purchase (atomically coupled), feats (full list +
  selection), skill allocation, bio, level-up, DR (both initial-load and
  recompute DTOs), spells_selected, and durability/HP (a real tab, live
  disk-confirmed both directions including the Dying/clamped-max
  transitions) are all real, persisted, and exposed/rendered.
- The multiclass `classSummary` parser and its silent-corruption bug both
  now have real regression coverage (item 22); a new
  `combat.base_attack_bonus` PCGen parity dimension is wired end-to-end
  and confirmed against real PCGen output (item 24).
- QA completed an exhaustive verification-completeness sweep today: 16
  distinct commits/areas independently re-verified clean by reading the
  actual code (not commit messages) — Defense tab, durability thresholds
  against real PF1 rules, all 11 classes' support-level labeling,
  wire-serialization with a genuine RED reproduction, feat catalog counts,
  level-up/bio/skill-allocation/money persistence, SkillAllocationDialog,
  Bio editor, Feat picker+Feats tab, Wizard spell routing, Actions tab
  (Money panel's static half also clean; live-UI leg inconclusive on an
  unrelated window-geometry quirk, not a code bug). Real findings from the
  sweep: item 23 (LevelUpDialog never collects a feat pick, stale
  excusing comment — frontend actively closing this now) and item 25 (a
  confirmed systemic pattern: several frontend persistence-wiring modules
  — classSummary parser now fixed, skillsModel, Bio editor, likely others
  — shipped with sound logic but zero dedicated test coverage; not
  re-flagged per-instance once the pattern was clear).
- Backend ran two self-directed scans today beyond the originally-assigned
  work, both real: the DTO-consumption sweep (2 gaps, both now closed)
  and the PCGen parity-comparator scan (1 real dimension added, a clean
  second-pass negative result closing that thread). Currently running a
  consolidated full-workspace health check as closure-readiness prep.
- Known, correctly-deferred future-epic gaps (not attempted this wave,
  flagged for operator review): AC/attack-bonus/skill-posture widening
  (risks item 1), the feat-effects engine's total absence (risks item 17),
  Wizard non-Human spell-math completeness (risks item 18).
- Operator-only open item: starting-wealth-by-class (risks item 7,
  content-provenance/licensing question, not an engineering call).
- driver.sh's cross-agent GUI collision fixed and pushed (f6fe0df2) --
  concurrent agents now namespace DISPLAY_NUM/state/log files via
  RUN_DESKTOP_AGENT=<name>, and cmd_stop's process-kill is DISPLAY-scoped
  instead of global. Unblocks frontend/QA's live GUI verification.
- risks-and-open-questions.md items 6, 9, 9a corrected from stale
  "backlog, non-blocking" to RESOLVED to match what's actually landed.

## Agent Status (2026-07-24, ~15:25 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | idle | closure-readiness pass done: genuinely clean (4271/4271 root-crate tests, 204/204 desktop, 0-hit formal 4-check doctrine audit against the full develop...tranche/6 diff); lead independently re-ran the doctrine checks and the full workspace suite to confirm |
| frontend | idle | closure-readiness pass done and pushed (c014ffec) — one real pre-existing gap found and fixed (WeaponsTab's dead Print button, risks item 30); lead-verified |
| qa | idle | standing by; nothing currently needs independent verification |

**Checkpoint (2026-07-24, ~15:25 ET): bounded swarm-actionable backlog is exhausted.** All three teammates idle with clean queues, not blocked on each other or infrastructure. Everything remaining in SWARM_TASKS.md is blocked/deferred on an operator decision (items 1, 18, 27) or is multi-cycle future-epic scope (class breadth, 12-class starting wealth). Written up plainly as a real checkpoint finding in SWARM_REPORT.md (risks item 31) rather than manufacturing busywork.

**Operator decisions received (2026-07-24, ~15:35 ET):** all three gated items resolved directly by the operator following the checkpoint. Item 27: widen the posture gate to accept any equipment (real engineering scope, backend to scope/sequence first). Item 18: widen Wizard non-Human spell-math now, backend's smaller-than-feared scoping read notwithstanding still needs full empirical verification. Item 1's multi-weapon case: add a real equipment-attachment schema field, sequenced together with item 27 since both need the same underlying data. New wave of real, bounded (item 18) and larger-scope (items 1/27) backend work dispatched.

## Agent Status (2026-07-24, ~22:45 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | idle | Ranger (class 1 of 24) fully landed and lead-verified (f75cdff0, 4296/4296 + 212/212), all 4 adversarial-review findings independently confirmed fixed; ready to start class 2 once QA's test-fixture update is confirmed |
| frontend | idle | queue clear |
| qa | working | landing the disclosed 4-site `tests/**` update for Ranger's dispatch widening (caught mid-flight, correct) |

**Operator directive (2026-07-24), acted on:** full class chassis across all 4 primary books is the swarm's primary active work. The adversarial review dispatched on the scoping plan found a real correctness gap (a false-positive-Computed risk) before it shipped — backend fixed all 4 findings, lead independently verified every one rather than trusting the report.

**LEAD CORRECTION (2026-07-24): "class 1 of 24 is genuinely done" above was an overstatement, caught by frontend.** Ranger's BAB/save dispatch-widening landed and is proven safe, but Ranger still cannot reach `Computed` — its spell posture is genuinely uncomputed and unconditionally blocked. Frontend investigated an instruction of the lead's before implementing it, found the premise false, live-verified via the real dev build, and correctly made no code change. Backend confirmed the corrected terminology going forward ("done" = reaches `Computed`, always) and landed APG's 6-class BAB/save/HP dispatch (`c511c132`) correctly labeled as still-Blocked from the start — lead-verified 283/283 lib + 212/212 desktop, including a dedicated multiclass-safety test proving backend proactively avoided the exact loophole Ranger's review found. Next: Ranger's real spellcasting (genuinely new engine work, no prior art in this codebase). See risks-and-open-questions.md item 8 for the full record.

## Agent Status (2026-07-25, ~05:55 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | idle | Cleric's real spell math ladder landed (fca4e64e), lead-verified 333/333 lib + 212/212 desktop, spell list spot-checked, table verified against 2 independent primary sources; also self-caught and fixed 2 of their own lib-test regressions before I even looked. Deciding on next class |
| frontend | working | still live-verifying the non-Human-Ranger-at-level-1 finding before committing the UI label |
| qa | working | reviewing Sorcerer + Cleric's combined 54-file test-cleanup batch (both diff-drafts accepted); proactively flagged the faster back-to-back pace, not yet a bottleneck |

**Progress: Fighter, Wizard, Rogue, Ranger, Paladin genuinely reach Computed (5 of 27). ACG/APG have real BAB/save/HP dispatch (still correctly Blocked pending skill/feature/spellcasting).**

**Progress: Fighter, Wizard, Rogue, Ranger, and Paladin genuinely reach Computed (5 of 27 classes). 22 remain.**

(d) Lead-side verification note (2026-07-24, ~14:20 ET)
------------------------------------------
Resumed after context compaction. Found backend's null-serialization fix
complete but uncommitted in the working tree (character_hub.rs +
risks-and-open-questions.md + 2 regenerated parity-report artifacts).
Independently re-ran the new wire-serialization test and the full desktop
suite (204/204, matching backend's own report) before committing/pushing
as 874df6db. Also independently confirmed
`buildLabelFixtureFreshness.test.ts` (risks item 2's frontend follow-up)
passes with zero commits touching the 3 named fixture files -- it was
never actually broken by the version bump in practice, so that item is
now fully resolved, not just "in flight."

No open blocked-by dependencies between teammates at this checkpoint.

## Agent Status (2026-07-25, ~06:35 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | working | Druid's spell posture in progress (uncommitted: pilot_compute.rs +638, druid_spell_list.rs 273 lines, character_hub.rs, crb/mod.rs wiring) -- status ping sent asking for the honest-framing confirmation and any self-caught regressions before commit |
| frontend | working | still live-verifying the non-Human-Ranger-at-level-1 finding -- status ping sent asking for the real safe level range before touching characterHubModel.ts |
| qa | idle | Sorcerer+Cleric 54-file batch closed; offered a proactive Bard spell-list/blocker survey (next class after Druid) as prep work, or standing by for frontend's Ranger UI change -- awaiting their pick |

## Agent Status (2026-07-25, ~07:05 ET)
| Agent | Status | Detail |
|---|---|---|
| backend | working | Druid landed and pushed (dda46d4a), lead-verified 344/344 lib + 212/212 desktop, 169-entry spell list count and 3 spot-checks all exact, gate-ordering fix confirmed directly in code, pre-existing level-15 cap confirmed not introduced this slice. Now scoping Bard (last of the 4 spell-needing CRB classes) |
| frontend | working | still live-verifying the non-Human-Ranger-at-level-1 finding |
| qa | working | picked the proactive Bard spell-list/blocker survey (their own choice, citing the ACG-survey precedent); 17 new Druid-named test files also queued from backend |

**Progress: Fighter, Wizard, Rogue, Ranger, Paladin genuinely reach Computed (5 of 27). Sorcerer, Cleric, Druid have real spell-posture progress, correctly still Blocked on their own named permanent burden. ACG/APG have real BAB/save/HP dispatch (still correctly Blocked pending skill/feature/spellcasting). 8 real class slices landed this session.**

**QA's Bard spell-list survey complete, lead independently re-derived the full per-level breakdown from the raw corpus and got an exact match.** Properly parsing `cr_spells.lst`'s `|`-separated multi-class `CLASSES:` groups (not a naive grep, which undercounts): 164 distinct real Bard spells, levels 0-6 (16/26/35/29/22/16/20 by level) -- lead's independent script reproduced every one of those 7 numbers exactly. Confirmed zero pre-built `bard_spell_list.rs` (same from-scratch shape as Cleric/Druid). Bard's BAB/save chassis is already grounded as standalone records, same "chassis done, dispatch not wired" state as the rest of the queue. Real permanent blocker identified: `class_feature.bard.bardic_performance_execution.unsupported` -- individual performance magnitudes (Inspire Courage, Fascinate, Lore Master, etc.) are already grounded as flat numbers, but the execution engine (action economy, round tracking, applying them to a real roll) and a few fully-ungrounded performances (Countersong, Distraction, Versatile Performance, Soothing Performance) are genuinely missing -- same permanent shape as Sorcerer's bloodline/Cleric's domain-powers, not expected to go conditional. The spell-side diagnostic (`class_spell.bard.spontaneous_known_and_per_day.unsupported`) is confirmed unconditional today, matching the exact pre-fix shape of every prior class before its slice landed.

**QA invoked the pacing hold (2026-07-25, ~07:10 ET) -- the escalation path set up 2 cycles ago fired for real.** Sorcerer (28) + Cleric (28) + Druid (17) test-cleanup files now total ~75, none reviewed yet -- the Bard survey filled QA's queue gap while backend kept landing classes back-to-back. QA told backend directly to hold on Druid's drafts and any further classes until the queue clears; confirmed via git status showing no new backend class commits since bb1b279d. Working order: Sorcerer -> Cleric -> Druid, same proportionate review as Paladin's batch for each (representative sampling, real RED->GREEN spot-checks, full-batch run), reporting as each clears. This is the correct call, not a stall -- logged plainly rather than letting the queue compound further.

**Frontend has a genuine in-progress fix for the Ranger UI label, not yet committed -- observed in the working tree, not touched.** `characterHubModel.ts` diff (uncommitted, actively being edited) proposes a new `full-except-human-level-1` support level: Ranger reaches `Computed` for every race at every level offered EXCEPT a single-class Human at level 1 specifically, which stays blocked on `explain_hybrid_level1_chassis`'s unconditional diagnostics (a historical boundary from Ranger's original hybrid-baseline slice, not touched by the later spell-posture work). Human reaches the same computed build from level 2 on. Claims live-verification (fresh Human level 1 stays Blocked; fresh Elf level 1 reaches Computed and stays Computed through level 4, past the spell-access threshold) but this is still frontend's own in-flight edit -- will independently verify once committed, per the standing "safety fix vs feature complete" discipline, before treating it as confirmed.

No new commits since fca4e64e. All three pings are routine check-ins, not corrections -- no discrepancy found this cycle.

**Frontend LIVE-VERIFIED the Ranger inverted-scope finding for real, code written.** Confirmed via the actual dev build: single-class Human Ranger level 1 stays Blocked (same 2 hybrid diagnostics); Elf Ranger level 1 reaches Computed and saved (real BAB+1/Fort+3/Ref+5/Will+1), then leveled through the real LevelUpDialog to levels 2/3/4 (past the spell-access threshold) staying Computed at every step (level 4: BAB+4, HP 32/32). Added the new `full-except-human-level-1` ClassSupportLevel value, set Ranger's levelOptions to `[1,2,3,4,5]` (the verified range). Found and fixed a stale test (`characterHubModel.test.ts`'s blanket "every other class stuck at [1]" assertion), same discipline as every prior self-caught regression this session. Committing once a second full suite run confirms clean.

**Paladin's UI label flagged as stale in the same shape -- lead independently confirmed, told frontend to fix now, not later.** Frontend proactively flagged that Paladin shares the identical `explain_hybrid_level1_chassis` function as Ranger. Lead verified directly: this shared function (pilot_compute.rs:8277) handles both Paladin and Ranger with the same unconditional Human-at-level-1-only gate (`hybrid_level1_class`/`HYBRID_BASELINE_LEVEL == 1`), and Paladin already reached Computed at ee3c50ce this session -- so Paladin's committed characterHubModel.ts row (still `human-diagnostics-only`, `[1]`) is stale right now, not pending a future landing. Told frontend to live-verify and fix it in the same pass as Ranger's fix.

**Backend's combat-time activation-state scoping plan landed (uncommitted, `combat-time-activation-state-scoping.md`) -- read directly, adversarial review launched per backend's own recommendation.** Central finding: `ActiveState` (already used for Power Attack, a non-equipment case, via a synthetic `equipment_selections` entry) is the right precedent to extend, not a new concept from scratch. Proposes a new `class_ability_activations: Vec<ClassAbilityActivation>` field, additive-only, feeding 4 already-integrated pillar functions (ability_modifiers, total_saves, combat_baseline AC and attack/damage) conditionally. Barbarian's Rage first (most self-contained, fatigue explicitly deferred as a named follow-on), Bard's Inspire Courage second reusing the pattern. Monk investigated and correctly found NOT the same shape (feat-effect-engine problem, not activation-state). This is a genuine new wire-contract concept touching already-tested integrated math -- matches the operator's "adversarial review for truly large expansions, used sparingly" bar exactly, and backend itself asked for one review pass before Barbarian starts. Review dispatched, focused on: the Power Attack precedent's real behavior, regression risk to the 5 classes that already reach Computed, the "zero blast radius"/serde-default claim, the deferred-Fatigue correctness question, and rounds-per-day budget validation gaps. QA's pacing hold gave backend the bandwidth to produce this while waiting -- good use of otherwise-idle time, not scope creep.

**ADVERSARIAL REVIEW COMPLETE (2026-07-25) -- real findings, plan sent back to backend for revision before Barbarian starts.** 6 concrete, code-verified findings: (1) HIGH -- the Power Attack "precedent" only ever gates on a required-inactive state, never conditionally adds a bonus when active -- the actual new pattern has zero precedent in this codebase, contrary to the plan's framing; (2) MEDIUM-HIGH -- `compute_ability_modifiers` doesn't take `CharacterInput` at all, needs a signature change the plan didn't call out; (3) MEDIUM -- no validation ties `ability_id` to the character's real class levels, so a spoofed "rage" entry on a non-Barbarian would get free bonuses today, same false-grounding shape the Ranger review caught; (4) MEDIUM -- rounds-per-day over-budget behavior is unspecified; (5) LOW-MEDIUM -- the plan's "JSON schema addition" action item refers to something that doesn't exist (CharacterInput has no serde/JSON path at all, both fixtures and saved characters use a hand-rolled key=value parser) -- the "zero blast radius" claim holds, just for a different reason than stated; (6) LOW -- Monk's Dodge dismissal is factually wrong, Dodge is already fully grounded unconditionally in this codebase (PF1 core removed the single-target version), likely the easiest of Monk's 7 feats not a harder one. Fatigue deferral endorsed as honest, not a defect -- reviewer suggested a non-blocking diagnostic noting it isn't modeled while Rage is active. Sent to backend with the exact fix list; holding on Barbarian implementation until the plan is revised, no second review needed once addressed (same pattern as Ranger's post-review fix cycle).

**RANGER AND PALADIN'S UI FIX VERIFIED (`3fd04f25`), lead independently confirmed.** Read the full diff directly -- matches frontend's report exactly. Ran the real repo test command (`npm test`, not a raw `vitest run` which falsely reported "no test suite found" due to a config mismatch) -- 78/78 test files pass including `characterHubModel.test.ts`. `npm run typecheck` clean. New `full-except-human-level-1` support level, `levelOptions: [1,2,3,4,5]` for both classes (a deliberately conservative subset of the real level-20 cap, lead-confirmed both `MAX_SUPPORTED_{RANGER,PALADIN}_LEVEL` constants). Frontend honestly flagged not having separately live-verified Paladin's level 5 (same mechanism, reasonable to trust, not silently assumed). This closes both classes' UI-catchup gap.

**COMBAT-TIME SCOPING PLAN REVISED, ALL 6 FINDINGS ADDRESSED -- lead independently verified the revision, Barbarian implementation greenlit.** Backend re-verified every finding directly before fixing it (not just trusting the review report), with honest inline corrections left in the doc rather than silently rewritten -- same discipline as every prior correction this session. Notably went beyond the ask on finding 2: found `compute_total_saves` already layers `feat_effects::save_bonuses_from_feats` onto its base total without touching the lower-level function (`pilot_compute.rs:19591-19597`) -- **lead independently confirmed this exact code** -- and proposed mirroring that shape for Rage (`apply_rage_ability_bonuses`, called once at `compute_ability_modifiers`'s single call site) instead of threading `CharacterInput` into the widely-shared base function. Endorsed. All other findings addressed as specced: class-ownership gating required before any pillar reads an activation, over-budget produces a claim-blocking diagnostic (never silent capping), rounds-per-day values get extracted into real pure functions (mirroring the spell-math `_table` pattern), the false JSON-schema action item dropped, Monk's Dodge dismissal corrected. Fatigue's non-blocking diagnostic accepted and folded into Barbarian's first cycle. Backend starting Barbarian now: schema+fixture-grammar+rounds-extraction+ownership-gate as one RED->GREEN unit first, then the 4 pillar integrations, then tests -- reporting as pieces land. No second review needed on this pattern-setter.

**REAL SCALE SURPRISE, self-caught by backend before writing code recklessly.** Adding `class_ability_activations: Vec<ClassAbilityActivation>` as a plain (non-Optional) field on `ChosenCharacterState` breaks compilation at every existing Rust struct-literal construction site of that type -- unlike a fixture-text line (which genuinely defaults empty through `ParsedFixture::default()`), a hand-written struct literal has no such default. Backend counted precisely: 64 sites in `tests/**`, 5 in `src/rules_core/**`, 3 in the desktop crate. Correctly distinguished this from QA's Sorcerer/Cleric/Druid wave -- a pure, zero-judgment, mechanically identical one-line fix at every site, nothing to review substantively -- and proceeded without a QA pause, which was the right call.

**Collision risk flagged, checked, resolved clean.** QA had 29 test files open uncommitted (all 20 Sorcerer files plus 2 others) when backend's mechanical pass started; the lead flagged the overlap risk before backend's fix landed, but the messages crossed -- backend had already applied the fix to all 64 files by the time the warning arrived. **Lead independently spot-checked the highest-risk overlap file** (`sd25_sorcerer_level_up_explanation_coverage.rs`) directly: backend's one-line addition landed cleanly at exactly the right spot, QA's full in-progress assertion rewrite underneath it fully intact, nothing reverted or altered. No actual harm despite the timing crossover -- append-only field additions to existing literals don't collide the way overlapping line edits would. QA notified directly so they're not surprised by the extra line when they return to those files. Backend's full-workspace `cargo build --tests` running now (backgrounded, real numbers pending) before moving to Barbarian's pillar integration.

**QA'S SORCERER TEST BATCH LANDED (`76fe82da`), lead independently verified.** All 27 files QA fixed (self-authored, since backend never delivered drafts) run 100% green -- lead ran every one explicitly, not just a sample. Read the diff directly for the file most at risk of the earlier schema-collision (`sd25_sorcerer_level_up_explanation_coverage.rs`): QA's match-based Some/None rewrite is exactly the shape described (fires => must be claim_blocking; absent => assert no known-spell count is fabricated), plus an injected real off-list-spell violation (Acid Arrow) in the one test whose job is proving the blocker still fires on a genuine violation. QA's stated RED-check (temporarily disabling `ground_sorcerer_known_spells`'s explanation-push to reproduce 4 real failures) is consistent with the actual code shape at `pilot_compute.rs:15625`.

**REAL, PREVIOUSLY-UNKNOWN REGRESSION FOUND during lead's full-workspace `--no-fail-fast` verification -- not on anyone's tracked backlog.** Running the complete suite (not just lib+desktop, which is what recent verifications had scoped to) surfaced 3 legacy GE-06-era architecture tests that all hardcode `class:cleric:1` as their "genuinely unsupported class" negative-control fixture -- now broken by Cleric's own dispatch-widening (`fca4e64e`), the exact same shape backend already self-caught and fixed twice in their own lib tests, just missed here because these 3 predate the sd13/sd18/sd25 naming convention and were never in anyone's per-class file list: `tests/ge06_pilot_base_computation.rs` (`missing_fighter_chassis_input_produces_claim_blocking_diagnostic`), `tests/ge06_pilot_selected_skill_modifiers.rs` (`unsupported_chassis_blocks_skill_modifiers`), `tests/ge06_pilot_total_saves.rs` (`unsupported_chassis_blocks_total_saves`). Confirmed `table_class_id` (`pilot_compute.rs:6798`) still doesn't recognize Barbarian/Bard/Monk, so any of those works as the replacement negative control. Flagged to backend as a mechanical, no-judgment fix (same shape as their own schema-addition pass), not urgent enough to pause Barbarian for. **Everything else currently failing in a full run is expected and already tracked** -- every `sd13_cleric_*`/`sd13_druid_*` file (Cleric's 28, Druid's 17), pending QA's Cleric->Druid pass in that order -- cross-checked file names directly, not assumed.

**QA independently found the same 3 ge06_* files during their own full-workspace regression check, converging with the lead's report.** Folding them into the Cleric batch (same root cause -- Cleric's dispatch-widening) rather than as a separate fix. Backend told to stand down on the fix the lead had flagged, avoiding duplicate work -- caught before any wasted effort. QA's Sorcerer batch narrative confirms the earlier corrected framing: 28 files total (27 self-authored + confirming backend's own already-fixed `sd13_sorcerer_bloodline_class_skill_choice.rs` needed nothing further), backend never delivered drafts. QA now moving to Cleric.

**MILESTONE: BARBARIAN GENUINELY REACHES COMPUTED (uncommitted, lead independently verified end-to-end) -- first class built on the new combat-time activation-state mechanism.** Real conditional rage-execution engine replacing the old unconditional diagnostic: not-raging is valid (honest record, no block), actively-raging-in-budget applies real Strength/Constitution/Will/AC bonuses, over-budget claim-blocks. Lead confirmed directly in the diff: `table_class_id` now recognizes Barbarian (`pilot_compute.rs:6887`), the rage validation is hoisted before the single-class/Human gate (same fix shape as every prior class), and the class-ownership gate (adversarial-review finding #3) is real -- ran `non_barbarian_characters_spoofed_rage_activation_is_ignored` directly, confirms a Fighter with a spoofed rage entry stays unaffected. Also caught a subtle correctness detail: the rounds-per-day budget check correctly uses pre-rage Constitution, avoiding a circular-boost bug. Ran all 7 new tests individually (all pass), full lib suite (364/364, up from 357), full desktop suite (212/212) -- all match backend's report exactly. Full writeup in risks-and-open-questions.md. New 19-file QA-facing wave flagged (sd13/sd18_barbarian_*), backend correctly holding for QA's capacity signal rather than drafting unprompted. 6 of 27 classes now genuinely reach Computed: Fighter, Wizard, Rogue, Ranger, Paladin, Barbarian.

**QA'S CLERIC BATCH LANDED (`c578ede6`), lead independently verified.** All 23 files run 100% green -- ran every one explicitly. Spot-checked the injected "Aid" violation directly against the raw corpus (`CLASSES:Cleric=2`, exact match, confirming it's a real 2nd-level cleric spell inaccessible at level 1). Full workspace `--no-fail-fast` run confirms only the expected, already-known Druid (17-file) and Barbarian (20-file, close to backend's 19 estimate) waves are red -- nothing else. QA now on Druid.

**IRONIC SELF-INFLICTED REGRESSION, already caught and fixed by backend before the lead even flagged it.** Barbarian's own dispatch-widening broke the same 3 `ge06_*` tests a SECOND time (this time because Barbarian, not Cleric, became the "unsupported" fixture that stopped being unsupported) -- found during the lead's full-workspace verification pass, but backend had already proactively swapped the negative control a third time (Cleric -> Barbarian -> Monk, `class:monk:1`, confirmed still absent from `table_class_id`), sitting uncommitted in the tree. Lead independently verified all 11 tests pass with the Monk swap. Good sign the swarm's self-correction discipline holds even for a fix breaking its own prior fix.

**QA'S DRUID BATCH LANDED (`5eae17d7`, 16 of 17 files), lead independently verified all 16 green.** Same match-based pattern as Sorcerer/Cleric; injected "Barkskin" violation spot-checked against the raw corpus earlier this session (`CLASSES:Druid,Ranger=2`, exact match). This commit also swept in (correctly, all legitimate) backend's uncommitted `durability.rs`/`ge06_*`/`v06_durability.rs` Monk-swap fixes -- **lead independently re-ran all 4 of those files too**, 23/23 tests pass.

**REAL PRODUCTION BUG FOUND (QA, mid-Druid-batch) -- Druid's LevelUpPlan silently drops real spell-math grants, lead independently confirmed.** `is_druid_pillar_id` (`level_up/druid.rs:100-101`) only matches `class_chassis.druid.`/`class_feature.druid.` prefixes -- it does NOT match `class_spell.druid.`, which Druid's seventh slice (`dda46d4a`) started pushing real explanations onto (`class_spell.druid.daily_preparation`, `class_spell.druid.total_spells_per_day.spell_level_N`). **Lead confirmed these are genuinely level-varying facts**, not flat constants: `druid_base_spells_per_day_table` shows spell_level_0 rising 3->4 from level 1->2. Every Druid level-up today silently omits real, correct spell-math grants from the plan shown to the player -- a real, user-facing gap, not a stale-assertion issue. QA correctly stayed in their lane (production code, not `tests/**`) and held the one affected file (`sd25_druid_level_up_explanation_filter_audit.rs`) rather than patching around it, continuing the rest of the batch meanwhile. Two other apparent gaps in the same file (bonus_spells_per_day/spell_save_dc at spell_level_1) are correctly NOT bugs -- both flat functions of Wisdom alone, same "granted at creation" exclusion category as 2 other ids already in the file; QA folding those into the exclusion list themselves. Routed the real fix to backend (widen the filter, with a proper RED->GREEN test first) -- QA's own audit-methodology offered as a starting point for the RED case. This is the same class of bug as the historical SD-24 Wizard level-up-filter finding, caught this time before it shipped further.

**BUG FIXED AND VERIFIED (`55a856c8` backend + `dfbb0810` QA), both lead independently verified -- turnaround was minutes, not a cycle.** Backend widened `is_druid_pillar_id` to also match `class_spell.druid.`, cited the exact same reasoning the lead confirmed (real level-varying facts, SD-24 Wizard bug shape), and pointed to QA's own audit test as the RED proof rather than duplicating a new one. QA's follow-up commit completed Druid's 17th file: folded the 2 non-bug ids into the existing exclusion category (verified constant across levels 1-15), narrowed the diagnostic-leak assertion to its still-true claim, and deliberately did NOT widen the audit to formally prove the gap a second time, avoiding duplicating backend's fix -- clean division of labor. **Lead independently ran the specific test** (`sd25_druid_level_up_explanation_filter_audit.rs`, all 5 pass) and a full workspace `--no-fail-fast` sweep: only the expected 20-file Barbarian wave remains red. One transient false-failure encountered mid-sweep (`sd20_levelup_druid.rs`'s capstone test failed to compile) -- traced immediately to backend's live, uncommitted, in-progress Inspire Courage work (two not-yet-defined function names), a known artifact of testing a shared checkout during active edits, not a real regression. Druid's full test batch (17 of 17) and the real level-up bug are now completely closed.

**Commit-boundary crossover on the Druid batch, self-caught by both sides, no actual damage.** QA's Druid commit (`5eae17d7`) unintentionally swept in backend's concurrently-staged Barbarian/ge06/durability work via a shared-index race (same class of hazard as an earlier SWARM_STATUS.md crossover this session) -- QA flagged it immediately, held the push, and correctly declined to do anything destructive (no reset) without checking first. Both QA and backend independently re-verified the combined commit's content against the actual committed HEAD (all Barbarian/ge06/durability tests still passing) before the lead's own verification (already done, see above) even reached them. Confirmed clean, no history rewrite needed -- exactly the right handling of a shared-checkout race.

**Bard scope addition endorsed -- lead independently verified the ClassMeta claim before agreeing.** Backend caught a real dead-code risk before writing Inspire Courage's pillar integration: Bard is deliberately excluded from `table_class_id` (same bucket as Monk), so `compute_combat_baseline` returns a claim-blocked zero today for every Bard input -- adding "Inspire Courage boosts melee_attack_bonus" without first widening `table_class_id` for Bard would be genuine dead code, violating the no-stub-mvp doctrine. **Lead confirmed directly**: `class_tables.rs:79`'s `ClassMeta` for `ClassId::Bard` already exists and matches (3/4 BAB, poor Fort, good Ref/Will, hit die 8) -- same safe-to-widen shape as Barbarian's pre-existing data. Endorsed folding the widening + gate-ordering hoist into this slice rather than treating it as a separate epic, mirroring Barbarian's own cycle exactly. Bard still won't reach full `Computed` from this slice alone -- the separate spontaneous-spell-posture diagnostic stays permanently blocking, same shape as Sorcerer/Cleric/Druid's own permanent burdens -- backend keeping that framing honest.

**BARD'S INSPIRE COURAGE VERIFIED (uncommitted at check time), lead independently confirmed end-to-end.** `table_class_id` recognizes Bard, gate-ordering fix hoisted correctly (mirrors Barbarian exactly), class-ownership validation confirmed via `non_bard_characters_spoofed_performance_activation_is_ignored`. Lead independently traced reachability: `has_supported_class_chassis` was already widened generically in a prior slice, so the attack-bonus integration is genuinely reachable (not dead code) once a Bard satisfies the same fixed-loadout precedent every other class uses. Ran all 4 new tests individually (pass), full lib 368/368, full desktop 212/212 -- all matching backend's report exactly. Honest framing on weapon-damage/saves-vs-fear (documented as un-integrable given no weapon-damage total or save-vs-effect-type sub-category exists anywhere in this codebase, not silently done less).

**QA CAUGHT ONE MORE REAL DRUID REGRESSION AFTER THE "FULLY CLOSED" CALL -- self-caught by re-running the full regression before pushing, not trusting the earlier "done" claim.** `tests/sd20_levelup_druid.rs`'s capstone test (outside the sd13/18/25 naming convention, missed by everyone including the lead) asserted "no grounded druid data exists beyond level 15" -- true when written, false now that backend's `is_druid_pillar_id` fix correctly surfaces `class_spell.druid.total_spells_per_day.*` rising past level 15 (Druid's spell math reuses Cleric's real, ungated, full 1-20 table, unlike the class-feature chassis which really is capped at 15). Fixed (`4e246e3b`): updated the assertion to expect real spell grants beyond level 15 while confirming no `class_chassis.druid.*`/`class_feature.druid.*` leaks there. **Lead independently ran the specific test and the full 4-test file** -- all pass. Druid is now genuinely, completely closed (18 of 18 test files total, counting this one). Good discipline from QA: re-verifying a "done" claim by actually re-running the suite rather than trusting the prior report, exactly the standard this swarm holds itself to.

**Backend self-caught a real doctrine-audit regression before committing -- a word wrap broke a literal substring match, lead independently verified the fix.** Bard's recognition-record text wrapped "packet placeholder" across two physical lines; `sd24_multiclass_dispatch_audit.rs`/`sd24_wired_integration_audit.rs` both do a literal line-based grep for that exact phrase to distinguish the reviewed anti-fabrication idiom from a genuinely new stub marker, so the wrap made it look like an unreviewed placeholder had appeared. Fixed by rejoining the phrase onto one line (no semantic change) -- **lead independently confirmed** the phrase is back on one line (`pilot_compute.rs:18937`) and ran both audit files directly: 5/5 each, clean. Worth remembering for future edits: any "packet placeholder" recognition sentence must stay on one source line or this exact audit trips.

**QA's fresh full-workspace survey found the real next-wave scope: 20 Barbarian files (expected) + 18 Bard files (expected, landed while QA was mid-Druid) + the 2 audit files (already fixed by backend, confirmed to QA directly, not something QA needs to touch).** 40 files total once the audits are excluded (38 real QA work: Barbarian's 20 + Bard's 18). Sequencing confirmed: Barbarian first (older wave), then Bard, same rigor as every prior batch.

**BARD'S INSPIRE COURAGE COMMITTED AND PUSHED (`0374b96a`), lead independently re-verified against the actual committed state.** Second class through the combat-time activation-state pattern -- proves it holds on a genuinely different pillar shape (attack-bonus only; damage and saves-vs-fear had no integration point to touch, honestly documented rather than silently attempted). **Lead re-ran the full lib suite directly against the committed HEAD**: 368/368, matches exactly. Backend correctly left `tests/sd13_barbarian_level1_chassis_baseline.rs` untouched on seeing QA's concurrent edit in the shared tree -- good discipline avoiding a collision. Final workspace count: 25 Barbarian files + 32 Bard files (`tests/sd13_bard_*`/`sd18_bard_*`/`sd25_bard_level_up_explanation_coverage.rs`) remain red, both queued for QA, nothing else. Backend redirected to new engine work (QA owns both test waves) -- options offered: Monk's scoping pass (needed before any Monk code, per the combat-time plan's own note that its 7 bonus feats are a feat-effect-engine problem) or picking up whichever of Sorcerer/Cleric/Druid's remaining work is cheapest next; backend's call given they have the most context on real cost.

**QA'S BARBARIAN TEST BATCH LANDED (`5ba54695`, 20 of 22 files), lead independently verified all 22.** Bigger rewrite than the spell-posture classes: Barbarian is full-BAB (`base_attack_bonus` genuinely `1 * level`, wired into the real integrated field, not just conditionality), and the old unconditional `rage_execution.unsupported` diagnostic is retired outright for a real conditional engine. **Lead independently confirmed the injected violation's math**: fixture's Constitution modifier +3, budget = 4 + 3 + 2*(1-1) = 7, injected `rounds_consumed_today: 8` genuinely exceeds it. Ran all 20 rewritten files plus the 2 already-clean ones (`sd13_barbarian_rage_power_slots`, `sd25_barbarian_level_up_explanation_filter_audit`) -- 22/22 pass. QA's own RED-check self-correction (a string-replace patch hit the wrong of two near-identical rounds-per-day checks, caught immediately when the failure didn't match expectations, redone precisely) is exactly the kind of self-verification this session depends on. QA now moving to Bard's 32-file wave. Meanwhile backend independently picked up Monk's Dodge exception (no separate scoping doc needed -- the earlier adversarial review already scoped it: Dodge's mechanical effect is already grounded elsewhere in the codebase, so a Monk whose bonus-feat choice is genuinely-active Dodge isn't blocked on it, while the other 6 restricted-list feats stay unconditionally blocked pending their own future engine work) -- in progress, uncommitted.

**NEW HAZARD CLASS FLAGGED (backend) -- a raw file-content lost-update race, distinct from every git-index race this session has hit before.** Backend appended a new test module to `pilot_compute.rs`, got the tool's routine "file modified on disk" note, but seconds later the whole addition had vanished (diff dropped from 176 to 64 insertions) -- a genuine lost-update: some other process's whole-file write landed on top of backend's, based on a copy taken before backend's edit existed. Not a git-staging issue at all (no `git add`/`git commit` involved) -- a raw OS-level race on N processes doing naive read-whole-file/write-whole-file edits on one shared inode with no locking. Backend caught it via the diff-size drop (not by trusting the tool's routine warning as noise), re-applied, verified twice via grep before trusting it stuck. **Lead independently verified the re-applied state**: all 3 new `monk_bonus_feat_dodge_closure_tests` pass, full lib suite 371/371 (up from 368). No data loss beyond a few seconds of backend's own work; nothing else appears affected. Worth remembering as a standing risk for the rest of this shared-checkout session -- re-verify via grep/diff-size after any "modified on disk" warning rather than assuming a routine retry fully restored the intended state.

**MONK'S DODGE FIX COMMITTED AND PUSHED (`18920c3d`), lead independently re-verified against the actual committed state.** A genuinely additive, zero-regression slice -- all 16 pre-existing Monk test files pass unchanged (none of their fixtures choose Dodge specifically), so no new QA-facing wave from this one. **Lead re-ran the full lib suite (371/371) and spot-ran 3 of the 16 Monk files directly** -- all clean. Monk still does not reach `Computed` overall: `table_class_id` was not widened for Monk this slice (base-attack/base-save/fast-movement stay standalone), and 6 of its 7 restricted-list feats remain unconditionally blocked pending their own future engine work. Backend redirected to pick whichever of Sorcerer/Cleric/Druid's remaining permanent blockers (bloodline/domain-powers/animal-companion) seems most tractable after a quick scope check, or continue Monk's other 6 feats if none stand out as clearly cheaper -- backend's own call given they have the most current context. QA confirmed actively working Bard's 32-file wave (`tests/sd13_bard_level1_spell_baseline.rs` mid-edit).

**SECOND MONK CLOSURE LANDED (`b1a453a1`, Catch Off-Guard + Throw Anything), lead independently verified against the raw PCGen corpus, not just the code.** Backend's scope check on Sorcerer/Cleric/Druid's remaining blockers found no cheap angle on any of the three (all genuinely need new subsystems) -- but re-scoping Monk's other 6 feats turned up a second real, zero-engine closure, same shape as Dodge: both feats' entire PF1 benefit applies only to improvised/splash weapons, and this codebase's bounded combat-baseline always computes a real, proficient Longsword attack, never improvised or splash. **Lead independently cross-checked both feats' exact benefit text against `cr_feats.lst`** (the raw PCGen corpus): "Catch Off-Guard... You do not suffer any penalties for using an improvised melee weapon. Unarmed opponents are flat-footed..." and "Throw Anything... You do not suffer any penalties for using an improvised ranged weapon. You receive a +1 circumstance bonus on attack rolls made with thrown splash weapons" -- byte-for-byte match, no missed clause. Confirmed via grep that "improvised"/"splash" appeared nowhere in `pilot_compute.rs` before this commit, so the "never modeled anywhere" claim holds. **Lead ran all 6 Monk bonus-feat tests (3 Dodge + 3 new), full lib (374/374), both doctrine audits (10/10, re-checked given the earlier line-wrap lesson), 3 spot-checked pre-existing Monk files, and the full desktop suite (212/212)** -- all clean, matching backend's report exactly. 3 of Monk's 7 restricted-list feats now genuinely closed (Dodge, Catch Off-Guard, Throw Anything); the remaining 4 (Combat Reflexes, Deflect Arrows, Improved Grapple, Scorpion Style) each need their own resolution engine, same bigger-lift category as Sorcerer/Cleric/Druid's blockers. Backend's own call on what's next -- mild lead preference toward Combat Reflexes (an AoO-counting engine) as the most self-contained of the 4, not a strong steer.

**REAL INFLECTION POINT REACHED, lead confirmed and analyzed the ROI before backend commits to a target.** Backend correctly checked whether the same "vacuous under this bounded scope" reasoning could close any of Monk's remaining 4 feats too -- it can't, and the distinction is real: Catch Off-Guard/Throw Anything's trigger (wielding an improvised/splash weapon) is a property of the character's own input, provably always-false here; the remaining 4 feats' triggers (an opponent provoking an AoO, an opponent's ranged attack, a grapple attempt, a successful unarmed hit) are all EXTERNAL EVENTS this headless snapshot-computation model has zero representation of at all -- **lead independently confirmed via grep**: no opponent-action/incoming-attack/grapple-resolution concept exists anywhere in `pilot_compute.rs`/`character_input.rs`. This is a genuine "the subsystem doesn't exist" gap, same scale as Sorcerer/Cleric/Druid's remaining blockers, not a session-sized incremental slice.

**Lead surfaced a real ROI asymmetry before backend picks a target**: verified directly (`pilot_compute.rs:15213-15269`) that Sorcerer's bloodline-power diagnostic is genuinely its ONLY remaining permanent blocker -- everything else (BAB/save/spell posture) is already real. The same shape almost certainly holds for Cleric's domain-powers and Druid's animal-companion blockers. That means finishing any ONE of those three subsystems makes a full additional class reach genuine `Computed` -- versus even a fully-built Combat Reflexes, which wouldn't move Monk's overall status at all (Monk still needs `table_class_id` widening plus all 3 other remaining feats regardless). Flagged this asymmetry to backend as input to their decision, not a directive -- backend has better context on relative build cost across all 4 candidates. Whatever's picked, a scoping doc comes first (mirroring the combat-time-activation-state doc), then one adversarial review before any code, same pattern as every genuinely large expansion this session.

**MAJOR CHECKPOINT -- ZERO FAILURES ACROSS THE ENTIRE WORKSPACE, lead independently confirmed with a full `--no-fail-fast` sweep.** QA landed Bard's test-cleanup wave (`6dcec5ba`, 22 files -- 30 Bard files total, close to the earlier 32-file estimate) while this cycle's analysis was in flight. **Lead ran all 22 files explicitly** (100% pass) plus a complete workspace sweep: `cargo test --workspace --no-fail-fast` returns **zero FAILED lines anywhere** -- every test-cleanup wave from this entire class-breadth epic (Sorcerer's 27, Cleric's 23, Druid's 17, Barbarian's 22, Bard's 22, plus Monk's 2 closures) is now fully closed and green simultaneously, for the first time this session. Full lib (374/374) and desktop (212/212) suites independently reconfirmed clean. This is a genuine, verified clean slate: nothing left in the tree references a retired diagnostic or stale assertion anywhere in the workspace. QA's next move (Monk's own test-cleanup, if any, or standing by) and backend's choice of next target (Sorcerer/Cleric/Druid's blocker vs. Monk's remaining feats) are the only open threads.

**BACKEND ANSWERED THE ROI QUESTION WITH A REAL SCOPING DOC (`sorcerer-arcane-bloodline-closure-scoping.md`), lead independently verified and endorsed.** Compared all three remaining permanent blockers' actual cost: Sorcerer's Arcane bloodline wins -- both of its two named pieces (Bloodline Arcana's metamagic-DC precondition, Arcane Bond's cast-a-spell benefit) are **provably vacuous**, the same "structurally can never trigger" shape as Monk's Catch Off-Guard/Throw Anything, not merely unmodeled. **Lead independently re-verified both claims directly**: `SpellSelection` (`character_input.rs:71-80`) has exactly 3 fields (spell_id/source_class_id/acquisition_mode) with no metamagic concept anywhere; grepped for any spell-casting-resolution function across `pilot_compute.rs` -- zero hits for any class, confirming "this codebase computes stats, never resolves an actual cast" holds universally, not just for Sorcerer. Cleric's domain-powers and Druid's animal-companion paths were both checked and correctly ruled out as genuinely more expensive (Cleric's Touch of Good needs a new "which roll type" schema concept plus real HP-healing application; Druid's animal-companion needs a full companion stat-block subsystem). Backend's remaining real work is small: recognize the Arcane Bond identity choice (mirrors the existing bloodline-choice pattern exactly) and name the flat 1/day budget informationally -- **lead answered the one open design question directly** (recognition-only, no consumption-tracking schema, since the benefit it would gate can never be exercised regardless -- tracking it would add complexity with no payoff). Given this is a single additive choice-constant (not a new schema/mechanism like `ClassAbilityActivation`) and the two riskiest claims are now independently verified, **endorsed without spinning up a full adversarial review** -- proportionate to the smaller footprint, unlike the combat-time-activation-state plan. Backend proceeding with the 5-step approach, gate-ordering check included.

**MILESTONE -- SORCERER GENUINELY REACHES COMPUTED (`d6067603`), the first class this session where a previously partial-progress class closes ALL THE WAY, lead independently verified end-to-end.** Backend implemented the endorsed 5-step plan exactly. **Lead confirmed directly in code** (`pilot_compute.rs:15283-15389`): the `arcane_bond_and_bloodline_progression.unsupported` diagnostic now only fires in the `else` branch of `recognized_arcane_bloodline && recognized_arcane_bond_name.is_some() && bonus_spells_and_feats_correctly_absent` -- a valid Arcane-bloodline+Arcane-Bond Sorcerer below level 3 never trips it, confirmed the whole block sits before the single-class/Human gate (correct ordering, matches every prior class). **Lead ran both "reaches Computed" tests directly** -- they assert real `HeadlessReceiptStatus::Computed` through the actual production `build_pilot_headless_receipt` pipeline, not a synthetic bypass. All 4 new tests pass, full lib suite 378/378 (exact match), both doctrine audits clean, 2 pre-existing Sorcerer files spot-checked clean, level 3+ confirmed still blocking on bonus grants in code. Backend also self-caught a message-text regression mid-verification (an existing test's substring check) by actually running the file rather than trusting the new tests alone -- exactly the discipline this session depends on. **7 of 27 classes now genuinely reach Computed**: Fighter, Wizard, Rogue, Ranger, Paladin, Barbarian, Sorcerer. Validates the scoping-doc-then-endorse pattern works for a full-closure case, not just dispatch-widening slices. Backend deciding next between Cleric/Druid's bigger blockers or Monk's remaining 4 feats.

**Frontend's Barbarian UI update in progress (uncommitted at check time), matches independent analysis.** Diff shows `supportLevel: 'full'` (not `full-except-human-level-1`), `levelOptions: [1, 2, 3]` -- consistent with frontend's earlier code-read (Barbarian never appears in `hybrid_level1_class`'s match arms) and the lead's own independent confirmation of the same fact two cycles ago. Will independently verify once committed.

**BARBARIAN'S UI FIX COMMITTED AND VERIFIED (`d020a5e8`), lead independently re-ran the real test suite against the committed state.** `supportLevel: 'full'`, `levelOptions: [1,2,3]` confirmed exactly in the committed file. Frontend live-verified through the real dev build: fresh Human Barbarian 1 (default, not-raging) and fresh Dwarf Barbarian 1 both reached Computed/Saved; leveled the Dwarf up to level 2, and when an X-server contention hiccup interrupted the level-3 UI flow mid-attempt, correctly fell back to reading the on-disk `authoritative_character_input.txt` directly to confirm level 2's persistence rather than giving up. **Lead ran `npm test` (78/78) and `npm run typecheck`** against the real committed HEAD -- both clean. This closes Barbarian's UI-catchup gap; Fighter, Wizard, Rogue are now joined by Barbarian as the only classes with unconditional `full` support (no race/level carve-out at all).

**Backend independently re-verified Cleric's domain-powers blocker before picking a next target -- confirmed no hidden Sorcerer-style shortcut exists, lead independently cross-checked both domain powers against the raw corpus.** Touch of Good (Good domain) and Rebuke Death (Healing domain) were both checked directly against `cr_abilities_class.lst`: Touch of Good genuinely needs a new "apply a bonus to whatever rolls happen in the next round" mechanism (this codebase already computes attack rolls/skill checks/saves as real values, so the precondition is representable in principle, just not yet built) -- **lead caught one small inaccuracy in backend's description** (the raw text applies the bonus to attack rolls AND skill checks AND ability checks AND saving throws simultaneously for the round, not a player's choice of one) but this doesn't change the substantive conclusion. Rebuke Death needs an explicit dice-roll (1d4 + level bonus) applied to a real HP total -- a randomness/dice-resolution capability this codebase has never had for anything, confirmed exactly as described. This is genuinely NOT the same "provably vacuous" shape as Sorcerer's blocker -- Cleric, Druid (companion stat-block subsystem), and Monk's remaining 4 feats (opponent-interaction engines) are all now confirmed comparable in scale, each needing real new mechanism work. Backend directed to write one comparative scoping doc across all remaining options (mirroring the Sorcerer/Cleric/Druid diagnostic-level comparison, one level deeper now comparing mechanisms) before committing to which to build -- likely warrants an adversarial review given the scale, same as the combat-time-activation-state plan.

**Comparative scoping doc landed (`next-mechanism-comparative-scoping.md`), a genuine judgment call flagged explicitly rather than decided silently -- adversarial review dispatched.** Headline finding: Cleric's Touch of Good (Good domain), IF self-scoped (the cleric touches herself, not an ally), needs no new mechanism at all -- reuses the exact `ClassAbilityActivation` pattern Rage/Inspire Courage already proved, since the schema has no `target` field. Backend correctly flagged this as a real design choice, not a free discovery: Touch of Good's real PF1 use is buffing an ally mid-combat, and self-scoping it is a deliberate narrowing, same shape as Inspire Courage's "attack bonus only" narrowing but different in character (backend's own words: "you touch yourself with an ally-buff power reads differently"). Also proposes splitting Cleric's single domain-powers diagnostic into two independent ones (Good vs Healing) so a Good-only Cleric could reach `Computed` while the existing Good+Healing fixture stays honestly `Blocked` on Rebuke Death alone -- mirroring Sorcerer's own bloodline/spell-posture independence. Rebuke Death, Druid's companion subsystem, and Monk's 4 remaining feats all reconfirmed as genuinely bigger (a second-creature/dice-roll/opponent-interaction concept none of which exists anywhere in the schema), though backend explicitly asked whether Rebuke Death might have its own hidden Arcane-Bond-shaped "recognize the choice, leave the effect unresolved" angle worth double-checking before ruling out for good. **Given this is a genuine representational-honesty judgment call the team explicitly asked for a second opinion on (not just a fact-check), dispatched an adversarial review** focused on all three questions: is the self-scoping honest or misleading, does the diagnostic split risk a false-Computed bug the way the original Ranger widening did, and is Rebuke Death correctly ruled out. Backend holding on any code until the review lands.

**ADVERSARIAL REVIEW COMPLETE -- found a real gap in the diagnostic-split plan, confirmed the other two calls, sent back to backend for revision.** All three code claims independently re-verified by the lead directly. **(1) Self-scoping: proceed, but the review corrected the doc's own analogy** -- Touch of Good narrows by TARGET (self instead of the RAW-primary ally-buff use), not by FACET the way Inspire Courage did (attack bonus vs damage, both real self-uses) -- a different, more novel kind of narrowing that needs explicit "self-application only, ally-targeting unmodeled" messaging, not implied full closure. **(2) Diagnostic split: a real gap, not just messaging.** Lead confirmed directly: the current diagnostic (`pilot_compute.rs:17420-17436`) is unconditional for ANY cleric regardless of domain -- splitting it without a catch-all for unrecognized domains (Fire, Travel, etc.) would let those clerics slip to `Computed` with domain powers entirely unmodeled, the exact Ranger-review bug class. **Lead also confirmed**: the domain-choice recognition (`pilot_compute.rs:17594`) only fires when BOTH Good AND Healing are selected together -- a Good-only Cleric doesn't hit it at all today, so the doc's "Good-only reaches Computed on Touch of Good alone" claim needs 2 more real pieces (widen the recognition to a Good-only case, address the separately-named "domain spell-list contents" sub-burden) that weren't scoped. **(3) Rebuke Death: correctly ruled out, confirmed** -- the Arcane Bond precedent genuinely doesn't transfer (Arcane Bond's benefit is provably vacuous for every input this system will ever compute; Rebuke Death's heal amount is real and merely not-yet-representable, so recognition-only closure would falsely claim more than is true). Sent the full findings to backend with the 3 required additions before any code -- proceed once revised, no second review needed on this pattern-setter.

**PLAN REVISED, ALL 3 GAPS ADDRESSED -- lead traced the pseudocode's edge cases directly and greenlit the build.** Backend's revision (recorded in-doc as "Revision after adversarial review"): (1) messaging now states explicitly that only self-application is grounded, ally-targeting is unmodeled since no target-creature entity exists; (2) the diagnostic split now preserves a real catch-all -- **lead traced every edge case in the pseudocode directly**: no-domain-chosen, an unrecognized domain (Fire/Travel/etc.) chosen, Healing-only-no-Good, and Good+an-unrecognized-domain all correctly fall through to the UNCHANGED original unconditional diagnostic; only Good-alone or Good+Healing hit the new conditional path, and Good+Healing still correctly pushes Rebuke Death's own separate claim-blocking diagnostic (stays honestly `Blocked` overall, matching today's fixture) -- confirmed `GOOD_DOMAIN_SELECTION`/`HEALING_DOMAIN_SELECTION` constants exist exactly as referenced; (3) domain spell-list contents gets a new permanent non-blocking diagnostic (mirrors Bard's "6 other performances, non-blocking" idiom) rather than building a real spell list this cycle, keeping the slice proportionate. Greenlit to build -- no second review needed, same as Ranger's post-review cycle.

**MILESTONE -- CLERIC'S GOOD-ONLY POSTURE GENUINELY REACHES COMPUTED (`b98a20d7`), lead independently verified against the exact reviewed pseudocode AND re-confirmed against the real committed HEAD.** Backend implemented precisely as revised, and went one step further than reviewed: Touch of Good gets real `ClassAbilityActivation`-based activation-state gating (not "Good chosen = always active"), correctly mirroring Rage/Inspire Courage's limited-use idiom rather than treating it as passive. **Lead read the actual code directly** (`pilot_compute.rs:17440-17540`): the catch-all/split logic matches the traced pseudocode exactly, Good+Healing still pushes Rebuke Death's own separate claim-blocking diagnostic, the non-blocking domain-spell-list-contents diagnostic is present. **Lead ran the reaches-Computed test directly**: asserts real `HeadlessReceiptStatus::Computed`, bonus math confirmed exact (`max(1/2,1)=1` matching the raw corpus formula). All 7 new tests pass (12 total in the module), full lib 384/384, desktop 212/212, both doctrine audits clean. **Full workspace sweep**: 35 unique failing tests, all confirmed Cleric-shaped (zero unrelated regressions) -- close to backend's own 34-test estimate. This is the second full-closure milestone this session (after Sorcerer) and the first time a genuine representational-honesty judgment call (self-scoping) survived an adversarial review and shipped correctly on the first attempt post-revision. Real Cleric test-cleanup wave (~22 files, 35 tests) assigned to QA. 8 of 27 classes now genuinely reach Computed: Fighter, Wizard, Rogue, Ranger, Paladin, Barbarian, Sorcerer, Cleric.

**Druid animal-companion scoping doc landed (`druid-animal-companion-closure-scoping.md`), lead endorsed the scoping approach without a full adversarial review, held a data-accuracy bar instead.** Third time this exact "provably vacuous pieces + one small genuinely-new addition" shape has held up (after Arcane Bond, Touch of Good's mechanism-level pieces). Backend's headline: Link (the Handle-Animal-check exemption) and Share Spells (casting a spell on the companion) are both provably vacuous -- **lead independently confirmed** zero computed Handle Animal check exists anywhere (only 2 hits, both Bard display-text prose), and Share Spells reuses the same "no spell-casting-resolution engine anywhere" finding already verified for Arcane Bond. What's new is smaller than the original comparative doc feared: since the companion is a wholly separate creature whose stats never touch the Druid's own integrated totals, this needs no new `CharacterInput` schema field at all -- just standalone derived explanation records, lower integration risk than Touch of Good's three-pillar touch. Backend explicitly planned a catch-all-preserving structure (domain-bond/unrecognized nature-bond falls through unchanged), applying the Cleric review's lesson proactively. **Lead's call**: endorse the scoping approach directly (no full review agent needed -- unlike Touch of Good, there's no self/ally-style representational-honesty ambiguity here), but flagged a real, unresolved factual gap: attempted to verify Wolf's exact companion stat block (Str/Dex/Con/BAB/saves/natural armor) against the raw PCGen corpus and could not cleanly reconstruct it from the relative `BONUS:STAT` deltas alone -- specifically doubted backend's cited "Str 15" against the lead's own recollection of "Str 13" from the published table (neither confirmed with certainty). Directed backend to cross-check the exact numbers against 2 independent primary sources before hardcoding them, same rigor as every spell list this session, rather than trusting either the doc or the lead's memory.

**QA'S CLERIC TOUCH-OF-GOOD TEST BATCH LANDED (`3da1cd35`, 22 files), lead independently verified all 22 and pushed.** Same pattern as every prior wave: the standard Good+Healing fixture's assertions updated to expect the new `rebuke_death.unsupported` id instead of the old generic `domain_powers.unsupported`, plus the non-blocking domain-spell-list-contents note; one call site correctly kept the old id since that specific test strips all domain selections and genuinely hits the catch-all. **Lead ran all 22 files explicitly** -- 100% pass -- then a complete workspace `--no-fail-fast` sweep: **zero failures anywhere in the entire workspace**, the cleanest state this session has reached. QA's commit was sitting unpushed in the shared checkout (local-only); lead pushed it after verification. Backend now actively building the Druid animal-companion work (`pilot_compute.rs` in progress, uncommitted).

**WOLF'S COMPANION STATS VERIFIED FOR REAL -- the lead's doubt was correct, backend re-checked against 3 independent sources and found the real numbers.** "Str 15" was wrong; the real ability scores (all 3 sources -- d20pfsrd, aonprd.com, and the PCGen corpus citing Core Rulebook p.56 -- agree exactly) are Str 13, Dex 15, Con 15, Int 2, Wis 12, Cha 6. Backend also caught and resolved a genuine 2-source disagreement on natural armor/Trip (d20pfsrd said +1 AC with Trip gated behind later advancement; aonprd.com AND the PCGen corpus both said +2 AC with Trip present from level 1) by going with the 2-of-3 majority backed by the stronger citation (an actual page reference), not splitting the difference or picking arbitrarily. **Lead independently re-verified the BAB/save progression formulas directly against `cr_classes_companion.lst`**: `BASEAB|classlevel*3/4`, `Fort/Ref|classlevel/2+2`, `Will|classlevel/3` -- exact match to backend's citation, and the level-2 companion math (BAB+1/Fort+3/Ref+3/Will+0) checks out precisely. Final verified stat line: HD 2 (d8), BAB+1, Fort/Ref+3/+3, Will+0, natural armor+2, bite 1d6+1 with Trip, attack+2. Backend building against this now -- no further verification gap remaining before code.

**MILESTONE (`9aeec493`, committed and re-verified against real HEAD) -- DRUID'S ANIMAL-COMPANION POSTURE GENUINELY REACHES COMPUTED at level 1.** Backend implemented exactly as scoped: recognition gate widened to `animal_companion_chosen && druid_level == 1`, catch-all preserved unchanged for every other case (domain-bond, unrecognized/absent nature bond, any other Druid level) -- **lead read the actual `if`/`else` structure directly** (`pilot_compute.rs:18513-18710`), confirmed the whole block sits before the single-class/Human gate (correct ordering), confirmed `wolf_companion_hit_dice`'s `debug_assert_eq!(companion_level, 1, ...)` correctly bounds this slice to the only verified case rather than silently extrapolating a formula. **Lead ran all 4 new tests directly**, including the reaches-Computed test asserting real `HeadlessReceiptStatus::Computed` with exact stat matches (attack bonus +2, Fortitude +3, AC 12, all 8 companion stat-block records present) and the honest non-blocking advancement-absent diagnostic. Full lib 388/388, desktop 212/212, both doctrine audits clean. **Backend fixed the flagged stale doc comment (both spots) before committing** -- **lead independently re-confirmed the fix landed correctly** in the real committed code, re-ran the full lib suite against the actual HEAD (388/388, exact match), and ran a full workspace sweep independently: exactly the 4 tests backend named (`druid_level1_base_attack_and_saves_do_not_disturb_existing_pillars_or_blockers`, `druid_level1_fabricates_no_spell_math`, `druid_level1_stays_blocked_on_animal_companion_execution_burden`, `druid_level1_stays_blocked_on_prepared_divine_spell_posture_burden`), zero unrelated regressions, level 2+ tests entirely unaffected. This is the third genuine full-closure milestone this session (after Sorcerer, Cleric) and the second time "endorse the scoping approach directly, skip the full review" proved sound for the vacuous-pieces-plus-one-small-addition shape. **9 of 27 classes now genuinely reach Computed**: Fighter, Wizard, Rogue, Ranger, Paladin, Barbarian, Sorcerer, Cleric, Druid (the reaches-Computed test confirms both the animal-companion closure AND a valid prepared-spell posture together get a level-1 Wolf-companion Druid to real `Computed`). Small 4-test wave assigned to QA. Session total: 4 full mechanisms built (Barbarian Rage, Bard Inspire Courage, Sorcerer Arcane Bond, Cleric Touch of Good) plus Druid's animal companion as a 5th, plus 3 Monk feat closures (Dodge, Catch Off-Guard, Throw Anything) -- all landing on the same "verify against real sources, preserve an honest catch-all, name what's still out of scope" discipline.

**QA'S DRUID TEST BATCH LANDED (`c4ffa208`), lead independently verified -- entire workspace is now genuinely clean.** RED-checked properly (forced the closure branch to fall through to the old catch-all, reproduced 3 real failures across 2 files, confirming the fix is tied to real branching logic, not just an id/count edit). **Lead ran a full workspace `--no-fail-fast` sweep**: zero failures anywhere. **Corrected a real misunderstanding from backend**: after finishing Druid, backend believed there were still "5 separate test waves in flight or queued" (Barbarian/Bard/Cleric/Druid plus whatever QA was mid-way through) and offered to take one off QA's plate -- but Barbarian's 25-file wave, Bard's 32-file wave, and Cleric's 34-test wave were all already cleared by QA earlier this session; only Druid's small 4-test wave was genuinely still outstanding, and QA had just finished it by the time backend asked. Corrected backend's picture of the real current state before they picked up stale work. Also endorsed backend's own call NOT to force Monk's Combat Reflexes (a real, correct engineering judgment: even fully built, there's no AoO-count total anywhere to apply it to, so it wouldn't move Monk's diagnostic at all -- not a cop-out, a genuine dead-end correctly identified twice now). Redirected backend toward either APG/ACG's untouched class-skill-lists (a fresh, unexplored bucket that might have its own early cheap win) or Bard's own spell posture (a known, proven shape) -- backend's call.

**MILESTONE (`86d26f88`, committed and re-verified against real HEAD) -- BARD NOW GENUINELY REACHES FULL COMPUTED.** Backend chose Bard's spell posture (proven shape, well-scoped) over Monk, and found a real bug along the way: Bard's known-spell diagnostic was STILL sitting unhoisted inside the Human/single-class-gated body -- the exact same gate-ordering bug every other class needed fixed, caught and fixed proactively as part of this same slice rather than left for a future review. **Lead confirmed directly in code** (`pilot_compute.rs:19483-19515`): `ground_or_block_bard_known_spells` is now called in the same already-hoisted block as `ground_or_block_bard_bardic_performance`, both unconditional on race/single-class status, correctly before the Human-only gate. `ground_or_block_bard_known_spells` mirrors Sorcerer's `unmet.is_empty()` pattern exactly. **Lead ran the key test directly**: `single_class_bard_not_performing_does_not_trip_the_performance_diagnostic` asserts both zero claim-blocking diagnostics AND real `HeadlessReceiptStatus::Computed` through the actual production pipeline for the bare default fixture (zero known spells, not performing -- a genuinely valid posture on both fronts). All 9 dispatch-widening-safety tests pass (5 new + 4 pre-existing), full lib 393/393, desktop 212/212, both doctrine audits clean. Honest framing on the level 11-20 spell-table extension gap, named explicitly rather than silently assumed (same "zero known spells always valid" logic covers it for now). **10 of 27 classes now genuinely reach Computed**: Fighter, Wizard, Rogue, Ranger, Paladin, Barbarian, Sorcerer, Cleric, Druid, Bard.

**Re-verified against the real committed state**: full lib 393/393 exact match, full workspace sweep confirms exactly the claimed 17-test wave (`tests/sd13_bard_*`, `tests/sd18_bard_*`, `sd25_bard_level_up_explanation_coverage.rs`), zero unrelated regressions. Assigned to QA. Minor correction: backend's own summary said "9 of 27" but their own name list (Fighter/Wizard/Rogue + Ranger/Paladin/Barbarian/Sorcerer/Cleric/Druid/Bard) totals 10, matching what's already tracked correctly here -- flagged directly, no impact on anything already recorded. **Session total: 6 full mechanisms/closures built this session on top of the 3 pre-existing classes** (Barbarian Rage, Bard Inspire Courage + known-spell posture, Sorcerer Arcane Bond, Cleric Touch of Good, Druid's animal companion), plus 3 Monk feat closures, plus one real production bug fix (`is_druid_pillar_id`). Backend's own sequencing question (Monk's remaining 4 feats vs. APG/ACG's 16 untouched classes) answered with a lead recommendation (not a directive) toward APG/ACG, given how much value the "check for a hidden cheap angle first" discipline has produced on every CRB class today and APG/ACG hasn't had that treatment applied at all yet.

**FIRST APG/ACG SCOPING DOC LANDED (`skald-acg-first-class-scoping.md`) -- backend surveyed all 16 classes, found a promising entry point (ACG's Skald), and correctly flagged the first-ever architectural boundary crossing this would require. Adversarial review dispatched.** Backend confirmed (via a research agent) that `compute_apg_class_chassis`/`compute_acg_class_chassis` are genuinely the only thing built for any of the 16 APG/ACG classes -- zero class-specific content anywhere. Found Skald (Bard+Barbarian hybrid)'s Inspired Rage shares the exact four-value shape as Barbarian's Rage (STR/CON/Will morale + AC penalty), with formulas verified against the raw corpus. **Lead independently re-verified the gate structure directly in code**: `has_supported_class_chassis` (gating `compute_total_saves`/`compute_combat_baseline`/`compute_selected_skill_modifiers`) resolves through `is_supported_generic_single_class` -> `multiclass_class_level_supported` -> `table_class_id`, CRB-only, confirmed APG/ACG classes are genuinely unrecognized by this gate today -- this would be the first time any pillar-integration gate crosses from CRB into APG/ACG. **Lead also independently found a real gap in the doc's self-application reasoning** before dispatching the review: compared Skald's Inspired Rage text against Bard's own Inspire Courage/Bardic Performance text side by side -- Bard's is EXPLICIT ("including yourself if desired," "inspire courage in your allies (including yourself)"), while Skald's text (both the general Raging Song description and Inspired Rage's own DESC) never uses that phrasing anywhere, only ever says "allies" -- the only hint of self-inclusion is a restriction-clause exception the doc leaned on, which is weaker evidence than the doc characterized it. Dispatched a full adversarial review covering both the gate-widening safety (does it risk admitting other ACG/APG classes, multiclass safety) and the self-application evidence gap. Backend holding on code until it lands.

**QA'S BARD KNOWN-SPELL WAVE LANDED (`cb372cb3`, 16 files), lead independently verified -- full workspace sweep confirms zero failures anywhere.** Exactly mirrors Sorcerer's known-spell closure pattern (spontaneous caster, empty known-spell selection is a valid posture). Real "Alter Self" (2nd-level bard spell) violation injected and RED-checked properly.

**ADVERSARIAL REVIEW COMPLETE -- found 2 real, concrete traps in the gate-widening plan, both independently confirmed in code; corrected the self-application framing.** **(1) Real over-widening risk confirmed**: `AcgClassId::from_class_id_str` (`rules_tables/acg/mod.rs:123-125`) returns `Some` for ANY of the 10 ACG classes, not just Skald -- the scoping doc's own named tool would silently admit Arcanist, Bloodrager, and all 8 others too under a naive `.is_some()` check, exactly the Ranger-review class of bug (the other 9 would gain real save/combat/skill computations while still LOOKING blocked via their own unconditional diagnostic -- existing tests only check "stays Blocked," not "produces no pillar output," so this would slip through undetected). Fix required: the actual check must be `== Some(AcgClassId::Skald)` plus a new test asserting the other 9 classes produce zero pillar-integration output. **(2) Real diagnostic/test collision confirmed**: `all_ten_acg_classes_stay_blocked_with_the_real_unconditional_diagnostic` (`pilot_compute.rs:23335`) loops all 10 ACG classes asserting each produces its own unconditional diagnostic, including Skald -- if Skald's diagnostic becomes conditional per the plan, this test breaks for Skald specifically unless explicitly carved out, AND the existing diagnostic's own message ("no named class-feature computation... no spellcasting") becomes false for Skald once Inspired Rage is real. Neither collision was in the plan's own test list. **(3) Self-application framing corrected**: the review agreed the inference is mechanically reasonable (the restriction clause implies the skald is under her own rage, since it only matters if she is) but rejected the doc's claim that this needs LESS disclaimer than Touch of Good -- it's the opposite: Touch of Good's self-targeting was RAW-explicit, only its significance was secondary; here self-application ITSELF is inferred from one negative clause with zero explicit corpus support, weaker evidence needing a stronger honesty note. Recommended explicit "modeled by inference... corpus carries no explicit self-inclusion clause here, unlike Bard's 'including yourself'" framing. All 3 findings sent to backend for revision before any code.

**PLAN REVISED, ALL 3 GAPS ADDRESSED -- lead read the revision directly and greenlit the build.** (1) Gate check corrected to the exact `AcgClassId::from_class_id_str(...) == Some(AcgClassId::Skald)`, plus a new positive test requiring the other 9 ACG classes produce ZERO `defense.total_save.*`/`combat.baseline_*`/`skill.selected_modifier.*` explanations at level 1 -- closes the exact leak the existing "stays Blocked" test couldn't catch. (2) The generic `class_feature.acg.skald.unsupported` diagnostic retires for Skald specifically (the other 9 keep it unchanged), replaced by a new, narrower `class_feature.acg.skald.spellcasting_deferred.unsupported` naming only what's genuinely still missing -- still claim-blocking, so Skald correctly stays `Blocked` overall this slice (no false-`Computed` risk introduced); the existing all-10-classes test gets Skald carved out with its own dedicated assertion pair. (3) Self-application framing corrected exactly as asked: the explanation text will state plainly that self-application is inferred from the exception clause alone, with no explicit "including yourself" language the way Bard's text has, rather than implying it's as settled as Touch of Good's case was. Greenlit -- no second review needed, same pattern as every prior post-review cycle. Backend building now.

**MILESTONE (uncommitted at check time) -- SKALD IS THE FIRST APG/ACG CLASS TO GET REAL PILLAR-INTEGRATION WORK, lead independently verified every piece before the commit.** Backend built exactly as revised. **Lead confirmed directly in code**: `is_supported_skald_single_class` (`pilot_compute.rs:6880-6888`) uses the exact `!= Some(AcgClassId::Skald)` guard, not a broad `.is_some()`. **Lead ran both the negative-leak test and its positive counterpart**: `the_other_nine_acg_classes_produce_zero_pillar_explanations_despite_a_satisfying_posture` (proves the other 9 ACG classes produce zero `defense.total_save.*`/`combat.baseline_*`/`skill.selected_modifier.*` output even under a genuinely satisfying posture) and `skald_alone_produces_real_pillar_explanations_under_the_satisfying_posture` (proves Skald DOES produce real output under the identical posture) -- together these prove the gate genuinely discriminates, not just happens not to admit the others. Confirmed the all-10-classes test correctly carves Skald out (`if class_id == "class:skald" { continue; }`, test name now slightly stale cosmetically but logic correct). Confirmed the self-application explanation text carries the corrected framing verbatim: "Self-application is modeled by inference from the corpus's \"allies other than the skald\" exception clause... the corpus carries no explicit self-inclusion language here, unlike Bard's \"including yourself\"". Confirmed Skald genuinely stays `Blocked` overall even while validly singing (the new `spellcasting_deferred` diagnostic still claim-blocks) while the real STR/CON/Will/AC values genuinely apply -- checked the exact numbers directly (Will save 4 = base 2 + Wis +1 + Inspired Rage +1; ability modifiers +1 STR/+1 CON) -- no false-`Computed` risk. Backend also found and fixed a related standing regression: `tests/sd24_acg_class_coverage_audit.rs`'s canary test (`zero_named_class_features_are_wired_for_any_acg_class_except_skalds_inspired_rage`, correctly renamed) and `rules_tables/acg/mod.rs`'s `class_coverage()` both needed updating for Skald's `named_features_wired` count -- **lead independently ran this file too**, 3/3 pass. Full lib 400/400, desktop 212/212, all 3 SD-24/doctrine audit files clean, full workspace sweep zero failures anywhere.

**Bloodrager (second APG/ACG class) verified against the raw corpus before backend built it, greenlit to skip a formal review.** Backend proposed reusing the Skald gate-widening pattern without a second adversarial review, citing Bloodrage's unambiguous self-only application and the pattern's two prior confirmations (Barbarian, Skald). **Lead independently pulled `acg_abilities_class.lst` directly**: confirmed the +4 Str/+4 Con/+2 Will/-2 AC shape matches Rage exactly, confirmed the self-application text ("Bloodrage counts as the barbarian's rage class feature...") carries zero ally/exception-clause language — genuinely unambiguous, unlike Skald's inference-based case. Flagged one nuance before backend built further: Bloodrage's rounds-per-day formula (`2 + CON mod + 2*level`) is NOT textually identical to Barbarian's own expression, though backend's own scoping doc (read after sending the flag) shows they'd already independently verified the two formulas are algebraically equivalent and confirmed Greater/Mighty/Tireless Bloodrage's thresholds match Barbarian's Greater/Mighty/Tireless Rage exactly (level 11, 17, 20) — thorough, no gap. Endorsed skipping the review; backend building now (`pilot_compute.rs`, 683 insertions uncommitted at check time).

**Real frontend backlog found and dispatched — status was genuinely idle, not misreported, but real work exists.** Cross-checked the operator's "never say idle without verifying" rule properly: frontend's last inbox activity really was ~10:06 (3+ hours stale), matching the Barbarian UI fix being its last real action. But grepping `characterHubModel.ts` directly found 4 classes (Sorcerer, Cleric, Druid, Bard) still labeled `supportLevel: 'human-diagnostics-only'` despite all four having reached genuine `Computed` status since that fix landed — the same stale-label gap Barbarian itself had before its own fix. Also checked whether the creation UI exposes the choice pickers Cleric (domain) and Druid (companion type) would need to actually reach their Computed posture through real user interaction — found no domain or animal-companion-type picker anywhere in the `.tsx` tree, a real open question for frontend to resolve (may mean a narrower supportLevel than `full`, or a choice-seeding default elsewhere not yet checked). Dispatched to frontend with per-class nuances rather than a blanket "copy Barbarian's fix."

**BLOODRAGER LANDED (uncommitted at check time, backend's sweep in flight), lead independently verified before backend's own report finished.** Crossed messages with backend, who had already built exactly to the spec both sides had converged on. **Lead independently confirmed in code, not just from the report**: `is_supported_bloodrager_single_class` (`pilot_compute.rs:6915-6922`) uses the exact `!= Some(AcgClassId::Bloodrager)` guard; `bloodrager_bloodrage_rounds_per_day`/`BLOODRAGER_BLOODRAGE_BASE_ROUNDS_PER_DAY` is a fully independent formula and constant, not a reuse of Barbarian's, exactly per the lead's own pre-build flag; Greater/Mighty/Tireless tier thresholds correctly reuse `BARBARIAN_GREATER_RAGE_LEVEL`/`BARBARIAN_MIGHTY_RAGE_LEVEL`/`BARBARIAN_TIRELESS_RAGE_LEVEL` (verified algebraically identical, not just textually similar) with a fatigue-not-modeled diagnostic mirroring Barbarian's own honesty pattern. **Lead ran the full lib suite independently**: 406/406, exact match to backend's own number. Second APG/ACG class-specific closure, second time the review-once-then-reuse-the-pattern discipline held without a second formal review.

**REAL PRODUCT-REACHABILITY GAP FOUND by frontend, independently corroborated by the lead from a third angle before frontend's own live-verify finished.** While updating `characterHubModel.ts`'s stale `human-diagnostics-only` labels for Sorcerer/Cleric/Druid/Bard (the lead's own dispatch), frontend discovered something bigger than a label fix: Sorcerer/Cleric/Druid's real `Computed` posture each requires a specific choice (`bloodline:arcane`+bond, `domain:good`, `bond:animal_companion` respectively) that **the real creation UI currently has no way to submit at all** -- confirmed by frontend two ways (no picker component in `CreateCharacterForm.tsx`; `character_hub.rs` never auto-seeds any of the three choice ids) and independently confirmed by the lead a third way: `composeCreateCharacterRequest.ts`'s `CreateCharacterFormFields`/`CreateCharacterRequest` DTO shape has exactly 7 fields (displayLabel/raceId/classId/level/abilityScores/abilityBonusTarget/savedAt) -- no choices field anywhere in the wire contract itself. This means Sorcerer/Cleric/Druid's `Computed` status may currently be reachable only via headless test fixtures that inject `selected_choices` directly, not through the shipped product -- a real caveat on the "10 of 27 classes reach Computed" framing used throughout SWARM_REPORT.md. Bard is unaffected (its known-spell posture needs no picker, empty selection is honestly valid). Frontend running a live dev-build verify now to confirm what a player actually sees before any doc gets the definitive framing; not treating this as scope to fix right now (a real picker UI + backend wiring is new work, not a label fix) -- will update `risks-and-open-questions.md` once frontend's live-verify lands.

**OPERATOR CORRECTION: the class/race detailed-status section had gone stale on the ACG side and in "what's queued next" — fixed.** The top-level Agent Status table and Progress line were being kept current every cycle, but the "Full class/race chassis breadth" section's ACG table and header still said "dispatch-only, no per-class work started" even after Skald's commit and Bloodrager's build both landed real per-class progress there — a real staleness gap, not just a perception issue. Fixed: ACG header now reflects 2 of 10 with real progress, Bloodrager's row updated to reflect its built-but-uncommitted state, an ACG tally line added (mirroring the CRB tally), the product-reachability gap (frontend's find) now has its own subsection there instead of only living in agent-status/status-log prose, and "what's queued next" rewritten to match actual current state (Bard/Barbarian waves that already closed removed, Bloodrager's wave and frontend's live-verify added). Going forward this section gets touched on every report update alongside the Agent Status table, not just when a class fully closes.

**Check-in: both backend's Bloodrager sweep and frontend's live-verify appear to have gone quiet, pinged both rather than assume either is still "working."** Uncommitted `pilot_compute.rs` diff unchanged in size across multiple cycles, no cargo/rustc process visible; no `apps/desktop/src` file changes since ~13:20, no npm/vite/tauri process visible either. This doesn't necessarily mean either is stuck (background sessions may not show processes in this shell), but "still working" was becoming a stale assumption rather than a verified one — same discipline as not defaulting anyone to "idle" without checking, applied in the other direction. Sent both a direct check-in asking for their actual current state; awaiting replies before updating Agent Status further.

**BLOODRAGER COMMITTED (`15560e62`) AND PUSHED WHILE THE LEAD WAS MID CHECK-IN -- crossed with the sweep, re-verified against the real committed HEAD from scratch rather than trusting the pre-commit verification alone.** Confirmed in the actual committed tree: `is_supported_bloodrager_single_class`'s exact `!= Some(AcgClassId::Bloodrager)` guard, `bloodrager_bloodrage_rounds_per_day`'s independent formula/constant, and the Greater/Tireless/Mighty tier-threshold reuse of Barbarian's own constants all match what was verified pre-commit exactly. **Lead ran three independent checks against the committed HEAD**: full lib suite (406/406), `sd24_acg_class_coverage_audit` directly (3/3, including the renamed `..._except_skalds_inspired_rage_and_bloodragers_bloodrage` canary), and a complete `cargo test --workspace --no-fail-fast` sweep (zero FAILED/error lines anywhere). Second APG/ACG class-specific closure now fully landed and independently confirmed clean. Frontend meanwhile confirmed (not a stall) that its live dev-build verify is just slow due to a cold Rust rebuild every attempt, given how much Rust surface this and the prior cycle touched.

**Backend proposed Brawler's AC Bonus as the next cheap ACG win (framed as "the exact same shape as Monk's own AC Bonus, even simpler than Bloodrager") -- lead independently pulled the corpus before greenlighting and found the framing was wrong on the mechanic itself.** Backend had already correctly scoped and ruled out Warpriest (Blessings/Fervor, Cleric-shaped cost) and Slayer (Studied Target, opponent-dependent, same non-vacuous shape as Monk's remaining 4 feats) via their own corpus checks -- good discipline, no lead correction needed there. But Brawler's AC Bonus: **lead confirmed directly in `acg_abilities_class.lst`** that it is NOT a Wisdom-to-AC bonus like Monk's (`pilot_compute.rs:14199`, `wisdom.max(0)`, live from level 1) -- it's a flat, LEVEL-SCALED dodge bonus, `(BrawlerLVL>3)+(BrawlerLVL>8)+(BrawlerLVL>12)+(BrawlerLVL>17)`, genuinely **+0 until 4th level**. Building it against Monk's formula would have silently computed a wrong nonzero value for every level-1 fixture this swarm predominantly tests. Separately confirmed the "light or no armor" condition IS real and satisfiable via the existing `CHAIN_SHIRT_ITEM_ID` fixed posture (Chain Shirt genuinely is light armor by RAW) -- but only via the same specific-item-id check pattern already used elsewhere, since **no armor-weight-category concept exists anywhere in the schema** (confirmed via grep -- only prose mentions in Evasion/Barbarian fast-movement diagnostics, both of which already routed around the same gap). Sent full correction to backend before any code landed; their call now on whether a level-4+ fixture is worth building for this slice or whether another target has better ROI.

**Addendum, fairness correction: backend's own scoping doc (`brawler-acg-third-class-scoping.md`) already had the correct level-scaled formula and the correct vacuous-armor-precondition reasoning by the time it was read.** The lead's corpus check happened independently and in parallel, not as the thing that fixed a real committed mistake -- worth stating plainly rather than let the prior entry read as more corrective than it was. Backend is now actively building against the doc's own correct reasoning: `is_supported_brawler_single_class` (exact `== Some(AcgClassId::Brawler)` match, confirmed in the live diff) and the `compute_acg_class_chassis` dispatch arm are wired; the actual `ground_brawler_ac_bonus_and_defer_the_rest` grounding function doesn't exist yet (in progress, diff currently 34 lines, won't compile yet). Third APG/ACG closure in flight, same twice-proven pattern discipline (exact-match gate, no second review needed).

**PRODUCT-REACHABILITY GAP CONFIRMED AND LANDED (`833ea89c`), lead independently re-verified against the real committed HEAD.** Frontend live-verified all four classes through the real dev build with default (untouched) settings, not assuming any bucket: Bard reached `Computed`/`Saved` and leveled cleanly to 2, set to `full`; Sorcerer/Cleric/Druid all returned "This build isn't ready yet" with exactly the predicted named diagnostic and none persisted to disk, set to a new honest `headless-only` bucket (copy names the real cause -- a missing picker -- not "isn't computed"). One correction to the lead's own earlier framing: Sorcerer's gap isn't just a level-3+ cap, both a bloodline AND an Arcane Bond choice are needed even at level 1-2, with no picker for either. **Lead independently confirmed the commit**: read the diff directly (matches every claim), ran the full frontend suite fresh (78/78) and `npm run typecheck` (clean) against the real committed HEAD. Wrote up the full finding in `risks-and-open-questions.md` (credited to frontend) and corrected `SWARM_REPORT.md`'s "10 of 27 Computed" framing to caveat that only 7 of those 10 are reachable through the real product today. This closes the reachability-gap investigation started this cycle -- frontend standing by, backend mid-build on Brawler's AC Bonus (third ACG closure).

**BRAWLER'S AC BONUS BUILT AND LEAD-VERIFIED PRE-COMMIT (uncommitted at check time) -- third APG/ACG class-specific closure, a structurally new shape.** Crossed messages with backend again, who'd already built to the exact corrected formula before the lead's earlier correction message landed. **Lead independently confirmed in code**: `brawler_ac_bonus`'s formula (`i16::from(level>3)+i16::from(level>8)+i16::from(level>12)+i16::from(level>17)`) matches the raw corpus exactly, `is_supported_brawler_single_class` uses the same exact-match discipline as Skald/Bloodrager. **Lead read and ran the new multi-level progression test directly** (`brawler_ac_bonus_progression_matches_the_corpus_formula_at_higher_levels`, 10 checkpoints from level 1 to 20) -- confirmed every stepped value is correct AND correctly integrates into the shared `defense.baseline_armor_class` total, the first time this session a first-closure slice needed a multi-level fixture (level 1 alone would have shown nothing, since the real bonus is +0 until 4th level -- backend anticipated this proactively). Unlike Skald/Bloodrager, this mechanic has no activation gate and no choice gate at all -- a pure function of class ownership and level, the simplest ACG mechanic built yet, and unaffected by the Sorcerer/Cleric/Druid choice-picker reachability gap. **Lead ran everything independently**: full lib 409/409, the progression test individually, `sd24_acg_class_coverage_audit` 3/3, full workspace sweep zero failures anywhere. Clean, no concerns -- told backend to commit whenever ready.

**Brawler committed (`19c792e1`) while the lead was mid docs-update -- re-verified against the real committed HEAD, matches the pre-commit check exactly.** Full lib 409/409, `sd24_acg_class_coverage_audit` 3/3, both re-run fresh against the actual committed tree, not just trusted from the pre-commit pass. Third APG/ACG class-specific closure now fully landed. 3 of 10 ACG classes (Skald, Bloodrager, Brawler) have real partial engine progress; 7 remain untouched beyond chassis dispatch. Backend idle, awaiting next assignment.

**Backend proposed Hunter's Animal Companion as a 4th ACG closure (after correctly scoping Warpriest/Slayer/most-of-Brawler's-own-other-features as harder-effort, same bucket as Cleric/Bard's own scope), lead independently verified the load-bearing corpus claim before greenlighting.** Backend found Hunter's own corpus text states its effective druid level equals its hunter level -- meaning it can reuse Druid's already-built, already-cross-checked Wolf companion stat block directly rather than re-deriving new numbers. **Lead independently pulled `acg_abilities_class.lst`** and confirmed the exact quote ("The hunter's effective druid level is equal to her hunter level"), verbatim match, no exaggeration. Flagged one honesty note before backend builds: the corpus also lets a Hunter pick from the full druid animal list, the same species-choice shape Druid's own nature bond has -- if scoped the same way (Wolf-only, level-1-only, matching what the stat-block constants actually cover), this closure will land in the same `headless-only` reachability bucket frontend just confirmed for Druid, not a more-reachable one. Greenlit without a formal review -- the gate-widening pattern is now proven 4 times (Barbarian's original review, Skald/Bloodrager/Brawler's reuses) and the one substantive scoping question is a repeat of Druid's own already-answered one, not new territory. Backend building now.

**HUNTER'S ANIMAL COMPANION BUILT AND LEAD-VERIFIED PRE-COMMIT (uncommitted at check time) -- fourth APG/ACG closure, first one built by reusing an earlier closure's own machinery.** Backend extracted Druid's inline Wolf-companion math into 2 shared functions (`ground_wolf_companion_stat_block`, `ground_wolf_companion_link_and_share_spells_vacuous`) rather than duplicating it, then correctly self-corrected the lead's own reachability framing before writing any docs: Hunter's companion is unconditional by RAW (no domain-vs-companion choice the way Druid's Nature Bond has), so it belongs in Brawler's always-on bucket, not Druid's choice-gated one. **Lead independently verified this correction** by pulling Druid's own Nature Bond corpus text directly ("This bond can take one of two forms... the first is... a domain... the second... an animal companion") -- confirms Druid's is a genuine either/or choice Hunter's text never offers. Also independently confirmed the HP arithmetic backend corrected mid-build: `average_hit_die_value(8) = 5` matches the existing function and its own test exactly. **Lead ran everything fresh**: full lib 411/411, `sd24_acg_class_coverage_audit` 3/3, Druid's own 15-test suite specifically (confirming the refactor is byte-identical for Druid), full workspace sweep zero failures anywhere. Clean, no concerns -- told backend to commit whenever ready. 4 of 10 ACG classes now have real partial progress.

**Hunter committed (`21c5b13a`) while the lead was mid docs-update -- re-verified against the real committed HEAD, matches the pre-commit check exactly.** Full lib 411/411, re-run fresh against the actual committed tree. Fourth APG/ACG class-specific closure now fully landed. 4 of 10 ACG classes have real partial engine progress; 6 remain untouched beyond chassis dispatch. Backend idle, awaiting next assignment.

**Lead set a hard boundary on the cheap-win scanning discipline: one more scan, then Monk's remaining feats regardless of outcome.** Backend asked for direction after Hunter's closure (4 of 10 ACG classes now real) -- had already scanned and correctly ruled out Warpriest/Slayer/Shaman/Swashbuckler as not-cheap, with only Arcanist/Investigator (ACG) and all 6 APG classes left unscanned. Given the scan-first discipline found a real win in 3 of the last 4 attempts, it earned one more pass -- directed backend to scan the remaining 8 classes, but explicitly not to let "one more cheap ACG class" become an indefinite loop: after this scan, whether or not it finds a 5th win, backend moves to Monk's remaining 4 feats (the genuinely deferred real engine work -- opponent-interaction/turn-economy modeling) and starts its scoping doc. Reconciled `SWARM_TASKS.md`/`SWARM_REPORT.md`'s queued-next list to reflect this explicit sequencing rather than an open-ended "keep scanning" posture.

**Final cheap-win scan complete (backend) -- no 5th win, confirms the vein is tapped, lead spot-verified the closest near-miss.** All 8 remaining classes (Arcanist, Investigator, Alchemist, Cavalier, Inquisitor, Oracle, Summoner, Witch) individually checked against the raw corpus: Arcanist/Investigator's resource pools only matter through spellcasting or touch too many pillars at once; Inquisitor/Oracle/Summoner/Witch's named mechanics are all chooser-list-with-real-variety shapes (Rogue-Talent-scale complexity); Alchemist's Mutagen is Rage-shaped but with real added complexity (player-chosen stat target, duration tracking) plus would be a first APG gate-widening. The one genuine near-miss: **Cavalier's Mount** reuses the Hunter/Druid pattern (effective druid level = cavalier level) but a Medium cavalier can only choose Camel/Horse (not Wolf, needing its own from-scratch stat-block verification), loses Share Spells, and would be the first APG gate-widening -- genuinely moderate, correctly not built now. **Lead independently pulled `apg_abilities_class.lst`** and confirmed all three details verbatim. Backend now starting Monk's remaining-4-feats scoping doc as directed; lead expects to dispatch an adversarial review before any code lands, same reasoning as the original combat-time-activation-state plan (opponent-interaction/turn-economy modeling, no existing precedent to reuse this time, unlike the last 4 ACG closures).

**Monk's remaining-4-feats scoping doc landed, revises the original premise -- lead independently verified every claim before dispatching a full adversarial review.** Backend re-read each feat's actual PF1 text rather than trusting the earlier "all 4 need a new opponent engine" characterization, and found 3 of 4 (Combat Reflexes, Scorpion Style, Improved Grapple) have a real, groundable static number derived purely from the Monk's own stats -- the capacity/DC/duration, not the trigger event itself. Only Deflect Arrows has zero standalone numeric value and genuinely needs the full opponent-interaction engine, correctly staying exactly as blocked as before. **Lead independently pulled `cr_feats.lst` directly** and confirmed all 4 feats' exact corpus text/formulas match the doc's citations byte-for-byte. **Lead independently confirmed in code**: zero `compute_cmb`/`compute_cmd`/`combat_maneuver` hits anywhere in `pilot_compute.rs` (CMB/CMD genuinely don't exist yet), and `explain_monk_level1_chassis` (`pilot_compute.rs:5074`) really is called unconditionally, standalone, outside `has_supported_class_chassis`'s reach -- the wiring complication backend flagged is real, not overstated. This is the first slice this session proposing a wholly new baseline pillar (CMB/CMD) rather than a class-specific bonus layered onto an existing one, so a full adversarial review was dispatched (not skipped), covering the representational-honesty question (grounding a capacity number without ever resolving the event it feeds into -- is the Sneak-Attack-die-count/Fascinate-DC analogy sound), the CMB/CMD architecture's risk to already-`Computed` classes like Fighter, gate-widening/diagnostic-narrowing safety, and backend's own open question on whether to split the CMB/CMD pillar into its own cycle. Backend holding on all code until it lands.

**ADVERSARIAL REVIEW COMPLETE -- greenlight with required changes, and a genuine scope reduction found, lead independently confirmed the two most load-bearing findings before relaying.** **(1) Improved Grapple needs no new pillar at all** -- the review found an existing precedent neither backend nor the lead had cited: the Dwarf Stability record (`pilot_compute.rs:5744-5775`) already grounds a grapple-adjacent CMD bonus MAGNITUDE (+4 `CMD_BullRush`/`CMD_Trip`) as a standalone recognition record with an explicit "no CMD-total engine exists" disclaimer -- the exact same shape as Improved Grapple's own `+2 CMB_Grapple,CMD_Grapple`. **Lead read the record directly and confirmed it matches exactly.** This means Improved Grapple closes today via the same idiom as Dodge/Catch Off-Guard/Throw Anything, with zero new architecture -- the CMB/CMD baseline pillar (the one piece the review flagged as needing real scrutiny) has no forced consumer in this slice after all and should be deferred to its own future, separately-reviewed cycle. **(2) A real stale-claim bug caught, independently confirmed**: the existing bonus-feat choice-recognition record (`pilot_compute.rs:15082-15084`) says "{feat_name}'s own mechanics are not grounded here... no attack-resolution, grapple-check, or DC/save engine exists" -- this becomes false for the 3 feats about to be grounded, the same "surrounding claim goes stale" shape this session has caught before in test fixtures, just in prose this time. **Rep-honesty verdict clean** (the Sneak-Attack-die-count/Fascinate-DC analogy holds precisely against real precedent) and **diagnostic-narrowing verdict clean** (catch-all preservation via early-return pattern confirmed structurally safe, no Cleric-shaped gap) -- neither needed changes. Also flagged 3 minor quote-fidelity fixes for the eventual explanation text (Combat Reflexes should cite the actual BONUS:VAR token; Improved Grapple should preserve the grapple-specific CMD qualifier; Scorpion Style's DC uses total/character level, not monk level, named honestly). Relayed all findings to backend; revising now, no second review needed given how targeted the required changes are.

**MONK'S REVISED BUILD VERIFIED PRE-COMMIT (uncommitted at check time) -- all 4 required review changes confirmed correct in code, one stale test caught before backend's own sweep reached it.** **Lead independently confirmed directly in code**: `monk_combat_reflexes_additional_attacks_of_opportunity`/`monk_scorpion_style_dc` formulas match the corpus exactly; the choice-recognition record's blanket claim (`pilot_compute.rs:15142`) is now correctly conditional on `feat_own_mechanics_closed_elsewhere`, distinguishing the 6 closed feats from Deflect Arrows; all 3 quote-fidelity fixes present verbatim (Combat Reflexes cites the real token and names its own gloss honestly, Improved Grapple preserves the grapple-specific CMD qualifier, Scorpion Style names the TL/monk-level equivalence explicitly). **Lead ran the full lib suite (414/414, matches exactly) and all 9 monk bonus-feat tests individually** -- all pass. **Lead's own full `cargo test --workspace --no-fail-fast` caught 1 real failure backend's own in-flight sweep hadn't reached yet**: `tests/sd13_monk_bonus_feat_choice.rs::monk_level1_recognizes_combat_reflexes_as_an_alternate_restricted_selection` asserts the old unconditional `bonus_feat.unsupported` diagnostic still fires and names Combat Reflexes -- correctly no longer true now that Combat Reflexes is genuinely grounded, the exact "stale pre-existing per-level test file" shape every prior class closure (Barbarian/Bard/Cleric/Druid/etc.) has hit. Flagged precisely to backend with the full panic detail rather than a vague "one test fails" -- their call whether to fix directly (small, single-file, mechanical) or route to QA.

**MONK'S 6TH AND 7TH FEATS COMMITTED (`e45b622f`) AND RE-VERIFIED AGAINST THE REAL HEAD.** Backend's fix updates the test to assert the new grounded behavior (Combat Reflexes' capacity now genuinely computed, not blocked) instead of the retired diagnostic. **Lead independently confirmed the fixture's real Dexterity (16) makes the expected capacity value (3) correct, not assumed** (`tests/fixtures/rules_core/pf1_human_monk_level1_sd13_bonus_feat_combat_reflexes.txt`). **Lead ran the specific test file (4/4), full lib (414/414), and a complete workspace sweep** -- zero failures anywhere, matching the earlier pre-fix numbers exactly except for this one now-passing test. Monk moves from 3 of 7 to 6 of 7 restricted-list feats closed; only Deflect Arrows remains, correctly staying blocked (no groundable static value exists for it). Third full adversarial-review cycle this session (after the original combat-time-activation-state plan and the ACG gate-widening reviews) to find real, concrete issues and produce a cleaner result after revision.

**Lead directed backend to Cavalier's Mount (APG) next, after weighing the open backlog backend offered.** Deflect Arrows and the CMB/CMD pillar were both correctly ruled out for now (neither has a forced reason to build today -- Deflect Arrows needs a whole new opponent-interaction engine with nothing consuming it yet, and the CMB/CMD pillar lost its one potential consumer when Improved Grapple turned out not to need it). Cavalier's Mount is the concrete, already-scoped candidate from the earlier ACG/APG scan. **Lead independently confirmed before greenlighting**: `ApgClassId` (`rules_tables/apg/mod.rs:46`) has its own `from_class_id_str`, structurally identical to `AcgClassId` -- so the gate-widening itself needs no new review, the pattern is proven 4 times now regardless of which book's class-id enum is involved. The one thing that DOES need real rigor is Horse's own stat block (Str/Dex/Con/BAB/saves/natural armor), same 2+-source verification discipline Wolf's got, since this is genuinely new data, not a reused precedent like Bloodrager/Brawler/Hunter's own closures were. Backend to write the scoping doc and verify Horse's numbers before the lead does a corpus spot-check, same as every prior class.

**Cavalier's Mount scoping doc landed, Horse's stat block cross-verified for real -- lead independently re-confirmed both resolved disagreements before greenlighting.** Backend fetched aonprd.com and d20pfsrd.com's own Animal Companion pages directly: ability scores agreed completely (Str 16/Dex 13/Con 15/Int 2/Wis 12/Cha 6, Large size), but 2 genuine disagreements surfaced -- natural armor (aonprd +4 vs d20pfsrd +1) and speed (aonprd 50ft vs d20pfsrd 60ft) -- both resolved via the raw PCGen corpus as tiebreaker, same "corpus-backed majority over an uncorroborated single reading" discipline Wolf's own Trip/natural-armor question used. **Lead independently pulled `cr_races_companion.lst:21`** and confirmed both resolutions exactly (`MOVE:Walk,50`, `BONUS:VAR|AC_Natural_Armor|4|TYPE=Base`). Also independently checked the wild (non-companion) Horse Bestiary entry out of curiosity on the minor attack-die question backend flagged as low-stakes -- confirmed it's an unrelated stat block (a different, non-companion monster context), not a real fourth disagreeing source, so backend's aonprd-based resolution stands unchallenged. Cavalier's own chassis confirmed pure-martial (no SPELLSTAT), so its new diagnostic will be named `other_features_deferred` like Brawler's, not `spellcasting_deferred`. Greenlit to build -- first-ever APG gate-widening, no new architecture needed, only fresh data verification.

**CAVALIER'S MOUNT BUILT AND LEAD-VERIFIED PRE-COMMIT (uncommitted at check time) -- first-ever APG class-specific closure, proving the ACG-side pattern generalizes cross-book.** **Lead independently confirmed in code**: the exact-match gate (`is_supported_cavalier_single_class`, `== Some(ApgClassId::Cavalier)`); `ground_horse_companion_stat_block`/`ground_horse_companion_link_vacuous`/`horse_companion_hit_dice` are genuinely parallel to Wolf's own functions, no shared code, per the lead's own review-carried concern about risking Druid's/Hunter's shipped output. Backend used the wild-Horse-monster data point the lead had flagged as a "dead end" as legitimate corroborating evidence instead (hooves-only, no bite, supporting hooves as the primary attack) -- a smart repurposing, not a contradiction. Share Spells correctly NOT fabricated as a vacuous-correction record (the Mount doesn't have that ability at all per RAW, unlike Druid's/Hunter's companions which do and got the correction). **Lead ran everything fresh**: full lib 417/417, Druid's 15 tests and Hunter's 2 tests individually confirmed unaffected, `sd24_apg_class_coverage_audit` 3/3 (mirrors the ACG fix backend applied proactively), AC=14 math confirmed (10 base + 4 natural armor), full workspace sweep zero failures anywhere. Clean, no concerns -- told backend to commit whenever ready. Also caught and fixed a stale "Monk 3 of 7" reference in the report's own top progress line that had been missed in an earlier update -- corrected to 6 of 7 alongside this cycle's Cavalier update, per the operator's standing "keep the class/race detail live" directive.

**Cavalier's Mount committed (`d256bc9c`) while the lead was mid docs-update -- re-verified against the real committed HEAD, matches the pre-commit check exactly.** Full lib 417/417, `sd24_apg_class_coverage_audit` 3/3, both re-run fresh against the actual committed tree. First-ever APG class-specific closure now fully landed and independently confirmed clean. Backend idle, awaiting next assignment.

**Lead declared the cheap-win scan genuinely complete and redirected backend to real, deliberate work: Alchemist's Mutagen.** Backend offered to keep scanning remaining classes for another cheap win; the lead named plainly that all 16 APG/ACG classes are now accounted for (5 closed, 11 explicitly ruled not-cheap across two full passes), so re-scanning Shaman/Slayer/Swashbuckler/Warpriest would just repeat verdicts already on record. **Lead independently pulled Mutagen's corpus text** (`apg_abilities_class.lst:75`) before directing backend to it: confirmed it's a real activation-gated ability (Rage-shaped: +4 physical stat, +2 natural armor, -2 mental-stat penalty, duration = level x10 minutes) but with a genuine choice dimension the 5 closed abilities didn't have -- which of Str/Dex/Con to enhance, with the paired mental penalty depending on that choice (Str->Int, Dex->Wis, Con->Cha). Flagged two things before backend scopes it: (1) this is closer in shape/size to Sorcerer's Arcane Bond or Cleric's Touch of Good than to Skald/Bloodrager/Brawler/Hunter/Cavalier's simpler closures, size it honestly; (2) it will likely land in the same `headless-only` reachability bucket as Sorcerer/Cleric/Druid, since "which stat" is a real choice with almost certainly no UI picker -- worth confirming and naming upfront rather than discovering it after the fact the way the CRB casters did. No CMB/CMD or Deflect Arrows work directed -- both still correctly lack a forced consumer.

**Mutagen scoping doc landed, greenlit without a full review after the lead traced the combination logic's edge cases directly.** Backend confirmed both things flagged upfront: (1) Mutagen genuinely combines choice-recognition (which of Str/Dex/Con) and activation-gating (mutated/not) for the first time this session -- proposed a 3-branch structure (not-active valid/active-with-recognized-choice grounds real values/active-without-recognized-choice claim-blocks) with no direct one-to-one precedent, flagged explicitly for a second look; (2) reachability confirmed `headless-only` via a direct grep of `CharacterSheet.tsx` (no generic choice-picker UI exists anywhere, level-up only ever submits one hardcoded HP choice). **Lead independently re-verified every formula token** (`MutagenStatPenalty|-2`, `MutagenACBonus|2`, `MutagenStatBonus|4`, the Str->Int/Dex->Wis/Con->Cha mapping verbatim in the corpus DESC) and **traced the 3-branch logic's edge cases directly**: confirmed "active without a recognized choice" can only ever represent a malformed/synthetic fixture (real PF1 play always picks the stat at brewing, before drinking), can't leak into the "not mutated" branch (gated on activation-presence, not choice-presence), and doesn't reopen any Cleric-shaped representational-honesty question (a mechanical combination of two proven patterns, not a new judgment call about what to claim). Greenlit without a full adversarial review -- proportionate to the risk, same reasoning as endorsing Druid's/Sorcerer's own scoping docs directly. Backend building now.

**MUTAGEN COMMITTED (`101bf40d`) AND RE-VERIFIED AGAINST THE REAL HEAD -- first closure combining choice-recognition and activation-gating, matches the traced logic exactly.** **Lead independently confirmed in code**: the exact-match gate, `alchemist_mutagen_mental_penalty_target`'s Str->Int/Dex->Wis/Con->Cha mapping verbatim, and read both new positive tests in full -- the Strength test (Str 4->6, Int 0->-1) and the Constitution test (Con 2->4, Cha -1->-2, explicitly re-asserting Strength/Intelligence stay at their unmutated baseline) together genuinely prove the choice drives behavior rather than a hardcoded pair, exactly the proof structure requested. AC math confirmed (17 base + 2 natural armor = 19). Confirmed the negative-leak test correctly renamed to "the_other_four" and excludes both Cavalier and Alchemist from the must-stay-blocked set. **Lead ran everything fresh**: full lib 423/423, `sd24_apg_class_coverage_audit` 3/3, full workspace sweep zero failures anywhere. 6 classes across both books now have real partial engine progress (Skald/Bloodrager/Brawler/Hunter ACG, Cavalier/Alchemist APG) -- the cheap-win-then-deliberate-work cycle that started after Bard's closure is now complete for both books. Clean, no concerns -- told backend to commit whenever ready.

**NEW PHASE: lead set direction for both backend and frontend now that the cheap-win backlog is genuinely exhausted -- two parallel tracks, not another scan.** Backend directed to scope (not build) which of the 3 partial ACG/APG casters (Skald/Bloodrager/Alchemist, all with deferred spellcasting) has the cheapest real completion path -- specifically checking whether any reuse an already-built CRB spell list (Skald possibly Bard's, Bloodrager possibly Sorcerer's/Bard's overlap) the way Druid reused Cleric's own base-spells-per-day table, rather than assuming each needs a from-scratch build. Frontend given a genuinely new assignment (not idle standby): scope the product-reachability picker gap for Sorcerer/Cleric/Druid found earlier -- a generic-vs-bespoke UI design question, the actual wire-contract change needed on `CreateCharacterRequest`, where it surfaces in the creation flow, and whether it's one bounded slice or needs splitting. Both scoping docs to be independently verified by the lead before any code, same discipline as every prior slice this session.

**Backend's spellcasting-cost comparison landed and lead-verified -- Skald is the clear, dramatic winner.** Backend confirmed Skald's own class line uses `SPELLLIST:1|Bard` and found zero per-spell `CLASSES:Skald` tags anywhere in `cr_spells.lst`/`acg_spells.lst` -- meaning Skald genuinely inherits Bard's entire already-built 164-entry spell list wholesale via PCGen's list-substitution mechanism, needing only a small class-specific spells-known/per-day progression table, not a fresh extraction. Bloodrager (202 real per-spell tags in `acg_spells.lst`) and Alchemist (122 in `apg_spells.lst`) both need genuine 100+-entry extractions, the same magnitude of work Bard's own list took originally. **Lead independently confirmed all 4 numbers directly**: the `SPELLLIST:1|Bard` token at `acg_classes.lst:278`, both zero-hit greps, and both nonzero-hit counts -- exact matches. Also noticed and flagged for later (not now): Investigator's own class line uses `SPELLLIST:1|Alchemist`, so Alchemist's list (whenever built) becomes reusable for Investigator too, the same shortcut shape. Greenlit Skald's spellcasting build without expecting a full review, given `BARD_SPELL_LIST` is directly reusable and the known-spell validation shape is already proven (Sorcerer/Bard's own posture pattern).

**Choice-picker scoping doc landed (`cfcd43de`), lead-verified -- found a genuinely cheaper path the original 4 questions didn't anticipate.** Frontend reframed the whole question: `compose_character_input` (`pf1_adapter.rs:268`) isn't a general composer at all, it's a single hardcoded canonical fixture, and already has exactly one precedent for a class needing an extra choice -- Wizard's school specialization is silently seeded (Evocation/opposed Necromancy+Transmutation) with zero picker anywhere in the UI, the code's own comment stating outright this is deliberate out-of-scope-for-now framing. **Lead independently confirmed both load-bearing claims directly**: the Wizard block (`pf1_adapter.rs:304-328`, comment text verbatim), and `LevelUpCharacterRequest`'s existing `additional_choices: Vec<SelectedChoiceDto>` field (`character_hub.rs:862-871`) as the proven shape Path B would extend `CreateCharacterRequest` with. Two real paths sized honestly: **Path A** (mirror Wizard exactly, ~30-45 lines total across 3 classes, zero wire-contract change, ships all three as genuinely creatable immediately, real cost is "silently the same canonical build, no player choice" named plainly) vs **Path B** (a real interactive picker -- wire-contract change, new frontend surface, and per-class option content that's NOT interchangeable: Sorcerer's linked bloodline+bond choice, Cleric's mostly-unsupported domain list, Druid's binary with only one side computing -- genuinely multi-part, not one bounded slice). Frontend also confirmed Hunter's/Cavalier's own companions are unconditional (never a choice per RAW), so a generic picker built for Druid's shape shouldn't be forced onto them -- directly answering the "don't assume" caveat from the original assignment. **Greenlit Path A now, deferred Path B as real future product-decision work.** Routed Path A's small Rust implementation to backend (keeps `pf1_adapter.rs` ownership clean) rather than having frontend build it directly; frontend's own follow-on (the `characterHubModel.ts` label update, from `headless-only` to `full` or possibly `full-except-human-level-1` pending a per-class re-check) queued for once it lands.

**Skald spellcasting scoping doc landed, an even deeper reuse than the comparison suggested -- lead independently verified the purity claim before endorsing.** Backend found Skald's own spells-known/spells-per-day tables are byte-identical to Bard's already-shipped tables (row for row through level 10), verified against 2 independent primary sources, with an honest note about a fetch-tool rendering artifact (a spurious leading "0" column) resolved by trusting Bard's own already-2-source-verified numbers as anchor rather than a fresh, unverified read. Proposed calling `bard_spell_level_access`/`bard_spells_known_table` directly for Skald rather than duplicating, since (unlike Wolf/Horse's functions, which push explanation records) these are pure numeric lookups with zero shared-mutation risk -- flagged as a related-but-distinct judgment call from the Wolf/Horse precedent, asking for confirmation. **Lead independently confirmed both functions are genuinely pure** (`pilot_compute.rs:22302-22335`, no explanations/diagnostics pushed) and endorsed direct reuse -- duplicating identical, side-effect-free logic would be pointless. Also independently noted (not a concern, just clarifying) that the per-day table itself isn't a standalone function today, just an inline match duplicated at 2 Bard call sites -- Skald will need its own copy of those specific match arms, which the doc already correctly implies rather than claiming a function to call. Backend also empirically confirmed, before building, that Bard's own known-spell closure already reaches full `Computed` today despite its other named features never being individually grounded -- meaning Skald likely reaches full `Computed` too once its own spellcasting narrows the same way. Building now.

**Backend self-caught and corrected a real mistake mid-build, before it shipped -- lead independently verified both halves of the correction.** Backend's own earlier prediction ("Skald likely reaches full Computed too, mirroring Bard's precedent") turned out wrong: checked WHY Bard reaches Computed and found it's not "unbuilt features don't block" at all -- Bard has ~20 other named features individually grounded from a pre-v0.6 SD13-E5 cycle (Bardic Knowledge, Fascinate DC/count, Frightening Tune DC, Inspire Courage, Versatile Performance, Deadly Performance DC, Inspire Competence, Inspire Heroics AC/save/target, Jack of All Trades, Lore Master, Soothing Performance, Well-Versed), and known-spell-posture/performance-execution were just the LAST two gates closing on an otherwise near-complete class. Skald has none of that prior investment -- only Inspired Rage is built. **Lead independently grepped both classes' actual grounded-feature ids**: Bard genuinely has ~20 (confirmed by name, matching the list above), Skald genuinely has exactly 1 (`inspired_rage_execution`). Confirms the correction is accurate, not overcautious. Skald will stay honestly Blocked on a retitled `other_features_deferred` diagnostic (mirroring Brawler's own naming) even after spellcasting closes -- still a real, worthwhile closure (Inspired Rage + spellcasting both genuinely grounded), just not a full-`Computed` one this cycle. Building now on the corrected understanding.

**SKALD SPELLCASTING COMMITTED (`7e7f6fbd`) AND RE-VERIFIED AGAINST THE REAL HEAD -- confirmed genuinely stays Blocked, real receipt status not just an empty-diagnostics claim.** **Lead independently confirmed in code**: `SKALD_CLASS_ID` filtering present in both spell-selection filters, `push_skald_other_features_deferred_diagnostic` called exactly once from the top-level dispatch (not duplicated), `bard_spell_level_access` reused directly for the access ceiling, Skald's own `skald_base_spells_per_day_table` matches Bard's per-day table byte-for-byte at every level checked, DC formula (10 + spell_level + CHA mod) confirmed correct. **Lead ran the actual Blocked-status test directly** (`skald_stays_blocked_with_the_new_narrower_diagnostic_not_the_retired_one`) -- asserts real `HeadlessReceiptStatus::Blocked` through the production pipeline with both the retired generic diagnostic AND the retired `spellcasting_deferred` diagnostic confirmed absent. Also answered backend's own open question on the SD-24 audit's `named_features_wired` count: **reproduced the exact corpus-grep methodology** (`grep -oE "KEY:Skald ~ [^\t]+" acg_abilities_class.lst | sort -u`, got exactly 20, matching `named_features_expected`) and confirmed spellcasting is NOT one of those 20 tagged ability records at all -- it's a structurally separate corpus mechanism (`SPELLLIST:` token, not a `KEY:Skald ~ ...` tag), so leaving the count at 1 (Inspired Rage only) is correct, not an undercount. **Lead ran everything fresh**: full lib 428/428, all 11 Skald tests individually, Bard's own 14 tests confirmed completely unaffected, `sd24_acg_class_coverage_audit` 3/3, full workspace sweep zero failures anywhere. Clean, no concerns -- told backend to commit whenever ready.

**Re-verification confirms `7e7f6fbd` matches the pre-commit check exactly** -- full lib 428/428, coverage audit 3/3 re-run fresh against the actual committed tree. Backend now starting Path A's choice-picker default (Sorcerer/Cleric/Druid), reading the full scoping doc first rather than working from the chat summary alone.

**MILESTONE -- PATH A SHIPPED (`9bafe303`), THE PRODUCT-REACHABILITY GAP FOR SORCERER/CLERIC/DRUID IS GENUINELY CLOSED, lead independently verified end-to-end including command-path equivalence.** Backend seeded canonical defaults in `compose_character_input` mirroring Wizard's own precedent exactly: Sorcerer gets Arcane bloodline + familiar bond, Cleric gets Good domain, Druid gets animal-companion nature bond (Wolf automatic). Fixed 2 pre-existing desktop tests whose fixtures assumed the old blocked posture (the diagnostic-shape pin now expects an empty set for these 3; the wealth-blocked-fixture test swapped its subject from Cleric, which now reaches Computed, to Oracle, which genuinely doesn't). **Lead independently read all 3 conditional blocks directly** in `pf1_adapter.rs`, confirmed each cites the real test it was checked against. **Lead ran the full desktop Rust suite (212/212) and frontend suite (78/78) fresh** against the actual committed HEAD -- both match exactly. **Lead went one step further than the existing tests to confirm real product-reachability, not just an internal diagnostic check**: traced `create_character_at_root`'s own logic (`character_hub.rs:622-635`) and confirmed it calls `compose_character_input` then gates Blocked/Saved on the exact same claim-blocking-diagnostics condition the updated `claim_blocking_diagnostic_ids` test exercises -- proving the existing test IS a genuine stand-in for the real command path, not a divorced check that could diverge from it. All 10 classes that reach `Computed` via the compute engine are now also reachable through the real product for the first time this session. Path B (a real interactive picker) stays deliberately deferred, same framing as Wizard's own precedent. Frontend cued for the one remaining piece: the `characterHubModel.ts` label update, re-checked per class (Sorcerer's level-2 cap, whether the shared hybrid-level-1-Human gate applies to any of the three) rather than assumed uniform, live-verified through the real dev build before finalizing.

**Frontend's per-class re-check surfaced real level-range differences the lead independently verified before frontend finalizes the label.** All three read directly rather than assumed: confirmed none of Sorcerer/Cleric/Druid appear in `hybrid_level1_class`'s match arms (only Paladin/Ranger do), so none need the `full-except-human-level-1` carve-out. But the ranges genuinely differ per class: **Sorcerer** only levels 1-2 (a real, unconditional level-3+ cutoff via `ARCANE_BLOODLINE_BONUS_LEVEL = 3`); **Cleric** no level cap found on the Good-domain-only path (`MAX_SUPPORTED_CLERIC_LEVEL = 20`); **Druid** literally only exactly level 1 (the code's own condition is `animal_companion_chosen_top && druid_level == 1`, level 2+ falls to the catch-all Blocked). **Lead independently confirmed all three constants/conditions directly in code**, matching frontend's read exactly. Frontend now live-verifying through the real dev build (Sorcerer level 2 Computed vs level 3 Blocked, Druid level 1 Computed vs level 2 Blocked) before finalizing each class's real `levelOptions`, rather than defaulting all three to the same shape.

**Backend found a 7th ACG/APG closure target: Inquisitor's Judgment (APG), a real course-correction from the earlier scan's "not cheap" verdict, lead independently verified before greenlighting.** Backend narrowed Judgment's full 10-type breadth down to Justice as the canonical MVP -- exactly the same "narrow to one canonical choice" move that made Alchemist's Mutagen and Cleric's Good-domain-only scoping tractable, not a reversal of the original read (which was about the full breadth). **Lead independently pulled the corpus and confirmed**: `InquisitorJudgmentTimes = 1+(InquisitorLVL-1)/3` matches exactly; Justice's own entry (`Judgment / Justice`, both Sacred/Profane variants) gives `1+InqJudgeJusticeLVL/5` with `InqJudgeJusticeLVL = InquisitorLVL` -- identical value regardless of alignment, confirming no branching needed for the number itself; zero `KEY:Inquisitor ~ Domain` entries anywhere (Domain genuinely grants no power, just spells); zero prior Inquisitor wiring in `pilot_compute.rs` beyond the chassis test row. Also confirmed the attack-bonus integration point backend plans to reuse is already proven -- Bard's Inspire Courage and Cleric's Touch of Good both already add their own class-gated terms into the exact same `melee_attack_bonus` sum (`pilot_compute.rs:23332-23336`), so this needs no new plumbing. No full review needed -- proceeding to build, with the scoping doc explicitly naming the "narrowed to one canonical choice, not a reversed verdict" framing per the lead's ask.

**MILESTONE -- PRODUCT-REACHABILITY GAP FULLY CLOSED (`adf57cfb`), lead independently re-verified against the real committed HEAD.** Frontend's `characterHubModel.ts` update lands with real, live-verified per-class support: Sorcerer `full`/`[1,2]` (level 3 correctly stays blocked, real diagnostic shown, not silently advanced -- confirmed via the actual `LevelUpDialog` boundary crossing), Cleric `full`/`[1,2,3]` (no cap), Druid `full`/`[1]` (level 2 correctly stays blocked, same boundary-crossing discipline). None needed `full-except-human-level-1` -- confirmed directly none of the three appear in `hybrid_level1_class`'s match arms. **Lead independently read the committed `CLASS_OPTIONS` entries directly** (matches exactly), **ran the full frontend suite (78/78) and typecheck** against the real committed HEAD -- both clean. A nice side-effect frontend flagged and the lead agrees is worth noting: the level-up flow handles Blocked boundary crossings gracefully (stays at last valid level, shows the real diagnostic, never silently corrupts or half-applies) -- a genuine positive finding about that code path's own correctness, discovered incidentally. **All 10 classes that reach `Computed` via the compute engine are now genuinely reachable through the real product** -- this closes out risks item 8's product-reachability sub-thread for good, the first time this whole "N of 27 Computed" claim has been simultaneously true for both the engine AND the shipped product.

**Real pre-commit gap found in Inquisitor's Judgment build -- backend's own doc comment misdescribed an existing precedent, caught before it shipped.** Lead independently verified the exact-match gate, Justice's attack-bonus formula (`1+level/5`), and the times-per-day formula (`1+(level-1)/3`) -- all correct, matching the corpus exactly. But `ground_or_block_inquisitor_judgment` never reads `activation.rounds_consumed_today` at all, unlike every prior activation-gated closure this session (Rage, Bloodrage, Skald's Inspired Rage), meaning a claimed-active Judgment with a wildly over-budget `rounds_consumed_today` would silently pass with the full bonus applied, no diagnostic. The code's own doc comment justified this by claiming "Bloodrage's/Rage's own rounds-per-day values" are similarly unenforced -- **lead independently re-checked this specific claim against Barbarian's real code** (`pilot_compute.rs:15535-15546`) and found it's factually wrong: Rage genuinely DOES check `rounds_consumed_today` against the budget and DOES claim-block with a real `rounds_exceeded` diagnostic when exceeded. Flagged precisely with both code citations rather than a vague "something seems off" -- backend fixing to add the same enforcement before committing.

**Over-budget gap fully fixed in Inquisitor's Judgment -- backend found the fix needed TWO spots, not one, and caught the second by writing the test first.** After confirming the doc comment's claim about Rage was wrong, backend added the over-budget check to `ground_or_block_inquisitor_judgment` (the diagnostic/grounding function) -- but then wrote the over-budget test before assuming that was sufficient, and it caught a second gap: `active_inquisitor_justice_judgment_bonus` (the separate helper `compute_combat_baseline` actually calls to apply the attack bonus) had its own independent budget check missing, so the diagnostic fired correctly while the +1 bonus still silently applied underneath. Fixed by mirroring `active_barbarian_rage_bonus`'s own two-check structure (grounding function AND bonus-application function both independently re-verify the budget). **Lead independently confirmed both fixes directly in code**: `active_inquisitor_justice_judgment_bonus` (`pilot_compute.rs:8349-8354`) now returns `None` when over budget, matching Barbarian's own structure exactly. **Lead read the new test in full** (`single_class_inquisitor_over_budget_judgment_stays_blocked_and_applies_no_bonus`) -- confirms both the diagnostic fires AND the real `combat.baseline_melee_attack_bonus` value stays at 5 (not 6), proving no silent leak, not just an assertion of intent. **Lead ran everything fresh**: full lib 435/435, all 7 Inquisitor tests individually, `sd24_apg_class_coverage_audit` 3/3, full workspace sweep zero failures anywhere. One purely cosmetic nit flagged, not blocking (a test function name that hasn't been updated to mention Inquisitor even though its body correctly includes it). Third APG class-specific closure, real bug found and fixed before it ever shipped.

**Doc reconciliation after the git-index race: Inquisitor's Judgment now recorded as fully committed (`22abe6e5`), tallies and queued-next list brought current.** 7 classes across both books now have real partial engine progress (Skald/Bloodrager/Brawler/Hunter on ACG, Cavalier/Alchemist/Inquisitor on APG); APG tally moves to 3 of 6. Queued-next list rewritten -- both Skald's spellcasting and Path A's choice-picker default (previously listed as "next up") are done and removed; Deflect Arrows, the remaining test-cleanup waves, and the 9 untouched ACG/APG classes are what's left in this bucket. Backend idle, no next target chosen yet as of this check.

**New phase directed: with the cheap-win scan genuinely exhausted (7 closed, 9 ruled not-cheap), backend is scoping the cheapest FULL build among the 9 untouched ACG/APG classes -- class-skill-list + named features + spellcasting, the same scale as the original Cleric/Druid/Sorcerer CRB builds, not another single-ability slice.** Directed the same comparative-scoping discipline used for Skald/Bloodrager/Alchemist's spellcasting cost: check for spell-list reuse (Investigator's own `SPELLLIST:1|Alchemist` token was found earlier but Alchemist's list still needs to actually exist as a built spell list, not just Mutagen, before that's a real shortcut -- worth re-checking), real corpus-derived class-skill-list sizes, and named-feature complexity (favor flat/simple over chooser-list-with-real-variety shapes, the same distinction that made Cleric's domain powers tractable vs. Investigator's Inspiration hard). Non-caster classes (Slayer, Swashbuckler) skip the spellcasting question but still need their own named-feature complexity weighed -- Slayer's Studied Target was already ruled opponent-dependent for the ability-only slice, worth checking whether that wall still applies to a full build. Backend to report a real cost comparison across all 9 before picking a target, given this is a bigger commitment than the last 7 closures.

**Full-build comparison across all 9 untouched ACG/APG classes landed, lead independently re-verified the load-bearing corpus claims before endorsing Arcanist.** Backend's finding: Investigator's `SPELLLIST:1|Alchemist` is a false shortcut (Alchemist's own spellcasting isn't built, only Mutagen); Witch has the fewest named features on paper (7) but the highest real cost once accounted for (fresh spell list, fresh known-spells table, AND a brand-new familiar stat-block this codebase has no equivalent infrastructure for at all); Oracle's spontaneous 9th-level table is the single biggest table-verification task of the 9; Warpriest's Cleric spell-list reuse is real but its Channel Energy formula genuinely differs from Cleric's own, not a clean copy. **Arcanist recommended and independently confirmed**: `CLASS:Arcanist`'s exact tokens (`SPELLSTAT:INT`/`MEMORIZE:YES`/`SPELLBOOK:YES`/`SPELLLIST:1|Wizard`) match, its class-skill list is genuinely 8 tokens, exactly 9 `KEY:Arcanist ~ ...` records confirmed matching the doc's own list, Arcane Reservoir's formula (`3+level/2` daily fill, `3+level` max) confirmed correct. Arcanist is the first APG/ACG class this session with a genuine shot at reaching full `Computed` (not just a narrower-Blocked diagnostic like all 7 prior closures) -- the deferred piece (Exploits) is a chooser-list, the exact same "grant-only identity record, no blocking" idiom that already let Bard/Wizard/Rogue reach `Computed` with their own unbuilt chooser-lists. Directed backend to write the actual build scoping doc next (same rigor as the original Cleric/Druid/Sorcerer builds, including independently verifying Arcanist's spells-known/per-day table against 2 sources before assuming byte-identity with Wizard's) -- lead will read it closely and decide on a formal review given the scale, without necessarily requiring one if the plan reuses proven patterns cleanly.

**Arcanist's build scoping doc landed with a real self-caught correction to the earlier comparison, lead independently re-verified before greenlighting.** Backend's own verify-before-build discipline paid off: the comparison doc had characterized Arcanist's `SPELLLIST:1|Wizard` reuse as sharing Wizard's own numbers, but checking the actual per-day formula against 2 independent sources found it's genuinely Arcanist's own table (level 1: 4 cantrips/2 first-level vs. Wizard's 3/1; 2nd-level access at level 4 vs. Wizard's level 3) -- the spell-list CONTENT and casting SHAPE are shared, the NUMBERS are not. **Lead independently re-derived this from the raw corpus tokens** (`ArcanistPreparedLVL_0/1/2`'s exact `BONUS:VAR` formulas, hand-evaluated at levels 1-3) and cross-checked against Wizard's own table in code (`pilot_compute.rs:19457-19464`) -- confirms the correction is real, not overcautious. Also confirmed the "Wizard needed a separate bootstrap-deadlock follow-on cycle to become product-reachable" precedent backend cites for Arcanist's own likely two-cycle shape genuinely exists in the codebase (`pf1_adapter.rs:89,261,267`). One genuine simplification found the other way: Arcanist has no arcane-school/specialization mechanic at all (confirmed via its full 9-record KEY list), so its spellbook validation needs no opposed-school gate the way Wizard's own does. Greenlit to build the full slice as one cohesive commit (gate widening, Arcane Reservoir, class-skill wiring, spellcasting, exploits-deferral diagnostic together) -- matching how Cleric/Druid/Sorcerer's own full spell-math builds each landed in one pass this session, not fragmented. No full adversarial review dispatched -- the gate-widening pattern is proven 7 times now, the spellbook-validation pattern mirrors an already-reviewed one, and the one real judgment call (parallel vs. shared spell-level-access function, given Wizard's own access ladder genuinely differs) backend already reasoned through correctly themselves. Building now -- first APG/ACG class with a genuine, if two-cycle, path to full product-visible `Computed`.

**Arcanist's full build code-complete, lead independently verified pre-commit including a real methodology question on the coverage count.** Backend flagged (rather than assumed) whether Cantrips should count as its own wired named feature alongside Arcane Reservoir and Spells Prepared, since `KEY:Arcanist ~ Cantrips` is a genuine, distinct corpus record. **Lead checked the actual code**: the cantrips NUMBER is grounded by the exact same generic per-day-table loop that produces every other spell level (`class_spell.acg.arcanist.base_spells_per_day.spell_level_0`, same id format as `spell_level_1`/etc.) -- no distinct logic implements Cantrips' own actually-distinguishing mechanical content (no-slot-consumption, not-expended-when-cast), which this codebase doesn't model for any class. **Recommended counting 2, not 3**, citing Bloodrager's own precedent (Greater/Mighty/Tireless Bloodrage are each their own separate `KEY:Bloodrager ~ ...` records too, correctly never double-counted beyond the base Bloodrage) -- backend had already applied this exact count by the time the recommendation landed. **Lead independently confirmed the real spellbook-validation test**: `single_class_arcanist_with_a_real_prepared_spell_grounds_the_spellbook_and_stays_blocked_only_on_exploits` asserts real `HeadlessReceiptStatus::Blocked` even with a genuinely valid recorded spellbook, exactly the honest two-cycle framing the scoping doc predicted (this build reaches "stays Blocked on exploits alone," the product-reachability piece stays a separate future follow-on). Also re-confirmed the exact-match gate and the zero-new-code class-skill-wiring claim (Arcanist's own list genuinely excludes Climb/Intimidate/Swim). **Lead ran everything fresh**: full lib 443/443, `sd24_acg_class_coverage_audit` 3/3, full workspace sweep in flight. One cosmetic nit flagged, not blocking (a test function name still says "four" closures though its body correctly includes Arcanist as a fifth). First ACG class with real spellcasting, verified thoroughly before it ships.

**MILESTONE -- ARCANIST'S FULL ACG BUILD COMMITTED (`ce73a598`) AND RE-VERIFIED AGAINST THE REAL HEAD -- fifth ACG/APG class-specific closure, first genuine full-scope build outside CRB.** Both background sweeps (workspace + desktop) finished clean while the lead's own check-in was in flight (slow due to build-lock contention between the two, not a stall). **Lead independently re-ran everything fresh against the actual committed tree**: full lib 443/443, `sd24_acg_class_coverage_audit` 3/3 (with the correctly-renamed "five closures" test), and all 8 Arcanist-specific tests individually, including `arcanist_gets_no_class_skill_bonus_on_any_of_the_three_skills` confirming the zero-new-code class-skill claim held. Arcanist's real spellbook validation, Arcane Reservoir, and class-skill wiring are all genuinely grounded; it stays honestly `Blocked` on `exploits_deferred` alone even with a valid recorded spellbook (confirmed via the actual `HeadlessReceiptStatus::Blocked` test, not assumed) -- a genuine, if not-yet-reached, path to full product-visible `Computed` exists, needing its own separate `compose_character_input`-seeding follow-on, the same two-cycle shape Wizard's own historical bootstrap-deadlock fix had and Sorcerer/Cleric/Druid's own Path A already proved out. This closure represents the biggest single build this segment has done outside the original CRB casters -- full chassis, real skill wiring, a genuinely independent (not Wizard's) spells-per-day table caught and corrected mid-scoping, and a resource pool, all landing together as one cohesive slice with zero full adversarial review needed given how proven every individual pattern already was. 5 of 10 ACG classes now have real partial engine progress; queued-next reconciled to include Arcanist's own product-reachability follow-on as the new top item.

**Backend self-caught a real gap before committing a speculative follow-on, cleanly reverted -- lead independently verified the correction and fixed the docs' own premature framing.** Backend attempted the natural next step after Arcanist landed: seeding a canonical starter spell in `compose_character_input`/`apply_level_up`, mirroring Wizard's own historical bootstrap-deadlock fix exactly. Caught before committing that it doesn't work: Arcanist's `exploits_deferred` diagnostic is unconditionally `claim_blocking` by design (same permanent shape as Cavalier/Alchemist/Inquisitor's own diagnostics), unlike Wizard's own remaining diagnostic which is genuinely conditional and clears once its conditions are met -- so seeding a spell has zero product-visible effect, `create_character_at_root` stays `Blocked` either way. Reverted the `pf1_adapter.rs` change entirely (confirmed clean, purely additive, desktop back to 212/212). **Lead independently confirmed the finding directly in code**: `push_arcanist_exploits_deferred_diagnostic` (`pilot_compute.rs:8886-8899`) hardcodes `claim_blocking: true` unconditionally with no gating condition, contrasted against Wizard's own genuinely-conditional diagnostic (`pilot_compute.rs:19783-19790`, only fires in the `else` branch when its own conditions are unmet). This also meant correcting the lead's own docs, which had prematurely framed Arcanist's remaining gap as "just needs a compose_character_input seed" right after the build landed -- fixed across `SWARM_REPORT.md`'s progress line, ACG table row, tally, and queued-next item, plus `SWARM_TASKS.md`'s own class-breadth row, to correctly state the real blocker is Exploits itself (or an equivalent deferral change), not a missing seed. Good discipline on backend's part catching this before shipping speculative work with no payoff, and flagging it explicitly so a future cycle doesn't rediscover it.

**Warpriest's full-build scoping doc landed with two real findings, lead independently verified both before greenlighting.** Backend moved to Warpriest (the documented runner-up from the earlier comparison) rather than tackling Arcanist's own Exploits (correctly judged too uncertain in scope, chooser-list-shaped like Investigator's Inspiration). Applied the Arcanist lesson immediately: checked Warpriest's real per-level `CAST:` rows directly in the corpus rather than assuming they matched Cleric's own (which they share a spell list with) -- **lead independently confirmed**: levels 1-2 match Cleric's exactly (`[3,1]`, `[4,2]`), but level 3 diverges (Warpriest `[4,3]`, no 2nd-level access yet, vs. Cleric's own `[4,2,1]`), and 2nd-level access doesn't start until Warpriest level 4 with count 1 vs. Cleric's level-4 count of 2 -- same "list content shared, numbers not" shape as Arcanist-vs-Wizard, caught before it became a doc claim this time. Proposed Destruction Blessing (`Destructive Attacks`: touch an ally, `+max(1,level/2)` morale bonus on weapon damage) as the domain-choice MVP, structurally identical to Cleric's own Touch of Good -- **lead independently confirmed the exact DESC text and formula**. Also found a real, independent correctness bug: Warpriest's own class-skill list genuinely includes Climb/Intimidate/Swim, but `selected_skill_class_skill_bonus_applies` (`pilot_compute.rs:23775-23779`) only recognizes Fighter/Rogue today -- **lead independently confirmed this in code** -- meaning a Warpriest reaching `has_supported_class_chassis` would silently be denied a bonus it genuinely earns, the exact mirror-image of the historical Wizard bug (false positive there, false negative here). Greenlit both Destruction Blessing and the skill-bonus widening fix -- no full review needed, the domain-narrowing pattern is proven and the skill-bonus fix already has a documented, precedented methodology from the Wizard case. Building now.

**Warpriest's full build code-complete, lead independently verified pre-commit including the skill-bonus fix's regression safety.** Confirmed `Orisons`' corpus text is genuinely the same "0-level, cast like any spell, not expended" shape as Arcanist's own Cantrips finding -- no distinct implementable content, so backend's `named_features_wired = 2` (Blessings, Sacred Weapon) is correct, confirmed in code. **Lead ran the specific skill-bonus regression tests directly**: Fighter/Rogue/multiclass-Fighter+Wizard still correctly get the class-skill bonus, Wizard/Arcanist still correctly get zero (both unaffected by the widening), and Warpriest now correctly gets it on all three skills -- the fix is clean, no collateral regression. Also independently confirmed the exact-match gate and ran all 8 Warpriest-specific tests individually (Destruction Blessing active/inactive branches, the spoofed-entry safety test, the real prepared-spellbook grounding). Backend's own dead-code catch (a helper written for Destructive Attacks' damage bonus with no real consumer, removed rather than forcing an artificial integration point) is exactly the discipline this session runs on. Full lib 451/451, coverage audit 3/3 (correctly renamed to "six" closures). Full workspace sweep in flight.

**MILESTONE -- WARPRIEST'S FULL ACG BUILD COMMITTED (`03bb35d3`) AND RE-VERIFIED AGAINST THE REAL HEAD -- sixth ACG/APG class-specific closure, second genuine full-scope build outside CRB.** The commit had sat uncommitted across several consecutive check-in cycles with no active cargo/rustc process; sent backend a check-in message, and on the next wakeup the working tree was clean with the commit landed (`03bb35d3`, immediately followed by an auto-heartbeat refresh). **Lead independently re-ran everything fresh against the actual committed tree, not trusting the pre-commit verification alone**: all 8 Warpriest-specific tests individually (dispatch-widening safety, both blocked/grounded postures, the skill-bonus test, Destruction Blessing active/inactive), `sd24_acg_class_coverage_audit` 3/3 with the correctly-renamed "six closures" canary, and full lib 451/451 -- all clean, matching the pre-commit numbers exactly. Warpriest's real prepared spellcasting (independently-verified own table diverging from Cleric's at level 3+), Blessings, Sacred Weapon, and Destruction Blessing's Destructive Attacks are all genuinely grounded; it stays honestly `Blocked` on `other_features_deferred` alone. The class-skill-bonus widening fix (Climb/Intimidate/Swim) is confirmed live and regression-clean on the committed HEAD. Updated `SWARM_REPORT.md` (Agent Status, progress line, ACG table row, ACG tally, both tallies in the header intro, the untouched-classes enumeration) and `SWARM_TASKS.md`'s class-breadth row to reflect 6 of 10 ACG classes with real progress, 7 classes (not 8) remaining fully untouched. Both backend and frontend now idle -- no new wave queued this cycle; watching for either teammate's next move or the operator's reply on the crontab question.

**Second comparative scoping doc landed (Slayer recommended), lead independently re-verified every claim before greenlighting.** Backend ran the same comparison discipline across the 7 remaining untouched ACG/APG classes (Investigator, Shaman, Slayer, Swashbuckler, Oracle, Summoner, Witch). Self-caught a real correction to the ORIGINAL comparison doc first: Shaman's Spirit Animal was characterized there as reusable Wolf-companion code -- checked directly against the corpus this time and it's genuinely a **Familiar** (`TYPE:...Familiar`), the same unbuilt infrastructure gap Witch's own Familiar needs, raising Shaman's real cost above the original estimate. Recommended Slayer: a non-caster (zero spellcasting scope) with 4 real flat formulas (Sneak Attack dice, Trap Sense, Trapfinding, Track) that don't integrate into an existing total, an idiom backend correctly identified as already-precedented (Barbarian's and Rogue's own Trap Sense are both already grounded as standalone flat records). Also flagged a third class-skill-bonus widening need (Slayer's own list includes Climb/Intimidate/Swim too). **Lead independently verified every claim directly against `acg_abilities_class.lst`/`acg_classes.lst` before responding**: Shaman's Spirit Animal record confirmed `TYPE:...Familiar`; Slayer confirmed non-caster (no `SPELLSTAT` token); all four formulas confirmed byte-exact (`SlayerLVL/3`, `max(1,SlayerLVL/3)`, `SlayerLVL/2`, `max(SlayerLVL/2,1)`); Slayer's own Class Skills record confirmed to include Climb/Intimidate/Swim; and the Barbarian/Rogue Trap Sense precedent confirmed directly in `pilot_compute.rs:16045-16062`/`:18259-18270` as genuinely standalone, zero-integration records. Greenlit Slayer as scoped -- gate widening, four flat formulas, narrower deferral diagnostic, and the third class-skill-bonus widening fix (to be verified with a dedicated test, not assumed). Building now.

**Slayer code-complete, lead independently verified pre-commit including the honest 4-count reasoning.** Backend followed the skill-bonus fix's own TDD discipline exactly as instructed: wrote `slayer_gets_the_class_skill_bonus_on_all_three_skills` first, confirmed it failed (left:3, right:6, proving the bug), then widened `selected_skill_class_skill_bonus_applies` to include Slayer and reran to confirm green. Went with a genuine `named_features_wired = 4` rather than folding like Arcanist/Warpriest's 2, reasoning that Sneak Attack/Trap Sense/Trapfinding/Track are four structurally independent mechanics with no shared table linking them (unlike Cantrips/Orisons sharing their class's general spellcasting table) -- documented inline so the counting methodology stays traceable across all three decisions. Also fixed two stale/incomplete docs while updating (`acg_slayer_coverage.md`'s old "Gap features" list only had 7 of the real 15 `KEY:Slayer` records). **Lead independently verified everything on the uncommitted working tree**: all four formula functions match the corpus byte-for-byte, `is_supported_slayer_single_class` mirrors the other six gates, the skill-bonus widening is in place, all 6 Slayer-specific tests pass, coverage audit 3/3 (correctly renamed to "seven" closures), full lib 457/457 -- all matching backend's own numbers exactly. When the tree sat uncommitted with backend idle, the lead ran the full workspace sweep directly (6000+ lines, zero FAILED lines, including the real end-to-end PCGen tests) and told backend it was clear to commit.

**MILESTONE -- SLAYER'S FULL ACG BUILD COMMITTED (`77910256`) AND RE-VERIFIED AGAINST THE REAL HEAD -- seventh ACG/APG class-specific closure, third genuine full-scope build outside CRB, first with zero spellcasting scope.** Backend's own workspace sweep also came back clean independently (477/477 test-result blocks, 457/457 lib, both audits, 212/212 desktop) before seeing the lead's confirmation -- two independent clean sweeps, not one relied on twice. Backend also caught that the lead's own prior docs commit (`b332722f`) was sitting locally unpushed on the shared checkout, confirmed via merge-base it was a genuine fast-forward ancestor before pushing both together -- good shared-checkout discipline. **Lead independently re-ran everything fresh against the actual committed tree**: all 6 Slayer-specific tests individually, `sd24_acg_class_coverage_audit` 3/3 with the correctly-renamed "seven closures" canary, and full lib 457/457 -- all clean, matching pre-commit exactly. Slayer's four flat formulas and the third class-skill-bonus widening fix are all genuinely grounded; it stays honestly `Blocked` on `other_features_deferred` alone (Studied Target, opponent-dependent; Slayer Talents, a chooser-list). Updated `SWARM_REPORT.md` (Agent Status, progress line, ACG section header/intro, ACG table row, ACG tally, untouched-classes enumeration) and `SWARM_TASKS.md`'s class-breadth row to reflect 7 of 10 ACG classes with real progress, 6 classes (not 7) remaining fully untouched. Backend idle, ready for whatever's next -- watching for the operator's reply on the crontab question, still unanswered.

**Second full-build comparison across the 6 remaining classes came up empty -- backend correctly pivoted to Arcanist's own Exploits, a potential first-ever product-visible `Computed` for a non-CRB class, flagged for review given the milestone size.** Investigator/Shaman/Swashbuckler/Oracle/Summoner/Witch all confirmed a real, concrete complexity blocker each -- the same "well is dry" finding as the original single-ability scan, now proven true at full-build scale too. Backend's real find: of Arcanist's 46 Exploits, one -- Metamagic Knowledge ("select one metamagic feat as a bonus feat") -- carries no Reservoir-cost tag at all, unlike the other 45 (Quick Study/Potent Magic/Fast Healing/See Magic all explicitly expend a Reservoir point); it's a one-time bonus-feat grant, the same shape as Fighter's own bonus feat choice. Found that `feat_prereqs::metamagic` (an unrelated, already-landed SD-20 Epic 3 module) already validates real Metamagic-category feats against the catalog -- genuine reuse, not new logic. Bigger find: `ground_or_block_arcanist_class_features` pushes exactly two diagnostics today (spellbook, conditional; `exploits_deferred`, unconditional) -- narrowing the second to recognize Metamagic Knowledge would clear the LAST claim-blocking gap, reaching real `Computed` for the first time among all ACG/APG classes. Flagged two questions before building: whether Empower Spell is the right canonical feat to recognize, and whether the payoff read is correct. **Lead independently verified everything before responding**: the 46-count grep, Metamagic Knowledge's own corpus record (confirmed no `BONUS:VAR` reservoir tag, uses `BONUS:ABILITYPOOL` instead), spot-checked all four contrasting exploits directly (all four genuinely expend a Reservoir point), read `feat_prereqs::metamagic.rs` in full (confirmed real, already-tested, honestly bounded to catalog-membership-only per its own doc comment -- no per-feat `PREREQ:` chain evaluated, a pre-existing and already-reviewed scope boundary from Epic 3), confirmed `ground_or_block_arcanist_class_features` pushes exactly two diagnostics as described, and independently verified Empower Spell's own real corpus record (`cr_feats.lst`) carries zero `PREREQ:` token of any kind -- a safe, unambiguous pick (no CRB metamagic feat carries a real prerequisite in PF1, unlike 3.5, so this wasn't actually a risky choice among alternatives). Greenlit the build, with an explicit reminder that reaching `Computed` in a test fixture is still a separate question from product-reachability (no UI picker for this new choice either) -- the same Path A distinction already established for Sorcerer/Cleric/Druid, to be named honestly in the closure writeup rather than conflated. Building now -- if this lands clean, it would be the first non-CRB class ever to reach genuine product-visible `Computed`.

**Metamagic Knowledge closure code-complete, lead independently ran the actual milestone test before committing.** Backend built exactly as scoped: `ground_or_block_arcanist_metamagic_knowledge` reads `choice:arcanist_metamagic_knowledge`, validates whatever feat is named via the reused `feat_prereqs::metamagic` module (deliberately general, not hardcoded to Empower Spell alone -- a second test proves Silent Spell is recognized equally, since the reused module validates any real Metamagic feat with the same confidence), grounds the real feat grant and demotes `exploits_deferred` to non-blocking on success, or leaves the original unconditional diagnostic untouched on an ineligible/missing choice. 9 new tests: the milestone case itself, the other-real-feat case, ineligible-feat-stays-blocked, no-choice-stays-blocked (the original shape, unchanged), Metamagic-Knowledge-without-a-spellbook-still-blocked (proving it's not sufficient alone), and the spoofed-entry non-leak test. `named_features_wired` confirmed correctly unchanged (Metamagic Knowledge is a sub-list entry under the already-counted Exploits slot, same reasoning as Destruction Blessing under Warpriest's Blessings). **Lead independently ran the actual milestone test on the working tree before backend committed**: `single_class_arcanist_with_a_valid_spellbook_and_recognized_metamagic_knowledge_reaches_computed` calls the real `build_pilot_headless_receipt` entry point and asserts `HeadlessReceiptStatus::Computed` with zero claim-blocking diagnostics -- not a shortcut internal check. Confirmed real: all 14 Arcanist tests pass, coverage audit 3/3 (unchanged), full lib 463/463, `acg/mod.rs` confirmed zero diff (proving the no-bump claim). When the tree sat uncommitted for a stretch, the lead ran the full workspace sweep directly (clean, zero failures) and told backend it was clear.

**MILESTONE -- ARCANIST'S METAMAGIC KNOWLEDGE EXPLOIT CLOSURE COMMITTED (`54d8048b`) AND RE-VERIFIED AGAINST THE REAL HEAD -- Arcanist is the first non-CRB class this segment to reach genuine `HeadlessReceiptStatus::Computed`.** Backend's own sweep also came back clean independently (477/477 test-result blocks, 463/463 lib, both audits, 212/212 desktop) before seeing the lead's confirmation. **Lead independently re-ran everything fresh against the actual committed tree**: the milestone test itself, all 14 Arcanist-specific tests, `sd24_acg_class_coverage_audit` 3/3 (correctly unchanged at "seven" closures), and full lib 463/463 -- all clean, matching pre-commit exactly. This is engine-`Computed` in a headless test fixture only, explicitly not yet product-reachable: no picker exists for `choice:arcanist_metamagic_knowledge` in the real character-creation UI, the same Path A gap Sorcerer/Cleric/Druid/Wizard needed closed -- named honestly in a new `risks-and-open-questions.md` entry (a new instance of the same resolved-item's gap, not conflated with the already-closed one) rather than let the milestone imply more than it delivers. Updated `SWARM_REPORT.md` (Agent Status, progress line and count 10/27 -> 11/27, ACG section header/intro, Arcanist's own table row promoted from Blocked to Computed, ACG tally) and `SWARM_TASKS.md`'s class-breadth row. 9 classes (not counting Arcanist) have real partial engine progress, 6 remain fully untouched. Backend idle, ready for whatever's next -- watching for the operator's reply on the crontab question, still unanswered, and for whether backend or the lead schedules Arcanist's own Path A follow-on.

**Arcanist's own Path A follow-on picked up and closed the same day -- but the lead's own follow-up check found the milestone was only half-true, and corrected it before it shipped as an overclaim.** Backend closed the backend half fast: `compose_character_input` (and `apply_level_up`'s multiclass-dip branch, mirroring Wizard's own dual-site precedent since Arcanist -- like Wizard, unlike Sorcerer/Cleric/Druid -- needs a bootstrapped spell too) now seeds both the starter spell and `choice:arcanist_metamagic_knowledge -> Empower Spell`. New milestone test `arcanist_level1_reaches_computed_from_compose_character_input_alone` mirrors the Wizard precedent exactly and passes. **Lead independently ran both new tests plus the full desktop suite (214/214) on the working tree before backend committed** -- all clean, matching backend's numbers. Backend committed `772298bc`, pushed clean, own sweep also independently clean (477/477 test-result blocks, 463/463 lib, both audits, 214/214 desktop).

**Before accepting "product-reachable" at face value, the lead checked the actual React `CreateCharacterForm` directly -- and found a second, separate, still-open gap the milestone framing would have silently glossed over.** `characterHubModel.ts`'s `CLASS_OPTIONS` array is the sole source for the Create Character form's class dropdown (confirmed: a plain HTML `<select>`, `CLASS_OPTIONS.map(...)`, no free-text/Other option anywhere). Grepped it directly: zero entries for Arcanist. This is NOT the same shape as Sorcerer/Cleric/Druid's own original gap -- those three were already listed in `CLASS_OPTIONS` (as `headless-only`) before their own Path A closure, so fixing the backend seed and flipping their `supportLevel` to `full` was the complete, real fix. Arcanist was never listed there at all, so backend's Path A closure -- fully correct and verified for the backend command path -- does NOT yet make Arcanist selectable in the real shipped app. **Caught and fixed the lead's own premature docs framing before it went out**: had already written "UI: full, live-verified reachable" into `SWARM_REPORT.md`'s Arcanist table row based on backend's own accurate-but-incomplete claim, corrected it to "backend command path only, NOT yet in the real UI dropdown" before committing, and rewrote the progress line, Agent Status row, `SWARM_TASKS.md`'s class-breadth row, and added a correction paragraph to the `risks-and-open-questions.md` entry backend had already written (marked `PARTIALLY RESOLVED` rather than `RESOLVED`) -- all now honestly distinguish "Computed via the backend command layer" from "selectable in the real shipped UI" for Arcanist specifically. The fix itself is well-understood and small (a `characterHubModel.ts` `CLASS_OPTIONS` entry, mirroring `adf57cfb`'s own update) but not yet built or scheduled. Flagged to backend as a distinct follow-on, not re-opening the Metamagic Knowledge or Path A closures themselves -- both of those remain fully verified and correctly committed.

**Backend correctly deferred the `CLASS_OPTIONS` fix to frontend rather than reaching outside its own owned surface, and picked up Oracle as its next full-build target instead.** Directed by the lead: the 6 remaining untouched classes are all genuinely harder than what's closed so far (Shaman/Witch need a brand-new Familiar subsystem, Summoner needs an Eidolon, Investigator's Inspiration is the hardest chooser-list on the roster, Swashbuckler's Panache/Deeds is similar scale), but Oracle's own cost looked like "just a big spontaneous table" -- no new subsystem. Backend's own scoping found a real structural correction to that framing worth naming honestly before building: Oracle is spontaneous (`SPELLSTAT:CHA`, `MEMORIZE:NO`), so it needs the FULL Sorcerer/Bard-shaped known-spell validation (a known-spell-count cap plus a separate per-day cap), not the simpler prepared-spellbook shape Arcanist/Warpriest used -- genuinely more machinery, though still no new subsystem. Also proactively applied the Arcanist-table lesson: didn't trust an internally-contradictory AoN fetch, cross-checked with a second independent search, and went with the raw corpus as the tiebreaker. **Lead independently verified every claim**: chassis tokens, the exact per-level `CAST`/`KNOWN` table (levels 1-3 byte-exact), the class-skill list (confirmed no Climb/Intimidate/Swim, no bug here), Healing Hands' real `BONUS:SKILL|Heal|4` record, Clouded Vision's real `BONUS:VAR|OracleCloudedVisionRange|30` record, and that the three referenced Sorcerer functions exist at the cited lines. Greenlit Life Mystery/Healing Hands as the MVP pick and one commit (not split), consistent with every prior full build this segment landing as one cohesive slice. Building now -- would be an 8th ACG/APG class-specific closure.

**Frontend found a real, load-bearing bug in `772298bc` before shipping the `CLASS_OPTIONS` entry -- held it back rather than list a class that would error on save, exactly the discipline that closed the original Sorcerer/Cleric/Druid gap properly.** Followed the same rigor as that earlier work: confirmed Arcanist's real scope (single-class only, `ARCANIST_SPELLBOOK_SUPPORTED_MAX_LEVEL = 3`, no race gate), added the `CLASS_OPTIONS` entry, then tried to actually create one through the real dev build before finalizing -- and hit a raw error instead of a graceful `Blocked` diagnostic: `EMPOWER_SPELL_METAMAGIC_SELECTION = "Empower Spell"` has zero colons, but `local_store.rs`'s own save-layer validation requires every `selection_id` to have at least 2 colon-segments to round-trip through its fixture grammar -- rejecting the save AFTER the compute engine already correctly reached `Computed`. The exact same gap category as risks-and-open-questions.md's item 23 (a real feat key with no colon), recurring in a new, load-bearing spot. Backend's own milestone test only exercises `build_pilot_headless_receipt` on the in-memory `CharacterInput` -- it never touches the actual save/round-trip path, so it couldn't have caught this. **Lead independently confirmed all of it**: the colon-count check's exact code and its real position on the save-write path (not a divorced check), that `"Empower Spell"` has zero colons while all four other Path A values (`bloodline:arcane`/`bond:familiar`/`domain:good`/`bond:animal_companion`) correctly have exactly one. **Caught a real flaw in the lead's own first-pass fix suggestion before backend acted on it**: initially suggested a "one-line rename" to a colon-satisfying string, then re-checked `ground_or_block_arcanist_metamagic_knowledge` and realized `evaluate_metamagic_feat_prerequisites` validates the selection value directly against the real feat catalog by name -- a naive rename would silently regress Arcanist back to `Blocked` (catalog lookup would fail) rather than actually fixing anything. Sent a correction before backend started: needs a real translation layer (seed a namespaced id, map it back to the literal feat name before the catalog lookup), not a drop-in rename. Frontend reverted their own `CLASS_OPTIONS` addition cleanly and will re-verify live once backend's real fix lands.

**Both the colon-convention fix and Oracle's 8th APG closure landed together (`25c5bcae`), re-verified against the real HEAD.** The real fix: `arcanist_metamagic_knowledge_feat_name` translates a namespaced `metamagic:<slug>` seed back to the literal feat name before the catalog lookup — a genuine, generic transform (works for any real CRB Metamagic feat, confirmed against all 9), not a rename. A new end-to-end regression test drives the real `create_character_at_root` save path and was verified to actually catch the original bug (reverted, confirmed red, restored, confirmed green). Oracle's own full APG build (spontaneous known-spell casting mirroring Sorcerer's shape, Life Mystery/Healing Hands, Clouded Vision) landed as the first genuine full build in APG, staying honestly permanently `Blocked` (proven via a dedicated test). **Lead independently re-ran everything**: the translation function against all 9 real Metamagic feats, the regression test's actual body and assertions, all 15 Arcanist + 14 Oracle tests, both coverage audits, full lib 474/474, full desktop 215/215, plus a full workspace sweep run fresh (clean, zero failures). Docs reconciled (`1f31e873`). Frontend now clear to re-attempt `CLASS_OPTIONS`.

**Third comparative scoping pass corrects the "all 5 remaining classes are harder" assumption -- Swashbuckler is genuinely cheap, greenlit as the 9th closure.** Rather than trusting the prior label at face value, backend re-ran the same corpus-verification discipline and found Swashbuckler was mis-lumped in: it's a non-caster (zero `SPELLSTAT`, same cheap structural shape as Slayer) with three real features with clean precedent (Panache: flat CHA-mod pool; Charmed Life: activation-gated save bonus with a real per-day budget, the same two-spot enforcement shape as Rage/Judgment; Nimble: flat AC dodge bonus, grounds standalone since this engine computes no player AC total). Also found Investigator is a real positive surprise (a PREPARED caster like Wizard/Arcanist, not spontaneous like Oracle) but genuinely costlier: its `SPELLLIST:1|Alchemist` needs a brand-new spell-list ingestion effort (confirmed zero Alchemist/formula spell list exists anywhere in this codebase), and its class-skill list is a partial 2-of-3 match (Climb+Intimidate, no Swim) that doesn't fit either existing branch of the binary per-class widening fix — a real design decision, not a mechanical copy. **Lead independently verified every claim**: Swashbuckler's chassis/29-KEY-count/class-skill-list/all three formulas (Panache's DESC text, Charmed Life's and Nimble's exact `BONUS:VAR` formulas) all confirmed byte-exact; Investigator's chassis/class-skill gap/Trapfinding+Trap Sense formulas/zero-Alchemist-spell-list-exists claim all confirmed too. Greenlit Swashbuckler as scoped. On the one open item (Charmed Life's negative-operand formula at level 1) — this codebase has no generic PCGen formula engine to settle it definitively, so gave backend the lead's own reasoning (Rust's native integer division truncates toward zero, which should naturally produce the expected real value of 3 uses/day at level 1) rather than a false-confidence answer, and left the depth of further verification to backend's own judgment given the low stakes (one boundary value). Building now.

**Operator directive: fix the crontab so the PF1e dashboard JSON refreshes automatically -- done, then a second, deeper operator finding ("Monk's been in-progress all day, things never get flagged completed") led to a real bug found and fixed in the dashboard's own classification logic, outside this repo.** First fix: the crontab's `PF1e dashboard JSON producer` entry (operator directive 2026-07-25) had a comment but no actual schedule/command line following it -- the dashboard had never auto-refreshed since it was set up. Added `*/5 * * * *` running `pf1e_dashboard_producer.py`, matching the `SWARM_STATUS.md` heartbeat's own cadence; verified the script runs cleanly and the JSON's own `generated_at` timestamp is now genuinely live.

The operator then flagged Monk specifically as suspicious, and checked further: Monk's own "in-progress" state IS accurate (still 6 of 7 feats, unchanged today), but Arcanist/Oracle/Alchemist/Inquisitor were genuinely misclassified. Root cause, found in `~/.hermes/profiles/god-emporer/skills/release-swarm-observer/scripts/observer.py` (a hermes skill script, not part of this repo): the dashboard's per-class state classifier depends entirely on regex-scraping `SWARM_STATUS.md`'s own narrative prose for two exact phrase shapes -- a `"<N> of 27 classes now genuinely reach Computed**: <list>"` reach-list line, and ALL-CAPS `"**CLASS'S FEATURE COMMITTED**"`-style milestone headers -- with a completely silent failure mode when the lead's prose style drifts even slightly. Confirmed directly: the reach-list regex's last real match in the whole document was still the 10-class "...Bard" line from before Arcanist ever existed (the lead's own later rewrites never repeated that exact phrase), and Alchemist's/Inquisitor's/Oracle's own entries used ordinary Title Case prose ("Alchemist's Mutagen", "Inquisitor's Judgment") rather than the ALL-CAPS convention the milestone-pattern regex requires -- so all three silently fell through to "untouched" despite substantial, well-documented real progress.

**Real fix, not a prose-style patch**: added `parse_class_report_table` to `observer.py`, a new parser reading `SWARM_REPORT.md`'s own structured per-class tables directly (the canonical, rigorously-maintained table this whole segment's discipline already produces every cycle, with clean `**Computed**`/`**Blocked** -- real progress:`/`dispatch-only ... untouched` markers per row) -- far more robust than scraping narrative prose for an undocumented phrase contract. Wired it in as the authoritative primary source in `parse_class_chassis_table`, falling back to the old reach-list/milestone-pattern prose heuristics only for any class the report doesn't cover. Updated both `pf1e_dashboard_producer.py` (the JSON producer) and `observer.py`'s own `main()` (the older `dashboard.html`/`SWARM_LIVE.txt` renderer) to read and thread `SWARM_REPORT.md` through. Verified end-to-end: re-ran both scripts fresh, confirmed Monk stayed correctly "in-progress" (no change, was never actually broken), Arcanist correctly promoted to "full" (was stuck at "in-progress"), and Alchemist/Inquisitor/Oracle correctly promoted to "in-progress" (were incorrectly "untouched") -- Swashbuckler correctly still shows "untouched" since its build hadn't committed yet at check time. Both scripts run clean with no errors.

**MILESTONE -- SWASHBUCKLER'S FULL ACG BUILD COMMITTED (`d267fe79`) AND RE-VERIFIED AGAINST THE REAL HEAD -- 9th ACG/APG class-specific closure.** Built exactly as scoped: Panache/Charmed Life/Nimble grounded, fourth class-skill-bonus widening fix, RED-then-GREEN discipline maintained. Two honest evidentiary findings surfaced mid-build rather than glossed over: Panache's base-class corpus record carries no literal `BONUS:VAR` at all (DESC-text-sourced, cross-validated against the Inspired Blade archetype's own formula) -- a different, weaker evidentiary path than every other formula this segment has grounded, named as such; Charmed Life's real grant level (2nd, not 1st) confirmed via web search, resolving the lead's own flagged negative-operand-truncation question by making it moot. **Lead independently re-confirmed the grant-level finding directly in the corpus's own per-level feature-grant table** (`acg_classes.lst`'s `2	ABILITY:...Charmed Life` row) -- an even stronger, primary-source confirmation than the web search alone. Also independently verified Panache's DESC text and the archetype cross-validation, the `named_features_wired = 3` fix (backend caught its own copy-paste bug pasting the doc comment into Slayer's match arm before shipping), all 8 Swashbuckler tests, coverage audit 3/3 ("eight" closures), full lib 482/482, desktop 215/215 (unchanged, confirmed this closure doesn't touch the desktop crate), and a full workspace sweep run fresh (clean, zero failures). Updated `SWARM_REPORT.md` (Agent Status, ACG section header/table/tally, untouched-count 5->4) and `SWARM_TASKS.md`'s class-breadth row.

**Operator proposed 3 parallel backend subagents (one per book: CRB/APG/ACG) to speed things up, assuming per-book file separation avoids conflicts.** Flagged a real constraint before proceeding: all class-closure logic (dispatch, formulas, diagnostics -- everything built this segment) lives in one shared file, `pilot_compute.rs`, regardless of book; only the chassis tables are genuinely per-book-separated. Three backend agents building full closures concurrently would all edit that same file, the same git-index race class already hit once this segment with just one backend agent plus frontend. Presented the tradeoff and let the operator choose rather than deciding unilaterally. **Operator chose the safe option**: don't split backend at all; instead add a dedicated read-only "scout" teammate that scopes the next class candidate ahead of backend's need, so backend never waits on scoping. Spawned via the Agent tool (no dedicated "add persistent teammate" tool exists mid-session, but a named, resumable background agent addressable via `SendMessage` achieves the same practical effect) with an explicit, hard constraint: scout may only create new standalone `.md` scoping docs under `docs/release/v0.6/`, never touch any `.rs` file or the canonical status docs -- zero write-contention risk with backend, since scout's only output is a new file backend/the lead review before any code gets written. First assignment: scope Investigator's full ACG build (the most promising of the 4 remaining untouched classes per the third comparative pass -- a real prepared caster like Wizard/Arcanist/Warpriest, needing a new Alchemist-formula spell-list ingestion effort and a real design decision on its own partial class-skill match). Standing by for scout's scoping doc.

**Scout's Investigator scoping doc landed -- the scout/backend split works exactly as intended, and the doc caught a real, subtle hazard the lead independently confirmed.** Scout re-verified every corpus claim directly (chassis, 95-KEY count, class-skill list, all three formulas) rather than trusting the prior comparative pass, confirmed the chassis table already exists (`class_investigator.rs`), and found a real, genuinely new situation: Investigator's class-skill list is a 2-of-3 partial match (Climb+Intimidate yes, Swim no) -- the first on the whole roster, since every currently-supported class is cleanly all-three-or-none. Scout read the actual code (`selected_skill_class_skill_bonus_applies`/`compute_selected_skill_modifiers`) before proposing a fix, correctly identifying this as FORCED (not optional) by the gate widening itself, and correctly self-corrected the earlier comparative doc's own floated "standalone fact" option as not fitting the real code shape (Climb/Intimidate ARE tracked by the shared totals function, unlike Trapfinding/Track which touch untracked skills). Also flagged a real, subtle transcription hazard: Investigator's Trapfinding has a `max(...,1)` floor while its Trap Sense does not -- the exact opposite of Slayer's own two formulas, despite both classes' records sharing the same `TrapSenseBonus` variable name in the corpus.

**Lead independently re-verified everything before greenlighting**: every corpus token, the chassis-table-already-exists claim, and -- critically -- read `selected_skill_class_skill_bonus_applies`/`compute_selected_skill_modifiers`/`unmet_selected_skill_posture_conditions` directly to confirm scout's "forced, not optional" framing is accurate (a single scalar bool really is applied uniformly to all three skills, and the posture gate really does key on `has_supported_class_chassis`). Also independently read Slayer's own `slayer_trap_sense_bonus`/`slayer_trapfinding_bonus` to confirm the swapped-floors hazard is real, not overcautious. Greenlit both of scout's recommendations: (A) per-skill widening of the class-skill-bonus function (small, 2 call sites, behavior-preserving for all 6 existing classes, the only correct model) over the honest-block fallback; and a smaller no-spellcasting MVP first (chassis + Trapfinding + Trap Sense + Inspiration pool-size, `named_features_wired = 3`), deferring the Alchemist-formula-list ingestion (confirmed: zero such list exists anywhere, 104 real raw records exist as source material) to its own follow-on slice, mirroring Skald's own spellcasting split. Handed off to backend to build (10th ACG/APG closure) and to scout for the next scoping target.

**Scout's Shaman/Summoner/Witch re-scoping landed, correcting the standing "all three blocked on a subsystem" verdict for two of the three.** Directed not to trust the old verdict at face value -- applied the Investigator discipline and found: Shaman and Witch each have a real, Oracle-Mystery-shaped MVP that does NOT need their respective Familiar. Shaman's Life Spirit grants Healer's Touch (`BONUS:SKILL|Heal|4`) -- the exact structural twin of Oracle's own Life Mystery -> Healing Hands; Witch's Ward hex (`BONUS:VAR|WitchWardBonus|2`, scaling to +4) is a flat, self-scoped deflection/resistance bonus, the cleanest of Witch's 19 hexes since most others are opponent/ally-targeted. Summoner remains genuinely subsystem-blocked -- nearly every feature of value requires the unbuilt Eidolon, and the only eidolon-independent fact (Summon Monster's uses/day pool) is thinner than any prior closure. **Lead independently verified every corpus token and formula** (all confirmed byte-exact) **and caught one real factual error in the doc's own supporting reasoning**: the claim "no Animal Companion stat block exists either" is wrong -- `ground_wolf_companion_stat_block` is a real, working function already reused by both Druid's and Hunter's own closures. The correct, narrower reason Shaman's Spirit Animal still stays deferred is that it's a Familiar, a genuinely different PF1 subsystem from an Animal Companion (different progression table, different scaling), not that no companion code exists at all -- flagged to scout to fix in the doc; the conclusion and recommendation are unaffected. Greenlit both Witch (Ward hex) and Shaman (Healer's Touch) as the next two closures after Investigator; held Summoner as genuinely not worth a thin closure, with the deliberate Familiar/Eidolon subsystem investment question deferred as a separate, bigger call rather than decided reflexively.

**Scout's background Eidolon-subsystem sizing research landed, self-corrected a real numeric error, and then found a real shipped bug during a systematic audit of already-landed closures.** Directed to size a bounded Eidolon MVP as background research (not an active decision) ahead of the Summoner question. Found the Eidolon is not strictly all-or-nothing: a bounded single-base-form MVP (fixed `AUTOMATIC` free evolutions, evolution-pool-size as a flat fact, chosen evolutions deferred) is real and comparably sized to a landed closure (Warpriest-ish), while the full evolution point-buy economy is a separate, bigger subsystem cost. Scout's first pass claimed 353 real `KEY:Evolution` records; **lead independently re-verified the count and found it wrong** -- the real count is 104, confirmed multiple ways; scout's own broader grep pattern had swept in 102 parallel `Temp Evolution` records plus ~200 category-scaffolding lines. Scout re-verified and corrected the doc throughout, softening the "comparable to a from-scratch spellcasting system" framing accordingly -- the core finding (bounded MVP is real, full economy is a separate bigger cost) survives the correction. Committed (`d9a41faa`). No build decision made -- background reference material only.

Directed next to systematically re-derive every already-landed closure's formulas from its own real corpus record rather than trusting docs or sibling classes, given the swapped-floors and Familiar-mislabel catches already this segment. **Found a real, active, shipped bug on the first pass**: Warpriest's `warpriest_sacred_weapon_base_dice` uses `level/20` for its dice-count denominator, but the corpus has three PREBASESIZE-gated branches (LT:M uses `/20`, EQ:M -- the Medium/Longsword branch this codebase's own fixture uses -- uses `/15`, GT:M uses a third formula) -- the code is using the wrong sibling branch's divisor. **Lead independently confirmed all three corpus branches and the code** before escalating: real impact, a level 15-19 Warpriest gets dice_count 1 instead of the correct 2 in this explanation. Severity low-moderate (flat informational record, not integrated into any total, only the 15-19 band diverges), but real and worth fixing -- the function's own doc comment cites the wrong number too, so it's self-consistent and wouldn't be caught by a doc-vs-code check alone, only by re-deriving from the raw corpus. Escalated to backend to fix whenever convenient; scout continuing the audit across the remaining closures.

**Operator feedback (2026-07-26): the cadence of starting new classes before finishing prior ones needed to slow down, and blockers needed to be visible as open questions.** Paused new class scoping/building (Summoner logged as an explicit open question, `risks-and-open-questions.md` item 38, rather than left implicit) and redirected backend toward deepening an already-in-progress class rather than starting a new one.

**Operator then asked about the book matrix showing all 4 books' 5 columns (races/classes/spells/equipment/feats) stuck "in-progress," and whether races/spells/equipment/feats could be parallelized.** Investigation found a second, distinct dashboard bug (separate from the earlier per-class classification fix): every in-scope book's entire 5-column row in `pf1e_dashboard_producer.py` was hardcoded to `"in-progress"` unconditionally -- none of the five columns could ever show "done" regardless of real progress underneath. Cross-checked the real state directly against `docs/architecture/rules-engine.md`'s per-domain engine catalog and `status.md`: races (all 7 core races have real trait-recognition, and race support isn't even book-scoped in this engine), spell schools (9/9 landed), and equipment effects (4/4 categories landed) are all substantially/fully complete at the engine layer -- but feat *effects* are only wired for 4 of the 185 CRB feats (Toughness, Great Fortitude, Iron Will, Lightning Reflexes), despite feat *prerequisites* being complete for all 185. **Fixed the dashboard properly** (not left as a placeholder): `classes`/`races` now aggregate from the same real per-class/per-race state the class-level matrix already computes; `spells`/`equipment` are hand-set to `full` with a citation to the two architecture docs (mirroring `support_state_matrix.rs`'s own citation-backed seed-row discipline); `feats` stays honestly `in-progress`, reflecting the real, substantial effects gap. Verified both dashboard scripts (`pf1e_dashboard_producer.py`/`observer.py`) still run clean after the fix.

**Dispatched a new teammate ("featmate") to widen feat-effects coverage**, per the operator's explicit go-ahead -- scoped to `feat_effects.rs`/`feat_prereqs.rs` only, explicitly forbidden from touching `pilot_compute.rs` or `rules_tables/{acg,apg}/`, so it runs with zero write-contention against backend's own class-dispatch lane. Directed to read the existing 4-feat pattern, the real 185-feat catalog, and produce a scoping doc (mirroring the house style) before writing any code, the same review gate every backend closure has gone through.

**MILESTONE -- FEAT-EFFECTS WIDENING LANDED (`e08f971f`) AND RE-VERIFIED AGAINST THE REAL HEAD -- first cross-lane featmate/backend closure.** Featmate's scoping doc (`feat-effects-widening-scoping.md`) found the real constraint before any code: a feat effect only matters if some already-computed value consumes it, and the only unexhausted live consumer left on the roster is `SelectedSkillModifiers` (Climb/Intimidate/Swim) -- HP (Toughness) and base saves (Great Fortitude/Iron Will/Lightning Reflexes) were already fully exhausted. That consumer lives in `compute_selected_skill_modifiers`, backend's exclusive `pilot_compute.rs` lane, so featmate correctly flagged the cross-lane wiring as an explicit open question rather than touching it. Resolved by directing backend to own the ~3-line consumer edit while featmate owned the pure `feat_effects.rs` function -- the same lane split `save_bonuses_from_feats`/`compute_total_saves` already established.

Featmate grounded three feats with real, provable, non-fabricated values: Athletic (+2 Climb, +2 Swim -- the corpus's `if(TOTALRANK>=10,4,2)` conditional is provably always `2` since the deterministic posture never reaches 10 ranks), Persuasive (+2 Intimidate only; its real +2 Diplomacy half has no live consumer and stays honestly ungrounded), and Intimidating Prowess (adds the real Strength modifier, not a flat constant, to Intimidate -- a genuine near-miss the scoping doc flagged as a possible fast-follow and the lead greenlit into this same batch). All three stack independently per real PF1 rules (different bonus sources). Backend then wired `skill_bonuses_from_feats` into `compute_selected_skill_modifiers`'s three totals and extended each explanation string to name the real feat source, plus a dedicated end-to-end consumer test module (`skill_boosting_feats_widen_selected_skill_modifiers_tests`) proving the real Fighter fixture gets +0 baseline and each feat lands on exactly its correct skill(s) with no cross-skill leakage, plus full three-feat stacking.

**Lead independently verified all of it, twice** (once pre-commit on the shared uncommitted tree, once post-commit in isolation against the real `e08f971f` HEAD): read the full `skill_bonuses_from_feats` implementation and its 9-test module (absent/empty/each-feat/two-way-stack/three-way-stack/negative-STR-edge-case/substring-prefix-false-match safety all present and correct), read the full consumer-wiring diff in `pilot_compute.rs` line-by-line (each `feat_skill_bonuses` field added to exactly the right total, each explanation `detail` string correctly extended), and read the full 4-test consumer module. Ran `cargo test --lib` (519/519, both before and after commit), both `sd24_acg_class_coverage_audit`/`sd24_apg_class_coverage_audit` (3/3 each, unaffected as expected -- this closure touches no class chassis). No regressions, nothing fabricated: every deferred half-effect (Persuasive's Diplomacy half, the other 8 two-skill General feats, Skill/Weapon/Spell Focus's chosen-target problem, Dodge, Initiative, CMB/CMD, equipment-gated AC feats, all Item Creation/Metamagic feats) is named honestly in the scoping doc rather than silently dropped. `named CRB feats with real mechanical effects wired: 4 -> 7 of 185`.

Featmate is holding, not scoping new work, per the operator's "complete before starting new" directive -- confirmed idle and standing by. Two candidate next targets are already named in the scoping doc's deferred list (the other 8 two-skill General feats, gated on more skills joining the computed surface; a chosen-target mechanism for Skill/Weapon/Spell Focus, a real design conversation not a quick widening) but neither is greenlit yet.

**Operator asked about Monk's real status, then flagged a third, distinct dashboard gap**: the per-class matrix in `PF1e-dashboard.json` had no field at all for a blocked class's actual blocker reason -- unlike the two earlier dashboard bugs (per-class classification, book-matrix hardcoding), this wasn't a wrong value, it was a genuinely missing field, so nothing could ever be populated there regardless of how much real blocker detail this swarm's own docs already carried. Fixed in `observer.py`/`pf1e_dashboard_producer.py` (outside this repo, not git-tracked): added `extract_open_question`, which derives a concise blocker clause directly from each class's own `SWARM_REPORT.md` table cell (preferring an explicit "stays Blocked on ..."/"Only `<feat>` remains ..." clause over the surrounding build narrative, stopping at the next literal `|` cell boundary rather than requiring a sentence-ending period since several real cells use an em-dash instead), and threads it through `parse_class_report_table`/`parse_class_chassis_table` into each `matrix.classes[]` entry's new `open_question` field. Summoner's entry additionally cites `risks-and-open-questions.md` item 38 by number via a small `KNOWN_RISK_ITEM_REFS` map. Also added a parallel `open_questions.feats` note on each in-scope book row, since the book-matrix's `feats` column had the same gap at the aggregate level (citing the real 7/185 feat-effects count, corrected from the stale "4/185" the code comment still had). Verified end-to-end: all 16 non-full classes (11 full, 16 in-progress/untouched across the 27-class roster) now carry a real, non-empty `open_question` -- spot-checked Monk ("Only Deflect Arrows remains -- the one feat with zero standalone numeric value, genuinely needs a full opponent-interaction/incoming-attack engine this codebase has no framework for at all") and Summoner ("dispatch-only, Blocked -- queued, untouched (see risks-and-open-questions.md item 38)"). Both `observer.py` and `pf1e_dashboard_producer.py` re-ran clean with no errors; the next 5-minute cron tick picks up the fix automatically.

**Operator reported still seeing nothing on the live dashboard -- the JSON fix alone wasn't the whole gap.** `PF1e-dashboard.html` (nginx-served at `10.0.0.134`/Tailscale, distinct from the older `dashboard.html` the observer skill also renders) is a thin client-side viewer that fetches `PF1e-dashboard.json` and renders it via its own `renderClassMatrix` JS function -- which had zero reference to `open_question` at all, so the new field had nowhere to display regardless of the JSON being correct. Fixed: added an "Open question" column to the class-matrix table header and row template, plus a CSS rule so the (up to ~280-char) text wraps sensibly instead of blowing out the table width. Verified against the actual live nginx-served page (not just the JSON file): Monk's real blocker text renders correctly end-to-end.

**Operator then directed: add TODO tasks to resolve all of our blockers, prioritize the blocked work.** Created 17 tasks via the shared task list (TaskList/TaskGet) -- one per non-full class (Bloodrager, Hunter, Inquisitor, Alchemist, Brawler, Cavalier, Skald, Investigator, Warpriest, Oracle, Witch, Shaman, Slayer, Swashbuckler, Monk), one for feat-effects' next slice, and Summoner explicitly tagged "DECISION NEEDED" and metadata-flagged blocked pending an operator choice between the bounded Eidolon MVP and the full evolution economy -- not assigned, not sized, per the standing instruction not to decide that one unilaterally. **Caught and corrected a real ordering mistake before dispatching**: an initial priority guess ranked Bloodrager/Alchemist/Cavalier near the top as "cheapest," but `SWARM_TASKS.md`'s own prior record already shows backend surveyed exactly these three and found them NOT cheap (Bloodrager 201 spell records, Alchemist 121 spell records + a different formula-based casting mechanic, Cavalier a 26-entry Order chooser bigger than any prior narrowing precedent) -- this is why Brawler was deepened instead of one of these three last cycle. Re-tagged all three low-priority with the real citation rather than silently leaving a misleading ranking in place. Tagged Oracle/Witch/Shaman/Slayer/Swashbuckler/Monk/Warpriest low-priority too, each for a real cited reason (proven permanently Blocked with a large remaining lift, or needing a brand-new architecture pillar/mechanism this codebase doesn't have at all). Left 6 tasks (Hunter, Inquisitor, Brawler, Skald, Investigator, feat-effects next slice) tagged "needs-scoping" rather than guessed -- dispatched scout to run a real comparative sizing pass across exactly these 6, same discipline as the Investigator/Shaman-Summoner-Witch re-scoping passes, specifically to check whether Inquisitor's 7 remaining judgments can cheaply reuse Justice's own already-built mechanism. Backend and featmate directed to hold for scout's sizing doc before picking anything up, so the swarm resumes on the genuinely cheapest real target rather than another guess.

**Scout's `blocker-backlog-comparative-sizing.md` landed -- Inquisitor confirmed the clear winner, greenlit and dispatched (11th ACG/APG closure in progress).** The judgment-reuse angle checked out: all 9 judgments (Justice + Destruction/Healing/Piercing/Protection/Purity/Resiliency/Resistance/Smiting) share the identical `BONUS:VAR|InqJudge<Type>LVL|InquisitorLVL` anchor, so the 8 non-Justice judgments reuse Justice's already-built activation-gated, uses-per-day-budget-enforced mechanism directly -- plus four genuinely-flat standalone features (Monster Lore `+WIS`, Stern Gaze `max(1,lvl/2)`, Cunning Initiative `+WIS init`, Track `+lvl/2`). Only fresh WIS-spontaneous spellcasting (no `SPELLLIST` reuse token) stays deferred. Ranked the rest: Skald (2-3 flat wins, cheap and small) 2nd, Hunter (1 flat win + spellcasting reusing already-built Druid+Ranger lists) 3rd, Investigator (bounded but Warpriest-sized, high-leverage since it also unblocks Alchemist's own spellcasting) 4th; deprioritized Brawler (cheap features already spent, remainder needs chooser/CMB machinery); held feat-effects (both named targets are dependency- or design-gated, not a bounded grounding).

**Lead independently re-verified the Inquisitor claims directly against the real PCGen corpus before greenlighting** (not this repo's own `data/corpus/` -- the raw PCGen `.lst` source lives in the separate `~/workspace/repos/pcgen` checkout): grepped `apg_abilities_class.lst` and confirmed all 9 `InqJudge*LVL|InquisitorLVL` anchors exact, all 4 flat-feature formulas byte-exact, and confirmed Justice's own mechanism (`active_inquisitor_justice_judgment_bonus`/`inquisitor_justice_judgment_attack_bonus`/`ground_or_block_inquisitor_judgment`, `pilot_compute.rs:8799-8948`) is real, already-built, and tested. Dispatched to backend with scout's own flagged caveat held explicit: each judgment's exact bonus *value* formula must be re-derived from its own real record at build time, not copied from Justice's `/5`. Sequencing confirmed for after Inquisitor closes: Skald, then Hunter, then Investigator. Scout holding, ready for a fresh Skald-scoping pass once Inquisitor is close to landing.

**MILESTONE -- INQUISITOR'S JUDGMENT WIDENED TO 4 OF 8 TYPES + STERN GAZE (`14126998`), 11TH ACG/APG CLASS-SPECIFIC CLOSURE, LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD -- and a real, self-caught correction to scout's own sizing pass along the way.** Before building the full 7-judgment/4-feature scope scout sized, backend applied this codebase's own established wiring-consumer discipline (the same one that already excluded Cunning Initiative from the original single-Justice closure) and found scout's sizing pass had confirmed formula existence but not consumer existence -- a real gap. Of the 4 "flat" features, only Stern Gaze (`max(1,level/2)` on Intimidate) has a live consumer (`compute_selected_skill_modifiers`'s existing Intimidate total); Monster Lore (Knowledge), Cunning Initiative (Initiative), and Track (Survival) all target skill/combat totals that don't exist anywhere in this engine, so they stay honestly deferred rather than built as dead grounding. Of the 8 non-Justice judgments, only Protection (`1+level/5` sacred/profane bonus to Armor Class, lands on `baseline_armor_class`) and Purity (`1+level/5` sacred/profane bonus on all saves, lands on `total_saves`) reuse Justice's mechanism onto a real computed total; Smiting (DR-bypass, no scaling) grounds as a standalone explanation-only fact mirroring Brawler's own Strike shape; the other 5 (Destruction/Healing/Piercing/Resiliency/Resistance) genuinely need engine state that doesn't exist (a damage-roll total, round-tick HP state, a concentration/CL-check total, a DR-received facet, chosen-energy-type + resistance state respectively) and stay deferred. **Lead independently re-verified every piece of this correction directly against the real PCGen corpus** before accepting the narrower scope: re-grepped `apg_abilities_class.lst` and confirmed Protection's and Purity's real DESC text and `1+InqJudge<Type>LVL/5` formulas byte-exact (re-derived independently, not copied from Justice), and confirmed via direct grep of `pilot_compute.rs` that no `compute_initiative`/`compute_survival`/Knowledge-total function exists anywhere in the crate -- the dead-consumer claim is real, not assumed. `named_features_wired` rises 1->2 for Inquisitor (Judgment slot + Stern Gaze slot). Documented thoroughly in `risks-and-open-questions.md` item 41. Lead re-ran everything against the real committed HEAD: full lib 524/524, `sd24_apg_class_coverage_audit` 3/3, all 12 Inquisitor-specific tests individually, full workspace sweep (477 result-blocks, 0 failures). Task #3 marked completed in the shared task list. Redirected backend to hold on Skald (#7) until scout's revised scoping pass (re-run with this same consumer-existence check applied explicitly, since Bardic Knowledge's own "all Knowledge skills" target may hit the identical dead-consumer wall Monster Lore just did) lands -- not built against the original, now-superseded sizing doc.

**Real cross-closure methodology inconsistency caught by scout while re-scoping Skald, corrected by the lead -- the "wiring-consumer constraint" applied to Inquisitor's Monster Lore/Cunning Initiative/Track was stricter than this codebase's own established, dominant, already-shipped convention.** Scout's revised Skald pass (`skald-remaining-features-scoping.md`) found that Bard's own Bardic Knowledge (`class_chassis.bard.bardic_knowledge`, `pilot_compute.rs:24728`) and Slayer's own Track (`class_feature.acg.slayer.track_bonus`, `:10569-10589`) are BOTH already-shipped standalone explanation records grounding a flat, verified magnitude with **no live consumer required at all** -- Bard's own doc comment: "it is not a full Knowledge-check resolution engine"; Slayer's own: "Survival is not among the three tracked skills either, so this grounds as a standalone flat record." Scout counted 31+ instances of this exact idiom across the codebase (Bard, Slayer, Barbarian, Rogue, Fighter, Swashbuckler). That means Inquisitor's Monster Lore/Cunning Initiative/Track deferral -- and the "needs a live consumer" framing the lead endorsed for it -- applied a stricter bar than the codebase's own dominant, shipped practice, not the actual established rule. **Lead independently verified both cited examples directly** before accepting the correction: read Bard's Bardic Knowledge record and Slayer's Track record in full, confirmed both are genuinely standalone (no total-integration, explicitly labeled as such) and both are real, non-fabricated, RAW-verified magnitudes -- satisfying the no-stub-mvp-doctrine's actual bar (a real value, honestly labeled as not-integrated) regardless of consumer existence. **Corrected canonical bar, decided by the lead as a methodology call** (not deferred to the operator -- this is an internal consistency question about already-established, already-shipped practice, the same category of call as the per-skill class-skill-bonus widening decision or the Investigator gate-widening choice): a feature grounds as a standalone record whenever its magnitude is genuine and verifiable; integration into an existing computed total is a bonus when available, never a requirement. Opened **task #18** (a bounded follow-on, not reopening the already-verified `14126998` commit) to ground Monster Lore/Cunning Initiative/Track as standalone facts the same way, and restored Skald's own task #7 scope to include Bardic Knowledge (matching Bard's own precedent exactly) alongside Well-Versed and Lore Master's pool-size. Flagged to featmate that this correction likely also reopens feat-effects' own "8 more two-skill General feats" deferral (task #16, currently on hold) -- their target skills not being computed doesn't actually block standalone grounding either, worth a fresh look whenever that task's turn comes. Backend directed to land task #18 first (same class, small and bounded), then Skald (#7) with the restored scope.

**Both teammates picked up parallel-lane work immediately, no idling**: featmate formally greenlit on task #16 (the 8 General feats, corrected bar) and scout dispatched to scope Hunter (#2) ahead of backend's need. Scout's Hunter pass caught its own correction: Animal Focus was originally deferred as "a chooser," but under the corrected bar each focus narrows to one canonical pick (Bull, `+2/+4/+6 STR` at levels 1/8/15) and grounds cleanly, the same Oracle-Mystery-style narrowing already used elsewhere -- **lead independently confirmed the exact formula and level gates directly against the corpus** before accepting it. Nature Training correctly stays deferred (verified zero `BONUS` token exists at all, a genuine no-op like Alchemist's Martial Training, not a missed win). Task #2's ownership was briefly misassigned to scout (a lead error) -- scout correctly flagged it back rather than attempting a build outside its read-only lane, reassigned to backend.

**MILESTONE -- INQUISITOR'S MONSTER LORE/CUNNING INITIATIVE/TRACK (TASK #18) + SKALD'S DAMAGE REDUCTION/BARDIC KNOWLEDGE (TASK #7) LANDED TOGETHER (`6c8ca561`), 12TH AND 13TH REAL DEEPENINGS, LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD.** Backend independently re-confirmed the lead's own DR-precedent argument before building it (Barbarian's `class_feature.barbarian.damage_reduction` genuinely is the same standalone shape, not shared plumbing) and built both corrections cleanly: Inquisitor's three facts (`named_features_wired` 2->5) and Skald's DR (`0/1/2/3` at levels 9/14/19, self-only, ally-extension via Raging Song correctly deferred) plus Bardic Knowledge (byte-identical to Bard's own formula), `named_features_wired` 1->3. Also incidentally fixed a real doc-comment misattachment (caught on backend's own re-read, not a logic bug) and restored Skald's remaining-feature naming from stale placeholder text to real corpus names. **Lead independently re-verified against the real committed HEAD**: full lib 540/540 (includes featmate's still-uncommitted, unconsumed `feat_effects.rs` leaf), both SD-24 coverage audits 3/3, all 13 Inquisitor-specific and 18 Skald-specific tests individually, Bardic Knowledge's formula confirmed byte-identical to Bard's own. Tasks #18 and #7 marked completed.

**Real git incident during this verification, caught and recovered cleanly**: while independently verifying featmate's separately-reported task #16 build, `feat_effects.rs` was found missing from the working tree entirely -- swept into an unexplained `git stash` entry by some process (confirmed NOT the auto-heartbeat cron, which explicitly skips on a dirty tree; source otherwise undetermined, consistent with the known "single shared checkout, multiple concurrent agents" hazard this swarm has flagged before). **Recovered without any data loss**: found the stash contained exactly featmate's isolated 176-line diff (nothing else), applied it, resolved an unrelated stale `Cargo.toml`/`Cargo.lock` conflict that rode along in the same stash (kept the current `0.6.0` version, discarded a stale `0.5.98` remnant), reverified featmate's own 29/29 `feat_effects` tests and 540/540 full lib, then dropped the now-redundant stash. Flagged the incident to featmate directly (their file had briefly vanished from `git status`) so they know to check `git stash list` before assuming lost work in the future. No process changes made pre-emptively -- noted as an open risk of the shared-checkout swarm architecture, not something to "fix" reactively without understanding the actual cause.

**Featmate's task #16 build reviewed and independently verified**: 8 previously-deferred two-skill General feats (Acrobatic, Alertness, Animal Affinity, Deceitful, Deft Hands, Magical Aptitude, Self-Sufficient, Stealthy) plus Persuasive's previously-deferred Diplomacy half, all grounded as `StandaloneSkillFeatFact` records via a new `standalone_skill_facts_from_feats` function -- deliberately kept separate from `skill_bonuses_from_feats` (different semantics: standalone facts on uncomputed skills vs. deltas folded into computed totals). **Lead independently confirmed all 8 formulas byte-exact** against this repo's own `feat_data/general.rs` corpus, confirmed Persuasive's Diplomacy/Intimidate split has no double-counting, and confirmed Athletic correctly yields nothing (both its skills are already computed). Turnkey consumer-wiring snippet handed off to backend for whenever they pick this up; featmate directed to commit their own verified work now.

**Scout's fresh Investigator (#8) pass under the corrected bar found two more real missed wins and one genuine gray zone, both resolved by the lead.** Poison Resistance (`+2` per tier vs poison, tiered by level, shared leverage with Alchemist's own deferred feature) and Alchemy (flat `+level` Craft-alchemy bonus, same shape as Bard's Bardic Knowledge) both confirmed byte-exact against the real corpus and folded into task #8's scope as a cheap pre-slice ahead of the larger spellcasting subsystem. **Genuine gray zone, ruled by the lead**: Investigator's Studied Combat/Studied Strike (and, consistently, Slayer's own Studied Target, task #13) are opponent-dependent -- their bonus only applies against a "studied" enemy, a targeting concept this engine models nowhere. Ruling: these stay deferred rather than grounded as "conditional" standalone facts, since an always-on self-buff (Poison Resistance, Nimble, Track) genuinely satisfies the corrected bar, but a bonus conditioned on an interaction the engine can't evaluate would overstate what's actually verified -- a real, principled line, not an arbitrary one. Applied consistently to both task #8 and task #13's descriptions so it won't be re-litigated per-class. Task #18's shared-list sequencing question also resolved: build the Alchemist formula list under #8, not a separate task.

**Featmate's Focus-feat chosen-target design doc landed, ratified with one real correction to its own Path A recommendation.** `focus-feat-chosen-target-design-scoping.md` reframed the "biggest locked door" in feat-effects: the standalone-grounding correction removes the *consumer* half of the blocker (a Focus bonus now grounds fine regardless of whether its target dimension is computed), leaving only the *recording* problem -- how to capture which skill/weapon/school the player chose. Two mechanisms proposed: (A) a compound key in `selected_feats` (like the existing hardcoded `feat:weapon_focus:weapon:longsword`), or (B) a parallel `SelectedChoice` entry reusing the already-shipped `choice:X -> Y` mechanism. **Lead independently verified the risk before ratifying**: grepped `feat_prereqs/metamagic.rs:118` (`entry.key == feat_id || entry.name == feat_id`) and every other `selected_feats` consumer in the codebase, confirming all use exact-string equality -- a compound key would silently break any real prerequisite lookup requiring plain "Skill Focus." **Mechanism B ratified, Mechanism A rejected** for that confirmed reason. **On Path A vs B, the lead pushed back on featmate's own honesty flag rather than defaulting to precedent**: every prior Path-A closure (Wizard's school, Sorcerer's bloodline, Arcanist's Metamagic Knowledge) silently seeds a canonical value for a choice where the class works the same regardless of which option -- a modest simplification. Skill Focus is categorically different: its entire value IS which skill was chosen, so silently seeding "targets Stealth" for a character who actually chose Perception would assert a specific, checkable, wrong number -- a real fabrication, a sharper violation of the no-stub-mvp-doctrine's honesty bar than any prior Path-A choice. **Decision: neither Path A nor Path B for this first slice** -- ground the fact only when an explicit `choice:skill_focus_target -> skill:X` is actually present, never fabricate one, treating product-UI-reachability (whichever path) as a wholly separate later question, the same sequencing already used for every class chassis (engine-Computed first, UI-reachability later). Featmate building the `feat_effects.rs` producer now under this framing; cleared to push `21de852c` (was holding it as a shared-state courtesy while backend had uncommitted work).

**MILESTONE -- HUNTER'S WILD EMPATHY + ANIMAL FOCUS LANDED (`f1c46e19`), 14TH REAL DEEPENING, LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD.** Backend built directly from scout's scoping doc with no re-derivation needed -- Wild Empathy (`CHA+level`, flat unconditional) and Animal Focus (Bull picked as the canonical option, `+2/+4/+6` STR at levels 1/8/15, activation-gated with a genuinely enforced per-day minutes budget, mirroring Judgment's own three-branch shape). Nature Training re-confirmed correctly deferred (zero numeric token). Spellcasting deliberately deferred to its own follow-on rather than bundled, per the Skald-split precedent. **Real, pre-existing scope boundary found and correctly worked around, not fixed as a bug**: a full-pipeline test at levels 5-20 panicked on `wolf_companion_hit_dice`'s own `debug_assert_eq!` ("only companion level 1 is grounded this slice") -- a boundary that predates this closure (shared with Druid's own Animal Companion math). Backend kept new tests at level 1, matching every other Hunter test, rather than expanding scope to fix an unrelated pre-existing limitation. **Lead independently confirmed this is a genuine, intentional, already-documented boundary** (not a bug needing escalation) before accepting the workaround, and independently verified Wild Empathy's formula byte-exact against the real corpus DESC text ("Hunter level + Charisma modifier"). `named_features_wired` 1→3. Isolated backend's own files via stash-and-restore while featmate's `feat_effects.rs` was mid-write (a real, transient, mutually-observed Cargo.toml/lock conflict during that window on the shared checkout, already reconciled -- both agents' own concurrent isolation-testing crossed paths, nothing lost). Lead re-ran everything against the real committed HEAD: full lib 556/556, `sd24_acg_class_coverage_audit` 3/3, all 9 Hunter-specific tests individually. Task #2 marked completed.

**Featmate's Skill Focus producer landed (`a903be53`), lead-verified against the real committed HEAD -- and the shared-checkout "gremlin" got a real root-cause diagnosis instead of another shrug.** `skill_focus_facts_from_choices` grounds the real `+3` (`["SKILL","%LIST","3","TYPE=SkillFocus"]`, confirmed byte-exact) per explicitly-chosen skill target, exactly the ratified no-silent-seeding design: requires both "Skill Focus" in `selected_feats` AND a matching `choice:skill_focus_target -> skill:<name>` in `selected_choices`, grounds nothing (no fabricated canonical skill) when either is missing. Lead re-ran `cargo test --lib feat_effects` (38/38) and the full suite (556/556), matching featmate's own report exactly. **The stash incident recurred mid-build** (featmate's in-progress test module briefly swept away a second time) -- this time actually investigated properly rather than accepted as coincidence: re-checked every cron job on the box (dashboard-watchdog.sh, dashboard-renderer.sh, the heartbeat script, the PF1e producer) and confirmed none touch git at all, and no git hooks are active on this repo. **Real, mechanistic root cause identified**: backend's own "stash-and-restore" isolation technique almost certainly uses a blanket `git stash` (no path filter), which captures the *entire* working tree including featmate's concurrently-edited `feat_effects.rs` -- if featmate keeps editing between backend's stash and restore, backend's own "restore exactly as found" genuinely restores the stale snapshot it captured, which is indistinguishable from a revert to the person whose file vanished. Not a mystery process -- a fixable technique gap. **Fix directed to backend**: scope isolation stashes to only the files under test (`git stash push -- <specific paths>`), never a blanket `git stash`, so a concurrently-edited file is never touched at all regardless of timing. Confirmed the redundant stash entry matched already-committed content byte-for-byte before dropping it. Task #16's Skill Focus slice complete on featmate's side; consumer wiring into `pilot_compute.rs` (for both Skill Focus and the earlier 8-General-feats slice) handed to backend as a lower-priority side-task alongside Investigator (#8), now dispatched and in progress.

**Backend's own isolation-technique fix landed the moment it was flagged, and Investigator's pre-slice (Poison Resistance + Alchemy) closed with a real, independently-verified correction to scout's own scoping doc.** Backend acknowledged the root cause immediately and proposed a better fix than the lead's own suggestion: stop stashing featmate's file in either direction at all, and reach for a `git worktree` instead of a stash for any future genuine isolation need, since a worktree never touches the shared tree. **Poison Resistance's real corpus derivation (`fb3c08b0`)**: backend re-checked `InvestigatorPoisonLVL`'s own gating directly rather than trusting the scoping doc's summary, and found the doc's claimed "+2/+4/+6/+8 scaling, immunity at 20th" was wrong on the specifics -- the real gates are `PREVARGTEQ:InvestigatorLVL,2/5/8/10`, giving None below level 2, `+2` (2-4), `+4` (5-7), `+6` (8-9), full immunity at 10+ (no `+8` tier exists at all, immunity is level 10 not 20). **Lead independently re-derived the same tiers directly against the raw corpus** before accepting the correction, confirmed byte-exact. Alchemy's flat `+level` Craft-alchemy bonus also confirmed correct (same shape as Bard's Bardic Knowledge). Scout's own doc corrected in the same cycle, framed honestly as a secondary-source slip rather than glossed over. `named_features_wired` 3→5 for Investigator. Lead re-ran everything against the real committed HEAD: full lib 557/557, `sd24_acg_class_coverage_audit` 3/3, all 7 Investigator-specific tests. Task #8 remains in progress (not completed) -- the pre-slice is done, the larger shared Alchemist-formula-list spellcasting subsystem (~104 real records) is next, backend scoping it now before writing code.

**Backend's Alchemist-formula spell-list scoping (`216de5b6`) landed with one real precedent-deviation flagged explicitly, decided by the lead rather than defaulted either way.** 104 spell records (13 new + 91 `.MOD` grafts onto CRB spells), Investigator's own prepared-caster shape (mirrors Arcanist's -- no opposed-school cost), and a 3-independent-source-verified Extracts Prepared table for all 20 levels all scoped. **The real question**: ground the full 1-20 table (backend's own recommendation, since Investigator's chassis already supports all 20 levels and the data is fully verified), or stay bounded to level 3 to match Wizard's/Arcanist's own convention? **Lead independently verified every load-bearing claim before deciding**: re-derived the 104-count and 14/25/20/16/14/15 level breakdown directly against `apg_spells.lst` (byte-exact), confirmed via Wizard's/Arcanist's own doc comments that their level-3 bound is genuinely "the same incremental-widening idiom every other per-level table uses," not a verification ceiling, and confirmed `MAX_SUPPORTED_BARBARIAN_LEVEL=20` and `MAX_SUPPORTED_MONK_LEVEL=12` already exist elsewhere in this same codebase -- settling that "stop at 3" was never an architectural rule, just a per-mechanism default that happened to never get a follow-on widening pass for Wizard/Arcanist specifically. **Decision: ground the full 1-20 table.** Investigator stays permanently Blocked overall on `other_features_deferred` regardless of this widening, so there's no risk of exposing an under-verified level range through the real product -- this only changes how far the spellcasting sub-diagnostic itself stops claim-blocking, not overall reachability. Artificially capping real, sourced, verified data would cut the "don't fabricate" doctrine the wrong way. Building now.

**MILESTONE -- INVESTIGATOR'S SPELLCASTING SUBSYSTEM LANDED (`80efdb44`), TASK #8 FULLY CLOSED, LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD.** The shared `alchemist_spell_list` module (104 real records, 0 duplicates, matching the scoped level breakdown exactly), Investigator's own prepared-extract validation (mirroring `ground_arcanist_prepared_spellbook` line-for-line -- no arcane-school opposed-cost mechanic to model), the full 1-20 Extracts Prepared table (matching every value in the scoped 3-source-verified table exactly, spot-checked directly in the committed code), and extract save DC (`10 + extract level + INT`) all landed together. **Lead independently re-verified every piece against the real committed HEAD** rather than trusting the pre-commit check alone: re-ran the spell-list entry count (104, matching), read `investigator_base_extracts_per_day` in full and confirmed every row matches the scoped table verbatim, confirmed the extract-DC formula against the corpus's own Alchemy DESC text. Full lib 569/569, `sd24_acg_class_coverage_audit` 3/3, all 13 Investigator-specific tests individually. `named_features_wired` rises to 6 (adds the spellcasting slot). Task #8 marked completed -- Investigator is now a genuine full build (chassis + 5 named features + real prepared spellcasting), stays Blocked only on `other_features_deferred` (Studied Combat/Strike, Investigator Talents). **Real side-benefit, flagged in task #4's own description**: the shared spell-list module is now sitting built and ready for Alchemist's own deferred spellcasting -- removes the ingestion cost entirely, though Alchemist's own formula-based validation shape (a different mechanic from Investigator's prepared shape) still needs its own wiring, not automatic. Backend holding for the next assignment.

**Scout's fresh Alchemist (#4) pass flips the class from "not cheap" to genuinely tractable -- and corrects the task's own note that its casting mechanic differs from Investigator's.** The original "not cheap" verdict was entirely an artifact of the missing spell list, now built. **Lead independently verified every load-bearing claim before greenlighting**: confirmed `SPELLSTAT:INT MEMORIZE:YES SPELLBOOK:YES` in `apg_classes.lst` is byte-identical to Investigator's own casting shape (the task's prior "different mechanic" note was wrong); confirmed all three of Bomb's formulas exact (`AlchemistBombAdditionalDice|(AlchemistBombLVL-1)/2`, `AlchemistBombDC|10+(AlchemistBombLVL/2)+INT`, `AlchemistBombTimes|AlchemistBombLVL+INT`) -- a real, marquee win the original assessment never credited, since Bomb is a weapon-damage idiom (like Sacred Weapon), not opponent-dependent; and confirmed Poison Resistance's tiers (`AlchemistPoisonLVL` gates at `AlchemistLVL,2/5/8/10`) are identical in shape to Investigator's own, though per the established "parallel copy over cross-class reuse" discipline it gets its own Alchemist-named formula, not a shared function call. **Greenlit and dispatched to backend**: Bomb + Poison Resistance + spellcasting (reusing the shared list and Investigator's own validation shape). Flagged a real formula-verification caution before building: the corpus carries a conditional `PREMULT`-gated variant of `AlchemistBombAdditionalDice` alongside the base formula, worth the same full-level-range verification rigor the Sacred Weapon dice-count bug taught, not just a base-formula spot-check.

**MILESTONE -- ALCHEMIST FULLY CLOSED (`9a0022e0`) ALONGSIDE THE FEAT-EFFECTS CONSUMER WIRING, TASKS #4 AND #16 BOTH DONE, LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD.** Backend investigated the flagged Bomb-formula hazard properly before trusting it, exactly the rigor asked for: the conditional `AlchemistBombAdditionalDice` override is gated behind `Alchemist_CF_BombDamage<N>` flags that only ever get set by Ultimate Magic's own Psychonaut/Reanimator archetypes; the `BonusBombCount/2` term is a Gnome-only Favored Class Bonus. **Lead independently re-verified both** before accepting: confirmed Ultimate Magic content is not ingested anywhere in this repo's own `data/corpus/` at all (the flag is only ever set in the separate PCGen source tree's `um_abilities_class.lst`, never reachable), and confirmed `BonusBombCount`'s own corpus record is explicitly `# Gnome Alchemist`-headed with `PREFACT:1,TEMPLATES,IsGnome=true` -- both genuinely provably vacuous, not hidden bugs, exactly the discipline the Sacred Weapon lesson was meant to instill. Bomb (damage/DC/uses-per-day), Poison Resistance (own parallel-copy formula, identical tiers to Investigator's), and prepared-extract spellcasting (reusing the shared spell-list module) all landed, `named_features_wired` 1->3. **Same commit also wires both of featmate's feat-effects producers** (the 8 General feats + Persuasive's Diplomacy half, and Skill Focus) into `pilot_compute.rs` via a new `ground_standalone_feat_skill_facts`, unconditional on class ownership since these are general feat effects. Lead re-verified the full committed HEAD: 582/582 lib, `sd24_apg_class_coverage_audit` 3/3, 19/19 Alchemist tests, all 6 consumer-wiring tests individually -- including the load-bearing `skill_focus_grounds_nothing_without_an_explicit_target_choice` proof that the no-silent-seeding design survived all the way through the real pipeline. Tasks #4 and #16 both marked completed. **Feat-effects tally corrected**: named CRB feats with real mechanical effects wired rises from 7 to 16 of 185 (adds the 8 standalone General feats + Skill Focus) -- updated the dashboard producer's own hardcoded count and citation (outside this repo, in the hermes skill scripts) to match, regenerated the JSON, confirmed it still runs clean. Backend holding for the next assignment.

**Catch-up entry -- several real events landed without a narrative write-up at the time; recording them now for continuity.** In order:

1. **Warpriest (task #9) re-scoped by scout, greenlit.** A more nuanced pass than Skald/Hunter/Investigator/Alchemist's clean 4-for-4: Fervor (uses `WarpriestLVL/2+WIS`, heal dice `1+max(0,min(20,WarpriestLVL)-2)/3`), Sacred Armor (enhancement `1+max(0,(min(20,WarpriestLVL)-7)/3)`, uses `WarpriestLVL`), and Channel Energy DC (`10+WarpriestLVL/2+WIS`) are real missed wins, lead-verified byte-exact against `acg_abilities_class.lst`. But ~15 of 66 blessing-power records are genuinely "touch a weapon to enhance it" or "summon a battle companion" abilities -- confirmed via direct DESC-text sampling -- needing a weapon-enhancement activation surface and a summon-monster subsystem this engine has neither of. Sacred Weapon's own active enhancement hits the same wall. Greenlit the tractable slice only; deferred the weapon-enhancement/summon question as its own future design decision, same category as Familiar/Eidolon. Dispatched to backend, still in progress.

2. **A real operational incident, found and fixed.** While independently verifying unrelated work, `docs/release/v0.6/SWARM_STATUS.md` was found committed as a literal 0-byte file on origin (`c377ca74`, an "auto-heartbeat refresh" commit). Root cause, confirmed via `/var/log/syslog`: the crontab fired dozens of duplicate job invocations within the same second (a pre-existing scheduling bug, unrelated to this swarm's own work), and the heartbeat script (`~/workspace/swarm-status-heartbeat.py`) had no defense against concurrent execution -- two-plus instances raced on a truncating `open(path, "w")` write, and one instance's `git add`/`git commit` captured another instance's transiently-empty file. **No data was actually lost** -- the local working tree still had the full content uncommitted -- but the committed/pushed record was genuinely empty. Restored immediately (`fb8b8ad6`), then hardened the script with three independent defenses: a non-blocking file lock (concurrent invocations now skip instead of racing), an atomic write (temp file + rename, so a concurrent reader can never observe a transiently-empty file), and a size sanity check (refuses to write/commit anything that looks like data loss regardless of cause). Verified the fix under simulated 3-way concurrent load: 2 instances correctly skipped, 1 ran cleanly, file stayed intact. Confirmed holding through many subsequent real cron ticks. The separate crontab duplicate-firing bug itself was not chased further -- the other affected scripts (`dashboard-watchdog.sh`/`dashboard-renderer.sh`) are idempotent restart-if-down checks, so duplicate firing there is harmless, unlike the heartbeat script's real write-then-commit vulnerability which is now fixed regardless of upstream cause.

3. **All three teammates (backend, scout, featmate) were found unreachable** shortly after the incident -- not just unresponsive, but genuinely gone (their team-session inbox files showed no activity for hours, correlating with roughly the same window as the crontab anomaly). Respawned all three fresh under a new team session (`session-d1699e87`, replacing the expired `session-495aade3`), each with a full brief covering their lane constraints, this segment's methodology history (especially the standalone-grounding correction), and their in-flight assignment. Updated the heartbeat script's `TEAM_DIR` constant to point at the new session so inbox-activity tracking stays accurate going forward.

4. **The 18-task shared backlog was found orphaned** by the same session boundary (the Task tool's list is scoped to the team session, and a new one had just been created) -- recreated all 18 tasks fresh, same subjects and same ID ordering (1-18), with statuses/owners restored to match reality (task #9 Warpriest in_progress/backend, task #10 Oracle in_progress at the time, task #16 completed, task #17 Summoner still explicitly gated pending an operator decision).

5. **Oracle (task #10) re-scoped by scout -- the most thorough pass this segment has produced.** Every formula re-derived directly from `apg_abilities_class.lst`, not carried from any prior doc. Real corrections found: **6 Curse records exist, not 5** (`Oracle ~ Tongues` is itself `TYPE:...OracleCurse` -- lead independently confirmed this exact typing); **Wasting's own `+4 Intimidate` nets to zero, not +4** (its `BONUS:SKILL|STAT.CHA|-4` hits Intimidate too, since it's a CHA-based skill -- lead independently confirmed both tokens present, matching real PF1 RAW); **the energy-resistance tier is 5/10/20, not 5/10/15** (both `PRECLASS` branches of the inner var stack at level 11+ -- lead independently re-derived the arithmetic and confirmed it exact, the same class of sibling-branch trap as the Sacred Weapon `/20`-vs-`/15` bug). Plus 4 more build-time hazards named (a multiclass half-progression var, Cinder Dance's mutual exclusion with the Lame curse, several corpus KEY typos that would silently break a naive name-keyed lookup, and two corpus-incomplete records marked with the corpus's own `# TODO` comment). **Greenlit**: Curses (Lame/Wasting/Deaf) + Tier 1 (6 revelations: Life/Channel reusing Shaman's own already-shipped Channel shape, Lore/Sidestep Secret, Nature/Nature's Whispers, Lore/Lore Keeper, Bone/Near Death, Flame/Cinder Dance), `named_features_wired` 2->11. Tier 2 (two shared parameterized mechanisms covering 8 more records) is an optional same-session follow-on; Tier 3 and all ally/environment/opponent-dependent revelations stay deferred. **Ruled two durable methodology questions** raised in the doc: a flat modifier grounds fine even on an *opposed* check (the Studied-Target line is specifically about needing a persistent tracked relationship with a specific opponent, not about a roll being opposed) -- and ground one canonical revelation per mystery in code, Warpriest-Blessing style, only one live per character (matches the real `OracleMysteryLevel` cap of exactly one revelation at level 1). Queued for backend behind Warpriest.

6. **Featmate's fresh CRB feat re-sweep under the corrected bar landed** (`2d1e0cf0`, `feat-effects-corrected-bar-resweep-scoping.md`) -- verified every candidate against both the `BONUS:` token AND the authoritative `BENEFIT:` prose, not this repo's own transcription. Found 17 more real, corpus-verified wins with no new engine needed: Improved Initiative (`COMBAT|INITIATIVE|4`, cleanest record in the catalog), Endurance (flat `+4`, also deepens Ranger's own existing grant-only record for free), the 11-feat combat-maneuver family (5 Improved `CMB_X,CMD_X|2` + 6 Greater `CMB_X|2`, byte-identical to the already-grounded Monk/Dwarf-Stability idiom), Stunning Fist (DC `10+(TL/2)+WIS`, confirmed byte-identical to the already-grounded `monk_scorpion_style_dc` -- a live inconsistency in Monk's own prior closure, not a new judgment call), and the movement trio. **Two real catches along the way**: Extra Channel's corpus token is a bare `BONUS:ABILITYPOOL|Extra Channel|1`, which reads as magnitude 1, but the real rule is +2 -- grounding from the token alone would have fabricated a wrong, checkable number; correctly deferred. A genuine duplicate `"Combat Expertise"` key exists in the generated `feat_data/combat.rs` (2 raw corpus records collapsed under one key by the generator) -- harmless today since nothing reads it, logged as a data-quality finding rather than hand-edited (the file carries its own "do not hand-edit, regenerate if corpus changes" discipline). **Lead independently verified all 5 of the highest-stakes claims** directly against `cr_feats.lst` before accepting: the Extra Channel token, the Combat Expertise duplicate count, Improved Initiative's formula, Stunning Fist's DC match, and one maneuver-family formula -- all exact. **Greenlit Category A** (the 17 no-new-engine wins) for featmate to build now; **split Category B** (Extra Rage/Performance/Ki/Lay On Hands -- these modify already-computed totals rather than grounding standalone facts, and need real occurrence-counting for their `STACK:YES MULT:YES` repeatability plus prereq-gating, a genuinely different shape needing backend's careful involvement) into its own task (#19), queued behind Warpriest/Oracle. Leadership's own `TL+CHA` formula deferred entirely (a single computable number from an almost-entirely-unmodeled cohort subsystem would be a thin, arguably misleading win). Catalog tally: 16 -> 33 of 185 once Category A lands.

**MILESTONE -- WARPRIEST'S GREENLIT DEEPENING LANDED (`c6dd2c6b`), TASK #9 CLOSED, ALONGSIDE FEATMATE'S CATEGORY A FEAT RE-SWEEP (`19ca4ae6`), LEAD-VERIFIED AGAINST THE REAL COMMITTED HEAD.** Backend re-derived every formula directly from the corpus rather than the scoping doc and caught two real hazards along the way: Fervor's `/3` sits outside its `max(0,...)` while Sacred Armor's sits inside -- adjacent records, opposite nestings, the exact sibling-branch shape the Sacred Weapon bug taught (they happen to agree numerically since the clamp floors negative values either way, but backend didn't rely on the coincidence and transcribed each precisely, pinned by full-level-range tests). And a genuine corpus discrepancy on Channel Energy's DC: the class feature's own `BONUS:VAR` reads `10+WarpriestLVL/2+WIS`, but the separate Channel Positive/Negative Energy display records carry `10+(TL/2)+CHA` in their DESC format-string args -- backend took the computed `BONUS:VAR` as authoritative (WIS is genuinely Warpriest's own key ability score, unlike Cleric's CHA, so the CHA variant reads as an un-updated copy-paste artifact of the Cleric original). **Lead independently confirmed both** directly against `acg_abilities_class.lst` before accepting -- the opposite nestings, and that the CHA variant really does live only in the two Channel-ability display records' own DESC args, not the actual computed token. Strength Surge (`max(1,WarpriestLVL/2)`, self-targeted per its own DESC, same minor-blessing grant gate as Destructive Attacks) rounds out the slice as a fourth standalone fact; correctly does not bump `named_features_wired` a sixth time since it folds into the existing Blessings slot. **Backend disclosed and properly retrofitted a real TDD-ordering slip** (wrote the implementation before the tests) rather than silently asserting compliance -- perturbed all four formulas, confirmed exactly 5 tests went red, restored and reconfirmed green. Verified in an isolated `git worktree` this time, not a stash, per the fix from the earlier incident. `named_features_wired` 2->5 for Warpriest. Lead re-ran everything against the real committed HEAD: full lib 621/621, both SD-24 coverage audits 3/3, all 18 Warpriest-specific tests individually. Task #9 marked completed -- Sacred Weapon's active enhancement and the ~15 touch-weapon/summon Blessings remain correctly deferred as the same named architecture gap, not folded into this closure.

**Featmate's Category A landed the same cycle.** All 17 no-new-engine wins from the re-sweep, verified against both the corpus's `BONUS:` token and its authoritative `BENEFIT:` prose. One real correction from the scoping doc during implementation: the combat-maneuver family is 12 feats, not 11 -- featmate deliberately included Improved Grapple (the scoping doc had excluded it since Monk's own synthetic `feat:improved_grapple` path already grants it, but a non-Monk picking it from the real catalog gets nothing today, a real gap this closes). **Lead independently confirmed the corrected count** directly against `cr_feats.lst`: 6 maneuvers (Bull Rush/Disarm/Grapple/Overrun/Sunder/Trip) x 2 tiers = 12, matching the commit exactly. `feat_effects` test count 38->68. Lead re-ran the full suite: 68/68 feat_effects tests, 621/621 full lib (combined with Warpriest's own landing). Producer side only -- consumer wiring into `pilot_compute.rs` (making these facts actually surface on a real character) stays backend's lane, queued behind Oracle and Category B.

<!-- AUTO-HEARTBEAT-START -->
## Auto Heartbeat (mechanical, cron-driven every 5 min -- lead does not hand-edit this section)
Last mechanical check: 2026-07-27T06:40:01
Latest commit on tranche/6: e5f25b22 chore(v0.6): auto-heartbeat refresh [skip lead-review] (5 minutes ago) (2026-07-27T06:35:02-04:00)
Last inbox activity (mtime proxy): backend=2026-07-26T21:32:41 frontend=unknown qa=unknown lead=2026-07-26T21:42:58
<!-- AUTO-HEARTBEAT-END -->
