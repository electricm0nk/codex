# v0.6 Alpha Release Swarm — Risks and Open Questions

> **Consolidated future-epic scoping** for the three architecture-level gaps
> tracked below (items 1, 17, 18) lives in a single cross-referenced writeup
> at `docs/release/v0.6/future-epic-scoping.md` — read that first for the
> operator-facing picture before diving into the chronological detail here.

## Operator directive: fully autonomous, operatorless operation (2026-07-23)

The operator instructed the lead directly: keep pushing into the remaining backlog, make judgment calls without pausing for operator confirmation, and do not stall waiting for operator input — this is intended to run unattended for multiple days ("don't want to come back after 4 days and find out you have been stalled for 3.75 days"). Effective immediately:

- Any item previously marked "needs operator sign-off" or "operator's call" in this document that does NOT require merge/publish authority is resolved by the lead's own best judgment below, documented with reasoning, rather than left open pending a response that may not come for days.
- Teammates are instructed not to wait for lead check-in between queued tasks — they work down their assigned list, message the lead only for genuine blockers or completions worth recording, and otherwise keep moving.
- The lead maintains a standing backlog queue at all times so no teammate goes idle without a next task already assigned.
- A recurring cron check-in (~every 20 min, session-only, 7-day auto-expiry) re-prompts the lead to verify no teammate has gone idle unassigned and to keep dispatching real work, as a backstop against the conversational turn-based flow ever going quiet.
- Merge-to-`develop` authority and any genuinely irreversible/high-blast-radius decision remain the operator's alone — autonomy here is about not stalling on ordinary engineering judgment calls, not about self-authorizing the closure handshake.

## Operator override: wired-integration ceremony waiver (2026-07-23)

Per `docs/governance/no-stub-mvp-doctrine.md` (`scope: universal`), the four-check audit criterion is "not optional and not waivable except by explicit operator override recorded in `risks-and-open-questions.md`." This file records that override for the v0.6 alpha release swarm, approved by the operator (Todd Hintzmann) via the launch-readiness revision of 2026-07-23:

- **Waived:** the per-cycle receipt *ceremony only* — no cycle receipts under `programs/codex/requirements/`, no closure-epilogue doctrine, no per-bundle kanban board. The swarm's audit evidence lands in `docs/release/v0.6/SWARM_REPORT.md` instead (raw grep output pasted per §7.1 of `release-swarm.md`).
- **NOT waived:** the four-check audit itself (forbidden tokens, no-op handlers, mock-library leaks, "Would …" strings). It runs against the swarm's combined diff (`git diff develop...tranche/6`) before the closure PR opens, executed by the QA engineer and re-verified by the lead. There is no in-repo runner script; the four greps are run by hand per the doctrine's "Per-cycle audit" section.

## Risks

1. **Alpha-bar distance — SHARPENED, then SCOPED.** Current state (`docs/architecture/status.md`, 2026-07-23): only single-class Fighter 1–3 reaches a `Computed` receipt; 7 of 10 CharacterSheet tabs stubbed; Feat picker and Money panel do not exist; level-up/skill/bio persistence are no-ops. The §1 bar (6-level multiclass, four books, PCGen parity, zero stubs) requires multiple task waves; the go/no-go checkpoints in §6 are the containment mechanism. **Update (backend, task 2 delivery):** the compute engine's `Computed` path is narrower than "Fighter 1–3" implied — it currently only computes for one exact hardcoded posture. Any other skill/equipment choice returns `Blocked` with diagnostics rather than a receipt — pre-existing engine narrowness, not something the swarm introduced, but it means most tester choices through the newly-wired UI will hit Blocked, not Computed, until this is widened.

   **Full scoping (backend proposal, 2026-07-23, greenlit in part):** the narrowness is actually 4 independent exactness gates in `pilot_compute.rs`, each class-agnostic except one sub-check, any deviation trips `Blocked` with no partial credit:
   1. `unmet_combat_posture_conditions` — exact Longsword+Chain Shirt, no shield, Power Attack selected-inactive, Dodge+Weapon Focus feats (one Fighter-only sub-check).
   2. `unmet_selected_skill_posture_conditions` — exactly Climb/Intimidate/Swim rank 1, chain shirt equipped, nothing else.
   3. A Human-Wizard-only spellbook posture gate (level ≤3, canonical Evocation specialization, exact prepared spellbook) — newly found, wasn't previously documented, fires independently.
   4. AC specifically: `baseline_armor_class` (the gate) never calls `equipment_effects.rs`'s `compute_equipment_effects`, which already computes *real, correct* per-item AC deltas for arbitrary gear and is already wired into the separate desktop-UI equipment tab. The correct math exists and is simply disconnected from the gate.

   **Sizing by pillar (backend's original proposal):** AC — small-medium, a real wiring job (initially greenlit, scoped to Fighter/Wizard/Rogue: splice the existing correct math into the gate). Attack bonus — large, needs genuinely new equipment-driven to-hit math that doesn't exist (feats/enhancement bonuses only today). Skills — medium-large: `allocate_skill_ranks` only maps 5 skills, class-skill recognition is Fighter-only, and has no armor-check-penalty term at all (explicitly deferred in its own doc comment) — not a drop-in fix even for the 5 skills it knows. Wizard spellbook gate — separate, untouched by the above.

   **AC SLICE DROPPED — the real finding is architectural, not per-pillar (backend, mid-implementation, 2026-07-23):** the original AC sizing assumed the correct per-item math could be spliced directly into the gate. It can't, cheaply. `compute_combat_baseline`/`baseline_armor_class` (the thing that gates `Computed`/`Blocked`) lives in the **headless** compute layer — `build_pilot_headless_receipt`/`compute_pilot_base_chassis` take a `CharacterInput` only, **no corpus parameter**, by design. Real per-item AC values only exist via `equipment_effects::arms_armor::compute_arms_armor_effect`, which needs a corpus-resolved `EquipmentRecord` — corpus access the headless layer structurally doesn't have. The static, corpus-free `equipment_tables()` carries no AC-bonus field at all. Bridging this means either (a) threading a corpus parameter through the entire headless chassis call chain — ~347 call-site grep hits, a blast radius comparable to or bigger than task 4's Rogue widening — or (b) moving the `Computed`/`Blocked` decision itself to the corpus-aware layer (`pilot_compute_corpus.rs`'s `compute_pilot_with_corpus`, which already has both), a real architecture decision about which layer owns claim-gating, not an implementation detail. Backend caught this *before* writing the change and stopped to ask rather than push forward on unscoped work or fabricate a corpus-free approximation — correct call, no code written.

   **Decision:** dropped for this swarm. Option 1 (leave the gate alone, since the corpus-aware desktop equipment tab already shows real AC for arbitrary gear today — that's existing behavior, not new work) is close to a no-op and not worth landing on its own. Option 2 (the corpus-threading/layer-ownership change) is a genuine future epic, not a wave-2 item — **flagging for the operator**: this likely isn't just an AC problem. If attack-bonus and general-skill widening (already sized large/medium-large above) hit the same headless/corpus-aware split, bridging that one architecture boundary might be the actual unlock for all three posture-narrowness pillars at once, rather than three independent slices. Worth a dedicated scoping pass before any future wave attempts AC, attack-bonus, or skill-posture widening again. Backend proceeding instead to items 2-4 (durability, money-conversion PCGen verification, comparator field-extraction), none of which share this problem.

   **Follow-up scoping pass, answered precisely, split by dimension (backend, 2026-07-24, read-only, no code).** Confirms the hypothesis for two of the three remaining pillars, refutes it for the third:
   - **Attack-bonus widening: YES, same wall, confirmed exactly.** Real to-hit for arbitrary weapons needs `equipment_effects/equipmods.rs`'s `compute_equipmods_effect`, which reads a weapon's magic enhancement bonus (`WeaponEnhancementBonus`, the corpus's `BONUS:WEAPON|TOHIT|...|TYPE=Enhancement` token) off a corpus-resolved `EquipmentRecord` — same structural requirement as AC's `compute_arms_armor_effect`. The static, corpus-free `equipment_tables()` table carries nothing enhancement-related.
   - **Skill-posture widening splits into two genuinely different sub-problems.** (1) Armor-check-penalty for arbitrary armor: **YES, same wall** — the real corpus record's `ACCHECK:` token exists (verified against `Leather Armor (Base)`, `ACCHECK:0`) but the static `equipment_tables()` doesn't carry it. (2) Class-skill-list recognition (which skills are class skills for Wizard/Rogue/Cleric/etc., beyond the already-fixed Fighter/Rogue case): **NO, this is NOT corpus-blocked** — it's a labor-volume problem, not an architecture wall. This data isn't in any static table today, but it doesn't need corpus access to get there: it can be hand-authored as literal per-class skill lists, exactly the existing `GROUNDED_FIGHTER_CLASS_SKILLS` precedent already used in `skill_allocation.rs`.
   - **Bottom line for the future-epic writeup:** bridging the headless/corpus-aware boundary would genuinely unlock both AC-shaped gaps (attack-bonus enhancement, skill ACP) at once — confirms the hypothesis for those two dimensions specifically. It would **not**, by itself, close the class-skill-list gap, which needs separate hand-authored per-class data regardless of which layer owns the gate. Both halves stated explicitly so a future scoping pass doesn't overstate what the architecture fix alone would buy.

   **NEW — Wizard spellbook gate is unreachable, not just narrow (frontend, 2026-07-23).** Gate 3 above turns out to be categorically worse than gates 1/2: it isn't merely one exact combination out of many, it's **structurally impossible to satisfy through any shipped UI path**. `compose_character_input` (the fixed-loadout composer every UI-created character goes through) never seeds the required `choice:wizard_school_specialization -> school:evocation` plus opposed-Necromancy/Transmutation choices for any class, and `CreateCharacterForm.tsx` has no arcane-school field at all. Verified live two independent ways — fresh Wizard creation, and multiclassing Wizard onto an existing character via level-up — both correctly `Blocked` with the real diagnostic, neither persisted (reject-don't-persist invariant held). This directly blocks bar item 3's "select spells at each spell-gaining level" for the entire class most associated with spellcasting, and is likely the deeper reason `status.md`'s "only Fighter reaches Computed" framing has held architecture-wide.

   **Decision:** seed the required choices as a hardcoded default in `compose_character_input`, mirroring the existing precedent — Fighter's loadout already hardcodes Power Attack/Dodge/Weapon Focus the same way, so this is consistent with the fixed-loadout composer's current maturity level, not a new kind of shortcut. Sufficient for the bar: item 3 requires spell *selection*, not arcane-school *choice* — hardcoding the school while leaving real spell selection intact satisfies the literal requirement. A real UI-facing arcane-school selector (frontend's Option A) is a separate, larger feature — **backlog, not blocking**. Backend pausing item 4 to take this first, since it unblocks a literal named bar requirement rather than test infrastructure.

   **SOBERING FOLLOW-UP — Wizard's fix is a lucky exception, not a pattern (frontend survey, 2026-07-23).** Checked the other four caster classes in `pilot_compute.rs` (Cleric, Druid, Bard, Sorcerer) for an equivalent single-seeded-choice gate. None exists. Each has its own `supported_X_level` gate with a doc comment listing genuinely unimplemented calculation subsystems, not missing UI inputs: **Cleric** — no channel energy resolution, no domain spell-list contents, no spellbook posture, no spells prepared, no spontaneous cure/inflict conversion, no spell slots per day, no save DCs, no Wisdom bonus spells. **Druid** — same shape, flat identity records for class features (Wild Empathy etc.) but zero spell math anywhere. **Bard** — flat numbers for non-spell features, explicitly "no spell math whatsoever," plus no bardic-performance execution engine at all (no action economy, no round tracking, no Fascinate resolution). **Sorcerer** — doc comment explicitly says "no bloodline arcana, or spell math," same shape as Bard. None of these are "one missing seeded choice away from Computed" the way Wizard was — they're missing entire arithmetic/mechanic subsystems, which seeding a default cannot fix. **Scoping implication:** the alpha bar's "any class... from the four primary books" (§1 item 2) is a substantially bigger lift than Wizard's fix suggests — backend's incoming fix makes Wizard the *second* class (alongside Fighter) a tester can reach `Computed` with, but the other four full/partial spellcasters remain genuine multi-epic engine work, consistent with `status.md`'s existing "only Fighter reaches Computed" framing. Worth the operator weighing this when judging how close v0.6 alpha actually is to its own bar. **PARTIALLY RESOLVED then CORRECTED, 2026-07-23: the compute-gate fix (3484b5d) landed, but frontend's live testing found it necessary, not sufficient — see the new item 10 below. Wizard does not yet reach `Computed` through any shipped UI path.**

   **Comparator wiring confirms the architecture split is real, not AC-specific — then finds a clean way around it for encumbrance (backend, 2026-07-23).** Wiring `SelectedParityDimensions::from_receipt` to expose the new encumbrance/durability/money PCGen-comparison dimensions turned out to be three genuinely different problems, not one small task — backend stopped before writing code and split them honestly: **(1) durability** — small, bounded, landed. **(2) encumbrance** — initially looked blocked by the exact same headless/corpus-aware wall that killed the AC slice (`compute_encumbrance` needs real corpus-resolved item weights; `from_receipt`'s headless receipt has no corpus access) — independent confirmation the boundary is real. **RESOLVED, not deferred after all**: backend found `contract::PilotReceipt` (produced by `contract::to_pilot_receipt`) is *already* corpus-aware and already carries both `chassis` (the same struct `from_receipt` reads) and a resolved `encumbrance: EncumbranceComputation` — so instead of forcing corpus access into the headless layer, backend added a new, additive `SelectedParityDimensions::from_pilot_receipt(receipt: &contract::PilotReceipt, ...)` that reads from this already-resolved data. `from_receipt` itself is provably untouched (zero blast radius on QA's 3 existing callers). This sidesteps the architecture wall for *parity-testing* purposes without touching the harder problem of moving the `Computed`/`Blocked` gate itself into corpus-awareness (that part of the finding, relevant to AC/attack-bonus/skill-posture widening, still stands as a future epic). **(3) money** — confirmed, not deferred: a different kind of gap entirely, `money.json`'s balance is persisted session state never derived from `CharacterInput`/`PilotReceipt` at all, so there's structurally nothing for any receipt-projection function to expose. **Backlog item stands**: money parity-testing, if ever wanted, needs its own comparison path reading `money.json` directly.

    **OPERATOR DIRECTIVE (Todd Hintzmann, 2026-07-24) — work out the math, add to backlog.** Un-deferred, alongside items 7 and 17. Scope: this is the harder of the three un-deferred items — it isn't a single build task like starting-wealth or a bounded first-slice like Toughness's HP, it's the design/derivation work `future-epic-scoping.md` (gap A) already flagged as needing "a dedicated scoping/design pass before committing to either fix shape." Two things need working out, not one: (1) **the architecture decision** — thread a corpus parameter through the headless chassis call chain (~347 call sites) vs. move the `Computed`/`Blocked` gate to the already-corpus-aware layer (`pilot_compute_corpus.rs`); (2) **the actual combat math that doesn't exist yet** — real equipment-driven attack-bonus (enhancement bonuses, currently only feats/base BAB are computed) and the armor-check-penalty term for arbitrary armor, both confirmed genuinely unwritten, not just disconnected (backend's item-1-adjacent scoping pass). AC itself is closer to a non-issue (the desktop equipment tab already shows correct AC via the corpus-aware path) — the real payoff here is attack-bonus and skill-ACP. **Queued, third in line** — dispatches resumed; backend working items 7 (done) → 17 → this one, expected to start with the architecture-decision half before any math-derivation work, since the fix shape determines where the new formulas need to live.

    **Design pass done (backend, `4dadad51`, `docs/release/v0.6/item-1-architecture-wall-design.md`) — the earlier sizing was too coarse, three real findings reshape the picture.** (1) **Most of the real math already exists**: `equipment_effects.rs`'s `compute_equipment_effects` is fully written, tested, and already running in production (the desktop equipment tab calls it directly for AC); `pilot_compute_corpus.rs` already carries a `DerivedEquipmentStats` struct on every resolved item, just never populated (`::default()` unconditionally). ACP turns out to be a few-line field addition to a function that already has the record in hand, not a corpus-access problem. (2) **Attack-bonus enhancement has a genuine data-model gap, separate from corpus access**: `EquipmentSelection` is flat (`{item_id, equipped_or_active, active_state}`) with no way to express that a "+1 Enhancement to Weapon" item attaches to a specific weapon — real PF1 composes a "+1 Longsword" as base+modifier; summing the enhancement bonus without resolving that attachment would silently misapply one weapon's bonus to another or double-count on a two-weapon build. Needs its own design decision (a schema change, or a narrower one-weapon-equipped convention), independent of whichever fix shape wins. (3) **The biggest correction — bridging the corpus wall alone would not unlock a single additional equipment loadout today**: `unmet_combat_posture_conditions`/`unmet_selected_skill_posture_conditions` gate on an *exact* hardcoded `item_id` (`"item:longsword"`, `"item:chain_shirt"`), completely independent of what the math could resolve for other gear — so even with full corpus access wired into the claim gate, zero currently-displayed numbers would change. The wall's payoff is entirely contingent on a separate, larger, unasked question: **would `Computed` ever accept an equipment loadout other than the one exact hardcoded posture?** That's a claim-gating philosophy call (how much variation is "supported enough" to claim correctness), not an engineering one — logged as item 27 below for the operator, distinct from the fix-shape question.

    **Three fix shapes compared, with real call-site counts**: (a) thread corpus through the headless chain — ~19+37+100ish call sites, large, and per finding (3) buys nothing today without the separate posture-gate decision; (b) move the `Computed`/`Blocked` decision to the corpus-aware layer — a real cross-cutting change to every command's save/mutate control flow, same ceiling as (a); (c) **NEW, recommended** — enrich the corpus-derived DTO section (where `compute_pilot_with_corpus` already has corpus in scope) by calling the already-existing `compute_equipment_effects`, surfaced as an additional, explicitly-not-claim-gated display section, same shape `corpus_derived.equipped_items` already uses today. Zero changes to `build_pilot_headless_receipt`'s signature or any of its call sites, zero changes to claim-gating semantics. Delivers real ACP and a second AC path now; does not close attack-bonus (needs the attachment decision first). Full comparison table in the design doc.

    **Shape (c) implemented (backend, `08a829a1`†).** `EquipmentStatEffect`/`EquipmentEffects` (`equipment_effects.rs`) gained a real `armor_check_penalty`/`armor_check_penalty_total` field, reading the `ACCHECK:` token `arms_armor.rs`'s own doc comment already cited as present — the few-line fix the design pass predicted. `pilot_compute_corpus.rs`'s `CorpusDerivedSection` gained a new `equipment_effects: EquipmentEffects` field, computed inside `compute_pilot_with_corpus` over the `EquippedActive`-filtered equipment (mirroring `contract::to_pilot_receipt`'s own filtering exactly) via the already-existing `compute_equipment_effects` — `build_pilot_headless_receipt`'s signature and every one of its call sites untouched, as scoped, and deliberately **not** wired into any claim-gated pillar. Surfaced through the desktop DTO layer as a new `CorpusDerivedDto.equipmentEffects` section (`armorClassDelta`, `armorCheckPenaltyTotal`, `maxDexCap`, `spellFailureChance`), explicitly separate from the claim-gated `PilotSnapshotDto` fields. Attack-bonus enhancement deliberately excluded (the attachment-relationship gap from the design pass still stands, unresolved). Genuine RED→GREEN at every layer: `arms_armor.rs`'s 3 existing tests extended with real ACCHECK assertions (Leather Armor `Some(0)`, Buckler `Some(-1)`, Longsword `None`); 3 new `pilot_compute_corpus.rs` tests (real penalty present, `SelectedInactive` correctly excluded, no-equipment correctly zero) with the wiring temporarily disabled to confirm the failure before restoring; 1 new end-to-end `character_hub.rs` test proving the real Fighter fixed loadout's Chain Shirt surfaces a real nonzero ACP through the actual `create_character` command, same disable-verify-restore discipline. 236/236 lib, 202/202 desktop, full workspace 4253/0 clean. †Landed via a shared-checkout index race (backend's staged work got swept into the lead's own docs commit while restaging mid-flight) — both sides independently confirmed the content is complete, correct, and unaffected by the race; attribution corrected here for the record, not re-done. Backend correctly declined to rewrite history over a cosmetic commit-message issue in a live shared checkout.

    **Attack-bonus, bounded single-weapon slice landed (backend, 2026-07-24), greenlit by the lead as a real engineering-scope call distinct from item 27's philosophy question.** `EquipmentEffects` gained `attack_bonus_delta: Option<i16>`: `Some(n)` only when exactly one real weapon (a record carrying a `DAMAGE:` token, the same signal `damage_total.rs`'s own `resolve_base_damage_dice` already uses to identify a weapon) is `EquippedActive`, summing every equipped `equipmods` item's TOHIT-affecting enhancement bonus (`"TOHIT"` or `"DAMAGE,TOHIT"`, explicitly excluding a `"DAMAGE"`-only bonus, which doesn't affect the attack roll). `None` for zero or two-or-more weapons equipped — the attachment ambiguity the design pass identified stays honestly unresolved rather than guessed at; `Some(0)` is a real, distinct value from `None` (unambiguous, just no enhancement present) so the frontend must not treat the two the same. No schema change to `EquipmentSelection` — the narrower, bounded alternative to the attachment-relationship redesign, matching this session's established "prove the narrow provably-correct case" discipline (Toughness-only, the durability multiclass-order deferral, etc.). Surfaced on `CorpusDerivedDto.equipmentEffects.attackBonusDelta`, same non-claim-gated section shape (c) already established. Genuine RED→GREEN: 6 new `equipment_effects.rs` tests (single weapon + real enhancement, Masterwork TOHIT-only, a DAMAGE-only bonus correctly excluded, single weapon with no enhancement yielding a real zero, zero weapons honestly absent, two weapons honestly ambiguous), disabled-then-restored to confirm the exact expected failures; 1 extended end-to-end `character_hub.rs` test proving the real Fighter fixed loadout (exactly one weapon, no equipmods) surfaces the real `Some(0)` through the actual `create_character` command. 242/242 lib, 202/202 desktop.

    **OPERATOR DECISION (Todd Hintzmann, 2026-07-24) — add an equipment-attachment schema field.** Explicit call, made after the swarm-wide closure-readiness checkpoint (item 31): resolve the multi-weapon ambiguity with a real data-model change rather than leaving it honestly absent long-term. `EquipmentSelection` needs a way to express that a modifier (e.g. a "+1 Enhancement") attaches to one specific weapon, not the whole loadout — the flat `{item_id, equipped_or_active, active_state}` shape has no slot for this today. Backend to design the schema addition (new field vs. a separate attachment table/map), migrate `EquipmentSelection` and its consumers, and widen `attack_bonus_delta` to resolve correctly for 2+ weapons once the attachment is explicit. This overlaps with item 27's decision (widening the posture gate to accept any equipment) — both need the same underlying attachment data, so backend should sequence/scope them together rather than solving the schema twice.

    **Scoping plan landed (backend, `1f8dd2fa`, `docs/release/v0.6/items-1-and-27-scoping.md`), design/scoping only, no code.** Verified PCGen's own `.pcg` convention for an enchanted weapon before designing: a single equipment entry carrying its own applied modifiers inline (`CUSTOMIZATION:...EQMOD=...`), not two entries needing a cross-reference — so the real schema shape is `applied_modifiers: Vec<String>` directly on `EquipmentSelection`, not a separate attachment/instance-id scheme (backend's first instinct, correctly discarded as heavier and non-PCGen-faithful). Real blast radius counted, not estimated: 67 `EquipmentSelection` construction sites (42 in `tests/**`); checked `ChosenCharacterState` as an alternative location too — 74 sites there, no cheaper. **Central finding**: items 1 and 27 are no longer separable — the original item-1 design pass's "bridging the wall buys nothing" conclusion assumed the posture gate stays exact-match; now that item 27 widens it, corpus-awareness becomes a genuine prerequisite. Three gate-widening approaches compared (extend the static bootstrap table; move `Computed`/`Blocked` to the corpus-aware layer; thread corpus through the full headless chain), with a 6-step bounded sub-task sequence at the end (schema field → per-weapon attack-bonus → static category data → corpus-aware verdict → command-layer wiring → frontend attachment UI), each independently testable per this session's established incremental-widening discipline.

    **OPERATOR DECISIONS (Todd Hintzmann, 2026-07-24) — all three open questions from the scoping plan resolved.** (1) Feat-completeness policy: **never block `Computed` on an unrecognized feat effect** — extends the existing precedent (items 14/17/18/20/21), an unknown feat effect stays a completeness gap, not a correctness bug. (2) Gate-widening mechanism: **replace the existing exact-posture gate outright**, not the additive/keep-both approach backend recommended as lower-risk — this means the Fighter/Wizard/Rogue posture must be re-verified against the new unified corpus-aware path too, not left as an untouched fast path; backend needs the fullest test coverage on sub-task 5 (command-layer wiring) as a result, every currently-`Computed` build must still compute identically through the new single path. (3) Sub-task 3 (static category pre-filter data-authoring pass): **skip it** — go straight to the corpus-aware layer (sub-task 4), which already resolves the same question at runtime; no separate static pass needed. Dispatched to backend: proceed with sub-task 1 (schema field), coordinate the ~67-site mechanical update with QA (42 of the sites are theirs), skip sub-task 3, and build the gate widening as a full replacement per decision (2).

    **Sub-task 1 landed (backend, `797bd436`).** `EquipmentSelection` gained `applied_modifiers: Vec<String>`, exactly the schema shape the scoping plan verified against PCGen's own `CUSTOMIZATION:EQMOD=` convention. All 25 non-QA `EquipmentSelection` construction sites fixed (14 `src/rules_core`, 11 `apps/desktop/src-tauri`); the fixture-text grammar gained a new `equipment_modifier=<item_id>:<modifier_item_id>` line (`apply_equipment_modifier`), attaching onto an already-parsed `equipment=` selection by `item_id` using the same last-colon convention `apply_equipment_selection` already relies on (item_id may itself contain a colon) — errors with a diagnostic if the `equipment_modifier=` line has no matching prior `equipment=` line. 4 new tests (default-empty, single/multiple attach, unmatched-id diagnostic); 262/262 lib (258 baseline + 4 new), 204/204 desktop, both crates build clean. The remaining 42 `tests/**` sites are QA-owned — exact file:line list and the one-line fix sent to QA directly, coordinated per the operator's dispatch rather than touched unilaterally. **Process note, no code impact**: `cargo fmt -p codex` (no path scoping) turned out to reformat all 516 tracked files in this crate, not just the ones touched this cycle — the crate had drifted from the currently-installed rustfmt's formatting. Caught before staging (verified every non-mine file's diff was pure reformatting, no semantic change, via `git status`/`git diff`), reverted everything except the 10 intentionally-edited files, redid those by hand. Flagging so nobody else runs an unscoped `cargo fmt` in this shared checkout; a per-file `rustfmt <path>` is safe.

    **Sub-task 2 design fork, resolved by the lead as a bounded engineering call (not escalated — distinct from items 1/18/27's philosophy questions).** Backend correctly flagged before writing code: `EquipmentEffects.attack_bonus_delta` is a single scalar for the whole loadout; with 2+ weapons each carrying their own `applied_modifiers`, summing their to-hit bonuses into one number has no real combat meaning (which weapon are you rolling to hit with?). **Resolved**: add `to_hit_bonus: Option<i16>` to `ResolvedEquipmentEffect` (one entry per equipped selection already), computed per-selection from that weapon's own `applied_modifiers`; keep `attack_bonus_delta` exactly as it means today (`Some` only for exactly one weapon equipped, `None` for 0 or 2+), now backed by real per-weapon data instead of the old heuristic, wire contract stays additive — nothing downstream breaks, sub-task 6's UI reads the new per-item field for the real multi-weapon case whenever it's built. Same "prove the narrow case, extend additively" discipline as shape (c) and the feat-effects widening.

    **Landed and lead-verified (backend, `5b0c7243`, 2026-07-24).** `compute_equipment_effects` now resolves each weapon's own `applied_modifiers` directly instead of the old "sum every flat equipmods item, gate on exactly-one-weapon-total" heuristic; `ResolvedEquipmentEffect` gained the agreed `to_hit_bonus: Option<i16>`; `attack_bonus_delta` kept its exact existing semantics, no wire/DTO change (`per_item` isn't exposed on the wire yet). **Real, deliberate behavior change**: an equipmods item equipped but not listed in any weapon's `applied_modifiers` no longer auto-attaches to "the only weapon present" — explicit attachment is now required, matching the PCGen-verified shape. Checked this against the one existing e2e regression risk: the Fighter fixed-loadout test in `character_hub.rs` asserts `attack_bonus_delta == Some(0)` — safe, because that fixture has zero equipmods equipped at all, so the auto-attach-vs-explicit-attach distinction never applies to it. 6 existing tests rewritten to use `applied_modifiers`, 2 new (unattached modifier doesn't count; two weapons resolve independently). **Lead independently re-ran**: `cargo test --lib` (263/263, matches exactly) and the desktop suite (204/204).

    **QA's fixture fix landed (`eeaa876d`, 2026-07-24) — real count correction, and a real bug self-caught mid-flight.** QA's first automated pass over-matched: a naive text search for `EquipmentSelection {` also caught `-> EquipmentSelection {` function-signature return types, not just struct literals, corrupting a few files with misplaced braces. Caught before landing, reverted everything, rewrote more carefully. **Corrected the real count**: backend's message said 42 sites; QA independently grepped and found 32 real struct-literal construction sites plus 10 separate function-signature matches that correctly should NOT get the field — the "42" didn't match backend's own enumerated list. Diff is exactly 20 files, 32 one-line insertions, matching the corrected count precisely. **Handled a live collision correctly**: backend's sub-task 2 was mid-edit (uncommitted) on `src/rules_core/equipment_effects.rs` while QA worked, breaking QA's first build attempt with an unrelated `E0063`. QA `git stash`ed just that one file, verified their own fixture changes in isolation, then popped the stash back untouched — did not attempt to fix or work around backend's in-progress file. **Lead independently re-ran**: `cargo build --tests` clean, full `cargo test --workspace` — 4280 total passed, 0 failed across all 478 test-result blocks, matches QA's report exactly. Both sub-tasks 1 and 2 fully closed. Backend cleared to proceed to sub-task 4 (the highest-risk step, replacing the exact-posture gate per the operator's decision); frontend cleared to start sub-task 6 (equipment-modifier-attachment UI).

    **Sub-task 6 design done (frontend, 2026-07-24), no code yet.** Confirmed there's genuinely no existing command surface for `applied_modifiers` before asking (only reference in `character_hub.rs` is a hardcoded `Vec::new()`). Design: an "Attach Modifier" button on each equipped item's Gear-tab row, reusing the existing generic `ItemPickerModal` scoped to the Equipmods category — no new catalog query needed. Sent backend a concrete proposal directly: a new `attach_equipment_modifier` command modeled on `purchase_equipment`'s atomic-purchase shape (charges the modifier's real `cost_gp`, matching every other cost-bearing pick in the app rather than a free-grant exception), reusing `PurchaseEquipmentResponse`'s Purchased/Blocked outcome shape; plus a small DTO addition so the Gear tab can show what's already attached (not just raw ids). Deliberately bounded and flagged as such: no detach/remove affordance, flat catalog cost rather than modeling real PF1 enchantment-cost stacking. Correctly holding off on writing UI code until backend's command lands, per the no-stub discipline — a button wired to a nonexistent command would be exactly the kind of stub this doctrine forbids.

    **Two real gaps found before building, resolved peer-to-peer without escalating (backend+frontend, 2026-07-24).** Backend checked the proposal against real data before implementing: (1) real CRB weapon-enhancement records (+1 through +10) have no known flat `cost_gp` in the static table — real PF1 enhancement pricing is a formula (bonus² × 2000gp), not a flat catalog price, so mirroring `purchase_equipment`'s block-on-unknown-cost exactly would leave the headline use case (attaching an actual enhancement) permanently `Blocked`; (2) the bundled desktop demo corpus has zero Equipmods records at all, so an attached modifier would silently not affect the computed attack bonus unless the existing `unresolved_equipment_item_ids` tracking (item 1's shape-B fix) extends to cover `applied_modifiers` too. **Resolved between backend and frontend directly**: treat unknown cost as free-to-attach (an explicit, flagged deviation, not silent), and extend the unresolved-ids tracking — which the frontend's existing `UnresolvedNotice` component picks up with zero new frontend work. Both correctly judged this doesn't need lead/operator escalation — same shape as other bounded first-pass simplifications already made this session (explicitly flagged, not a claim-gating philosophy question), flagged to the lead for visibility rather than for a decision. Lead agrees with that read; logging for the record, not overriding.

    **Sub-task 6 backend half landed and lead-verified (`3a05539a`, 2026-07-24).** New `attach_equipment_modifier` Tauri command, mirroring `purchase_equipment_at_root`'s atomic resolve-cost → check-affordability → mutate → charge shape: validates `modifier_item_id` against the real catalog first (same check `append_to_character` already runs, rejecting a fabricated id before any cost/target check), treats an unknown `cost_gp` as free-to-attach per the agreed deviation. `ResolvedEquipment`/`ResolvedEquipmentDto` gained `applied_modifiers` (nested resolved records, reusing the existing DTO shape); `compute_pilot_with_corpus` resolves each selection's own modifiers the same way it resolves the selection itself, folding any unresolvable modifier into the existing `unresolved_equipment_item_ids` list. `rule_system_adapter.rs`'s `TestPf1Delegate` now reuses the shared mapper instead of hand-rolling a second mirror. **Real production bug found and fixed along the way, not introduced by this sub-task**: `SavedCharacterStore`'s save/load round-trip never had a writer for `applied_modifiers` — sub-task 1 only built the fixture-text *parser* side (for tests), never the production *writer* `character_hub.rs`'s real save path uses, so every attached modifier was silently dropped on save. Fixed in `local_store.rs` (validate + render), 2 new round-trip tests. 7 new desktop tests, 4 new lib tests, all genuine RED→GREEN. **Lead independently re-ran**: full workspace (4284/4284, matches exactly) and desktop suite (211/211, matches exactly). Frontend cleared to wire the Gear tab against the real command/DTO shape.

    **QA finding, landed (backend, 2026-07-24) — silent corpus-resolution drops now traceable, shape B of QA's two proposed fixes.** QA root-caused the Spells tab inconsistency to something bigger than the Spells tab: the desktop app's only bundled `corpus` (`corpus_fixtures.rs`) is a deliberately tiny ~4-record demo fixture (its own doc comment: "not a general corpus provider, exhaustive corpus coverage is out of scope") — any real, disk-persisted `spells_selected`/`equipment_selections` entry outside those exact records silently vanished from every corpus-derived output (`school_coverage`, `equipped_items`, and now shape (c)'s `equipment_effects`) with zero diagnostic, indistinguishable from "nothing selected." This also capped shape (c)'s real-world value: its ACP/AC looked complete only because the Fighter fixed loadout happens to use exactly the 2 bundled equipment records. **Decision (backend, greenlit as an engineering-scope call, "your read" per the lead):** shape B (an honest "unresolved" signal) over shape A (bundle more records) — bundling more records doesn't fix the bug, it just moves the boundary; the app structurally cannot ship the full ~652-spell/hundreds-of-equipment-record corpus, so any fixed bundle size is permanently, arbitrarily incomplete. `CorpusDerivedSection` gained `unresolved_spell_ids`/`unresolved_equipment_item_ids: Vec<String>` (`pilot_compute_corpus.rs`), populated wherever `spell_id_resolve`/`equipment_id_resolve` returns `None` instead of the previous silent `continue`, surfaced on `CorpusDerivedDto` for the frontend to render as an honest "not shown — outside demo corpus" indicator (UI treatment is frontend's call, not built here). Genuine RED→GREEN: 3 new `pilot_compute_corpus.rs` tests (an unresolvable equipment selection tracked, an unresolvable spell tracked, everything-resolves leaves both lists genuinely empty), both push calls disabled to confirm the exact expected failures before restoring; 1 new end-to-end `character_hub.rs` test against the real bundled corpus (not a hand-built fixture) confirming a real `add_equipment_selection` for an item outside the demo bundle surfaces correctly — which also surfaced a real, pre-existing, unrelated fact worth noting: the fixed Fighter loadout's own `item:shield`/`power_attack` placeholder entries were already silently unresolvable before this fix (not new, not a bug this introduces, just now visible instead of invisible). 245/245 lib, 203/203 desktop.

    **Follow-on bug found and fixed (QA finding + backend fix, 2026-07-24) — `EquipmentEffectsDto`'s three `Option` fields never actually hid on absence.** `maxDexCap`/`spellFailureChance`/`attackBonusDelta` lacked `#[serde(skip_serializing_if = "Option::is_none")]` (unlike the precedented `damage_reduction` field in the same file), so a Rust `None` serialized as `"maxDexCap":null` — key present, literal `null`, not an omitted key — defeating the frontend's `!== undefined` hide-checks (`null !== undefined` is `true`) and rendering garbled `"+null"`/`"null%"` strings. QA confirmed three independent ways (a throwaway serialization test, tracing the zero-transform `invoke()` call, running the actual JS template logic in Node) before flagging; the one character live-tested earlier (Chain Shirt + exactly one weapon) happened to resolve `Some(...)` for all three fields, so the `None`/hide path was never exercised until now. **Fixed (backend)**: added the same `skip_serializing_if` attribute to all three fields. Genuine RED→GREEN: a new permanent wire-serialization test (`equipment_effects_dto_omits_its_optional_fields_when_none_and_includes_them_when_some`) reproduced the exact bug (`"maxDexCap":null` etc.) with the attribute temporarily removed, then confirmed fixed after restoring. Frontend's TS type already declared these fields as optional (`maxDexCap?: number`, not `| null`), so no frontend change was needed — the wire shape now matches what frontend was always coded to expect. 204/204 desktop, frontend typecheck clean.

    **Independently verified, both structural claims confirmed, not trusted (QA, 2026-07-24).** Confirmed the `EquippedActive`-only filter is genuinely load-bearing, not vacuous — reproduced RED by replacing the filter with a pass-through, watched a `SelectedInactive` item's real `-2` ACCHECK leak through the test, restored. Confirmed the new field is genuinely inert with respect to claim-gating from three independent angles: structurally (`base` is computed via the unmodified `compute_pilot_base_chassis` first, with zero lines of that function touched in the diff), by call-order (`create_character_at_root`'s `Blocked` early-return happens before `compute_pilot_with_corpus` is ever called, same shape as item 7's wealth gate), and empirically (the new desktop test asserts `Saved`, not `Blocked`, on a build that also carries a real nonzero ACP). Also confirmed `arms_armor.rs`'s `None`-vs-`Some(0)` semantic distinction is preserved correctly (no token vs. a real zero-value token). Ran clean throughout, RED/GREEN reproductions restored byte-identical each time.
2. **RESOLVED — 0.6 version bump.** Landed as `0c614d9`: `buildVersionTriple.test.ts:44-47` anchor updated `0.5.` → `0.6.` — and its previously-unnoticed sibling `apps/desktop/src/sd21/buildVersionTriple.test.ts` (backend caught it proactively, same documented reason, flagged for visibility rather than silently expanding scope). Triggered one downstream pre-existing test (`buildLabelFixtureFreshness.test.ts`, expects literal "Codex 0.6.0-test" in 3 frontend-owned fixtures). **Confirmed resolved (lead, 2026-07-24):** full suite run (`node scripts/run-tests.mjs`) is 69/69 files passing including this one, with zero commits ever touching the 3 named fixture files — it was never actually broken by the version bump in practice.
3. **Three writers, one checkout.** No worktrees by design; commit serialization (§2) mitigates git-index and cargo target-dir races but depends on teammate discipline. First symptom to watch: a "committed" change absent from `git log` on `tranche/6`.

    **New hazard found and self-caught (backend, 2026-07-24) — unscoped `cargo fmt -p codex` silently reformats all 516 tracked files.** Full account under item 1's sub-task-1 landing note above. Standing lesson for this checkout: `cargo fmt -p codex` (no path scoping) is unsafe right now — it would silently rewrite every teammate's files, including in-progress uncommitted work, since the crate has drifted from the currently-installed `rustfmt`'s formatting. A per-file `rustfmt <path>` is safe.
4. **Unsigned Windows installers.** SmartScreen interposition is expected and accepted for alpha (bar item 1 qualifier). Code-signing remains out of scope.
5. **Observer lane may be dark.** As of 2026-07-23 no listener and no tick loop are running (§8.3.1 status check). If the operator does not bring it up pre-launch, the only visibility is `SWARM_STATUS.md` via `cat`.
6. **RESOLVED — false alarm, real infra bug: batched/delayed SendMessage delivery to backend.** Lead initially logged this as "backend not reliably stopping on instruction" after backend appeared to execute BAB/save stacking and equipment AC/carry-capacity work despite a reprioritization, a pause-and-explain, and a hard stop sent via SendMessage. **Correction:** none of those messages reached backend until long after the fact — backend's entire session shows zero inbound content until every message (routing fix, reprioritization, task-2 approval, pause, hard stop, escalation) arrived simultaneously in a single batch, well after task 5 (`d475097`) was already committed. Backend was, the whole time, faithfully and well executing its *original* wave-1 brief exactly as spawned (task 4 = BAB/save stacking, task 5 = equipment AC/carry-capacity, in that order) — including sound judgment calls (holding the commit for QA to sequence test updates, auditing equipment AC before assuming work was needed, scoping a 63-test-breaking overreach back down to 40). When the batch did land, backend correctly held at the *last* instruction ("wait for a message from me before doing anything at all") rather than self-authorizing off older content in the same batch that could have read as permission — exemplary handling of a confusing situation, not a discipline problem. Delivery was verified fixed with an explicit ack-the-literal-sentence test before backend was resumed. The commit (`d475097`) stands; it was legitimate work under a legitimate brief. Frontend and QA showed no equivalent delivery gap in the same window, so this looks scoped to backend's inbox/session specifically rather than a systemic swarm-messaging failure — still worth the operator/infra owner looking into if this recurs, since it cost real time and caused a false lead-side escalation.

## Open questions

1. **RESOLVED (autonomous, 2026-07-23) — Tranche-6 CI.** No tranche-specific workflow exists past `tranche-3-ci.yml` (none was authored for tranche/5 either). Decision: no new workflow needed — `publish-tester-release.yml` + the promotion gates are the established precedent (tranche/5-x never got its own CI either) and already exercise the desktop build/test/installer path. Revisit only if a real gap surfaces in practice.
2. **RESOLVED (autonomous, 2026-07-23) — Book-stub label reconciliation.** SD-26's `decisions.md §10` pins the literal `"SD-27"` as the stub resolution bundle while all 21 landed stubs carry `"SD-27+ (unscheduled)"`. Decision: out of scope for this swarm — it's an SD-26/SD-27 bundle-labeling question, not a v0.6 alpha content question, and the swarm's `data/stubs/` work doesn't depend on which label is "correct." Leave both as-is; whoever launches SD-27 reconciles it then.
3. **STANDING GUIDANCE (autonomous, 2026-07-23) — alpha-bar staging.** The bar is kept whole for now. Given the operator's autonomous-operation directive (top of this doc), the lead will keep pushing real, bounded backlog work (more class coverage, more parity tests, more UI completions) without narrowing the bar unilaterally — narrowing the bar itself is a strategic call reserved for the operator. If the swarm reaches a point where no further bounded, valuable work remains and the full bar is clearly out of reach, that finding gets written up plainly in this doc for the operator's eventual review — not decided silently.

    **Bounded-backlog assessment, written up as promised (lead, 2026-07-24).** As of this point all 26 numbered risk items are either resolved or correctly deferred; items 22-26 (the last wave — characterProgression coverage, LevelUpDialog's feat pick, the parity-comparator BAB dimension, the systemic frontend-test-coverage pattern across 5 modules, the Load-list refresh gap) all closed today, each with real fixes, real tests, and independent re-verification. Two consecutive backend self-directed scans (item 21's correctness-bug sweep, item 24's parity-comparator sweep) both came back clean on their second pass. A full consolidated workspace health check is clean. QA's completeness sweep worked through essentially every commit this swarm produced.

    **What's genuinely left, plainly stated:**
    - **Architecture-level, correctly deferred, not bounded work** (`future-epic-scoping.md` has the full detail): bridging the headless/corpus-aware wall for attack-bonus and skill-ACP widening; the feat-effects engine's total absence (every feat but 3 hardcoded ones does nothing mechanically); Wizard non-Human spell-math completeness. None of these are a same-session task — each is a real design/engineering effort of its own.
    - **Class/multiclass breadth**: only Fighter/Wizard/Rogue reach `Computed` at all; the other 8 CRB classes each need their own multi-cycle engine work (confirmed multiple times this session, not assumed).
    - **Operator-only, not an engineering call**: starting-wealth-by-class (item 7) — a content-provenance/licensing question.
    - **Observer-lane status** (item 5 under Risks) — outside the lead's control, operator-side infrastructure.

    **Not claiming the alpha bar is met** — it isn't, on breadth (2 of 4 books' worth of classes reachable) and on the three architecture gaps above. What this assessment says is narrower: the *bounded, same-session* backlog — bugs, wiring gaps, missing tests, missing UI surfaces for already-computed data — is genuinely exhausted right now, not abandoned early. Continuing to hold the autonomous-operation posture: teammates stay on independent verification, consistency maintenance, and readiness for either (a) the operator authorizing a run at one of the architecture-level items, or (b) anything new that surfaces from continued use/testing. Not manufacturing further busywork to appear active.
4. **RESOLVED — "durability" definition (§1 item 4).** QA flagged that the alpha bar's calculation list ("... AC, durability, carry capacity ...") is ambiguous between character survivability and item hardness/breakage; the codebase has no item-hardness/breakage mechanic anywhere. Ruling (lead, 2026-07-23): **durability = the character's displayed survivability stat — max/current HP, temporary HP, nonlethal damage tracking, and unconscious/dying/death thresholds** — distinct from "level-up hit points" (§1 item 4's separate entry), which is specifically the hit-die + Con-mod increment calculated during the level-up flow. This keeps the bar scoped to character-sheet mechanics already in the codebase's model and introduces no new game system. QA may proceed writing red-green tests against this definition. Follow-up (QA, same day): durability has almost no production surface either — only an isolated level-1 fighter HP value, no aggregate max/current/temp HP or dying-death fields anywhere — so this is a real wave-2 build item, not just a naming clarification.

    **Backend build landed, frontend never followed (backend's DTO-consumption sweep, 2026-07-24): full command/DTO shapes for whoever wires the UI, so this can be picked up cold.**

    - **`load_character_durability`** — request `{ characterId: string }` → `Result<CharacterDurabilityDto, string>`. Errs (not a `Blocked`-style payload, a rejected IPC call) when the saved build isn't single-class Fighter/Wizard/Rogue at a level `durability::compute_max_hp` recognizes — same scoping restriction the compute layer already has elsewhere, not new. A Durability tab needs to handle this the same way other tabs handle an unsupported-class build (a "not available for this build" fallback), not treat it as an unexpected failure.
    - **`adjust_character_hp`** — request `{ characterId: string, deltaHp?: number, deltaNonlethal?: number }` (both default 0) → same `CharacterDurabilityDto`, recomputed. **This is already atomic and delta-based, not two calls needing client-side coordination** — unlike the money/equipment-purchase coupling this doc flagged earlier, there's no read-then-compute-then-write race to design around: send `deltaHp: -8` to deal 8 lethal damage, `deltaNonlethal: +5` to add 5 nonlethal, and the server clamps (`current_hp` capped at `max_hp` on healing, `nonlethal_damage` floored at 0 on recovery) and reclassifies `status` in the same call. A "type an absolute new HP value" input just needs `deltaHp = newValue - currentHp` computed client-side before the one call — still one round trip, not two.
    - **`CharacterDurabilityDto`'s 4 fields, in PF1 terms**: `maxHp` — the character's ceiling, computed fresh from class hit die + Con modifier each load, never stored. `currentHp` — the live tracked value, persisted in a `hp.json` sidecar, defaults to `maxHp` the first time a character is loaded (no special "initialize" call needed). `nonlethalDamage` — PF1's separate nonlethal pool, persisted alongside `currentHp`; `currentHp` itself is never reduced by nonlethal damage. `status` — a pre-computed label, one of `Normal` / `Staggered` (`nonlethalDamage == currentHp`) / `Disabled` (`currentHp == 0`) / `Unconscious` (`nonlethalDamage > currentHp`) / `Dying` (`currentHp < 0`) / `Dead` (`currentHp <= -constitutionScore`) — a minimal UI can render this string directly (e.g. as a badge/color) rather than re-deriving thresholds client-side.
    - **What doesn't exist yet, flagged so it isn't assumed**: no temporary HP field (already noted above as explicitly out of scope for this wave); no dedicated "heal to full"/"stabilize" convenience call (achievable client-side as `deltaHp: maxHp - currentHp`, just not a named single-purpose command); no death-save/stabilization mechanic beyond the `Dying`/`Dead` labels themselves (labels only, no dice-roll flow) — consistent with the alpha bar not naming that affordance.
    - **Net for whoever builds this**: the backend side is a clean, already-atomic read/write pair with no coupling hazard to design around — the missing piece is purely a new HP display + a damage/heal input calling `adjust_character_hp`, no new backend work anticipated for a minimal version.

    **Status thresholds verified against real PF1 rules, not just internal consistency (backend, 2026-07-24).** `classify_durability`'s exact formula (`durability.rs:99-113`) checked against the real d20/PF1 "Injury and Death" and "Nonlethal Damage" rules: Dead (`current_hp <= -constitution_score`, correctly the Constitution *score* not modifier), Dying (`current_hp < 0`), Disabled (`current_hp == 0`), Unconscious (`nonlethal_damage > current_hp`), Staggered (`nonlethal_damage == current_hp`), Normal (fallthrough) — all 6 match. One precedence question confirmed deliberate: at `current_hp == 0` with `nonlethal_damage == 0`, Disabled and Staggered are both literally true, but the `if/else if` order checks Disabled first — correct, matches the real ruling that 0-hp-Disabled is the more specific controlling rule. **Sourcing caveat stated plainly**: no local PCGen corpus source contains this rule text (checked; PCGen's data only references these conditions as effects *caused by* specific abilities, not the core rules defining the thresholds) — verified against the well-established, unchanged-since-3.5e d20 SRD Open Game Content text instead, same sourcing category as `money.rs`'s already-flagged pp/gp/sp/cp ratio. Clean result, no mismatch — safe for frontend to render these labels as ground truth.

    **Backend half landed, frontend half never started (backend, 2026-07-24, DTO-consumption sweep).** `CharacterDurabilityDto` (`maxHp`/`currentHp`/`nonlethalDamage`/`status`) and its two Tauri commands (`load_character_durability`, `adjust_character_hp`) are real, registered, computed, and fully tested backend-side — but frontend has zero `invoke()` calls for either command and zero components/tabs mentioning "durability" anywhere (case-insensitive grep, zero hits). `CreateCharacterForm.tsx`'s own `maxHp` is a separately-computed client-side local value, not a read of the backend DTO. Matches the untracked/unattributed `SWARM_TASKS.md`'s durability row still showing "queued" (that file's overall claims are not trusted, per the earlier flag, but this one line happens to be independently confirmed true). This is real remaining bar-item-4 distance: not an architecture gap, a genuine missing UI surface (HP/dying-death display, damage tracking) — bounded, frontend-shaped work whenever dispatched.

    **RESOLVED — frontend built the Durability/HP tab (`75b083bd`).** New `characterDurability.ts` boundary wraps both commands exactly per the scoping above; `CharacterSheet.tsx` gained durability state, a loader effect (mirrors the money pattern), `handleAdjustHp`, and a new `DurabilityPanel` inside the Defense tab — current/max HP, nonlethal damage, an emphasized status badge when not `Normal`, and Damage/Heal buttons styled like `MoneyPanel`'s Add/Spend. Falls back to an honest "not available for this build" line when durability is `null` (covers both in-flight loading and a genuinely unsupported build, same treatment as other unreachable-build cases). **Live-verified against the real dev build, both directions**: a supported Dwarf Rogue 2 went 19/19 → damaged to 12/19 (disk-confirmed) → healed to 15/19 → damaged into negative HP, status correctly flipped to "Dying" and rendered emphasized, then healed back to exactly 19 (clamped at max, no overshoot). An unsupported Wizard/Rogue multiclass correctly showed the fallback line, no crash, and confirmed no stray `hp.json` was written for it. No new test file (UI-only, no new pure logic beyond what the live drive already covered); 63/63 existing suite still green, typecheck clean.
5. **RESOLVED (autonomous, 2026-07-23) — Pets/animal-companion gap vs. alpha bar item 2.** Frontend's stub-tab audit confirmed Pets (animal companions) is a genuine pre-existing non-goal — `pilot_compute.rs` marks it "named-but-unproven," no stat-block engine exists at all. This predates the swarm. Ruling adopted as final: **not a blocker** — bar items 2-4 don't explicitly name companion management as a required affordance, so Druid/Ranger/Hunter/Cavalier remain creatable/advanceable/calculable per the bar's literal text, just without a working companion. A real companion engine is large (no stat-block system exists at all) and out of scope for this swarm; logged here as a genuine future feature, not attempted. Final attestation should note this explicitly as a known, accepted limitation rather than silently omit it.
6. **RESOLVED — damage reduction exposed through the DTO, and now actually rendered.** Landed (`f7ce289d`): `pilot_compute.rs`'s existing per-class DR explanations are now exposed through the DTO layer. **Frontend follow-through landed (`26ac0704`)**: the Defense tab (previously the literal "coming soon" stub) now renders "Damage Reduction: X/—" when present, added `damageReduction` to the shared `PilotSnapshotDto` TS type (was entirely missing there too — the DTO-consumption sweep that found this gap was right). Absent case live-verified honestly across all 3 chassis-supported classes (Fighter/Wizard/Rogue, including a multiclass) — `undefined`, not a fabricated zero, correct stub text still shown. Populated case **cannot** currently be live-verified: Barbarian is the only DR-granting class and it's `human-diagnostics-only` (always `Blocked`), so no live UI path today ever produces a saved character with a real non-zero value — a structural fact, not a testing gap, noted in the code rather than glossed over. One honest note left in-code at the time, initially unfixed: the "Recompute" action's own DTO didn't carry `damageReduction`, so it wouldn't have refreshed if a real value ever became reachable later — **closed the same day, see the `af2d8b9f` entry immediately below**, not left standing. Not part of the alpha bar's explicit calc list (item 4 doesn't name DR), but a real completeness improvement landed opportunistically, now genuinely end-to-end.

    **Recompute-DTO parity gap closed (backend, `af2d8b9f`).** Found the DTO under its real Rust name (`CharacterSnapshotDto` in `recomputeCharacter.rs`, backing `recompute_character`) and added `damage_reduction: Option<i16>` with the identical serde shape `PilotSnapshotDto` already uses, populated from the same source field (`PilotDefenseViewModel::damage_reduction`) at the one construction site. Since no live pipeline can exercise a real non-`None` value (Barbarian never reaches `Computed`, same structural fact as above), added the meaningful test that *is* available instead: a direct serialization test proving `Some(3)` emits the camelCase `"damageReduction"` key and `None` omits it entirely (not `null`), plus a golden-path assertion that the existing Fighter fixture correctly stays `None` — same wire-shape rigor as the earlier `corpusDerived` regression tests, without needing a real compute pass to get it. 2 new tests, full suite green, zero blast radius.

    **Independently re-verified, not just self-reported (QA, 2026-07-24).** Read the actual diff and code rather than trusting the commit message: (1) confirmed `damageReduction?: number` is genuinely on the shared `PilotSnapshotDto` interface every consumer imports, not used ad-hoc; (2) read `DefenseTab`'s JSX directly — `!== undefined` guards the rendered line, falling through to the stub text on absence, with `snapshot?.damageReduction` correctly propagating `undefined` all the way from a `Blocked`/`None` snapshot; (3) traced Barbarian's unreachability in the compute layer itself rather than trusting the claim — `table_class_id` (used by both the single-class dispatch and `is_supported_multiclass_mix`'s per-class check) has no Barbarian entry, so no single-class or multiclass Barbarian-containing build can produce anything but `class_chassis.unsupported` → `Blocked` → `snapshot: None`. All three claims hold completely. Clean, independently confirmed record.
7. **RESOLVED (2026-07-24) — starting wealth by class, operator-provided with citation.** Was: exhaustively searched and correctly left unresolved (QA/backend found no citable value in any local corpus source, correctly declined the file-shape-stub trap in `docs/release/SD-22/artifacts/corpus/{acg,apg}/class_*.lst.md`, correctly declined to implement from unverified memory) — a genuine content-provenance/licensing question, not an engineering call, deliberately kept out of the autonomous-operation decision set.

    **Operator (Todd Hintzmann) provided the full table directly, sourced from d20pfsrd.com's "Character Creation" page** (`https://www.d20pfsrd.com/basics-ability-scores/character-creation/`, Pathfinder SRD / OGL content):

    | Class | Starting Wealth | Average |
    |---|---|---|
    | Alchemist | 3d6 × 10 gp | 105 gp |
    | Barbarian | 3d6 × 10 gp | 105 gp |
    | Bard | 3d6 × 10 gp | 105 gp |
    | Cavalier | 5d6 × 10 gp | 175 gp |
    | Cleric | 4d6 × 10 gp | 140 gp |
    | Druid | 2d6 × 10 gp | 70 gp |
    | Fighter | 5d6 × 10 gp | 175 gp |
    | Gunslinger | 5d6 × 10 gp | 175 gp |
    | Inquisitor | 4d6 × 10 gp | 140 gp |
    | Magus | 4d6 × 10 gp | 140 gp |
    | Monk | 1d6 × 10 gp | 35 gp |
    | Ninja | 4d6 × 10 gp | 140 gp |
    | Oracle | 3d6 × 10 gp | 105 gp |
    | Paladin | 5d6 × 10 gp | 175 gp |
    | Ranger | 5d6 × 10 gp | 175 gp |
    | Rogue | 4d6 × 10 gp | 140 gp |
    | Samurai | 3d6 × 10 gp | 105 gp |
    | Sorcerer | 2d6 × 10 gp | 70 gp |
    | Summoner | 2d6 × 10 gp | 70 gp |
    | Witch | 3d6 × 10 gp | 105 gp |
    | Wizard | 2d6 × 10 gp | 70 gp |

    Plus the companion **Character Wealth by Level** table (for characters created above 1st level — PC level 2 through 20, 1,000 gp through 880,000 gp), same source. Worth noting: the Alchemist value (3d6×10) exactly matches what the earlier file-shape stub in `docs/release/SD-22/artifacts/corpus/apg/class_alchemist.lst.md` showed — that stub's *value* was apparently correct, it was the *provenance* (an unlicensed placeholder file, not a real source) that made it unusable at the time; this operator-cited value is independently sourced and now usable.

    **Implemented (backend, 2026-07-24).** `money::starting_wealth_gp` (`src/rules_core/money.rs`) returns the operator-cited average gp value for any of the 11 PF1 Core Rulebook classes `ClassId` models (all 11 rows populated, not just Fighter/Wizard/Rogue — mirrors `class_tables()`'s own "complete table, narrower dispatch" precedent, since a class outside the 3 that currently reach `Computed` never reaches the wealth-granting code path anyway). The 12 non-CRB classes in the operator's table (Alchemist, Cavalier, Gunslinger, Inquisitor, Magus, Ninja, Oracle, Samurai, Summoner, Witch) are deliberately omitted — no `class:<name>` id for any of them is recognized anywhere in this crate yet, so their rows would be inert data with nothing to key against. **Deterministic by design, not a rolled value**: PF1's own rule is to roll the die (e.g. Fighter 5d6×10), but this crate has no random-number generator anywhere and is deterministic throughout (every computed value is exactly reproducible from its inputs) — rolling would be a genuine architecture departure, not a drop-in fix. Uses the operator's own printed "Average" column directly (a citation, not a re-derived computation); a future "reroll" UI affordance, if ever wanted, is a separate frontend feature layered on top of this default. `create_character` (refactored into a testable `create_character_at_root` split, mirroring every other mutation command's own `_at_root` precedent) now initializes the new character's `money.json` balance via the existing `adjust_character_money_at_root` sidecar mechanism (item 9's own infrastructure, reused rather than duplicated) immediately after the build reaches `Computed` and saves — atomic with creation, no separate call for the caller to remember. A `Blocked` build is granted no wealth, fabricated or otherwise (verified by test). 3 new desktop-crate tests (Fighter/Wizard/Rogue's distinct correct balances, and a Blocked-build zero-grant proof) plus 5 new lib-crate tests on `starting_wealth_gp` itself (the 3 reachable classes, the other 8 CRB classes, an independent dice-count×35 arithmetic cross-check, and unrecognized-class `None` handling). Genuine RED→GREEN: the wealth-granting call was temporarily disabled, the two positive tests failed with the exact expected 0-vs-17500/7000 mismatch, then restored and confirmed green. 228/228 lib, 200/200 desktop.

    **Independently re-verified, clean (QA, 2026-07-24).** Hand-cross-checked all 11 CRB rows against `starting_wealth_gp`'s match statement (Barbarian 105, Bard 105, Cleric 140, Druid 70, Fighter 175, Monk 35, Paladin 175, Ranger 175, Rogue 140, Sorcerer 70, Wizard 70 — all exact) against both the citation and independent PF1 knowledge, not just the citation re-read. Traced the `Blocked`-gets-no-wealth claim directly in `create_character_at_root`: the early `Blocked` return happens structurally before the wealth-granting call, not just an empty-valued grant — and noted the test proving this deliberately uses Cleric (a class `starting_wealth_gp` *does* recognize) to rule out a false-positive where the gate is secretly keyed on `None` rather than `receipt.status`. Reproduced RED themselves (disabled the grant, watched the exact `0` vs `17500`/`7000` failure, restored). Full `cargo test --workspace` regression clean, no failures anywhere.
8. **RESOLVED — Feats tab now shows a character's existing feat list.** `chosen.selected_feats` exposed through `load_saved_character`'s response (backend, `1509124`), wired into the Feats tab (frontend, `aa611ce`) — live-verified: Aldric's Feats tab correctly showed all 5 feats (3 fixed-loadout + Cleave + newly-added Toughness), revision bumped on the new add.

    **Independently verified, both commits together (QA, 2026-07-24).** `febf4d80` (Feat picker)'s own "can't show the complete list" caveat was accurate when written, and its real follow-up `aa611ce1` genuinely closes it — traced all 6 `toCharacterMutationRefresh` call sites in `CharacterSheet.tsx` individually, confirmed each correctly threads the unchanged feat list for non-feat mutations and the two feat-adding paths correctly append rather than fabricate, mirroring the real server-side behavior. Both relevant test files re-run, pass. Noted as a positive contrast worth calling out: unlike item 23's stale LevelUpDialog comment, this caveat was properly retired by a real follow-up commit rather than left to rot.
9. **RESOLVED — Money panel now coupled to equipment purchases.** Landed (`29e67515`): new atomic `purchase_equipment` command resolves real catalog cost headlessly (no corpus wall), pre-checks affordability, adds the equipment and deducts its cost together in one mutation, honestly `Blocked` (never free, nothing partially applied) if unaffordable or cost unknown. Closes the gap flagged against frontend's `59d5bc0` money panel — buying a weapon/armor now actually spends money.

    **Independently verified, static leg clean, live-UI leg inconclusive on an environment quirk (QA, 2026-07-24).** Confirmed `characterMoney.ts`'s `gpToCopper` (`Math.round(valueInGp * 100)`) mirrors `money.rs`'s `gp_to_copper` exactly (same `COPPER_PER_GOLD = 100` ratio, same rounding), and the boundary wiring (`loadCharacterMoney`/`adjustCharacterMoney`) correctly calls the matching Rust commands. Attempted the live-UI leg with `RUN_DESKTOP_AGENT=qa` — confirmed the driver.sh fix works as intended (frontend's concurrent session on `:96` completely unaffected by QA's on `:98`, no collision) — but hit a real, separate environment quirk: this session's window reports `1920x1200` via `xdotool getwindowgeometry` but genuinely renders/screenshots at `1280x900`, and the Load Character dialog's action-button row doesn't appear where expected in that smaller canvas, blocking the path to the Money panel. Not a code bug — stopped cleanly, no stray processes, frontend's session confirmed untouched throughout. Called clean on the strength of the static verification (exact formula match, direct source read of the wiring) rather than blocked; live-UI leg left inconclusive, not failed.
9a. **Backend field landed, frontend consumption never followed through.** Landed (`034d1b84`), same shape of fix as item 6 (DR) and item 8 (feats) — intended to unblock frontend's earlier `record_and_prepare_spell_selection`-always workaround so the UI could detect "is this the character's first spell" directly. **DTO-consumption sweep (backend, 2026-07-24) found zero frontend reads of `spellsSelected` anywhere** — `CharacterSheet.tsx`'s `handleAddSpell` still unconditionally routes every Wizard spell pick through `recordAndPrepareSpellSelection`, never checking the loaded count to use the cheaper `addSpellSelection` path once the character already has ≥1 spell. Not a correctness bug (repeated record-and-prepare calls are additive/safe), but the field the backend fix enabled was never actually wired up — small, bounded frontend follow-up whenever dispatched.
10. **RESOLVED, LIVE-VERIFIED (2026-07-23) — Wizard is a genuinely playable second class now.** Frontend live-tested backend's Wizard fix (3484b5d) and found two further, independent gaps underneath it:
    - **RESOLVED — level-up path gap.** `apply_level_up` seeded zero choices for any class — only `compose_character_input` (character creation) was touched by 3484b5d, so multiclassing Wizard onto an existing character still hit the OLD diagnostic. Fixed in `1aabdf8`: seeds the same canonical choices in the new-class-entry branch only, guaranteed to fire exactly once per character (verified with a dedicated test — `wizard_has_canonical_specialization_selections` requires exactly 2 opposed-school entries, so a re-seed on a later level-up would break it, and doesn't).
    - **RESOLVED — bootstrap deadlock (the real bottleneck).** `create_character` only ever persists a proven-`Computed` build, so a fresh Wizard (empty spellbook) could never be saved in the first place. The Add Spell picker couldn't bootstrap around this either: `unmet_wizard_spellbook_conditions` requires one spell recorded as **both** `AcquisitionMode::Known` and `AcquisitionMode::Prepared`, but `add_spell_selection` only sets one mode per call, and `mutate_saved_character_at_root` discards (never persists) any call that doesn't independently reach `Computed` — so a Known-only call was discarded, a Prepared-only call was discarded, neither built toward the other. Backend's own earlier passing test only reached `Computed` by constructing `CharacterInput` directly in Rust, bypassing the command layer — real proof the compute logic was correct, not proof the UI could produce this state.

      **Root cause confirmed + fix landed for the second-call problem (backend, 2026-07-23).** Backend independently re-derived the same root cause before reading the framing above (verification, not agreement-seeking) and confirmed the scope precisely: purely a bootstrap/chicken-and-egg problem for the *first* spell — once a character has ≥1 Known and ≥1 Prepared entry, subsequent single-mode `add_spell_selection` calls already work fine, so `add_spell_selection` itself needed no redesign. Landed as `record_and_prepare_spell_selection` (`6e12437`) — a new Tauri command whose mutation closure pushes both a Known and a Prepared `SpellSelection` entry (same spell) in one `mutate_saved_character_at_root` call, atomically satisfying both conditions on the first call, same additive-command shape as every other `add_*_selection` command, no changes to `add_spell_selection` or the never-persist-unproven invariant. Scoped Wizard-only, shape generalizes without rework if another prepared caster gets a similar gate later. **This command is real and correct** — it just turned out not to be sufficient on its own; see the next sub-item.
    - **STILL OPEN, discovered after the above landed — a character holding a Wizard class level can never get saved in the first place.** Frontend live-tested `record_and_prepare_spell_selection` against a real build and found it still `Blocked`, on the new-and-correct "no wizard spells recorded/prepared" diagnostic (confirming 3484b5d's school-choice fix genuinely works) — never persisted, both via fresh creation and via leveling an existing character into Wizard as a third class. Read `record_and_prepare_spell_selection_at_root` directly: like every mutation command, it operates via load-existing-saved-character → mutate → recompute → save-if-`Computed`. It **requires a character that is already saved while holding a Wizard class level**. But `create_character` and `apply_level_up` both discard (never persist) a build that isn't independently `Computed`, and "Wizard class level + zero spells" is never `Computed` — so nothing in the current command surface can ever produce that starting state. This is a distinct, one-level-deeper chicken-and-egg from the one already fixed: class-acquisition and first-spell-acquisition are each independently gated, and neither can happen before the other. Backend's own earlier passing test proves the compute layer can represent this state (it saves a `SavedCharacterEnvelope` directly via `SavedCharacterStore::save`, bypassing the `Computed` gate — a test-only shortcut, not something the UI can do) — same "compute-correct vs. UI-reachable" distinction that's recurred at every layer of this investigation. Plausible direction floated to backend (not mandated): have class-acquisition itself (`compose_character_input` for creation, and `apply_level_up`'s new-class-entry branch) atomically seed one canonical starter spell as both Known and Prepared in the same mutation that adds the class level and school choices — mirrors the exact pattern already used for the school-specialization seeding. Backend investigating and proposing before any code, same diligence as every prior layer. **Net effect, unchanged from before this whole sub-thread**: bar item 3's "select spells at each spell-gaining level" remains untestable for Wizard. Frontend's UI routing change (Wizard spell picks always via `record_and_prepare_spell_selection`) is still correct, necessary work and was committed regardless — it's just not sufficient by itself.
    - **Minor, not urgent** — `apply_record_and_prepare_spell_selection` doesn't validate `source_class_id` against classes the character actually holds. Not a real discoverable tester workflow either way (flagged by frontend as a test-fixture curiosity only, explicitly not recommended as something to build UI around). Backend's call whether to fold into whatever it builds next or leave as backlog.
    - **RESOLVED, live-verified (backend `b2a5eb6`, frontend verification 2026-07-23).** `compose_character_input` and `apply_level_up`'s new-class-entry branch now both seed one canonical starter spell (`evocation.0.light`, Known+Prepared, reusing an already-proven-safe spell_id rather than a new literal) alongside the school-specialization choices — verified against `unmet_wizard_spellbook_conditions`'s own budget math (1 consumed vs. 3 available cantrips, comfortably under budget at every supported level 1–3) and confirmed not to touch the separate `WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL=3` ceiling. Both existing deadlock-reproduction tests rewritten (not deleted) to explicitly clear `spells_selected` first, turned into standing regression guards for the mechanism; 3 new tests prove the actual fix. 187/187 desktop suite.

      **Live verification (frontend, full walkthrough with `b2a5eb6` pulled):** created "Elara Spellweaver" (Human Wizard) — creation form reported "computed and saved" for the first time in this investigation. Loaded the real sheet: Wizard 1, HP 8/8, a real Progression rail (Arcane bond/school/Cantrips/Scribe Scroll/Spells-per-day/Spellbook). Used the real Add Spell picker twice — Magic Missile (Evocation, rev.1→rev.2), then Alarm (Abjuration, rev.2→rev.3), both genuinely persisted. This closes the entire three-layer bootstrap investigation: class acquisition (`3484b5d`/`1aabdf8`), then first-spell bootstrap (`b2a5eb6`), all confirmed with real UI-driven persistence, not just compute-layer proof. **This is the first point in the swarm where a second class (Wizard, alongside Fighter) is genuinely creatable/loadable/level-up-able end-to-end through the shipped UI** — the "second class reaches Computed" claim struck twice earlier in this doc is now actually true for class acquisition and the first spell. **CORRECTION (see item 13): the "slot math summed to exactly 2/2, honestly enforced" claim originally recorded here was wrong** — it was a coincidence of the UI's own client-side arithmetic, not server-side enforcement; the actual budget check never runs against real spells at all, see below.

      **RESOLVED (2026-07-24) — render-staleness bug.** Right after a spell-add mutation, the Spells tab's corpus-derived reachability list briefly still showed "No corpus-reachable spells selected yet" even though the mutation succeeded — closing and reopening the character showed it correctly. Not Wizard-specific: same shared mutation pipeline every Add Weapon/Add Armor/Add Spell action uses. Root cause: `character_hub.rs`'s `CreateCharacterResponse::Saved` variant has `#[serde(tag = "kind")]` with no `rename_all`, so `corpus_derived` serialized literally as `"corpus_derived"` on the wire while the TS boundary's asserted-not-validated type declared `corpusDerived` — silently `undefined` at runtime. `summary`/`snapshot`/`diagnostics` all happen to have no underscores, which is exactly why every other post-mutation update looked correct and only corpus-derived data looked stale. **Fixed (backend, `498679d1`) with the correct targeted approach, avoiding the tag-renaming trap**: a per-field `#[serde(rename = "corpusDerived")]` on just the one field with an underscore, not an enum-wide attribute. Backend also caught and fixed an identical latent bug it had introduced earlier the same session in `PurchaseEquipmentResponse` (same pattern, before this bug class was known) before it became a second incident, and proactively swept the rest of the desktop crate's `#[serde(tag = ...)]` enums — none of the others needed a change (already `kebab-case` by deliberate design, or no underscored fields). **Genuine RED→GREEN**: 2 new tests serialize a real response with `serde_json` and assert on the actual JSON keys directly — the only test shape that can catch this bug class, since a test on Rust struct field values alone is blind to it. Backend reverted the fix, watched both tests fail with the exact snake_case symptom, then restored and confirmed green. 196/196 desktop suite.

      **Independently re-verified end to end, not just the diff (QA, completeness sweep, 2026-07-24).** Ran the 2 tests directly (pass). Reproduced RED themselves — removed the `#[serde(rename = "corpusDerived")]` attribute, reran, watched the exact reported symptom (literal snake_case `corpus_derived` while every other field stayed correctly camelCase, the `"Saved"` tag untouched) — restored, confirmed GREEN again. Confirmed the TS side genuinely expects `corpusDerived` (`loadCreateCharacter.ts:102`), closing the loop end to end. Independently checked the "swept the rest of the crate" claim rather than trusting it: grepped for every `#[serde(tag = ...)]` enum in the crate — exactly 4 exist, the 2 already fixed plus `Ge08BaselineArmorClass` (no underscored fields, genuinely unaffected) and `IssueUrlError` (uniform `kebab-case`, its own doc comment confirms the React consumer matches kebab-case throughout, not a camelCase boundary — also genuinely unaffected). Clean, fully independently confirmed.
11. **RESOLVED (2026-07-24) — a real, PCGen-loadable Wizard `.pcg` fixture now exists.** QA originally looked for one to mirror `sd26_pilot_case_verification.rs`'s Fighter pilot case and found none accessible (the one Wizard `.pcg` that exists, `characters/SpecialWizard.pcg`, is `GAMEMODE:3e`, wrong ruleset). Hand-authoring from scratch looked multi-cycle-shaped and was correctly not attempted speculatively; QA's later scoping pass found real syntax templates for every section needed and revised the estimate to one focused cycle. **Backend built it for real**: verified by actually running it through the real headless Gradle PCGen pipeline twice, not just plausible-looking syntax. Resolved the one flagged unknown (whether `PROHIBITED:` takes a comma-separated list in Pathfinder mode for PF1's 2 opposed schools) by confirming against a real bundled fixture. Caught and fixed a genuine authoring bug of its own along the way (pre-adjusted ability score double-counting with PCGen's bonus-pool mechanism) by diffing against the proven Fighter fixture rather than guessing. **Result: 8 of 9 core dimensions match Codex's compute engine exactly** — including real external validation of the class-skill-modifier fix from item 14 (Climb/Swim now correctly 3 against real PCGen output, not the old buggy 5, independent confirmation beyond QA's earlier corpus-citation check). The one remaining mismatch is the same pre-existing, already-documented, out-of-scope Weapon-Focus attack-bonus discrepancy the Fighter pilot case already has — not new. Fixture at `programs/codex/requirements/GE-05-oracle-validation-and-parity-harness/artifacts/pf1-crb-human-wizard-level1-v06-alpha-swarm.pcg`, handed to QA to build the actual comparator test.

    **Scoping pass, not a build (QA, 2026-07-23).** Revisited under the autonomous-operation directive to scope (not attempt) the task properly. Real finding, better than the original framing: `.pcg` is plain-text and line-oriented, not binary — genuinely readable/authorable. Found working templates for every section a Wizard fixture needs: the Fighter fixture for the class/skill/feat/equipment shape, `code/testsuite/PCGfiles/pf_Paladin.pcg` (Pathfinder-mode, prepared-caster) for the exact `SPELLBOOK`/`SPELLNAME` syntax, and `characters/SpecialWizard.pcg` for the `SUBCLASS`/`SPECIALTIES`/`PROHIBITED` arcane-school syntax. One real unknown flagged rather than assumed: that last template is `GAMEMODE:3e` where a specialist prohibits only 1 school, but PF1 requires 2 opposed schools — whether `PROHIBITED:` takes a comma-separated list in Pathfinder mode is unconfirmed, the first real step for whoever builds this. **Revised estimate: one focused cycle, not multiple**, now that the format is de-risked. Owner: backend or a dedicated future cycle — still not squeezed into this wave, but no longer assumed to be large.
12. **RESOLVED — Wizard has no spell save DC calculation.** While doing item 11's LST cross-check, QA found there was no `wizard_spell_save_dc` computation anywhere in `pilot_compute.rs` — Paladin/Ranger/Sorcerer/Bard all have the standard `10 + spell_level + ability_modifier` formula grounded (Cha/Wis/Cha/Cha respectively) with matching `tests/sd13_*_spell_save_dcs.rs` files; Wizard had neither. Confirmed via real PCGen data (`cr_classes.lst:281`, `SPELLSTAT:INT`) that the correct input is Intelligence, not Wisdom or Charisma like the other four. Not a bug — explicitly disclaimed as out-of-scope in `explain_wizard_level1_prepared_spell_baseline`'s original doc comment when that function was first written. Fixed (`3b39731`): `10 + spell_level + intelligence_modifier`, mirroring the exact existing pattern for the other 4 classes, 3 new tests, coordinated cleanly with QA on the one stale test it caused.
13. **RESOLVED, LIVE-VERIFIED (2026-07-23) — Wizard spell-slot budget enforcement never applied to any real spell.** Found by frontend while investigating item 10's cosmetic render-staleness finding, then verified live rather than left as a code-reading suspicion. Root cause in `pilot_compute.rs`'s `parse_wizard_spellbook_spell_id`: it expects the bespoke `<school>.<level>.<name>` format (matching the one seeded starter spell, `evocation.0.light`), but real corpus spell keys ("Magic Missile", "Alarm", "Grease") contain no dots at all — `spell_id.splitn(3, '.')` yields one segment, `level_token = parts.next()?` returns `None`, and the function returns `None` for every real spell. `unmet_wizard_spellbook_conditions`'s slot-consumption loop uses `.filter_map(parse_wizard_spellbook_spell_id)` to build the `consumed` sum, so every real-catalog spell is silently dropped before the over-budget check ever runs — the check can mathematically never fire against real spells, only the one bespoke-format seed spell is ever counted. **This means item 10's earlier "slot math summed to exactly 2/2, honestly enforced" observation was a coincidence of the UI's own client-side arithmetic (the picker showing "N of 2" locally), not a server-verified budget — corrected above.** Verified live: a Wizard at a 2-slot budget added two real spells (looked correct at "2/2" in the UI), then a third 1st-level spell (Grease) — expected `Blocked`, got silent acceptance and persistence (`rev.3`→`rev.4`, confirmed on disk). A Wizard can add unlimited real spells today with zero slot enforcement. Directly relevant to alpha bar item 4 ("spell slot allocation... matches PCGen") — this is a real gap, not cosmetic, and more significant than the render-staleness issue that led to finding it. Frontend correctly did not propose a fix (real ambiguity in the right repair shape — resolve real `spell_id`s against actual school/level data via the corpus, widen the parser, or something else backend would judge better — and it's backend's file).

    **Fix landed (backend, `365b3a1a`).** Confirmed this was NOT the same headless/corpus-aware architecture wall as AC/encumbrance — the real spell catalog (`SPELL_LIST`, 652 CRB records) is a plain `pub const` compiled directly into the binary, already headless-accessible with zero corpus threading, unlike equipment. `parse_wizard_spellbook_spell_id` now resolves real `SPELL_LIST` keys directly first, falling back to the old synthetic convention for zero blast radius on QA's 3 existing fixture files. `WIZARD_STARTER_SPELL_ID` upgraded from the synthetic placeholder to the real key `"Light"`, since real resolution now exists. **Genuine RED→GREEN verification, not just claimed**: backend temporarily disabled the fix, reran the new regression test, watched it fail with Grease silently accepted exactly as frontend reported, then restored the fix and confirmed it passes. New test reproduces frontend's exact scenario through the real command surface (not a parser-only unit test): Magic Missile + Alarm accepted at 2/2, Grease as a third prepared spell correctly `Blocked`, not persisted. 188/188 desktop, 197/197 lib.

    **Live re-verification (frontend, 2026-07-23).** Built a fresh Wizard ("Verify Slotbudget") rather than reuse the earlier "Elara Spellweaver" save, whose disk state still carries the pre-fix Grease artifact as stale data, not a live bug. Through the real Add Spell UI (full 652-spell catalog): Magic Missile accepted (rev.2, disk-confirmed) → Alarm accepted (rev.3, disk-confirmed, 2/2 real slots) → Grease as a third spell honestly `Blocked` with a real diagnostic ("spell level 1 over-prepared: 3 slots consumed... but only 2 available"), revision stayed at rev.3, no Grease entry on disk — mutation discarded, not silently persisted. Frontend verified both outcomes against disk rather than the screen, since the still-open render-staleness bug (below) makes accepted-vs-blocked look identical at a glance in the UI. Reproduces the exact original repro end-to-end, both sides disk-confirmed. This closes the entire Wizard investigation: class acquisition, first-spell bootstrap, and now slot-budget enforcement, all live-verified.

14. **RESOLVED (2026-07-24) — `compute_selected_skill_modifiers` applied the Climb/Intimidate/Swim class-skill bonus regardless of actual class.** Found by QA's systematic Fighter/Wizard/Rogue gap sweep (autonomous mode), verified concretely rather than reasoned abstractly. `CLASS_SKILL_BONUS` (+3) was written when this function only served Fighter (whose real PF1 class-skill list genuinely includes all three skills); the `Computed`/`Blocked` gate (`unmet_selected_skill_posture_conditions`) was later widened to any dispatch-supported class, but the +3 itself was never made class-aware. Rogue is coincidentally correct (its real class-skill list also includes all three). Wizard's real class-skill list includes none of them (Wizard: Appraise/Craft/Fly/Knowledge-all/Linguistics/Profession/Spellcraft) — a Wizard built with the deterministic Climb/Intimidate/Swim posture currently gets `climb = 5` (rank 1 + STR 3 + class-skill 3 − ACP 2); the real value should be `2` (no class-skill bonus). No claim-blocking diagnostic fires — this is a silently wrong number in the explanations list, not a caught/Blocked case, live right now for any Wizard (or Wizard-containing multiclass) that reaches this path. **Distinct from and more urgent than** the already-known, correctly-deferred "class-skill recognition is Fighter-only" finding in `skill_allocation.rs` (item 1's sizing discussion) — that one is medium-large and doesn't gate anything reaching `Computed` today; this one does, and is wrong today, not theoretical. QA correctly filed rather than fixed (production code, not their lane). Backend redirected here as top priority, ahead of the render-staleness investigation (item 10's render-staleness sub-item — reopened, see there, not actually eliminated).

    **Fix landed (backend, `93a0636d`), verified against the real PCGen corpus before writing code**, not memory: checked `cr_abilities_class.lst` directly and confirmed Fighter (`:2835`) and Rogue (`:2838`) both have Climb/Intimidate/Swim as real class skills, Wizard (`:2565`) has none of the three — resolvable entirely with data already available headlessly, no corpus wall this time. `compute_selected_skill_modifiers` now only applies the +3 when the character has at least one class (of the two, Fighter or Rogue, that actually grants it) that includes the skill; multiclass gets it correctly via PF1's real union rule (a Fighter/Wizard multiclass still gets the bonus via the Fighter side — explicitly tested, not assumed). 4 new inline tests. One real test break found and coordinated with QA before pushing: `tests/sd21_wizard_chassis_computes.rs`'s `wizard_level3_with_ge06_combat_posture_clears_the_selected_skill_diagnostic` asserted the old buggy `climb = 1`; correct value for that fixture's ability scores is `-2`.

    **Two smaller findings from the same sweep, logged, not actioned:** level-up hit points has an inconsistent exposure pattern (Fighter has a dedicated `class_chassis.fighter.level_1_hit_points` explanation + catalogue test; Wizard/Rogue don't have an equivalent per-class explanation, even though `durability.rs`'s `compute_max_hp` already computes the mathematically-identical, PCGen-cross-verified value for all three) — not a missing calculation, likely not worth dedicated work. Bonus spells from high ability score, including the Wizard Intelligence-based bonus-spells record, confirmed present and correctly wired — no gap.

    **Follow-up sweep (QA, same day): the adjacent narrow-posture-gate functions came back clean.** Checked `unmet_combat_posture_conditions`'s Fighter bonus-feat sub-check (correctly guarded by `supported_fighter_level(input).is_some()`, genuinely skipped for non-Fighter) and `compute_combat_baseline`'s Weapon/Armor Training bonuses (default to level=1 for non-Fighter via `.unwrap_or(1)`, correctly producing a 0 bonus — semantically right, not a bug despite the implicit fallback). Closes out the "does this bug pattern recur nearby" question with certainty rather than leaving it assumed.

15. **RESOLVED — class-dropdown honesty fix, plus a real positive surprise: Wizard and Rogue actually support any race, not just Human.** Frontend's class-dropdown audit (autonomous mode) found the create-character dropdown's "(Human only, partial)" label was actively false for 8 of 11 CRB classes (Paladin/Ranger/Sorcerer/Bard/Barbarian/Monk/Cleric/Druid): each has its own "does not compute a supported chassis" doc comment in `pilot_compute.rs`, and live-testing (fresh Human Barbarian) confirmed a real named `Blocked` diagnostic, never `Saved`, for **any** race including Human — the non-Human fallback text ("Pick Human to see what's computed") was false for these 8; Human only gets nicer-named diagnostics instead of 4 generic ones, never an actual computed build. Fixed with a new `ClassSupportLevel` variant (`human-diagnostics-only`, distinct from the genuinely-working `partial-human-only`) and honest dropdown/description/fallback text. Not a stub or silent failure — the `Blocked` path itself was always correctly detailed, only the labeling overclaimed. Pushed `34635157`, 62/62 suite green.

    **Bonus finding, opposite direction — real, free alpha-bar progress.** Wizard and Rogue, both labeled `partial-human-only`, turn out to be mislabeled too: confirmed via git archaeology (`3484b5d`, the Wizard fix's original commit, never had a Human condition) and live-verified (fresh Elf Wizard 1 and fresh Elf Rogue 1 both reached `Computed`/`Saved` through the real creation UI, disk-confirmed with `race_id=race:elf` and correct distinct stat blocks) that neither ever had a race restriction — `supported_wizard_level`/`supported_rogue_level` and every downstream gate check class_id and level only, never race. Reclassified both to `full`, same as Fighter. This means bar item 2 ("any class or race from the four primary books") is materially closer than believed for the three working classes — the "Human only" framing had been a stale, never-verified assumption carried forward since the original investigations, not a real constraint.

    **Confirmed through level-up too, not just creation (frontend, follow-up).** Leveled both the Elf Rogue and Elf Wizard from creation up one level via the real `LevelUpDialog`: Elf Rogue 1→2 (HP 10→17, BAB +0→+1, Reflex +4→+5) and Elf Wizard 1→2 (HP 8→14, BAB +0→+1, Will +3→+4), both disk-confirmed with correct progression and `race_id=race:elf` retained. The `full` race-support claim holds through the level-up path, not just a creation-time fluke.

    **Independently re-derived, all 11 rows, not just the two already touched (QA, 2026-07-24, completeness sweep).** Read every classification directly against `pilot_compute.rs` rather than trusting the commit: Fighter/Wizard/Rogue's `supported_X_level` functions check only `class_id` and level, confirmed no `race_id` reference in either — genuinely `full`. All 8 `human-diagnostics-only` classes (Paladin, Ranger, Sorcerer, Bard, Barbarian, Cleric, Druid, Monk) individually checked, not spot-checked — every one has the identical `supported_X_level` gate plus a `race_id != HUMAN_RACE_ID` early return, and none appear in `table_class_id`'s exact 3-class allowlist, which is what guarantees `Blocked` regardless of race for all 8, Human included. `describeClassSupportLevel`'s description strings don't overclaim anywhere. `characterHubModel.test.ts` re-ran clean. Clean, independently confirmed record for all 11 classes, not just the ones this doc had already touched.

16. **RESOLVED (2026-07-24) — 4 of 7 grounded races were missing their real third ability-score adjustment.** Elf fixed first (`9ec0e036`), then Dwarf/Gnome/Halfling + the size-explanation text fix landed together (`2f05dee4`) — backend independently re-verified all 3 remaining races against the real PCGen corpus rather than trusting QA's citations (confirmed Dwarf +2 Wis, Gnome +2 Cha, Halfling +2 Cha, all matching exactly), correctly declined to wire size into AC/attack (real future scope, not folded in), and applied the shared-checkout commit-serialization mitigation (checked `git diff --cached --stat` before committing) so no attribution race recurred. All 4 races now have accurate racial ability-adjustment recognition records; QA independently confirmed zero blast radius held (full integration suite clean) rather than trusting the claim. Catalogue adoption for Dwarf/Gnome/Halfling landed too (`fb01768d`) — QA independently re-verified all 3 PCGen citations before adopting rather than trusting the commit message, 34/34 new tests plus a full 474/474 suite run. Fully closed end to end: fix + independent re-verification + catalogue coverage for all 4 races. Following up on item 15's Elf-Wizard/Rogue confirmation, QA traced PCGen's actual race data all the way through the real reference chain (`cr_races.lst`'s per-race `.MOD` lines are only source-page citations; the real base race lives in a separate PCC pack per race → `<race>_abilities_race.lst`, each `BONUS:STAT` line explicitly tagged `TYPE:...Racial Default...`) for all 7 races Codex grounds (`explain_*_race_seam` in `pilot_compute.rs`), not just Elf. **Broken, same shape each time — a fixed physical +2/-2 pair is grounded, the race's real +2 mental-ability component is dropped:**
    - **Elf**: has DEX+2/CON-2, real is DEX+2/**INT+2**/CON-2 (`elf_abilities_race.lst:18`). Matters most since Elf Wizard is confirmed reachable and Intelligence is Wizard's casting stat.
    - **Dwarf**: has CON+2/CHA-2, real is CON+2/**WIS+2**/CHA-2 (`dwarf_abilities_race.lst:18`).
    - **Gnome**: has CON+2/STR-2, real is CON+2/**CHA+2**/STR-2 (`gnome_abilities_race.lst:18`).
    - **Halfling**: has DEX+2/STR-2, real is DEX+2/**CHA+2**/STR-2 (`halfling_abilities_race.lst:18`).

    **Confirmed correct, not affected**: Half-Elf and Half-Orc are both correctly implemented as a player-chosen floating +2 to any one ability score (QA caught and corrected its own initial misremembering that Half-Elf got nothing, by live-checking the actual code rather than assuming from the pattern — PCGen confirms Half-Elf gets the same floating +2 as Half-Orc). Human is already correct (live arithmetic on the player's chosen target, unrelated to this bug family). **Scope-checked, not assumed**: Dwarf's size/speed independently spot-verified against the same PCGen source (Medium, 20ft, both match) — the bug is isolated to the ability-modifier section specifically, each race's other trait records (size/speed/senses/immunities) look independently correct.

    Since each affected race's explanation is recognition-only (expects the fixture's chosen score to already reflect the adjustment, doesn't compute it live), any character of these 4 races built per Codex's own documented convention silently ends up 2 points off on the missing ability versus real PF1 — and for any calc keyed to that ability (saves, skills, spell DCs for casters), the error propagates. **QA's read, worth taking seriously**: this consistent a pattern across 4 races reads like a systemic authoring gap (built from a "2-stat template" that never accounted for these races' real 3-stat adjustment), not 4 independent oversights — recommends one bundled fix over 4 separate incremental ones, which the lead adopted. QA correctly filed all 4 rather than fixing them (production code, not their lane). Owner: backend, bundling all 4 into one fix, queued immediately after the render-staleness close.

    **Small, distinct addendum folded into the same fix (QA, same sweep):** while verifying Halfling's other named traits (Keen Senses, Sure-Footed, Fearless, Halfling Luck — all confirmed correct against real PCGen data), found Gnome's and Halfling's `race.<X>.trait_bundle.size` explanation text self-contradicts: it correctly sets `GNOME_SIZE_CATEGORY`/`HALFLING_SIZE_CATEGORY` to `"Small"` and correctly cites `SIZE:SMALL` from the corpus, but then claims Small "contributes no numeric effect to attack rolls, AC, skill checks... or any other computed value" — true for the other 5 grounded races (all Medium), false for real PF1 Small size in general (+1 AC, +1 attack, -1 CMB/CMD, +4 Stealth per SRD). Looks like the size-explanation template was copied across races without adjusting the claim for the two that are actually Small. **Correctly right-sized, not overstated**: this is a misleading claim in a recognition-only explanation, not a live wrong number — `compute_combat_baseline` has no size-modifier term for *any* race yet (confirmed during the earlier class-skill-bonus sweep), so nothing numeric is actually wrong today. Two independent pieces: (1) small/cheap — correct the misleading text, folded into backend's current race-bundle fix; (2) larger — actually wire a size modifier into AC/attack/CMB/Stealth, which overlaps the already-tracked, already-deferred AC/attack-bonus posture-narrowness architecture item (this doc's item 1) — not picked up now, noted as belonging alongside that existing tracking rather than as fresh scope.

17. **MAJOR ARCHITECTURE FINDING, now operator-directed to build — there is no feat-effects engine anywhere in the compute pipeline.** QA verified concretely, not reasoned abstractly: built a real `CharacterInput` from the GE-06 fixture (already `Computed`), added `feat:toughness` to `selected_feats` alongside the required trio, ran it through the real `build_pilot_headless_receipt` entry point, compared before/after. Status stayed `Computed` both times (the combat-posture gate only checks Dodge/Weapon Focus *presence*, no exact-feat-set restriction, so an extra feat doesn't risk breaking the build). Toughness genuinely present in `selected_feats` (checked directly). **HP was 12 both with and without Toughness** — checked via both `class_chassis.fighter.level_1_hit_points` and `durability::compute_max_hp` independently. Real PF1 Toughness is a flat +3 HP at level 1; the real value should be 15. Zero explanations anywhere mention "toughness." **Confirmed system-wide, not Toughness-specific**: grepped `pilot_compute.rs` for any `selected_feats` reference beyond the two hardcoded presence-checks (Dodge, Weapon Focus) in the posture gate — zero hits. There is no general feat-effects computation anywhere in this file; Power Attack, Dodge, and Weapon Focus are the only 3 feats with any mechanical consequence at all, and even those are hardcoded into one fixed deterministic posture, not a general mechanism. Every other selectable feat (of the 185-record CRB catalog the picker exposes) is purely recorded data with zero computed effect. **Correctly sized, not understated or overstated**: QA explicitly declined to presume the sizing call, but flagged this is architecturally the same shape as the already-deferred AC/attack-bonus/skill-posture item (this doc's item 1) — "the correct math doesn't exist yet, not just disconnected." **Not assigned to backend, not attempted this wave** — a general feat-effects engine is squarely future-epic scope, same bucket as the headless/corpus-aware architecture question. Flagged here for the operator's eventual review alongside item 1, since it materially affects how far v0.6 alpha actually is from bar item 3's "select feats" affordance being mechanically meaningful, not just UI-present.

    **Un-deferred and landed (backend, 2026-07-24) — operator directive: "easy enough, you need to create a feats engine," scoped to one real feat first.** New module `src/rules_core/feat_effects.rs`: `hp_bonus_from_feats(selected_feats: &[String]) -> i16` scans for grounded feat effects and sums them, scoped to exactly Toughness's real flat +3 HP for now (not a 185-feat resolver in one pass, per the operator's own scoping) — shaped to extend one feat/match-arm at a time later. **A real string-convention trap caught before it could ship silently wrong**: QA's original finding used `"feat:toughness"` (matching the three hardcoded fixed-loadout feats' bespoke `"feat:<name>"` id space), but traced the real live pipeline before writing the fix and found the actual value a live user's `selected_feats` entry contains is the raw catalog key, `"Toughness"` (verified through the full chain: `feat_data/general.rs`'s `FeatTableEntry.key`, `FeatCatalogEntryDto`'s verbatim passthrough with no transform, `CharacterSheet.tsx`'s `entry.key` sent directly as `featId`). Keying on `"feat:toughness"` (QA's test string, not a real production shape) would have compiled, passed a same-shaped unit test, and never fired for a single real user. `durability.rs`'s `compute_max_hp` signature is untouched (avoiding blast radius into the parity-comparator's own call site); `load_character_durability_at_root` (`character_hub.rs`) now adds the feat bonus on top of `compute_max_hp`'s result as a separate, explicit step. Genuine RED→GREEN: the wiring was temporarily disabled, the new test failed with the exact expected 12-vs-15 mismatch, then restored and confirmed green. 5 new lib tests (including one that explicitly proves the old `"feat:toughness"` string does NOT match, pinning the correct convention) + 1 new desktop test. 233/233 lib, 201/201 desktop. Widening to more feats is real future scope, not attempted here — the pattern is proven, not yet generalized.

    **Widened to 3 more feats (backend, 2026-07-24): Great Fortitude, Iron Will, Lightning Reflexes — each a real flat +2 to one named save.** Picked over Skill Focus (the other obvious candidate) after checking its catalog record directly: its `effect` qualifiers carry a `%LIST` placeholder, PCGen's own marker for a player-chosen target, so it needs to know *which* skill was picked — `selected_feats` has no slot to record that (the same shape of gap Weapon Focus's hardcoded compound id only works around for the one fixed-loadout case, not a general catalog pick). Left for a future slice rather than forced into this bounded pass. Unlike Toughness (wired into `durability.rs`'s separate, non-claim-gated command), these 3 extend `pilot_compute.rs`'s `compute_total_saves` directly — the claim-gated `total_saves` pillar itself. This does not reopen the item-1 architecture question: the non-claim-gated DTO-enrichment pattern (shape (c)) exists specifically for corpus-blocked math; feats are headless-accessible (`selected_feats` needs no corpus), so wiring straight into the claim-gated computation is the direct, correct fix — the same category as fixing items 14/20's silently-wrong values, not a workaround. `feat_effects::save_bonuses_from_feats` returns a small local `SaveBonusesFromFeats` struct (deliberately not reusing `pilot_compute::BaseSaves`, keeping `feat_effects.rs` a dependency-free leaf module). Genuine RED→GREEN: 7 new `feat_effects.rs` tests (each feat isolated, all three stacking, the real "Improved" variant feats correctly not matching) plus 2 new end-to-end `pilot_compute.rs` tests against the real Fighter fixture, wiring temporarily disabled to confirm the exact expected failure before restoring. 254/254 lib, 203/203 desktop.

    **Independently re-verified, clean, real ID chain re-traced end to end (QA, 2026-07-24).** Confirmed `"Toughness"` (not `"feat:toughness"`) is genuinely what a live pick sends by re-tracing the full chain themselves: `feat_data/general.rs`'s `key` field → `feat_catalog.rs`'s verbatim passthrough → `CharacterSheet.tsx`'s `entry.key` sent as `featId` in both `handleAddFeat` and `handleLevelUpFeatPick`. Confirmed additivity (exactly one call site for `hp_bonus_from_feats`, zero feat-related logic anywhere else in `compute_max_hp`/`explain_fighter_level1_hit_points` — no double-counting risk). Reproduced RED themselves (disabled the bonus term, watched the exact 12-vs-15 mismatch, restored).

    **3-feat widening (f38e9f33) independently re-verified, clean (QA, 2026-07-24).** Catalog keys re-confirmed directly against `feat_data/general.rs` rather than trusted from the commit message: `"Great Fortitude"`→`["SAVE","Fortitude","2"]`, `"Iron Will"`→`["SAVE","Will","2"]`, `"Lightning Reflexes"`→`["SAVE","Reflex","2"]`, all matching the code's constants and +2 magnitude exactly; the "Improved" variant feats used in the non-match test confirmed as real, distinct catalog entries (`effect: None`), not strawmen. Stacking confirmed non-double-counting: `save_bonuses_from_feats` has exactly one call site, applying each field to its own save only. No claim-gating risk: the bonus sits after `compute_total_saves`'s existing early-return, pure arithmetic on an already-decided path. RED→GREEN reproduced independently (swapped in `SaveBonusesFromFeats::default()`, got the exact expected `left: 4 / right: 6` failures on both new tests, restored). Ran clean: 7/7 new `feat_effects.rs` tests, 2/2 new `pilot_compute.rs` tests, 254/254 full lib suite. **Item 17 fully independently verified end to end, both slices, nothing outstanding.**

    **RESOLVED (frontend, `7360fe4a`) — the Defense tab doesn't refetch durability after a feat pick.** Was: `CharacterSheet.tsx`'s `durability` state only refetched on a `useEffect` keyed to `[props.row.characterId]`; neither `handleAddFeat` nor `handleLevelUpFeatPick` triggered a refetch. **Fixed**: a shared `refreshDurability()` helper now called after a successful feat grant on both paths, falling back to `null` on failure (same honest treatment as the initial per-character load). Live-verified both paths (HP 22/22→25/25 via the Feats-tab picker, then live update again via a level-up feat pick), no reselect needed. **Independently verified (QA, 2026-07-24)**: confirmed both call sites invoke `refreshDurability()`, correctly placed strictly after the `Blocked` early-return so it never fires on a rejected mutation; confirmed the failure-path fallback matches the initial-load pattern exactly (`catch { setDurability(null) }`, identical shape); confirmed no ordering/race risk (`await addFeatSelection` resolves, including its synchronous save, before the refetch fires). Live-UI leg attempted but inconclusive on an unrelated environment rendering issue (`libEGL`/DRI3 error, screenshots came back black despite a correctly-launched window) — correctly not chased further per standing practice, called clean on the strength of the code read, regression suite (69/69, typecheck clean), and the commit's own live-verified numbers.

    **OPERATOR DIRECTIVE (Todd Hintzmann, 2026-07-24) — build it, no longer deferred.** Explicit call: "easy enough, you need to create a feats engine." Un-deferred and added to the actionable backlog. Scope, per `future-epic-scoping.md`'s own sizing note (gap B): a per-feat effect resolver wired into HP/attack/skill/save totals for the CRB feat catalog — starting with a real, bounded slice (the feats with the most mechanical weight and highest chance of a real fixture to verify against, e.g. Toughness's flat +3 HP, since that's the exact gap QA's own verification already demonstrated) rather than all 185 records at once, following this swarm's established "prove one real case end-to-end before widening" discipline. **In progress (backend, 2026-07-24)** — dispatches resumed; backend landed item 7 first, now starting this one.

18. **RESOLVED, real precise gap identified (2026-07-24) — a documented Wizard spellbook ceiling (level 3) didn't fire at level 4, for non-Human Wizards specifically.** `pilot_compute.rs` documents `WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL=3` with claim-blocking diagnostics expected past that level. Frontend live-drove an Elf Wizard through real level-ups 1→2→3→4 via the real `LevelUpDialog` — all four succeeded, disk-confirmed real progression each step (HP 8→14→20→26, BAB/Will climbing correctly). No `Blocked` wall appeared at level 4 as documented. Frontend sent backend the specifics with line numbers and an explicitly-unconfirmed guess (possibly the restrictive check only fires from a level-1-specific function, not the wider chassis-widening path that's since been built) — correctly didn't dig further into backend's file themselves. **Deliberately NOT logged as a bonus/positive finding yet, unlike the "any race" discoveries** — the ceiling may exist precisely because the compute formulas past level 3 were never validated as correct, in which case "unblocked" could mean level 4+ Wizards now silently produce wrong spell-slot/save-DC/whatever values instead of being honestly stopped, the same shape of problem as the class-skill-modifier bug (item 14) — just newly *exposed* rather than newly *introduced*. Backend asked to determine which it is (stale leftover check vs. a real validation gap) before this gets resolved either direction; if it's the latter, QA should spot-check the level 4 Wizard's actual computed values against real PF1/PCGen once backend clarifies what's reachable.

    **QA's static sweep (2026-07-24): the ceiling is correctly enforced in `pilot_compute.rs`, verified 3 ways, not assumed.** Systematically grepped all level-comparison gates, all `_MAX_LEVEL`/`SUPPORTED_MAX` constants, and all "only supported for"/"bounded to" comments — `WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL` is the only sub-ceiling of its shape in the file, no other class has a hidden one like it. Confirmed the gate fires correctly three ways: (1) `unmet_wizard_spellbook_conditions` checks `level > WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL` and pushes `claim_blocking: true` diagnostics; (2) `build_pilot_headless_receipt` derives overall `Blocked` status directly from `diagnostics.iter().any(|d| d.claim_blocking)` — no parallel status logic that could diverge; (3) an existing test (`tests/sd13_wizard_level4_progression.rs`) already proves it fires at exactly level 4 through the real pipeline. Also checked the Tauri layer (`apply_level_up` has no bypassing path, defers entirely to the standard pipeline) and the reverse failure mode (all 11 classes' level gates use an identical, correct pattern; all `claim_blocking: false` diagnostics checked individually, none relate to a dropped threshold). **Conclusion: the engine-side enforcement is airtight** — if frontend's "didn't fire" symptom is real, it's very likely UI-side, matching the shape of the earlier render-staleness bug (server-correct, client looks wrong), not a second engine gap. QA and backend reconciling directly with frontend rather than duplicating investigation.

    **Root cause found (frontend, 2026-07-24): both QA's static analysis and the original live symptom were correct, from different angles.** The ceiling itself lives inside `explain_wizard_level1_prepared_spell_baseline` (`pilot_compute.rs:14950`), which has `if input.chosen.race_id != HUMAN_RACE_ID { return; }` immediately after its level guard — for any non-Human Wizard, the entire function, ceiling included, never runs at all. QA's sweep correctly found the enforcement airtight (true, for the Human path it traced); frontend correctly found no wall at level 4 for an Elf Wizard (true, because the whole function is skipped for non-Human). **Not a regression, and not "level 4+ silently computes wrong values" as originally feared**: BAB/saves/HP for a non-Human Wizard remain correct, live-verified, because those route through a separate chassis path that was already widened to be race-agnostic. **What's actually missing for non-Human Wizards**: the Wizard-specific spellbook validation (the ceiling itself never blocks them past level 3) and spell-save-DC grounding (lives in the same gated function) — both real, previously-invisible gaps that frontend's own "any race" discovery exposed, not something that discovery broke.

    **Empirically confirmed at every level, not just level 4 (backend, 2026-07-24).** Composed an Elf Wizard level 1 directly and called `build_pilot_headless_receipt` — status `Computed`, but ZERO explanations with a `class_chassis.wizard`/`class_spell.wizard` id prefix, zero wizard-spell diagnostics of any kind, structurally absent with no claim-blocking flag raised about the absence. Separately confirmed a Human Wizard leveled 1→4 correctly hits `Blocked` at level 4 exactly as documented — the ceiling works precisely as designed for the one race it's scoped to. So the original symptom wasn't level-4-specific at all: the entire spellbook-gate mechanism, ceiling included, never engages for a non-Human Wizard at *any* level, 1 through 20. **Correction to item 19's earlier framing**: the wizard-spell-save-DC fix from item 12 lives inside this same Human-gated function, so "Elf Wizard's spell save DC is shorted by 2" was itself imprecise — it isn't shorted, it's **absent entirely** for any non-Human Wizard (corrected here rather than left standing).

    **Decision (lead, 2026-07-24): documented as a deferred completeness gap, not fixed and not blocked.** Backend proposed two shapes — widen the function to all races (real future-epic effort, everything in it currently reasoned in Human-specific terms, would need per-race re-verification) or add a claim-blocking diagnostic for non-Human Wizards naming the absence (small, honest, but would **regress already-verified, disk-confirmed capability** — the swarm proved an Elf Wizard can add spells and reach `Computed` live, end to end). Neither is right: widening is out of scope for this wave, and blocking conflates a genuine **completeness gap** (wizard-specific spell math doesn't exist yet for non-Human, nothing is computed *wrong*) with a **correctness bug** (a value that's wrong, like the class-skill-modifier bug) — the same distinction item 17's feat-effects finding already established (a Fighter can select Toughness and reach `Computed` even though it does nothing; that's correctly not blocked either). Filed here alongside item 17 as the third tracked completeness/architecture gap (with the AC/attack-bonus item), not assigned to anyone this wave.

    **Lead correction (2026-07-24, ~14:40 ET) — briefly mis-dispatched to backend as active backlog after a context compaction; corrected before backend started.** This item's own decision two paragraphs up ("not fixed and not blocked... not assigned to anyone this wave") stands; SWARM_TASKS.md's row is corrected back to `blocked`/deferred, not `in progress`. No engineering work was actually done under the mistaken dispatch — caught by re-reading this doc before trusting the task-table summary alone. Backend independently caught the same contradiction from their own memory of this item's history before writing any code, and correctly stood down pending lead confirmation rather than picking a side themselves.

    **Scoping note, informational only, not a re-opening of scope (backend, 2026-07-24).** While getting oriented on the (mistaken) dispatch, backend read `explain_wizard_level1_prepared_spell_baseline` and its two callees before being told to stand down. Observation for whoever revisits this item's scope later: the widening effort may be smaller than the original decision estimated — the ~400-line function's only Human-specific content is the one `race_id != HUMAN_RACE_ID` race-gate line plus two cosmetic "Human Wizard" strings in explanation text; no formula in the body actually depends on race, since `ability_modifiers` already arrives race-adjusted via `apply_human_ability_bonus` upstream. Not verified beyond a read-through, not acted on, and does not by itself change this item's `blocked`/deferred status — logged here purely so the information isn't lost before an eventual real scope decision.

    **OPERATOR DECISION (Todd Hintzmann, 2026-07-24) — widen it now, given backend's smaller-than-feared scoping read.** Explicit call, made after the swarm-wide closure-readiness checkpoint (item 31): remove the `race_id != HUMAN_RACE_ID` gate in `explain_wizard_level1_prepared_spell_baseline` and re-verify the formulas for a non-Human Wizard end to end. Backend's scoping note (above) found only one race-gate line plus two cosmetic strings are actually Human-specific — no formula depends on race since `ability_modifiers` arrives pre-adjusted — but that was a read-through, not a full verification; backend should confirm this empirically (the same "prove it, don't reason about it" discipline this swarm has used throughout) before landing, including a real non-Human Wizard reaching `Computed` with correct spell-save-DC and the spellbook ceiling firing correctly past level 3. Dispatched to backend.

    **RESOLVED, landed and lead-verified (backend, `7c2d2ad2`, 2026-07-24).** Race gate removed from `explain_wizard_level1_prepared_spell_baseline`; the "Human Wizard" explanation strings generalized via a new `race_display_label` helper (derives the display name from `race_id`, same "derive, don't hardcode" pattern `class_summary_label` already used). Empirically verified exactly as required — 4 new tests through the real `build_pilot_headless_receipt` entry point, not just the narrower chassis function: an Elf Wizard reaches `Computed` with a correct racially-adjusted spell-save-DC at level 1, stays within the ceiling at level 3, and correctly hits `WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL` at level 4 — all previously entirely absent for non-Human Wizards; Human Wizard behavior confirmed unchanged in the same pass. **Lead independently re-ran** the 4 new tests (pass), the full root-crate workspace suite (4275/4275, 0 failed, matches exactly — up from 4271 by the 4 new tests), and the desktop suite (204/204, unaffected — checked first since `character_hub.rs`/`pf1_adapter.rs` both reference this ceiling). Item 18 fully closed.

    **Independently re-verified, thorough full-function trace (QA, 2026-07-24).** Traced every formula in the function's full body (lines 15001–15412) by hand, not just the 4 new tests' coverage: confirmed BAB/saves/spell-slot-ladder/Scribe-Scroll/specialist-bonus/Intense-Spells are pure level-driven functions; confirmed `spell_save_dc` and `force_missile_uses_per_day` (the only two reading ability modifiers) receive an already race-adjusted `ability_modifiers.intelligence` (`apply_human_ability_bonus` passes non-Human scores through unchanged; the frontend bakes the real racial adjustment in before submission); confirmed `unmet_wizard_spellbook_conditions`/`ground_wizard_prepared_spellbook` have no race reference. Grepped the whole function for `race_id`/`HUMAN_RACE_ID`/`race:` — only hit is the new `race_display_label` call. Checked all 7 curated races through `race_display_label` (not just Elf), all correct PF1 naming, throwaway test reverted cleanly. Confirmed the "Human Wizard" string change is safe — grepped the tree for every `"Human Wizard"` hit, none are runtime string-matches against the live explanation text. **Reproduced RED themselves and surfaced a sharper finding than originally scoped**: restoring the old race gate didn't just lose the spell-save-DC — the Elf Wizard stayed `Computed` past the real spellbook ceiling at level 4 with zero ceiling diagnostic at all, meaning the pre-fix bug was a genuine correctness gap (enforcement silently never ran for non-Human), not merely a missing display value. Ran clean: 4/4 new tests, 258/258 full lib suite, 204/204 desktop — matches the commit's counts exactly.

    **Sweep for the same shape elsewhere (frontend, 2026-07-24): Wizard's gap does not repeat as a correctness issue, but one new question surfaced.** Checked all 11 `MAX_SUPPORTED_*_LEVEL` constants — `WIZARD_SPELLBOOK_SUPPORTED_MAX_LEVEL` is the only secondary/nested ceiling in the file, doesn't recur. Checked all 15 `if input.chosen.race_id != HUMAN_RACE_ID { return; }` occurrences: 2 are legitimately Human-only-named functions; 13 are per-class `explain_*` functions — 8 already match the known `human-diagnostics-only` pattern (expected, not new); of the 3 classes that reach `Computed` (Fighter/Wizard/Rogue), Rogue's `explain_rogue_level1_chassis` is race-gated the same way but only takes an `explanations` param, no `diagnostics` — structurally can't affect `Computed`/`Blocked`, so non-Human Rogues lose flavor-text grounding (Sneak Attack/Trapfinding/Evasion records) but nothing the frontend currently displays — cosmetic, not correctness. **One new genuine question**: Fighter's `validate_fighter_feat_choice_legality` (line 7867) is also race-gated *and* carries claim-blocking diagnostics for non-canonical feat choices — its own doc comment claims it's redundant with the separate, already-race-agnostic `unmet_combat_posture_conditions` gate ("any other posture is already claim-blocked upstream"). Frontend correctly did not assert this overlap themselves and flagged it for backend to trace — if the claim is wrong, a non-Human Fighter with an invalid feat choice could reach `Computed` when it shouldn't, the opposite failure direction from the Wizard gap (something that should Block but might not, vs. something absent). Backend investigating; frontend live-testing the same question from the UI side in parallel.

    **RESOLVED — the overlap claim was wrong; real, currently-reachable gap fixed (backend, 2026-07-24).** `unmet_combat_posture_conditions` only ever checked the level-1 Fighter bonus-feat slot; `CANONICAL_FIGHTER_FEAT_CHOICES` covers 17 slots total (levels 2–20 bonus feats plus 4 weapon-training groups), so the other 14+ slots had zero validation for any non-Human Fighter — not theoretical, already reachable today via `level_up_character`'s caller-supplied `additional_choices` with no other check catching it. **Confirmed empirically before fixing, not assumed**: built an Elf Fighter, gave it a wrong bonus-feat choice at level 2, got zero diagnostics — genuinely `Blocked`-worthy input silently passing. Fixed by removing the incorrect Human-only race gate from `validate_fighter_feat_choice_legality` — Fighter's own bonus-feat/weapon-training choices are Fighter class features, not Human-specific; the one genuinely race-coupled table entry is already self-limiting (absent for non-Human, existing "slot absent, skip" logic already handles it correctly). 2 new tests (the real gap now caught; canonical selection still passes for any race), full suite clean, zero blast radius. This is the opposite failure direction from item 18's Wizard gap — something that should have Blocked and now correctly does, rather than something absent that was reaching `Computed` unflagged.

    **Nuance, live-verified (frontend, 2026-07-24): the fix is correct but currently has no UI attack surface.** `compose_character_input` hardcodes the canonical values for both slots this check protects (`choice:level_1_character_feat → feat:power_attack`, `choice:fighter_bonus_feat → feat:weapon_focus:weapon:longsword`) unconditionally for every character — no create-form field or level-up affordance lets a player submit anything else for these two slots. Live-verified with a fresh Dwarf Fighter: `Computed`/`Saved` successfully, disk-confirmed identical canonical choice records to a Human Fighter, no fabricated third slot (the Human-only `choice:human_bonus_feat` entry correctly absent, not flagged as illegal — matches the validation function's own "absent slot is not fabricated" doc comment). So the divergent-choice scenario the check exists to catch can't currently be triggered by anyone, Human or non-Human, through the shipped UI — the gap backend closed is real and correct defense-in-depth at the command/API layer (`level_up_character`'s `additional_choices` parameter could still be invoked non-canonically by something other than today's UI), not an active user-visible bug today. Doesn't diminish the fix's value, just calibrates its actual current exposure for the record.

    **Second, independent gap in the same function, also confirmed and fixed (backend, `0eb9ea65`).** The gate used `supported_fighter_level` (single-class-only), so it skipped every Fighter+X multiclass mix entirely — regardless of race, even Human. Confirmed empirically before fixing: a Human Fighter1/Rogue3 with a wrong level-1 bonus-feat choice produced zero diagnostics. Fixed with a one-line swap to `fighter_level_in_mix`, the multiclass-aware equivalent already established elsewhere in the file (`wizard_level_in_mix`'s precedent) — no other change needed since the loop body never used the specific level value. 2 new tests, full suite clean. This closes both dimensions of the original wrong doc-comment claim: race and multiclass, same function, two independently-confirmed gaps. Very likely shares the same "no current UI attack surface" property as the race fix above (same hardcoded-canonical-values mechanism regardless of class-mix) — not separately re-verified, flagged for anyone who wants certainty later.

    **Systematic sweep, complete (backend, `68721ca0`): a third instance found and fixed, correctly risk-tiered as non-exploitable.** Checked all 4 `_legality`/`_conditions`/`validate_` gate functions that exist in `pilot_compute.rs` for the same brittleness. Found one more: `unmet_combat_posture_conditions`'s own Fighter bonus-feat sub-check had the identical blind spot (`supported_fighter_level` instead of `fighter_level_in_mix`) — confirmed empirically (the function's own `unmet` list came back empty for the multiclass scenario) before fixing. **Precisely risk-tiered, not overstated**: this instance was NOT actually exploitable at the system level, since `validate_fighter_feat_choice_legality` (already fixed for multiclass) independently catches the exact same slot — overall `Computed`/`Blocked` status was already correct either way. Fixed anyway as a real internal-consistency issue (two checks that shouldn't be able to silently diverge later), explicitly flagged as a different risk tier than the first two fixes. The other 2 candidate functions checked out clean: `unmet_wizard_spellbook_conditions` takes an already-resolved level as a parameter (caller already multiclass-aware); `unmet_selected_skill_posture_conditions` has no per-class level lookup at all. **Sweep complete — all 4 gate functions in the file now covered, no further instances found.**

19. **RESOLVED, MAJOR — CreateCharacterForm never actually submitted racial ability adjustments, for any of the 4 fixed-adjustment races, since the form was first built.** Frontend live-verified backend's Elf +2 Intelligence fix (`9ec0e036`) and found it didn't change the visible outcome: a fresh Elf Wizard's ABILITIES panel showed `INT 10 +0`, `DEX 14 +2`, `CON 14 +2` — none of Elf's three racial adjustments (+2 DEX, -2 CON, +2 INT) were actually applied. Root cause was entirely frontend-side, unrelated to any of backend's race-bundle explanation-text work: `CreateCharacterForm.tsx` computed `calculatedScore()` (raw + racial adjustment + allocation + age) for the on-screen "Calculated" preview column only, but submitted `rawScore()` — the unadjusted value — as the actual `abilityScores` in the create request. Confirmed against the engine's real contract via `pilot_compute.rs`'s own doc comment on `apply_human_ability_bonus`: every non-Human race's submitted score is expected to already bake in its racial adjustment (Human is the sole exception — pre-bonus base plus a server-side choice). The frontend never honored that contract, for any of the 4 races, independent of and predating this session's explanation-text fixes — those were correct about what a race's adjustment *should* be, just describing a number the frontend was never actually sending. **Fixed** (`f2c616ed`): new `applyRacialAbilityAdjustments()` in `composeCreateCharacterRequest.ts`, wired into the submission path, no-op for Human, 3 new regression tests. **Live re-verified end-to-end**: a fresh Elf Wizard now saves `dexterity:16, constitution:12, intelligence:12` (disk-confirmed) and the sheet shows the full correct cascade — DEX +3/CON +1/INT +1, Knowledge skills +1, DEX-based skills +3, AC/saves all shifted correctly. Since this is the same code path for all 4 races, QA re-verifying Dwarf/Gnome/Halfling now get correct submitted scores too, same rigor as the original race sweep — this closes the loop on the full 4-race gap (recognition-text fix + submission-bug fix + live verification), not just the explanation-text half.

    **Dwarf/Gnome/Halfling re-verified — real production-code execution, short of full live-UI proof due to an environment blocker (QA, 2026-07-24).** Confirmed `applyRacialAbilityAdjustments` is called unconditionally regardless of race (no race-specific branching at the call site) and that `RACE_OPTIONS`' catalog data has carried the correct full 3-stat adjustment for all three races since the file was first authored (checked via git history, independent of and predating backend's engine-side text fix). **Executed the real production code, not a reimplementation**: imported the actual function and catalog directly and confirmed Dwarf (CON+2/WIS+2/CHA-2), Gnome (CON+2/CHA+2/STR-2), and Halfling (DEX+2/CHA+2/STR-2) all compute correctly. **Found a real, separate gap while there**: `composeCreateCharacterRequest.test.ts`'s regression coverage is incomplete — the Dwarf test only asserts unrelated stats stay untouched, never checks the actual adjusted CON/WIS/CHA values, and Gnome/Halfling have zero coverage at all (only Elf got full treatment). Correctly filed to frontend rather than edited (frontend's file). **Could not complete the full live-UI-then-disk-confirm verification** matching frontend's methodology — the desktop app environment was consistently unstable (webview renders solid black, process/Xvfb pair dies within seconds, ~6 attempts, ruled out as a resource issue with 17GB free) — flagged honestly as an environment blocker rather than either fabricating a live-verified claim or silently giving up. **Accepted as sufficient** given the strength of the evidence (real code, real data, mechanically identical to the already-fully-verified Elf path) — not re-attempted further. Worth watching whether this environment instability is session-wide (frontend's capstone walkthrough uses the same GUI environment).

20. **RESOLVED — `skill_allocation.rs`'s class-skill recognition was Fighter-only, silently leaving Wizard's and Rogue's cross-class rank cap unenforced.** Dispatched as a bounded widening task following backend's own scoping finding (item 1) that class-skill-list recognition is a labor problem, not an architecture wall. Turned out to be more than pure widening: confirmed empirically before fixing that a level-1 Wizard could allocate 5 ranks to a cross-class skill (real PF1 cap is 1) with zero enforcement — because neither Wizard nor Rogue had ANY grounded class-skill posture in this module, `has_grounded_class_skill_posture` was false for both, so the cross-class half-cap logic never engaged at all, not just "no class-skill bonus." Same failure shape as item 14 (a silently wrong number, no claim-blocking diagnostic), but on rank enforcement rather than a modifier value. **Fixed (`21f815c1`)**, grounded against the real PCGen corpus: Rogue gets all 5 of this module's bounded skills (Climb/Intimidate/Swim/Diplomacy/Disable Device) as real class skills (`cr_abilities_class.lst:2838`, verified exact match); Wizard gets a genuinely empty grounded list (`cr_abilities_class.lst:2565`, checked zero overlap, not assumed) — but even the empty grounding matters, since it's what flips the cross-class cap from unenforced to correctly enforced for Wizard. Scoped to Wizard/Rogue only, same bounded-caution philosophy as the existing Fighter posture. 4 new tests, full suite clean, zero blast radius (QA's existing `skill_allocation` tests never touched Wizard/Rogue at all). QA to independently verify.

    **QA's independent catalogue coverage landed (`d35521ec`, lead stewardship during a quota outage).** QA wrote real fixture-driven tests (through the actual `load_character_input_fixture` parser, not hand-built structs) re-verifying both PCGen citations independently and cross-checking against the separate `compute_selected_skill_modifiers` fix's own fixture to prove the two modules now agree on Wizard's class-skill list. Finished but uncommitted when QA hit a session quota limit; the lead verified it (3/3 pass, full workspace suite clean) before landing it on QA's behalf rather than risk losing finished work during the outage.

21. **RESOLVED, SIGNAL FINDING — systematic sweep for other Fighter-only-grounded computations came back clean, one real near-miss traced and ruled out.** Following the pattern that found items 14 and 20 (widening Fighter-only recognition surfacing silent bugs for Wizard/Rogue), backend swept `pilot_compute.rs`, `skill_allocation.rs`, `durability.rs`, and `money.rs` for any remaining instance of the same shape. **One candidate investigated and ruled out**: `explain_rogue_level1_chassis` gates on the single-class-only `supported_rogue_level` (no `rogue_level_in_mix` counterpart), superficially the same shape as items 14/20's root cause — but tracing where its output goes shows its explanations (sneak-attack-die-count, trapfinding, Evasion, Trap Sense, Uncanny Dodge) are standalone, never wired into `compute_fighter_chassis`, `compute_total_saves`, or `compute_combat_baseline`, unlike Fighter's Bravery/bonus-feat progression which feeds real feat-choice validation. No downstream consumer exists, so for a multiclass build missing this record, nothing is silently wrong and no rule goes unenforced — same cosmetic-only shape already established for non-Human Rogue in item 18's sweep, not a new correctness bug. **Everything else checked out clean**: the other `FIGHTER_CLASS_ID` sites in `pilot_compute.rs` are deliberate, already-widened 3-class dispatch points with scope-decision doc comments; `unmet_selected_skill_posture_conditions` was already widened pre-swarm (SD-21 E6b.1); `supported_fighter_level`'s one remaining Fighter-only use (armor training) is correctly Fighter-exclusive, a real class feature, not a generalization gap; `durability.rs` is fully class-generic with an explicit Fighter/Wizard/Rogue-parity test; `money.rs` has no class branching at all, out of scope for this pattern. **Backend's own read, adopted by the lead**: this is a genuine signal, not an absence of looking — the swarm's remaining distance from the alpha bar is now confirmed architecture-level (items 1, 17, 18), not further silent-correctness bugs in the three working classes' shared computation paths.

22. **RESOLVED — `characterProgression.ts` now has real regression coverage.** Landed (`ca25bfef`): new `characterProgression.test.ts` covers `parseHeldClasses` (including the exact multiclass regression case `d03bc89d` fixed, plus an unknown-class-id fallback), `formatHeldClasses`, `totalCharacterLevel`, `casterLevel`, `classSkillPointsBase`, `classHitDie`, `classWeaponProficiency`, `maxHitPoints` (both single-class and a multiclass case verifying only the character's true first-ever level maximizes its hit die), `totalSkillPoints`, `buildLevelEntries`, `buildNextEntries`, and `previewLevelUp`. 63/63 suite green. Genuine RED caught along the way: frontend's own first-draft assumption about the title-case regex (that underscores would become spaces) was wrong — `_` counts as a `\w` word character, so the whole segment is one contiguous match and only the leading letter capitalizes — caught when the test failed and fixed to match real behavior, not adjusted to match the wrong assumption.

23. **RESOLVED — LevelUpDialog now collects a real feat pick at feat-gaining levels.** QA verified `e8e45976` (LevelUpDialog wiring) and found the mechanics clean, but flagged a real, understated-scope gap: the excusing comment about no feat catalog was stale, and Fighter 1→2 genuinely should offer a bonus-feat pick and didn't. **Fixed (`ddfc66bb`)**: new `levelGrantsFeat(features)` pure predicate in `characterProgression.ts` (covers the universal odd-level feat, Fighter's bonus combat feat, and Wizard's periodic bonus feat, all already represented as feature strings in the existing table). When a level grants a feat, `handleLevelUpAccept` defers and opens the exact same feat `ItemPickerModal` the Feats-tab "Add Feat" button uses (same catalog, no second picker built), retitled "Pick a feat — level N"; picking one persists the level-up first, refreshes so the sheet is never stale, then grants the feat via the real `addFeatSelection` command. Cancelling aborts the whole level-up rather than leaving a half-applied state. When a level grants no feat, behavior is byte-identical to before. **Real deviation from the original suggestion, found by testing, not assumed**: frontend tried also recording the pick via `additionalChoices` (mirroring the hit-die choice) and the backend rejected it outright — `local_store.rs`'s round-trip grammar requires at least two colon-segments in a `selection_id`, which `hp:average` happens to satisfy and a real feat catalog key like "Cleave" never will. Since `additionalChoices` is inert anyway (nothing gates on it) and the real grant lives entirely in `chosen.selected_feats` via `addFeatSelection`, the doomed provenance entry was correctly dropped rather than breaking every real feat pick to satisfy a decorative record. **Live-verified both branches end to end**: a Dwarf Fighter 1→2 got the picker, picked Cleave, confirmed on disk and in the Feats tab (HP recomputed to 22/22); a Human Wizard 1→2 (no feat at that level) leveled up with no interruption, exactly as before. New test (`verifiesLevelGrantsFeatDetectsEveryRealFeatSource`) covers all three real feat sources plus a true no-feat level. 63/63 suite green, typecheck clean.

24. **RESOLVED — PCGen parity comparator gained a new dimension, `combat.base_attack_bonus`.** Backend's self-directed scan (following up on the earlier DTO-consumption sweep) found the comparator only ever exposed `combat.baseline_melee_attack_bonus` (the Strength/feat-inclusive total), never the raw class-table BAB PCGen's own export template carries separately (`<attack><melee><bab>`). Verified empirically before implementing — ran the real PCGen pipeline against the Fighter fixture, confirmed `<bab>+1</bab>` matches Codex's own value. Landed across the full stack (`cda3bf1c` + QA's catalogue adoption `b8eff433`): `SelectedParityDimensions::from_receipt`/`from_pilot_receipt`, `pcgen-normalize-output.py` extraction (deliberately from `/character/attack/melee/bab`, not the confusingly-named sibling element — documented why), a new Python test file (RED→GREEN), 2 new Rust lib tests. **Real end-to-end confirmation**: both `sd26_pilot_case_verification.rs` and `v06_wizard_pilot_case_verification.rs` (which drive the actual PCGen pipeline) show genuine agreement — Fighter BAB 1==1, Wizard BAB 0==0 (correctly reflecting Wizard's poor BAB progression at level 1). Cross-surface protocol followed correctly: backend held the commit until QA landed the 4 mechanically-affected test files (`b8eff433`), then pushed. Small hygiene fix rode along: added a Python section to `.gitignore` (`__pycache__/`, `*.pyc`) since the new Python test file was the first thing to need it.

    **Second-pass scan, clean, real negative result (backend, 2026-07-24).** Went a level deeper on the same method (check `base-xml.ftl` directly for other exportable-but-unexposed fields) against everything Codex actually computes: touch/flat-footed AC and AC sub-modifiers (PCGen exports them, but Codex only ever computes one aggregate `baseline_armor_class` — new engineering, not wiring, same bucket as items 1/17/18); initiative (explicitly out of scope per `pilot_compute.rs`'s own doc comment, not computed anywhere); a skill-point pool total (zero hits anywhere in `rules_core` — only individual ranks/modifiers exist); encumbrance sub-fields like lift/push/drag capacity (`encumbrance.rs`'s `CarryingCapacityThresholds` only has light/medium/heavy, already fully exposed). DR was the one candidate Codex does compute but, unlike BAB, has no live PCGen fixture yet to verify against (no Barbarian pilot case exists) — not a same-day wiring win the way BAB was. Also swept every owned production file for TODO/FIXME/XXX/"unwired" markers — zero hits, consistent with the no-stub discipline held all session. Genuine clean result, not an absence of looking — this closes out the comparator-scan thread the same way item 21 closed the correctness-bug thread.

25. **RESOLVED — the frontend test-coverage backlog pattern is closed.** Was: multiple frontend logic modules shipped with sound reasoning but zero dedicated test coverage. QA verified `75200fcb` (SkillAllocationDialog wiring) and found the logic itself clean: `skillIdFor`'s "only 5 confirmed skill ids" claim matches `skill_key_ability_modifier` in `skill_allocation.rs` exactly; unrecognized skill ids are genuinely inert (a silent `continue`, no fabrication, no rejection); `handleSkillAllocationAccept` correctly checks `blocked` and returns before touching local state. But grepping every `.test.ts` in the tree for `skillIdFor`/`setSkillAllocations` turns up nothing — the 30-of-35 skill-id mappings beyond the 5 the engine actually recognizes have no regression test pinning them down.

    **Fourth instance confirmed (QA, `94a38657`, Bio editor wiring) — pattern is now systemic, no longer flagged individually.** Bio editor's own logic verified clean too: on-blur-not-per-keystroke persistence confirmed across all 8 fields, the character-switch `useEffect` has a real `cancelled`-guard against a stale in-flight load landing after a switch (not just claimed), and load/save failure handling is correctly non-load-bearing (bio is flavor text) vs. surfaced through the standard mutation-error banner. Same gap: zero test files reference `characterBio.ts`/`updateCharacterBio`/`loadCharacterBio`. That's 4 for 4 (`characterProgression.ts` — now resolved as item 22 —, `skillIdFor`/`setSkillAllocations`, the stale LevelUpDialog comment's underlying module, and now Bio) — QA's own judgment, adopted: this is a real, systemic gap across this wave of frontend persistence-wiring commits, not four independent coincidences, and not worth re-flagging one at a time going forward. **Owner: frontend, backlog** — a deliberate pass across `characterHub/` for modules with this shape (sound logic, zero dedicated test file) once the current queue (item 23 — since resolved — and any further items QA's sweep surfaces) is clear, rather than four-plus piecemeal fixes.

    **Self-audit complete, pattern fully closed (frontend, `0c602c72`, 2026-07-24).** Diffed exactly the files this swarm created/touched on `tranche/6` (`git diff $(git merge-base develop tranche/6) tranche/6 -- apps/desktop/src/`, matching this item's own "this wave of frontend persistence-wiring commits" framing) and checked each non-test file for a sibling `.test.ts`. Found and closed 8 in `boundary/` with real logic and zero coverage: the standard no-Tauri-runtime-throws case for `addFeatSelection.ts`/`listFeats.ts`/`loadCreateCharacter.ts`/`loadSavedCharacterDetail.ts`/`purchaseEquipment.ts`/`recordAndPrepareSpellSelection.ts` (same template as the existing `addSpellSelection.test.ts`); `characterDurability.ts` (both functions throw with no runtime, no "resolve to default" case unlike bio/money); `characterMoney.ts` (`gpToCopper`'s real conversion logic including the fractional-copper rounding case, plus its own asymmetric load/adjust behavior). Checked everything else this swarm touched (`characterHub/*.ts`, `testSupport/makeSurface.ts`) and found nothing further — already covered, a React component (no jsdom in this runner, an established non-goal), or fixture/test-support data rather than logic. **Lead-verified**: 77/77 test files passing (up from 69), typecheck clean, sampled `characterMoney.test.ts`/`characterDurability.test.ts` directly — real, specific assertions against real behavior, not scaffolding. **Item 25 fully closed, not partial** — no remaining candidates in frontend's ownership.

    **Independently sample-reviewed, clean (QA, 2026-07-24).** Read source-then-test for the 6 files the lead hadn't personally sampled (`addFeatSelection`/`listFeats`/`purchaseEquipment`/`recordAndPrepareSpellSelection`/`loadCreateCharacter`/`loadSavedCharacterDetail`). Confirmed each is a thin `invoke()` wrapper with no client-side logic beyond its `hasTauriRuntime()` guard (e.g. `listFeats`'s real filtering is server-side in Rust), so the no-runtime-throws test is genuinely the full available surface, not a shortcut — and confirmed it matches the pre-existing `addSpellSelection.test.ts`/`addEquipmentSelection.test.ts` convention byte-for-byte, not a new pattern invented for this batch. Noted `recordAndPrepareSpellSelection.test.ts` uses the real `WIZARD_STARTER_SPELL_ID` value (`'Light'`), not an arbitrary placeholder. Reproduced a real regression on one file (`purchaseEquipment.test.ts`): temporarily broke the source's error string, watched the test fail with the exact expected mismatch, restored byte-identical — confirms the assertions are load-bearing. All 6 pass individually; full suite 77/77 matches the lead's count. Nothing to flag.

    **Scoping pass (QA, 2026-07-24), not a build — turns "there's a pattern" into a concrete checklist frontend can execute directly.** One more targeted grep (every `characterHub/*.ts`/`boundary/*.ts` file without a sibling `.test.ts`) turned up two more real candidates beyond the three already named (`skillsModel.ts`/`setSkillAllocations.ts`, `characterBio.ts`, and the Wizard spell-routing logic): `abilityScoreMethods.ts` (real dice/point-buy logic, untested) and `portraitImageProcessing.ts` (Canvas/File-API logic, lower priority — needs a DOM shim this test runner doesn't have, different treatment than the rest). `previewData.ts` was checked and is data-only (a fixture, not logic) — not added to this list.

    - **`skillsModel.ts` / `boundary/setSkillAllocations.ts`** — the 30-of-35 unconfirmed mappings named above. Concrete cases: (1) `skillIdFor` on a parenthetical skill name (e.g. `"Knowledge (Arcana)"` → `"skill:knowledge_arcana"`, not a double-underscore or trailing-underscore artifact — the regex's edge case); (2) `skillIdFor` on a multi-word non-parenthetical name (`"Sense Motive"` → `"skill:sense_motive"`, `"Sleight of Hand"` → `"skill:sleight_of_hand"`); (3) `isClassSkill` for at least one class/skill pair per confirmed-vs-unconfirmed boundary (a class that has the skill, a class that doesn't, and — if feasible — a case exercising the multiclass union); (4) `boundary/setSkillAllocations.ts` itself only needs the standard no-Tauri-runtime-throws case every other boundary file already has (see `addSpellSelection.test.ts` for the exact template).
    - **`boundary/characterBio.ts`** — thin wrapper, same shape as every other tested boundary file: (1) `loadCharacterBio` outside a Tauri runtime resolves to `BLANK_CHARACTER_BIO` rather than throwing (the one behavior specific to this file vs. the others — bio's absent-case is a resolved default, not an error); (2) `updateCharacterBio` outside a Tauri runtime throws the exact descriptive message, matching the `addSpellSelection.test.ts` template.
    - **Wizard spell-routing logic** — currently inline in `CharacterSheet.tsx`'s `handleAddSpell`, not an extractable pure function today (unlike the other three, which already have clean module boundaries). Recommend extracting the two decisions it makes — (a) resolve `primaryClassId` preferring a held Wizard over `heldClasses[0]`, (b) choose `recordAndPrepareSpellSelection` vs. plain `addSpellSelection` based on that class id — into one small pure function (e.g. alongside `skillsModel.ts` or a new `spellRoutingModel.ts`), then test: a Fighter-only build routes to plain `addSpellSelection`; a Fighter/Wizard multiclass resolves to Wizard (not `heldClasses[0]`) and routes to the atomic path; a Wizard-only build routes to the atomic path. This is a slightly bigger lift than the other three (needs the extraction first) — flagging that distinction rather than treating all four as equal-sized tasks.
    - **`abilityScoreMethods.ts`** (found this pass, not previously named) — real, currently-untested logic: `pointBuyCost` at the table boundaries (7 and 18) and one in-range value; `generateAbilityScorePool` for each non-random `kind: 'pool'` method returns the right fixed values (`eliteArray` → `[15,14,13,12,10,8]` exactly) and the right *count* (6) for the randomized ones without asserting exact values; `rollStraightAbilityScores` maps its six rolls onto `ABILITY_KEYS` in the documented Str/Dex/Con/Int/Wis/Cha order, not any other order. The reroll-if-weak retry loop's 200-attempt cap is a defensive bound, not itself easily testable without mocking `Math.random` — reasonable to leave unverified rather than force a brittle test around it.
    - **`portraitImageProcessing.ts`** — lower priority, flagged for completeness rather than urged: the pure validation logic (`ACCEPTED_PORTRAIT_TYPES.includes`, the size-limit check) is testable today with a plain fake `File` object and no DOM, but the actual crop/resize path needs a `<canvas>`, which this `tsx`-based test runner doesn't provide — either accept partial coverage (just the two validation early-returns) or treat as out of scope for this backlog pass.

    **Not built this pass, deliberately** — this is a checklist, not new test files; QA's lane is `tests/**` (Rust), these are all frontend `.ts` modules. Owner: frontend, whenever the current queue is clear.

    **Checklist fully executed (frontend, `f905d656`).** All five modules covered per the exact cases named above: `skillsModel.ts` (parenthetical/multi-word normalization, class-skill boundary, multiclass union), `boundary/setSkillAllocations.ts` + `boundary/characterBio.ts` (standard no-runtime-throws template, Bio's absent-case-resolves-to-default behavior specifically verified), Wizard spell-routing (genuinely extracted into a new `spellRoutingModel.ts::resolveSpellRouting`, all 3 named cases plus 2 more — an already-bootstrapped Wizard routing to the plain path, zero-held-classes resolving to `null` rather than a fabricated id), `abilityScoreMethods.ts` (point-buy boundaries, `eliteArray`'s exact values, Str/Dex/Con/Int/Wis/Cha key order verified via insertion order), and `portraitImageProcessing.ts`'s two DOM-free validation cases as scoped. Since the spell-routing extraction touched real shipped code (`CharacterSheet.tsx`'s `handleAddSpell` now calls the new pure function instead of its old inline logic), frontend treated it as non-risk-free and live-verified it against the real dev build: adding a spell to an already-bootstrapped Wizard persisted correctly through the plain path, no stray Prepared entry, correct on-disk shape. 6 new test files, 1 new source file, 69/69 suite green, typecheck clean.

    **Independently re-verified, all 5 modules, extra scrutiny on the one that touched shipped code (QA, 2026-07-24).** Read the `CharacterSheet.tsx` diff directly and confirmed the spell-routing extraction is faithful and behavior-preserving — `resolveSpellRouting`'s output feeds the exact same downstream branches the old inline logic did, and it correctly delegates the bootstrap decision to the already-tested `isWizardSpellBootstrap` rather than duplicating that logic. Ran all 5 test files individually rather than trusting the aggregate count; each matches the scoped cases precisely, including independently re-confirming `skillsModel`'s multiclass-union claim ("Bluff via Rogue") against the real `CLASS_SKILLS` data and the point-buy table values in `abilityScoreMethods`. One transient suite hiccup noted honestly (68/69 with no printed FAIL line on 1 of 4 runs, didn't reproduce on 2 subsequent clean runs) rather than either ignored or overreacted to. Clean bill, no gaps found.

26. **RESOLVED — Load Character list now refreshes its cached row on every sheet mutation.** Found by frontend while live-verifying item 23. Root cause (`2b13a23c`): `CharacterHubPage.tsx`'s `onDetailRefreshed` already rebuilt the open sheet's own `row` from the mutation's fresh summary, but the separate list `surface` state never got the same treatment — only a full reload refreshed it. Fixed with `replaceRowInSurface(surface, updatedRow)` in `buildCharacterHubListSurface.ts` (in-place patch by `characterId`, no-ops on an unknown id rather than fabricating a row), called alongside the existing `sheet.row` patch in the same callback — one mutation, both pieces of state updated together, no extra IPC round-trip. 2 new unit tests (matching-row update, unknown-id no-op). Live-verified: leveling a Wizard from 1→2 and reopening the Load list immediately showed "Elf Wizard 2," no app restart needed. 69/69 suite green, typecheck clean.

27. **OPERATOR QUESTION (surfaced by backend's item-1 design pass, 2026-07-24) — should `Computed` ever accept an equipment loadout other than the one exact hardcoded posture?** Not an engineering call: `unmet_combat_posture_conditions`/`unmet_selected_skill_posture_conditions` gate on exact `item_id` matches (`"item:longsword"`, `"item:chain_shirt"`), completely independent of whether the compute engine could correctly handle other gear. This means bridging the headless/corpus-aware architecture wall (item 1's fix shapes a/b) buys **zero** currently-displayed value on its own — every build that reaches `Computed` today already uses only the one hardcoded posture, so widening what the math *could* compute changes nothing unless this gate is separately widened to accept other item ids. This is a claim-gating philosophy question (how much loadout variation is "supported enough" to honestly claim `Computed`, given the no-stub/never-persist-unproven-build discipline this whole swarm runs on) — distinct from and prerequisite to any value from shapes (a)/(b). Shape (c) (backend's recommendation, see item 1) sidesteps this entirely by surfacing corpus-derived AC/ACP as a non-claim-gated display section instead of trying to widen the gate. Not resolved here; genuinely the operator's call.

    **OPERATOR DECISION (Todd Hintzmann, 2026-07-24) — widen the gate to accept any equipment.** Explicit call, made after the swarm-wide closure-readiness checkpoint (item 31): `unmet_combat_posture_conditions`/`unmet_selected_skill_posture_conditions` should stop gating on exact hardcoded `item_id`s and instead validate whatever loadout is actually selected. This is real, substantial engineering scope — effectively item 1's original fix shapes (a)/(b) (thread corpus through the compute pipeline, or move the claim gate to the corpus-aware layer), not a quick flip, and every equipment/weapon combination that newly reaches `Computed` needs to be verified genuinely correct, not just unblocked. **Not dispatched as a single bounded task** — given the scale (backend's own item-1 design doc already sized shape (a) at ~347 call sites), backend should first produce a scoping/sequencing plan (same shape as `item-1-architecture-wall-design.md`) before implementation starts, so this doesn't turn into an unbounded, un-verifiable mega-change. This decision also directly informs item 1's multi-weapon question below (an equipment-attachment schema is needed either way).

    **SCOPE DISCOVERY, backend stopped before coding (2026-07-24) — the gate can't be swapped in isolation without regressing into fabricated values.** Before writing sub-task 4, backend traced `unmet_combat_posture_conditions`/`unmet_selected_skill_posture_conditions`'s own gated functions (`compute_combat_baseline`'s `baseline_armor_class`/`baseline_melee_attack_bonus`, `compute_selected_skill_modifiers`'s climb/intimidate/swim) and found they are hardcoded arithmetic on compile-time constants (`ARMOR_CLASS_BASE + CHAIN_SHIRT_ARMOR_BONUS + ... `), not real per-item corpus resolution — zero corpus access anywhere in either function, by construction (the headless layer has no corpus parameter). **Why this matters**: naively widening only the gate check while leaving the arithmetic untouched would let a character in, say, a Breastplate instead of the fixed Chain Shirt reach `Computed` and then get served the *exact same hardcoded* numbers regardless of what they actually have equipped — not a completeness gap but a fabricated-value regression, the exact failure mode the no-stub-mvp doctrine exists to catch. Backend correctly asked whether this now needs the operator's eyes since it reads as a correctness question, not just an engineering one.

    **Lead ruling: no operator escalation needed — this is a doctrine-mandated implementation detail of the already-made decision, not a new policy question.** The operator decided the *policy* (widen the gate, replace it outright); they never authorized shipping fabricated numbers to get there, and the no-stub-mvp doctrine (`scope: universal`, standing, not re-litigated per-decision) already forbids it regardless of who decides. There is only one correct way to implement the operator's decision without violating that doctrine: move the arithmetic to real corpus-resolved computation. **Approved sequencing** (backend's own proposal): (1) new corpus-aware `baseline_armor_class`/`baseline_melee_attack_bonus`, sourced from `compute_equipment_effects`'s already-real `armor_class_delta`/`armor_check_penalty_total`/`attack_bonus_delta`/`to_hit_bonus` (sub-tasks 1/2's own output) — verified byte-identical against today's hardcoded formula for the existing fixed posture, which doubles as both the compatibility check and the correctness proof the corpus-resolved path is right; (2) same treatment for the selected-skill pillar; (3) only then swap the command-layer gate to consult the new path. `base_attack_bonus`/`base_saves` are unaffected (already class/level-generic, not posture-gated). Sub-task 4 is now meaningfully bigger than originally scoped — flagged here plainly rather than quietly absorbed, per this doc's own recurring practice.

    **Further boundary drawn, first slice scoped to armor/shield only (backend, 2026-07-24, lead-approved).** A second hardcoded dependency surfaced: `fighter_weapon_training_attack_bonus` (Weapon Training's contribution to melee attack bonus) is gated on the "Heavy Blades" weapon-training group, hardcoded because the fixed posture's Longsword happens to be a Heavy Blade. Widening the *weapon* to any resolvable item would require mapping an arbitrary weapon to its real PF1 weapon-training group — a distinct, real data-mapping problem, not something already built. **This slice's boundary**: widen armor/shield only (any resolvable `arms_armor`-category item or genuine absence, real `armor_class_delta`/`max_dex_cap`/`armor_check_penalty_total`), leave the weapon requirement exactly as today (must still be the Longsword, keeping Weapon Training's group-matching valid) and feat requirements exactly as today (a separate, undecided question from the already-settled "never block on an unrecognized feat *effect*" policy). Still a real, meaningful widening — a Fighter could wear Breastplate/Full Plate or go shieldless and get genuinely correct AC. Weapon-loadout widening is a distinct, later increment. Same "prove the narrow case first" discipline as Toughness-before-widening and the single-weapon-before-per-weapon slice; lead agrees, no operator input needed.

    **Both pillars built and lead-verified, not yet committed (backend, 2026-07-24).** New `compute_combat_baseline_from_corpus`/`compute_selected_skill_modifiers_from_corpus` in `pilot_compute_corpus.rs`, real per-item resolution via `compute_equipment_effects`. Exposed ~29 previously-private `pilot_compute.rs` helpers/constants as `pub(crate)` to avoid duplicating the level-based Weapon/Armor Training math and posture constants — verified via `git diff` that each is a pure visibility-prefix addition, zero behavior change. **The correctness proof**: one test drives both the old hardcoded path and the new corpus-aware path through the identical fixed posture at every Fighter armor/weapon-training breakpoint (levels 1/3/5/7/9/11/15/20) plus Wizard and Rogue level 1, asserting byte-identical `melee_attack_bonus`/`armor_class`/`climb`/`intimidate`/`swim` — passes with no discrepancy. 7 more tests prove the real widening: a Breastplate computes its own correct AC; an equipped shield (previously required-absent) adds its real bonus; no armor computes a real unarmored AC; a real enhancement attached to the required Longsword (connecting sub-tasks 1/2/6 end to end) raises the attack bonus by its exact real value; a second weapon and an unresolvable item are both honestly `Blocked`, not guessed. **Lead independently ran** the new 8-test module (`corpus_aware_posture_widening_tests`): 8/8 pass, matches exactly. Not yet wired into any command (sub-task 5) or committed as of this check.

    **Sub-task 6 landed both halves and lead-verified (backend `3a05539a` + frontend `a768ad85`, 2026-07-24).** Frontend wired the Gear tab: an "Attach Modifier" button per equipped item opens the existing `ItemPickerModal` scoped to Equipmods; new `attachEquipmentModifier.ts` mirrors `purchaseEquipment.ts`'s exact shape; `ResolvedEquipmentDto` gained `appliedModifiers` for nested display. Live-verified end to end against a real character: attached both an unknown-cost enhancement (free-attach path) and a known-$0-cost Masterwork modifier (a different code path) to two different items, both persisted correctly to disk targeting the right item, both survived a reload, both correctly surfaced via the unresolved-corpus indicator since the demo corpus has zero Equipmods records to resolve against. **Honest limitation noted by frontend**: the nested-nested-modifier-under-its-weapon display path is code-correct but not live-reachable today given the empty demo corpus — same "structurally correct but currently unreachable" shape as Barbarian DR and the Wizard-bootstrap branch noted elsewhere this session. **Lead independently verified**: full workspace 4284/4284 (unaffected — frontend's changes are TS-only), desktop 211/211, frontend suite 78/78 (up from 77), typecheck clean. Sub-task 6 fully closed.

28. **NEW FINDING (QA, 2026-07-24) — no unequip/remove-equipment affordance exists anywhere in the app; a 0-weapons character state is unreachable through the live UI.** Surfaced while trying to live-verify the null-serialization fix's 0-weapon case: `compose_character_input` always equips exactly one weapon (Longsword) at creation for every class, and there is no "unequip"/"remove" control anywhere in the Gear tab or elsewhere. The 2+-weapons case is reachable (Add Weapon twice); 0-weapons is not reachable by any live path today. Not blocking the null-serialization verification itself (proven at the wire/test level instead, see the note under item 17's row/task table), but a real, previously-undocumented UI completeness gap in its own right — flagged here rather than silently worked around. Not attempted this wave; future UI-affordance backlog.

29. **ENVIRONMENT NOTE (QA, 2026-07-24) — the live-UI screenshot pipeline in this container is now recurringly broken, not a one-off flake.** Second occurrence today (first was item 17's Defense-tab check): `driver.sh` launches the app cleanly (correct window, correct title) but every screenshot comes back solid black, with `libEGL warning: DRI3 error: Could not get DRI3 device` in the launch log both times. `LIBGL_ALWAYS_SOFTWARE=1`/`WEBKIT_DISABLE_COMPOSITING_MODE=1` were tried as workarounds and made no difference (likely not forwarded through to the `tauri dev` child process). Not chased further per standing practice (static/test-level verification substituted, same precedent as items 9/17), but now reads as a standing container graphics-stack limitation rather than a flake — worth a driver/environment-level fix if live-UI passes are needed going forward, since it's blocking every recent attempt.

30. **RESOLVED — frontend closure-readiness pass, one real pre-existing gap found and fixed.** Ran a full sanity baseline (typecheck, 77/77 tests, production build, `cargo check`) plus a grep-based four-check doctrine sweep (TODO/FIXME/stub tokens, empty `() => {}` handlers, "would-X" strings, test-mocks in shipping modules) across `apps/desktop/src/**` — all clean. Confirmed tab completeness (§1.5): only Pets and Overrides fall through to "coming soon," both already-recorded deliberate non-goals, not silent stubs. **Real gap found and fixed (`c014ffec`)**: `WeaponsTab`'s own "Print" button (pre-existing since 2026-07-09/SD-23, not introduced by this swarm) had no `onClick` at all — a genuine no-empty-handlers doctrine violation the grep sweep's "empty arrow function" pattern couldn't catch, since a *missing* handler prop doesn't match that shape. The top-menu's own "Print" a few lines away already does the real thing (`window.print()`); wired the tab button to the same call. Swept the rest of `apps/desktop/src/**/*.tsx` for the identical shape (a `<button>` with no `onClick` and no `type="submit"`) and found no other instances. **Lead-verified independently**: 77/77 tests, typecheck clean, diff is exactly the 1-line fix claimed. Live end-to-end pass covered a genuine multiclass level-up, bio persistence, money/equipment purchase, and skill allocation — all held up clean; skill allocation's exact-posture scoping (Climb/Intimidate/Swim rank-1 only) is confirmed honest `Blocked` behavior, not a bug, consistent with item 27's framing. **Noted, not fixed, not new**: `WeaponsTab`'s per-weapon stat table never populates — this is the already-tracked item 1 architecture gap (AC/attack-bonus math), not a separate defect. Frontend's queue is clean beyond this one fix.

31. **CHECKPOINT FINDING (lead, 2026-07-24) — bounded, swarm-actionable backlog is exhausted; everything remaining is operator-gated or future-epic.** Both backend's and frontend's closure-readiness passes (this item and item 30) came back clean: frontend found and fixed exactly one real pre-existing gap; backend found genuinely nothing across a full workspace test+build (4271/4271 root-crate, 204/204 desktop), a fresh DTO-consumption re-check of everything landed today, and a formal 4-check no-stub-mvp doctrine audit against the *entire* `develop...tranche/6` diff (not just today's work) — 0 hits on all four checks. **Lead independently re-ran the doctrine grep checks (0 hits, matches) and the full workspace test suite.** Cross-checked against SWARM_TASKS.md: every remaining open row is one of — `blocked` on an equipment-attachment schema decision (item 1's multi-weapon case), `blocked` on an explicit operator decision (item 27's posture-gate widening), `blocked`/deferred as a documented completeness gap not assigned this wave (item 18), or `blocked` as multi-cycle future-epic scope (8-class breadth, 12-class starting wealth) — nothing left that is both bounded and swarm-actionable without further operator input. This is a real checkpoint, not a stall: the swarm hasn't run out of things to *do* in the sense of being stuck, it has reached the edge of what this wave's scope can close without an operator call on items 1/18/27 or a decision to open a new, larger wave (class/multiclass breadth). Flagged here plainly per standing guidance rather than manufacturing further busywork.
