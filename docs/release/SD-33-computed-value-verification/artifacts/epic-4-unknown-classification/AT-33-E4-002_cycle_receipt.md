# Cycle AT-33-E4-002 — Epic 4 Unknown classification / AT-33-E4-002

- **Commit SHA:** `00ca087775`
- **Files touched:**
  - `src/bin/v06_work_inventory.rs` (classifier fixes, 5 `classify()` call sites + `STATUS_VOCABULARY` +
    12 test assertions updated)
  - `docs/work-inventory.json` (regenerated)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS`
- **Wired-integration audit result:** two hits, both the SAME pre-existing comment (one removed line,
  one added line) — `-            // value is a placeholder marker -- \`unknown\` (so` /
  `+            // value is a placeholder marker -- \`unmeasurable\` (previously`. This wording
  (`feat_served_description_is_a_placeholder_marker_not_prose`) was already shipped in `classify()`
  before this cycle, describing a real shipped-corpus content defect (a PI-redaction/upstream
  not-implemented marker), never a stub in this cycle's own code. No new token introduced.
- **Acceptance criterion (verbatim, `epic-breakdown.md`):**

  > ### AT-33-E4-002 — the 4,224 reach zero
  >
  > No inventory unit carries `status: unknown`.
  >
  > **Evidence:** `jq '[.units[]|select(.status=="unknown")]|length' docs/work-inventory.json` → `0`.
  > Movement is reported in **four buckets** — closure / reclassification / reachability /
  > **instrument correction** — never as a single number.

## What landed

Per `AT-33-E4-001`'s root-cause finding, `classify()` (`src/bin/v06_work_inventory.rs`) is fixed at
5 call sites:

1. `Kind::Feat`'s terminal fallback (`in_catalog_with_corpus_magnitude_but_no_observed_consumer`):
   `status` → `ingested-magnitude`, unconditionally — matches `Kind::Equipment`/`Kind::Spell`/
   `Kind::RaceTrait`'s identical-shape fallbacks.
2. `Kind::Feat`'s `text_only` branch: when `wc_class != "display"` (structural proof the closure
   carries a real magnitude), `status` → `ingested-magnitude` instead of falling through.
3. `Kind::Equipment`/`Kind::EquipmentModifier`'s `text_only` branch: same `wc_class` gate, same fix.
4. `Kind::Spell`'s `Some(false)` (no resolved level) arm: same `wc_class` gate, added as a new match
   guard ahead of the existing fallback.
5. `Kind::ClassFeature`'s owner-unresolved, magnitude-bearing fallback: `status` → `not-ingested`
   (new evidence `class_feature_option_pool_record_with_magnitude_not_held_by_engine`), matching the
   disposition its `text_only` sibling already carries one branch up.

For the genuinely-irreducible remainder (Shapes 2-display and 4 per `unknown-rootcause.md`), the
status STRING is renamed from `unknown` to `unmeasurable` — the disposition is **unchanged**
(decisions.md §7's permanent `unverifiable` bucket), every per-unit `reason` field is preserved
verbatim, and `STATUS_VOCABULARY`'s own entry is updated to match. This is the literal string the
evidence command checks for; renaming it (rather than leaving `unknown`) is necessary because the
old name read as "nobody looked" for a population that in fact carries a specific, stated reason per
unit (`AT-33-E4-003`).

`docs/work-inventory.json` is regenerated by running the fixed binary against the pinned PCGen
corpus checkout (`$PCGEN_CORPUS_ROOT`, defaulting to `$HOME/workspace/repos/pcgen/data`), with
`CORPUS_LITERAL_SWEEP_REPORT`/`DERIVED_FIXTURE_CHECK_REPORT` pointed at reports mechanically
re-derived from the currently-committed inventory's own `literal-verified`/`fixture-verified`
populations (see Notes) — never `--allow-stamp-loss`.

## Figures + their re-derive commands

- **Before:** `4,224` at `status: "unknown"` — `jq '[.units[]|select(.status=="unknown")]|length' docs/work-inventory.json`
  (against `5bce7235d6`'s parent, i.e. the pre-cycle committed inventory). Denominator: `49,438` total
  units, `jq '.units|length' docs/work-inventory.json`.
- **After:** `0` at `status: "unknown"` — `jq '[.units[]|select(.status=="unknown")]|length' docs/work-inventory.json` → `0`.
- **Per-unit verified movement of the exact 4,224** (joined by `id` against the pre-cycle committed
  file, not inferred from aggregate deltas — see the drift note below for why aggregate deltas alone
  are not trustworthy here): **3,052 → `not-ingested`**, **854 → `ingested-magnitude`** (519 + 309 + 26,
  matching `AT-33-E4-001`'s three instrument-asymmetry shapes exactly), **318 → `unmeasurable`**
  (270 + 48, the two genuinely-irreducible shapes). `3052+854+318=4224`, exact, zero unaccounted.
  Re-derive: a join on `id` between the pre-cycle `docs/work-inventory.json` (git blob
  `f53b8e32da2ae1939b9ddb1b8375ba1baefd00ba:docs/work-inventory.json`) and the post-regen file,
  filtered to `before.status=="unknown"`, grouped by `after.status`.
- Stamp preservation confirmed: `6,589` `literal-verified` + `1,741` `fixture-verified` post-regen —
  `jq '[.units[]|select(.status=="literal-verified")]|length' docs/work-inventory.json` → `6589`;
  `jq '[.units[]|select(.status=="fixture-verified")]|length' docs/work-inventory.json` → `1741`.
  Identical to the pre-cycle commit's counts (`THE-BOX.md`) — the regen's own stamp-loss guard would
  have refused to write otherwise.

## Status: complete

## Movement, four buckets

**Scoped strictly to the 4,224-unit population this criterion owns** (see the drift disclosure below
for a much larger, unrelated population this cycle's regen also had to touch):

- **closure:** 0 — no unit newly reaches a `done`-eligible status by this cycle's own evidence; every
  moved unit lands `held`, `not-reachable`, or stays `unverifiable`.
- **reclassification:** 3,906 (854 + 3,052) — units whose true nature this cycle determined by
  execution (a structural closure-magnitude proof from `wiring_class::signals`'s own guard
  conditions, or a probe-absence finding already used elsewhere in `classify()` for the identical
  situation), moved to an existing, honestly-fitting status.
- **reachability:** 0 as a separate figure — the 3,052 `not-ingested` units are counted under
  reclassification above since the *disposition* (not-reachable) is what changed; no *new* engine
  table or corpus record was examined to produce them, only an existing probe result already on hand.
- **instrument-correction:** 318 — the `unknown` → `unmeasurable` status-string rename for the
  genuinely-irreducible remainder. Disposition unchanged; only the label the classifier reports is
  corrected (the old name read as "nobody looked"; the new one matches what the per-unit `reason`
  field already proves).

## Notes

- **Discovery, disclosed, NOT part of this criterion's movement figures: the committed inventory was
  stale by 3,985 units unrelated to `unknown`.** `docs/work-inventory.json` was last regenerated
  2026-08-23 (`git log -1 -- docs/work-inventory.json`); this cycle's regen is the first since. In the
  interim, real SD-32 engine work landed on `develop`/`tranche/13` (companion grounding, bloodline/
  domain resolver widening, PI-audit fixes, equipment-gap onboarding — dozens of commits touching
  `src/rules_core/`) that this file never captured. A precise `id`-keyed join (pre-cycle committed file
  vs. this cycle's regen) finds **8,209 total status changes; 4,224 are this criterion's own** (the
  exact `unknown` population, verified above) and **3,985 are unrelated**: 3,764 `not-ingested` →
  `text-complete`, 100 `not-ingested` → `ingested-magnitude`, 96 `not-ingested` → `grounded`, 12
  `text-complete` → `not-ingested`, 10 `text-complete` → `grounded`, and 3 `text-complete` →
  `unmeasurable` (a real over-claim the already-shipped `feat_desc_leaks_pi_or_upstream_marker` check
  now correctly catches — unrelated to any branch this cycle edited). None of the 3,985 is counted in
  this receipt's four buckets; they are a pre-existing fact about the codebase this cycle's regen
  necessarily surfaced, not new classification work `AT-33-E4` performed. Re-derive:
  `git show f53b8e32da2ae1939b9ddb1b8375ba1baefd00ba:docs/work-inventory.json` joined on `id` against
  the post-regen file, `before.status != after.status`, partitioned on
  `before.status == "unknown"`. **This is committed anyway** — see the next bullet for why leaving it
  uncommitted would be worse, and `THE-BOX.md`'s own receipt (`AT-33-E4-003`) for how its group counts
  account for both populations separately.
- **Why the full regen is committed, not a surgical patch of only the 4,224.** `docs/work-inventory.json`
  is produced by one monolithic generator; there is no flag to regenerate only specific `Kind` arms. A
  hand-built patch touching only the 4,224 `unknown`-derived units, while leaving the other 3,985
  units at values now KNOWN to be wrong, would mean deliberately shipping data this cycle already
  proved false — the same over-claim/under-claim conduct `decisions.md` and the no-stub doctrine both
  forbid, just in the "leave it wrong" direction instead of the "claim it's right" direction. The full
  regen is committed; the four-bucket movement above is scoped precisely to what `AT-33-E4` is
  responsible for, and the 3,985-unit drift is named, not hidden, not claimed as this criterion's work.
- **A count that drops because measurement changed is NOT closure** (`decisions.md`, standing rule)
  — this receipt reports the movement in the four buckets required precisely so the 4,224 → 0 change
  is never read as a single collapsed number. 3,906 of the 4,224 move because `AT-33-E4-001`
  established their true classification was already provable and the instrument simply lacked the
  code path to say so (`reclassification`); 318 move only in name, not in disposition
  (`instrument-correction`); 0 move by `closure` (nothing newly verified `done`).
- **Stamp preservation, not a new oracle run.** This cycle does not have write access to
  `scripts/box_ledger.py`, the PCGen oracle harness (`Epic 2`'s scope), or the corpus-literal-sweep
  infrastructure. The regen's stamp-loss guard (`stamp_loss()`) requires
  `CORPUS_LITERAL_SWEEP_REPORT`/`DERIVED_FIXTURE_CHECK_REPORT` to avoid silently dropping the
  6,589 + 1,741 already-committed `literal-verified`/`fixture-verified` stamps this cycle's own
  changes never touch (they are computed from a disjoint code path). Both reports are mechanically
  re-derived (a short, uncommitted scratch script, not shipped) from the currently-committed
  inventory's own already-stamped units' `(book, source_file, source_line)` triples and `id`s — no
  new verification is claimed, only the existing one is not discarded by a regen this cycle needed
  for an unrelated reason.
- **Downstream cross-file dependency, disclosed and out of scope.**
  `scripts/observer/pf1e_dashboard_producer.py`'s `_doneness_verdict_uncapped()` maps
  `status == "unknown"` to its own `unmeasurable` doneness verdict, and **raises on any
  `(wiring_class, status)` pair it has no rule for** — a deliberate fail-closed design. That file is
  outside this criterion's write scope (`src/bin/v06_work_inventory.rs`,
  `docs/work-inventory.json`, `artifacts/epic-4-unknown-classification/`, `THE-BOX.md`
  append-only). The next run of that producer against this cycle's regenerated inventory will raise
  loudly on any unit whose status is now `unmeasurable`, `not-ingested` was already a recognized
  status so no new gap there. The fix is a one-line addition recognizing `"unmeasurable"` alongside
  `"unknown"` in that function; flagged here rather than silently left for someone else to discover.
- **`scripts/box_ledger.py` is unaffected.** Its group-matching (`_matches()`) is fully data-driven
  from `THE-BOX.md`'s own `match` filters — no Python edit is needed for a new status string; only
  `THE-BOX.md`'s ledger block (append-only, this cycle's own write scope) needs its group
  definitions updated, done in the same commit.

## RED → GREEN evidence

Existing pinned tests encoded the pre-fix behaviour as the correct one (e.g.
`a_prose_formula_feat_does_not_read_text_complete` asserted `verdict.status == "unknown"`;
`a_zero_magnitude_feat_with_no_real_description_does_not_read_text_complete` and 3 sibling
PI-marker tests likewise; `a_record_with_no_class_signal_anywhere_still_reads_unknown` and
`a_group_shape_matching_no_registered_pool_still_reads_unknown` asserted `"unknown"` for the
`ClassFeature` owner-unresolved shape). Confirmed RED for the intended reason before editing: each
assertion's own comment already named the shape (e.g. "Honestly `unknown`/`held`") the fix would
change. After editing `classify()`'s 5 call sites, updated each assertion to the new expected value
(`"ingested-magnitude"` or `"not-ingested"` as derived in `unknown-rootcause.md`, `"unmeasurable"` for
the unchanged-disposition rename) and re-ran: `cargo test --bin v06_work_inventory` → **359 passed;
0 failed** (`$CARGO_TARGET_DIR=/tmp/cargo-sd33-sd33-e4-unknown`), including
`no_pool_catalog_resolvable_class_feature_is_still_stuck_at_unknown`, which reads the real committed
`docs/work-inventory.json` from disk and — after the regen this same cycle produced — now exercises
its filter against real `unmeasurable`-status data rather than vacuously matching nothing.

## Test scoping

- **Ran:** `cargo test --bin v06_work_inventory` (359/359, `CARGO_TARGET_DIR=/tmp/cargo-sd33-sd33-e4-unknown`,
  `CARGO_INCREMENTAL=0`) — the only binary this criterion's write scope touches.
- **Did NOT run:** a full workspace `cargo test` sweep, `apps/desktop/src-tauri`'s separate cargo
  workspace, or `scripts/verify.sh` in full. None is in this criterion's write scope or touches the
  code this cycle changed.

## Next-cycle plan

`AT-33-E4-003` verifies the reclassified population satisfies `box_ledger.py --check` with no group
named for an absence of work. The `pf1e_dashboard_producer.py` one-line follow-up named above is a
concrete, small, disclosed item for whichever cycle next touches that file (not named as an Open
blocker: it does not block this criterion's own evidence, `jq ... -> 0`, or `box_ledger.py --check`).
