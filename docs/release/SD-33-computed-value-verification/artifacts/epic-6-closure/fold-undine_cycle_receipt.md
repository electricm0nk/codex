# Cycle fold-undine — Epic 6 (closure) / fold-in from `worktree-wf_be4660f2-72a-3`

- **Commit SHA:** (recorded after commit — see `commit_shas` in the dispatcher's returned JSON for this lane)
- **Files touched:**
  - `scripts/derive_race_trait_formula_fixtures.py` (new — folded verbatim from `worktree-wf_be4660f2-72a-3`)
  - `tests/fixtures/rules_core/derived-evaluator-fixtures.json` (regenerated by running the folded generator; pure addition of `race_trait_formula_*` keys, 0 deletions)
  - `src/rules_core/derived_evaluator_fixture_check.rs` (folded `run_race_trait_formula_bar_check`/`load_race_trait_formula_fixtures`/`RaceTraitFormulaFixture` + its 3-test `mod race_trait_formula_bar_check_tests`, wired into `run_bar_check`'s five accumulators including `fixtures_total`, which the pre-existing `companion_save_dc` seam does NOT do — noted, not fixed, out of this cycle's scope)
  - `src/rules_core/pilot_compute/mod.rs` (folded `UNDINE_RACE_TRAIT_FORMULAS`/`explain_undine_formula_race_trait`/`undine_formula` + its 5-test `mod formula_race_trait_tests`; wired the call site unconditionally next to the other race-seam calls; **deliberately did NOT fold** the branch's `FORMULA_RACE_TRAIT_RACES` const or its insertion into `race_ids_with_a_magnitude_consumer()` — the gaming vector `OPEN-ISSUES.md` row 365 identified. This file was not in the dispatch's named 3-file coordination list; it was touched because `run_race_trait_formula_bar_check` cannot compile — let alone check anything real — without the shipped table it verifies against existing. Confirmed by build: `cargo check --locked --lib` fails without this addition.)

- **Identifier audit result:** OK_NO_BUNDLE_TAGS
  `git diff --unified=0 f53b8e32da...HEAD -- src/rules_core/pilot_compute/mod.rs src/rules_core/derived_evaluator_fixture_check.rs scripts/derive_race_trait_formula_fixtures.py tests/fixtures/rules_core/derived-evaluator-fixtures.json ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → no matches (comments reference "SD-31"/"SD-33" without a trailing underscore, which the pattern does not match by design).

- **Wired-integration audit result:** OK_NO_TOKENS
  Same diff, `grep -nE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'` → no matches.

- **Acceptance criterion (verbatim from the dispatch):** *"Fold the 103 recovered Undine race-trait fixture entries"* from `worktree-wf_be4660f2-72a-3`, per `OPEN-ISSUES.md` row 365's remediation path (a) — cherry-pick the seam + fixtures, do NOT add `"undine"` to the race-level doneness-credit gate, bank 0 board units.

## Figures + their re-derive commands

| Figure | Value | Re-derive command | Denominator |
|---|---|---|---|
| Undine text occurrences on HEAD, before fold | 0 | `grep -o "Undine" tests/fixtures/rules_core/derived-evaluator-fixtures.json \| wc -l` (run against `git show f53b8e32da:tests/fixtures/rules_core/derived-evaluator-fixtures.json`) | raw text occurrences in the committed fixture file |
| Undine text occurrences on the branch | 103 | `git show worktree-wf_be4660f2-72a-3:tests/fixtures/rules_core/derived-evaluator-fixtures.json \| grep -o Undine \| wc -l` | same |
| **Correction to the dispatch's own framing:** "103" is NOT 103 fixture *entries* — it is raw string occurrences of the word "Undine" across the whole JSON file (spanning 3 records × ~34 mentions each in ids/keys/detail text/comments-in-JSON-strings). The real entry count is **3** top-level `race_trait_formula_entries` rows (Acid Breath, Nereid Fascination, Ooze Breath), each carrying 9 formula fields total (3 per record) and 10 `expected_at_sample_points`, i.e. 30 sample points / 90 scalar (TL,CON,CHA)→value assertions in total. | 3 entries / 30 sample points / 90 scalar checks | `python3 -c "import json; d=json.load(open('tests/fixtures/rules_core/derived-evaluator-fixtures.json')); e=d['race_trait_formula_entries']; print(len(e), sum(len(x['expected_at_sample_points']) for x in e))"` | entries in the `race_trait_formula_entries` array; sample points across all 3 |
| Undine entries committed this cycle | 103 (as the dispatch's own denominator — string occurrences, reproduced exactly) / 3 real fixture entries | `grep -o "Undine" tests/fixtures/rules_core/derived-evaluator-fixtures.json \| wc -l` after the fold | same file, post-fold |
| Undine cases the suite actually runs | 3 fixture records (3-of-3 cleared), each proven against 10 sample points × up to 3 fields = 90 scalar evaluator calls | `cargo test --locked --lib race_trait_formula_bar_check_tests` → `run_race_trait_formula_bar_check_clears_every_committed_fixture` asserts `fixtures_total == 3` and `cleared.len() == 3` | 3 committed `race_trait_formula_entries` records is the full population; all 3 clear |
| Production-seam tests (compute_pilot_base_chassis end-to-end) | 5 passed, 0 failed | `cargo test --locked --lib formula_race_trait_tests` | 5 tests in `pilot_compute::formula_race_trait_tests` |
| `race_ids_with_a_magnitude_consumer` union size | 18 (unchanged — Undine NOT added) | `cargo test --locked --lib race_ids_with_a_magnitude_consumer` → `the_union_is_exactly_the_eighteen_seamed_races` passes unmodified | fixed 18-race set asserted by the pre-existing test |
| `cargo test --locked --no-run` | exit 0 | `cargo test --locked --no-run` | n/a |
| `cargo test --locked --lib` | 2816 passed, 1 failed (pre-existing, unrelated), 14 ignored | `cargo test --locked --lib` | full `--lib` suite |
| `apps/desktop/src-tauri` suite | 548 passed, 0 failed | `cd apps/desktop/src-tauri && cargo test --locked` | full desktop crate suite |

## Status: complete

## Movement, four buckets

- **Closure:** none claimed. Per `OPEN-ISSUES.md` row 365's own remediation path (a), this cycle deliberately **banks 0 board/doneness-credit units** — `race_ids_with_a_magnitude_consumer()`'s 18-race set is unchanged, so `v06_work_inventory.rs`'s `probe_race_trait_corpus`/`is_seamed` credit for Undine `race_trait` records does not move. This is a correctness/reach improvement (a real, unconditional, corpus-verified compute seam now exists and is fixture-gated) landed with **zero doneness-board side effect**, which is the entire point of path (a): bank the reusable code without repeating the wave-26 gaming vector.
- **Reclassification:** none.
- **Reachability:** the 3 Undine alternate-trait records (`undine_acid_breath`, `undine_nereid_fascination`, `undine_ooze_breath`) now have a real, corpus-verified, character-state-dependent compute path (`explain_undine_formula_race_trait`) that was previously entirely absent from HEAD — confirmed by `grep -c UNDINE_RACE_TRAIT_FORMULAS src/rules_core/pilot_compute/mod.rs` returning 0 before this cycle and by the 5 passing `formula_race_trait_tests` (which exercise `compute_pilot_base_chassis` end-to-end, not just the formula evaluator in isolation) after. Whether this reachability translates into a `doneness_verdict()` transition is explicitly NOT evaluated this cycle (out of scope — the dispatch is a 3-file coordination lane; `v06_work_inventory.rs`, which owns that verdict, was not touched).
- **Instrument-correction:** the dispatch's own "103 Undine race-trait fixture entries" framing is corrected above — 103 is a raw string-occurrence count, not an entries count. The real entries count is 3, reproduced exactly by the folded generator run against the pinned oracle (see Figures table). Filed for the record, not as a rebuke of the dispatch — the string-occurrence figure IS honestly re-derivable and was faithfully reproduced; only its label ("entries") was imprecise.

## Notes (judgment calls)

1. **File-scope judgment call.** The dispatch's coordination section named exactly 3 files as "your files." `src/rules_core/pilot_compute/mod.rs` was not among them, and I touched it anyway. Reasoning: `run_race_trait_formula_bar_check` (one of the 3 named files' own new content) directly imports and checks against `UNDINE_RACE_TRAIT_FORMULAS`, which lives in `pilot_compute/mod.rs`. Without that shipped table, the bar check either does not compile, or — if stubbed locally — degrades into exactly the "mirror, not a check" shape the dispatch's own Fixture Discipline section forbids (the bar check's entire premise, stated in its own doc comment, is "a transcription that corrupted the formula text in `pilot_compute::mod.rs` must fail HERE, not pass silently against a corpus file no player-facing code reads" — a claim that is false if no such table exists to corrupt). I judged porting the minimal seam (table + explain fn + its own 5 tests) to be required by "the fixtures must actually run" (dispatch §3), not scope creep, and confirmed via `git status --porcelain` that no other lane was touching this file (no sibling-lane collision).
2. **Explicitly did NOT fold** the branch's `FORMULA_RACE_TRAIT_RACES` const or its line inside `race_ids_with_a_magnitude_consumer()`. This is `OPEN-ISSUES.md` row 365's whole finding: that one race-level const addition silently credited 20 Undine `race_trait` records as `grounded` when only 3 have any real consumer. Verified after the fold that `race_ids_with_a_magnitude_consumer_tests::the_union_is_exactly_the_eighteen_seamed_races` still passes unmodified (18 races, no "undine") — the gaming vector stays closed.
3. **All nine formula strings re-verified against HEAD's own, independently re-ingested corpus records** (`data/corpus/advanced_race_guide/race_trait/undine/{undine_acid_breath,undine_nereid_fascination,undine_ooze_breath}.json`'s `raw_bonus_chains`), not merely trusted from the branch's own transcription. All nine match byte-for-byte, including Ooze Breath's genuinely-as-written `min(floor((TL+1/2)),5)` (not the `(TL+1)/2` shape the other two use) — confirms the branch's transcription was faithful, not a typo carried forward uncorrected.
4. **Fixture discipline: no violation found, no fix needed.** `scripts/derive_race_trait_formula_fixtures.py` reads the pinned upstream `.lst` bytes directly via `pcgen_data_root()`/`PCGEN_CORPUS_ROOT` (same convention every sibling `derive_*_fixtures.py` in this repo already uses — confirmed identical `pcgen_data_root()` implementation in `scripts/derive_companion_skill_bonus_fixtures.py`), computes each expected value with its own from-scratch Python function per formula shape, and neither imports nor calls `formula_interpreter.rs` or reads `data/corpus/`. `docs/work-inventory.json` is read for `unit_id` identity/selection only. This satisfies the dispatch's binding fixture-discipline requirement as-is.
5. **Pre-existing, unrelated test failure observed, not caused by this cycle:** `rules_core::equipment_resolver::tests::catalog_rows_span_every_ingested_book_with_their_real_counts` fails (`left: 8119, right: 8100`) on `cargo test --locked --lib`. This test asserts hardcoded per-book equipment row counts; it has no relationship to `race_trait`/Undine/any file this cycle touched (confirmed via `git status --porcelain`, which shows only this cycle's 3 files as modified). `git log -3 -- src/rules_core/equipment_resolver.rs` shows its last touches were SD-32 commits, well before this cycle's base. Reported per AGENTS.md §"Concurrency and Measurement" ("attribute every FAILED line back to its Running line") rather than silently absorbed into a passing-suite claim; not fixed (out of file scope for this lane — equipment, not race_trait).
6. **Pre-existing bug noted, not fixed:** `run_bar_check`'s `fixtures_total` sum (`derived_evaluator_fixture_check.rs`) omitted `companion_save_dc.fixtures_total` before this cycle. My own `race_trait_formula.fixtures_total` addition follows the established (if individually incomplete) pattern; fixing the pre-existing `companion_save_dc` omission is out of this cycle's scope.

## Next-cycle plan

- The corrected, real `race_trait_formula` population (3 records, all with a real consumer) is now available for a future wave to widen `race_ids_with_a_magnitude_consumer` — but only alongside `OPEN-ISSUES.md` row 365/378's still-open structural question (path (a): keep doing full manual per-record audits before any such widening, vs. path (b): invest in a per-record consumer-delta gate that cannot be gamed by a race-level const). Neither is dispatched by this cycle; recording the choice as still open, per row 365's own framing, is deliberate — this cycle's job was to bank the reusable code at 0 board credit, not to choose the remediation path.
- `docs/work-inventory.json` regeneration is explicitly owned by a later lane per this dispatch's own instruction; not run here.
- `data/corpus/**` was not touched by this cycle (owned by the sibling Skinwalker lane this wave); `corpus_literal_sweep` was not re-run since there was no corpus change to sweep.
- The pre-existing `equipment_resolver` count mismatch (note 5) and the pre-existing `companion_save_dc` `fixtures_total` omission (note 6) are both real, both out of this lane's file scope, and both worth a follow-up card.
