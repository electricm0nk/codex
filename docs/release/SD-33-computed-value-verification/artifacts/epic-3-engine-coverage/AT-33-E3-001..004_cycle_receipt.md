# Cycle AT-33-E3-001..004 — Epic 3: Engine coverage / AT-33-E3-001, AT-33-E3-002, AT-33-E3-003, AT-33-E3-004

- **Commit SHA:** recorded in a follow-up docs commit on this same cycle branch (this receipt is written before the commit that carries it; re-derive via `git log --oneline -1 -- src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/`)
- **Files touched:**
  - `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` (modified — census source switched from SD-32's frozen file to a fresh `scripts/shape_ledger.py` regeneration; two new tests)
  - `docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/coverage-gap-rootcause.md` (new — AT-33-E3-001)
  - `docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json` (new — AT-33-E3-004's regenerated SD-33-path artifact; SD-32's `artifacts/gate-2-engines/formula_interpreter.corpus-wide.json` was never touched, confirmed by `git status --porcelain` showing no diff on that path throughout this cycle)
  - `docs/release/SD-33-computed-value-verification/THE-BOX.md` (append-only note, no group/count changed — no unit's `docs/work-inventory.json` `status` moved; this cycle never writes that file)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
  - Both re-run on the final diff: `git diff --unified=0 f53b8e32da2ae1939b9ddb1b8375ba1baefd00ba -- src/rules_core/pilot_compute/formula_interpreter.rs src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs src/bin/formula_interpreter.rs docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/ | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → no match (`OK_NO_BUNDLE_TAGS`); same diff `| grep -nE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'` → no match (`OK_NO_TOKENS`). The untracked new JSON artifact (not in `git diff`, since it has no base version) was separately greped directly: `grep -inE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b' docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json` → no match.

---

## AT-33-E3-001 — the cause of the 41% is diagnosed before anything is run

- **Acceptance criterion (verbatim, epic-breakdown.md):**
  ```
  41% coverage is a symptom. A committed root-cause statement explains why
  6,854 units were never run — per family, since the gap is uneven (F1 28%,
  F8 21%, F2 64%). Evidence: artifacts/epic-3-engine-coverage/coverage-gap-
  rootcause.md, with the coordinates of sampled units traced concretely.
  Generic pass, not per-object lanes — the analysis is by mechanism.
  ```
- **Figures + their re-derive commands:**
  - Prior committed run population = 4,798 of the true 11,652 (denominator: F1..F9 formula-bearing units) — `jq .total_population docs/release/SD-32-compute-library-and-cause-closure/artifacts/gate-2-engines/formula_interpreter.corpus-wide.json`
  - Frozen Gate 1 census (SD-32, 2026-08-14) F1..F9 population = 11,338 of the true 11,652 — `python3 -c "import json;d=json.load(open('docs/release/SD-32-compute-library-and-cause-closure/artifacts/gate-1-shape-closure/ledger.json'));print(sum(1 for r in d['rows'] if r['family'] in [f'F{i}' for i in range(1,10)]))"`
  - True current F1..F9 population = 11,652 — `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json` (family rollup, F1..F9 summed)
  - Layer 1 (stale run artifact vs. stale-but-full census) explains 6,540 of 6,854 gap units = 95.4% (denominator: the full 6,854-unit gap) — `11,338 − 4,798`
  - Layer 2 (stale census vs. current corpus/inventory) explains 314 of 6,854 gap units = 4.6% (denominator: the full 6,854-unit gap) — `11,652 − 11,338`
  - Commit evidence for Layer 1: `git log --oneline --graph 25dbee17aa..80329736f4 -- docs/release/SD-32-compute-library-and-cause-closure/artifacts/gate-1-shape-closure/ledger.json` → 9 commits changing the census after the Gate 2 run (`25dbee17aa`) was committed
  - Per-family old-run-coverage percentages (F1 28.4% of 6,308; F8 20.9% of 196; F2 63.8% of 2,337 — each states its own denominator, the True pop. column) — command in the rootcause doc §3
- **Status:** complete
- **Movement, four buckets:** instrument-correction (the previously-reported 4,798-of-11,652 population and its 6,854-unit gap are corrected to their real cause — staleness, not a code defect — with no unit's true status changed)
- **Notes:** The root cause is two independent, execution-verified staleness layers, not a single defect: (1) SD-32's committed Gate-2 run artifact predates 9 later commits that grew its own Gate-1 census inside SD-32 itself and was never regenerated (accounts for 95.4% of the gap); (2) the frozen Gate-1 census is itself stale against today's corpus/inventory (4.6%). Neither layer is family-specific — the per-family unevenness the criterion names falls out of how large each family's slice of the original small snapshot happened to be. Confirmed the criterion's own stated percentages (F1 28%, F8 21%, F2 64%) independently, rather than merely restating them. "Refused" (240 of 11,652) and "unjoined" (786 of 11,652) are named as two further, honest, SEPARATE findings — recognition/join completeness is not coverage, and neither bucket is folded silently into "recognised" or excluded from the reported population.
- **Next-cycle plan:** none required by this criterion; see the "unjoined" forward-scope note in the rootcause doc §5 (porting `shape_ledger.py`'s three-way join fallback into this Rust module) if a future cycle wants to close that separate, smaller gap.

---

## AT-33-E3-002 — F1's gap closes

- **Acceptance criterion (verbatim):** "F1 is the largest absolute gap (6,308 formula-bearing, 1,790 run). Its uncovered units run. **Evidence:** a fresh corpus-wide run reporting F1 population == F1 formula-bearing count, both figures stated."
- **Figures + their re-derive commands:**
  - F1 true formula-bearing count = 6,308 — `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json` (family rollup, `F1`)
  - F1 fresh corpus-wide run population = 6,308 — `jq '.families.F1.population' docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json`
  - Equal (`6,308 == 6,308`) — same two commands, diffed
- **RED→GREEN evidence:** New test `f1_population_matches_the_current_true_formula_bearing_count_not_the_stale_sd32_census` (`src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs`). RED (pre-fix, module still reading SD-32's frozen census): `assertion left == right failed ... left: 6032 right: 6308`. GREEN (post-fix, module regenerates fresh): test passes — `cargo test --locked --lib -p codex rules_core::pilot_compute::formula_interpreter_corpus_wide::tests::f1_population_matches_the_current_true_formula_bearing_count_not_the_stale_sd32_census` → `test result: ok. 1 passed`.
- **Status:** complete
- **Movement, four buckets:** closure (6,308 of 6,308 F1 formula-bearing units now walked by the engine's corpus-wide scan, up from the stale 4,798-run artifact's 1,790)
- **Notes:** "Run" here means population-walked (fed to `scan_ledger_rows`, counted per-family), matching the criterion's own evidence bar exactly. 5,655 of F1's 6,308 (89.7% of F1) also `recognised` (grammar accepted); 49 `refused` (named, e.g. `var("CL=...")` — unrecognised function, a grammar gap, not a coverage failure); 604 `unjoined` (this module's own narrower join misses formula text the census's fuller join found — a separate, named finding per the rootcause doc, not silently folded into "recognised").
- **Next-cycle plan:** none — criterion's evidence bar (population parity) is met.

---

## AT-33-E3-003 — every remaining family closes to 100% (11,652 of 11,652)

- **Acceptance criterion (verbatim):** "F2–F9 reach full population coverage. **Evidence:** per-family table in the receipt, each row stating run-population and true-population."
- **Per-family table** (run-population from the fresh SD-33 artifact; true-population from `scripts/shape_ledger.py`'s fresh rollup — both commands above; every row is equal, denominator is each row's own True pop. column):

  | Family | Run-population | True-population | Equal? |
  |---|---:|---:|---|
  | F1 | 6,308 | 6,308 | yes |
  | F2 | 2,337 | 2,337 | yes |
  | F3 | 671 | 671 | yes |
  | F4 | 1,086 | 1,086 | yes |
  | F5 | 589 | 589 | yes |
  | F6 | 391 | 391 | yes |
  | F7 | 12 | 12 | yes |
  | F8 | 196 | 196 | yes |
  | F9 | 62 | 62 | yes |
  | **Total** | **11,652** | **11,652** | **yes** |

  Re-derive both columns at once: `jq -r '.families | to_entries[] | "\(.key) \(.value.population)"' docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json` (run-population) against `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json` (true-population, family rollup).
- **Status:** complete
- **Movement, four buckets:** closure (all nine families, 11,652 of 11,652 total, reach full population coverage)
- **Notes:** The fix is a single mechanism (§ module doc "SD-33 AT-33-E3-001/002/003 root-cause fix") applied once, not nine per-family patches — `scan_ledger_rows` already counted every row handed to it; the defect was entirely in which rows it was handed. This satisfies "generic pass, not per-object lanes" for the fix itself, mirroring E3-001's own diagnostic posture.
- **Next-cycle plan:** none.

---

## AT-33-E3-004 — the corpus-wide run reports 100% (11,652 of 11,652) with its denominator

- **Acceptance criterion (verbatim):** "The regenerated `formula_interpreter.corpus-wide.json` covers **11,652 of 11,652**, and the receipt states both numbers. **Output path (must, not the binary's default):** `cargo run --locked --bin formula_interpreter -- --corpus-wide --output docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json` ... **Evidence:** the artifact at the SD-33 path above, plus the comparison command from `README.md §4` row G returning `0`."
- **Command run exactly as specified:**
  ```
  $ cargo run --locked --bin formula_interpreter -- --corpus-wide --output docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json
  formula-interpreter-corpus-wide: population=11652 (matches closed Gate 1 census) recognised=10626 refused=240 unjoined=786
  formula-interpreter-corpus-wide: F1 population=6308 recognised=5655 refused=49
  formula-interpreter-corpus-wide: F2 population=2337 recognised=2281 refused=55
  formula-interpreter-corpus-wide: F3 population=671 recognised=527 refused=5
  formula-interpreter-corpus-wide: F4 population=1086 recognised=1070 refused=5
  formula-interpreter-corpus-wide: F5 population=589 recognised=577 refused=12
  formula-interpreter-corpus-wide: F6 population=391 recognised=352 refused=12
  formula-interpreter-corpus-wide: F7 population=12 recognised=8 refused=0
  formula-interpreter-corpus-wide: F8 population=196 recognised=136 refused=60
  formula-interpreter-corpus-wide: F9 population=62 recognised=20 refused=42
  ```
- **Figures + their re-derive commands:**
  - Regenerated population = 11,652 — `jq .total_population docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json`
  - True F1..F9 population (`README.md §4` row E) = 11,652 — `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json` (family rollup, F1..F9 summed)
  - Row G comparison (E − new F): `11,652 − 11,652 = 0` — `python3 -c "import json; d=json.load(open('docs/release/SD-33-computed-value-verification/artifacts/epic-3-engine-coverage/formula_interpreter.corpus-wide.json')); print(11652 - d['total_population'])"` → `0`
  - SD-32's Gate 2 evidence file untouched: `git status --porcelain -- docs/release/SD-32-compute-library-and-cause-closure/artifacts/gate-2-engines/formula_interpreter.corpus-wide.json docs/release/SD-32-compute-library-and-cause-closure/artifacts/gate-1-shape-closure/ledger.json` → no output (clean)
- **Status:** complete
- **Movement, four buckets:** closure (population coverage closes 11,652 of 11,652, from the prior 4,798-of-11,652 committed figure)
- **Notes:** "Recognition rate is a separate number from coverage" (epic-breakdown NOTE) — this run's 91.2% recognition (10,626 of 11,652) and its 240 named refusals are reported alongside the 100% population coverage, never conflated with it. The 786 `unjoined` units are likewise named, not silently excluded from the population count (they ARE counted, per-family, in `population`; they are simply not fed to the grammar because this module's own narrower join misses their formula text — see the rootcause doc §5 for why, and that this is out of this criterion's evidence bar).
- **Next-cycle plan:** none for this criterion. Epic 5 (re-verification) and any future engine-coverage cycle inherit a corpus-wide artifact that is honest about population, recognition, and join-completeness as three separate numbers.
