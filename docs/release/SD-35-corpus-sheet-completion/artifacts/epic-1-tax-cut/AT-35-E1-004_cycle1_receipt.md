# Cycle AT-35-E1-004_cycle1 — Epic 1 — Tax cut / AT-35-E1-004

- **Commit SHA:** `2bf452b038392a138bde9693ff680e2bdebe77a4` (code + retro events, pushed fast-forward on `d1b5738658` — no rebase needed); docs commit (this receipt, `progress.md`, `kanban.md`) is the commit after it on `tranche/15`; cycle start `d1b57386581ae77c38012c01e1ac6d437162eeed` (E1-001, E1-002, E1-003, E1-005, E1-006 all landed beneath it, as the dispatch required).
- **Scope gate:** `SCOPE_GATE: EXEMPT (gate-retargeting cycle — closes zero units by design, decisions.md §2)`. `scripts/cycle_scope_gate.py` exists in this tree (AT-35-E1-001); the exemption is by design, not by absence.
- **Files touched:** `scripts/denominator_gate.py` (`SHEET_COMPLETION_BUNDLE_DIR`; two entries appended to `DEFAULT_GLOBS` and to `PROVENANCE_DEFAULT_GLOBS`; docstring and comments), `scripts/tests/test_denominator_gate.py` (`TestDefaultGlobsWidenedToSD35`, 8 tests; two existing tests widened), `scripts/verify.sh` (the `denominator-gate` and `figure-provenance` stage comment blocks only — no stage added, no code path changed), `docs/retro/events/at-35-e1-004.jsonl` (new; the two `correction` events below), `docs/retro/events/sd31-transcribe.jsonl` (`verify.sh`'s own `verification` events from the stage runs, folded per the clean-tree rule), this receipt, `progress.md`, `kanban.md`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 1 file-touch set>`). One self-heal on the way: the constant was first named with a bundle tag, which the audit caught on the working tree; renamed to the bundle's slug, and the SD-34 lines it sat beside were restored byte-identical so the pre-existing tagged identifier from SD-34 does not enter this cycle's diff.
- **Wired-integration audit result:** OK_NO_TOKENS (same base, same paths)
- **Acceptance criterion:** `workflow-instruction.md §7`'s receipt carries the `rust_lines_changed / units_closed` and `pcgen_live_files` rows, produced by `cycle_scope_gate.py --receipt`. `scripts/denominator_gate.py` (`BUNDLE_DIR` at :101 and `DEFAULT_GLOBS` at :121 still point at **SD-33** — never advanced to SD-34) and the `figure-provenance` stage default to SD-33 **and** SD-34 **and** this package; nothing already scanned stops being scanned. **Evidence:** `scripts/verify.sh --only denominator-gate` default run lists every SD-35 `.md` in `files_checked`, `violations=0`; `--only figure-provenance` exits 0 across the package.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=1 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since d1b57386581ae77c38012c01e1ac6d437162eeed --before /tmp/wi-before-AT-35-E1-004.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E1-004` (it also printed `residue_gate=present` and `regressed=0 added=0 dropped=0`). The first half of the criterion is satisfied by this row existing: `§7` carries both rows and the script produces them.
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `2bf452b038` — equal to E1-005's baseline, not risen.
- **Oracle parity:** N/A — no `Number` mapping added, no live path touched.
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 2 (the two corrections below; `cmp /tmp/wi-before-AT-35-E1-004.json docs/work-inventory.json` identical, 0 of 49,438 units moved).
- **Refused tokens:** none (no units scoped).
- **Discoveries:** (1) the criterion's premise was stale — `DEFAULT_GLOBS` already covered SD-34 (AT-34-E1-006, `SD34_BUNDLE_DIR` at :117, entries at :137-138; default `files_checked=186` before this cycle), so the missing scope was SD-35 only; `correction` event `1788835950593-at-35-e1-004-417b71`. (2) SD-33 cannot enter the `figure-provenance` default: its receipts and root docs read `files_checked=78 figures_examined=137 violations=44` under `--check-provenance`, in a folder no later bundle may write to, so widening would make the stage permanently red against the criterion's own "exits 0" evidence; the provenance default is SD-34 + SD-35 (SD-33 remains in the denominator default in full) and the exclusion is pinned by `test_provenance_default_still_excludes_sd33`; `correction` event `1788835950721-at-35-e1-004-6a17c1`. (3) SD-35's receipts are named `<criterion>_cycle<N>_receipt.md`, which the SD-33/SD-34 `*_cycle_receipt.md` glob does not match — the SD-35 entries sweep `artifacts/**/*.md`, pinned by `test_sd35_cycle_numbered_receipt_naming_is_covered`. (4) The 11 token-mapping denominator violations E1-003 and E1-005 reported at their cycle starts were already cleared by AT-35-E1-002 (`815139fadd`) — the explicit-glob run read `files_checked=27 violations=0` at this cycle's start.
- **Figures + their re-derive commands:**
  - default denominator run `files_checked=213 violations=0` (186 before this cycle + 27 SD-35 files) — `python3 scripts/denominator_gate.py --check | tail -2`; denominator: every file the three bundle globs resolve
  - default provenance run `files_checked=143 figures_examined=139 violations=0` (116 files / 128 figures before) — `python3 scripts/denominator_gate.py --check-provenance | tail -3`
  - SD-35 `.md` files on disk `27` (14 root + 13 under `artifacts/`), `27` in the denominator default, `27` in the provenance default — `find docs/release/SD-35-corpus-sheet-completion -name '*.md' | wc -l` and the two `expand_paths` assertions in `python3 -m unittest scripts/tests/test_denominator_gate.py`
  - explicit-glob run at cycle start `files_checked=27 violations=0` — `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'`
  - SD-33 provenance `files_checked=78 figures_examined=137 violations=44` — `python3 scripts/denominator_gate.py --check-provenance 'docs/release/SD-33-computed-value-verification/artifacts/**/*_cycle_receipt.md' 'docs/release/SD-33-computed-value-verification/*.md'`
  - stage `PASS denominator-gate (files_checked=213 violations=0)` and `PASS figure-provenance (files_checked=143 figures_examined=139 violations=0)` — `scripts/verify.sh --only denominator-gate` / `scripts/verify.sh --only figure-provenance`
  - unit tests `Ran 54 tests OK` (46 before; RED first: `FAILED (errors=6)`, every error an `AttributeError` on the not-yet-defined constant) — `cd scripts && python3 -m unittest tests.test_denominator_gate`
  - stages `45` all / `39` quick, unchanged by this cycle — `scripts/verify.sh --list | grep -c -v '^stage '` / `scripts/verify.sh --list | grep -c 'yes   yes'`
  - `live_files=260 live_hits=12736` — `python3 scripts/pcgen_residue_gate.py --check | tail -1`
  - cold `cargo test --locked --no-run -j 6` wall `2:31.06`, max RSS `2,214,020` kB — `/usr/bin/time -v cargo test --locked --no-run -j 6` with `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-004` emptied, `CARGO_INCREMENTAL=0`
  - `cargo test --locked --lib -j 6` → `3186 passed; 0 failed; 14 ignored` in `150.82s` — same target dir
  - Rust lines changed this cycle `0` — `git diff --stat d1b5738658..HEAD -- '*.rs'`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (cold target dir); `cargo test --locked --lib -j 6` → 3186 passed, 0 failed, 14 ignored (= `BASELINE_ROOT_LIB_TESTS`); `--no-fail-fast` skipped — no `src/`, `tests/`, or classifier change (`git diff --stat d1b5738658..HEAD -- '*.rs'` empty); desktop crate and frontend: epic cadence (no `apps/` change); clippy: no Rust targets touched. Run at `d1b5738658` + this cycle's working tree (identical Rust). Fast gates at `2bf452b038`: `completion_atlas.py --check` exit 0 (`unclassified=0 overlap=0 citation_failures=0 stale_derived_at=False`), `shape_engine_boundary.py --check` exit 0 (`magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`), `missing_engine_tables.py --check` exit 0 (`citation_failures=0`), `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` → `0` (directory does not exist yet — Epic 2), `scripts/verify.sh --only pi-sweep` → PASS (11 hits over `src/rules_core/rules_tables`, 11 baseline rows), `python3 scripts/denominator_gate.py --check '<the two SD-35 globs>'` → `files_checked=27 violations=0`. Skipped because the instrument does not exist yet: `sheet_rule_convert -- --check` (AT-35-E2-001), `token_coverage.py --check` (AT-35-E2-004), `corpus_literal_sweep` (no corpus record changed).
- **Sweep population:** N/A (no corpus record changed)
- **Oracle pin:** N/A
- **Status:** complete
- **Notes:** `completion_atlas.py --check` re-stamped `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`'s `derived_at` as a side effect; reverted (`git checkout --`) as outside this epic's file-touch set, same as E1-005. This receipt is inside the widened default scope it created, so both gates were run on it before its own commit.
- **Next-cycle scope:** criterion at zero (both defaults carry this package; the ratio and residue rows are produced mechanically). Epic 1 is at 6 of 6 complete — the epic wrap-up (`workflow-instruction.md §10`) is next.
