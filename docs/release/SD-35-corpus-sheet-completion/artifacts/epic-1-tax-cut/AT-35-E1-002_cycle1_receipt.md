# Cycle AT-35-E1-002 cycle 1 — Epic 1 — Tax cut / AT-35-E1-002

- **Commit SHA:** `815139fadd` (`feat(sd35): AT-35-E1-002 — content-anchored citations …`, rebased onto AT-35-E1-001, E1-006 and E1-005; pre-rebase `a193887fb5`) — code + artifacts; this receipt, `progress.md` and `kanban.md` land in the following commit
- **Scope gate:** `SCOPE_GATE: EXEMPT (instrument-hardening cycle — closes zero units by design, decisions.md §2)` — `scripts/cycle_scope_gate.py` did not exist in this cycle's tree at `53296d80f0` (AT-35-E1-001 landed on `origin/tranche/15` during this cycle and arrived on the pre-push rebase; its `--receipt` mode was then run for the rows below)
- **Files touched:** `scripts/completion_atlas.py` (content-anchor resolver `resolve_content_anchor`, ten anchors, `--by-kind`, `--by-evidence`), `scripts/shape_engine_boundary.py` (ladder anchor, imports the resolver), `scripts/missing_engine_tables.py` (two arm anchors, imports the resolver), `scripts/verify.sh` (+3 stages: `shape-engine-boundary-selftest`, `shape-engine-boundary`, `missing-engine-tables`; 40 → 43 in isolation, **42 → 45** once rebased onto AT-35-E1-001's `cycle-scope-gate-selftest` and AT-35-E1-005's `pcgen-residue-gate` — the one real conflict of each rebase, resolved by re-applying the three insertions onto upstream's file), `scripts/tests/test_shape_engine_boundary.py` (rewritten: RED→GREEN on synthetic source), `scripts/tests/test_completion_atlas.py` and `scripts/tests/test_missing_engine_tables.py` (citation-shape tests moved to anchors, RED→GREEN classes added — these two are the instruments' own tests, closed-under-the-change per AGENTS.md "FILES YOU OWN"), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/{completion-atlas.json,missing-engine-tables.json,shape-engine-boundary.md}` (regenerated by the three `--check`s: citations now carry `context_fn`/`anchor`/`resolved_line`; atlas `derived_at` stamp), package docs `decisions.md §3` (stage count 43), `technical-design.md §6` (stage table + 40 → 47), `forward-scope-register.md` (D1.2 timeout row: already closed), `artifacts/epic-2-sheet-rule/token-mapping/{SYNTHESIS,formula,prereq,prose}.md` (11 denominator-gate lines fixed, no figure changed), `artifacts/epic-1-tax-cut/citation-anchor-proofs.md` (new), this receipt, `docs/retro/events/at-35-e1-002.jsonl` (2 corrections). `scripts/verify-baselines.env`: **unchanged** — it carries no stage-count field (`grep -in stage scripts/verify-baselines.env` finds only prose), so the collision the dispatch warned about did not arise.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS (one self-healed: the word "placeholder" in a new source comment, rephrased before commit)
- **Acceptance criterion:** `scripts/completion_atlas.py`, `scripts/shape_engine_boundary.py`, and `scripts/missing_engine_tables.py` pin `file:line` citations into `src/bin/v06_work_inventory.rs`. Every code edit shifts them; wave 51 found two of three drifted at HEAD. Replace the line-number pin with a **content anchor** — the exact source lines the bucket keys on, inside a named function — resolved by search at check time, so a refactor that moves code keeps the citation green and a change to the cited content still fails closed (SD-34 AT-34-E1-002 condition 6 preserved). Same cycle: add `--by-kind` and `--by-evidence` output modes to `completion_atlas.py` (only `--check`, `--by-book`, `--book` exist at authoring). Wire `shape_engine_boundary.py --check`, `missing_engine_tables.py --check`, and `scripts/tests/test_shape_engine_boundary.py` into `scripts/verify.sh` as real stages, and give `site-dashboard-check` the timeout wrapper SD-33's register asked for. Every place that asserts the stage count moves in the same cycle. **Evidence:** RED→GREEN — move a cited function 50 lines by inserting a comment block, all three `--check`s stay green; change one cited condition, all three fail. `scripts/verify.sh --list` shows the new stages; `scripts/verify.sh --only shape-engine-boundary` exits 0 with population printed.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=1 pcgen_live_files=260` — literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 53296d80f05fe8c32e867f90dd5064a125187f32 --before /tmp/wi-before-AT-35-E1-002.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E1-002` (detail lines: `regressed=0 added=0 dropped=0`), run after the pre-push rebases brought AT-35-E1-001's and AT-35-E1-005's scripts into the tree (the first rebase, before E1-005, printed the same rows with `pcgen_live_files=unavailable`, `residue_gate=absent`). It agrees with the rows computed by hand before the rebase: `cmp docs/work-inventory.json /tmp/wi-before-AT-35-E1-002.json` → IDENTICAL; `git diff --numstat 53296d80f0 -- '*.rs' | awk '{s+=$1+$2} END {print s+0}'` → 0; one `cargo test --locked --no-run` session. The coarse-grep stand-in from `content-unit-inventory.md §6` (`grep -rlE 'PcgenFormulaEvaluator|render_pcgen_desc|raw_tokens|bonus_stack_reader|pre_tokens' src/rules_core src/saved_character src/campaign src/homebrew_authoring apps/desktop/src-tauri/src | wc -l`) read 78 at start and at end; AT-35-E1-005's gate counts 260 because it also counts token-syntax literals (its receipt names the difference).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` — `python3 scripts/pcgen_residue_gate.py --check` at the rebased HEAD (the gate did not exist at `53296d80f0`; the coarse grep read 78 at start and at end, and no live-side file was touched by this cycle: `rust_lines_changed=0`). Equal to AT-35-E1-005's first recording — not risen.
- **Oracle parity:** N/A (no `Number` mapping, no live path touched)
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction: 16 `file:line` pins → 16 content anchors (10 atlas buckets, 1 four-line ladder, 2 engine-surface arms); 3 `verify.sh` stages added; 11 denominator-gate violations in Epic 2's pre-launch token-mapping docs fixed (inherited, not this cycle's — see Discoveries).
- **Refused tokens:** none
- **Discoveries:** (1) `site-dashboard-check` **already had** the timeout wrapper the criterion asks for (`run_site_dashboard_check`: `timeout "${SITE_DASHBOARD_CHECK_TIMEOUT_S:-2400}s"`, exit-124 and exit-3 paths both fail loudly — SD-34 AT-34-E6-001 wave 27); nothing to add — `correction` event `1788831396464-at-35-e1-002-ec135f`, `forward-scope-register.md` D1.2 row updated. (2) §6 step 3's denominator gate with the `artifacts/**/*.md` glob was **RED at cycle start** (`files_checked=21 violations=11`, all in `artifacts/epic-2-sheet-rule/token-mapping/`, committed pre-launch at `ba6b65e7b9`; the launch audit's item 12 scanned only the package root, `files_checked=16 violations=0`) — fixed in place by adding each line's denominator without changing a figure; `prose.md:23`'s "58% of every token instance" states no instance denominator anywhere in the lane's output (its own denominator is ND 21,722 units / CW 51,474 records) and is now marked an inherited estimate (a corpus-wide re-derivation was sampled at ~45 ms/record under the sibling lanes' build load, projecting ~38 min for 51,474 records, and not run) — `correction` event `1788832427529-at-35-e1-002-302ae5`. (3) `scripts/cycle_scope_gate.py` and `scripts/pcgen_residue_gate.py` were absent at `53296d80f0` (the E1-001 lane the pipeline places before this one had not pushed); both landed on `origin/tranche/15` during this cycle, arrived on the two pre-push rebases, and the receipt rows and residue line above are their literal output at the rebased HEAD.
- **Figures + their re-derive commands:** stages `45` of 45 at the rebased HEAD (43 before the rebases brought in `cycle-scope-gate-selftest` and `pcgen-residue-gate`) — `scripts/verify.sh --list | tail -n +2 | wc -l`; python cases `127` of 127 across the three instrument suites + `test_denominator_gate.py` — `cd scripts && python3 -m unittest tests/test_completion_atlas.py tests/test_missing_engine_tables.py tests/test_shape_engine_boundary.py tests/test_denominator_gate.py`; boundary self-test `15` of 15 — `scripts/verify.sh --only shape-engine-boundary-selftest`; atlas `population=49438 … citation_failures=0` — `python3 scripts/completion_atlas.py --check`; boundary `magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True` (26,396 of 49,438; 8,784 of 26,396) — `python3 scripts/shape_engine_boundary.py --check`; tables `population=449 kinds=2 citation_failures=0` (449 of 49,438) — `python3 scripts/missing_engine_tables.py --check`; resolved anchor lines at HEAD — `python3 -c "import sys; sys.path.insert(0,'scripts'); import completion_atlas as CA; print({b: c['resolved_line'] for b, c in CA.resolved_citations().items()})"` → DONE 13147, A 16442, B 16041, C 16347, D 13375, M 13158, V 17281, U 13463, X 13423, Z 13284; denominator gate `files_checked=21 violations=0` — `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'`; pi-sweep `11 hits over src/rules_core/rules_tables, 11 baseline rows` — `scripts/verify.sh --only pi-sweep`; `pcgen_live_files=78` (of the live-side directories named in the grep) — command in the receipt rows above; lib tests `3186 passed, 0 failed, 14 ignored` — `cargo test --locked --lib -j 6`.
- **Build scope verified:** `cargo test --locked --no-run -j 6` NO_RUN_EXIT=0 (cold `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-002`, `CARGO_INCREMENTAL=0`, 340 s wall at box load avg ~89 from three concurrent Epic 1 lanes); `cargo test --locked --lib -j 6` → 3186 passed, 0 failed, 14 ignored, LIB_EXIT=0; **`--no-fail-fast` full workspace skipped**: no `src/` file changed (`rust_lines_changed=0`; the RED→GREEN mutation of `v06_work_inventory.rs` was restored before any build) and the classifier is untouched, so §6 step 3's condition for it does not hold; `corpus_literal_sweep` skipped (no corpus record changed); `sheet_rule_convert --check` and `token_coverage.py --check` skipped (instruments from AT-35-E2-001/E2-004, not in this tree); `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` → 0 (directory does not exist yet); clippy N/A (no Rust touched); desktop crate and frontend at epic cadence (no `apps/` touch). Run at 53296d80f0 + this cycle's working tree.
- **Sweep population:** N/A
- **Oracle pin:** N/A
- **Status:** complete
- **Notes:** Three stages, not the two `decisions.md §3` projected, because `verify.sh` pairs every python gate with its `-selftest` and the criterion names the test file as a stage in its own right; `decisions.md §3` and `technical-design.md §6` now say 45 (after E1-001 + E1-005 + E1-002) / 47 (projected) with the re-derive command. `test_completion_atlas.py` and `test_missing_engine_tables.py` were edited although §3's file-touch set names only `test_shape_engine_boundary.py`: they assert on the citation shape this criterion changes, and leaving them red would have failed their own instruments' `--check` self-tests. `verify-baselines.env` untouched (no stage-count field exists there).
- **Next-cycle scope:** criterion at zero
