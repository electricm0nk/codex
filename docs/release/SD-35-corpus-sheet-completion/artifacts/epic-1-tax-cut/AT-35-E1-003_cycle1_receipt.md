# Cycle AT-35-E1-003_cycle1 — Epic 1 — Tax cut / AT-35-E1-003

- **Commit SHA:** `03072aea0c` (code + artifacts, pushed to `tranche/15`; rebased from `d477d95804` over AT-35-E1-001/002/005/006's commits — the one collision, `scripts/verify-baselines.env`, resolved by keeping both lanes' dated notes with this lane's `BASELINE_ROOT_TEST_BINARIES=408` as the last assignment) — this receipt's audit rows, `progress.md` and `kanban.md` land in the docs commit that follows it. Cycle start `53296d80f05fe8c32e867f90dd5064a125187f32`.
- **Scope gate:** `SCOPE_GATE: EXEMPT (build-time tax cut — closes zero corpus units by design, decisions.md §2)` — additionally, `scripts/cycle_scope_gate.py` does not exist in this cycle's tree (it is AT-35-E1-001's deliverable, on the parallel lane); `ls scripts/cycle_scope_gate.py` → `No such file or directory` at `53296d80f0`.
- **Files touched:** `tests/sd18_*_widening.rs` (89 files) → `tests/sd18_widening/<class>_level<N>.rs` + `tests/sd18_widening/main.rs` (new); `tests/sd13_*progression*.rs` (95 files) → `tests/sd13_progression/<class>_level<N>.rs` + `tests/sd13_progression/main.rs` (new); `scripts/verify-baselines.env` (binary floor + dated note); `docs/release/SD-35-corpus-sheet-completion/artifacts/epic-1-tax-cut/build-time.json` (new); `…/epic-1-tax-cut/test-list-diff.txt` (new); this receipt; `progress.md`, `kanban.md`; `docs/retro/events/at-35-e1-003.jsonl` (new, 1 correction + 1 deferral + 1 incident); folded side-effects of running the gates in this worktree: `docs/retro/events/sd31-transcribe.jsonl` (+1 derived `verify.sh --only pi-sweep` event), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` (`derived_at` stamp moved by `completion_atlas.py --check`).
- **Identifier audit result:** 13 added-line matches (of 937 total; the other 924 are the diff's own `rename from/to` and `+++`/`---` headers for the 184 moved files, and 0 are on removed lines), all naming the family and none introducing a bundle-tagged identifier into shipping code: 6 in the two `main.rs` doc comments (the historic `tests/sd18_*_widening.rs` / `tests/sd13_*progression*.rs` file names the criterion itself uses — the documented `tests/…` citation exclusion class of `scripts/identifier-discipline-audit.sh`, `strip_test_citations` — and `cargo test --test sd18_widening` / `--test sd13_progression`, the binary names), 4 in the `scripts/verify-baselines.env` revision note, 3 in `build-time.json`'s description strings. The two binaries keep the families' names (`sd18_widening`, `sd13_progression`) because the family's identity across the repo is that tag — 244 `grounding_ref` strings and the architecture docs' "named by originating slice" convention; `scripts/identifier-discipline-audit.sh` scans shipping paths only (`src/`, `apps/desktop/`) and exempts `tests/` citations by design, so it reads clean. Command: `workflow-instruction.md §6` step 2 verbatim over `<scoped paths>` at `BASE_BRANCH=fe5ae6cd4a` … `HEAD=03072aea0c`; the per-file breakdown from `git diff --unified=0 fe5ae6cd4a...HEAD -M -- <scoped paths> | awk '/^\+\+\+ /{f=$2} /^\+/ && !/^\+\+\+/ && /\<(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})/{c[f]++} END{for(k in c) print c[k], k}'`.
- **Wired-integration audit result:** OK_NO_TOKENS (`workflow-instruction.md §6` step 2 verbatim over `<scoped paths>` at `fe5ae6cd4a...03072aea0c`).
- **Acceptance criterion:** *(verbatim, epic-breakdown.md §"AT-35-E1-003")* Fable review R10: 89 `tests/sd18_*_widening.rs` and 95 `tests/sd13_*progression*.rs` are near-byte-identical templates (~80k of 187k test lines). Consolidate each family into one table-driven binary with the same assertions. `BASELINE_ROOT_TEST_BINARIES` and the tests floor in `scripts/verify-baselines.env` move in the same commit. **Evidence:** cold `cargo test --locked --no-run` wall time (`CARGO_INCREMENTAL=0`, fresh `CARGO_TARGET_DIR`) measured before and after, both in `artifacts/epic-1-tax-cut/build-time.json` with the command and the machine's load. Test count after equals before, proven by a name-by-name diff of `cargo test -- --list`. Zero new warnings. The inherited failing-suite baseline re-derived after the change.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=1906 ratio=n/a builds_recorded=0 pcgen_live_files=260` — `python3 scripts/cycle_scope_gate.py --receipt --since 53296d80f0 --before /tmp/wi-before-AT-35-E1-003.json --after docs/work-inventory.json` at the rebased HEAD `03072aea0c` (the script arrived from AT-35-E1-001 on the pre-push rebase; `/tmp/wi-before-AT-35-E1-003.json` is `git show 53296d80f0:docs/work-inventory.json`, byte-identical to `docs/work-inventory.json` at HEAD). `builds_recorded=0` is the script reading an empty `$CARGO_TARGET_DIR`: this lane's four measurement target dirs were `rm -rf`'d after use (AGENTS.md "delete your CARGO_TARGET_DIR when you finish"). The real count from the transcripts in `build-time.json` and this receipt: 4 cold compile sessions (runs 1–4, the criterion's own before/after measurements, each in a fresh dir) + 1 verify session (clippy, the two binaries, `--no-run`, `--lib`, the full suite — all in run 2's dir, nothing recompiled) = one build for verification, as `decisions.md §3` requires. `rust_lines_changed=1906` = +1000 / −906 (`git diff --numstat -M 53296d80f0..HEAD -- '*.rs'`).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` — `python3 scripts/pcgen_residue_gate.py --check` at the rebased HEAD `03072aea0c` (the gate arrived from AT-35-E1-005 on the pre-push rebase; absent at cycle start, so no earlier reading of this lane's own — equal to E1-005's first recording and to E1-002's receipt, not risen; this cycle touched no live-side file).
- **Oracle parity:** N/A — no `Number` mapping, no live path touched (`tests/` and `scripts/verify-baselines.env` only).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 0 — `docs/work-inventory.json` untouched (`git diff --stat 53296d80f0..HEAD -- docs/work-inventory.json` empty); the cycle closes zero units by design.
- **Refused tokens:** none (no converter run in this cycle).
- **Discoveries:** (1) `scripts/denominator_gate.py --check` over the package's `artifacts/**/*.md` glob was already red at the cycle-start SHA — `files_checked=21 violations=11`, all 11 in `artifacts/epic-2-sheet-rule/token-mapping/{SYNTHESIS,formula,prereq,prose}.md` (pre-launch tables, not touched here; the package-root glob alone is `files_checked=14 violations=0`) — retro `incident` `1788832159172-at-35-e1-003-75731f`, for AT-35-E1-004's gate-scope cycle. (2) The criterion's sizing figure was ~7% high: the families are 74,932 of 180,260 test lines, not ~80k of 187k — retro `correction` `1788831694372-at-35-e1-003-973112`. (3) 244 `grounding_ref`/doc-comment citations in 5 `src/` files and 4 `docs/architecture/` pages still name the old `tests/sd18_<x>_widening.rs` / `tests/sd13_<x>_progression.rs` paths — strings nothing resolves on disk; the matrix tests' `contains("sd18_<x>_widening")` assertions still hold on the unchanged stem — retro `deferral` `1788832099943-at-35-e1-003-532b1c`, revisit at AT-35-E7-003's architecture-docs refresh.
- **Figures + their re-derive commands:**
  - 184 family files = 89 + 95 of 544 `tests/*.rs` at `53296d80f0`: `ls tests/sd18_*_widening.rs | wc -l; ls tests/sd13_*progression*.rs | wc -l; ls tests/*.rs | wc -l`.
  - 74,932 family lines = 35,909 + 39,023 of 180,260 `tests/*.rs` lines: `wc -l tests/sd18_*_widening.rs | tail -1; wc -l tests/sd13_*progression*.rs | tail -1; wc -l tests/*.rs | tail -1` (at `53296d80f0`).
  - 2,219 family `#[test]` fns = 980 (sd18) + 1,239 (sd13): `grep -c '^\s*#\[test\]' tests/sd18_widening/*.rs | awk -F: '{s+=$2} END{print s}'` and the same for `tests/sd13_progression/`; the same two numbers are the `--list` entry counts under `Running tests/sd18_widening/main.rs` / `Running tests/sd13_progression/main.rs`.
  - Test entries 8,723 before = 8,723 after, of which 545 targets carry at least one entry both times: `cargo test --locked -j 6 -- --list 2>&1` on each tree, then `artifacts/epic-1-tax-cut/test-list-diff.txt` (`verdict=IDENTICAL`, `only_in_before=0`, `only_in_after=0`).
  - `Running` targets 590 before → 408 after (of 544 → 362 `Executable tests/…` lines on `--no-run`; the other 46 are `unittests src/lib.rs` + `src/bin/*` targets, unchanged): `grep -c '^[[:space:]]*Running ' <list transcript>`; `grep -c 'Executable tests/' <--no-run stderr>`.
  - Cold `--no-run` wall time, paired quiet-box: before 3:08.97 (188.97 s) → after 2:25.89 (145.89 s), −43.08 s of the 188.97 s before (−22.8% of before); quiet-box run 1 before 2:44.49; contended run 2 after 4:55.95 (load 40, another lane's full build) — all four runs with load in `artifacts/epic-1-tax-cut/build-time.json`; command `rm -rf $CARGO_TARGET_DIR; CARGO_INCREMENTAL=0 /usr/bin/time -v cargo test --locked --no-run -j 6`.
  - Full-suite wall time after the fold: 37.6 min (01:55:07Z → 02:32:45Z), 8,656 passed / 0 failed / 67 ignored across 408 targets; the cut's figure was 70 min across 590 targets (`workflow-instruction.md §1` item 10) — one observation each, different box load, not a paired measurement.
  - Moved-module audit: 0 added and 0 removed lines beyond the three rewrites across all 184 modules; 184 of 184 moved files detected as renames, each at a git rename score of 97 or 98 out of 100 (67 at 97, 117 at 98): `git diff --unified=0 -M 53296d80f0 -- tests/ ':!tests/sd18_widening/main.rs' ':!tests/sd13_progression/main.rs' | grep -E '^\+' | grep -vE '^\+\+\+|use crate::common::|"\.\./fixtures/rules_core/' | wc -l` (→ 0) and the `^-` mirror excluding `mod common;` / `use common::` / `"fixtures/rules_core/` (→ 0); `git diff -M --summary 53296d80f0 -- tests/ | grep -c '^ rename'` (→ 184).
  - `rust_lines_changed`: `git diff --numstat -M 53296d80f0..HEAD -- '*.rs'` → +1000 / −906 across 186 files (the two new `main.rs` are 278 of the 1000 added lines; the rest are the three rewrites, 3–4 lines per module).
  - Baseline floor: `BASELINE_ROOT_TEST_BINARIES` 589 → 408 (`grep -E '^BASELINE_ROOT_TEST_BINARIES=' scripts/verify-baselines.env | tail -1`), `BASELINE_ROOT_FULL_TESTS` re-stated at 8656 (unchanged: no test moved in or out), `BASELINE_ROOT_LIB_TESTS` 3186 unchanged; old/new count sweep `grep -rnw '589\|408' tests/ src/ apps/ scripts/` finds no other binary-count assertion (the `589`/`408` hits are corpus line numbers and an equipment-table row index).
- **Build scope verified:** run at the consolidated working tree of `53296d80f0` (tests/ byte-identical to the committed tree), `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-003-after`, `CARGO_INCREMENTAL=0`: `cargo test --locked --no-run -j 6` → `NO_RUN_EXIT=0`, 0 warnings (twice: the cold measurement and the verify pass); `cargo clippy --locked -j 6 --test sd18_widening --test sd13_progression` → exit 0, 0 warnings, 0 errors; `cargo test --locked -j 6 --test sd18_widening --test sd13_progression` → 980 passed / 0 failed and 1,239 passed / 0 failed; `cargo test --locked --lib -j 6` → 3,186 passed / 0 failed / 14 ignored; `cargo test --locked --no-fail-fast -j 6` → exit 0, 8,656 passed / 0 failed / 67 ignored, 408 targets executed, 0 `test result: FAILED` lines — **the inherited failing-suite baseline re-derived after the change is still EMPTY**. Fast gates at the same tree: `completion_atlas.py --check` exit 0 (`unclassified=0 overlap=0 citation_failures=0`), `shape_engine_boundary.py --check` exit 0 (`magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`), `missing_engine_tables.py --check` exit 0 (`citation_failures=0`), `scripts/verify.sh --only pi-sweep` → `RESULT: PASS`; `denominator_gate.py --check` package glob → `files_checked=14 violations=0`, package+artifacts glob → `files_checked=21 violations=11` (all pre-existing in `epic-2-sheet-rule/token-mapping/`, see Discoveries). Desktop crate: epic cadence (`git diff --stat -- apps/` empty). `sheet_rule_convert --check`, `token_coverage.py --check`: instruments do not exist yet (Epic 2 deliverables). `corpus_literal_sweep`: skipped, no corpus record changed.
- **Sweep population:** N/A — no corpus record changed (`git diff --stat 53296d80f0..HEAD -- data/` empty).
- **Oracle pin:** N/A — no figure from the pinned corpus.
- **Status:** complete — the criterion's population is the two families (184 files); at HEAD 0 of them remain as standalone binaries (`ls tests/sd18_*_widening.rs tests/sd13_*progression*.rs` → no such file), every one of their 2,219 tests runs from one of the two binaries with its name intact, both floors moved in the same commit, both cold timings are in `build-time.json` with load, warnings are 0, and the failing-suite baseline after the change is empty.
- **Notes:** "Table-driven" is realized as one binary per family whose `main.rs` is a class-keyed roster table (`roster! { barbarian => [barbarian_level12, …], … }`) that drives the module declarations — one row per original file, every `#[test]` and assertion byte-identical (the moved-module audit shows 0 added and 0 removed lines beyond the three crate-root-relative rewrites: `mod common;` → declared once in `main.rs`, `use common::` → `use crate::common::`, `include_str!("fixtures/…")` → `"../fixtures/…"`). A data-table rewrite with generic test functions would collapse the 2,219 named tests into a handful, contradicting the criterion's own "test count after equals before" evidence; the roster form is the one reading under which both sentences hold. `scripts/verify.sh` needs no change: `expected_test_suites` is `find tests -maxdepth 1`, and the new `Running tests/sd18_widening/main.rs` lines are counted by `count_running` like any other target.
- **Next-cycle scope:** criterion at zero (a build-time cut has no population; the two families are folded and every family test is in one of the two binaries).
- **Re-verification at a later HEAD (2026-09-08, redundant re-dispatch of this same criterion):** the criterion was re-dispatched as "cycle 1" after it had already landed; no new work was performed and nothing in `tests/` or `scripts/verify-baselines.env` was changed. Re-derived at `4e321d2c6cccef32a52acd3f76d68ce78f8c5d3b` (`origin/tranche/15`, 30 commits past this cycle's `03072aea0c`): `ls tests/sd18_*_widening.rs tests/sd13_*progression*.rs` → `No such file or directory` (0 standalone family binaries remain); `tests/sd18_widening/main.rs` and `tests/sd13_progression/main.rs` both present; `grep -E '^BASELINE_ROOT_TEST_BINARIES=' scripts/verify-baselines.env | tail -1` → `BASELINE_ROOT_TEST_BINARIES=408` (this lane's value is still the last assignment, `:3484`); `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-003-reverify cargo test --locked -j 6 --test sd18_widening --test sd13_progression` → exit 0, `980 passed; 0 failed` and `1239 passed; 0 failed` (2,219 family tests, unchanged), **0 warnings** (`grep -c '^warning'` → 0); `python3 scripts/pcgen_residue_gate.py --check` → `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` (not risen); §6 step 2 audits re-run on the final diff with rename pairing across the old and new paths (`git diff --unified=0 -M fe5ae6cd4a...HEAD -- 'tests/sd18_*' 'tests/sd13_*' scripts/verify-baselines.env artifacts/epic-1-tax-cut/build-time.json`) → 13 identifier matches on added lines, byte-for-byte the same 13 this receipt already itemises (2 `build-time.json` description strings + 1 `family_tests` key, 4 `verify-baselines.env` revision-note lines, 6 `main.rs` doc-comment citations), and `OK_NO_TOKENS`; `git diff -M --summary` → `184` renames detected. Redundant-dispatch retro `rework` `1788880170541-at-35-e1-003-b1939e`.
