# Cycle AT-35-E1-005_cycle1 — Epic 1 — Tax cut / AT-35-E1-005

- **Commit SHA:** `ca2e2105edb45cd317d536526b598a44ad0de510` (code, as pushed — rebased twice, onto `800c363e42` then `f43116b870`; pre-rebase `a6a9ad299b`); docs commit `ac7a03e2de7123e8ce42c27de537f678e8ab3c68` (this receipt, `progress.md`, `kanban.md`, the transcript); this SHA-pin follow-up is the commit after that; cycle start `53296d80f05fe8c32e867f90dd5064a125187f32`
- **Scope gate:** `SCOPE_GATE: EXEMPT (gate-building cycle — this cycle CREATES pcgen_residue_gate.py; closes zero units by design, decisions.md §2)`. `scripts/cycle_scope_gate.py` (AT-35-E1-001) was **absent** in this cycle's tree at `53296d80f0` — `ls scripts/cycle_scope_gate.py` → `No such file or directory` — so no `--min 500` line exists to quote; the exemption is by design, not by absence. AT-35-E1-001 landed on `origin/tranche/15` (`1d821cdc8d`) while this cycle ran; after the rebase its `--receipt` was run for real (row below).
- **Files touched:** `scripts/pcgen_residue_gate.py` (new), `scripts/pcgen-residue-baseline.env` (new), `scripts/tests/test_pcgen_residue_gate.py` (new), `scripts/verify.sh` (stage `pcgen-residue-gate` in both sets + `run_pcgen_residue_gate` + case arm), `scripts/verify-baselines.env` (dated note only — stage count 41 → 42 / quick 35 → 36, re-derived from the rebased tree; no key moves), `docs/retro/events/at-35-e1-005.jsonl` (new; the `correction` below + the stage runs), `docs/retro/events/sd31-transcribe.jsonl` (verify.sh's own `verification` events from the stage proof runs, folded per the clean-tree rule), this receipt, `progress.md`, `kanban.md`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 1 file-touch set>` + the same grep over the three new files)
- **Wired-integration audit result:** OK_NO_TOKENS (same base, same paths)
- **Acceptance criterion:** `scripts/pcgen_residue_gate.py --check` (`technical-design.md §0`, `§6`) greps the live side — `src/rules_core/**` minus `cache_gen/`, `src/saved_character/**`, `src/campaign/**`, `src/homebrew_authoring/**`, `apps/desktop/**` — for the PCGen surface (`raw_tokens`, `PcgenFormulaEvaluator`, `render_pcgen_desc`, `bonus_stack_reader`, `pre_tokens`, and PCGen token-syntax literals), prints `live_files` and `live_hits`, and fails when either exceeds the baseline in `scripts/pcgen-residue-baseline.env`. `--closure` fails on anything above zero. `--rebaseline` is allowed only after a cycle that reduced the count. The baseline is recorded in this cycle from the real grep (a coarse count at authoring found 78 files — `content-unit-inventory.md §6`). **Evidence:** RED→GREEN — plant one `raw_tokens` read in `src/rules_core/`, the gate fails; remove it, the gate passes; `--closure` fails at the baseline (correct — Epic 6 is what makes it pass). Wired into `verify.sh` as `pcgen-residue-gate`. `scripts/tests/test_pcgen_residue_gate.py`.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=1 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 53296d80f05fe8c32e867f90dd5064a125187f32 --before /tmp/wi-before-AT-35-E1-005.json --after docs/work-inventory.json` (run after the rebase; it also printed `residue_gate=present` and `regressed=0 added=0 dropped=0`). The same rows were first computed by hand while the script was absent and agree: `closed`/`relabeled` from `cmp /tmp/wi-before-AT-35-E1-005.json docs/work-inventory.json` → identical (id-set diff empty, 0 of 49,438 moved); `rust_lines_changed` from `git diff --stat 53296d80f0 -- '*.rs'` → empty; `ratio` undefined at 0/0; `builds_recorded=1` (one `cargo test --locked --no-run` + one `--lib` on a cold `CARGO_TARGET_DIR`); `pcgen_live_files` from `python3 scripts/pcgen_residue_gate.py --check`.
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` (first recording; no previous receipt to be above). Breakdown from the same command: identifiers `raw_tokens` 47 files / 265 hits, `PcgenFormulaEvaluator` 14 / 100, `render_pcgen_desc` 17 / 109, `bonus_stack_reader` 7 / 20, `pre_tokens` 6 / 20 (`identifier_files=68 identifier_hits=514`); token syntax `BONUS:` 146 / 2403, `DEFINE:` 34 / 151, `PRE[A-Z]+:` 126 / 8078, `SAB:` 0 / 0, `DESC:` 142 / 596, `%CHOICE` 9 / 68, `%LIST` 25 / 141, `TYPE=` 74 / 785; by root `src/rules_core` 208 files / 12242 hits, `apps/desktop` 52 / 494, the other three roots 0 / 0. Denominator for every file figure: the 260 live-side source files that hit (of every `.rs .ts .tsx .js .jsx .mjs .cjs` file under the five live roots).
- **Oracle parity:** N/A — no `Number` mapping added, no live path touched.
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 1 (the residue figure: 78 coarse → 260 files / 12,736 hits exact; a `correction` retro event, id `1788831658230-at-35-e1-005-0d124e`).
- **Refused tokens:** none (no units scoped).
- **Discoveries:** the live-side residue is dominated by **token-syntax literals in generated `src/rules_core/rules_tables/**`** (8,078 `PRE[A-Z]+:` and 2,403 `BONUS:` hits of 12,736 total) — the inventory's §6 table counted only the five identifiers and predicted 78 files; Epic 6's population is the 260-file / 12,736-hit surface, of which the identifier readers (68 files) are the part AT-35-E6-001..003 name explicitly and the generated tables are the part AT-35-E6-002's relocation must also cover. Emitted as the `correction` event above. Also: `SAB:` has 0 live hits today.
- **Figures + their re-derive commands:**
  - `live_files=260 live_hits=12736` — `python3 scripts/pcgen_residue_gate.py --check` (denominator: source files under the five live roots minus `cache_gen/`)
  - `identifier_files=68 identifier_hits=514` — same command, the `identifier_*` line
  - coarse comparison `82` files (`69` without `cache_gen/`) — `grep -rlE 'PcgenFormulaEvaluator|render_pcgen_desc|raw_tokens|bonus_stack_reader|pre_tokens' src/rules_core src/saved_character src/campaign src/homebrew_authoring apps/desktop | wc -l` (all file types; the 78 at authoring scanned `apps/desktop/src-tauri/src` only)
  - planted probe `live_files=261 live_hits=12737 verdict=FAIL_INCREASED` exit 1, removed `verdict=PASS` exit 0 — the plant/remove transcript in `progress.md`
  - `--check --closure` → `live_files=260 live_hits=12736 verdict=FAIL` exit 1
  - `--rebaseline` at the baseline → `rebaseline=REFUSED ... verdict=FAIL_NOT_REDUCED` exit 1
  - stages `42` all / `36` quick after the rebase (`41` / `35` in this cycle's pre-rebase tree, before AT-35-E1-001's `cycle-scope-gate-selftest` landed under it) — `scripts/verify.sh --list | grep -c -v '^stage '` / `... | grep -c 'yes   yes'`
  - `scripts/verify.sh --only pcgen-residue-gate` → `PASS pcgen-residue-gate (live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS)`; `PCGEN_RESIDUE_GATE_CLOSURE=1 scripts/verify.sh --only pcgen-residue-gate` → `FAIL ... verdict=FAIL`, `RESULT: FAIL`
  - `python3 -m unittest scripts/tests/test_pcgen_residue_gate.py` → `Ran 15 tests ... OK` (first run RED: `ModuleNotFoundError: pcgen_residue_gate`)
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (cold `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-005`, `CARGO_INCREMENTAL=0`); `cargo test --locked --lib -j 6` → 3186 passed, 0 failed, 14 ignored (= `BASELINE_ROOT_LIB_TESTS`); `--no-fail-fast` skipped — no `src/`, `tests/`, or classifier change (`git diff --stat 53296d80f0 -- '*.rs'` empty); desktop crate and frontend: epic cadence (no `apps/` change); clippy: no Rust targets touched. Run at `53296d80f0` + this cycle's working tree (identical Rust). Fast gates: `completion_atlas.py --check` exit 0 (`unclassified=0 overlap=0 citation_failures=0`), `shape_engine_boundary.py --check` exit 0 (`magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`), `missing_engine_tables.py --check` exit 0 (`citation_failures=0`), `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` → `0` (directory does not exist yet — Epic 2), `scripts/verify.sh --only pi-sweep` → PASS (11 hits over `src/rules_core/rules_tables`, 11 baseline rows), `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'` → `files_checked=22 violations=11`, exit 1 — **all 11 in `artifacts/epic-2-sheet-rule/token-mapping/`** (`prereq.md` 5, `SYNTHESIS.md` 3, `formula.md` 2, `prose.md` 1), committed at `a232e27b03` before this cycle's start and outside Epic 1's file-touch set; this cycle's own files check clean (`... --check progress.md kanban.md 'artifacts/epic-1-tax-cut/*.md'` → `files_checked=3 violations=0`). Recorded as an `incident` retro event; the pre-existing red belongs to the token-mapping owner / AT-35-E1-004 (which widens the gate's default scope to this package), not fixed here because the missing denominators are that synthesis's figures, not mine. Skipped because the instrument does not exist yet: `sheet_rule_convert -- --check` (AT-35-E2-001), `token_coverage.py --check` (AT-35-E2-004), `corpus_literal_sweep` (no corpus record changed).
- **Sweep population:** N/A (no corpus record changed)
- **Oracle pin:** N/A
- **Status:** complete
- **Notes:** `completion_atlas.py --check` re-stamped `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`'s `derived_at` to HEAD as a side effect; reverted (`git checkout --`) as outside this epic's file-touch set — the next atlas run re-stamps it. Comment/doc-string mentions of PCGen tokens on the live side count as hits, deliberately (the docstring says why); the baseline is the exact count under that rule, and the rule is the same at closure.
- **Next-cycle scope:** criterion at zero (the gate exists, the baseline is recorded, the stage is wired). Epic 6 moves the counts; `--rebaseline` after each reduction.
