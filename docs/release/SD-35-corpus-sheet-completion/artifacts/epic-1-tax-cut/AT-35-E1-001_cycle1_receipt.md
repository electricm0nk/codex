# Cycle AT-35-E1-001 cycle 1 — Tax cut / AT-35-E1-001

- **Commit SHA:** `1d821cdc8d` (code, test, `verify.sh` stage, retro event) on cycle-start `53296d80f0`; this receipt and the board rows land in the following commit.
- **Scope gate:** `SCOPE_GATE: EXEMPT (gate-building cycle — this cycle CREATES cycle_scope_gate.py; it closes zero units by design, decisions.md §2)`. The gate did not exist at cycle start (`ls scripts/cycle_scope_gate.py` → `No such file or directory` at `53296d80f0`). Live runs of the finished gate at HEAD, re-deriving `technical-design.md §6`'s three worked examples verbatim:
  - `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature` → `scoped=7866 remaining_non_done=23315 floor=500 verdict=PASS` (exit 0)
  - `python3 scripts/cycle_scope_gate.py --min 500 --bucket A --kind companion` → `scoped=28 remaining_non_done=23315 floor=500 verdict=FAIL_UNDER_FLOOR` (exit 1)
  - `python3 scripts/cycle_scope_gate.py --min 500` → `scoped=23315 remaining_non_done=23315 floor=500 verdict=PASS` (exit 0)
- **Files touched:** `scripts/cycle_scope_gate.py` (new, 453 lines), `scripts/tests/test_cycle_scope_gate.py` (new, 479 lines, 51 cases), `scripts/verify.sh` (+55/−1: `cycle-scope-gate-selftest` in both stage sets, run function, dispatch case), `docs/retro/events/at-35-e1-001.jsonl` (new, 1 incident), this receipt, `progress.md`, `kanban.md`. Re-derive: `git diff --stat 53296d80f0 HEAD`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** `scripts/cycle_scope_gate.py --min 500 <scope flags>` reads `docs/work-inventory.json`, applies the scope filter (any combination of `--bucket`, `--kind`, `--evidence-prefix`, `--token`, `--book`), prints the scoped population **and** the whole remaining non-DONE population, and exits non-zero when the scoped population is under the floor and is not the whole remainder. `--receipt --before <inventory> --after <inventory>` computes units closed (id-set moved into DONE), units relabeled, `rust_lines_changed / units_closed` from `git diff --stat`, the live-side PCGen file count from `pcgen_residue_gate.py`, and prints the receipt rows of `workflow-instruction.md §7` verbatim. **Evidence:** RED→GREEN — a 12-unit scope exits 1; a 500-unit scope exits 0; a 12-unit scope that is the entire remainder exits 0. `scripts/tests/test_cycle_scope_gate.py` covers all three. Wired into `scripts/verify.sh` as `cycle-scope-gate-selftest`.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=unavailable` — from `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E1-001 python3 scripts/cycle_scope_gate.py --receipt --since 53296d80f0 --before /tmp/wi-before-AT-35-E1-001.json --after docs/work-inventory.json` (detail lines above it: `regressed=0 added=0 dropped=0`). `builds_recorded=0` is correct: the cycle touched no `.rs` or Cargo file (`git diff --name-only 53296d80f0 HEAD | grep -E '\.rs$|Cargo'` → empty) and ran no cargo command. `pcgen_live_files=unavailable` is correct: `scripts/pcgen_residue_gate.py` does not exist in this tree (AT-35-E1-005 creates it); the gate prints `unavailable` rather than a fabricated zero, proven by `TestResidueGateProbe.test_missing_gate_is_unavailable_never_zero`.
- **PCGen residue:** N/A — `scripts/pcgen_residue_gate.py` absent at `53296d80f0` and at HEAD (`ls scripts/pcgen_residue_gate.py` → `No such file or directory`). Nothing on the live side was touched (no `.rs` in the diff), so the count cannot have risen.
- **Oracle parity:** N/A — no `Number` mapping added; no live path touched.
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 0 — `docs/work-inventory.json` is byte-identical to the cycle-start snapshot (`cmp /tmp/wi-before-AT-35-E1-001.json docs/work-inventory.json` → no output; `added=0 dropped=0`).
- **Refused tokens:** none (no units scoped; gate-building cycle).
- **Figures + their re-derive commands:**
  - `scoped=7866 remaining_non_done=23315` for bucket B `class_feature`, of 49,438 units in `docs/work-inventory.json` — `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature`
  - `scoped=28` for bucket A `companion`, of 23,315 non-DONE — `python3 scripts/cycle_scope_gate.py --min 500 --bucket A --kind companion`
  - `remaining_non_done=23315` with by-bucket `A:449 B:11589 C:4180 D:1982 M:4334 U:202 V:392 X:168 Z:19` (sums to 23,315) — `python3 scripts/cycle_scope_gate.py --min 500`; cross-check `python3 scripts/completion_atlas.py --check` (same partition function, `completion_atlas._bucket_of`)
  - 51 self-test cases — `python3 -m unittest scripts/tests/test_cycle_scope_gate.py` → `Ran 51 tests ... OK`
  - 41 `verify.sh` stages after this cycle (40 at authoring, `workflow-instruction.md §4`) — `scripts/verify.sh --list | grep -c ''` → 42 lines (1 header + 41 stages); `cycle-scope-gate-selftest` is line 21, in both sets. No baseline variable or test asserts the stage count (`grep -rn 'BASELINE_STAGES\|41 stages\|40 stages' scripts/ tests/ src/ apps/desktop/src` → empty), so nothing else moved.
  - 2 compile sessions detected on a real target dir by the `builds_recorded` mechanism, validating the fingerprint-clustering assumption before it shipped — `python3 -c "import sys; sys.path.insert(0,'scripts'); import cycle_scope_gate as g; print(g.build_sessions('/tmp/cargo-sd35-launch'))"` → 2 (70 `invoked.timestamp` files clustering at 18:48 and 20:06 on 2026-09-07)
  - denominator gate on the package: `violations=11` of `files_checked=21` — `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'`; all 11 in 4 pre-launch files under `artifacts/epic-2-sheet-rule/token-mapping/` (SYNTHESIS.md 3, formula.md 2, prereq.md 5, prose.md 1), last committed at `da61655028`, before this cycle started
- **Build scope verified:** no cargo command run — the cycle touched no `.rs`, `Cargo.toml`, or `Cargo.lock` (`git diff --name-only 53296d80f0 HEAD` lists only `scripts/**` and `docs/**`), so `--no-run` / `--lib` / `--no-fail-fast` / `clippy` / `sheet_rule_convert --check` / `token_coverage.py --check` (instruments not yet in the tree) were skipped with that reason. Ran at `1d821cdc8d`: `python3 -m unittest scripts/tests/test_cycle_scope_gate.py` → `Ran 51 tests OK`; `RETRO_DISABLE=1 scripts/verify.sh --only cycle-scope-gate-selftest` → `PASS (51 cases passed)`, exit 0; `bash -n scripts/verify.sh` → OK; `python3 scripts/completion_atlas.py --check` → `missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` (its `derived_at` restamp of the SD-34 atlas artifact was reverted — out of this lane's write scope); `python3 scripts/shape_engine_boundary.py --check` → `magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`; `python3 scripts/missing_engine_tables.py --check` → `citation_failures=0`; `python3 scripts/denominator_gate.py --check <package globs>` → `violations=11` (pre-existing, see Notes); `python3 scripts/denominator_gate.py --check-provenance <package globs>` → `violations=0`; `RETRO_DISABLE=1 scripts/verify.sh --only pi-sweep` → PASS, exit 0. Desktop crate and frontend: epic cadence (no `apps/` touched).
- **Sweep population:** N/A — no corpus records changed.
- **Oracle pin:** N/A — no figure came from the pinned corpus.
- **Status:** complete
- **Notes:** `--or` is the mechanism `decisions.md §2` asks for ("bundled with others until the cycle reaches 500"): repeats of a flag OR, different flags AND, `--or` unions clauses. `--token` matches the unit's `tokens` list when the inventory carries one (AT-35-E2-004 onward) and, until then, `wiring_class_signals` whole (`derived:bonus`) or by family (`bonus`). The denominator-gate violations are in four pre-launch token-mapping files outside this lane's file-touch set; AT-35-E1-004 (the gates point at this package) owns the package-wide pass — recorded as retro incident `denominator-gate-red-on-package-prose`. Five parallel worktrees were live at dispatch (`git worktree list`), so `scripts/verify.sh` was rebased at push time; the stage list is additive.
- **Next-cycle scope:** criterion at zero. AT-35-E1-002 and AT-35-E1-005 are this lane's next cycles (`workflow-instruction.md §3`); every later cycle's first command is `python3 scripts/cycle_scope_gate.py --min 500 <scope flags>`.
