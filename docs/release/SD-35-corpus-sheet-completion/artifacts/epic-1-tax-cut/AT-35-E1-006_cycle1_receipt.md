# Cycle AT-35-E1-006 cycle 1 — Epic 1 — Tax cut / AT-35-E1-006

- **Commit SHA:** `9cc73dca76` (the work; authored as `0dac3ddc9f`, rebased onto AT-35-E1-001's `b826695604` before push) and `800c363e42` (this receipt, the row-map key rename, `progress.md`, `kanban.md`); the SHA fill-in and literal receipt line land in one follow-on commit. Cycle start `53296d80f0` (= `origin/tranche/15` at rebase; `docs/work-inventory.json` byte-identical to the `4c6c57eb9f` cut: `git diff --stat 4c6c57eb9f HEAD -- docs/work-inventory.json` empty).
- **Scope gate:** `SCOPE_GATE: EXEMPT (docs-only fold of SD-34's closure epilogue — closes zero units by design, decisions.md §2 and §12)`. `scripts/cycle_scope_gate.py` did not exist at this cycle's start (`ls scripts/cycle_scope_gate.py` → No such file at `53296d80f0`); AT-35-E1-001 landed it (`1d821cdc8d`) while this cycle ran, and the rebase before push brought it in — the receipt rows below are its literal output. `scripts/pcgen_residue_gate.py` is still absent (AT-35-E1-005 builds it) — the residue check could not run; the coarse grep stands in.
- **Files touched:** `docs/retro/sd34-book-completion-retrospective.md` (new), `docs/release/SD-34-book-completion/references/README.md`, `docs/release/SD-34-book-completion/progress.md` (frontmatter `status` + prepended fold entry), `docs/release/SD-34-book-completion/kanban.md` (row mapping only — a `Folded → SD-35 <criterion>` pointer prepended to the Notes cell of the 17 open rows, plus one header paragraph; statuses unchanged), `docs/release/SD-35-corpus-sheet-completion/references/README.md`, `docs/release/SD-35-corpus-sheet-completion/decisions.md` (§9 L14–L16), `docs/release/SD-35-corpus-sheet-completion/artifacts/epic-1-tax-cut/sd34-deferral-dispositions.json` (new), `docs/release/SD-35-corpus-sheet-completion/artifacts/epic-1-tax-cut/sd34-open-row-map.json` (new — named by `acceptance-and-verification.md` AT-35-E1-006 row), `docs/retro/events/at-35-e1-006.jsonl` (new: 9 `resolution` + 1 `incident`), `docs/retro/events/sd31-transcribe.jsonl` (+1 `verification` row appended by `verify.sh --only pi-sweep`; the inherited shell `RETRO_ACTOR` is `sd31-transcribe`), this receipt, `progress.md`, `kanban.md`. No `src/`, `scripts/`, `tests/`, `data/`, `apps/`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS on every line this cycle authored, after one self-heal: the row map's row-number key was first spelled with an `sd34` prefix and an underscore, matched `\bsd[0-9]+_`, and was renamed `row` before the follow-on commit. The raw `git diff --unified=0 fe5ae6cd4a...HEAD -- <scoped paths> | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` still prints hits: they are the 17 SD-34 kanban rows re-emitted with the `Folded →` prefix, whose pre-existing text carries SD-13- and SD-25-era test identifiers from SD-34's own history — verified by pairing each `+` line, prefix stripped, against its `-` line (scratch `audit_added_lines.py`: `added_lines=1296 reprefixed_sd34_rows=17 authored_lines=1279`, 0 tag hits on authored lines).
- **Wired-integration audit result:** OK_NO_TOKENS on authored lines (same pairing; the raw grep's hits are the re-prefixed SD-34 rows quoting a wave-26 gate-lane note about a legitimate UI string at `reach_gate.rs:3192`).
- **Acceptance criterion:** `decisions.md §12`. SD-34 merged (PR #383, `fe5ae6cd4a`) without its closure epilogue. This docs-only cycle writes `docs/retro/sd34-book-completion-retrospective.md` from `python3 scripts/retro.py summary --since 2026-08-27 --json` in `docs/retro/sd31-retrospective.md`'s shape (raw tally, what the data says, what worked, what did not, named changes for the next bundle), cites it from `../SD-34-book-completion/references/README.md` **and** this package's `references/README.md`, maps SD-34's 17 open `kanban.md` rows to the SD-35 criterion that owns their units, and records in SD-34's `progress.md` that the bundle closed by operator merge with its epilogue folded here. Any "changes for the next bundle" the retrospective names that `decisions.md §9` does not already carry are added to `§9` in the same cycle. **The 29 open deferrals** `retro.py summary --since 2026-08-27 --json` reports at the cut (SD-34 recorded 3; the rest are SD-34 wave deferrals) are each dispositioned: resolved (with the resolving SHA), or mapped to the SD-35 criterion whose population owns the units, so `deferrals.open` for the SD-34 window reads 0 or names only SD-35-owned items. **Evidence:** the file exists; `grep -c sd34-book-completion-retrospective` ≥ 1 in both `references/README.md` files; the row map's unit counts sum to `completion_atlas.py --book core_rulebook --check` + `--book ultimate_campaign --check` non-DONE totals at `4c6c57eb9f`; SD-34's `progress.md` frontmatter `status` reads closed-by-fold; the 29-deferral disposition table in `artifacts/epic-1-tax-cut/sd34-deferral-dispositions.json` with every id. `SCOPE_GATE: EXEMPT` in the receipt (`decisions.md §2`).
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=unavailable` — literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 53296d80f0 --target-dir /tmp/cargo-sd35-AT-35-E1-006 --before <docs/work-inventory.json as of 4c6c57eb9f, via git show> --after docs/work-inventory.json` (`regressed=0 added=0 dropped=0`; `pcgen_live_files=unavailable` because `pcgen_residue_gate.py` is absent — the script refuses to fabricate a zero). Stand-in for the residue figure: `78` files by the coarse grep (`grep -rlE 'PcgenFormulaEvaluator|render_pcgen_desc|raw_tokens|bonus_stack_reader|pre_tokens' src/rules_core src/saved_character src/campaign src/homebrew_authoring apps/desktop/src-tauri/src | wc -l` → 78, equal to the authoring figure in `content-unit-inventory.md §6`; not risen).
- **PCGen residue:** `pcgen_residue_gate.py --check` not runnable (absent); coarse grep 78 of 78 files at authoring — unchanged.
- **Oracle parity:** N/A (no `Number` mapping; no live path touched).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 0 (docs only; `completion_atlas.py --check` DONE 26123 of 49438 before and after).
- **Refused tokens:** none.
- **Figures + their re-derive commands:**
  - Retro window: 387 events, 29 open deferrals before this cycle → `python3 scripts/retro.py summary --since 2026-08-27 --json` (`events.total`, `deferrals.open`), generated 2026-09-08T01:33Z; after this cycle's 9 `resolution` events: `deferrals.open=20`, `deferrals.resolved=14` of 34 (same command).
  - Dispositions: 29 of 29 — 8 resolved, 1 superseded, 20 mapped → `python3 -c "import json;d=json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-1-tax-cut/sd34-deferral-dispositions.json'));print(d['open_at_cut'],d['disposition_counts'])"`.
  - Row map: 17 open rows of 37; 1,590 of 1,590 units (core_rulebook 1,529 of 6,701 non-DONE: B 392, C 191, D 36, M 663, V 122, U 10, X 115; ultimate_campaign 61 of 265: D 2, M 36, U 21, X 2) → `python3 scripts/completion_atlas.py --book core_rulebook --check`, `--book ultimate_campaign --check`; per-kind split via `completion_atlas._bucket_of` over `docs/work-inventory.json`; `sum_check` field in `sd34-open-row-map.json`. By SD-35 owner: E3-001 392, E3-003 191, E4-001 699, E4-002 122, E5-002 38, E5-003 31, E5-004 117 (sums to 1,590).
  - Citations: 1 of 1 line in each `references/README.md` → `grep -c sd34-book-completion-retrospective docs/release/SD-34-book-completion/references/README.md docs/release/SD-35-corpus-sheet-completion/references/README.md`.
  - SD-34 board: DONE 12,265 of 49,438 at open (`artifacts/epic-1-atlas/AT-34-E1-001_cycle_receipt.md`) → 26,123 of 49,438 at the cut (`python3 scripts/completion_atlas.py --check | grep '  DONE:'`).
  - Kanban rows tagged: 17 of 17 open rows → `grep -c 'Folded → SD-35' docs/release/SD-34-book-completion/kanban.md` prints 18 (17 rows + 1 header paragraph).
- **Build scope verified:** not run this cycle — `git diff --stat 53296d80f0..HEAD` touches no `src/`, `tests/`, `data/`, `apps/`, `Cargo.*`, so every build input is byte-identical to the `tranche/15` cut, where `workflow-instruction.md §1` item 10 recorded `cargo test --locked --no-run` exit 0, 590 targets / 8,656 passed / 0 failed, desktop 573 / 0. A cold rebuild in a fresh `CARGO_TARGET_DIR` for a docs-only diff would cost the half-hour `decisions.md §2` exists to stop spending. Python gates run at `0dac3ddc9f`: `completion_atlas.py --check` exit 0 (`population=49438 unclassified=0 overlap=0 citation_failures=0`); `shape_engine_boundary.py --check` exit 0 (`magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`); `missing_engine_tables.py --check` exit 0 (`citation_failures=0`); `scripts/verify.sh --only pi-sweep` RESULT: PASS; `denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'` **exit 1: `files_checked=21 violations=11`** — all 11 in `artifacts/epic-2-sheet-rule/token-mapping/{SYNTHESIS (3), formula (2), prereq (5), prose (1)}.md`, committed by the token-mapping synthesis (`ba6b65e7b9`) and untouched by this cycle; `§1` item 12 ran the gate with the package-root glob only (`files_checked=16`), so the `artifacts/**` glob was never green. Recorded as retro incident `1788831974625-at-35-e1-006-ae4135` (`denominator-gate-red-on-untouched-artifacts`); owner AT-35-E2-001 / the synthesis. `token_coverage.py`, `sheet_rule_convert --check`, the `data/sheet_rules/` grep: instruments not yet built (Epic 2). `clippy`: no Rust touched.
- **Sweep population:** N/A (no corpus record changed).
- **Oracle pin:** N/A.
- **Status:** complete
- **Notes:** (1) The one `superseded` disposition (`1788329951728-sd34-orchestrator-11556d`, fable-review P1s R11-01/R14-02) is **not a fix**: the defects are unfixed at HEAD (`git log --grep 'R11-01\|R14-02\|character_id'` finds only the review `f42deaf5a1` and its fold `ff5f19e05e`) and `forward-scope-register.md` C2.5 declares all eight P1s out of SD-35 scope pending "a dedicated correctness SD-N, or the operator's call to fold into SD-35 by ruling". The deferral's recorded state (no lane owner inside SD-34) is what the register row supersedes. Surfaced for the operator. (2) Carried one-liner from SD-34 `forward-scope-register.md` C1.8 at `358a71516f` (`monk_ki_pool` "size" suffix in `CLASS_FEATURE_ID_MAGNITUDE_SUFFIXES`, `src/bin/v06_work_inventory.rs`): `core_rulebook:class_feature:monk_ki_pool` is bucket C at the cut (`status=engine-does-not-hold`, `evidence=no_explanation_id_and_no_diagnostic_names_this_feature`), so it is AT-35-E3-003's unit; the classifier one-liner is applied there, converter/classifier side, not in this docs-only cycle. (3) `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` was re-stamped (`derived_at`) by `completion_atlas.py --check` and reverted with `git checkout --` — outside this cycle's file set; the committed stamp `4c4d07f89d` is still an ancestor of HEAD (`stale_derived_at=False`). (4) SD-34 `kanban.md` statuses were left as they stood; the row-open grep in `workflow-instruction.md §1` item 4 still counts 17 by design — the header paragraph and each row's `Folded →` pointer say why.
- **Next-cycle scope:** criterion at zero.
