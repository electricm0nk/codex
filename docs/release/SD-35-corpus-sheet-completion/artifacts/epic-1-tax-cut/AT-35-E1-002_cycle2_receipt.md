# Cycle AT-35-E1-002 cycle 2 — Epic 1 — Tax cut / AT-35-E1-002

- **Commit SHA:** see the commit carrying this file (`fix(sd35): AT-35-E1-002 cycle 2 …`), on `4e321d2c6c`
- **Scope gate:** `SCOPE_GATE: EXEMPT (instrument-hardening cycle — closes zero units by design, decisions.md §2)`
- **Files touched:** `scripts/tests/test_shape_engine_boundary.py`, `scripts/tests/test_completion_atlas.py`, `scripts/tests/test_missing_engine_tables.py`, `scripts/shape_engine_boundary.py` (one hardcoded prose fraction in the generated report, made computed — see Discoveries), the three regenerated instrument artifacts `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/{completion-atlas.json,missing-engine-tables.json,shape-engine-boundary.md}` (written by the `--check`s themselves), this receipt, `progress.md`, `kanban.md`, `docs/retro/events/at-35-e1-002.jsonl` (2 corrections). No `verify.sh`, no `verify-baselines.env`, no Rust, no corpus.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- scripts/completion_atlas.py scripts/shape_engine_boundary.py scripts/missing_engine_tables.py scripts/tests/test_{shape_engine_boundary,completion_atlas,missing_engine_tables}.py scripts/verify.sh | grep -cE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → `0`. The 4 hits the same grep returns when `scripts/verify-baselines.env` is added to the pathspec are **AT-35-E1-003's**, not this criterion's: comment prose naming the real consolidated test paths `tests/sd18_widening/main.rs` and `tests/sd13_progression/main.rs`.)
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** `scripts/completion_atlas.py`, `scripts/shape_engine_boundary.py`, and `scripts/missing_engine_tables.py` pin `file:line` citations into `src/bin/v06_work_inventory.rs`. Every code edit shifts them; wave 51 found two of three drifted at HEAD. Replace the line-number pin with a **content anchor** — the exact source lines the bucket keys on, inside a named function — resolved by search at check time, so a refactor that moves code keeps the citation green and a change to the cited content still fails closed (SD-34 AT-34-E1-002 condition 6 preserved). Same cycle: add `--by-kind` and `--by-evidence` output modes to `completion_atlas.py` (only `--check`, `--by-book`, `--book` exist at authoring). Wire `shape_engine_boundary.py --check`, `missing_engine_tables.py --check`, and `scripts/tests/test_shape_engine_boundary.py` into `scripts/verify.sh` as real stages, and give `site-dashboard-check` the timeout wrapper SD-33's register asked for. Every place that asserts the stage count moves in the same cycle. **Evidence:** RED→GREEN — move a cited function 50 lines by inserting a comment block, all three `--check`s stay green; change one cited condition, all three fail. `scripts/verify.sh --list` shows the new stages; `scripts/verify.sh --only shape-engine-boundary` exits 0 with population printed.
- **Why a cycle 2:** cycle 1 landed the criterion at `815139fadd` and it was marked complete. Re-verified at `4e321d2c6c` — six Epic 2 / Epic 3 commits later — the criterion's own deliverable is **intact** (`citation_failures=0` in all three instruments, across a corpus-wide conversion that rewrote `v06_work_inventory.rs`: exactly the drift the content anchors were built to survive), but the stage this criterion added to `verify.sh`, `shape-engine-boundary-selftest`, was **RED**. Four SD-34-era live-figure *equality* pins in the three instrument test files had gone stale because Epic 2's conversion legitimately drained the populations they pinned. A red stage in this criterion's own file-touch set is this criterion's blocker (`workflow-instruction.md §8`, self-healable: "a count assertion this cycle's own change moved" — here, moved by the bundle the instrument measures).
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=1 pcgen_live_files=260` — literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 4e321d2c6cccef32a52acd3f76d68ce78f8c5d3b --before /tmp/wi-before-AT-35-E1-002-c2.json --after docs/work-inventory.json` (detail lines: `regressed=0 added=0 dropped=0`, `closed_by_kind=` empty, `relabeled_moves=` empty). `builds_recorded=1` is the tool's floor, not a cargo invocation this cycle made — see **Build scope verified**.
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` — `python3 scripts/pcgen_residue_gate.py --check`. Identical to cycle 1's line; not risen.
- **Oracle parity:** N/A (no `Number` mapping, no live path touched, no Rust changed)
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / **instrument-correction: 4 stale live-figure equality pins re-shaped into ceiling-plus-structure assertions**, restoring `shape-engine-boundary-selftest` to green.
- **Refused tokens:** none
- **Discoveries:** the criterion's content anchors are **proven in production**, not only against synthetic source: between `815139fadd` and `4e321d2c6c`, Epic 2 rewrote the classifier in `src/bin/v06_work_inventory.rs` (`AT-35-E2-003` added the `sheet-complete` rung, moving every anchored condition), and all sixteen anchors still resolve — `citation_failures=0` in all three instruments. The *equality pins on live populations* in the same files are the shape that did **not** survive, and cycle 1's own comment in `test_shape_engine_boundary.py` had already recorded that shape going stale for six SD-34 waves. Re-pinning to the new live value each cycle would repeat it a seventh time, so the assertion is now the invariant that holds under the bundle's direction of travel (`AGENTS.md` rule 8 — a recurrence gets a mechanism, not another edit). Recorded as `correction` event `1788880325253-at-35-e1-002-c18322`. **Second discovery, same shape:** regenerating the boundary report exposed a *hardcoded* prose fraction in `shape_engine_boundary.py`'s report template — "Roughly a third of the shape engine's own feedstock is still stuck downstream of it", true at 8784/26396, false at 363/26396. It is now computed from the two figures it describes (`1.4%`), so it cannot go stale (`AGENTS.md` rule 9). `correction` event `1788880480278-at-35-e1-002-1f21ab`. **One misfiled duplicate:** `1788880149398-sd31-transcribe-8d1c42` in `docs/retro/events/sd31-transcribe.jsonl` is the same correction, written under the wrong actor because `RETRO_ACTOR` was not exported in that shell (each `Bash` call resets the environment); the canonical record is the `at-35-e1-002.jsonl` one.
- **Figures + their re-derive commands:**
  - stages `48` of 48 — `scripts/verify.sh --list | tail -n +2 | wc -l` (45 at the end of cycle 1; +3 from AT-35-E2-004's `token-coverage` pair and its sibling. This criterion's three stages — `shape-engine-boundary-selftest`, `shape-engine-boundary`, `missing-engine-tables` — are rows 22–24 of `scripts/verify.sh --list`.)
  - python cases `138` of 138 across the three instrument suites + `test_denominator_gate.py` — `cd scripts && python3 -m unittest tests/test_completion_atlas.py tests/test_missing_engine_tables.py tests/test_shape_engine_boundary.py tests/test_denominator_gate.py` → `Ran 138 tests … OK` (before this cycle: `FAILED (failures=4)`)
  - atlas `population=49438 buckets=10 unclassified=0 overlap=0 … citation_failures=0`, DONE 48034 / A 1 / B 437 / C 79 / D 43 / M 63 / V 392 / U 202 / X 168 / Z 19 (each of 49,438 units) — `python3 scripts/completion_atlas.py --check`
  - boundary `magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True` (26,396 of 49,438 units; 363 of those 26,396) — `python3 scripts/shape_engine_boundary.py --check`
  - tables `population=1 kinds=1` (`power` 1, `ultimate_psionics`; 1 of 49,438 units) `citation_failures=0` — `python3 scripts/missing_engine_tables.py --check`
  - stage runs — `scripts/verify.sh --only shape-engine-boundary` → `PASS (magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True)`; `--only shape-engine-boundary-selftest` → `PASS (ran 15)`; `--only missing-engine-tables` → `PASS`; `--only pi-sweep` → `PASS`
  - denominator gate `files_checked=38 violations=0` (38 of 38 package `.md` files in the two globs) — `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'`
  - residue `live_files=260 live_hits=12736` (of the six live-side roots the gate greps) — command in the **PCGen residue** row
  - fail-closed proof `4 of 4 mutations → failures=1 each` — `cd scripts && python3 <scratchpad>/mutproof.py`, which monkeypatches each instrument to (1) over-count `not_held_by_engine`, (2) regress bucket A to 450, (3) report a third kind, (4) report a `power` unit from `bestiary`, and asserts each re-shaped test fails
- **Build scope verified:** **no cargo run this cycle, and none required.** `rust_lines_changed=0` (`git diff --numstat 4e321d2c6c -- '*.rs'` is empty), no `src/`, `apps/`, `tests/` or `data/` path is touched, and the classifier is untouched — `workflow-instruction.md §6` step 3's conditions for `--no-fail-fast`, `corpus_literal_sweep`, `sheet_rule_convert --check`, clippy, the desktop crate and the frontend all evaluate false. The three python instruments, their four suites and the four `verify.sh` stages named above are the whole verification surface this cycle moves, and all are green at `4e321d2c6c` + this working tree. `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` → `0`.
- **Sweep population:** N/A (no corpus record changed)
- **Oracle pin:** N/A
- **Status:** complete
- **Notes:** the three test files are the criterion's own instruments (cycle 1's receipt records the same "closed under the change" reasoning for the two beyond `§3`'s named `test_shape_engine_boundary.py`). Nothing outside them changed, so no other lane's file is disturbed and `scripts/verify-baselines.env` — Epic 1's one collision — is untouched by this criterion in both cycles.
- **Next-cycle scope:** criterion at zero
