# Cycle AT-35-E2-005_cycle2 — Epic 2 — Sheet rule / AT-35-E2-005

- **Commit SHA:** `33deab007b87c0844a34d2e1fd13302d286ccf5d` (the evaluator fix, the harness fix, the re-run parity artifacts, the design amendment and the retro events, as pushed; followed by a one-line fold of a live `sd31-transcribe` retro-log append found on the shared checkout); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`
- **Scope gate:** `scoped=1404 remaining_non_done=1404 floor=500 verdict=PASS` — `python3 scripts/cycle_scope_gate.py --min 500` (no scope flags: the whole remainder, all 37 books; `scoped_by_bucket=A:1 B:437 C:79 D:43 M:63 U:202 V:392 X:168 Z:19`, `scoped_by_kind=ability:91 class:144 class_feature:641 companion:13 equipment:188 equipment_modifier:40 feat:93 monster:2 power:1 race_trait:168 skill:7 spell:6 template:9 trait:1`) at cycle start `bfa6e81364`. `python3 scripts/pcgen_residue_gate.py --check` at start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `src/rules_core/sheet_rule.rs` (live side, no PCGen: `HeldSet.classes` + `HeldSet::holds` treating a `class` kind rule id as held by the character's levels; `render_sheet` prints a `#bonusN` sibling only when its own `applies` includes; 3 new tests), `scripts/oracle_harness/sheet_parity.py` (roster pre-bakes each race's fixed ability adjustment read from the pinned PCGen data; the comparator removes `ACCHECK` from `SKILL.n.MISC` on armor-check skills and joins `also` values in their own role), `scripts/oracle_harness/sheet-totals.txt.ftl` (`ACCHECK`, `SKILL.n.ACHECK`), `scripts/tests/test_sheet_parity.py` (12 → 17 tests), `technical-design.md §2` (the held-set paragraph names both evaluator mechanisms), `artifacts/epic-2-sheet-rule/oracle-parity/` (`roster/` 4 non-human `.txt` fixtures re-baked, 29 `.pcg` byte-identical; `exports/` 29 re-exported at the same pin, +553 rows; `ours.json`; `sheet-parity.json`), `docs/retro/events/at-35-e2-005.jsonl` (3 corrections + 1 deferral). **No mapping row added**; `src/pcgen_import/sheet_rule/` and `data/sheet_rules/**` untouched (the conversion pass wrote a byte-identical package); `docs/work-inventory.json` regenerated once, guarded, and reverted — its only diff was `generated_at` (0 units moved, below).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` at start and on the final diff)
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own code diff (`git diff --unified=0 bfa6e81364...HEAD -- src scripts ':!**/*.test.*'`); the §6 grep over the whole Epic 2 set (`fe5ae6cd4a...HEAD`) finds the same 6 pre-existing hits AT-35-E2-002 and cycle 1 recorded, none in code: 3 `data/sheet_rules/**` prose hits (`tophet_swallow_whole` "hack", `plant_growth` "hack", `courtly_hunter_courtly_companion` "[… not yet implemented]" — rulebook text, correction `1788844812035-at-35-e2-002-7cbeb2`) and 3 `docs/work-inventory.json` rows (`core_rulebook:class_feature:empty_selection_standard_{barbarian,monk,rogue}`) whose corpus description carries the word, on both sides of cycle 1's regenerated file.
- **Acceptance criterion:** Run the converter over every record, regenerate `data/sheet_rules/`, regenerate the inventory once. Report: units moved into DONE by bucket and kind (id-set diff), the refused report by token type, the projected remainder per token type, and the pass's wall time. **No mapping row is added in this cycle.** Then run the oracle harness over the fixture roster and compare every `Number` value the evaluator produces against PCGen's exported total for the same character — this is the "use PCGen to test the rewrite" check, and it runs here first so a wrong mapping is caught before Epics 3–4 build on it. **Evidence:** the receipt with `cycle_scope_gate.py --receipt` rows; `token-coverage.json` re-derived; `completion_atlas.py --check` before and after; the oracle comparison with `PCGEN_ORACLE_SHA`, `compared=<n> agree=<n> disagree=<n>` and every disagreement named with its `Expr` and PCGen's value.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=120 ratio=n/a builds_recorded=2 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since bfa6e81364acd488246d3022f0262a77fb47497d --before /tmp/wi-before-AT-35-E2-005.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-005` at `33deab007b87c0844a34d2e1fd13302d286ccf5d` (`closed_by_kind=` empty, `relabeled_moves=` empty, `regressed=0 added=0 dropped=0`; `rust_lines_changed` 120 is the one live-side file — `git diff --numstat bfa6e81364...HEAD -- src/rules_core/sheet_rule.rs` → `117 3`, of which the tests hunk (`@@ -2188,6 +2209,99 @@`) is +93 and the two mechanisms with their doc comments are +24/−3; `ratio=n/a` because `closed=0` — `decisions.md §4`'s ratio has no denominator in a zero-closure cycle, and the epic wrap-up reads this row as "what the lines bought": 11 → 0 parity disagreements; `builds_recorded` 2: the release build for the converter/parity binaries and the debug build for the inventory tools, the verification chain's own `--no-run` build reusing the latter's artifacts)
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `33deab007b` — unchanged (the one live-side file changed, `src/rules_core/sheet_rule.rs`, reads the package and the facts only; no PCGen token, formula or `raw_tokens` read was added).
- **Oracle parity:** **`compared=8 agree=8 disagree=0`** (`unverifiable=39`) over the evaluator's `Number` values, **`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6`** — `python3 scripts/oracle_harness/sheet_parity.py compare --ours artifacts/epic-2-sheet-rule/oracle-parity/ours.json --exports artifacts/epic-2-sheet-rule/oracle-parity/exports --output artifacts/epic-2-sheet-rule/oracle-parity/sheet-parity.json` (cycle 1: `compared=12 agree=1 disagree=11 unverifiable=42`). Same 29-character roster; the engine rendered **270 lines, 28 `Number` + 19 `also` numbers = 47 `Number` values** (`class_feature` 18, `race_trait` 4, `skill` 3, `feat` 2, `equipment` 1) — cycle 1's 488 lines / 272 values included **218 gated sibling lines that printed although their gates excluded** (Fighter Bonus Feats' 22 archetype `#bonusN` −1 lines per fighter, the Climb/Swim +8 Racial and +3/+6 Skill Focus lines, the chain shirt's "Broken" −2, …); those are now off the sheet. **The 8 agreements:** `deterministic_human_fighter_l1` `target:Ac:Armor` 4 = `AC.ARMOR` 4 (the Broken sibling no longer prints), `target:Ac:Dodge` 1 = `AC.DODGE` 1, `target:Skill:climb` 3 = `SKILL.3.MISC-ACCHECK` 3, `target:Skill:swim` 3 = `SKILL.17.MISC-ACCHECK` 3, `target:Skill:intimidate` 3 = `SKILL.10.MISC` 3; `human_bard_l1` Bardic Performance **7** rounds/day = DESC 7 and `human_bard_l10` **25** = DESC 25 (`Sum[Const(2), AbilityMod(Cha), Mul[Const(2), Var BardLevel]]`, the bard-level `Var` now folding from `ClassLevel("bard")`); `human_paladin_l10` Lay on Hands **8** uses/day = DESC 8 (`Sum[Div[Var PaladinLevel, Const(2)], AbilityMod(Cha)]`). **The 39 unverifiable, by reason:** `no-component-export` 19 (17 `Pool` counters — Bonus Feats, Arcane Bond, Weapon Training picks; 1 `WeaponAttack` on the chosen Weapon Focus weapon; 1 `Other("accheck")`), `no-pcgen-ability-named` 16 (the gnome's / half-elf's racial SLA lines Speak with Animals / Dancing Lights / Ghost Sound / Prestidigitation, which PCGen lists as spells, not abilities), `DESC-has-no-CasterLevel` 2 + `DESC-has-no-SaveDc` 2 (`paladin_detect_evil` CL 1 / CL 10 and DC 13: PCGen's description prints "60 feet" and "3 rounds", never a caster level or a DC — cycle 1 booked these 4 as disagreements through a role-blind join). The chassis totals those lines feed (context, not the criterion's population): `compared=382 agree=376 disagree=6 unverifiable=140` (cycle 1: 359 / 23 / 140); the 6: `halfling_fighter_l1` saves +1 short of PCGen (`CHECK.{0,1,2}.TOTAL` 2/2/3 vs ours 1/1/2 — Halfling Luck's +1 is a `Var` contribution `Halfling_HalflingLuck_SaveBonus` on a `Text` race-trait rule; no held rule renders a Save-target line for it and the chassis holds no racial save bonus) and `human_paladin_l10` saves +3 short (Divine Grace, `core_rulebook:class_feature:paladin_divine_grace` `Max[AbilityMod(Cha), Const(0)]` on each save, `print=false`, not seeded — the chassis grounds no Divine Grace); the 140 are the chassis's documented zero-sentinel for members without the deterministic posture. **B1 stays cleared** and the template is wider: `ACCHECK` and `SKILL.n.ACHECK` (`NONE`/`YES`/`NONPROF`/`DOUBLE`/`WEIGHT`) on every export.
- **Movement, four buckets:** closure **0** / relabel **0** / reachability **0** / instrument-correction **3** (retro corrections `1788863033934-at-35-e2-005-21cf2f`, `1788863034117-at-35-e2-005-7b04c9`, `1788863985904-at-35-e2-005-660e06`) — `python3 <scratch>/movement.py /tmp/wi-before-AT-35-E2-005.json docs/work-inventory.json` after the guarded regen: `units before=49438 after=49438 added=0 dropped=0`, buckets `A:1 B:437 C:79 D:43 DONE:48034 M:63 U:202 V:392 X:168 Z:19` on both sides, `bucket moves: none`, `status moves: none`, `evidence moves: 0`. Expected: the rung reads each rule's value **form** through `evaluate` for the probe (not `render_sheet`), so neither the sibling gate nor the class-level fold moves a stamp; the converter is unchanged, so the refusal set is unchanged.
- **Refused tokens:** unchanged from cycle 1 — the converter's report is byte-identical (`records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`), and **the remainder refusals leave non-DONE at HEAD is 659 of 1,404** (`refused_non_done=659`, 69 strings, multiplicity 851): `FORMULA:var(COUNT)=210, unmapped:STARTSKILLPTS=119, SPELLS (PI-redacted token)=66, FORMULA:malformed (parser refusals)=62, BONUS:[redacted PI]=62, DEFINE (PI-redacted token)=40, unmapped:MODTOSKILLS=37, unmapped:SPELLSTAT=23, unmapped:MEMORIZE=19, FORMULA:var(<export token>) (ENCUMBERANCE)=17, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlinePowers …)=12, unmapped:SPELLLIST=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlineSpells …)=11, FORMULA:var(SKILL.<name>.MISC)=11, FORMULA:CL-no-owner=11, BONUS:EQM=11, BONUS:ITEMCOST=10, FORMULA:var(STAT)=7, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=7, BONUS:STAT (target BASESPELLSTAT;Class)=7, BONUS:SITUATION (target shape)=6, unmapped:KNOWNSPELLS=5, FORMULA:var(SPELLFAILURE)=5, and 46 strings at 1–4 each` (`artifacts/epic-2-sheet-rule/token-coverage.json` `refusal_shapes`). The other **745** non-DONE units are non-promotable statuses: V `literal-verified` 389 + `fixture-verified` 3 (AT-35-E4-002), U `unmeasurable` 202, X `deferred-with-reason` 137, Z `not-started` 19 (Epic 5) — deferral `1788863079814-at-35-e2-005-291114`. This cycle is a zero-new-mappings cycle by the criterion's own text; every one of the 659 needs a converter mapping row (AT-35-E4-001), and the 182 refused `class` records (all of `unmapped:STARTSKILLPTS/SPELLSTAT/MEMORIZE/SPELLLIST/BONUSSPELLSTAT/SPELLBOOK`) are the ones whose absence the class-level fold now routes around.
- **Discoveries:** (1) **cycle 1's root cause 2 was mis-attributed** — `held_set` does seed classes (`package.find("class", slug)`); the declarer is absent because the converter refuses all 182 `class` records, and the contribution is `ClassLevel(<class>)`, computable from the facts — fixed on the live evaluator, no seeding change and no mapping row (correction `…-21cf2f`); (2) **cycle 1's discovery 3 was a harness defect, not a chassis one** — the chassis's fixture contract pre-bakes every non-Human race's fixed adjustment into the score; the roster generator wrote the flat base scores for the engine while PCGen applied the race; 22 of the 23 chassis disagreements (correction `…-7b04c9`); (3) **the comparator itself produced 4 of cycle 1's 11 disagreements and folded the armor check penalty into the skill oracle** (correction `…-660e06`). Two remaining-step observations for the atlas, neither a new token type or kind: Halfling Luck's +1 save is a `Var` contribution on a `Text` race-trait rule with no held Save-target consumer (Epic 3 holdings), and `paladin_divine_grace` is `print=false` with Save targets the chassis totals do not yet fold (Epic 3 / Epic 5 — the design's "same fold, keyed by `target`" is not wired into the chassis totals). No new token type, kind, or remaining-step category outside `token-coverage.json` / the atlas.
- **Figures + their re-derive commands:**
  - conversion pass wall time **24.72 s** (release, this box, 49,438 records; `git status --porcelain -- data/sheet_rules | wc -l` → **0**, byte-identical to the committed package) — `/usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/release/sheet_rule_convert`; `--check` **20.47 s** → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`; **measured first** in cycle 1 (`--one` ≈ 8 s fixed cost, projected ≈ 23 s), so the projection stated before this run was cycle 1's 24.6 s
  - inventory regeneration wall time **748.95 s** (`CORPUS_LITERAL_SWEEP_REPORT=… DERIVED_FIXTURE_CHECK_REPORT=… /usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/debug/v06_work_inventory`; prerequisites `corpus_literal_sweep --json-out` 172.17 s → `CLEAN`, `derived_evaluator_fixture_check --json-out` 15.00 s → `1839 unit(s) cleared over 2580 fixture row(s); 0 failed`); `git diff --stat -- docs/work-inventory.json` → `1 insertion(+), 1 deletion(-)` (`generated_at` only), reverted with `git checkout`
  - id-set movement (denominator: 49,438 units) — the movement script above: **0 / 0 / 0**; `completion_atlas.py --check` **before** (at `bfa6e81364`) and **after** (at `33deab007b`) both `population=49438 buckets=10 unclassified=0 overlap=0` `DONE: 48034 A: 1 B: 437 C: 79 D: 43 M: 63 V: 392 U: 202 X: 168 Z: 19` `done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas artifacts; reverted, outside this epic's set — as every Epic 2 cycle)
  - `token_coverage.py --check` re-derived: `non_done=1404 tokened=1399 token_less=5 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS` — the ledger is unchanged (no converter change); projected remainder per token type as cycle 1 (`carrying_non_done`: `TYPE 1126, CATEGORY 1002, SOURCE* 913, KEY 904, DESC 605, ABILITY 564, BONUS:VAR 502, …`; 7 types carry ≥ 500 non-DONE units)
  - oracle roster export wall time **221.68 s** for 29 characters at `--jobs 3` (one BatchExporter run **19.76 s**, the template validated on the deterministic fighter first: `ACCHECK=-2`, `SKILL.3.ACHECK=YES`, `SKILL.10.ACHECK=NONE`, 18 skills = 7 `YES` + 11 `NONE`) — `python3 scripts/oracle_harness/sheet_parity.py export --roster <dir> --out <dir> --jobs 3`; roster regeneration `python3 scripts/oracle_harness/sheet_parity.py roster --out <dir>` → Dwarf `{CON +2, WIS +2, CHA -2}`, Elf `{DEX +2, INT +2, CON -2}`, Gnome `{CON +2, CHA +2, STR -2}`, Halfling `{DEX +2, CHA +2, STR -2}`, Human / Half-Elf / Half-Orc none (a choice, left unmade on both sides), `git diff --stat -- roster` → 4 `.txt` files, 29 `.pcg` byte-identical (`diff` against `pcg_text` → identical); engine side **6.92 s** for 29 characters — `$CARGO_TARGET_DIR/release/sheet_rule_parity --roster <dir> --output ours.json`
  - parity rows by family/verdict — `python3 -c` over `sheet-parity.json` `results`: `lines agree 8 / disagree 0 / unverifiable 39`, `chassis agree 376 / disagree 6 / unverifiable 140`; `unverifiable_reasons` `engine-posture-absent 140, no-component-export 19, no-pcgen-ability-named 16, DESC-has-no-CasterLevel 2, DESC-has-no-SaveDc 2`
  - rendered lines 488 → **270** and `Number` values 272 → **47** — `python3 -c` over `ours.json` (lines / `form == "number"` / `also` `Resolved` values)
  - the 182 refused `class` records — `python3 -c` over `data/sheet_rules/_refused.json` `entries` with `:class:`; `test -d data/sheet_rules/core_rulebook/class` → absent
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-005`, `CARGO_INCREMENTAL=0`, 99.5 s); `cargo test --locked --lib -j 6` → **3217 passed; 0 failed; 14 ignored** (53.7 s; 3 new `sheet_rule::evaluate_tests`, RED first: `Resolved(0)` vs `Resolved(5)`, four Climb lines vs one, `5 rounds` vs 7 — then GREEN 31 `sheet_rule::` tests); `cargo test --locked --no-fail-fast -j 6` → **412 test binaries, 412 ok, 0 FAILED — 8,721 passed, 0 failed, 67 ignored** (2,361.0 s; `grep -c '^test result'` / `awk` over the result lines in the run log); `cargo clippy --locked --tests --lib --bin sheet_rule_parity -j 6` → 0 warnings (108.9 s); `python3 -m unittest scripts/tests/test_sheet_parity.py` → `Ran 17 tests … OK` (RED on the HEAD module first: 4 failures + 3 errors, `AttributeError: parse_racial_stat_bonuses`, `'SKILL.1.MISC' != 'SKILL.1.MISC-ACCHECK'`, the role-blind `also:DC 15` join); `sheet_rule_convert -- --check` PASS; literal scan 0; residue PASS; atlas / token-coverage / shape-engine-boundary (`magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True`) / missing-engine-tables (`citation_failures=0`) / denominator-gate (`files_checked=34 violations=0`) / pi-sweep (`RESULT: PASS`, 39.7 s) green; desktop crate and frontend: epic cadence (`apps/` untouched); run at SHA `33deab007b87c0844a34d2e1fd13302d286ccf5d`.
- **Sweep population:** `corpus_literal_sweep` examined the corpus as the guarded regen's prerequisite: `CLEAN` (172.17 s); no corpus record changed (the sweep ran for the stamp guard, not for a corpus edit).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; every BatchExporter run in `oracle-parity/exports/` ran against `$PCGEN_REPO_DIR/build/install/pcgen` at that pin via `charbuild_remainder_run_one.sh`; the roster's racial adjustments were read from the same checkout's `core_essentials/races/*/*_abilities_race.lst`)
- **Status:** partial — the criterion's population is not zero at HEAD (1,404 non-DONE of 49,438: 659 refused by token type, 745 in non-promotable statuses), and this cycle closes none by the criterion's own "no mapping row" rule; what it names as its own evidence — the pass, the report, the ledger and the oracle check — is re-run at HEAD, and the oracle check now agrees on every value it can compare (8 of 8), with the two live-evaluator defects the first run found fixed on the live side.
- **Notes:** Judgment calls, each cited: (a) this remainder cycle could close zero units without violating "No mapping row is added in this cycle", so it spent its build on the parity findings the criterion exists to surface — the evaluator fixes are in Epic 2's file-touch set (`src/rules_core/sheet_rule.rs`) and cycle 1's receipt had routed them to AT-35-E3-001, which is gated on this criterion; no Epic 3 cycle is live (§8, no conflicting cycle). (b) Only siblings are gated at render time; a principal rule the seed holds still prints when its `applies` excludes — `Holdable::ClassSkill` reads `facts.class_skills`, which `CharacterFacts::from_character` never fills, so gating principals would drop every seeded skill's class-skill +3 (Epic 3's `applies`/holdings scope, named here rather than widened). (c) `scripts/oracle_harness/` is Epic 4's file-touch set, touched here as cycle 1 did (B1's owner); tool side (`decisions.md §11`). (d) The regenerated inventory was not committed: a `generated_at`-only diff is churn, and the receipt carries the regen's figures. (e) The full `--no-fail-fast` run is the build this cycle pays; the desktop crate and frontend are at epic cadence (`apps/` untouched; `reach_gate.rs`'s sheet-lines test pins no line count).
- **Next-cycle scope:** AT-35-E2-005 has no zero-population form without mapping rows; Epic 2 is complete as a set of instruments and Epic 3 opens on `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --or --bucket C --or --bucket D` (`scoped=559 remaining_non_done=1404 floor=500 verdict=PASS`) or the whole 1,404 remainder; its first mechanisms are now the holdings the parity names (Halfling Luck's Save consumer, Divine Grace's `print=false` Save targets, `facts.class_skills`), not the two evaluator defects, which are closed. AT-35-E4-001's mapping scope is the 659 refused units by the token strings above (the ≥ 10-type re-scope rule of §8 applies: 69 strings / ~16 shapes), the 182 `class` records first.
