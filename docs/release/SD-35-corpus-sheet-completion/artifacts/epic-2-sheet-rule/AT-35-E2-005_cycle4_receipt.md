# Cycle AT-35-E2-005_cycle4 — Epic 2 — Sheet rule / AT-35-E2-005

- **Commit SHA:** the commit carrying this receipt and the two retro events on `tranche/15` (the cycle's tree at `bf9594943f053c7341cd19c6f69f0ab5bfed2bc0` is where every figure below was re-derived; the cycle moved no file under `src/**`, `scripts/**`, `data/sheet_rules/**`, `apps/**` or `docs/work-inventory.json`); the docs commit carrying `progress.md` and `kanban.md` follows it
- **Scope gate:** `scoped=1404 remaining_non_done=1404 floor=500 verdict=PASS` — `python3 scripts/cycle_scope_gate.py --min 500` (no scope flags: the whole remainder, all 37 books; `scoped_by_bucket=A:1 B:437 C:79 D:43 M:63 U:202 V:392 X:168 Z:19`, `scoped_by_kind=ability:91 class:144 class_feature:641 companion:13 equipment:188 equipment_modifier:40 feat:93 monster:2 power:1 race_trait:168 skill:7 spell:6 template:9 trait:1`) at cycle start `bf9594943f053c7341cd19c6f69f0ab5bfed2bc0`. `python3 scripts/pcgen_residue_gate.py --check` at start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `docs/release/SD-35-corpus-sheet-completion/artifacts/epic-2-sheet-rule/AT-35-E2-005_cycle4_receipt.md` (this file), `docs/retro/events/at-35-e2-005.jsonl` (1 deferral + 1 rework), `docs/retro/events/codex.jsonl` + `docs/retro/events/root.jsonl` (two derived `reclaim.sh` incident appends from the shared checkout's cron at 08:00:15 EDT, folded as the standing "fold live retro-log append" practice — `git log --oneline -3 -- docs/retro/events/codex.jsonl`). **No mapping row added**; the conversion pass wrote a byte-identical package (`git status --porcelain -- data/sheet_rules | wc -l` → 0); the guarded inventory regen's only diff was `generated_at`, reverted; the engine-side parity re-run produced the same 270 lines and the same `sheet-parity.json` (`cmp` identical), `ours.json` reverted (its only diff was the `roster` path spelled relative instead of absolute).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` at start and on the final diff — `BASE_BRANCH=fe5ae6cd4a5f3c65d5d10f4d523f00e33b04ac47`)
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff (`git diff --unified=0 bf9594943f...HEAD -- <Epic 2 file-touch set> docs/retro/events ':!**/*.test.*'` → no match); the §6 grep over the whole Epic 2 set (`fe5ae6cd4a...HEAD`) finds the same 6 pre-existing hits AT-35-E2-002 and cycles 1–3 recorded, none in code: 3 `data/sheet_rules/**` prose hits (`tophet_swallow_whole` "hack", `plant_growth` "hack", `courtly_hunter_courtly_companion` "[… not yet implemented]" — rulebook text, correction `1788844812035-at-35-e2-002-7cbeb2`) and 3 `docs/work-inventory.json` rows (`core_rulebook:class_feature:empty_selection_standard_{barbarian,monk,rogue}`) whose corpus description carries the word.
- **Acceptance criterion:** Run the converter over every record, regenerate `data/sheet_rules/`, regenerate the inventory once. Report: units moved into DONE by bucket and kind (id-set diff), the refused report by token type, the projected remainder per token type, and the pass's wall time. **No mapping row is added in this cycle.** Then run the oracle harness over the fixture roster and compare every `Number` value the evaluator produces against PCGen's exported total for the same character — this is the "use PCGen to test the rewrite" check, and it runs here first so a wrong mapping is caught before Epics 3–4 build on it. **Evidence:** the receipt with `cycle_scope_gate.py --receipt` rows; `token-coverage.json` re-derived; `completion_atlas.py --check` before and after; the oracle comparison with `PCGEN_ORACLE_SHA`, `compared=<n> agree=<n> disagree=<n>` and every disagreement named with its `Expr` and PCGen's value.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since bf9594943f053c7341cd19c6f69f0ab5bfed2bc0 --before /tmp/wi-before-AT-35-E2-005.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-005` (`closed_by_kind=` empty, `relabeled_moves=` empty, `regressed=0 added=0 dropped=0`; `rust_lines_changed` 0 — no Rust file touched; `ratio=n/a` because `closed=0`; `builds_recorded` 0 — cycle 3's warm `CARGO_TARGET_DIR` served every binary: `cargo test --locked --no-run -j 6` finished in 1.17 s with nothing to compile)
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and after the cycle's work — unchanged (no live-side file touched).
- **Oracle parity:** **`compared=42 agree=41 disagree=1`** (`unverifiable=5`) over the evaluator's `Number` values, **`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6`** — engine side re-run at HEAD (`$CARGO_TARGET_DIR/release/sheet_rule_parity --roster <oracle-parity>/roster --output <oracle-parity>/ours.json` → `characters=29 lines=270 wall=5.0s`, 47 `Number` values, the `characters` array identical to cycle 3's), then `python3 scripts/oracle_harness/sheet_parity.py compare --ours <oracle-parity>/ours.json --exports <oracle-parity>/exports --output <oracle-parity>/sheet-parity.json` → `lines compared=42 agree=41 disagree=1 unverifiable=5; chassis compared=382 agree=376 disagree=6 unverifiable=140; characters=29 exports_missing=0`, `sheet-parity.json` byte-identical to cycle 3's (`cmp`). The exports were **not** re-exported: no live-side, converter or template change since cycle 3, the pin is unchanged, and cycle 3's 29 exports at the same pin are the oracle. **The one disagreement, named (unchanged from cycle 3):** `deterministic_human_fighter_l1` `target:WeaponAttack:{"Chosen": "core_rulebook:feat:weapon_focus"}` **ours 0, PCGen 1** (`WEAPON.0.TOTALHIT` +6 − `ATTACK.MELEE.TOTAL` +5) — `Expr` `{"Number": {"Var": "vb1e14268d73c2def"}}`, whose var table `data/sheet_rules/_vars/vb1e14268d73c2def.json` has one contribution, `Const(1)` (`Base`) declared by `core_rulebook:class_feature:default` (`cr_abilities.lst:11`), a rule the held set does not hold — a holdings gap (Epic 3, AT-35-E3-001), not a mapping or evaluator defect. The 5 unverifiable: `SPELL-dc-not-numeric` 4 (Dancing Lights, Speak with Animals, Detect Evil ×2 — PCGen prints a blank `DC` for a no-save spell), `no-component-export` 1 (`Other:accheck`). The 6 chassis disagreements are Halfling Luck's +1 and Divine Grace's +3 on the three saves (`halfling_fighter_l1` `CHECK.{0,1,2}.TOTAL` ours 1/1/2 vs 2/2/3; `human_paladin_l10` ours 6/3/9 vs 9/6/12) — Epic 3 / Epic 5 holdings, as cycles 2–3 recorded.
- **Movement, four buckets:** closure **0** / relabel **0** / reachability **0** / instrument-correction **0** — after the guarded regen `git diff --stat -- docs/work-inventory.json` → `1 insertion(+), 1 deletion(-)` (`generated_at` `2026-09-08T09:12:49Z` → `2026-09-08T12:15:38Z` only), reverted with `git checkout`; `cmp docs/work-inventory.json /tmp/wi-before-AT-35-E2-005.json` after the revert → identical, so buckets `A:1 B:437 C:79 D:43 DONE:48034 M:63 U:202 V:392 X:168 Z:19` on both sides, no bucket, status or evidence move. Expected: `git diff --stat c0f16fe417..HEAD -- src scripts data/sheet_rules docs/work-inventory.json apps` is empty — nothing that feeds the converter, the classifier or the parity changed since cycle 3.
- **Refused tokens:** unchanged from cycles 1–3 — the converter's report is byte-identical (`records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`), and **the remainder refusals leave non-DONE at HEAD is 659 of 1,404** (`refused_non_done=659`, 69 strings / 81 shapes, multiplicity 851): `FORMULA:var(COUNT)=210, unmapped:STARTSKILLPTS=119, SPELLS (PI-redacted token)=66, FORMULA:malformed (parser refusals)=62, BONUS:[redacted PI]=62, DEFINE (PI-redacted token)=40, unmapped:MODTOSKILLS=37, unmapped:SPELLSTAT=23, unmapped:MEMORIZE=19, FORMULA:var(<export token>) (ENCUMBERANCE)=17, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlinePowers …)=12, unmapped:SPELLLIST=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlineSpells …)=11, FORMULA:var(SKILL.<name>.MISC)=11, FORMULA:CL-no-owner=11, BONUS:EQM=11, BONUS:ITEMCOST=10, FORMULA:var(STAT)=7, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=7, BONUS:STAT (target BASESPELLSTAT;Class)=7, BONUS:SITUATION (target shape)=6, unmapped:KNOWNSPELLS=5, FORMULA:var(SPELLFAILURE)=5, and 46 strings at 1–4 each` (`artifacts/epic-2-sheet-rule/token-coverage.json` `refusal_shapes`). The other **745** non-DONE units are non-promotable statuses: V `literal-verified` 389 + `fixture-verified` 3 (AT-35-E4-002), U `unmeasurable` 202, X `deferred-with-reason` 137, Z `not-started` 19 (Epic 5) — deferral `1788869018190-at-35-e2-005-ab58c2`. Every one of the 659 needs a converter mapping row (AT-35-E4-001); this criterion forbids adding one.
- **Discoveries:** none — no new token type, kind, or remaining-step category outside `token-coverage.json` / the atlas; the cycle is a byte-for-byte re-derivation of cycle 3 (rework `1788869018329-at-35-e2-005-7d897d` records why it ran: cycle 3's `partial` re-triggered the remainder loop on a criterion whose own rule makes its population unreachable; the §8 >10-refused-type rule was named by cycle 3 and by this cycle's dispatch prompt, and nothing mechanical stopped the re-dispatch).
- **Figures + their re-derive commands:**
  - conversion pass wall time **25.64 s** (release, this box, 49,438 records; `git status --porcelain -- data/sheet_rules | wc -l` → **0**, byte-identical to the committed package) — `/usr/bin/time -f 'wall=%es' cargo run --locked --release --bin sheet_rule_convert`; `--check` **20.23 s** → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`; **projection stated before the run:** cycle 3's 25.44 s — the standing measurement holds
  - inventory regeneration wall time **726.14 s** (`CORPUS_LITERAL_SWEEP_REPORT=… DERIVED_FIXTURE_CHECK_REPORT=… /usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/debug/v06_work_inventory`; prerequisites `corpus_literal_sweep --json-out` **147.48 s** → `CLEAN`, `derived_evaluator_fixture_check --json-out` **12.08 s** → `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested`); projection stated before the run: cycle 3's 776.27 s; `git diff --stat -- docs/work-inventory.json` → `1 insertion(+), 1 deletion(-)` (`generated_at` only), reverted
  - id-set movement (denominator: 49,438 units) — `cmp` above: **0 / 0 / 0**; `completion_atlas.py --check` **before** (cycle-start inventory) and **after** (the regenerated inventory, before its revert) both `population=49438 buckets=10 unclassified=0 overlap=0` `DONE: 48034` `done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas artifacts; reverted, outside this epic's set — as every Epic 2 cycle)
  - `token_coverage.py --check` re-derived: `check shape_totals: shapes=81 ok=True` / `check partition: token_types=231 ok=True` / `non_done=1404 tokened=1399 token_less=5 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS` — `token-coverage.json` unchanged (`git status --porcelain` on it empty); projected remainder per token type as cycles 1–3 (`carrying_non_done`: `TYPE 1126, CATEGORY 1002, SOURCE* 913, KEY 904, DESC 605, ABILITY 564, BONUS:VAR 502, …`; 7 types carry ≥ 500 non-DONE units)
  - engine side **5.27 s** for 29 characters (`characters=29 lines=270`); parity rows by family/verdict from `sheet-parity.json`: `lines agree 41 / disagree 1 / unverifiable 5`, `chassis agree 376 / disagree 6 / unverifiable 140`; `unverifiable_reasons` `engine-posture-absent 140, SPELL-dc-not-numeric 4, no-component-export 1`
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
  - distinct refused token strings **69** (shapes **81**) over the 659 refused non-DONE units — `token-coverage.json` `refusal_shapes`; §8's threshold is 10
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-005`, `CARGO_INCREMENTAL=0`, 1.17 s — every target cached from cycle 3, no Rust source changed); `cargo test --locked --lib -j 6` → **3217 passed; 0 failed; 14 ignored** (52.24 s); `cargo test --locked --no-fail-fast` **not run** — §6 requires it when `src/` or the classifier changed, and neither did (`git diff --stat bf9594943f..HEAD -- src` is empty); clippy **not run** — no Rust target touched; `python3 -m unittest scripts/tests/test_sheet_parity.py` → `Ran 24 tests … OK`; `sheet_rule_convert -- --check` PASS; literal scan 0; residue PASS; atlas / token-coverage / shape-engine-boundary (`magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True`) / missing-engine-tables (`citation_failures=0`) / denominator-gate (run on this receipt before the docs commit; its line is in `progress.md`'s entry) / pi-sweep (`RESULT: PASS`, 8.46 s, `11 hits over src/rules_core/rules_tables, 11 baseline rows`) green; desktop crate and frontend: epic cadence (`apps/` untouched); run at SHA `bf9594943f053c7341cd19c6f69f0ab5bfed2bc0`.
- **Sweep population:** `corpus_literal_sweep` examined the corpus as the guarded regen's prerequisite: `CLEAN` (147.48 s); no corpus record changed (the sweep ran for the stamp guard, not for a corpus edit).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; the 29 exports in `oracle-parity/exports/` are cycle 3's, run against `$PCGEN_REPO_DIR/build/install/pcgen` at that pin; this cycle joined the HEAD engine output to them).
- **Status:** blocked-escalated — `workflow-instruction.md §8`'s non-self-healable "more than 10 distinct refused token types surfacing in one cycle (the mapping table is behind the corpus — re-scope, do not grind)": 69 strings / 81 shapes over 659 units, unchanged across four cycles, on a criterion whose own text forbids the only mechanism that moves them. The criterion's evidence obligations (the pass timed, the report by token type, the projected remainder, the ledger re-derived, the atlas checked before and after, the oracle comparison with its one disagreement named with `Expr` and PCGen's value) are met at HEAD and were met at cycle 3; its population (1,404 non-DONE) cannot reach zero without a mapping row, so `partial` would re-trigger a fifth identical cycle. This is a request to the orchestrator to re-scope, not an operator ruling — no `## Open blockers` entry is filed.
- **Notes:** (a) Every figure was re-derived at HEAD rather than quoted from cycle 3, so the cycle paid the pass (25.64 s), the guarded regen (≈ 14.8 min) and the engine-side parity (5.27 s), but not a PCGen re-export (≈ 3.5 min at `--jobs 3`): nothing the exporter reads changed, and `sheet-parity.json` came out byte-identical. (b) The two `reclaim.sh` derived events on `codex.jsonl` / `root.jsonl` landed on the shared checkout during this cycle (a cron, not this agent — `.reclaim-claim` protected the target dir) and are folded rather than left untracked. (c) `builds_recorded=0`: no build was paid.
- **Next-cycle scope:** not a cycle 5 on AT-35-E2-005. Epic 3 opens on `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --or --bucket C --or --bucket D` (`scoped=559 remaining_non_done=1404 floor=500 verdict=PASS`) or the whole 1,404 remainder — its first mechanism is the holdings gap the parity names (Weapon Focus's `class_feature:default` declarer; the 19 unprinted pools; Halfling Luck's and Divine Grace's save consumers). AT-35-E4-001's mapping scope is the 659 refused units by the token strings above, the 182 `class` records first.
