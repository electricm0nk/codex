# Cycle AT-35-E2-001_cycle2 — Epic 2 — Sheet rule / AT-35-E2-001

**This is a re-dispatch of an already-closed criterion, not new work.** AT-35-E2-001 landed at
`72ad0be010` (receipt `AT-35-E2-001_cycle1_receipt.md`; `kanban.md` row 7 `complete`). This cycle
re-verified every clause of the criterion's `Evidence:` sentence at HEAD `4510517993`, changed no
code and no data, and closes zero units — the same shape as the three earlier re-dispatch
re-verifications on this branch (`986084c5a4`, `06872eff73`, `3c43cf0531`).

- **Commit SHA:** `969d5b9402cc29bf42b662bfeeea155e84714d5e` (this receipt, `progress.md`, `kanban.md`, the two retro events — a docs-only commit; this line's own SHA is pinned by the follow-up commit, the pattern `942c8d3ae5` set for `AT-35-E1-002_cycle2`). The tree verified is HEAD `4510517993194cdc89f4dae56ca1380836fb6b15`, unchanged by this cycle.
- **Scope gate:** `SCOPE_GATE: EXEMPT (converter-building cycle — closes zero units by design; AT-35-E2-005 is the pass that moves the population)` (`decisions.md §2`; the criterion is additionally already at zero, so the re-verification moves nothing by construction). `python3 scripts/pcgen_residue_gate.py --check` at cycle start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `artifacts/epic-2-sheet-rule/AT-35-E2-001_cycle2_receipt.md` (this file, new), `progress.md`, `kanban.md`, `docs/retro/events/at-35-e2-001.jsonl` (2 corrections appended). **No file outside `docs/` changed** — no `src/`, no `scripts/`, no `data/`, no `apps/`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...4510517993 -- <Epic 2 file-touch set>`, `':!**/__tests__/**' ':!**/*.test.*'`)
- **Wired-integration audit result:** **4 hits, every one attributed and none a stub in shipping code** — the cycle-1 receipt's `OK_NO_TOKENS` is corrected here (retro `1788882358027-at-35-e2-001-83a134`). The audit's keyword class is a grep over English prose as well as code, and all four hits are prose:
  | Hits | File | Word | What it actually is |
  |---:|---|---|---|
  | 1 | `data/sheet_rules/bestiary_3/monster_ability/tophet_swallow_whole.json` | `hack` | Pathfinder rules text — "attempt to **hack** or smash its way out" |
  | 1 | `data/sheet_rules/core_rulebook/spell/plant_growth.json` | `hack` | Pathfinder rules text — "must **hack** or force a way through" |
  | 1 | `data/sheet_rules/ultimate_intrigue/class_feature/courtly_hunter_courtly_companion.json` | `not yet implemented` | the **source's own editorial bracket** inside the transcribed `Desc`, not a claim by our code — recorded as a table defect below |
  | 6 | `docs/work-inventory.json` | `placeholder` | `reason` fields describing the source's CHOOSE-menu "no selection" rows (pre-existing wording, unchanged by this cycle) |
  No `STUB`, `MOCK`, `todo`, `fixme` or `"Would …"` string appears in any code path. `workflow-instruction.md §8`'s non-self-healable "a stub, inline mock, or `\"Would …\"` string in shipping code" is **not** met.
- **Acceptance criterion:** `src/bin/sheet_rule_convert.rs` + `src/pcgen_import/sheet_rule/` (`technical-design.md §1`). Input: every corpus record's token closure. Output: `data/sheet_rules/<book>/<kind>/<key>.json`, one `SheetRule` per record in **our** schema — `label`, `value` (`Number(Expr)` / `Dice` / `Text`), substituted `prose`, `applies`, `provenance`. **No PCGen token, formula string, or variable name appears in the output.** A token type with no mapping row makes the record `Refused { token_type }`, written to `data/sheet_rules/_refused.json` and counted per token type. The converter calls the existing PCGen formula parser to get an AST and maps the AST to `Expr`; it never emits the source string. **The mapping rows are transcribed from `token-mapping/mapping-table.v1.json`** (`decisions.md §15`); a mapping not in the table is a table defect to record, not a rule to invent in the cycle. **Evidence:** `cargo run --locked --bin sheet_rule_convert -- --check` exits 0 with `records=49438 converted=<n> refused=<n>` summing to the corpus; `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` is **0**. Unit tests on real records for each value form: the racial SLA DC converting to `Sum([Const(10), SpellLevel, AbilityMod(Cha)])`; a weapon converting to `Dice{"1d8", Some(Const(2))}`; a `%CHOICE` trait converting to `Text` with `Choice` in `applies`. One converter gate **per kind** reading the live corpus directory: every record of that kind converts or is in `_refused.json`.
- **Evidence, clause by clause, re-derived at HEAD:**
  | Clause | Command | Result |
  |---|---|---|
  | `--check` exits 0, counts sum to the corpus | `cargo run --locked --release --bin sheet_rule_convert -- --check` | `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS (33.8s)`, `CHECK_EXIT=0`; 47,628 + 1,810 = **49,438** |
  | no source format in our data | `grep -rlE 'BONUS:\|DEFINE:\|PRE[A-Z]+:\|%CHOICE\|CL=' data/sheet_rules/ \| wc -l` | **0** |
  | one gate per kind, over the live corpus directory | `cargo test --locked --test sheet_rule_convert_gate -j 6` | **27 passed, 0 failed** (189.32 s) — 19 per-kind gates + the three value-form tests on real records + literal scan + freshness + determinism + the token census (the 27th, added by AT-35-E2-004; cycle 1 recorded 26) |
  | the three value forms, and the schema | `cargo test --locked --lib sheet_rule -j 6` | **31 passed, 0 failed**, 3,200 filtered out |
  | per-kind conversion, all 19 kinds | the `kind …` lines of the `--check` log | ability 4337=4246+91, class 185=3+182, class_feature 18043=17683+360, companion 1696=1682+14, deity 459=458+1, domain 183=183+0, equipment 6223=6044+179, equipment_modifier 1532=1504+28, feat 2764=2030+734, language 136=136+0, monster 1270=1257+13, monster_ability 3806=3795+11, power 421=420+1, race 95=68+27, race_trait 2561=2431+130, skill 149=141+8, spell 2843=2829+14, template 2248=2234+14, trait 487=484+3 — **every kind sums** |
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 4510517993194cdc89f4dae56ca1380836fb6b15 --before /tmp/wi-before-AT-35-E2-001.json --after docs/work-inventory.json` (`regressed=0 added=0 dropped=0`). All four zeros are correct: the cycle re-verified an already-closed criterion and wrote nothing outside `docs/`.
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` — identical at start and end, and identical to the cycle-1 receipt's. Nothing on the live side was touched.
- **Oracle parity:** N/A — no `Number` mapping row was added (no mapping row of any kind was added). The fixture-roster comparison is AT-35-E2-005's evidence and stands at `compared=42 agree=41 disagree=1 unverifiable=5` (`oracle-parity/sheet-parity.json`).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 2 (retro corrections `1788882358027-at-35-e2-001-83a134` — the cycle-1 audit row; `1788882373210-at-35-e2-001-f81ef5` — the table defect below).
- **Refused tokens:** none refused *by this cycle* — it added no mapping row and converted nothing new. The converter's standing refusal set at HEAD is unchanged from cycle 1: **1,810 records / 81 refusal shapes / 231 token types**, of which **659 are non-DONE** (`token_coverage.py --check` → `non_done=1404 tokened=1399 token_less=5 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS`). Those 659 are owned by AT-35-E4-001 under `### AT-35-E2-005-DISPOSITION`'s hand-off table, not by this criterion. Cycle 1's log recorded 82 shapes; the live census reads **81** — AT-35-E2-004's ledger is the current instrument.
- **Discoveries:** one, and it is a **table defect recorded rather than fixed**, exactly as the dispatch scope note requires. `data/sheet_rules/ultimate_intrigue/class_feature/courtly_hunter_courtly_companion.json` prints the source's own editorial bracket — `[Change to magical beast and stacking restriction not yet implemented]` — as sheet prose. It is a faithful transcription of the source `Desc`, so the converter is behaving as specified; what is missing is a mapping-table row that scrubs source editorial annotations out of printed prose. **No such row exists in `token-mapping/mapping-table.v1.json`, so inventing one inside this cycle would be the defect the scope note names** (`decisions.md §15`). Filed as retro correction `1788882373210-at-35-e2-001-f81ef5` against the mapping table, for the table's owner. Magnitude: 1 record of 47,628 converted; it changes no computed value and no count. This is **not** a `## Open blockers` entry — the criterion's Definition of Done does not require it, and nothing downstream is paused by it.
- **Figures + their re-derive commands:**
  - `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081` — `cargo run --locked --release --bin sheet_rule_convert -- --check` (denominator: the 49,438 inventory units joined to corpus records; the two parts sum to it exactly)
  - literal scan **0** files — `grep -rlE 'BONUS:\|DEFINE:\|PRE[A-Z]+:\|%CHOICE\|CL=' data/sheet_rules/ \| wc -l` (denominator: all 52,720 files under `data/sheet_rules/`)
  - converter gate **27 passed / 0 failed**; lib **31 passed / 0 failed** — the two `cargo test` lines above
  - refused non-DONE **659** of **1,404** non-DONE, over 49,438 — `python3 scripts/token_coverage.py --check`
  - residue **`live_files=260 live_hits=12736`** — `python3 scripts/pcgen_residue_gate.py --check` (denominator: the five live roots `src/rules_core`, `src/saved_character`, `src/campaign`, `src/homebrew_authoring`, `apps/desktop`)
  - wall time **33.8 s** for the whole population (release, this box, cold-compiled first) — the `--check` summary line's own parenthetical; cycle 1 measured ~23 s and AT-35-E2-005 measured 24.6 s / 25.64 s. The three figures are the same pass on a differently-loaded box, not a regression: this run shared the box with another lane's build.
  - atlas `unclassified=0 overlap=0 done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` — `python3 scripts/completion_atlas.py --check`
  - `shape_engine_boundary.py --check` → `magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True`; `missing_engine_tables.py --check` → `population=1 kinds=1 (power 1) citation_failures=0`; `denominator_gate.py --check` over the package → `files_checked=40 violations=0`; `scripts/verify.sh --only pi-sweep` → `RESULT: PASS` (11 hits, 11 baseline rows)
- **Build scope verified:** `cargo test --locked --no-run -j 6` → `NO_RUN_EXIT=0`; `cargo test --locked --lib sheet_rule -j 6` → 31 passed / 0 failed; `cargo test --locked --test sheet_rule_convert_gate -j 6` → 27 passed / 0 failed. The full `cargo test --locked --no-fail-fast -j 6` was **not** re-run and is **not** required by `workflow-instruction.md §6` step 3 — its condition is "when `src/` or the classifier changed", and this cycle changed neither (`rust_lines_changed=0`). The desktop crate and frontend are at epic cadence (`apps/` untouched). `cargo clippy` not re-run: no Rust source changed. `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-001`, `CARGO_INCREMENTAL=0`. Run at SHA `4510517993194cdc89f4dae56ca1380836fb6b15`.
- **Sweep population:** N/A — no corpus record changed, so `corpus_literal_sweep` is skipped per `§6` step 3's guard. `docs/work-inventory.json` was not regenerated for the same reason (its inputs are unchanged; regenerating would move only `generated_at`).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`) — every rule's `provenance.oracle_pin` carries it; the closure is read from that checkout.
- **Status:** complete
- **Notes:** Three judgment calls. (a) The criterion was already at zero on arrival, so the cycle's whole product is the re-verification and the two corrections; no work was re-done and no figure was re-stated without re-deriving it. (b) `completion_atlas.py --check` again re-stamped `../SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`'s `derived_at`, as cycle 1 recorded in note (f); reverted with `git checkout --`, outside this epic's file-touch set. (c) The cycle-1 receipt's `82` refusal strings and `26` gate tests are both superseded by the live instruments (81 shapes, 27 tests) — the drift is AT-35-E2-004's ledger and its added test, not a regression, and the receipt above states the live figure with its command.
- **Next-cycle scope:** criterion at zero — no next cycle for AT-35-E2-001. Epic 2 is complete across all five criteria (rows 7–11) plus row 30's disposition. The live front is Epic 3: `kanban.md` row 12 (AT-35-E3-001) is `blocked-escalated` awaiting the orchestrator's re-scope of its 214 converter-refused units into AT-35-E4-001's 659-unit refused bundle, per `### AT-35-E2-005-DISPOSITION`.
