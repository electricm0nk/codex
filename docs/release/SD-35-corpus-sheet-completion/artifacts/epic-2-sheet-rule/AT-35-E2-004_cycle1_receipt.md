# Cycle AT-35-E2-004_cycle1 — Epic 2 — Sheet rule / AT-35-E2-004

- **Commit SHA:** `344f18d1e1` (code, data, scripts, tests, ledger, retro events, the two package-doc figure fixes — as pushed; preceded by `9fe67f8096`, a fold of a live `sd31-transcribe` retro-log append found on the shared checkout); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`. Cycle start `6ce95e2b87`.
- **Scope gate:** `SCOPE_GATE: EXEMPT (ledger-building cycle — closes zero units by design)` (`decisions.md §2`; the ledger names the population every later cycle scopes, and AT-35-E2-005 is the pass that moves units). `python3 scripts/pcgen_residue_gate.py --check` at cycle start (`6ce95e2b87`): `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** converter side — `src/pcgen_import/sheet_rule/ctx.rs` (`RecordCtx.tokens`, `RecordCtx.refusal_under`, `carry`, `refuse_under`), `src/pcgen_import/sheet_rule/convert.rs` (`token_key`; every refusal site in the convert loop and the token-less-feat prerequisite loop records the head it fired under; `Converted.tokens` / `Converted.refusal_under`), `src/pcgen_import/sheet_rule/mod.rs` (`TokenCensus`, `TokenCensusRecord`, `TOKEN_LESS`; `Run.tokens`; the two pre-conversion refusal branches census as `token-less`; `_tokens.json` rendered one record per line through the shared `escape_token_strings`), `data/sheet_rules/_tokens.json` (new, generated: 49,438 entries, 14.6 MB), `src/bin/v06_work_inventory.rs` (`InventoryUnit.tokens` from `load_sheet_rule_tokens`, written as `"tokens": [...]` on every unit; 2 tests), `tests/sheet_rule_convert_gate.rs` (+1 census test on real records and the whole run), `scripts/token_coverage.py` (new), `scripts/tests/test_token_coverage.py` (new, 14 tests), `scripts/verify.sh` (`token-coverage-selftest` + `token-coverage` in both stage sets), `artifacts/epic-2-sheet-rule/token-coverage.json` (new, derived), `docs/retro/events/at-35-e2-004.jsonl` (2 corrections), `decisions.md §3` and `technical-design.md §6` (stage count 46 → 48, the gate table). **Live side untouched.**
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 6ce95e2b87...HEAD -- <Epic 2 file-touch set> scripts/verify.sh tests/sheet_rule_convert_gate.rs ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` on this cycle's diff, and the same grep over `fe5ae6cd4a...HEAD`).
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's diff (`git diff --unified=0 6ce95e2b87...HEAD -- <the same paths> | grep -nE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'`; `_tokens.json` carries 0 such words). Over the whole Epic 2 set since `fe5ae6cd4a` the grep finds the same **3** rulebook-prose lines in `data/sheet_rules/**` that AT-35-E2-002 recorded (correction `1788844812035-at-35-e2-002-7cbeb2`), none in code.
- **Acceptance criterion:** `scripts/token_coverage.py --check` writes `artifacts/epic-2-sheet-rule/token-coverage.json`: per top-level PCGen token type — units carrying it (non-DONE, all 37 books), units converted, units refused **because of this token**, and the converter mapping row that handles it. Sums checked: every non-DONE unit appears under at least one token or under `token-less`; the refused set across all tokens equals `_refused.json`'s id set. **This is a tool-side script reading the corpus; it is allowed to read PCGen.** **Evidence:** `--check` exits 0 with sums printed; RED→GREEN on a planted double-count. Wired into `verify.sh` as `token-coverage`. The remainder is named by token type from here on.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=295 ratio=n/a builds_recorded=3 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 6ce95e2b87b05fe9a6eac6094831f71a3cf5ca17 --before /tmp/wi-before-AT-35-E2-004.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-004`, run at `344f18d1e1` (`git diff --numstat 6ce95e2b87 -- '*.rs'`: `v06_work_inventory.rs` +111/−1, `convert.rs` +37/−10, `ctx.rs` +20/−0, `mod.rs` +70/−8, `sheet_rule_convert_gate.rs` +38/−0). `builds_recorded` 3: the RED compile of the census gate test (fails on the three missing fields — TDD's RED for the intended reason), the two-binary build that regenerated the package once, and the one verification chain. No inventory regeneration (`closed=0` by design; the `tokens` field lands on every unit at AT-35-E2-005's regen).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `344f18d1e1` — unchanged. The census, the ledger and the inventory's `tokens` list live on the converter/tool side (`src/pcgen_import/`, `src/bin/`, `scripts/`, `data/sheet_rules/`); no live-side file changed.
- **Oracle parity:** N/A — no `Number` mapping row was added; the converter's conversion is unchanged (`cargo run --locked --bin sheet_rule_convert -- --check` → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS (110.2s)`; the regeneration changed no file but the new `_tokens.json` — `git status --porcelain data/sheet_rules/` after the run listed only `?? data/sheet_rules/_tokens.json`).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 2 (retro corrections `1788853487539-at-35-e2-004-df8cba`, `1788853487721-at-35-e2-004-e91bf0`).
- **Refused tokens:** the ledger's remainder, by the token type each refusal arose under, **non-DONE units** (a unit refused under k token types counts under each; 49 token types, sum with multiplicity 850 over **659 distinct non-DONE refused units of 23,315 non-DONE**): `ABILITY=200, unmapped:STARTSKILLPTS=119, SPELLS (PI-redacted token)=66, BONUS:[redacted PI]=62, BONUS:VAR=60, DEFINE (PI-redacted token)=40, DESC=40, unmapped:MODTOSKILLS=37, unmapped:SPELLSTAT=23, BONUS:COMBAT=19, unmapped:MEMORIZE=19, BONUS:SKILL=15, ASPECT:<display sub-key>=13, unmapped:SPELLLIST=12, BONUS:EQM=11, BONUS:STAT=11, BONUS:ITEMCOST=10, BONUS:MOVEADD=9, BONUS:SITUATION=9, BONUS:MISC=5, token-less=5, unmapped:KNOWNSPELLS=5, PREVARGTEQ=4, PREVARNEQ=4, TEMPBONUS=4, [redacted PI] token=4, unmapped:NUMPAGES=4, unmapped:SPELLBOOK=4, BENEFIT=3, BONUS:HP=3, BONUS:WEAPONPROF=<name>=3, HITDIE (the %-step shape)=3, unmapped:BONUSSPELLSTAT=3, ASPECT:CheckCount / ASPECT:CheckType=2, BONUS:ABILITYPOOL=2, BONUS:SKILLRANK=2, unmapped:DOMAIN=2, unmapped:PRESPELLSCHOOL=2, ADD=1, ASPECT:NAME=1, BONUS:DR=1, BONUS:EQMWEAPON=1, BONUS:PCLEVEL=1, BONUS:SAVE=1, DR=1, NATURALATTACKS=1, PREVAREQ=1, SIZE (formula)=1, unmapped:ITEMCREATE=1` — `python3 -c "import json; L=json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-2-sheet-rule/token-coverage.json'))['token_types']; print(', '.join(f'{t}={v[\"refused_because_of_this_token_non_done\"]}' for t,v in sorted(L.items(), key=lambda kv:(-kv[1]['refused_because_of_this_token_non_done'], kv[0])) if v['refused_because_of_this_token_non_done']))"`. All statuses: 1,810 refused of 49,438 (sum with multiplicity 1,916 over 55 token types). No unit was scoped by this cycle, so no `deferral` event is owed (`workflow-instruction.md §2.3`).
- **Discoveries:** (1) **The E2-001 receipt's "82 refusal strings" is 81** — `data/sheet_rules/_refused.json` `by_token_type` has 81 keys at that receipt's tree and at HEAD; the ledger's `shapes=81` is the figure from here on (`…-df8cba`). (2) **The design's "47 stages" is 48** — the `<gate>-selftest` / `<gate>` pairing AT-35-E1-002 established makes `token-coverage` two stages (`…-e91bf0`; `decisions.md §3` and `technical-design.md §6` corrected in this commit). (3) **The census attributes the formula-shaped refusals the refusal report could not**: `FORMULA:var(COUNT)` (211 records) arises under `ABILITY` in 196 of them — the `ABILITY:...` grant token, not a bonus formula — so AT-35-E4-001's remainder for that shape is an `ABILITY` mapping question, not a `BONUS:VAR` one; `FORMULA:CL-no-owner` splits `BONUS:VAR` 11 / `DESC` 10 / `BONUS:COMBAT` 4. (4) **Only 7 of the 837 token-less records are non-DONE** (829 `no_corpus_record` + 8 `no_source_row`; the other 830 are already DONE by an earlier rung), so the mapping table's "3 unjoined spells" figure understates the token-less population but overstates its cost. (5) **24 token types have no mapping row** (`unmapped_token_types` in the ledger) — the same 24 heads E2-001 recorded as table defects (`…-4e50ce`), now each a ledger row with its non-DONE count, i.e. a scopeable mechanism rather than a note.
- **Figures + their re-derive commands:**
  - the ledger's sums (denominator: the 49,438 units of `docs/work-inventory.json` at `6ce95e2b87`, DONE per `completion_atlas.py`): **`non_done=23315 tokened=23308 token_less=7 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS`** — `python3 scripts/token_coverage.py --check` (last line; 2.6 s); the six named checks each print `ok=True` (`population`, `double_count`, `coverage`, `refused_set`, `shape_totals`, `partition`)
  - unmapped token types **24** of 231 — `python3 -c "import json; print(len(json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-2-sheet-rule/token-coverage.json'))['unmapped_token_types']))"`
  - token types carrying at least 500 non-DONE units **44** of 231 (the batch floor, `decisions.md §2`) — `python3 -c "import json; print(sum(1 for v in json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-2-sheet-rule/token-coverage.json'))['token_types'].values() if v['carrying_non_done']>=500))"`; the compute-bearing ones at the top: `BONUS:VAR` 6,963 (6,703 converted / 260 refused), `ABILITY` 6,167 (5,795 / 372), `PREVARGTEQ` 2,964 (2,763 / 201), `PREMULT` 2,694, `PREABILITY` 2,692, `!PRE* (all negated forms)` 1,900 (1,693 / 207), `CHOOSE` 1,785, `CLASSES` 1,543, `BONUS:ABILITYPOOL` 1,208, `PRECLASS` 1,191, `NATURALATTACKS` 981, `RANGE` 975, `PRESIZEEQ` 885, `BONUS:SKILL` 718, `SPELLS` 629, `PREFACT` 573, `ASPECT:CheckCount / ASPECT:CheckType` 560, `CSKILL` 520 (425 / 95) — each a `carrying_non_done` / `converted_non_done` / `refused_non_done` row of the same file
  - refusal strings **81** — `python3 -c "import json; print(len(json.load(open('data/sheet_rules/_refused.json'))['by_token_type']))"` (81 at `72ad0be010` too: `git show 72ad0be010:data/sheet_rules/_refused.json | python3 -c "import json,sys; print(len(json.load(sys.stdin)['by_token_type']))"`); shapes with a non-DONE record **69** of 81 — `python3 -c "import json; print(sum(1 for v in json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-2-sheet-rule/token-coverage.json'))['refusal_shapes'].values() if v['non_done']))"`
  - the census: **49,438 entries, 49,443 lines, 14,631,801 bytes** — `python3 -c "import json; print(len(json.load(open('data/sheet_rules/_tokens.json'))['entries']))"`, `wc -lc data/sheet_rules/_tokens.json`; source-format literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
  - the regeneration: `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 files=52721 (103.3s)` — `cargo run --locked -j 6 --bin sheet_rule_convert` (debug build, this box; E2-001's 23 s was a release build); `--check` **110.2 s** — the `chain-srcheck.log` line above
  - verify stages **48** (46 before this cycle) — `scripts/verify.sh --list | tail -n +2 | wc -l`; the two new stages green on the live tree: `scripts/verify.sh --only token-coverage-selftest --only token-coverage` → `PASS token-coverage-selftest (14 cases passed)`, `PASS token-coverage (non_done=23315 … verdict=PASS)`, `RESULT: PASS`
  - python RED→GREEN: `python3 -m unittest scripts/tests/test_token_coverage.py` → **RED `ModuleNotFoundError: No module named 'token_coverage'`** before the script existed; **GREEN `Ran 14 tests … OK`** after — the planted-double-count cases (`test_a_planted_duplicate_census_entry_fails_the_check`: `verdict=FAIL_DOUBLE_COUNT` exit 1, then `verdict=PASS` exit 0 with the plant removed; `test_a_planted_duplicate_token_on_one_record_fails_the_check`) plus coverage, refused-set, shape-total, population, stale-artifact and input-error cases
  - Rust RED→GREEN: `cargo test --locked --test sheet_rule_convert_gate -j 6 --no-run` → **RED `error[E0609]: no field `tokens` on type `Converted`` / `no field `refusal_under`` / `no field `tokens` on type `Run`` (9 errors)** before the census existed; **GREEN** in the chain below (`token_census_names_the_row_for_every_token_and_the_head_under_each_refusal ... ok`)
  - `completion_atlas.py --check` → `population=49438 buckets=10 unclassified=0 overlap=0` `done_evidence_violations=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's three atlas artifacts; reverted, outside this epic's set — same as E2-001/E2-002/E2-003); `shape_engine_boundary.py --check` → `magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`; `missing_engine_tables.py --check` → `citation_failures=0`; `denominator_gate.py --check` over the package globs → `files_checked=31 violations=0`; `scripts/verify.sh --only pi-sweep` → `RESULT: PASS`; `corpus_literal_sweep` skipped (no corpus record changed)
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-004`, `CARGO_INCREMENTAL=0`, 121 s wall, 0 warnings); `cargo test --locked --lib -j 6` → `3214 passed; 0 failed; 14 ignored` (53 s); `cargo test --locked --no-fail-fast -j 6` → **411 test binaries, 411 ok, 0 FAILED; 8,718 passed, 0 failed, 67 ignored** (8,715 + the 2 `load_sheet_rule_tokens_tests` + the census gate test; `awk` over the `test result` lines of `chain-full.log`; 2,356 s), no compiler warnings; `cargo clippy --locked --tests -j 6 --lib --bin v06_work_inventory --bin sheet_rule_convert --test sheet_rule_convert_gate` → 0 warnings (86 s); desktop crate and frontend at epic cadence (no `apps/` touch). Run on the working tree whose content is `344f18d1e1` (the fold commit `9fe67f8096` carries two retro-log lines only); chain total 2,727 s — `/tmp/cargo-sd35-AT-35-E2-004/chain.log`.
- **Sweep population:** N/A (no corpus record changed; `docs/work-inventory.json` not regenerated — AT-35-E2-005 regenerates it once, and every unit then carries its `tokens` list).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; unchanged; the converter read the pinned tree to regenerate the package — the census is derived from it).
- **Status:** complete
- **Notes:** Judgment calls, each cited: (a) **the census is the converter's, not a second reading of `data/corpus`** — the converter's closure (base row, `.COPY=` base, corpus-wide `.MOD` rows, class level lines, the `.lst` row for the 1,008 no-`raw_tokens` units — `technical-design.md §1`, B2/B5/B7/B9) is the population it converts, and `data/corpus` has no record for 829 of these units; "units carrying it" counted from `raw_tokens` would count a different thing (AGENTS.md rule 7: a proof is only as wide as the cases it covers). (b) **A token type is the mapping-table row key** `row_for_head` resolves (`BONUS:VAR`, `PREVARGTEQ`, `DESC`, …), so "the converter mapping row that handles it" is the row itself; a head with no row is its own key (`unmapped:<HEAD>` / `BONUS:<SUB>`), matching the refusal string. (c) **`refused_because_of_this_token` is attributed at the refusal site** (the head the convert loop was processing), which is what makes formula-shaped refusals attributable (discovery 3); `refused_non_done` on a row counts carrying units refused for *any* shape, so the row's partition `carrying = converted + refused` sums. (d) **`--check` fails on a stale committed ledger and rewrites it** — the artifact is "re-derived, never appended" (`workflow-instruction.md §5`), and a regen without a re-derive is the drift this gate exists to catch; the file has no timestamp or SHA so identical inputs are byte-identical. (e) **The inventory gains a `tokens` list** because `cycle_scope_gate.py --token` was written to read one "AT-35-E2-004 onward"; it is scope metadata only (no status pass reads it), empty until AT-35-E2-005's regen. (f) The two stages follow the selftest/gate pairing (discovery 2). (g) Prior receipts are not edited for the 82 → 81 figure; the correction event and this receipt carry it.
- **Next-cycle scope:** criterion at zero (the ledger, its check, its self-test and its stages exist; the remainder is named by token type above). AT-35-E2-005 next: regenerate the inventory once (`cargo run --locked --bin v06_work_inventory`; projected ~2 min for the package load + rung, per E2-003's timing), re-derive the ledger (`python3 scripts/token_coverage.py --check`), and run the oracle comparison; its `Refused tokens` row reads from the ledger's `refused_because_of_this_token_non_done` column after the regen.
