# Cycle AT-35-E2-001_cycle1 — Epic 2 — Sheet rule / AT-35-E2-001

- **Commit SHA:** `72ad0be010b9064add262f503500bad277142342` (code + generated package, as pushed); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`
- **Scope gate:** `SCOPE_GATE: EXEMPT (converter-building cycle — closes zero units by design; AT-35-E2-005 is the pass that moves the population)` (`decisions.md §2`). `python3 scripts/pcgen_residue_gate.py --check` at cycle start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `src/rules_core/sheet_rule.rs` (new — OUR schema v2, no source-format reading; the live evaluator lands in AT-35-E2-002), `src/rules_core/mod.rs` (`pub mod sheet_rule`), `src/pcgen_import/sheet_rule/{mod,table,closure,ctx,formula,prereq,prose,convert}.rs` (new — the converter), `src/pcgen_import/mod.rs`, `src/bin/sheet_rule_convert.rs` (new), `tests/sheet_rule_convert_gate.rs` (new — 3 value-form tests on real records, 19 per-kind gates, literal scan, freshness check, determinism), `scripts/verify.sh` (stage `sheet-rules-check`, all-set only, 45 → 46), `data/sheet_rules/**` (new, generated: 47,628 rule files + 5,081 `_vars/` tables + `_refused.json` + 9 `_defects/` + `_report.json`), `scripts/oracle_harness/var_names.json` (new, generated, tool side), `docs/retro/events/at-35-e2-001.jsonl` (3 corrections + 1 deferral), `decisions.md` §3 / `technical-design.md` §6 (stage count 46).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set>`)
- **Wired-integration audit result:** OK_NO_TOKENS (same base, same paths)
- **Acceptance criterion:** `src/bin/sheet_rule_convert.rs` + `src/pcgen_import/sheet_rule/` (`technical-design.md §1`). Input: every corpus record's token closure. Output: `data/sheet_rules/<book>/<kind>/<key>.json`, one `SheetRule` per record in **our** schema — `label`, `value` (`Number(Expr)` / `Dice` / `Text`), substituted `prose`, `applies`, `provenance`. **No PCGen token, formula string, or variable name appears in the output.** A token type with no mapping row makes the record `Refused { token_type }`, written to `data/sheet_rules/_refused.json` and counted per token type. The converter calls the existing PCGen formula parser to get an AST and maps the AST to `Expr`; it never emits the source string. **The mapping rows are transcribed from `token-mapping/mapping-table.v1.json`** (`decisions.md §15`); a mapping not in the table is a table defect to record, not a rule to invent in the cycle. **Evidence:** `cargo run --locked --bin sheet_rule_convert -- --check` exits 0 with `records=49438 converted=<n> refused=<n>` summing to the corpus; `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` is **0**. Unit tests on real records for each value form: the racial SLA DC converting to `Sum([Const(10), SpellLevel, AbilityMod(Cha)])`; a weapon converting to `Dice{"1d8", Some(Const(2))}`; a `%CHOICE` trait converting to `Text` with `Choice` in `applies`. One converter gate **per kind** reading the live corpus directory: every record of that kind converts or is in `_refused.json`.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=6463 ratio=n/a builds_recorded=3 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 928272a4444dd1cd9e5f177a3ba22ab6b3e36730 --before /tmp/wi-before-AT-35-E2-001.json --after docs/work-inventory.json` (`builds_recorded` > 1: a converter-building cycle iterates build → run → `--check` until the package is clean; 6 release rebuilds of one binary plus the test build, each recorded as a session by the timestamp clustering).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at end — unchanged (the schema file `src/rules_core/sheet_rule.rs` carries no source-format identifier or literal; the converter's own formula parser sits on the tool side, `src/pcgen_import/`).
- **Oracle parity:** N/A — the cycle adds `Number` mapping rows only as transcriptions of the table; the fixture-roster comparison is AT-35-E2-005's evidence (`B1`: the export set still lacks skill/speed/DR/DC/spells-per-day tokens).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 3 (retro corrections `1788840316439-at-35-e2-001-8ed3da`, `1788840316570-at-35-e2-001-569089`, `1788840316691-at-35-e2-001-4e50ce`).
- **Refused tokens:** 1,810 records / 82 refusal strings (a record refused by k types counts under each; type-sum 2,131) — `no_corpus_record=829, FORMULA:var(COUNT)=211, unmapped:STARTSKILLPTS=162, unmapped:SLOTS=95, FORMULA:malformed (parser refusals)=87, SPELLS (PI-redacted token)=78, BONUS:[redacted PI]=62, unmapped:ALTTYPE=49, unmapped:SPELLSTAT=49, unmapped:MODTOSKILLS=41, DEFINE (PI-redacted token)=40, unmapped:MEMORIZE=38, FORMULA:var(<export token>) (ENCUMBERANCE)=23, FORMULA:var(SKILL.<name>.MISC)=20, unmapped:SPELLLIST=19, FORMULA:CL-no-owner=16, unmapped:NUMPAGES=16, unmapped:ARMORTYPE=15, unmapped:KNOWNSPELLS=14, BONUS:EQM=13, BONUS:SITUATION (target shape)=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlinePowers …)=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlineSpells …)=11, FORMULA:var(AC)=11, BONUS:ITEMCOST=10, unmapped:GLOBALVAR:ABILITY=9, unmapped:SPELLBOOK=9, FORMULA:identifier DEFINEd nowhere (HP …)=8, FORMULA:var(STAT)=8, HITDIE (%-step: %+1, %/4)=8, no_source_row=8, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=7, BONUS:STAT (target BASESPELLSTAT;Class)=7, NATURALATTACKS (die shape)=7, FORMULA:var(<export token>) (ACCHECK)=6, FORMULA:var(<export token>) (MOVEBASE)=6, FORMULA:var(SPELLFAILURE)=5, unmapped:PAGEUSAGE=5, unmapped:BONUSSPELLSTAT=4, unmapped:DOMAIN=4, unmapped:[redacted PI]=4, BONUS (shape)=3, FORMULA:var(<export token>) (ENCUMBRANCE)=3, unmapped:ITEMCREATE=3, unmapped:PRESPELLSCHOOL=3, and 36 strings at 1–2 each` — the full list is `data/sheet_rules/_refused.json` `by_token_type` (`_report.json` `refused_by_token_type`); 659 of the 1,810 are non-DONE units (of 23,315)
- **Discoveries:** (1) **24 token heads outside the mapping table** once the closure is read from the pinned tree (class continuation rows, `.MOD` rows, the 1,008 no-`raw_tokens` base rows): `STARTSKILLPTS` 162, `SLOTS` 95, `ALTTYPE` 49, `SPELLSTAT` 49, `MODTOSKILLS` 41, `MEMORIZE` 38, `SPELLLIST` 19, `NUMPAGES` 16, `ARMORTYPE` 15, `KNOWNSPELLS` 14, `GLOBALVAR:ABILITY` 9, `SPELLBOOK` 9, `PAGEUSAGE` 5, `BONUSSPELLSTAT` 4, `DOMAIN` (non-level rows) 4, `ITEMCREATE` 3, `PRESPELLSCHOOL` 3, `MONCCSKILL` 2, `GROUP` 1, `PRESPELLSCHOOLSUB` 1, `PROHIBITSPELL` 1, and `BONUS` sub-targets `EQM` 13, `ITEMCOST` 10, `LOADMULT` 2, `EQMWEAPON` 1 — refused as `unmapped:<HEAD>` / `BONUS:<SUB>` (table defects, correction `…-4e50ce`); `PRERACETYPE` (37) converts under the PRERACE row's `RACETYPE=` clause as a documented head alias. (2) The epic's evidence sentence names `SpellLevel` where the table row folds the same-record setter to `Const(<spell level>)` (correction `…-8ed3da`) and a `+2` die literal no corpus record carries (correction `…-569089`). (3) Two feat descriptions spell `(NCL=` in English — the `=` glyph is dropped so the literal grep stays 0 (`_defects/literal-in-prose.json`, 55 lines incl. the same shape in `SPROP` texts). (4) Refusals are 82 distinct strings / ~16 token SHAPES (per-name `FORMULA:identifier DEFINEd nowhere (<name> …)` and per-prefix `FORMULA:var(<prefix>)` rows are one shape each) — above `§8`'s 10-type threshold, by construction of a converter-building cycle: the set is the table's own REFUSE rows plus the 24 table defects above, and it is what AT-35-E2-004 counts per shape and AT-35-E4-001 scopes by family; nothing here blocks this criterion's Definition of Done.
- **Figures + their re-derive commands:**
  - `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081` — `cargo run --locked --release --bin sheet_rule_convert -- --check` (denominator: `docs/work-inventory.json` units; 47,628 + 1,810 = 49,438)
  - refused per token type — same command, the `refused <n> <type>` lines; also `data/sheet_rules/_refused.json` `by_token_type` (a record refused by k types counts under each: type-sum 2,131 over 1,810 records)
  - refused non-DONE **659** of 1,810 (of 23,315 non-DONE); non-DONE converted **22,656** — `python3 -c` over `_refused.json` × `completion_atlas._bucket_of` (in `progress.md`)
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
  - wall time **~23 s** for the whole population (release build, this box; `--check` re-converts in memory and reads 52,720 files) — `time cargo run --locked --release --bin sheet_rule_convert -- --check`; the timing AT-35-E2-005 projects from
  - stages **46** all / 40 quick — `scripts/verify.sh --list | tail -n +2 | wc -l`
  - `_defects/`: `unresolved-references` 13,124 lines (Special Ability 3,522, FEAT 1,987, Internal 1,886 — references whose (category, KEY) joins no shipped record; a ledger for AT-35-E3-001's holdings lookup, not a refusal), `undefined-variables` 711, `grants-to-unconverted-targets` 789, `grant-by-type` 613, `choice-marker-without-choose` 126, `undeclared-contributed-variables` 56, `literal-in-prose` 55, `glued-tokens` 4, `double-percent-slot` 3 — `python3 -c "import json,os; ..."` over `data/sheet_rules/_defects/*.json` (`progress.md`)
  - table transcription: 249 rows both ways — `cargo test --locked --lib table_is_a_transcription_of_the_json`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-001`, `CARGO_INCREMENTAL=0`); `cargo test --locked --no-fail-fast -j 6` → 411 test binaries, 410 ok, 1 FAILED — the lib binary at the pre-fix tree (3203 passed, 3 failed: `sheet_rule::{ctx,formula,table}::tests` expectation drift, fixed in the same cycle); re-run at HEAD `cargo test --locked --lib -j 6` → 3206 passed, 0 failed, 14 ignored; 8,699 tests passed across the run; `cargo test --locked --test sheet_rule_convert_gate` → 26 passed, 0 failed (178 s; the three value-form tests, 19 per-kind gates, literal scan, freshness, determinism); `cargo clippy --locked --tests -j 6` on the touched targets → 0 warnings on `src/rules_core/sheet_rule.rs`, `src/pcgen_import/sheet_rule/**`, `src/bin/sheet_rule_convert.rs`, `tests/sheet_rule_convert_gate.rs` (15 fixed in the cycle); desktop crate and frontend: epic cadence (`apps/` untouched); run at SHA `72ad0be010b9064add262f503500bad277142342`.
- **Sweep population:** N/A (no corpus record changed; `docs/work-inventory.json` not regenerated — its inputs (corpus records, classifier) are unchanged and it does not read `data/sheet_rules/` until AT-35-E2-003; regenerating would move only `generated_at`)
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; every rule's `provenance.oracle_pin`; the closure is read from that checkout)
- **Status:** complete
- **Notes:** Judgment calls, each cited: (a) population = the 49,438 inventory units joined to corpus records (the evidence line's literal `records=49438`); unjoined units refuse as `no_corpus_record` (829, 3 non-DONE). (b) Token source = the record's shipped (product-identity-screened) `raw_tokens` when present, the pinned base row when absent (B7), `.MOD` rows always from the corrected corpus-wide, KEY-matched, `_pfs/`-skipping index (B9, R3), class continuation rows + numbered level lines (B5), the pinned DEFINE/BONUS:VAR index for variable resolution (B2); every emitted text passes `pi_screening::normalized_term_hit` (C20, `provenance.pi`). (c) The converter carries its own source-formula parser (`formula.rs`) rather than calling `rules_core::pilot_compute::formula_interpreter`: that live parser deliberately refuses table-mapped shapes (`charbonusto`, `count`, `mastervar`, `var`, bare-numeric `if`, `skillinfo` beyond `TOTALRANK`) and widening it would move live-side gates and the residue count; the design already schedules that file's move to the tool side (AT-35-E6-001). (d) One record file holds a JSON **array** of `SheetRule` — the principal rule plus siblings (`#spellN_…`, `#bonusN`, `#naturalN`, `#alt`) for the multi-line rows the table names (SPELLS, SAVE|ALL, NATURALATTACKS, ALTDAMAGE). (e) Token types in `_refused.json` / `_defects/` / `_report.json` are written with `:`, `%`, `=` JSON-escaped (`:` …) so the data package carries no source-format literal while a JSON reader sees the real string. (f) `completion_atlas.py --check` re-stamped `SD-34/artifacts/epic-1-atlas/completion-atlas.json`'s `derived_at`; reverted (`git checkout --`), outside this epic's file-touch set. (g) Same-record-only variables are folded at convert time (row 27) only when every own contribution is ungated or compare-gated (0/1 arithmetic, C17); otherwise the name goes through the `_vars/` table (row 28) — `_vars/` has 5,081 tables for the ids some rule references.
- **Next-cycle scope:** criterion at zero (the converter exists, writes our schema, the package is clean and fresh, the per-kind gates are green). AT-35-E2-002 next (`src/rules_core/sheet_rule.rs` gains `evaluate`; the schema is already in place). The refused remainder above is AT-35-E2-004's ledger input and AT-35-E4-001's scope; AT-35-E2-005 re-runs the pass (projected wall time ~23 s + inventory regen).
