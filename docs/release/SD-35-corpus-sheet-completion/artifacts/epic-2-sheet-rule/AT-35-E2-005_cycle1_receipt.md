# Cycle AT-35-E2-005_cycle1 — Epic 2 — Sheet rule / AT-35-E2-005

- **Commit SHA:** `51f91bba1124e65f99599472fc7b38c499ede2bc` (code, the regenerated inventory, the re-derived token ledger, the parity artifacts and the retro events, as pushed; preceded by `47bfea1a1d`, a fold of a live `sd31-transcribe` retro-log append found on the shared checkout); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`
- **Scope gate:** `scoped=23315 remaining_non_done=23315 floor=500 verdict=PASS` — `python3 scripts/cycle_scope_gate.py --min 500` (no scope flags: the whole remainder, all 37 books; `scoped_by_bucket=A:449 B:11589 C:4180 D:1982 M:4334 U:202 V:392 X:168 Z:19`) at cycle start `87647621a6`. `python3 scripts/pcgen_residue_gate.py --check` at start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `docs/work-inventory.json` (regenerated once, guarded — 21,911 units stamped `sheet-complete`), `data/sheet_rules/**` (regenerated once: byte-identical to the committed package, 0 files changed), `artifacts/epic-2-sheet-rule/token-coverage.json` (re-derived), `scripts/oracle_harness/sheet-totals.txt.ftl` (new — the B1 export tokens: skills, initiative, speed, vision, DR, SR, spells cast/known/DC per class and level, weapon attack/damage, every SA and FEAT with its substituted description), `scripts/oracle_harness/sheet_parity.py` (new — roster / export / compare), `scripts/tests/test_sheet_parity.py` (new, 12 tests), `src/bin/sheet_rule_parity.rs` (new, tool side — the engine side of the parity run through `with_sheet_rules`, the desktop's own path), `src/rules_core/class_feature_pool_catalog.rs` + `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` (two population pins moved by this cycle's own regen, §8 self-heal), `artifacts/epic-2-sheet-rule/oracle-parity/` (new — `roster/` 29 × `.txt` + `.pcg`, `exports/` 29 PCGen exports, `ours.json`, `sheet-parity.json`), `docs/retro/events/at-35-e2-005.jsonl` (3 corrections + 1 deferral). **No mapping row added**; `src/pcgen_import/sheet_rule/` untouched.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` at start and on the final diff)
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own code diff (`git diff --unified=0 87647621a6...HEAD -- src scripts tests ':!**/*.test.*'`); the §6 grep over the whole Epic 2 set (`fe5ae6cd4a...HEAD`) finds 9 hits, none in code: the same 3 pre-existing `data/sheet_rules/**` prose hits AT-35-E2-002 recorded (`tophet_swallow_whole` "hack", `plant_growth` "hack", `courtly_hunter_courtly_companion` "[… not yet implemented]" — rulebook text, correction `1788844812035-at-35-e2-002-7cbeb2`) and 3 `docs/work-inventory.json` rows (`core_rulebook:class_feature:empty_selection_standard_{barbarian,monk,rogue}`) whose corpus description carries the word, present on both the removed and the added side of the regenerated file (pre-existing corpus text, re-serialized).
- **Acceptance criterion:** Run the converter over every record, regenerate `data/sheet_rules/`, regenerate the inventory once. Report: units moved into DONE by bucket and kind (id-set diff), the refused report by token type, the projected remainder per token type, and the pass's wall time. **No mapping row is added in this cycle.** Then run the oracle harness over the fixture roster and compare every `Number` value the evaluator produces against PCGen's exported total for the same character — this is the "use PCGen to test the rewrite" check, and it runs here first so a wrong mapping is caught before Epics 3–4 build on it. **Evidence:** the receipt with `cycle_scope_gate.py --receipt` rows; `token-coverage.json` re-derived; `completion_atlas.py --check` before and after; the oracle comparison with `PCGEN_ORACLE_SHA`, `compared=<n> agree=<n> disagree=<n>` and every disagreement named with its `Expr` and PCGen's value.
- **Receipt rows (mechanical):** `closed=21911 relabeled=0 rust_lines_changed=337 ratio=0.02 builds_recorded=4 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 87647621a68d48683d5756656988c4a4bd50d54b --before /tmp/wi-before-AT-35-E2-005.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-005` at `51f91bba1124e65f99599472fc7b38c499ede2bc` (`closed_by_kind=ability:1975 class:3 class_feature:12215 companion:661 deity:417 domain:147 equipment:165 equipment_modifier:433 feat:980 language:114 monster:25 monster_ability:13 power:420 race:59 race_trait:1183 skill:44 spell:946 template:1983 trait:128`, `regressed=0 added=0 dropped=0`; `rust_lines_changed` 337 is the new tool-side binary (252) plus the four pin edits — no live-side code changed; `builds_recorded` 4: the converter's release build, the inventory/sweep/fixture-check debug build, the one verification chain (`--no-run` → `--lib` → `--no-fail-fast`), and the targeted re-run of the four moved pins after their edits).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `51f91bba1124e65f99599472fc7b38c499ede2bc` — unchanged (no live-side file changed; `sheet_rule_parity.rs` is `src/bin/`, tool side, and reads the package and the chassis only).
- **Oracle parity:** **`compared=12 agree=1 disagree=11`** (`unverifiable=42`) over the evaluator's `Number` values, **`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6`** — `python3 scripts/oracle_harness/sheet_parity.py compare --ours artifacts/epic-2-sheet-rule/oracle-parity/ours.json --exports artifacts/epic-2-sheet-rule/oracle-parity/exports --output artifacts/epic-2-sheet-rule/oracle-parity/sheet-parity.json`. Roster: 29 characters (the deterministic Human Fighter 1 with its GE-05 `.pcg` twin; the 11 CRB classes as Human at L1 and L10; the 6 other CRB races as Fighter 1; ability scores 12/10/8/18/14/16 so every modifier is distinct). The engine rendered **488 lines, 251 `Number` + 21 `also` numbers = 272 `Number` values** (`class_feature` 231, `skill` 11, `race_trait` 4, `equipment` 3, `feat` 2), folded per sheet-total target into **54 comparison rows**: 12 with a PCGen value, 42 with none (`no-component-export` 24 — `Pool` counters such as Bonus Feats/Arcane Bond/Weapon Training picks, `WeaponAttack`, `Other`; `no-pcgen-ability-named` 18 — the racial SLA lines Speak with Animals / Dancing Lights / Ghost Sound / Prestidigitation PCGen lists as spells, not abilities). The chassis totals those lines feed (context, not the criterion's population): `compared=382 agree=359 disagree=23 unverifiable=140` (the 140 are the chassis's documented zero-sentinel for members without the deterministic Chain Shirt / Longsword / Climb-Intimidate-Swim posture). **Every disagreement, with its `Expr` and PCGen's value** (full rows in `sheet-parity.json` `disagreements`):
  1. `deterministic_human_fighter_l1` `target:Skill:climb` ours **17** vs `SKILL.3.MISC` **1** — `core_rulebook:skill:climb` `Const(3)` ClassSkill (gate `Holds ClassSkill climb ∧ SkillRanks ≥ 1`) + `#bonus1` `Const(8)` Racial (gate `Holds Movement Climb ≥ 1`) + `#bonus2` `Const(3)` / `#bonus3` `Const(6)` SkillFocus (gates `Holds feat:skill_focus`, the latter ∧ `SkillRanks ≥ 10`). PCGen: +3 class skill − 2 armor check penalty. The three gated siblings print although their gates exclude (discovery 1); the −2 ACP is a `chain_shirt#bonus2` line targeted `Other("armor accheck")` that no skill fold reads.
  2. `deterministic_human_fighter_l1` `target:Skill:swim` ours **17** vs `SKILL.17.MISC` **1** — the same four rows on `core_rulebook:skill:swim` (`Const(3)`, `Const(8)` Racial gated on a Swim speed, `Const(3)`/`Const(6)` SkillFocus).
  3. `deterministic_human_fighter_l1` `target:Skill:intimidate` ours **9** vs `SKILL.10.MISC` **3** — `core_rulebook:skill:intimidate` `Const(3)` + `#bonus1` `Const(3)` / `#bonus2` `Const(6)` SkillFocus, gated on `feat:skill_focus`; PCGen: +3 class skill only.
  4. `deterministic_human_fighter_l1` `target:Ac:Armor` ours **2** vs `AC.ARMOR` **4** — `core_rulebook:equipment:chain_shirt` `Const(4)` (gate `Var vfa99a2909219f520 == 0`) + `#bonus1` `Const(−2)` Armor (gate `ItemHas "EQMOD Special Quality ~ Broken ~ Armor"`), the Broken row printing on an unbroken shirt (discovery 1).
  5. `human_bard_l1` `bard_bardic_performance` `also` rounds/day ours **5** vs DESC **7** — `Var vb7765d10517c9c35` ← `Sum[Const(2), AbilityMod(Cha), Mul[Const(2), Var va84302068c788265]]` ← `Var v9c32b989af8797c0` declared only by `core_rulebook:class:bard` (`ClassLevel("bard")`), which is never held, so the level term is 0: 2 + 3 + 0 = 5 (discovery 2). PCGen: 4 + 3 + 2·0 = 7.
  6. `human_bard_l10` the same rule ours **5** vs DESC **25** (4 + 3 + 2·9).
  7. `human_paladin_l10` `paladin_lay_on_hands` `also` uses/day ours **3** vs DESC **8** — `Var v34eeee55389302e7` ← `Sum[Div[Var vb2d44aba35dd27e2, Const(2)], AbilityMod(Cha)]`, the paladin-level `Var` declared by `core_rulebook:class:paladin` only (discovery 2): 0 + 3 = 3; PCGen ⌊10/2⌋ + 3 = 8.
  8. `human_paladin_l1` `paladin_detect_evil` `also` CL ours **0** vs DESC numbers **[3, 60]** — `Var v75e0a3acc8b517a3` (declared by `core_rulebook:class:paladin` / `class:ex_paladin`, unheld → 0; discovery 2). PCGen's substituted description prints "3rd level" and "60 feet", never the caster level, so the DESC numbers are role-mismatched: the CL 0 is wrong on its own terms.
  9. `human_paladin_l1` the same rule `also` DC ours **13** vs DESC **[3, 60]** — `Sum[Const(11), AbilityMod(Wis)]` = 11 + 2; PCGen's description carries no DC, so this row is a comparator limitation (a DESC-number join cannot see a DC PCGen does not print), not an evaluator error.
  10. `human_paladin_l10` `paladin_detect_evil` CL ours **0** vs **[3, 60]** — as 8.
  11. `human_paladin_l10` the same rule DC ours **13** vs **[3, 60]** — as 9.
  The one agreement: `deterministic_human_fighter_l1` `target:Ac:Dodge` ours **1** = `AC.DODGE` **1** (`core_rulebook:feat:dodge` `Const(1)` Dodge). Chassis disagreements (23): the 4 non-human, non-half roster races' fixed ability adjustments are not applied by `compute_pilot_base_chassis` (`ability_mod.*` and the saves derived from them on dwarf/elf/gnome/halfling: 22 rows), and `human_paladin_l10`'s saves lack Divine Grace (+3 Cha, an unheld class feature; 1 row) — discovery 3; `deterministic_human_fighter_l1`'s longsword attack agrees with `WEAPON.0.TOTALHIT` (+6) once compared with the weapon line rather than the generic `ATTACK.MELEE.TOTAL`. **B1 is cleared:** `sheet-totals.txt.ftl` exports `SKILL.n.TOTAL/RANK/ABMOD/MISC`, `INITIATIVEMOD/MISC/BONUS`, `MOVE.n.NAME/RATE`, `VISION`, `DR`, `SR`, `SPELLLISTCLASS.c.CASTERLEVEL`, `SPELLLISTCAST/KNOWN/DC.c.l` for l = 0..9, `WEAPON.n.TOTALHIT/DAMAGE/CRIT/MULT`, `SA.n.NAME/DESC`, `FEAT.n.NAME/DESC`, on top of SD-33's stats/HP/AC/BAB/CMB/CMD/CHECK set — proven live on all 29 exports (`exports/*.txt`, 170–213 lines each).
- **Movement, four buckets:** closure **21,911** (into DONE, by id-set: from B 11,152 / M 4,271 / C 4,101 / D 1,939 / A 448; by kind `class_feature` 12,215, `template` 1,983, `ability` 1,975, `race_trait` 1,183, `feat` 980, `spell` 946, `companion` 661, `equipment_modifier` 433, `power` 420, `deity` 417, `equipment` 165, `domain` 147, `trait` 128, `language` 114, `race` 59, `skill` 44, `monster` 25, `monster_ability` 13, `class` 3; by evidence `sheet_rule_rendered:words` 16,614 / `number` 4,400 / `dice` 897; status moves `engine-does-not-hold→sheet-complete` 17,640, `ingested-magnitude→sheet-complete` 4,271 — exactly AT-35-E2-003's projection) / relabel **0** / reachability **0** / instrument-correction **3** (retro corrections `1788858560458-at-35-e2-005-5a8235`, `1788858560648-at-35-e2-005-cfe0bc`, `1788858560835-at-35-e2-005-961e3c`)
- **Refused tokens:** the converter's refused report is unchanged from AT-35-E2-001 (1,810 records / 81 strings, type-sum 2,131: `no_corpus_record=829, FORMULA:var(COUNT)=211, unmapped:STARTSKILLPTS=162, unmapped:SLOTS=95, FORMULA:malformed (parser refusals)=87, SPELLS (PI-redacted token)=78, BONUS:[redacted PI]=62, unmapped:ALTTYPE=49, unmapped:SPELLSTAT=49, unmapped:MODTOSKILLS=41, DEFINE (PI-redacted token)=40, unmapped:MEMORIZE=38, …` — `_refused.json` `by_token_type`). **The remainder that refusals leave non-DONE at HEAD: 659 of 1,404** (`refused_non_done=659`, 69 strings, multiplicity 851): `FORMULA:var(COUNT)=210, unmapped:STARTSKILLPTS=119, SPELLS (PI-redacted token)=66, FORMULA:malformed (parser refusals)=62, BONUS:[redacted PI]=62, DEFINE (PI-redacted token)=40, unmapped:MODTOSKILLS=37, unmapped:SPELLSTAT=23, unmapped:MEMORIZE=19, FORMULA:var(<export token>) (ENCUMBERANCE)=17, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlinePowers …)=12, unmapped:SPELLLIST=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlineSpells …)=11, FORMULA:var(SKILL.<name>.MISC)=11, FORMULA:CL-no-owner=11, BONUS:EQM=11, BONUS:ITEMCOST=10, FORMULA:var(STAT)=7, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=7, BONUS:STAT (target BASESPELLSTAT;Class)=7, BONUS:SITUATION (target shape)=6, unmapped:KNOWNSPELLS=5, FORMULA:var(SPELLFAILURE)=5, and 46 strings at 1–4 each` (`artifacts/epic-2-sheet-rule/token-coverage.json` `refusal_shapes` / `token_types[*].refused_because_of_this_token_non_done`). The other **745** non-DONE units are not refusals: their statuses sit outside the rung's promotable set — V `literal-verified` 389 + `fixture-verified` 3 (AT-35-E4-002), U `unmeasurable` 202, X `deferred-with-reason` 137 (of 168; the other 31 are refused), Z `not-started` 19 (Epic 5) — deferral `1788859084916-at-35-e2-005-4c14f4`.
- **Discoveries:** (1) **Sibling lines print regardless of their own gate** — `held_set` inserts every `#bonusN` sibling of a held rule outright and `Evaluator::line` reads `applies` only for `Situational` text, never to exclude; 4 of the 11 line disagreements (correction `…-5a8235`; AT-35-E3-001's holdings/`applies` scope — the fix is on the live evaluator, one `Gate::Exclude` arm). (2) **Class-level variables fold to 0** — `BardLevel`/`PaladinLevel`-shaped `Var`s are declared only by the `class` kind rule (`core_rulebook:class:bard` etc.), which `HeldSeed` never seeds, so the fold has no HELD declarer; 5 of the 11 (correction `…-cfe0bc`; AT-35-E3-001 seeds the class record, or the converter folds `ClassLevel` for the declaring class's own features). (3) **The chassis does not apply non-human racial ability adjustments** (dwarf/elf/gnome/halfling) and holds no Divine Grace; 23 chassis-family disagreements (correction `…-961e3c`; Epic 5 chassis scope). Also: the rung stamped exactly the 21,911 AT-35-E2-003 projected; `mechanism_units` of `class_feature_owner_matched_non_excluded_remainder_is_24_and_named_by_subcause` fell 162 → 1 and F1 5,124 → 239 — both pins re-derived and moved in this cycle (§8, count assertions moved by this cycle's own change). No new token type, kind, or remaining-step category outside `token-coverage.json` / the atlas.
- **Figures + their re-derive commands:**
  - conversion pass wall time **24.6 s** (release, this box, 49,438 records → 52,721 files, byte-identical to the committed package: `git status --porcelain -- data/sheet_rules | wc -l` → 0) — `/usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/release/sheet_rule_convert`; `--check` **20.7 s** → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`; **measured first:** `--one <id>` on 3 records = 7.7–8.3 s each (fixed closure-load cost), so the projection stated before the run was ≈ 8 s fixed + 49,438 × ~0.3 ms ≈ 23 s (AT-35-E2-001's measured 23 s); actual 24.6 s
  - inventory regeneration wall time **802.6 s** (`CORPUS_LITERAL_SWEEP_REPORT=… DERIVED_FIXTURE_CHECK_REPORT=… /usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/debug/v06_work_inventory`; prerequisites `corpus_literal_sweep --json-out` 142.8 s → `CLEAN`, `derived_evaluator_fixture_check --json-out` 11.8 s → `verified=1839 failed=0`); a first, unguarded run (726.9 s) was **refused by the stamp-loss guard** (it would have dropped 7,395 of 9,694 stamps — the oracle dispositions chain off the sweep/fixture stamps), which is the guard working; the rung line: `stamped 21911 unit(s) (dice=897, number=4400, words=16614)` — whole cycle ≈ 0.55 s per closed unit
  - id-set movement (denominator: 49,438 units, 0 added / 0 removed) — `python3 <scratch>/movement.py /tmp/wi-before-AT-35-E2-005.json docs/work-inventory.json` (bucket via `completion_atlas._bucket_of` on both snapshots): DONE 26,123 → **48,034**; non-DONE 23,315 → **1,404** (A 449→1, B 11,589→437, C 4,180→79, D 1,982→43, M 4,334→63, U 202, V 392, X 168, Z 19 unchanged); non-DONE by kind at HEAD `class_feature 641, equipment 188, race_trait 168, class 144, feat 93, ability 91, equipment_modifier 40, companion 13, template 9, skill 7, spell 6, monster 2, trait 1, power 1`
  - `completion_atlas.py --check` **before** (at `87647621a6`): `population=49438 buckets=10 unclassified=0 overlap=0` `DONE: 26123 A: 449 B: 11589 C: 4180 D: 1982 M: 4334 V: 392 U: 202 X: 168 Z: 19` `done_evidence_violations=0 stale_derived_at=False citation_failures=0`; **after**: `DONE: 48034 A: 1 B: 437 C: 79 D: 43 M: 63 V: 392 U: 202 X: 168 Z: 19` `unclassified=0 overlap=0 done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas `derived_at`; reverted, outside this epic's set — as AT-35-E2-001..004)
  - `token_coverage.py --check` re-derived: `non_done=1404 tokened=1399 token_less=5 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS` (first run rewrote the stale ledger → `FAIL_STALE_ARTIFACT`, second run `PASS`; 24 token types still unmapped); projected remainder per token type (`carrying_non_done`): `TYPE 1126, CATEGORY 1002, SOURCE* 913, KEY 904, DESC 605, ABILITY 564, BONUS:VAR 502, VISIBLE 426, DEFINE 366, PREVARGTEQ 287, !PRE* 224, BONUS:COMBAT 173, EQMOD 159, BONUS:SAVE 150, …` — 7 types carry ≥ 500 non-DONE units (44 before this pass)
  - oracle roster export wall time **211 s** for 29 characters at `--jobs 3` (**18.4 s** for one BatchExporter run, `/usr/bin/time -v` on the fighter) — `python3 scripts/oracle_harness/sheet_parity.py export --roster <dir> --out <dir> --jobs 3`; engine side **6.6 s** for 29 characters — `$CARGO_TARGET_DIR/release/sheet_rule_parity --roster <dir> --output ours.json`
  - parity rows by family/verdict — `python3 -c` over `sheet-parity.json` `results`: `lines agree 1 / disagree 11 / unverifiable 42`, `chassis agree 359 / disagree 23 / unverifiable 140`; `unverifiable_reasons` `engine-posture-absent 140, no-component-export 24, no-pcgen-ability-named 18`
  - two moved pins — `python3 -c` over `docs/work-inventory.json` (book `core_rulebook` ∧ status `engine-does-not-hold` ∧ evidence `class_feature_owner_matched_by_name_but_record_not_held_by_engine`): 162 → **1** (`core_rulebook:class_feature:sorcerer_bonus_spell_l3_fly`, excluded-class, in `_refused.json`); `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` → `F1 239` (was 5,124; 25.7 s)
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-005`, `CARGO_INCREMENTAL=0`, 121.9 s); `cargo test --locked --lib -j 6` → `3212 passed; 2 failed; 14 ignored` at the pre-pin tree (the two population pins this cycle's own regen moved, `class_feature_owner_matched_non_excluded_remainder_is_24_and_named_by_subcause` and `f1_population_matches_the_current_true_formula_bearing_count_not_the_stale_sd32_census`), re-run at HEAD after the pin edits → `2 passed; 0 failed`; `cargo test --locked --no-fail-fast -j 6` → **412 test binaries (411 + the new tool binary's), 410 ok, 2 FAILED — 8,714 passed, 4 failed, 67 ignored** (2,367.9 s; `grep -c '^test result'` / `awk` over the result lines); the 4 failures are the four pins moved by this cycle's own regeneration (the 2 lib pins above and, in `tests/v06_work_inventory.rs`, `zero_magnitude_option_pool_class_features_are_engine_does_not_hold_not_unknown` + `ultimate_psionics_appears_in_the_inventory_with_real_per_kind_status`, both now reading the rung's `sheet-complete` word), all re-derived and re-run at HEAD: `cargo test --locked --test v06_work_inventory -j 6` → `16 passed; 0 failed; 1 ignored`; `cargo clippy --locked --tests --bin sheet_rule_parity` → 0 warnings; `python3 -m unittest scripts/tests/test_sheet_parity.py` → `Ran 12 tests … OK` (RED first: `KeyError: 'target:Ac:None'` and the AC-part join before the comparator carried per-type AC parts, then GREEN); desktop crate and frontend: epic cadence (`apps/` untouched); run at SHA `51f91bba1124e65f99599472fc7b38c499ede2bc`.
- **Sweep population:** `corpus_literal_sweep` examined the corpus as the guarded regen's prerequisite: `CLEAN`, 3,138 tokens exempted under `decisions.md §24` redaction across 1,058 `codex_generated_name` records (no corpus record changed; the sweep ran for the stamp guard, not for a corpus edit).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; the closure the converter read; every BatchExporter run in `oracle-parity/exports/` ran against `$PCGEN_REPO_DIR/build/install/pcgen` at that pin via `charbuild_remainder_run_one.sh`)
- **Status:** partial — the criterion's population is not zero at HEAD (1,404 non-DONE of 49,438: 659 refused by token type, 745 in non-promotable statuses); the pass, the report, the ledger and the oracle check that the criterion names all landed.
- **Notes:** Judgment calls, each cited: (a) `scripts/oracle_harness/` is Epic 4's file-touch set, written here because the dispatch assigned `blockers.md` B1 (the export tokens must exist before the parity run) to this cycle; the template and driver are tool side (`decisions.md §11`) and are the E4-002 run's instruments. (b) The parity run compares the evaluator's `Number` values against PCGen the only two ways PCGen exports them — a folded contribution against the component of the sheet total it feeds (skill misc, an AC part by type, save misc less the ability, initiative misc, base attack/save, speed, DR, ability score), and a standalone number (a DC, CL, uses/day, a plain magnitude) against the numbers PCGen printed in the same-named ability's substituted description — and books a `Number` with neither as `unverifiable` with its reason, never as agreement. (c) The chassis's zero-sentinel fields (`baseline_armor_class`, `baseline_melee_attack_bonus`, the three selected skills — documented "zero when the deterministic posture is absent") are `unverifiable`, not disagreements, on roster members without that posture. (d) `apply_sheet_complete_rung` ran exactly as AT-35-E2-003 wrote it — no rung, classifier, or converter change in this cycle; the `_defects/`, `_refused.json` and `_report.json` are byte-identical to `72ad0be010`'s. (e) The stray `docs/retro/events/sd31-transcribe.jsonl` append on the shared checkout (1 line, another session's) is folded in its own commit, as AT-35-E2-002/E2-004 did.
- **Next-cycle scope:** Epic 2 is complete as a set of instruments; Epic 3 opens. AT-35-E3-001: `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature` now reads `scoped=214 remaining_non_done=1404 floor=500 verdict=FAIL_UNDER_FLOOR` (B `class_feature` at HEAD; whole B is 437), so the floor rule bundles it with the rest of B/C/D (`--bucket B --or --bucket C --or --bucket D` → `scoped=559 remaining_non_done=1404 floor=500 verdict=PASS`) or takes the whole 1,404 remainder — and the two evaluator findings above (sibling gates, class-level `Var` declarers) are its first mechanisms, ahead of any `applies` widening. AT-35-E4-001's mapping scope is the 659 refused units by the token strings above (the ≥ 10-type re-scope rule of §8 applies: 69 strings / ~16 shapes).
