# Cycle AT-35-E2-003_cycle1 — Epic 2 — Sheet rule / AT-35-E2-003

- **Commit SHA:** `a81c2a005c` (code, tests, retro events, as pushed; preceded by `f731af3759`, a fold of a live `sd31-transcribe` retro-log append found on the shared checkout); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`. Cycle start `9c8a3abe3d`.
- **Scope gate:** `SCOPE_GATE: EXEMPT (status-vocabulary cycle — closes zero units by design)` (`decisions.md §2`; the status is defined and wired here, the pass that moves units is AT-35-E2-005, which regenerates the inventory once). `python3 scripts/pcgen_residue_gate.py --check` at cycle start (`9c8a3abe3d`): `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `src/bin/v06_work_inventory.rs` (`STATUS_VOCABULARY` gains `sheet-complete`; the rung — `SheetRuleProbe`, `load_sheet_rule_probe`, `load_sheet_rule_refused_ids`, `sheet_line_form`, `render_for_probe`, `apply_sheet_complete_rung`, `SHEET_KINDS_WITH_ON_SCREEN_TEST`, `SHEET_COMPLETE_PROMOTABLE_STATUSES`; called in `main` after the bucket-V oracle pass; `DONE_RUNG_STAMP_STATUSES` gains it and the stamp-loss message names it; 5 tests in `apply_sheet_complete_rung_tests`, `stamped_ids_finds_only_the_done_rung_statuses` +1 row), `src/rules_core/rules_tables/companion_chassis.rs` (`HELD_STATUSES` 5 → 6), `scripts/completion_atlas.py` (`_bucket_of` → DONE; `_done_evidence_is_supported(evidence, status)` requires `sheet_rule_rendered:<number|dice|words>` for `sheet-complete`; DONE's `evidence_source` text), `scripts/observer/pf1e_dashboard_producer.py` (`_doneness_verdict_uncapped`: `sheet-complete` → `done` for every wiring class), `scripts/tests/test_cycle_scope_gate.py` (done tuple), `scripts/tests/test_completion_atlas.py` (+3 tests), `scripts/tests/test_pf1e_dashboard_producer.py` (`STATUS_WORDS` +1, +1 test, grid comments 45 → 50), `docs/retro/events/at-35-e2-003.jsonl` (2 corrections).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 9c8a3abe3d...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` on this cycle's diff, and the same grep over `fe5ae6cd4a...HEAD`).
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's diff (`git diff --unified=0 9c8a3abe3d...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'`). Over the whole Epic 2 set since `fe5ae6cd4a` the grep finds the same **3** rulebook-prose lines in `data/sheet_rules/**` that AT-35-E2-002 recorded (correction `1788844812035-at-35-e2-002-7cbeb2`), none in code.
- **Acceptance criterion:** Add `sheet-complete` to `v06_work_inventory`'s `status_vocabulary` with the meaning in `technical-design.md §3`. Add the classifier rung above `engine-does-not-hold` / `ingested-magnitude`, below `grounded`. `completion_atlas.py`'s DONE set gains it, its DONE-evidence check requires `sheet_rule_rendered:<form>`. **Every consumer that raises on an unknown status is found by grep and updated in the same cycle.** **Evidence:** `grep -rln "oracle-unverifiable" src scripts apps tests | wc -l` before equals `grep -rln "sheet-complete" src scripts apps tests | wc -l` after. `completion_atlas.py --check` `unclassified=0 overlap=0 done_evidence_violations=0`.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=442 ratio=n/a builds_recorded=2 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 9c8a3abe3d5c60995241f4f679eef5e8405c16e4 --before /tmp/wi-before-AT-35-E2-003.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-003`, run on the working tree whose content is `a81c2a005c` (`git diff --numstat 9c8a3abe3d -- '*.rs'`: `v06_work_inventory.rs` +430/−6, `companion_chassis.rs` +5/−1). `builds_recorded` 2: the one verification chain (`--no-run` → `--lib` → `--no-fail-fast`; its third stage recompiled the one bin after the `#[cfg(test)]` fix for a `dead_code` warning the first stage surfaced) and the post-chain `clippy` / `sheet_rule_convert -- --check` compile session. No inventory regeneration (`closed=0` by design).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `a81c2a005c` — unchanged. The rung reads the converter's package (`SheetRule` JSON, `_refused.json`) and the evaluator's output only; no token, formula or `raw_tokens` read was added anywhere.
- **Oracle parity:** N/A — no `Number` mapping row was added (the converter is untouched; `sheet_rule_convert -- --check` → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`).
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 2 (retro corrections `1788849081903-at-35-e2-003-b92fcf`, `1788852125221-at-35-e2-003-41c58b`).
- **Refused tokens:** none (no converter run; `_refused.json` unchanged at 1,810 records).
- **Discoveries:** (1) **The census evidence does not hold as a literal equality: before=6, after=7** (`…-b92fcf`). The five files that *branch* on a status are in both sets (`completion_atlas.py`, `pf1e_dashboard_producer.py`, `test_cycle_scope_gate.py`, `v06_work_inventory.rs`, `companion_chassis.rs`); the sixth before-file, `formula_interpreter_corpus_wide.rs`, names the oracle words in the F1 pin's history narrative (they moved that pin at SD-34 wave 22) — `sheet-complete` has not moved it (no regen this cycle) and is not written there; the two after-only files are the RED→GREEN tests for the atlas and the producer. No consumer was missed: `grep -rn "unknown status\|status_vocabulary\|oracle-agree"` over `src scripts apps tests` finds no further site (`reachability_audit.py` widens its grid to any word on a real unit and will grid `sheet-complete` once the inventory carries it; `tests/v06_work_inventory.rs` asserts only that every unit's status is declared, which the regenerated document will satisfy). (2) **The producer's grid test covered 9 of 11 vocabulary words** — `oracle-agree`/`oracle-unverifiable` were never added to `STATUS_WORDS` when `decisions.md §19` landed them (`…-41c58b`); this cycle adds only `sheet-complete` (10 of 12), the two oracle words being out of scope — the producer maps them, the gap is coverage. (3) **4,681 of 47,628 top-level rules are `print: false`** (R1's `VISIBLE:DISPLAY` rows) — the rung stamps them with the form of their value like any other: the evaluator renders the line, `render_sheet` keeps it off the paper, and under R1 the unit is done. (4) The rung's third condition ("the kind's on-screen test is green") is checked by a generator as *the kind has an on-screen test* — the list is pinned to the frontend test's own `KINDS` array by reading the file, and the suite's greenness is the epic gate's (it ran `101/101` at AT-35-E2-002; `apps/` is untouched here).
- **Figures + their re-derive commands:**
  - consumer census: before **6** — `grep -rln "oracle-unverifiable" src scripts apps tests | wc -l`; after **7** — `grep -rln "sheet-complete" src scripts apps tests | wc -l`; symmetric difference (3 files, named above) — `comm -3 <(grep -rln "oracle-unverifiable" src scripts apps tests | sort) <(grep -rln "sheet-complete" src scripts apps tests | sort)`
  - the rung over the live package (denominator: the 47,628 top-level rule ids in `data/sheet_rules/`, i.e. every converted record; `#suffix` siblings excluded): **47,628 stamped, number=8017 dice=1461 words=38150, rendered in 2.28 s** after a 6.78 s package load — `CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-003 cargo test --locked --bin v06_work_inventory -j 6 apply_sheet_complete_rung_tests -- --nocapture` (the `sheet-complete rung:` and `kind=` lines); per kind (number/dice/words): `ability 1112/2/3132`, `class 3/0/0`, `class_feature 2631/132/14920`, `companion 795/3/884`, `deity 0/0/458`, `domain 3/0/180`, `equipment 378/541/5125`, `equipment_modifier 7/0/1497`, `feat 293/3/1734`, `language 0/0/136`, `monster 1249/2/6`, `monster_ability 288/10/3497`, `power 9/0/411`, `race 11/2/55`, `race_trait 488/8/1935`, `skill 103/0/38`, `spell 54/0/2775`, `template 422/758/1054`, `trait 171/0/313`; refused ids 1,810, none with a rule (asserted)
  - projection for AT-35-E2-005 (denominator: the 49,438 units of `docs/work-inventory.json` at `9c8a3abe3d`): **47,628 units carry a converted rule (every top-level rule id is an inventory id; 0 rule ids outside the inventory)**; of those, by current status: `engine-does-not-hold 17640, text-complete 10810, oracle-unverifiable 8348, grounded 5049, ingested-magnitude 4271, oracle-agree 765, literal-verified 388, unmeasurable 198, deferred-with-reason 137, not-started 19, fixture-verified 3`; **the rung will move 21,911** (17,640 + 4,271) at the next regeneration, by kind `ability 1975, class 3, class_feature 12215, companion 661, deity 417, domain 147, equipment 165, equipment_modifier 433, feat 980, language 114, monster 25, monster_ability 13, power 420, race 59, race_trait 1183, skill 44, spell 946, template 1983, trait 128` — `python3 -c "import json,glob,collections as c; wi=json.load(open('docs/work-inventory.json'))['units']; rules={r['id'] for f in glob.glob('data/sheet_rules/*/*/*.json') for r in json.load(open(f)) if '#' not in r['id']}; print(c.Counter(u['status'] for u in wi if u['id'] in rules)); print(c.Counter(u['kind'] for u in wi if u['id'] in rules and u['status'] in ('engine-does-not-hold','ingested-magnitude')))"` (an estimate of E2-005's closure: E2-005's own converter run may change the rule set)
  - hidden rules **4,681** of 47,628 top-level — `python3 -c "import json,glob; print(sum(1 for f in glob.glob('data/sheet_rules/*/*/*.json') for r in json.load(open(f)) if '#' not in r['id'] and not r.get('print',True)))"`
  - vocabulary **12** words in code vs `STATUS_WORDS` **10** — the `python3` comparison named in `…-41c58b`
  - `completion_atlas.py --check` → `population=49438 buckets=10 unclassified=0 overlap=0` `done_evidence_violations=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas artifacts; reverted, outside this epic's set — same as AT-35-E2-001/E2-002); `shape_engine_boundary.py --check` → `magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`; `missing_engine_tables.py --check` → `citation_failures=0`; `denominator_gate.py --check` over the package globs → `files_checked=30 violations=0`; `scripts/verify.sh --only pi-sweep` → `RESULT: PASS`; literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`; `scripts/token_coverage.py` does not exist yet (AT-35-E2-004) — skipped; `corpus_literal_sweep` skipped (no corpus record changed)
  - python RED→GREEN: `python3 -m unittest scripts.tests.test_completion_atlas scripts.tests.test_cycle_scope_gate scripts.tests.test_pf1e_dashboard_producer` → **RED `Ran 132 … FAILED (failures=5, errors=1)`** before the three scripts changed (unknown status → `None`/unmapped; no `status` parameter), **GREEN `Ran 143 … OK`** after (with `scripts.tests.test_reachability_audit`)
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-003`, `CARGO_INCREMENTAL=0`, 2 min 53 s wall); `cargo test --locked --lib -j 6` → `3214 passed; 0 failed; 14 ignored`; `cargo test --locked --no-fail-fast -j 6` → **411 test binaries, 411 ok, 0 FAILED; 8,715 passed, 0 failed, 67 ignored** (8,710 + the 5 rung tests; `grep -c '^test result'` / `awk` over the result lines of the chain log), no compiler warnings; `cargo clippy --locked --tests -j 6 --lib --bin v06_work_inventory` → 0 warnings; desktop crate and frontend at epic cadence (no `apps/` touch). Run at the working tree whose content is `a81c2a005c` (the fold commit `f731af3759` carries one retro-log line only).
- **Sweep population:** N/A (no corpus record changed; `docs/work-inventory.json` not regenerated — AT-35-E2-005 regenerates it once, and the stamp-loss guard now names `sheet-complete`).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; unchanged; no figure here came from the pinned corpus).
- **Status:** complete
- **Notes:** Judgment calls, each cited: (a) the rung runs **last** of the status passes so it lifts only `engine-does-not-hold`/`ingested-magnitude` and never overwrites a stamp the passes above wrote (`technical-design.md §3`, "ladder position"); `unmeasurable`/`deferred-with-reason`/`not-started`/bucket V are not in its promotable set — Epic 5's residue criteria own those buckets. (b) The probe character is the deterministic Human Fighter 1 fixture holding the rule **outright** (its own 41-rule held set plus this rule), evaluated exactly as `render_sheet` evaluates a held rule — not a per-rule fixpoint (47,628 fixpoints against one); the rendered form is the rule's own. (c) `print: false` rules are stamped (discovery 3, R1). (d) `sheet-complete` joins `DONE_RUNG_STAMP_STATUSES` — a regen on a tree with `data/sheet_rules/` missing or truncated refuses to write instead of silently dropping ~22k units back to `engine-does-not-hold`. (e) The producer maps `sheet-complete` → `done` for every wiring class, resolved before the per-class dispatch like `deferred-with-reason`/`not-started`: the sheet rule's bar is class-independent, so the `ambiguous` lower-bound rule yields `done` too. (f) The three SD-34 atlas artifacts re-stamped by the `--check` runs were reverted (precedent E2-001/E2-002). (g) The one `dead_code` warning (a test-only constant) was fixed in-cycle with `#[cfg(test)]` before the chain's third stage, which recompiled the bin; the clippy run is on the fixed source.
- **Next-cycle scope:** criterion at zero (the status, the rung, and every consumer exist; residue unchanged). AT-35-E2-004 next (`scripts/token_coverage.py`); AT-35-E2-005 then regenerates the inventory once and reports the id-set movement the projection above estimates at 21,911.
