# Cycle AT-35-E2-005_cycle3 — Epic 2 — Sheet rule / AT-35-E2-005

- **Commit SHA:** `c0f16fe4177a9df8cbb4f15b6b14b2698d4f6300` (the widened export template, the harness's three new joins, 7 new tests, the 29 re-exported oracle files plus the filled template, the re-run parity artifact and the two retro events, as pushed); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`
- **Scope gate:** `scoped=1404 remaining_non_done=1404 floor=500 verdict=PASS` — `python3 scripts/cycle_scope_gate.py --min 500` (no scope flags: the whole remainder, all 37 books; `scoped_by_bucket=A:1 B:437 C:79 D:43 M:63 U:202 V:392 X:168 Z:19`, `scoped_by_kind=ability:91 class:144 class_feature:641 companion:13 equipment:188 equipment_modifier:40 feat:93 monster:2 power:1 race_trait:168 skill:7 spell:6 template:9 trait:1`) at cycle start `b3f032a9346ed0f08b062dd75ba944389b02d908`. `python3 scripts/pcgen_residue_gate.py --check` at start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `scripts/oracle_harness/sheet-totals.txt.ftl` (tool side: `POOL.<n>.NAME/SIZE` via the JEP `charbonusto("ABILITYPOOL", <category>)` for every `ABILITYCATEGORY` the pinned Core Rulebook chain declares, filled into the `pool_categories` marker by the export step; `SPELLBOOK.<n>.NAME` and `SPELLMEM.<class>.<book>.<level>.<spell>.{NAME,TIMES,TIMEUNIT,CASTERLEVEL,DC}` over every book, class index 0..`COUNT[SPELLRACE]+COUNT[CLASSES]-1` and level 0..9), `scripts/oracle_harness/sheet_parity.py` (`pool_categories()` / `pool_categories_from_rows()` / `render_template()`; `Pool` joins `POOL.<n>.SIZE` by the category slug; `WeaponAttack` compares with `WEAPON.0.TOTALHIT-ATTACK.MELEE.TOTAL` when exactly one weapon is wielded; a standalone value whose DESC join finds no number in its role falls back to the same-named `SPELLMEM` row in the same role — `TIMES` for uses and for a spell-like ability's principal value, `CASTERLEVEL`, `DC`; absolute paths for the BatchExporter; `--only` for template validation), `scripts/tests/test_sheet_parity.py` (17 → 24 tests), `artifacts/epic-2-sheet-rule/oracle-parity/` (`exports/` 29 `.txt` re-exported at the same pin, +15,559 rows; `exports/_template/sheet-totals.txt.ftl` the filled template as run; `sheet-parity.json`; `ours.json` re-generated and byte-identical), `docs/retro/events/at-35-e2-005.jsonl` (1 correction + 1 deferral). **No mapping row added**; `src/**`, `src/pcgen_import/sheet_rule/`, `data/sheet_rules/**` and `apps/**` untouched (the conversion pass wrote a byte-identical package); `docs/work-inventory.json` regenerated once, guarded, and reverted — its only diff was `generated_at` (0 units moved, below).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set> scripts/oracle_harness ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` at start and on the final committed diff at `c0f16fe417`)
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff (`git diff --unified=0 b3f032a934...HEAD -- scripts docs/release/SD-35-corpus-sheet-completion/artifacts ':!**/*.test.*'` → no match); the §6 grep over the whole Epic 2 set (`fe5ae6cd4a...HEAD`) finds the same 6 pre-existing hits AT-35-E2-002 and cycles 1–2 recorded, none in code: 3 `data/sheet_rules/**` prose hits (`tophet_swallow_whole` "hack", `plant_growth` "hack", `courtly_hunter_courtly_companion` "[… not yet implemented]" — rulebook text, correction `1788844812035-at-35-e2-002-7cbeb2`) and 3 `docs/work-inventory.json` rows (`core_rulebook:class_feature:empty_selection_standard_{barbarian,monk,rogue}`) whose corpus description carries the word, on both sides of cycle 1's regenerated file.
- **Acceptance criterion:** Run the converter over every record, regenerate `data/sheet_rules/`, regenerate the inventory once. Report: units moved into DONE by bucket and kind (id-set diff), the refused report by token type, the projected remainder per token type, and the pass's wall time. **No mapping row is added in this cycle.** Then run the oracle harness over the fixture roster and compare every `Number` value the evaluator produces against PCGen's exported total for the same character — this is the "use PCGen to test the rewrite" check, and it runs here first so a wrong mapping is caught before Epics 3–4 build on it. **Evidence:** the receipt with `cycle_scope_gate.py --receipt` rows; `token-coverage.json` re-derived; `completion_atlas.py --check` before and after; the oracle comparison with `PCGEN_ORACLE_SHA`, `compared=<n> agree=<n> disagree=<n>` and every disagreement named with its `Expr` and PCGen's value.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since b3f032a9346ed0f08b062dd75ba944389b02d908 --before /tmp/wi-before-AT-35-E2-005.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-005` at `c0f16fe4177a9df8cbb4f15b6b14b2698d4f6300` (`closed_by_kind=` empty, `relabeled_moves=` empty, `regressed=0 added=0 dropped=0`; `rust_lines_changed` 0 — no Rust file touched, the cycle is tool-side Python, a FreeMarker template and artifacts; `ratio=n/a` because `closed=0` — `decisions.md §4`'s ratio has no denominator in a zero-closure cycle; `builds_recorded` 0 — cycle 2's warm `CARGO_TARGET_DIR` served every binary this cycle ran (`cargo test --locked --no-run` finished in 1.27 s with nothing to compile), so no build was paid)
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `c0f16fe417` — unchanged (no live-side file touched; the only new PCGen reads are in `scripts/oracle_harness/`, the test oracle `decisions.md §11` names as one of the two permitted readers).
- **Oracle parity:** **`compared=42 agree=41 disagree=1`** (`unverifiable=5`) over the evaluator's `Number` values, **`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6`** — `python3 scripts/oracle_harness/sheet_parity.py compare --ours artifacts/epic-2-sheet-rule/oracle-parity/ours.json --exports artifacts/epic-2-sheet-rule/oracle-parity/exports --output artifacts/epic-2-sheet-rule/oracle-parity/sheet-parity.json` (cycle 2: `compared=8 agree=8 disagree=0 unverifiable=39`; cycle 1: 12/1/11/42). Same 29-character roster, same 270 rendered lines and **47 `Number` values** (`ours.json` re-generated at HEAD and byte-identical to cycle 2's — no live-side change). **The one disagreement, named:** `deterministic_human_fighter_l1` `target:WeaponAttack:{"Chosen": "core_rulebook:feat:weapon_focus"}` **ours 0, PCGen 1** (`WEAPON.0.TOTALHIT` +6 − `ATTACK.MELEE.TOTAL` +5 — the longsword's own bonus, which is Weapon Focus) — `Expr` `{"Number": {"Var": "vb1e14268d73c2def"}}`; the var table `data/sheet_rules/_vars/vb1e14268d73c2def.json` has one contribution, `Const(1)` (`Base`) declared by **`core_rulebook:class_feature:default`** (the corpus's `CATEGORY=Internal|Default.MOD` row, `cr_abilities.lst:11`), a rule the character's held set does not hold, so the fold yields 0. Not a mapping defect (the row transcribes the corpus) and not an evaluator defect (the fold is right for what is held): a **holdings** gap — Epic 3's `applies`/holdings scope (AT-35-E3-001) decides whether the Internal default is held by every character; named here, not widened. **The 41 agreements:** the 8 of cycle 2 (`Ac:Armor` 4, `Ac:Dodge` 1, `Skill:climb` 3, `Skill:swim` 3, `Skill:intimidate` 3, Bardic Performance 7 and 25 rounds/day, Lay on Hands 8 uses/day) plus 33 new — **17 `Pool` sizes** (`fighter_bonus_feat` 1 on all 8 level-1 fighters and **6** on `human_fighter_l10` = `POOL.47.SIZE`; `armor_training_choice` 1, `weapon_training_i` 1; `nature_bond` 1 ×2; `arcane_bond` 1 ×2; `combat_style` 1, `ranger_combat_style_feat` **3**), **14 gnome racial SLA values** (Dancing Lights / Ghost Sound / Prestidigitation / Speak with Animals: `value` 1 and `1/day` = `SPELLMEM.0.2.0.<n>.TIMES` 1, `CL 1` = `CASTERLEVEL` 1, and `DC 14` = `DC` 14 for Ghost Sound and Prestidigitation), and **2 Detect Evil caster levels** (`CL 1` / `CL 10` = `SPELLMEM.0.2.0.0.CASTERLEVEL` under the `Class` spellbook, PCGen's `DetectEvilLVL`). **The 5 unverifiable, by reason:** `SPELL-dc-not-numeric` 4 (PCGen prints a blank `DC` for a spell with no saving throw: Dancing Lights, Speak with Animals, Detect Evil ×2 — our `DC 14` / `DC 15` / `DC 13` lines have no oracle, not a wrong one), `no-component-export` 1 (`Other:accheck`, Armor Training's armor-check reduction, 0 on an unarmored fighter). The chassis totals those lines feed (context, not the criterion's population): `compared=382 agree=376 disagree=6 unverifiable=140`, unchanged from cycle 2 — the 6 are Halfling Luck's +1 and Divine Grace's +3 on the three saves (Epic 3 / Epic 5 holdings, cycle 2's receipt). **B1 is wider:** the export now carries every category pool, the wielded weapon's own bonus and every spell-like ability's uses / caster level / DC.
- **Movement, four buckets:** closure **0** / relabel **0** / reachability **0** / instrument-correction **1** (retro correction `1788867844870-at-35-e2-005-57c381`: cycle 2's 8/8 was an export-coverage floor — 34 of its 39 unverifiable values were reachable through PCGen's own export vocabulary, and one of them disagrees) — after the guarded regen `git diff --stat -- docs/work-inventory.json` → `1 insertion(+), 1 deletion(-)` (`generated_at` only), reverted with `git checkout`; `cmp docs/work-inventory.json /tmp/wi-before-AT-35-E2-005.json` → identical, so buckets `A:1 B:437 C:79 D:43 DONE:48034 M:63 U:202 V:392 X:168 Z:19` on both sides, no bucket, status or evidence move. Expected: the converter is unchanged, so the refusal set and every rung reading are unchanged.
- **Refused tokens:** unchanged from cycles 1–2 — the converter's report is byte-identical (`records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`), and **the remainder refusals leave non-DONE at HEAD is 659 of 1,404** (`refused_non_done=659`, 69 strings, multiplicity 851): `FORMULA:var(COUNT)=210, unmapped:STARTSKILLPTS=119, SPELLS (PI-redacted token)=66, FORMULA:malformed (parser refusals)=62, BONUS:[redacted PI]=62, DEFINE (PI-redacted token)=40, unmapped:MODTOSKILLS=37, unmapped:SPELLSTAT=23, unmapped:MEMORIZE=19, FORMULA:var(<export token>) (ENCUMBERANCE)=17, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlinePowers …)=12, unmapped:SPELLLIST=12, FORMULA:identifier DEFINEd nowhere (Bloodrager_CF_BloodlineSpells …)=11, FORMULA:var(SKILL.<name>.MISC)=11, FORMULA:CL-no-owner=11, BONUS:EQM=11, BONUS:ITEMCOST=10, FORMULA:var(STAT)=7, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=7, BONUS:STAT (target BASESPELLSTAT;Class)=7, BONUS:SITUATION (target shape)=6, unmapped:KNOWNSPELLS=5, FORMULA:var(SPELLFAILURE)=5, and 46 strings at 1–4 each` (`artifacts/epic-2-sheet-rule/token-coverage.json` `refusal_shapes`). The other **745** non-DONE units are non-promotable statuses: V `literal-verified` 389 + `fixture-verified` 3 (AT-35-E4-002), U `unmeasurable` 202, X `deferred-with-reason` 137, Z `not-started` 19 (Epic 5) — deferral `1788868218173-at-35-e2-005-43630b`. This cycle is a zero-new-mappings cycle by the criterion's own text; every one of the 659 needs a converter mapping row (AT-35-E4-001).
- **Discoveries:** (1) **cycle 2's "agrees on every value it can compare" was true of the harness, not of the oracle** — PCGen has no export token for a category's pool, but its JEP `charbonusto("ABILITYPOOL", <category>)` totals it, `WEAPON.n.TOTALHIT` less `ATTACK.MELEE.TOTAL` isolates the wielded weapon's own bonus, and the `SPELLS:` token's spell-like abilities sit in named spellbooks reachable by `SPELLMEM` (correction `…-57c381`); (2) **PCGen's `Detect Evil,11+WIS` DC formula is never printed** — `SpellMemToken` prints a blank `DC` for a no-save spell, so our `SaveDc` on Detect Evil, Dancing Lights and Speak with Animals has no oracle (a `SPELL-dc-not-numeric` reason, not a disagreement); (3) a remaining-step observation for Epic 3, neither a new token type nor a kind: the reverse census `pcgen_pools_without_engine_line=19` — PCGen exports a non-zero pool the sheet prints no `Pool` line for on 19 (character, category) pairs (Rage Power 5, Rogue Talent 5, Monk Bonus Feat 1/4, Mercy 3, Versatile Performance 3, Favored Enemy 1/3, Favored Terrain 2, Favored Enemy/Terrain Bonus 2/1, Hunter's Bond 1, Divine Bond 1, Bloodline 1 ×2, Wizard Bonus Feat 2, Weapon Training II 1, Adaptability Bonus Feat 1, Gnome Obsessive Skill Bonus 1), i.e. the holdings do not yet surface those class features' pools — the same holdings gap the Weapon Focus disagreement names. No new token type, kind, or remaining-step category outside `token-coverage.json` / the atlas.
- **Figures + their re-derive commands:**
  - conversion pass wall time **25.44 s** (release, this box, 49,438 records; `git status --porcelain -- data/sheet_rules | wc -l` → **0**, byte-identical to the committed package) — `/usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/release/sheet_rule_convert`; `--check` **20.16 s** (20.33 s in the verification pass) → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`; **projection stated before the run:** cycle 2's 24.72 s (cycle 1's `--one` ≈ 8 s fixed cost, projected ≈ 23 s) — the standing measurement holds
  - inventory regeneration wall time **776.27 s** (`CORPUS_LITERAL_SWEEP_REPORT=… DERIVED_FIXTURE_CHECK_REPORT=… /usr/bin/time -f 'wall=%es' $CARGO_TARGET_DIR/debug/v06_work_inventory`; prerequisites `corpus_literal_sweep --json-out` **141.47 s** → `CLEAN`, `derived_evaluator_fixture_check --json-out` **12.08 s** → `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested`); projection stated before the run: cycle 2's 748.95 s; `git diff --stat -- docs/work-inventory.json` → `1 insertion(+), 1 deletion(-)` (`generated_at` only), reverted
  - id-set movement (denominator: 49,438 units) — `cmp` above: **0 / 0 / 0**; `completion_atlas.py --check` **before** (cycle-start inventory) and **after** (the same file, in the verification pass) both `population=49438 buckets=10 unclassified=0 overlap=0` `DONE: 48034` `done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas artifacts; reverted, outside this epic's set — as every Epic 2 cycle)
  - `token_coverage.py --check` re-derived: `check partition: token_types=231 ok=True` / `non_done=1404 tokened=1399 token_less=5 refused=1810 refused_non_done=659 token_types=231 shapes=81 verdict=PASS` — the ledger is unchanged (no converter change); projected remainder per token type as cycles 1–2 (`carrying_non_done`: `TYPE 1126, CATEGORY 1002, SOURCE* 913, KEY 904, DESC 605, ABILITY 564, BONUS:VAR 502, …`; 7 types carry ≥ 500 non-DONE units)
  - template validation on 3 members first (`sheet_parity.py export … --only gnome_fighter_l1 human_paladin_l1 deterministic_human_fighter_l1 --jobs 3`) **24.62 s** → `POOL.COUNT=230`, gnome `SPELLMEM.0.2.0.{0..3}` = the four SLAs under `SPELLBOOK.2.NAME=Racial`, paladin `SPELLMEM.0.2.0.0.NAME=Detect Evil` under `Class`, fighter `WEAPON.COUNT=1 WEAPON.0.TOTALHIT=+6 ATTACK.MELEE.TOTAL=+5`; then the roster export **211.06 s** for 29 characters at `--jobs 3` (projection stated first: cycle 2's 221.68 s) — `python3 scripts/oracle_harness/sheet_parity.py export --roster <dir> --out <dir> --jobs 3` → `characters=29 failed=0`; `git diff --stat -- exports` → `29 files changed, 15559 insertions(+)`; pool categories filled from the pinned data: **230** (`python3 -c 'import sheet_parity as SP; print(len(SP.pool_categories()))'`); `SPELLMEM` rows across the 29 exports: **420** (`grep -hcE '^SPELLMEM\.[0-9.]+\.NAME' exports/*.txt | paste -sd+ | bc`); engine side **6.64 s** for 29 characters — `$CARGO_TARGET_DIR/release/sheet_rule_parity --roster <dir> --output ours.json` (`characters=29 lines=270`; `characters` array identical to cycle 2's)
  - parity rows by family/verdict — `python3 -c` over `sheet-parity.json` `results`: `lines agree 41 / disagree 1 / unverifiable 5`, `chassis agree 376 / disagree 6 / unverifiable 140`; `unverifiable_reasons` `engine-posture-absent 140, SPELL-dc-not-numeric 4, no-component-export 1`
  - reverse pool census **19** (denominator: the 29 exports' `POOL.<n>.SIZE > 0` rows less the generic `Ability Bonus` / `Favored Class`, joined by slug to `ours.json`'s `Pool` targets) — the inline `python3` in this cycle's transcript, reproducible from `exports/*.txt` and `ours.json`
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-005`, `CARGO_INCREMENTAL=0`, 1.27 s — every target cached from cycle 2, no Rust source changed); `cargo test --locked --lib -j 6` → **3217 passed; 0 failed; 14 ignored** (51.78 s); `cargo test --locked --no-fail-fast` **not run** — §6 requires it when `src/` or the classifier changed, and neither did (`git diff --stat b3f032a934..HEAD -- src` is empty); clippy **not run** — no Rust target touched; `python3 -m unittest scripts/tests/test_sheet_parity.py` → `Ran 24 tests … OK` (RED on the HEAD module first: 4 failures + 3 errors — `no-pcgen-pool` / `no-component-export:Pool` / `no-component-export:WeaponAttack` / `no-pcgen-ability-named:Dancing Lights`, `AttributeError: pool_categories_from_rows` / `render_template` / `pool_categories`); `sheet_rule_convert -- --check` PASS; literal scan 0; residue PASS; atlas / token-coverage / shape-engine-boundary (`magnitude_bearing=26396 not_held_by_engine=363 citation_ok=True`) / missing-engine-tables (`citation_failures=0`) / denominator-gate (run on this receipt before the docs commit; its line is in `progress.md`'s entry) / pi-sweep (`RESULT: PASS`, 8.48 s) green; desktop crate and frontend: epic cadence (`apps/` untouched); run at SHA `c0f16fe4177a9df8cbb4f15b6b14b2698d4f6300`.
- **Sweep population:** `corpus_literal_sweep` examined the corpus as the guarded regen's prerequisite: `CLEAN` (141.47 s); no corpus record changed (the sweep ran for the stamp guard, not for a corpus edit).
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; every BatchExporter run in `oracle-parity/exports/` ran against `$PCGEN_REPO_DIR/build/install/pcgen` at that pin via `charbuild_remainder_run_one.sh`; the 230 pool categories were read from the same checkout's `core_rulebook/`, `core_essentials/` and `core_essentials/races/*/` `*abilitycategories*.lst`)
- **Status:** partial — the criterion's population is not zero at HEAD (1,404 non-DONE of 49,438: 659 refused by token type, 745 in non-promotable statuses), and this cycle closes none by the criterion's own "no mapping row" rule; what it names as its own evidence — the pass, the report, the ledger and the oracle check — is re-run at HEAD, and the oracle check now compares 42 values (cycle 2: 8) and names one disagreement with its `Expr` and PCGen's value.
- **Notes:** Judgment calls, each cited: (a) a third remainder cycle on a criterion that forbids mapping rows cannot move units, so its budget went to the one evidence row still short — 39 of 47 `Number` values unverifiable — on the tool side (`decisions.md §11`: the oracle harness is a permitted PCGen reader; `blockers.md` B1's owner is this criterion). (b) The template is filled at export time from the pinned data rather than hand-listing categories: a per-kind read of the oracle's own tables, not a per-unit fixture (`decisions.md §4`); the filled copy is committed under `exports/_template/` so the export is reproducible. (c) The Weapon Focus disagreement is reported, not fixed: the fix is a holdings decision (is `core_rulebook:class_feature:default` held by every character?), which is Epic 3's `applies`/holdings scope and would also change the 19 pools the sheet does not print. (d) `scripts/oracle_harness/` is Epic 4's file-touch set, touched here as cycles 1–2 did (B1's owner). (e) The regenerated inventory was not committed: a `generated_at`-only diff is churn; the receipt carries the regen's figures. (f) No build was paid this cycle (`builds_recorded=0`): §6's full `--no-fail-fast` run is conditional on `src/` moving, and it did not.
- **Next-cycle scope:** AT-35-E2-005 has no zero-population form without mapping rules — a fourth remainder cycle on it would repeat this one; `workflow-instruction.md §8`'s ">10 distinct refused token types" rule applies to the 659 (69 strings / 81 shapes): re-scope, do not grind. Epic 2 is complete as a set of instruments and Epic 3 opens on `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --or --bucket C --or --bucket D` (`scoped=559 remaining_non_done=1404 floor=500 verdict=PASS`) or the whole 1,404 remainder; its first mechanism is now concrete — the holdings gap the parity names twice (Weapon Focus's `class_feature:default` declarer; the 19 unprinted pools), then Halfling Luck's Save consumer and Divine Grace's `print=false` Save targets. AT-35-E4-001's mapping scope is the 659 refused units by the token strings above, the 182 `class` records first.
