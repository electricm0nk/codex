# Cycle AT-35-E2-002_cycle1 — Epic 2 — Sheet rule / AT-35-E2-002

- **Commit SHA:** `909bb0837cec26e713a75dffe30a5d98be12e991` (code, tests, retro events, as pushed; preceded by `097e7c1aa5`, a fold of a live `sd31-transcribe` retro-log append found on the shared checkout); the docs commit carrying this receipt, `progress.md` and `kanban.md` follows it on `tranche/15`
- **Scope gate:** `SCOPE_GATE: EXEMPT (live-evaluator + sheet-section cycle — closes zero units by design)` (`decisions.md §2`; the status `sheet-complete` that moves units is AT-35-E2-003, the pass that moves them AT-35-E2-005). `python3 scripts/pcgen_residue_gate.py --check` at cycle start (`643cc89bba`): `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** `src/rules_core/sheet_rule.rs` (the evaluator: `Rat`, `SheetLine`/`SheetLineValue`, `SheetRulePackage`, `CharacterFacts`, `HeldSeed`/`HeldSet`/`held_set`, `evaluate`, `evaluate_applies`, `render_sheet`, `step_die`/`fold_dice_modifier`; 8 tests in `evaluate_tests`), `src/rules_core/corpus_loader.rs` (`load_sheet_rules`, `load_sheet_rules_filtered`, `SheetRuleLoad`), `src/rules_core/pilot_compute/mod.rs` (`PilotBaseChassisComputation.sheet_lines` + `with_sheet_rules`), `src/rules_core/pilot_view_model.rs` (2 struct literals gain the field), `apps/desktop/src-tauri/src/character_hub.rs` (`SheetLineDto`, `sheet_rule_package` once per process, `sheet_lines_for`, `LoadSavedCharacterResponse.sheet_lines` / `sheet_rules_unavailable_reason`), `apps/desktop/src-tauri/src/rule_system_adapter.rs` (the adapter's response constructor carries the same two fields), `apps/desktop/src-tauri/src/stub_adapter.rs` (1 literal), `apps/desktop/src-tauri/src/reach_gate.rs` (`sheet_rule_lines_cross_the_ipc_carrying_label_and_value`), `apps/desktop/src/boundary/loadSavedCharacterDetail.ts` (`SheetLineDto`, the two response fields), `apps/desktop/src/characterHub/CharacterSheet.tsx` (`RulesAndFeaturesSection`, `groupSheetLinesByKind`, `sheetLineKindLabel`, mounted in the Actions tab; `engineRecords` re-reads the lines with the rest), `apps/desktop/src/characterHub/classFeaturesModel.ts` (`buildClassFeatureSurface(..., sheetLines)` — the `Not computed` lane keeps only records with no rule) + `.test.ts` (+1), `apps/desktop/src/characterHub/rulesAndFeaturesSection.test.ts` (new — 19 per-kind DOM tests + 5), `apps/desktop/src/characterHub/{characterSheetRefresh,previewData}.ts` (response literals), `docs/retro/events/at-35-e2-002.jsonl` (3 corrections).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (`git diff --unified=0 fe5ae6cd4a...HEAD -- <Epic 2 file-touch set> ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` at start and at `909bb0837c`)
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff (`git diff --unified=0 643cc89bba...909bb0837c -- src apps/desktop/src-tauri apps/desktop/src ':!**/*.test.*'`). The §6 grep over the whole Epic 2 file-touch set since `fe5ae6cd4a` finds **3 hits, all in `data/sheet_rules/**` prose text carried verbatim from the corpus** (`bestiary_3:monster_ability:tophet_swallow_whole` "hack", `core_rulebook:spell:plant_growth` "hack", `ultimate_intrigue:class_feature:courtly_hunter_courtly_companion` "[… not yet implemented]" — the rulebook's own bracketed note), none in code; they pre-date this cycle (AT-35-E2-001's generated package) and are recorded as correction `1788844812035-at-35-e2-002-7cbeb2`.
- **Acceptance criterion:** `src/rules_core/sheet_rule.rs` evaluates `Expr` against the computed character — a match over the enum, no parser, no strings (`technical-design.md §2`). One generic "Rules and features" section in `apps/desktop/src/characterHub/CharacterSheet.tsx` lists every held rule's line, grouped by kind, on the existing sheet IPC. The `Not computed` lane keeps only records with no rule. **Evidence:** `cargo test --locked --lib sheet_rule` — one test per value form (the racial SLA rule evaluating to `Resolved(15)` at Charisma 14 and spell level 3; the weapon to `Dice("1d8+2")`; the choice trait to `Words`). 19 frontend tests, one per kind, asserting a held record's label and value are in the DOM for a fixture character. `pcgen_residue_gate.py --check` unchanged from baseline (this cycle adds zero live PCGen). Desktop crate tested explicitly.
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=1818 ratio=n/a builds_recorded=3 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 643cc89bba0de7e46341541be793d1039700a218 --before /tmp/wi-before-AT-35-E2-002.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E2-002` (`builds_recorded` 3: the lib-test iteration while the evaluator was written, the one verification chain `--no-run` → `--lib` → `--no-fail-fast`, and the clippy fix's lib rebuild; the desktop crate's separate target dir is not counted).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and at `909bb0837c` — unchanged. The evaluator, the loader, the DTO and the section read `SheetRule` / `VarTable` JSON and `PilotBaseChassisComputation` only.
- **Oracle parity:** N/A — no `Number` mapping row was added (the converter is untouched); the fixture-roster comparison is AT-35-E2-005's evidence.
- **Movement, four buckets:** closure 0 / relabel 0 / reachability 0 / instrument-correction 3 (retro corrections `1788844811737-at-35-e2-002-796340`, `1788844811883-at-35-e2-002-decb80`, `1788844812035-at-35-e2-002-7cbeb2`).
- **Refused tokens:** none (no converter run; `data/sheet_rules/_refused.json` unchanged at 1,810 records — `cargo run --locked --bin sheet_rule_convert -- --check` → `records=49438 converted=47628 refused=1810 rules=66514 var_tables=5081 verdict=PASS`).
- **Discoveries:** (1) **The leaf facts are narrower than `technical-design.md §2` states** — `PilotBaseChassisComputation` carries ability modifiers, BAB, base/total saves, three selected skill modifiers, explanations and diagnostics; size comes from `race_tables::race_size` for the 7 CRB races (Medium otherwise), walk speed from the `race.<slug>.trait_bundle.speed` explanation when emitted, and hit dice / CR / highest spell level / master level / other speeds are 0 until a later cycle carries them (correction `…-796340`; `CharacterFacts::from_character` names each). (2) **The package carries no `Granter::Class` or `Granter::Race` rows** (census over `core_rulebook`, `advanced_players_guide`, `bestiary`, `ultimate_psionics`: `Rule` 8,103, `ClassSpellList` 5,149, `Deity` 2,126 — nothing else) and 1,187 of 1,738 CRB `class_feature` rules have no `granted_by` at all (`fighter_bravery`: `applies: Always`, tag `FighterClassFeatures`); class features are placed by the chassis' own grounded `class_feature.*` explanation records joined by `<class>_<feature>` slug, racial traits by the race resolver's applied-trait keys, until AT-35-E3-001 derives `applies`/grants (correction `…-decb80`). (3) **Templates are not universal**: the first fixpoint held every `template` rule with no granter and `applies: Always` (starting gold, ghost, lich, zombie …, 1,000+ lines for a level-1 fighter); a rule nothing grants and nothing seeds is never held — `applies` says whether this character *may* hold it, not that it does (fixed in the cycle, pinned by `a_fixture_fighter_renders_lines_grouped_by_kind`). (4) A multi-line record's siblings print as separate lines: `core_rulebook:class_feature:fighter_bonus_feats` carries 22 `#bonusN` siblings and prints 23 lines — the converter's SAVE|ALL / per-level shape, AT-35-E3-001's placement question, not a rendering defect. (5) The §6 wired-integration grep matches rulebook prose in the generated package (correction `…-7cbeb2`).
- **Figures + their re-derive commands:**
  - `sheet_rules package: 47628 rule files, 5081 var files, 66147 rules, loaded in 2.66 s` (debug, parallel parse) — `cargo test --locked --lib -j 6 sheet_rule -- --nocapture` (the `package()` OnceLock line)
  - per-kind evaluation of every rule against the deterministic Human Fighter 1 (denominator: 66,147 rules in the package; a rule's `#suffix` siblings count as rules) — same command, the `kind=<k> number=<n> dice=<d> words=<w>` lines: `ability 2036/3/3138`, `class 12/0/0`, `class_feature 5205/134/14953`, `companion 3687/104/917`, `deity 0/0/458`, `domain 5/0/180`, `equipment 888/552/5143`, `equipment_modifier 7/0/1498`, `feat 422/3/1734`, `language 0/0/136`, `monster 8483/1010/625`, `monster_ability 958/16/3612`, `power 17/0/426`, `race 46/6/55`, `race_trait 701/28/1945`, `skill 325/0/38`, `spell 268/0/2805`, `template 1093/847/1085`, `trait 230/0/313` — 19 kinds
  - the fixture Human Fighter 1 (+ `feat:acrobatic`, 10 ranks Acrobatics, race traits `Human ~ Bonus Feat` / `Human ~ Skilled`) renders **45 lines** across kinds `class_feature, equipment, feat, race_trait, skill` — same command, the `fixture fighter lines:` line; `core_rulebook:feat:acrobatic` → `+4`, `#bonus1` (Fly) → `+2`, `core_rulebook:equipment:longsword` → `1d8`
  - value-form proofs — same command: `advanced_players_guide:race_trait:racial_sla_ill_omen` at Charisma 14 → `also` `DC 13` / `CL 1` / `1/day` (the converter folded the spell level to `Const(1)`, AT-35-E2-001 correction `…-8ed3da`); the design shape `Sum([Const(10), SpellLevel, AbilityMod(Cha)])` at spell level 3 → `Resolved(15)`; `core_rulebook:equipment:longsword` → `Dice("1d8")`, with `Const(2)` → `1d8+2`, with `AbilityMod(Str)` → `1d8+4` (Str 16 + Human +2), one size step → `1d10`; `advanced_players_guide:trait:trait_magical_knack` → `Words` with `(choice not yet made)`, `Wizard` once chosen
  - frontend: `node scripts/run-tests.mjs` → `101/101 test files passed` (100 before + `rulesAndFeaturesSection.test.ts`: 19 per-kind tests + 5 section tests; `classFeaturesModel.test.ts` +1); `tsc --noEmit` exit 0
  - desktop crate: `cargo test --locked -j 6` in `apps/desktop/src-tauri` (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-002-desktop`) → `574 passed; 0 failed` (573 + the reach-gate IPC test); `cargo clippy --locked --tests -j 6` there → 0 warnings
  - literal scan **0** files — `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l`
  - `completion_atlas.py --check` → `population=49438 buckets=10 unclassified=0 overlap=0` `stale_derived_at=False citation_failures=0` (re-stamped SD-34's atlas `derived_at`; reverted, outside this epic's set — same as AT-35-E2-001); `shape_engine_boundary.py --check` → `magnitude_bearing=26396 not_held_by_engine=8784 citation_ok=True`; `missing_engine_tables.py --check` → `citation_failures=0`; `denominator_gate.py --check` over the package globs → `violations=0`; `scripts/verify.sh --only pi-sweep` → `RESULT: PASS`; `scripts/token_coverage.py` does not exist yet (AT-35-E2-004) — skipped
  - granter census `{'Rule': 8103, 'ClassSpellList': 5149, 'Deity': 2126}` (denominator: every `granted_by` row over the top-level rule files of 4 books) — `python3 -c "import json,glob,collections as c; g=c.Counter(); [g.update(list(x['by'].keys())[0] for r in json.load(open(f)) for x in (r.get('granted_by') or [])) for b in ['core_rulebook','advanced_players_guide','bestiary','ultimate_psionics'] for f in glob.glob(f'data/sheet_rules/{b}/*/*.json')]; print(dict(g))"`; CRB `class_feature` rules with no `granted_by` **1187 of 1738** — `python3 -c "import json,glob; rs=[r for f in glob.glob('data/sheet_rules/core_rulebook/class_feature/*.json') for r in json.load(open(f))]; print(len(rs), sum(1 for r in rs if not r.get('granted_by')))"`
- **Build scope verified:** `cargo test --locked --no-run -j 6` exit 0 (`CARGO_TARGET_DIR=/tmp/cargo-sd35-AT-35-E2-002`, `CARGO_INCREMENTAL=0`, cold, 2 min 53 s); `cargo test --locked --lib -j 6` → `3214 passed; 0 failed; 14 ignored` (3206 + 8); `cargo test --locked --no-fail-fast -j 6` → **411 test binaries, 411 ok, 0 FAILED; 8,710 passed, 0 failed, 67 ignored** (`grep -c '^test result' full.log` / `awk` over the result lines); `cargo clippy --locked --lib -j 6` → 0 warnings after moving `Rat`'s arithmetic onto `std::ops::{Add, Mul, Div}` (3 `should_implement_trait` warnings fixed in the cycle; lib re-tested green); desktop crate and frontend run in THIS cycle (`apps/` touched) — see the figures above; run at SHA `909bb0837c` (the clippy fix is the last code change; the sweep ran on the tree one commit-diff earlier, differing only in the three `Rat` method-to-trait moves, whose 28 lib tests were re-run at HEAD).
- **Sweep population:** N/A (no corpus record changed; `docs/work-inventory.json` not regenerated — its inputs are unchanged and it does not read `data/sheet_rules/` until AT-35-E2-003)
- **Oracle pin:** `7f818006e371188e5717fd18d74d18a420747fc6` (`scripts/pcgen-oracle-pin.env`; unchanged; no figure here came from the pinned corpus)
- **Status:** complete
- **Notes:** `scripts/verify.sh --only figure-provenance` was red on one pre-existing line of `AT-35-E2-001_cycle1_receipt.md` (the `~23 s` wall time with no command); the command is added there in this cycle's docs commit (self-heal, `§8`). Judgment calls, each cited: (a) `character_hub.rs` and `rule_system_adapter.rs` are touched although §3's Epic 2 set names only `reach_gate.rs` on the desktop side — the DTO producer is the IPC the criterion says the section rides on (AGENTS.md: "`FILES YOU OWN` must be closed under the change it mandates … DTO producers"). (b) `SheetLine` carries `kind` and `printed` beyond the design's struct: the section groups by kind, and the printed form (`+2` for a targeted bonus, `15` otherwise) is the one truncation's output. (c) The `also` role prints as `"6/day"` / `"CL 5"` / `"DC 15"` per the design; a `Uses` period other than `day` prints `"<n> <period>"`. (d) A `Choice` term with no selection makes the whole value `Words`; a `ChoiceName` slot prints `(choice not yet made)`. (e) The seed's class-feature bridge takes the chassis' grounded `class_feature.*` records (level-correct today) rather than every rule tagged `<Class>ClassFeatures` (level-blind); AT-35-E3-001 replaces the bridge with derived `applies`. (f) `ClassSpellList` grants do not hold a spell — a class list is not the character's sheet; a selected spell is seeded. (g) The desktop's package `OnceLock` mirrors `race_trait_picker::race_corpus`; a load failure carries `sheet_rules_unavailable_reason` so the section says why it is empty. (h) The reach test creates a Human Fighter 3 through the real `create_character_at_root` path and reads `load_saved_character_at_root` — identity-only lines fail it the way the Feats-tab defect would.
- **Next-cycle scope:** criterion at zero (the evaluator, the loader, the IPC field, the section and the per-kind proofs exist; residue unchanged). AT-35-E2-003 next (`sheet-complete` status + classifier rung; `SheetLine.kind` and the desktop's `sheet_lines` are its consumers' inputs). Discoveries (2)–(4) are AT-35-E3-001's placement scope; (1)'s missing leaf facts are named in `CharacterFacts::from_character` for AT-35-E5.
