# Cycle AT-35-E3-004_cycle1 — Epic 3 — Place and surface / AT-35-E3-004

- **Commit SHA:** the docs commit carrying this receipt, `artifacts/epic-3-place-and-surface/rate-ledger.json`, the `progress.md` entry and `kanban.md` row 15, on `tranche/15` (cycle start `697b7780ea28f9a7ee284a54d676069c23961101`; the cycle moved no file under `src/**`, `scripts/**`, `tests/**`, `data/**` or `apps/**`, and `docs/work-inventory.json` is untouched — `cmp docs/work-inventory.json /tmp/wi-before-AT-35-E3-004.json` identical).
- **Scope gate:** `SCOPE_GATE: EXEMPT (ledger cycle — records this epic's per-cycle rows; closes zero units by design)` — `decisions.md §2` / `workflow-instruction.md §6` step 1's floor exemption. The exemption is legitimate here and only here: the cycle moves no unit, and the epic's unit population was already 0 at cycle start (`python3 scripts/completion_atlas.py --check` → `DONE 49438 of 49438`, every other bucket 0), so `cycle_scope_gate.py --min 500` with no flags would read `scoped=0 remaining_non_done=0`. **Nothing is exempt from the residue check:** `python3 scripts/pcgen_residue_gate.py --check` at start → `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` (exit 0).
- **Files touched:** `docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/rate-ledger.json` (verification stamp + this cycle's row + totals `cycles` 4 → 5); this receipt (new); `docs/release/SD-35-corpus-sheet-completion/progress.md` (prepended entry); `docs/release/SD-35-corpus-sheet-completion/kanban.md` (row 15 pointer re-pointed at this receipt); `docs/retro/events/at-35-e3-004.jsonl` (new). No file outside `docs/`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS — `git diff --unified=0 fe5ae6cd4a...HEAD -- src/pcgen_import/sheet_rule/ src/rules_core/class_feature_pool_catalog.rs src/bin/v06_work_inventory.rs data/sheet_rules/ docs/work-inventory.json docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/ ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → no match, both before and after this cycle's edits.
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff. Over the whole Epic 3 file-touch set since `fe5ae6cd4a` the same grep (`\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b`) returns only the pre-existing non-code hits every prior Epic 3 receipt recorded: 3 rulebook-prose strings inside `data/sheet_rules/**` `ProsePiece::Text` (Tophet "hack or smash", Plant Growth "hack or force", Courtly Companion "not yet implemented" — corpus text, recorded by AT-35-E2-002's correction `1788844812035-at-35-e2-002-7cbeb2`), the `engine_diagnostic:vacuous_placeholder_row_no_corpus_content_to_render` evidence strings in `docs/work-inventory.json`, and the prior receipts' own quotations of that grep pattern. No stub, inline mock or `"Would …"` string in shipping code; this cycle ships no code at all.
- **Acceptance criterion (verbatim, `epic-breakdown.md` `### AT-35-E3-004 — the rate ledger`):**
  > `artifacts/epic-3-place-and-surface/rate-ledger.json`: per cycle — mechanism, scoped
  > population, units closed, units relabeled, wall time, `rust_lines_changed / units_closed`,
  > `builds_recorded` (must be 1), `pcgen_live_files` (must not rise).
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 697b7780ea28f9a7ee284a54d676069c23961101 --before /tmp/wi-before-AT-35-E3-004.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E3-004` (`residue_gate=present`; `closed_by_kind=` empty; `relabeled_moves=` empty; `regressed=0 added=0 dropped=0`). `builds_recorded=0` because the diff is docs-only and no cargo build was paid — the same shape as `AT-35-E3-001_cycle1`; the ledger's `reading_rule` now names 0 explicitly so a reader does not read it as a missing figure.
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` — identical at cycle start and at HEAD, and identical to every prior Epic 3 receipt. Not risen.
- **Oracle parity:** N/A — no `Number` mapping added, no live path touched, no figure taken from the pinned corpus.
- **Movement, four buckets:**
  - **closure (into DONE, by id-set):** none — 0 units; the corpus was already `DONE 49438 of 49438` at cycle start.
  - **relabel (bucket to bucket):** none.
  - **reachability:** none.
  - **instrument-correction:** one **completeness** correction, not a figure correction. The ledger claimed "one row per Epic 3 cycle" while carrying 4 rows for what is now a 5-cycle epic — its own cycle was structurally missing, because a ledger written by cycles 3 and 4 cannot contain the cycle that verifies it. Fixed by adding the `AT-35-E3-004_cycle1` row and a `verified_at` block that states the closure test (`ls …/*_receipt.md | wc -l` equals `len(cycles)`). **No transcribed figure was wrong:** all four pre-existing rows re-verified exactly against their receipts.
- **Refused tokens:** none — this cycle read no corpus record and added no converter mapping row.
- **Discoveries:** none. Nothing this cycle touched surfaced a token type, kind or remaining-step category the atlas or `token-coverage.json` did not already carry (`token_coverage.py --check` → `token_types=232 shapes=1 refused_non_done=0 verdict=PASS`; `completion_atlas.py --check` → `unclassified=0 overlap=0 citation_failures=0`).
- **Figures + their re-derive commands:**
  - **Ledger rows = 5, receipts = 5, discrepancies = 0.** `ls docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/*_receipt.md | wc -l` → `5`; `python3 -c "import json;print(len(json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/rate-ledger.json'))['cycles']))"` → `5`. Denominator: the Epic 3 cycles that have run, all five.
  - **Row-by-row transcription check.** `grep -hnE '^\- \*\*(Receipt rows|Scope gate|Status)' docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/*_receipt.md` — every ledger `scope_gate`, `units_closed`, `units_relabeled`, `rust_lines_changed`, `ratio`, `builds_recorded` and `pcgen_live_files` value matches its receipt's literal line: `AT-35-E3-001_cycle1` `scoped=214 … FAIL_UNDER_FLOOR` / `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260`; `AT-35-E3-001_cycle2` `scoped=516 … PASS` / `closed=618 relabeled=0 rust_lines_changed=232 ratio=0.38 builds_recorded=3 pcgen_live_files=260`; `AT-35-E3-002_cycle1` `scoped=786 remaining_non_done=786 … PASS` / `closed=786 relabeled=0 rust_lines_changed=266 ratio=0.34 builds_recorded=3 pcgen_live_files=260`; `AT-35-E3-003_cycle1` `scoped=0 remaining_non_done=0 … PASS_WHOLE_REMAINDER` / `closed=0 relabeled=0 rust_lines_changed=37 ratio=n/a builds_recorded=1 pcgen_live_files=260`.
  - **Totals = the sum of the rows.** `python3 -c "import json;c=json.load(open('docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/rate-ledger.json'))['cycles'];print(len(c),sum(x['units_closed'] for x in c),sum(x['units_relabeled'] for x in c),sum(x['rust_lines_changed'] for x in c),sum(x['builds_recorded'] for x in c))"` → `5 1404 0 535 7`, matching `totals`. Denominator: the same five rows.
  - **`ratio_over_the_epic` = 0.38.** `535 / 1404 = 0.381…`. Denominator: the **1,404** units non-DONE of 49,438 at Epic 3's first cycle (`a542652c5e`), not the 23,315 of bundle launch — the two zero-closing cycles contribute Rust lines and builds but no closures, so the ratio's denominator stays 1,404.
  - **`builds_recorded` total = 7 over 5 cycles, against a target of 1 per unit-moving cycle.** Two cycles ran 3 each; both receipts name the overrun as three sequential prerequisite builds, not three verification passes, and the ledger `note` rows carry that verbatim. Re-derive by reading the `builds_recorded` field of each row.
  - **`pcgen_live_files` = 260 at every row, start and end.** `python3 scripts/pcgen_residue_gate.py --check`. Denominator: the 260-file live surface of the `AT-35-E1-005` baseline (`scripts/pcgen-residue-baseline.env`).
- **Build scope verified:** no cargo build — the cycle changed no Rust and no corpus record (`git diff --stat 697b7780ea..HEAD -- src scripts tests data apps` empty), so per `workflow-instruction.md §6` step 3 ("run … after the last figure-moving commit") the cargo stages and `corpus_literal_sweep` are not run, the `AT-35-E3-001_cycle1` precedent. The instrument gates that can invalidate this cycle's artifact **were** run, at `697b7780ea`: `completion_atlas.py --check` EXIT=0 (`done_evidence_violations=0 missing_clearing_mechanisms=0 stale_derived_at=False citation_failures=0`); `token_coverage.py --check` EXIT=0 (`non_done=0 tokened=0 token_less=0 refused=142 refused_non_done=0 token_types=232 shapes=1 verdict=PASS`); `shape_engine_boundary.py --check` EXIT=0 (`magnitude_bearing=26396 not_held_by_engine=0 citation_ok=True`); `missing_engine_tables.py --check` EXIT=0 (`population=0 kinds=0 citation_failures=0`); `grep -rlE 'BONUS:|DEFINE:|PRE[A-Z]+:|%CHOICE|CL=' data/sheet_rules/ | wc -l` → `0`; `pcgen_residue_gate.py --check` PASS; `denominator_gate.py --check` and `verify.sh --only pi-sweep` results recorded in **Notes**.
- **Sweep population:** N/A — no corpus record changed, so `corpus_literal_sweep` was not re-run (`§6` step 3's guard).
- **Oracle pin:** N/A — no figure in this receipt came from the pinned corpus. (`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6` is unchanged and unread by this cycle.)
- **Status:** **complete.** The criterion's artifact exists at `artifacts/epic-3-place-and-surface/rate-ledger.json` and carries, for **every** Epic 3 cycle including this one, all seven fields the criterion names — mechanism, scoped population, units closed, units relabeled, wall time, `rust_lines_changed / units_closed` (as `ratio`, `null` where the divisor is 0), `builds_recorded` and `pcgen_live_files` — each transcribed from that cycle's own receipt and re-verified at `697b7780ea`. The criterion's unit population is 0 at HEAD (it moves no units by design); the two constraint clauses are reported, not silently satisfied: `builds_recorded` is 1 on one cycle, 0 on the two docs-only cycles and 3 on two cycles (named as overruns in their `note` rows, per `decisions.md §3`), and `pcgen_live_files` did not rise on any cycle (260 throughout).
- **Notes:** The ledger was authored incidentally by `AT-35-E3-002_cycle1` and extended by `AT-35-E3-003_cycle1`, and `kanban.md` row 15 already read `complete` before this cycle ran. That was a claim without a receipt: no cycle had verified the transcription, and the row set could not include the verifying cycle. This cycle is that receipt. `denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'` and `scripts/verify.sh --only pi-sweep` were run on the final tree; their literal outputs are in the retro event log `docs/retro/events/at-35-e3-004.jsonl` and in `progress.md`'s entry for this cycle. No `## Open blockers` entry: nothing here needs an operator ruling.
- **Next-cycle scope:** criterion at zero — `AT-35-E3-004` needs no further cycle. Epic 3's four criteria are all at `complete`; the epic's remaining exposure is elsewhere on the board (rows 17, 22, 23 `in-progress`, carried by `deferral 1788922132640-at-35-e3-002-ac4da5`), not in this ledger.
