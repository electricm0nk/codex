# Cycle AT-35-E3-001_cycle1 — Epic 3 — Place and surface / AT-35-E3-001

- **Commit SHA:** the docs commit carrying this receipt, the `progress.md` entry, `kanban.md` row 12 and `docs/retro/events/at-35-e3-001.jsonl`, on `tranche/15` (cycle start `8cc4ea1516295f09de9479d5a1acd2d1006d477d`; the cycle moved no file under `src/**`, `scripts/**`, `tests/**`, `data/**` or `apps/**`, and `docs/work-inventory.json` is untouched — `cmp docs/work-inventory.json /tmp/wi-before-AT-35-E3-001.json` identical)
- **Scope gate:** `scoped=214 remaining_non_done=1404 floor=500 verdict=FAIL_UNDER_FLOOR` (exit 1) — the literal last line of `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature` at `8cc4ea1516` (`scope=bucket=B AND kind=class_feature`, `scoped_by_bucket=B:214`, `scoped_by_kind=class_feature:214`). **The cycle did not start** (`workflow-instruction.md §6` step 1: "a cycle that cannot pass the floor does not start; it reports the scoped population and stops"; `§8`: "a cycle under the floor that is not the whole remainder" is non-self-healable). Not `EXEMPT` — a unit-moving criterion may not claim the exemption. `python3 scripts/pcgen_residue_gate.py --check` at start: `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS`.
- **Files touched:** this receipt (new); `docs/release/SD-35-corpus-sheet-completion/progress.md` (prepended entry; status matrix Epic 3 → in progress 1); `docs/release/SD-35-corpus-sheet-completion/kanban.md` (row 12 → `blocked-escalated` with pointer); `docs/retro/events/at-35-e3-001.jsonl` (new — 1 correction + 1 deferral). The atlas `--check` re-stamped `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` `derived_at`; reverted, outside this cycle's set — as every prior cycle did.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS — `BASE_BRANCH=fe5ae6cd4a5f3c65d5d10f4d523f00e33b04ac47` (`git merge-base HEAD origin/develop`); `git diff --unified=0 "${BASE_BRANCH}...HEAD" -- src/pcgen_import/sheet_rule/ src/rules_core/class_feature_pool_catalog.rs src/bin/v06_work_inventory.rs data/sheet_rules/ docs/work-inventory.json docs/release/SD-35-corpus-sheet-completion/artifacts/epic-3-place-and-surface/ ':!**/__tests__/**' ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → no match, at start and on the final diff (this cycle's own diff over the Epic 3 file-touch set is empty).
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff (empty over the Epic 3 file-touch set). Over the whole set since `fe5ae6cd4a` the same grep (`\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b`) finds 9 pre-existing hits, none in code: 3 rulebook-prose hits inside `data/sheet_rules/**` `ProsePiece::Text` strings (Tophet "hack or smash", Plant Growth "hack or force", Courtly Companion "not yet implemented" — corpus text, recorded by AT-35-E2-002's correction `1788844812035-at-35-e2-002-7cbeb2`), and 6 in `docs/work-inventory.json` that are the 3 pre-existing `engine_diagnostic:vacuous_placeholder_row_no_corpus_content_to_render` evidence strings appearing once removed and once re-added when AT-35-E2-004 added the `tokens` field to every unit. Once this receipt is committed the same grep over `artifacts/epic-3-place-and-surface/` also matches this row's own quotation of the pattern and of those three prose hits — a receipt quoting the audit, as every prior receipt recorded; no code hit.
- **Acceptance criterion:** verbatim from `epic-breakdown.md` `### AT-35-E3-001`: "7,866 units at authoring across three evidence families (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`, `class_feature_option_pool_record_with_magnitude_not_held_by_engine`, `class_feature_option_pool_record_not_held_by_engine`). Under the new design "held" means the character's holdings include the rule — `SheetRule.applies` says which class/level/choice holds it, derived at convert time from the corpus's own `CLASS` / `ABILITYCATEGORY` declarations, not a hand-kept list. Widen the converter's `applies` derivation and the live holdings lookup. SD-33's open deferral 1 (1,128 unmatched pool-group prefixes) closes here. **Evidence:** `completion_atlas.py --by-kind` reports `class_feature` B at 0; movement by id-set diff; every cycle's `cycle_scope_gate.py` output."
- **Receipt rows (mechanical):** `closed=0 relabeled=0 rust_lines_changed=0 ratio=n/a builds_recorded=0 pcgen_live_files=260` — the literal last line of `python3 scripts/cycle_scope_gate.py --receipt --since 8cc4ea1516295f09de9479d5a1acd2d1006d477d --before /tmp/wi-before-AT-35-E3-001.json --after docs/work-inventory.json --target-dir /tmp/cargo-sd35-AT-35-E3-001` (`residue_gate=present`, `closed_by_kind=` empty, `relabeled_moves=` empty, `regressed=0 added=0 dropped=0`; no build was paid — the cycle did not start).
- **PCGen residue:** `live_files=260 live_hits=12736 baseline_files=260 baseline_hits=12736 verdict=PASS` at start and after (no live-side file touched) — not risen.
- **Oracle parity:** N/A (no `Number` mapping added; no live path touched).
- **Movement, four buckets:** closure **0** / relabel **0** / reachability **0** / instrument-correction **1** (correction `1788879241856-at-35-e3-001-ccb13f`, below). Buckets at HEAD unchanged: `A 1 B 437 C 79 D 43 M 63 V 392 U 202 X 168 Z 19`, non-DONE **1,404 of 49,438** (`python3 scripts/completion_atlas.py --check` → `population=49438 buckets=10 unclassified=0 overlap=0 done_evidence_violations=0`).
- **Refused tokens:** the scoped 214 units are **214 of 214 converter-refused** (every id is an entry of `data/sheet_rules/_refused.json`; status `engine-does-not-hold` 214 of 214). By first refused token type, summing to 214: `FORMULA:var(COUNT)=169, FORMULA:malformed (parser refusals)=11, BONUS:[redacted PI]=6, FORMULA:var(STAT)=6, BONUS:SITUATION (target shape)=2, FORMULA:var(SPELLFAILURE)=2, FORMULA:var(<export token>) (ENCUMBERANCE)=2, BONUS:STAT (target BASESPELLKNOWNSTAT;Class)=2, FORMULA:identifier DEFINEd nowhere (CharmLVL)=1, FORMULA:identifier DEFINEd nowhere (Gunslinger)=1, FORMULA:identifier DEFINEd nowhere (Inquisitor)=1, FORMULA:identifier DEFINEd nowhere (Cavalier)=1, FORMULA:identifier DEFINEd nowhere (Sorcerer)=1, FORMULA:identifier DEFINEd nowhere (HP)=1, FORMULA:identifier DEFINEd nowhere (OccultistNecromanticFocus)=1, BONUS:LOADMULT=1, FORMULA:var(<export token>) (MOVEBASE)=1, FORMULA:var(<export token>) (ENCUMBRANCE)=1, BONUS:STAT (target BASESPELLSTAT;CLASS)=1, FORMULA:var(AC)=1, FORMULA:var(HP)=1, FORMULA:var(MOVE)=1` — **24 distinct refused token types** (multiplicity 220 over 214 units: 209 units carry 1 type, 4 carry 2, 1 carries 3; `FORMULA:var(COUNT)` 171 of 220 by multiplicity). 24 > 10 is `§8`'s "more than 10 distinct refused token types surfacing in one cycle — re-scope, do not grind". Deferral `1788879242003-at-35-e3-001-bf4043` names all of it.
- **Discoveries:** one `correction` event (`1788879241856-at-35-e3-001-ccb13f`): `epic-breakdown.md` `### AT-35-E3-001` and this cycle's dispatch prompt carry **7,866** (the authoring figure, pre-Epic 2); at `8cc4ea1516` the population is **214 of 1,404 non-DONE of 49,438**, and every one of the 214 is converter-refused — the `applies` widening the criterion names has no non-refused unit left to move (AT-35-E2-005 cycle 1 stamped every non-refused B unit `sheet-complete`; the DISPOSITION owner rule already routes refused B units to AT-35-E4-001). Caught before implementation, by re-deriving rather than copying. No new token type: all 24 are in `token-coverage.json`'s 81 shapes.
- **Figures + their re-derive commands:**
  - scoped **214 of 1,404 non-DONE of 49,438**, `FAIL_UNDER_FLOOR` — `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature`
  - `class_feature` B **214 of 18,043** `class_feature` units; whole bucket B **437 of 1,404** (`class_feature 214, class 115, ability 82, race_trait 14, feat 6, monster 2, spell 2, template 1, companion 1`) — `python3 scripts/completion_atlas.py --by-kind`; `python3 scripts/cycle_scope_gate.py --min 500 --bucket B` → `scoped=437 … verdict=FAIL_UNDER_FLOOR`
  - evidence families over the 214: `class_feature_owner_matched_by_name_but_record_not_held_by_engine` **154**, `class_feature_option_pool_record_with_magnitude_not_held_by_engine` **42**, `class_feature_option_pool_record_not_held_by_engine` **18** (sum 214) — `python3 scripts/cycle_scope_gate.py --min 500 --bucket B --kind class_feature --evidence-prefix class_feature_option_pool` → `scoped=60`; the owner-matched family is the 214 − 60 remainder, re-derivable by `--evidence-prefix class_feature_owner_matched`
  - books over the 214: inner_sea_combat 87, advanced_players_guide 36, advanced_race_guide 29, ultimate_intrigue 23, ultimate_psionics 7, ultimate_combat 6, horror_adventures 5, advanced_class_guide 4, core_rulebook 4, inner_sea_magic 3, and 7 books at 1–2 (sum 214) — `python3 -c "import json,sys;sys.path.insert(0,'scripts');import completion_atlas as A,collections as C;u=json.load(open('docs/work-inventory.json'))['units'];print(C.Counter(x['book'] for x in u if A._bucket_of(x)=='B' and x['kind']=='class_feature'))"`
  - refused **214 of 214** and the 24 token types — the same one-liner joined on `{e['id'] for e in json.load(open('data/sheet_rules/_refused.json'))['entries']}` and counting `entries[*].token_types` (denominators: 214 scoped units; 1,810 refused records)
  - a floor-passing re-scope: `python3 scripts/cycle_scope_gate.py --min 500 --bucket A --bucket B --bucket C --bucket D --bucket M` → `scoped=623 remaining_non_done=1404 floor=500 verdict=PASS` (`class_feature 302, class 144, ability 91, …` — all 623 are converter-refused per the DISPOSITION hand-off: A 1 + B 437 + C 79 + D 43 + M 63); or no flags → the whole 1,404 (`PASS_WHOLE_REMAINDER`)
  - receipt rows — the `--receipt` invocation above; PCGen residue — `python3 scripts/pcgen_residue_gate.py --check`
  - denominator gate on the final tree — `python3 scripts/denominator_gate.py --check 'docs/release/SD-35-corpus-sheet-completion/*.md' 'docs/release/SD-35-corpus-sheet-completion/artifacts/**/*.md'` (result in `progress.md`'s entry)
- **Build scope verified:** no build — the cycle did not start and nothing outside `docs/` changed (`git diff --stat 8cc4ea1516..HEAD -- src scripts tests data apps` empty), so `cargo test --locked --no-run` / `--lib` / `--no-fail-fast` / clippy / `sheet_rule_convert -- --check` / `corpus_literal_sweep` / `token_coverage.py` / `shape_engine_boundary.py` / `missing_engine_tables.py` / `pi-sweep` are not run (`§6` step 3: the build runs after a figure-moving change). Gates run at `8cc4ea1516`: `cycle_scope_gate.py --min 500` (FAIL_UNDER_FLOOR, the finding), `pcgen_residue_gate.py --check` PASS, `completion_atlas.py --check` green, `denominator_gate.py --check` on the final tree.
- **Sweep population:** N/A (no corpus record changed; no regen).
- **Oracle pin:** N/A (no figure from the pinned corpus).
- **Status:** blocked-escalated — `workflow-instruction.md §8`'s non-self-healable list, two entries: **a cycle under the floor that is not the whole remainder** (214 < 500, 214 ≠ 1,404) and **more than 10 distinct refused token types** (24). This is the orchestrator re-scope outcome `§6` step 1 names, not an operator ruling — no `## Open blockers` entry. The criterion's population is **214, not 0**, so it is not `complete`; nothing was closed, so it is not `partial`.
- **Notes:** (a) The three evidence families the criterion names have **0** non-refused units left at HEAD — the `applies` widening AT-35-E2-005 cycle 1 already performed corpus-wide closed every B unit the converter could convert; what remains is mapping-row work (`FORMULA:var(COUNT)` first), which is `src/pcgen_import/sheet_rule/` mapping rows — AT-35-E4-001's file-touch set (`workflow-instruction.md §3` Epic 4), not Epic 3's `applies` derivation. (b) SD-33 open deferral 1 (1,128 unmatched pool-group prefixes) is not closed by this cycle; it stays with this criterion until `class_feature` B reads 0. (c) Bundling every kind's B (437) is still under the floor; the smallest passing bundle is the refused buckets A+B+C+D+M (623), which is AT-35-E4-001's population by the DISPOSITION owner rule.
- **Next-cycle scope:** re-dispatch as **AT-35-E4-001 cycle 1** over the refused units by refusal string — `python3 scripts/cycle_scope_gate.py --min 500 --bucket A --bucket B --bucket C --bucket D --bucket M` → `scoped=623 remaining_non_done=1404 floor=500 verdict=PASS` (the 36 refused U/V/X units ride along under `--or`), or the whole remainder with no flags → `PASS_WHOLE_REMAINDER`; AT-35-E3-001 re-runs `--bucket B --kind class_feature` after those mapping rows land and closes at `class_feature` B = 0.
