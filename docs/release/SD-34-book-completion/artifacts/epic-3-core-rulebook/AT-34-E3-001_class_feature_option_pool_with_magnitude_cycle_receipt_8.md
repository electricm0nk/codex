# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine` mechanism, cycle 8)

- **Commit SHA:** filled after commit (see git log; this receipt is committed in the same
  commit as the code, so `git log -1` at the time of reading resolves it)
- **Files touched:** `src/rules_core/pilot_compute/mod.rs` (new `UNIVERSAL_SCHOOL_SELECTION`
  constant, new `wizard_has_canonical_universal_selection`, one new computation block gated
  on it -- Hand of the Apprentice uses-per-day, Metamagic Mastery uses-per-day, 2 new
  `ComputationExplanation` pushes), `src/bin/v06_work_inventory.rs`
  (`probe_wizard_arcane_school_wiring` gained a fifth variant swapping the specialization
  choice to `"school:universal"` and CLEARING the opposed-schools choice entirely, 3 new
  tests, doc comments naming the fifth school and rewriting the stale "not built yet" list),
  `scripts/completion_atlas.py` (10 shifted `file:line` citations, re-derived by direct grep
  against the post-edit file, not assumed), `scripts/missing_engine_tables.py` (2 shifted
  `file:line` citations, re-derived), `docs/work-inventory.json` (regenerated, guarded),
  `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
  `.../missing-engine-tables.json` (regenerated by their own `--check` runs),
  `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, this receipt,
  `docs/retro/events/sd34-at-34-e3-001.jsonl`.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` for this cycle's own diff (`git diff
  --unified=0 617c1d3b40 -- src/rules_core/ src/bin/ scripts/` -- the file-touch set actually
  edited this cycle -- contains zero `sd[0-9]+_`/`t_[0-9a-f]{8,}` matches). The wider
  epic-scoped diff against `origin/develop` does surface many `sd32_*` marker-string matches,
  but every one is either pre-existing `docs/work-inventory.json` data-field content from
  earlier already-audited cycles, or prose *inside* prior cycles' own committed receipt
  markdown files quoting that same finding -- none is new code this cycle wrote.
- **Wired-integration audit result:** `OK_NO_TOKENS` for this cycle's own diff (same narrow
  command as above, zero matches). No stub/mock/placeholder token appears in any line this
  cycle added.
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**970** Core Rulebook units
  whose table exists but which are not in it. Evidence: the atlas reporting bucket B at zero
  for `core_rulebook`, and the mechanism that placed them named -- by mechanism, not per
  record." (970 is `epic-breakdown.md`'s own stale, whole-bucket figure; this cycle owns
  exactly ONE of the nine mechanisms `decisions.md §14` names, and does not itself close the
  bucket.)
- **Status:** partial

Bucket B for `core_rulebook` is nine distinct mechanisms (`decisions.md §14`). This cycle owns
exactly one: `class_feature_option_pool_record_with_magnitude_not_held_by_engine` --
continuing cycle 7's own work (`..._cycle_receipt_7.md`, 261 -> 258).

## Population, re-derived (not quoted)

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
```
Before this cycle: **258** of 553 `core_rulebook` bucket-B units (re-derived at this cycle's
own start SHA `617c1d3b40`, matching cycle 7's own closing figure exactly -- no drift to
correct). Corpus-wide (37 books) before: **2,973** of 49,438 units.

## Mechanism-specific direction followed

Read cycle 7's own receipt in full before touching anything
(`AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_7.md`), continuing from
its own named remainder rather than re-deriving it. Its own next-cycle plan named "one more
wizard school, built end-to-end" among the remaining schools (Divination, Enchantment,
Illusion, Necromancy, Universal). Reading `data/corpus/core_rulebook/class_feature/` directly
(never assumed) found Universal has only **2** power records (`hand_of_the_apprentice.json`,
`metamagic_mastery.json`) -- cheaper than Divination's 3 (`diviner_s_fortune`, `forwarned`,
`scrying_adept`) and Enchantment/Illusion/Necromancy's 3 each -- so Universal was taken.

**Universal is genuinely different in shape from every specialist school cycles 4/6/7 already
grounded**, and this cycle verified that difference directly against the corpus and the
engine before writing any code: a wizard who declines to specialize gains the Universal
School's own powers instead of a specialist school's, and PF1's own rule (confirmed by
reading `Universal School ~ *`'s own raw tokens, which carry no opposed-school choice
machinery at all -- only the shared `BONUS:VAR|OppositionalSchool|-2`) is that a universalist
"need not select an opposition school." Every specialist gate this mechanism already built
(`wizard_has_canonical_{abjuration,transmutation,conjuration}_selection`) requires the
opposed-schools choice set to carry exactly two selections; the new universalist gate
requires it to carry **zero**.

## The real fix: read the engine's own Universal-school corpus records directly before building anything

Reading `data/corpus/core_rulebook/class_feature/universal_school/*.json` directly found the
SAME underlying shape cycles 4-7's own school groundings each already established: real,
non-fabricated `BONUS:VAR` formulas the engine had never wired to `classify()`, because the
corpus record's `group` prefix (`"Universal School"`) can never equal `"wizard"`.

**New formulas built this cycle** (`src/rules_core/pilot_compute/mod.rs`), gated on a new
`wizard_has_canonical_universal_selection` (specialization == `"school:universal"` AND zero
opposed-school selections -- deliberately NOT mirroring the specialist gates' `opposed.len()
== 2` check, since that shape is wrong for this arm):

- **Hand of the Apprentice** (`Universal School ~ Hand of the Apprentice`) --
  `UniversalHandOfTheApprenticeTimes|ArcaneSchoolPowerTimes`, the same shared "3 + Intelligence
  modifier" idiom the pre-existing Force Missile / Telekinetic Fist / Acid Dart groundings
  already use, unlocked from level 1 (`PREVARLT:Wizard_CF_SchoolPower1,1` on the record
  itself).
- **Metamagic Mastery** (`Universal School ~ Metamagic Mastery`) --
  `UniversalMetamagicMasteryTimes|(UniversalSchoolLVL-8)/2+1`, gated on the power's own
  level-8 unlock (`PREVARLT:Wizard_CF_SchoolPower8,1`).

**Deliberately NOT claimed**, consistent with every prior school's precedent: the top-level
`"Universal School"` recognition record itself carries only the shared, non-numeric
`Pool_ArcaneOppositionSchool`/`OppositionalSchool` bookkeeping every school carries
identically -- no per-record formula, no explanation id, not this mechanism's shape.

## The probe

Extended `probe_wizard_arcane_school_wiring` (`src/bin/v06_work_inventory.rs`) with a fifth
variant: swap the specialization choice to `"school:universal"` and **retain-away** every
existing `choice:wizard_opposed_schools` selection with no replacement pushed (unlike every
specialist variant, which always pushes exactly two). Verified before committing by checking
the two target `corpus_key`s directly against the regenerated inventory after the guarded
regen, not by trusting the predicted delta alone -- the implementation closed exactly the 2
records predicted on the first attempt, no correction round needed.

`classify()`'s pre-existing `wizard_arcane_school_wired` check (unchanged this cycle -- exact
`unit.key` `BTreeSet` membership, not a substring match) now also resolves the corrected set.
Doc comments naming the schools this probe grounds were rewritten to name Transmutation,
Conjuration, and Universal (cycles 6-8), replacing a stale list that had drifted since cycle 6
(it still said "Every OTHER school (Conjuration, Divination, ...")" even after Conjuration was
grounded in cycle 7 -- fixed this cycle as a byproduct, not separately tracked as a defect
since it never affected any gate).

## RED -> GREEN

RED (confirmed for the intended reason): before this cycle's probe extension, both
`core_rulebook` target records fell through the same owner-resolution path every sibling
record does (`group` can never equal `"wizard"`), landing at `engine-does-not-hold` /
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` -- the live corpus data
itself is the RED proof (`docs/work-inventory.json` at this cycle's start SHA lists exactly
this verdict for both).

GREEN: three new tests added to `class_feature_text_complete_rung_tests`:
- `a_wizard_universal_school_record_the_probe_observed_reaches_grounded` -- probe fact seeded
  with `"Universal School ~ Hand of the Apprentice"`; asserts `status == "grounded"`, the
  pre-existing `wizard_arcane_school_probe_observed_a_real_computed_magnitude` evidence string
  (the SAME evidence id every prior school already uses -- one probe, one evidence string,
  five schools now).
- `a_wizard_universal_metamagic_mastery_record_the_probe_observed_reaches_grounded` -- same
  proof for the level-8 `"Universal School ~ Metamagic Mastery"` power, a DIFFERENT record
  within the same group prefix.
- `a_wizard_universal_school_record_the_probe_never_observed_is_unaffected` -- negative
  control, empty `EngineFacts`, uses the top-level `"Universal School"` recognition record
  itself (deliberately never claimed); asserts the pre-existing `engine-does-not-hold` verdict
  is unchanged, doubling as proof the probe's own bound is honored.

```
cargo test --locked --bin v06_work_inventory universal
running 47 tests
test class_feature_text_complete_rung_tests::a_wizard_universal_metamagic_mastery_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_universal_school_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_universal_school_record_the_probe_never_observed_is_unaffected ... ok
test result: ok. 47 passed; 0 failed; 0 ignored; 0 measured; 361 filtered out; finished in 0.01s
```

Live regen confirms **2** corpus-wide records moved -- both `core_rulebook` (this mechanism's
own scope), with **no cross-book side effect**: unlike Transmutation's own Arcanist exploit
counterpart, `advanced_class_guide` carries no `"Universal School"` record at all (checked
directly, not assumed: `grep -rl "Universal School" data/corpus/advanced_class_guide/` returns
nothing):

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
w = [x for x in d['units'] if x.get('evidence','')=='wizard_arcane_school_probe_observed_a_real_computed_magnitude'
     and x['corpus_key'].startswith('Universal')]
print(sorted((x['book'], x['corpus_key']) for x in w))
print(len(w))
"
[('core_rulebook', 'Universal School ~ Hand of the Apprentice'), ('core_rulebook', 'Universal School ~ Metamagic Mastery')]
2
```

Whole-file before/after diff against the committed `HEAD` copy of `docs/work-inventory.json`
confirms exactly the 2 target records moved (4 changed lines, old+new versions each), plus the
`generated_at` timestamp and the `by_status`/`class_feature` summary counters -- no other
record anywhere in the 49,438-unit file moved.

## Discoveries

**No probe defect this cycle**, unlike cycle 6's own two-round history. The universalist
gate's shape (zero opposed selections, not two) was identified BEFORE writing the probe
variant by reading the corpus record's raw tokens first, so the first probe implementation
already cleared the opposed-schools choice set correctly and the first full guarded regen
closed exactly the 2 records predicted.

**Regen required the sweep/fixture-check env vars this time** (`CORPUS_LITERAL_SWEEP_REPORT`,
`DERIVED_FIXTURE_CHECK_REPORT`) -- the binary's own stamp-loss guard refused an unguarded regen
(`this run would drop 9516 of 9516 verification stamp(s)`), correctly, since no
`--allow-stamp-loss` was ever passed. Ran `corpus_literal_sweep --json-out` and
`derived_evaluator_fixture_check --json-out` first (both CLEAN, matching the unchanged
baseline below), pointed the two env vars at their reports, then regenerated successfully.
Documented here since prior receipts' regen sections did not mention this step explicitly --
worth carrying forward for the next cycle rather than re-discovering it.

**Unrelated pre-existing test failure, NOT this cycle's to fix, re-flagged for the owning
cycle (unchanged from cycles 3-7's own receipts).** `cargo test --locked --test
v06_work_inventory` still fails one test on
`core_rulebook:class_feature:empty_selection_standard_barbarian` -- a different sibling
mechanism's own fix (`vacuous_placeholder_row_no_corpus_content_to_render`, Barbarian's own
PCGen CHOOSE-menu placeholder row), confirmed present in `docs/work-inventory.json` before
this cycle touched anything, unrelated to Universal School.

**Regen wall-clock cost, re-confirmed.** The full guarded regen (`cargo run --release --bin
v06_work_inventory`), took roughly 2-3 minutes of wall time this cycle (release build already
warm from the sweep/fixture-check builds); ran once to completion, no correction round needed.

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| Mechanism population before | 258 | `python3 -c "..."` (above) against `docs/work-inventory.json` at start SHA `617c1d3b40` | of 553 `core_rulebook` bucket-B units |
| Mechanism population after | **256** | same command against the regenerated `docs/work-inventory.json` | of 553 `core_rulebook` bucket-B units (pre-cycle denominator) |
| Corpus-wide mechanism population before | 2,973 | same command, no `book` filter | of 49,438 units |
| Corpus-wide mechanism population after | 2,971 | same command, no `book` filter | of 49,438 units (delta -2, exactly this cycle's 2 closures) |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) before | 534 | `python3 scripts/completion_atlas.py --check --book core_rulebook` at start SHA | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) after | 532 | `python3 scripts/completion_atlas.py --check --book core_rulebook` | of 6,701 `core_rulebook` units |
| Units closed (corpus-wide) | 2 (both `core_rulebook`, no cross-book side effect) | `evidence == "wizard_arcane_school_probe_observed_a_real_computed_magnitude"`, Universal subset | of 258 (core_rulebook), 2,973 (corpus-wide) |
| `completion_atlas.py --check` (corpus-wide) | `population=49438 unclassified=0 overlap=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| `missing_engine_tables.py --check` | `population=449 citation_failures=0` | `python3 scripts/missing_engine_tables.py --check` | of 449 |
| `corpus_literal_sweep` | `48708 examined of 51482 read, 0 findings` (before and after -- no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/release/corpus_literal_sweep"` | of 51,482 |
| `derived_evaluator_fixture_check` | `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/release/derived_evaluator_fixture_check"` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `408 passed; 0 failed` | `cargo test --locked --bin v06_work_inventory` | of 408 (405 + 3 new) |
| `cargo test --locked --lib` | `2910 passed; 0 failed; 14 ignored` | `cargo test --locked --lib` | of 2924 |
| `cargo test --locked --no-run` (workspace) | exit 0 | `cargo test --locked --no-run` | -- |
| `cargo test --locked --test v06_work_inventory` | `15 passed; 1 failed (pre-existing, unrelated -- see Discoveries); 1 ignored` | `cargo test --locked --test v06_work_inventory` | of 17 |
| `F1` (`shape_ledger.py` rollup) | 5400 -> 5400 (unchanged, checked not assumed -- neither new formula is bare-literal: one is the shared `ArcaneSchoolPowerTimes` idiom, F4-shaped; the other is a clamped per-level expression, F5-shaped) | `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` | of not-done units after this cycle's regen |
| `box_ledger.py --check` | exit 1, 7 stale-count WARNINGs (inherited THE-BOX.md drift, unowned by this mechanism, pre-existing, unchanged shape from cycles 6-7), `uncovered=19859 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
256
```
Not zero. **Status: partial**, remainder named below.

## Build scope verified

`cargo test --locked --no-run` exits 0 (workspace), `cargo test --locked --lib` 2910/2910
pass, `cargo test --locked --bin v06_work_inventory` 408/408 pass, `cargo test --locked --test
v06_work_inventory` 15/16 pass (1 pre-existing unrelated failure) -- all run at this cycle's
own post-regen HEAD (the commit this receipt lands in). Desktop crate
(`apps/desktop/src-tauri`) not re-tested this cycle: no file under that tree was touched.

## Sweep population

`corpus_literal_sweep`: 48,708 of 51,482 examined, before and after -- unchanged, since no
`data/corpus/**` file was added or regenerated this cycle (only `docs/work-inventory.json`,
which is not itself a corpus record).

## Oracle pin

N/A -- no figure in this receipt came from the pinned PCGen oracle corpus.

## Movement, four buckets

- **Closure:** 2 (`Universal School ~ Hand of the Apprentice`, `~ Metamagic Mastery`, both
  bucket B -> DONE via `grounded`).
- **Reclassification:** 0.
- **Reachability:** 2 (two new `ComputationExplanation` records now answer `held` for these
  exact corpus keys, through the SAME generic probe/consumer already shipped for every prior
  school -- this cycle only widened when it is called and to which choice shape).
- **Instrument-correction:** 0 (the starting population re-derived cleanly to the same 258
  cycle 7 reported; no wrong prior claim was found in this mechanism's own count).

## Remainder — this mechanism's own 256 units, named by sub-cause and re-derived fresh

| Sub-cause | Population | Status |
|---|---:|---|
| `Domain Power` (Cleric) | 56 | Unstarted this mechanism-cycle; owned by a different sub-cause than the wizard-school lever this cycle used. |
| `Domain Base` (Cleric) | 33 | Investigated in cycle 5: genuinely computable DC formula, never consumed by any real domain-power computation -- left named rather than force-closed. |
| Remaining wizard-school clusters (Divination, Enchantment, Illusion, Necromancy — none built yet) | ~34 (re-derive at next cycle's own start; not directly re-verified this cycle, no formula code touches them) | Same lever this cycle and cycles 4/6/7 used, one school per cycle. |
| `Favored Enemy Bonus` / `Favored Terrain Bonus` (Ranger) | 0 (closed cycle 5) | CLOSED. |
| `Bardic Performance` (Bard) | 0 (closed cycle 5) | CLOSED. |
| `Weapon Training` remainder (Fighter) | 48 | Unstarted this mechanism-cycle (cycle 5 closed 4 of 52 canonical groups). |
| `New Arcana` (Sorcerer) | 0 | Ruled out cycle 4: a genuine free chooser, no canonical value to credit. |
| Small/long-tail groups | ~85 | Not re-enumerated this cycle; inherited from cycle 7's own partition, re-derivation deferred to whichever cycle next works this mechanism. |

**This receipt does not re-derive the full 256-unit partition fresh** (unlike cycle 7's own
practice) -- this cycle's own scope was narrowly "build the cheapest remaining wizard school
end-to-end," verified against the corpus and the engine directly, and the closing count (256)
is independently confirmed by the row-count command above. The next cycle on this mechanism
should re-run cycle 7's own partition query fresh before picking its own next lever, per
`decisions.md §12` L2 ("never carry your own number forward -- re-derive it").

`decisions.md §16` ("only the count grounds") was checked against Universal specifically: both
closed units are flat, non-choice per-power formulas (a duration/uses-per-day magnitude and a
uses-per-day magnitude), not a "pick N from an eligible set" shape -- §16 does not apply.

## Notes

- **This cycle's fix is deliberately minimal and additive**: one new constant, one new gate
  function (mirroring the existing specialist gates' shape but with the opposed-schools
  precondition inverted to match PF1's own universalist rule), one new computation block, zero
  changes to any anti-fabrication/description-quality/collision gate. Every existing test that
  exercised the specialist gates (Evocation/Abjuration/Transmutation/Conjuration) stayed green
  unmodified.
- **The universalist arm is a genuinely different choice shape from every specialist arm this
  mechanism has grounded so far** (zero opposed selections vs. exactly two) -- this was
  verified by reading the corpus record's raw tokens BEFORE writing the gate function, not
  assumed from the specialist precedent, avoiding the kind of probe-defect two-round history
  cycle 6 hit when it initially left an incompatible default choice unswapped.

## Next-cycle plan

1. Pick the next-cheapest remaining wizard school (Divination has 3 power records, same
   shape count as Enchantment/Illusion/Necromancy) and build it end-to-end the same way, OR
   take the `Weapon Training` remainder (48 units, a real per-group arithmetic lift already
   scoped by cycle 5's own next-cycle plan).
2. Re-derive this mechanism's full remainder partition fresh before picking (`decisions.md
   §12` L2) -- this receipt's own remainder table above is inherited, not fresh, and states so.
3. `Domain Power` (56) and `Domain Base` (33) remain the two largest named sub-causes,
   unstarted by this specific lever; a future cycle should investigate whether either has a
   comparably cheap real-formula path.
