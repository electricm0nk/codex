# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-003 (bucket `M` — `skill_content` sub-cause)

- **Commit SHA:** this receipt is committed in the same commit as the code/docs
  it describes; `git log -1` at the time of reading resolves it. Started from
  `3cc878de05` (`origin/tranche/14` at rebase time).
- **Files touched:**
  - `src/rules_core/skill_allocation.rs` — new `pub fn
    skill_bonus_is_grounded_for_display_name(display_name: &str) -> Option<i8>`,
    the classifier-facing entry point promised (but not wired) by the prior,
    uncommitted-receipt `c5c4a1b788` cycle's own doc comment on
    `class_skill_bonus_is_grounded`. Normalizes a corpus skill record's own
    display name the same way `class_skill_set`'s `TYPE=` expansion does, then
    checks it against every class this module recognizes at all (Fighter,
    Rogue, Wizard, in that fixed order), returning the first real,
    fixture-executed `class_skill_bonus` hit or `None`. New test
    `skill_bonus_is_grounded_for_display_name_normalizes_and_checks_every_recognized_class`
    in `at_34_e3_003_full_class_skill_list_tests`.
  - `src/bin/v06_work_inventory.rs` — `simple_kind_verdict` (the shared
    classifier nine `Kind` arms route through: `Skill`, `Template`, `Domain`,
    `Deity`, `Language`, `Ability`, `Trait`, `RaceTrait` generic ×2) gains a new
    trailing parameter, `grounded_magnitude: Option<i8>`. Structurally, this
    function had **no path to `grounded` at all** before this cycle — every
    held, non-`text_only` record fell through to `ingested-magnitude`
    unconditionally, regardless of whether a real compute path for that
    record's magnitude existed. `grounded_magnitude` is the caller's own,
    already-executed fixture proof; when `Some(bonus)`, the record now
    promotes to `grounded` with evidence
    `"{kind_label}_magnitude_computed_and_verified_by_fixture_execution_flat_{bonus}"`.
    All 8 other call sites pass `None` and are byte-identical to their
    pre-cycle behaviour — this is a pure widening. `Kind::Skill`'s own call
    site is the only one computing a real value:
    `skill_allocation::skill_bonus_is_grounded_for_display_name(&unit.name)`
    when `!text_only`. Two new classify()-level tests:
    `a_fighter_class_skill_bonus_promotes_a_held_skill_record_to_grounded`
    (positive) and `a_skill_no_recognized_class_grounds_stays_ingested_magnitude`
    (negative control — `Perform (Sing)`, a real held record no recognized
    class treats as a class skill, must stay exactly where it was).
  - `docs/work-inventory.json` (regenerated at HEAD, guarded three-pass
    pipeline — `corpus_literal_sweep --json-out` →
    `derived_evaluator_fixture_check --json-out` → `v06_work_inventory` with
    both report env vars set, no `--allow-stamp-loss` needed or used).
  - `scripts/completion_atlas.py` — 10 `BUCKET_DEFINITIONS` `file:line`
    citations re-derived and corrected (this cycle's own insertion shifted
    every one, same hazard the `U`-bucket cycle's receipt names;
    `citation_failures=0` after, verified against real line content, not
    computed by offset alone).
  - `scripts/missing_engine_tables.py` — 2 `ENGINE_SURFACE_CITATIONS`
    `file:line` citations re-derived and corrected for the same reason
    (`citation_failures=0` after).
  - `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
    `.../missing-engine-tables.json` — regenerated by their own `--check`
    runs after the citation fixes above (counts and citations only, no hand
    edits).
  - This receipt, `docs/release/SD-34-book-completion/progress.md`,
    `docs/release/SD-34-book-completion/kanban.md`.

- **Identifier audit result:** OK_NO_BUNDLE_TAGS on this cycle's own diff —
  `git diff --unified=0 3cc878de05 -- src/rules_core/skill_allocation.rs
  src/bin/v06_work_inventory.rs scripts/completion_atlas.py
  scripts/missing_engine_tables.py | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'`
  → zero matches. The **wider**, epic-file-touch-set diff since the tranche
  cut (`git diff --unified=0
  ea2b3396f2fde9223dde93522bd2288b463a21ee...HEAD -- src/rules_core/ src/bin/
  scripts/oracle_harness/ artifacts/epic-3-core-rulebook/`) carries 20 matches
  — all in `src/rules_core/pilot_compute/mod.rs`, all pre-existing legitimate
  citations of real `sd13_*`/`sd25_*` **test file names** from an
  already-landed sibling cycle's doc-comment rewrite (widening the `sd13_*`/
  `sd25_*` explanation-filter allowlists), none introduced by this cycle and
  none inside either file this cycle touched — reported per `decisions.md
  §12` L1 rather than silently narrowed away.
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own diff
  (same command, second pattern) → zero matches. The wider epic-diff carries
  17 matches, all the word "placeholder" used in its real PF1/PCGen domain
  sense (PCGen's own "no selection" CHOOSE-menu placeholder rows —
  `AT-34-E3-001`'s already-landed vacuous-placeholder mechanism), never a code
  stub; verified none are in this cycle's own two touched files.
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "AT-34-E3-003 —
  buckets M, V, D, U, X close" (per-bucket, atlas reporting zero for
  `core_rulebook`, movement in four buckets). This cycle closes part of
  bucket `M`'s `skill_content` sub-cause only; the criterion as a whole
  stays open (`M`'s other sub-causes, and buckets V/D/X, are untouched).

- **Figures + their re-derive commands:**
  - Corpus-wide bucket `M`: **5,114 → 5,038** (−76), denominator 49,438 total units — `python3 scripts/completion_atlas.py --check`.
  - `core_rulebook` bucket `M`: **1,048 → 972** (−76), denominator 6,701 `core_rulebook` units — `python3 scripts/completion_atlas.py --book core_rulebook --check`.
  - `core_rulebook` DONE: **4,254 → 4,330** (+76), denominator 6,701 — `python3 scripts/completion_atlas.py --book core_rulebook --check`; corpus-wide DONE: **24,166 → 24,242** (+76), denominator 49,438 — `python3 scripts/completion_atlas.py --check`.
  - Whole-corpus before/after diff by unit id (script:
    `/tmp/cargo-sd34-at-34-e3-003/diff_inventory.py`, comparing `git show
    3cc878de05:docs/work-inventory.json` against the regenerated
    `docs/work-inventory.json`). Re-derive the before/after unit counts: `python3 -c "import json,subprocess; before=json.loads(subprocess.check_output(['git','show','3cc878de05:docs/work-inventory.json'])); after=json.load(open('docs/work-inventory.json')); print(len(before['units']), len(after['units']))"` → `49438 49438`. **49,438 units before, 49,438 after, 0
    added, 0 removed, exactly 76 changed — all in `core_rulebook`, all
    `kind == "skill"`, all one status transition
    `ingested-magnitude → grounded`, all one evidence transition
    `skill_content_table_holds_record_magnitude_not_yet_computed →
    skill_content_magnitude_computed_and_verified_by_fixture_execution_flat_3`.**
    No other book's `skill` units moved (the fix is corpus-wide by
    construction, but no other book happens to carry a `skill` record whose
    display name both (a) sits in `ingested-magnitude` and (b) is a class
    skill for Fighter, Rogue, or Wizard under this module's own bounded
    posture — denominator: 149 `skill`-kind units exist corpus-wide, 110 of
    them in `core_rulebook`).
  - `core_rulebook` `skill_content` sub-cause of bucket `M`: **95 → 19**
    (−76), denominator 95 (the sub-cause's own starting population, re-derived
    fresh at cycle start via
    `python3 -c "import json,collections; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if x['book']=='core_rulebook' and x['kind']=='skill']; print(collections.Counter(x['status'] for x in u))"`
    — never trusted from the epic-breakdown's or kanban's own inherited
    figures, both of which cited stale numbers this cycle disproved: the
    inherited `epic-breakdown.md` total of "512 M" for the whole criterion
    was already superseded by the kanban's own live re-derive of 1,048 (`python3 scripts/completion_atlas.py --book core_rulebook --check`, `M` row, pre-cycle) before
    this cycle touched anything).

- **Row-count command output:**
  ```
  $ python3 -c "
  import json
  d = json.load(open('docs/work-inventory.json'))
  units = d['units']
  m = [u for u in units if u.get('book')=='core_rulebook' and u.get('status')=='ingested-magnitude']
  print('core_rulebook M count:', len(m))
  import collections
  print(collections.Counter(u.get('evidence') for u in m))
  "
  core_rulebook M count: 972
  Counter({'equipment_table_entry_with_corpus_magnitude': 276,
  'ability_content_table_holds_record_magnitude_not_yet_computed': 217,
  'equipment_own_line_has_no_magnitude_but_closure_wiring_class_does': 147,
  'race_trait_generic_table_holds_record_magnitude_not_yet_computed': 119,
  'template_content_table_holds_record_magnitude_not_yet_computed': 96,
  'in_catalog_with_corpus_magnitude_but_no_observed_consumer': 47,
  'domain_content_table_holds_record_magnitude_not_yet_computed': 34,
  'skill_content_table_holds_record_magnitude_not_yet_computed': 19,
  'spell_list_entry_with_resolved_level': 15,
  'race_trait_states_a_universal_sheet_modifier_pending_compute': 2})
  ```
  Sum 276+217+147+119+96+47+34+19+15+2 = **972**, matching
  `python3 scripts/completion_atlas.py --book core_rulebook --check`'s own
  live `M: 972` exactly.

- **Build scope verified:**
  - `cargo test --locked --lib rules_core::skill_allocation::` — 14/14 pass
    (1 new: `skill_bonus_is_grounded_for_display_name_normalizes_and_checks_every_recognized_class`).
  - `cargo test --locked --bin v06_work_inventory` — **435/435 pass** (2 new:
    `a_fighter_class_skill_bonus_promotes_a_held_skill_record_to_grounded`,
    `a_skill_no_recognized_class_grounds_stays_ingested_magnitude`).
  - `python3 -m unittest scripts.tests.test_completion_atlas` — 38/38 pass.
  - `cargo test --locked --no-run` (full workspace, `--no-run` exit 0),
    run at this cycle's own HEAD (after the last commit that could move a
    figure — `decisions.md §12` L7).
  - `cargo test --locked --lib` (full workspace, run mid-cycle with only this
    cycle's two Rust files — `skill_allocation.rs`,
    `v06_work_inventory.rs` — modified and no JSON/script regen yet; see
    Discoveries): **2,922 passed, 5 failed, 14 ignored.** The 5 failures
    (`rules_core::class_feature_pool_catalog::tests::
    class_feature_owner_matched_non_excluded_remainder_is_24_and_named_by_subcause`,
    three in `rules_core::pilot_compute::formula_interpreter_corpus_wide::tests`,
    `rules_core::rules_tables::companion_chassis::tests::
    grant_token_only_rows_dispatch_to_already_held_content`) are confirmed
    **pre-existing, not caused by this cycle** — `git diff --stat` at the
    time this cycle ran `--lib` showed only `src/bin/v06_work_inventory.rs`
    and `src/rules_core/skill_allocation.rs` touched, neither of which the
    5 failing tests' own modules import or exercise; root cause traced via
    `git log -S"oracle-agree" -- src/bin/v06_work_inventory.rs` to
    `fef202a566` (`AT-34-E3-005`'s own already-landed bucket-V oracle
    disposition cycle), which introduced the `oracle-agree`/
    `oracle-unverifiable` statuses that `scripts/observer/
    pf1e_dashboard_producer.py`'s `_doneness_verdict_uncapped` does not yet
    map for `wiring_class == "derived"` (`ValueError: doneness: unmapped
    'derived' + 'oracle-agree'`) — outside this criterion's own file-touch
    set (`scripts/coverage_ledger.py`/`scripts/observer/
    pf1e_dashboard_producer.py`, not `src/rules_core/`), and a different
    criterion's (`AT-34-E3-005`'s) own defect, not `M`'s. Filed as a retro
    `incident`, named in Notes, not silently fixed here to avoid colliding
    with that criterion's own bookkeeping (same posture the already-merged
    `AT-34-E3-004` cycle took toward an out-of-scope denominator-gate
    finding).
  - `apps/desktop/src-tauri` (separate cargo workspace, `CARGO_TARGET_DIR=
    /tmp/cargo-sd34-at-34-e3-003-desktop`): `cargo test --locked --no-run
    --manifest-path apps/desktop/src-tauri/Cargo.toml` — exit 0. Tested
    explicitly because `src/rules_core/skill_allocation.rs` is lib code the
    desktop crate depends on transitively (`codex = { path = "../../.." }`
    in its `Cargo.toml`); the change is purely additive (one new `pub fn`)
    and this confirms it.

- **Sweep population:** no `data/corpus/**` records added or regenerated —
  only the engine classifier and the derived inventory changed.
  `corpus_literal_sweep`: **48,708 examined of 51,482 read** before this
  cycle's own regen pass (used as its `CORPUS_LITERAL_SWEEP_REPORT` input)
  and **48,708 examined of 51,482 read** in an independent re-run after —
  0 delta, correctly matching 0 corpus records added
  (`decisions.md §12` L8).

- **Oracle pin:** `PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6`
  (`scripts/pcgen-oracle-pin.env`) — the corpus enumeration half of this
  cycle's regen ran against this pinned checkout at
  `/home/ubuntu/workspace/repos/pcgen` (confirmed at-pin, clean, before use).

- **Status:** partial

- **Movement, four buckets:** closure (the `skill_content` units genuinely
  newly promoted to `grounded` this cycle — a real, fixture-executed compute
  path now backs their magnitude, not a reclassification of the same
  evidence); reclassification 0; reachability 0; instrument-correction 0.

- **Notes:**
  - **Inherited figures corrected, not trusted.** The dispatch brief's
    inherited sub-cause figures (`equipment_table_entry_with_corpus_magnitude`
    ~276, `ability_content...` ~217, `equipment_own_line...` ~147,
    `race_trait_generic...` ~119, `template_content...` ~96) were re-derived
    fresh at cycle start against `docs/work-inventory.json` at this cycle's
    own rebase point and matched exactly — but the brief did not name
    `skill_content` (95), `in_catalog_with_corpus_magnitude_but_no_observed_consumer`
    (47), `domain_content...` (34), `spell_list_entry...` (15), or
    `race_trait_states_a_universal_sheet_modifier_pending_compute` (2), all
    of which this cycle's own full re-derive surfaced and which sum with the
    named five to the brief's own stated 1,048 total exactly
    (276+217+147+119+96+95+47+34+15+2 = 1,048).
  - **A prior, uncommitted-receipt cycle (`c5c4a1b788`, "AT-34-E3-003
    bucket-M skill widening") had already built the Rust engine half of this
    fix** (widening `skill_allocation.rs`'s `class_skill_set` from a
    3-skill/3-class bounded slice to Fighter's and Wizard's real, full
    class-skill lists) but explicitly deferred "the classifier's own
    grounding check" as "the next cycle's work — not yet done." That commit's
    own doc comment on `class_skill_bonus_is_grounded` named the exact
    missing piece this cycle builds
    (`skill_allocation::skill_bonus_is_grounded_for_display_name`, consumed
    by `v06_work_inventory.rs`'s `Kind::Skill` arm). No prior progress.md/
    kanban.md entry existed for that commit — it was real, tested (13/13),
    committed engine work that never moved the atlas and was never reported;
    this cycle is the one that actually closes the units it made possible.
  - **The fix is generic by construction, corpus-wide, not `core_rulebook`-
    scoped** — `simple_kind_verdict`'s new `grounded_magnitude` parameter
    and `Kind::Skill`'s probe call apply identically to every book's `skill`
    records. The whole-corpus diff proves this (0 hand-scoping): the reason
    only `core_rulebook` moved is that no other book happens to carry a
    `skill`-kind record matching Fighter/Rogue/Wizard's recognized lists
    while sitting in `ingested-magnitude` — a fact about the corpus, not a
    book-filter in the code.
  - **`simple_kind_verdict` had no path to `grounded` at all before this
    cycle** — a real, corpus-wide structural gap this cycle closes generically
    for `Kind::Skill` and leaves open (by design, `None` passed) for
    `Template`/`Domain`/`Deity`/`Language`/`Ability`/`Trait`/`RaceTrait`
    generic, each of which would need its own real compute-and-verify probe
    (analogous to `skill_bonus_is_grounded_for_display_name`) before it could
    use the same new parameter — this is the shape the next cycles on `M`'s
    `ability_content` (217) and `template_content` (96) sub-causes would
    follow, since both already route through this same shared function.
  - **Out-of-scope discovery, not fixed here:** `cargo test --locked --lib`
    surfaced 5 failing tests (named above) rooted in `AT-34-E3-005`'s own
    already-landed `oracle-agree`/`oracle-unverifiable` status introduction
    tripping an unmapped `(wiring_class, status)` pair in
    `scripts/observer/pf1e_dashboard_producer.py`'s `_doneness_verdict_uncapped`.
    This is a genuine NEW-since-the-tranche-cut regression (confirmed via
    `git log -S"oracle-agree" -- src/bin/v06_work_inventory.rs` → introduced
    by `fef202a566`, landed 2026-08-30, after the workflow-instruction.md §1
    item 10 baseline was measured 2026-08-26/27) but belongs to a different
    criterion's file-touch set and a different criterion's own bookkeeping.
    Retro `incident` filed: `1788095140139-sd34-at-34-e3-003-1a6793`
    (`docs/retro/events/sd34-at-34-e3-003.jsonl`, recurrence key
    `doneness-verdict-unmapped-status-pair`).
    Flagged here for the closure scan / next `AT-34-E3-005` cycle to fix, not
    self-healed inline (would collide with that criterion's own receipts).
  - Denominator gate against this package:
    `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'`
    → `files_checked=15 violations=6`, all 6 pre-existing (`FRT_HVY`'s quoted
    corpus prose, already flagged by the already-merged `AT-34-E3-004` cycle);
    unchanged before/after this cycle's own edits, none introduced by this
    cycle's own new prose.

- **Remainder — every unit in `AT-34-E3-003`'s five buckets, named, at HEAD:**

  | Bucket | `core_rulebook` population (live) | Clearing mechanism | This cycle |
  |---|---:|---|---|
  | `M` | 972 | running the compute path (per sub-cause below) | closed 76 of 1,048 (`skill_content` sub-cause) |
  | `V` | 81 | the SD-33 oracle harness | untouched |
  | `D` | 366 | per named sub-cause (not yet enumerated by this cycle) | untouched |
  | `U` | 10 | operator ruling on the `%CHOICE`/`%d<N>` shape | untouched (still awaiting the ruling `AT-34-E3-003`'s prior cycle flagged) |
  | `X` | 115 | `decisions.md §17`'s new choice-filter mechanism (not yet built) | untouched |

  Sum: 972 + 81 + 366 + 10 + 115 = **1,544**, matching
  `python3 scripts/completion_atlas.py --book core_rulebook --check`'s own
  live sum of those five buckets. (`D`'s live population, 366, is itself a
  correction of the prior cycle's own receipt, which cited 382 — re-derived
  fresh at this cycle's own start, before any edit, and confirmed unmoved by
  this cycle's own diff: an intervening, already-landed cycle between that
  receipt and this one's start moved `D` from 382 to 366 by some other
  mechanism this cycle did not investigate, since `D` is out of this cycle's
  own chosen scope. Retro `correction` filed:
  `1788095131228-sd34-at-34-e3-003-9daa73`
  (`docs/retro/events/sd34-at-34-e3-003.jsonl`).)

  **`M`'s own 972, named by sub-cause (every remaining unit, populations
  summing exactly to 972):**

  | Sub-cause (evidence string) | Population | Clearing mechanism |
  |---|---:|---|
  | `equipment_table_entry_with_corpus_magnitude` | 276 | running the equipment compute path (shape engine) |
  | `ability_content_table_holds_record_magnitude_not_yet_computed` | 217 | a real compute-and-verify probe for `Kind::Ability`, same shape as this cycle's own `skill_bonus_is_grounded_for_display_name`, wired into `simple_kind_verdict`'s existing `grounded_magnitude` parameter |
  | `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` | 147 | resolving the token-closure magnitude to the record it belongs to (equipment `.COPY=`/`.MOD` chain) |
  | `race_trait_generic_table_holds_record_magnitude_not_yet_computed` | 119 | a real compute-and-verify probe for `Kind::RaceTrait`'s generic table, same shared-parameter shape |
  | `template_content_table_holds_record_magnitude_not_yet_computed` | 96 | a real compute-and-verify probe for `Kind::Template`, same shared-parameter shape |
  | `in_catalog_with_corpus_magnitude_but_no_observed_consumer` | 47 | wiring an observed consumer for the catalog entry |
  | `domain_content_table_holds_record_magnitude_not_yet_computed` | 34 | a real compute-and-verify probe for `Kind::Domain`, same shared-parameter shape |
  | `skill_content_table_holds_record_magnitude_not_yet_computed` | 19 | widening `skill_allocation.rs`'s recognized-class posture beyond Fighter/Rogue/Wizard (these 19 are class skills only for classes this module has no grounded data for — Bard's `Perform` family and similar) |
  | `spell_list_entry_with_resolved_level` | 15 | running the spell compute path |
  | `race_trait_states_a_universal_sheet_modifier_pending_compute` | 2 | running the universal-sheet-modifier compute path |

  Sum: 276+217+147+119+96+47+34+19+15+2 = **972**, matching
  `completion_atlas.py --book core_rulebook --check`'s own live `M` count
  exactly.

- **Next-cycle plan:**
  1. **`ability_content` (217) is the largest remaining `M` sub-cause and the
     largest tractable next step** — it already routes through the same
     `simple_kind_verdict`/`grounded_magnitude` mechanism this cycle wired,
     so closing it is "build one real compute-and-verify probe for
     `Kind::Ability`, pass its result into the existing parameter," not new
     plumbing. Per `decisions.md §12` L11, its own per-unit cost has not been
     measured — sample a handful of `ability_content` records before
     committing to the full 217.
  2. **`equipment_table_entry_with_corpus_magnitude` (276) and
     `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does`
     (147) together are the single largest population (423)** but do NOT
     route through `simple_kind_verdict` — `Kind::Equipment`'s own inline
     arm would need an equivalent `grounded_magnitude`-shaped rung. Real
     engineering, not yet scoped or costed this cycle.
  3. **`race_trait_generic` (119), `template_content` (96), `domain_content`
     (34) are all `simple_kind_verdict` callers already** and are the next
     cheapest wins after `ability_content`, each needing its own real probe.
  4. **`skill_content`'s own remaining 19** need either an operator ruling to
     widen `skill_allocation.rs`'s recognized-class posture past Fighter/
     Rogue/Wizard (a real, cited, per-class widening — same shape as this
     cycle's own Fighter/Wizard full-list work), or confirmation that they
     stay `ingested-magnitude` until a class this module can prove reaches
     `Computed` for real characters is added.
  5. **Buckets `V` (81), `D` (366), `U` (10), `X` (115) are entirely
     untouched by this cycle** and remain exactly as the prior `AT-34-E3-003`
     cycle's own receipt left them (`D`'s live count corrected to 366, see
     Notes).
  6. **Out-of-scope discovery to hand off:** the `oracle-agree`/`derived`
     doneness-mapping crash (5 failing lib tests) belongs to `AT-34-E3-005`'s
     own file-touch set and should be fixed there or at the Epic 6 closure
     scan, whichever comes first.
