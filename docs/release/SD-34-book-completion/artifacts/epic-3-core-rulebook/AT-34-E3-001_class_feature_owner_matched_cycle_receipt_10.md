# Cycle 10 — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 — bucket-B batch cycle (all three
`decisions.md §14` mechanisms in one build)

This cycle is dispatched as a SINGLE batch across all three of `AT-34-E3-001`'s remaining
`core_rulebook` mechanisms — `class_feature_owner_matched_by_name_but_record_not_held_by_engine`
(~242, mechanism 1), `class_feature_option_pool_record_with_magnitude_not_held_by_engine` (~208,
mechanism 2), `class_feature_option_pool_record_not_held_by_engine` (~34→25, mechanism 3) — per
the operator's own explicit "make every edit, compile once, test once" cost argument (543
integration test targets, ~38GB cold build). Real, verified code movement happened only on
mechanism 1 this cycle; mechanisms 2 and 3 are re-derived and reported with no code change (see
their own sections below) — this receipt is filed under the owner_matched numbering (cycle 10)
because that mechanism is where the movement is, but covers the whole batch's own investigation.

- **Commit SHA:** filled in after push (see `git log -1` at push time).
- **Files touched:**
  - `src/rules_core/pilot_compute/class_feature_grant_consumer.rs` — removed
    `LEVEL_UP_PILLAR_FILTERED_CLASSES` (the class-wide Druid/Monk exclusion) entirely; the
    citation-based property (`decisions.md §18`) is now the only gate for every class this
    module serves, no exceptions. Updated/replaced 3 tests, extended
    `previously_gated_classes_now_emit_citation_backed_explanations_by_construction` to include
    Monk, corrected the pinned `the_live_scale_of_this_waves_widening_is_measured_and_pinned`
    census from `(136, 26, 6, 43, 1)` to `(136, 32, 0, 43, 1)`.
  - `src/rules_core/class_feature_pool_catalog.rs` — corrected a STALE pinned assertion
    (`class_feature_owner_matched_non_excluded_remainder_is_24_and_named_by_subcause`'s own
    `excluded, 218` pin) to `215`, the number matching the CURRENT `docs/work-inventory.json`
    (regenerated by a prior wave-9 cycle after this test was last written; see "Instrument
    correction found" below). This test read `docs/work-inventory.json` directly and was never
    re-checked against the regeneration.
  - `tests/sd25_druid_level_up_explanation_filter_audit.rs` — added
    `NEW_CORPUS_RECORD_GRANT_FACT_IDS` (3 ids: `nature_bond`, `orisons`, `spontaneous_casting`)
    to the flat/never-diffing exclusion set, with a new constant-value proof test.
  - `tests/sd25_monk_level_up_explanation_filter_audit.rs` — added
    `NEW_LEVEL_ONE_CORPUS_RECORD_GRANT_FACT_IDS` (3 ids: `flurry_of_blows`, `stunning_fist`,
    `unarmed_strike`) to the flat/never-diffing exclusion set, with a new constant-value proof
    test.
  - `tests/sd13_bard_level1_spell_baseline.rs`, `tests/sd13_bard_level6_progression.rs`,
    `tests/sd13_bard_level7_progression.rs`, `tests/sd13_bard_level8_progression.rs`,
    `tests/sd13_bard_level9_progression.rs`, `tests/sd13_sorcerer_level1_spell_baseline.rs`,
    `tests/sd13_sorcerer_level9_progression.rs`, `tests/sd13_sorcerer_level10_progression.rs` —
    7 PRE-EXISTING stale anti-fabrication gates found and fixed this cycle (see "Discoveries
    beyond this cycle's own scope" below) — none caused by this cycle's own code change; all
    found only because this cycle ran the full `sd13_*`/`sd25_*` set together for what appears
    to be the first time.
  - `docs/release/SD-34-book-completion/artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_10.md`
    (this file), `docs/release/SD-34-book-completion/progress.md`,
    `docs/release/SD-34-book-completion/kanban.md`.
  - `docs/work-inventory.json` — **NOT regenerated this cycle**, per this cycle's own
    instruction ("a separate regeneration cycle measures what you moved"). All figures below for
    the code change's effect are EXPECTATIONS, re-derivable by the command shown, not measured
    row counts from a regenerated inventory.

- **Identifier audit result:** `git diff --unified=0 -- <all files above except the two progress
  docs> | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → non-empty, but every
  match is a legitimate prose citation of a real, pre-existing test/mechanism name in a doc
  comment (the same style every prior cycle in this mechanism's own history uses) — **OK_NO_BUNDLE_TAGS**
  in the sense the check exists for (no smuggled internal-tracking tag in a shipped identifier).
- **Wired-integration audit result:** same diff, `grep -nE
  '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'` → zero matches,
  **OK_NO_TOKENS**.

## Mechanism 1 — `class_feature_owner_matched_by_name_but_record_not_held_by_engine`

### Re-derived population (do not carry forward a prior number without checking)

```bash
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_owner_matched_by_name_but_record_not_held_by_engine']
print(len(u))
"
```
→ **239** at this cycle's start (not 242 — a prior wave-9 shared regeneration cycle already
moved 3 units off this evidence string; cycle 9's own receipt, written before that regeneration
ran, still says 242). Per-class split, re-derived directly against the live corpus (same method
cycle 7/8/9 used): Sorcerer 137, Cleric 38, Monk 25, Wizard 5, Paladin 5, Bard 4, Druid 1 — **215
excluded** (the original seven-class definition), **24 non-excluded**, both unchanged from
cycle 9's own characterization (18 null-description + 6 real-description-but-gated by
`class_feature_pool_catalog`'s own safety gates — a DIFFERENT catalog/gate than this mechanism's
own `class_feature_grant_consumer.rs`).

### The fix this cycle makes — same citation property, one more class-wide exception removed

This cycle's dispatch brief named Monk (25) + Druid (1) as "stay blocked behind
`is_druid_pillar_id`/`is_monk_pillar_id`" and asked for that filter to be widened by the same
citation property. **Direct re-reading of both functions' live source (not the inherited claim)
found they are ALREADY wide enough**: `is_druid_pillar_id`
(`src/rules_core/level_up/druid.rs`) already matches `class_chassis.druid.`/`class_feature.druid.`/
`class_spell.druid.`; `is_monk_pillar_id` (`src/rules_core/level_up/monk.rs`) already matches
`class_chassis.monk.`/`class_feature.monk.` — both already admit this module's own
`class_feature.<class>.corpus_record.*` id shape by prefix. Separately, **`v06_work_inventory.rs`'s
own `classify()` never reads `LevelUpPlan`/`level_up::` at all** (confirmed: `grep -c
"level_up::" src/bin/v06_work_inventory.rs` → 0) — it drives `compute_pilot_base_chassis`
directly, so whether an explanation ever reaches a `LevelUpPlan` screen has never been the gate
on this mechanism's own `docs/work-inventory.json` verdict. The cited "SEPARATE, THIRD reason"
for excluding Druid/Monk (`class_feature_grant_consumer.rs`'s own inherited doc comment) was
STALE — an earlier investigation's finding that was never re-checked after `is_druid_pillar_id`
was independently widened (v0.6 alpha swarm, 2026-07-25, predates this whole SD-34 bundle) to add
the `class_spell.druid.` prefix.

**The actual, only remaining blocker was `LEVEL_UP_PILLAR_FILTERED_CLASSES`
(`class_feature_grant_consumer.rs`'s own class-wide early-return for Druid/Monk) — removed this
cycle.** No change to `src/rules_core/level_up/{druid,monk}.rs` was needed or made (both files
untouched, confirmed by `git diff --stat` showing zero lines in either).

### What actually got unblocked — re-derived by direct production-pipeline probe, not assumed

A throwaway integration test (not committed) drove `compute_pilot_base_chassis` directly for a
level-1..20 Human Druid and Human Monk and collected every `class_feature.<class>.corpus_record.*`
id that survives production's own collision guard (the real pipeline, not an isolated call):

```
DRUID (all levels 1..=20): nature_bond, orisons, spontaneous_casting
MONK (all levels 1..=20): abundant_step, diamond_soul, flurry_of_blows, maneuver_training,
                          perfect_self, quivering_palm, stunning_fist, unarmed_strike
```

Cross-referencing each against LIVE `docs/work-inventory.json` (`python3 -c "..."`, filtering
`book=='core_rulebook'` and `corpus_key` in this list):

| Corpus key | Current status/evidence | Expected after this fix |
|---|---|---|
| `Monk ~ Flurry of Blows` | `engine-does-not-hold` / `class_feature_owner_matched_by_name_but_record_not_held_by_engine` | **grounded** — closes THIS mechanism's own population (239 → 237 expected) |
| `Monk ~ Unarmed Strike` | `engine-does-not-hold` / `class_feature_owner_matched_by_name_but_record_not_held_by_engine` | **grounded** — closes THIS mechanism's own population |
| `Monk ~ Abundant Step` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — a DIFFERENT mechanism's own bucket-B population, not this one |
| `Monk ~ Diamond Soul` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — different mechanism |
| `Monk ~ Maneuver Training` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — different mechanism |
| `Monk ~ Perfect Self` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — different mechanism |
| `Monk ~ Stunning Fist` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — different mechanism |
| `Monk ~ Quivering Palm` | already `grounded` (different evidence) | unchanged status; evidence string may churn |
| `Druid ~ Nature Bond` | `engine-does-not-hold` / `no_explanation_id_and_no_diagnostic_names_this_feature` | grounded — different mechanism |
| `Druid ~ Orisons` | already `text-complete` (DONE, different rendering path) | unchanged |
| `Druid ~ Spontaneous Casting` | already `text-complete` (DONE) | unchanged |

**Expected THIS mechanism's own movement: 2 closures** (`Monk ~ Flurry of Blows`,
`Monk ~ Unarmed Strike`, both real, citation-backed, non-`%`-leaking descriptions, neither
colliding with `monk.rs`'s own hand-wired `class_chassis.monk.flurry_of_blows_attack_bonus/
_attack_count`/`unarmed_strike_damage_die(_count)` ids — the collision guard compares the id's
TRAILING dot-segment only, and none of those four segments equals this module's own
`flurry_of_blows`/`unarmed_strike` slugs). **Expected BONUS movement outside this mechanism's own
scope: 6 more units** (5 Monk + 1 Druid) currently sitting under the DIFFERENT
`no_explanation_id_and_no_diagnostic_names_this_feature` evidence string in a different
mechanism's own bucket-B population — this cycle's fix moves them too, as a side effect, since
`classify()`'s generic matcher recognizes the `class_feature.<owner>.corpus_record.<slug>` id
shape regardless of which named mechanism originally bucketed the corpus key.

The remaining 23 Monk + 0 further Druid corpus-record-shaped units in the owner_matched-24
non-excluded population are UNCHANGED (18 null-description — `atlas-defects.md`'s own open
definitional question, left in bucket B per this cycle's explicit instruction; 6 real-description
but refused by `class_feature_pool_catalog`'s own render-and-refuse/engine-effect-token gates —
unaffected by anything this cycle touched, a DIFFERENT file/gate).

### Instrument correction found (unrelated to this cycle's own code change)

`class_feature_pool_catalog.rs`'s own
`class_feature_owner_matched_non_excluded_remainder_is_24_and_named_by_subcause` test reads
`docs/work-inventory.json` directly and pins `excluded, 218`. This FAILED at this cycle's first
full-lib run — **not because of anything this cycle changed** (this test's own file was never
touched by this cycle's code edit) but because a PRIOR wave-9 shared regeneration cycle already
moved 3 excluded-class units off this evidence string (2 to `text-complete`, at least 1 further
reclassified), and this test's own pin was never re-checked against that regeneration. Re-derived
directly: **215**, not 218 (Sorcerer 137, Cleric 38 [was 39], Monk 25, Wizard 5 [was 7],
Paladin 5, Bard 4, Druid 1 — Monk and Druid BYTE-FOR-BYTE unchanged, matching wave-9's own "0
Druid/Monk movement confirmed" finding). Corrected in this cycle's own diff (see Files touched).

## Mechanism 2 — `class_feature_option_pool_record_with_magnitude_not_held_by_engine`

### Re-derived population

```
$ python3 -c "... evidence=='class_feature_option_pool_record_with_magnitude_not_held_by_engine' ..."
208
```
Matches cycle 9's own expected 256→208 (48 closed via Weapon Training's 14-group generalization),
confirmed by the already-landed wave-9 shared regeneration.

### No code change this cycle — findings only

Cycle 9's own named remainder (Domain Power 56, Domain Base 33, remaining wizard-school clusters
[Divination/Enchantment/Illusion/Necromancy] ~34, small/long-tail groups ~85) was re-checked, not
carried forward blindly:

- **`pilot_compute.rs` has NO existing formula for Divination, Enchantment, Illusion, or
  Necromancy school powers** (`grep -c "class_feature.school.divination" src/rules_core/
  pilot_compute/mod.rs` → 0; same for the other three) — unlike Universal (cycle 8), whose
  `hand_of_the_apprentice_uses_per_day`/`metamagic_mastery_uses_per_day` formulas ALREADY existed
  in `pilot_compute.rs` before that cycle merely added the `v06_work_inventory.rs` probe branch
  recognizing them. Grounding a NEW school from scratch is real per-power PF1-rules engineering
  (verified against two primary sources, the same rigor every prior school cycle in this
  mechanism's own history applied), not a probe-only change — genuinely different, larger scope
  than this cycle's batch could safely fit alongside mechanism 1's own investigation and the 8
  pre-existing test regressions this cycle also had to fix (see Discoveries) without risking the
  exact kind of rushed, under-verified magnitude fabrication this bundle's own gates exist to
  catch.
- Domain Power (56)/Domain Base (33) were already investigated and left named by cycle 5/9's own
  receipts; this cycle found no new information changing that characterization.

**This cycle makes zero code changes to this mechanism.** Its 208-unit population, and cycle 9's
own remainder table, stand unmodified.

## Mechanism 3 — `class_feature_option_pool_record_not_held_by_engine`

### Re-derived population

```
$ python3 -c "... evidence=='class_feature_option_pool_record_not_held_by_engine' ..."
25
```
Matches cycle 9's own expected 34→25 (9 closed via the wizard-opposition-school-spell-list join),
confirmed by the already-landed wave-9 shared regeneration (which also found and repaired a
rebase regression that had silently deleted this exact fix — see that regeneration's own
receipt).

### No code change this cycle — plainly, this is not narrow work

Cycle 9's own remainder table names 25 units across 4 real, unbuilt subsystems:
proficiency/mechanical-grant possession-tracking (20 units, needing a generic multi-class
`GrantedFact` possession ledger), companion/mount registration (3 units, gated on a real Paladin
Special Mount computation that does not exist), and 2 units already owned by mechanism 2's own
Domain Power sub-cause. **None of these is a narrow catalog-widening or `classify()`-gate fix —
each needs a genuinely new engine subsystem** (a per-character possession ledger distinguishing
which of several classes' indirection targets a character actually holds; a Special Mount
chassis computation plus a `choice:special_mount` registration). Building either properly, with
the primary-source verification and TDD rigor this codebase's own established pattern requires,
is real, multi-day engineering — attempting either as an add-on to this cycle's own batch (which
already required fixing 8 pre-existing test regressions to keep the full suite green) would risk
exactly the rushed, under-verified magnitude fabrication this bundle's own gates exist to
prevent. **Stated plainly, per this cycle's own instruction: this is not narrow work.**

**This cycle makes zero code changes to this mechanism.** Its 25-unit population, and cycle 9's
own remainder table, stand unmodified.

## Discoveries beyond this cycle's own three mechanisms — 8 pre-existing stale anti-fabrication
gates, found and fixed, none caused by this cycle's own code

This cycle ran the FULL `sd13_*`/`sd25_*` set (184 test binaries) together for what appears to be
the first time in this bundle's own history (each prior cycle ran only its own scoped subset, per
`workflow-instruction.md §2.5`'s own scoping rule — a reasonable per-cycle choice that left this
gap unfound across ~35 cycles). 8 tests failed; **every one was independently confirmed, by code
inspection, to be pre-existing and unrelated to this cycle's own `class_feature_grant_consumer.rs`
edit** (none of the 8 touches Druid or Monk's own class-wide gate at all):

1. **`sd13_bard_level1_spell_baseline.rs`** (`bard_level1_fabricates_no_spell_or_class_feature_math`)
   — missed the SAME `class_feature.bard.corpus_record.` prefix carve-out cycle 8's own `§18`
   widening already applied to the 5 sibling `sd13_bard_level4..8_progression.rs` files; this ONE
   file (a different shape, level-1-specific) was overlooked.
2-4. **`sd13_bard_level{6,7,8}_progression.rs`** — a genuinely EARLIER regression (`AT-34-E3-001`
   mechanism 2 cycle 5) legitimately grounded Suggestion's flat Will-save DC
   (`class_feature.bard.suggestion_dc`, level 6+) against a real, cited corpus formula, but never
   added it to these 3 files' own closed-namespace `known_bard_ids` exhaustive lists.
5. **`sd13_bard_level9_progression.rs`** (`bard_level9_does_not_fabricate_inspire_greatness`) —
   the SAME mechanism-2 cycle 5 legitimately grounded Inspire Greatness's flat affected-ally
   count (`class_feature.bard.inspire_greatness_allies`, level 9+), never reflected in this
   test's own negative control.
6-7. **`sd13_sorcerer_level{9,10}_progression.rs`** (`sorcerer_level{9,10}_does_not_fabricate_
   the_ninth_level_bloodline_entries`) — a PRE-EXISTING, SD-32-era generic bloodline-power pass
   (`push_generic_pool_group_selection_magnitude`, `decisions.md §17`) legitimately emits real,
   citation-backed `class_feature.sorcerer.bloodline.generic.<bloodline>.<power>.<var>` ids for
   the fixture's selected Arcane bloodline; several real PCGen var names (e.g.
   `SorcererBloodlineFeatImprovedCounterspell`) contain "new_arcana"/"bloodline_power"-shaped
   substrings these tests' own negative controls were written before that generic pass existed.
8. **`sd13_sorcerer_level1_spell_baseline.rs`** (`sorcerer_level1_fabricates_no_spell_math`) —
   the SAME generic bloodline pass; a real var name containing "spell" as a substring
   (`...ImprovedCounterspell`) tripped this file's own closed exhaustive check.

**Every fix is an ADDITIVE, scoped prefix/id carve-out for a REAL, already-verified, citation-
backed record — none weakens any gate for anything outside that one namespace, and none was
found to actually be a fabrication** (each was independently traced to its own production code's
doc comment citing a real corpus record and formula, and in three cases to an already-passing
dedicated regression test proving the SAME mechanism correct in isolation:
`pool_group_closure_census_across_all_six_pools`,
`every_real_druid_explanation_id_survives_...`,
`every_real_monk_explanation_id_survives_...`). Full re-run of all 184 `sd13_*`/`sd25_*` targets
after every fix: **0 failed** (see Build scope verified).

## Row count command output (this cycle's own artifacts)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
for ev in ['class_feature_owner_matched_by_name_but_record_not_held_by_engine',
           'class_feature_option_pool_record_with_magnitude_not_held_by_engine',
           'class_feature_option_pool_record_not_held_by_engine']:
    u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold' and x['evidence']==ev]
    print(ev, len(u))
"
class_feature_owner_matched_by_name_but_record_not_held_by_engine 239
class_feature_option_pool_record_with_magnitude_not_held_by_engine 208
class_feature_option_pool_record_not_held_by_engine 25
```
These are the PRE-this-cycle's-own-code-effect counts (`docs/work-inventory.json` not
regenerated this cycle, per instruction). Mechanism 1's expected count after regeneration: 237
(239 - 2, this mechanism's own 2 closures only — the other 6 units this cycle's fix also moves
belong to a different mechanism's own evidence string and do not subtract from THIS count).

## Build scope verified

- `cargo test --locked --no-run` (full workspace): **exit 0**.
- `cargo test --locked --no-run --manifest-path apps/desktop/src-tauri/Cargo.toml`: **exit 0**.
- `cargo test --locked --lib` (full): **2917 passed; 0 failed; 14 ignored**.
- `cargo test --locked --bin v06_work_inventory` (full): **419 passed; 0 failed**.
- All 184 `sd13_*`/`sd25_*` integration test targets, run together in full (`--no-fail-fast`,
  every target): **0 failed** after this cycle's fixes (8 targets initially failed, all fixed and
  reconfirmed green — see Discoveries).
- All run against `CARGO_TARGET_DIR=/tmp/cargo-sd34-batch`, this cycle's own single, isolated
  target directory, reused across every iteration (one cold build, warm thereafter).

## Sweep population

N/A — this cycle added or regenerated zero corpus records (`data/corpus/**` untouched) and did
not regenerate `docs/work-inventory.json` (per this cycle's own instruction). `corpus_literal_
sweep`'s examined-population is unchanged from the pre-cycle baseline.

## Oracle pin

N/A — no figure in this receipt is derived from the pinned PCGen oracle corpus.

## Movement, four buckets

- **Closure (expected, mechanism 1 only):** 2 — `Monk ~ Flurry of Blows`, `Monk ~ Unarmed
  Strike`, bucket B → grounded, real citation-backed roster facts, confirmed non-colliding with
  `monk.rs`'s own hand-wired chassis explanations.
- **Reclassification:** 0 expected within mechanism 1's own evidence string (the 6 bonus units
  that move belong to a DIFFERENT mechanism's evidence string, not a reclassification within this
  one).
- **Reachability:** 0 — none of the 2 (or 8) newly-emitted ids reaches a `LevelUpPlan` player
  screen differently than before; this cycle closes only the "does the engine hold a real fact"
  question (`docs/work-inventory.json`'s own bucket-B question), the identical scope every prior
  cycle in this mechanism used.
- **Instrument-correction:** 2 — (1) `class_feature_pool_catalog.rs`'s own stale `excluded, 218`
  pin corrected to 215 (pre-existing, found this cycle, unrelated to this cycle's own code
  change); (2) `class_feature_grant_consumer.rs`'s own pinned census corrected from `(136, 26, 6,
  43, 1)` to `(136, 32, 0, 43, 1)` (a direct, mechanical consequence of removing
  `LEVEL_UP_PILLAR_FILTERED_CLASSES` this cycle — the 6 previously-excluded-but-resolvable
  records, all Monk, now correctly count as `newly_resolved`).

## Remainder — every unit named by sub-cause, mechanism by mechanism

### Mechanism 1 (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`), 239 → 237 expected

| Sub-cause | Population | Disposition |
|---|---:|---|
| Excluded-class (Sorcerer/Cleric/Monk/Wizard/Paladin/Bard/Druid), citation-gate admits when a real, unresolved-formula-free description exists | 215 | Sibling scope (`decisions.md §18`'s own widening lane) — 2 of Monk's own 25 close this cycle (Flurry of Blows, Unarmed Strike); the remaining 213 stand per that lane's own receipts. |
| Non-excluded, `description: null` (zero-description internal bookkeeping) | 18 | Left in bucket B — `atlas-defects.md`'s own OPEN definitional question, not this cycle's to decide. |
| Non-excluded, real description but refused by `class_feature_pool_catalog`'s own render-and-refuse/engine-effect-token gates | 6 | Left in bucket B — needs real per-character grant/formula wiring (a different, larger engineering task; see cycle 9's own receipt for the per-record breakdown). |

### Mechanism 2 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine`), 208 unchanged

Unchanged from cycle 9's own remainder table: Domain Power (Cleric) 56, Domain Base (Cleric) 33,
remaining wizard-school clusters (Divination/Enchantment/Illusion/Necromancy, none built) ~34,
small/long-tail groups ~85 (re-enumeration owed to whichever cycle next works this mechanism).

### Mechanism 3 (`class_feature_option_pool_record_not_held_by_engine`), 25 unchanged

Unchanged from cycle 9's own remainder table: proficiency/mechanical-grant possession-tracking
(weapon-flavored) 8, `Weapon and Armor Proficiency ~ {Druid,Monk}` 2, proficiency/mechanical-
grant possession-tracking (armor/shield-flavored + standalone extras) 10, companion/mount
registration 3, Domain Power `CLASS_FEATURE_POOLS` registration gap (owned by mechanism 2) 2.
`8+2+10+3+2=25`.

## Notes

- **This cycle's own central finding is an instrument-correction on a prior cycle's own
  reasoning, not a discovery of new work**: the "SEPARATE, THIRD reason" `class_feature_grant_
  consumer.rs`'s own inherited doc comment gave for keeping Druid/Monk class-wide-excluded (a
  `LevelUpPlan`-reachability concern) was real when first found (an actual historical failure, per
  that doc comment's own citation of `nature_bond`/`orisons`/`spontaneous_casting` being
  "silently DROPPED" — genuinely true of a DIFFERENT, wholly separate consumer, the `LevelUpPlan`
  screen, which this cycle's fix does not touch or need to touch) but was never checked against
  the SPECIFIC question this mechanism's own `docs/work-inventory.json` verdict actually asks
  (does `compute_pilot_base_chassis` hold the fact at all, regardless of `LevelUpPlan`
  reachability). Two genuinely different consumers of the same explanation id were conflated
  across several cycles' own doc comments; this cycle traces both all the way to their own
  source and corrects the conflation, per `decisions.md §12`'s own re-derivation discipline.
- **The 8 pre-existing test regressions found this cycle are a strong argument for occasionally
  running the FULL `sd13_*`/`sd25_*` set together**, not just the scoped subset each prior cycle's
  own dispatch brief named — every one of the 8 was a REAL, already-landed grounding (from
  mechanism 2's own earlier cycles, and from an unrelated SD-32-era generic pool mechanism) that
  its own test suite never caught because no single prior cycle's own scoped run ever exercised
  it. None was found by inspection; all 8 were found only by the full run this cycle's own
  dispatch brief explicitly required.
- **Concurrency:** this cycle ran alone in its own worktree with a dedicated
  `CARGO_TARGET_DIR=/tmp/cargo-sd34-batch`; no sibling-lane collision was observed (`git status
  --porcelain` checked before every stage; no `-A`/wildcard `git add` used).

## Next-cycle plan

1. A future mechanism-2 cycle should ground ONE more wizard school (Divination, Enchantment,
   Illusion, or Necromancy) end-to-end — real per-power PF1-rules formula engineering plus the
   `v06_work_inventory.rs` probe branch, mirroring cycles 6/7/8's own established two-source-
   verification rigor. This is real, scoped, but NOT narrow — size it as its own criterion.
2. A future mechanism-3 cycle should build the generic multi-class `GrantedFact` possession
   ledger (20 of the 25 remaining units) OR the Paladin Special Mount chassis computation (3
   units, likely more expensive per-unit) — both are genuinely new subsystems, not narrow
   catalog-widening; size and dispatch each as its own criterion rather than folding into this
   mechanism's remaining scope.
3. Mechanism 1's own remaining 24 non-excluded units (18 null-description, 6 gate-refused) wait
   on the same two blockers cycle 9 named: the bundle's open zero-description ruling, and real
   per-character grant/formula engineering for Rogue Talent-shaped records. Neither is this
   cycle's or a future narrow cycle's to close without that ruling or that engineering.
4. `AT-34-E6-001` (final-acceptance scan) should re-derive this cycle's own figures at its own
   HEAD, per its own standing instruction, rather than trusting this receipt.
