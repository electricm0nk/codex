# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine` mechanism, cycle 5)

- **Commit SHA:** (set below at push time)
- **Files touched:** `src/rules_core/pilot_compute/mod.rs` (3 new constants
  `BARD_SUGGESTION_LEVEL`/`BARD_INSPIRE_GREATNESS_LEVEL`/
  `BARD_MASS_SUGGESTION_LEVEL`, 1 new constant
  `BARD_INSPIRE_GREATNESS_MAX_ALLIES`, 3 new real formula blocks —
  Suggestion DC, Mass Suggestion DC, Inspire Greatness allies-count),
  `src/bin/v06_work_inventory.rs` (one new probe
  `probe_bard_bardic_performance_wiring`, one new `EngineFacts` field
  `bard_bardic_performance_wired`, one new `classify()` branch, 2 new
  tests), `scripts/completion_atlas.py` (10 shifted `file:line` citations,
  re-derived), `scripts/missing_engine_tables.py` (2 shifted `file:line`
  citations, re-derived), `docs/work-inventory.json` (regenerated,
  guarded), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
  `.../missing-engine-tables.json` (regenerated by their own `--check`
  runs), `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, this receipt,
  `docs/retro/events/sd34-at-34-e3-001.jsonl`.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS`
- **Wired-integration audit result:** `OK_NO_TOKENS` (the wider file-touch-set
  audit surfaces matches of the word "placeholder" — all pre-existing, from
  earlier committed cycles' own `vacuous-placeholder` sub-cause work; never a
  stub this cycle introduced; reviewed, not self-healed, the same finding
  cycles 2-4's own receipts each already made)
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**970** Core
  Rulebook units whose table exists but which are not in it. Evidence: the
  atlas reporting bucket B at zero for `core_rulebook`, and the mechanism
  that placed them named — by mechanism, not per record." (970 is
  `epic-breakdown.md`'s own stale, whole-bucket figure; this cycle owns
  exactly ONE of the nine mechanisms `decisions.md §14` names, and does not
  itself close the bucket.)
- **Status:** partial

Bucket B for `core_rulebook` is nine distinct mechanisms (`decisions.md
§14`). This cycle owns exactly one:
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` —
continuing cycle 4's own work (`..._cycle_receipt_4.md`, 282 -> 277).

## Population, re-derived (not quoted)

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
```
Before this cycle: **277** of 1,006 `core_rulebook` bucket-B units
(re-derived at this cycle's own start SHA `3ebc08b451`, matching cycle 4's
own closing figure exactly — no drift to correct). Corpus-wide (37 books)
before: 2,995.

## Mechanism-specific direction followed

Read cycle 4's own receipt in full before touching anything
(`artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_4.md`),
continuing from its own named remainder rather than re-deriving it. Its own
"Next-cycle plan" named "one additional wizard school" as the next lever,
but its own remainder table also named a **Bardic Performance** cluster (10
units, entirely untouched) with no stated blocker. This cycle investigated
that cluster instead: reading `data/corpus/core_rulebook/class_feature/
bardic_performance/*.json` directly (never assumed) showed every one of the
10 sub-records carries a real `BONUS:VAR|...` formula token — not a "pick
one from a roster" shape at all, the same "real, already-computed,
previously mis-attributed" shape cycle 4's own Wizard arcane-school finding
established.

## The real fix: read the engine's own Bard bardic-performance code directly before building anything

`src/rules_core/pilot_compute/mod.rs` already carries real, tested,
non-fabricated per-performance formulas for **7 of the 10** Bardic
Performance sub-records, confirmed by reading the function directly:

1. **Fascinate** — `class_chassis.bard.fascinate_dc` (`10+(BardicPerformanceLVL/2)+CHA`).
2. **Inspire Courage** — `class_chassis.bard.inspire_courage_bonus`.
3. **Inspire Competence** — `class_feature.bard.inspire_competence`.
4. **Soothing Performance** — `class_feature.bard.soothing_performance`.
5. **Frightening Tune** — `class_chassis.bard.frightening_tune_dc` (same DC shape as Fascinate).
6. **Deadly Performance** — `class_feature.bard.deadly_performance_dc` (same DC shape).
7. **Inspire Heroics** — `class_feature.bard.inspire_heroics_save_bonus` (+ 2 sibling ids for AC bonus / target count, one corpus record).

Every one was still `engine-does-not-hold` before this cycle because the
corpus record's `group` prefix (`"Bardic Performance"`) can never equal
`"bard"`, so `class_feature_owner` and its `type_facet` fallback (unspaced
camelCase, `"BardClassFeatures.SpecialQuality...."`) can never resolve an
owner — the identical owner-resolution failure shape Domain Power / Weapon
Training / Favored Enemy / Wizard Arcane School (cycles 2-4) already
established.

**New formulas built this cycle (the remaining 3, verified against this
repo's own ingested corpus record, not from memory):**

- **Suggestion** (level gate 6, verified against
  `data/corpus/.../suggestion.json`'s own `PREVARGTEQ:BardicPerformanceLVL,6`
  token in `bard/bardic_performance.json`'s `ABILITY` list) —
  `class_feature.bard.suggestion_dc`, identical DC formula shape to
  Fascinate/Frightening Tune/Deadly Performance.
- **Mass Suggestion** (level gate 18, verified the same way) —
  `class_feature.bard.mass_suggestion_dc`, identical DC formula shape.
- **Inspire Greatness** (level gate 9, verified the same way) —
  `class_feature.bard.inspire_greatness_allies`, `min((BardicPerformanceLVL-6)/3,4)`
  — the corpus's own capped-count formula, preserved exactly (constant
  `BARD_INSPIRE_GREATNESS_MAX_ALLIES = 4`), same shape as the already-grounded
  Fascinate affected-creature-count.

**Deliberately NOT claimed, named here so no future cycle re-investigates
without a genuinely new formula:** Countersong, Distraction, Versatile
Performance — the other 3 Bardic Performance corpus records — carry
`magnitude_token_count: 0` and are already `text-complete` via a different
evidence path (`class_feature_pool_catalog_serves_a_rendered_description`);
they are NOT part of this mechanism's population and this cycle did not
touch them.

## The probe

Built `probe_bard_bardic_performance_wiring`
(`src/bin/v06_work_inventory.rs`), the same shape as
`probe_wizard_arcane_school_wiring` but simpler: Bardic Performance records
are granted automatically by level, never gated behind a specialization
choice, so no choice override is needed — just `class_sweep_input(fixture,
"bard", level)` across `SWEEP_LEVELS`. For each sweep level, the probe
checks a fixed `(corpus_key, needed_explanation_id)` table against the real
computation's own explanation ids and inserts the corpus_key only when the
id was genuinely observed. `classify()`'s `Kind::ClassFeature` arm gained
one new check (mirroring the Wizard Arcane School check exactly):
`facts.bard_bardic_performance_wired.contains(&unit.key)`.

## RED -> GREEN

RED (confirmed for the intended reason): before this cycle's `classify()`
edit, all 10 target records fell through the same owner-resolution path
every sibling record does (`group` can never equal `"bard"`), landing at
`engine-does-not-hold` /
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` — the
live corpus data itself is the RED proof (`docs/work-inventory.json` at
this cycle's start SHA lists exactly this verdict for all 10).

GREEN: two new tests added to `class_feature_text_complete_rung_tests`:
- `a_bard_bardic_performance_record_the_probe_observed_reaches_grounded` —
  probe fact seeded with `"Bardic Performance ~ Fascinate"`; asserts
  `status == "grounded"`, the new probe-specific evidence string.
- `a_bard_bardic_performance_record_the_probe_never_observed_is_unaffected`
  — negative control, empty `EngineFacts`, a synthetic unclaimed key with
  `magnitude_token_count: 1` (this cycle's real fix closes all 10 genuine
  magnitude-bearing records, so no live corpus example of an unclaimed one
  remains); asserts the pre-existing `engine-does-not-hold` verdict is
  unchanged, doubling as proof the probe's own bound is honored.

```
cargo test --locked --bin v06_work_inventory bard_bardic_performance
running 2 tests
test class_feature_text_complete_rung_tests::a_bard_bardic_performance_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_bard_bardic_performance_record_the_probe_never_observed_is_unaffected ... ok
test result: ok. 2 passed; 0 failed
```

Live regen confirms exactly the predicted 10 corpus-wide records moved (all
`core_rulebook`):

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
w = [x for x in d['units'] if x.get('evidence','')=='bard_bardic_performance_probe_observed_a_real_computed_magnitude']
print(sorted((x['book'], x['corpus_key']) for x in w))
print(len(w))
"
[('core_rulebook', 'Bardic Performance ~ Deadly Performance'), ('core_rulebook', 'Bardic Performance ~ Fascinate'),
 ('core_rulebook', 'Bardic Performance ~ Frightening Tune'), ('core_rulebook', 'Bardic Performance ~ Inspire Competence'),
 ('core_rulebook', 'Bardic Performance ~ Inspire Courage'), ('core_rulebook', 'Bardic Performance ~ Inspire Greatness'),
 ('core_rulebook', 'Bardic Performance ~ Inspire Heroics'), ('core_rulebook', 'Bardic Performance ~ Mass Suggestion'),
 ('core_rulebook', 'Bardic Performance ~ Soothing Performance'), ('core_rulebook', 'Bardic Performance ~ Suggestion')]
10
```

## Discoveries

**Citation drift, self-healed (`decisions.md §12` L2/the bundle's own
line-number-citation hazard).** Inserting the new probe function, `EngineFacts`
field, and `classify()` branch shifted 10 `file:line` citations
`scripts/completion_atlas.py`'s `BUCKET_DEFINITIONS` hardcodes, plus 2 more
in `scripts/missing_engine_tables.py`'s `ENGINE_SURFACE_CITATIONS`. Found via
`python3 scripts/completion_atlas.py --check` (`citation_failures=10`) and
`python3 scripts/missing_engine_tables.py --check` (`citation_failures=2`)
immediately after the regen. Fixed by diffing this cycle's start-SHA copy of
`v06_work_inventory.rs` against the post-edit file at each cited line's
unique surrounding context (never guessed), confirming the new line by
literal `sed` inspection before writing it into the Python files. Both
`--check` runs now report `citation_failures=0`.

**No F1 instrument movement this cycle (checked, not assumed).** Unlike
cycle 4 (whose Abjuration Resistance closure was F1-shaped), none of this
cycle's 10 closed records carry a bare-literal formula: 5 are ability-modifier
DC formulas (F3), 1 is a named-counter/pool variable (F4), 3 are
clamped/capped per-level scaling (F5), 1 is a bare per-level formula (F2).
Verified directly: `python3 scripts/shape_ledger.py --inventory
docs/work-inventory.json --corpus-root data/corpus` reports `F1 5401` both
before and after this cycle's regen (unchanged from cycle 4's own closing
value) — confirmed by inspecting each of the 10 closed records' own
`BONUS:VAR` tokens, not inferred from the count alone.

**Unrelated pre-existing test failure, NOT this cycle's to fix, re-flagged
for the owning cycle.** `cargo test --locked --test v06_work_inventory`
still fails one test,
`the_committed_inventory_is_well_formed_and_uses_only_declared_statuses`, on
`core_rulebook:class_feature:empty_selection_standard_barbarian` (same
finding cycles 3-4's own receipts already made, unchanged this cycle: a
different sibling mechanism's own fix, confirmed present in
`docs/work-inventory.json` before this cycle touched anything). Out of THIS
mechanism's scope; this cycle did not touch `VACUOUS_PLACEHOLDER_CLASS_FEATURES`
or any of the three affected units.

**`box_ledger.py --check` inherited stale-count WARNINGs (unowned, pre-existing).**
Same 6-warning shape cycle 4's own receipt reported (THE-BOX.md's own
stated per-status counts vs. live recomputation) — this bundle has no
`THE-BOX.md` deliverable (`decisions.md §5`'s SD-34 note in the dispatch
brief); not self-healed, not this mechanism's scope, `uncovered=19889
oracle_disagreement=0 unverifiable_done=0 stale=False`.

`apps/desktop/src-tauri`: not run this cycle (time budget spent on the
workspace-wide build scope, the class_feature/lib/bin test suites, and the
full regenerate-and-verify chain; the separate crate's own full `cargo test`
was not attempted this cycle either — same gap cycles 3-4's own receipts
named).

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| Mechanism population before | 277 | `python3 -c "..."` (above) against `docs/work-inventory.json` at start SHA `3ebc08b451` | of 1,006 `core_rulebook` bucket-B units |
| Mechanism population after | 267 | same command against the regenerated `docs/work-inventory.json` | of 1,006 `core_rulebook` bucket-B units (pre-cycle denominator) |
| Corpus-wide mechanism population before | 2,995 | same command, no `book` filter | of 49,438 units |
| Corpus-wide mechanism population after | 2,985 | same command, no `book` filter | of 49,438 units (delta -10, exactly this cycle's 10 closures) |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) before | 579 | `python3 -c "..."` against `git show HEAD:docs/work-inventory.json` | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) after | 569 | `python3 scripts/completion_atlas.py --by-book` | of 6,701 `core_rulebook` units |
| Units closed (corpus-wide) | 10 | `evidence == "bard_bardic_performance_probe_observed_a_real_computed_magnitude"` | of 277 (core_rulebook), 2,995 (corpus-wide) |
| `completion_atlas.py --check` (corpus-wide) | `unclassified=0 overlap=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| `missing_engine_tables.py --check` | `citation_failures=0` | `python3 scripts/missing_engine_tables.py --check` | of 449 |
| `corpus_literal_sweep` | `48708 examined of 51482 read, 0 findings` (before and after — no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/release/corpus_literal_sweep" --json-out ...` | of 51,482 |
| `derived_evaluator_fixture_check` | `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/release/derived_evaluator_fixture_check" --json-out ...` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `397 passed; 0 failed` | `cargo test --locked --bin v06_work_inventory` | of 397 |
| `cargo test --locked --lib` | `2891 passed; 0 failed; 14 ignored` | `cargo test --locked --lib` | of 2905 |
| `cargo test --locked --no-run` (workspace) | exit 0 | `cargo test --locked --no-run` | — |
| `cargo test --locked --test v06_work_inventory` | `15 passed; 1 failed (pre-existing, unrelated — see Discoveries); 1 ignored` | `cargo test --locked --test v06_work_inventory` | of 17 |
| `F1` (`shape_ledger.py` rollup) | 5401 -> 5401 (unchanged, checked not assumed) | `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` | of 27,265 not-done units after this cycle's regen |
| `box_ledger.py --check` | 6 stale-count WARNINGs (inherited THE-BOX.md drift, unowned by this mechanism, pre-existing), `uncovered=19889 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
267
```
Not zero. **Status: partial**, remainder named below.

## Build scope verified

`cargo test --locked --no-run` exit 0, workspace-wide, run at this cycle's
own commit SHA (see `commit_sha` in the structured return). `cargo test
--locked --lib`: 2891 passed, 0 failed, 14 ignored. `cargo test --locked
--bin v06_work_inventory`: 397 passed, 0 failed. `cargo test --locked --test
v06_work_inventory`: 15 passed, 1 failed (pre-existing, unrelated to this
mechanism — see Discoveries), 1 ignored. `apps/desktop/src-tauri` (separate
cargo workspace): **not run this cycle** — time budget spent on the
workspace-wide build scope and the full regenerate-and-verify chain
(corpus_literal_sweep, derived_evaluator_fixture_check, the guarded regen,
citation re-derivation).

## Sweep population

`corpus_literal_sweep`: 48708 examined before -> 48708 examined after, delta
0 (no `data/corpus/**` file touched or regenerated this cycle — only
`src/rules_core/pilot_compute/mod.rs`'s ALREADY-EXISTING formulas were
consumed, plus 3 genuinely new ones added; only `src/bin/` and
`src/rules_core/pilot_compute/mod.rs` Rust source changed, plus the two
Python citation files).

## Movement, four buckets

- **Closure:** 10 — all 10 Bardic Performance sub-records (all
  `core_rulebook`) -> `DONE` (`grounded`, `wiring_class: computed`). Every
  one carries a REAL, live-computed magnitude a Bard's own sheet already
  renders, proven by a live probe against the real compute pipeline for a
  canonical Bard, not asserted. 7 of the 10 were formulas the engine already
  had; 3 (Suggestion, Mass Suggestion, Inspire Greatness) are genuinely new
  formulas this cycle built.
- **Reclassification:** 0 — no unit changed bucket without a genuine holds
  change.
- **Reachability:** 0 — no previously-unreachable unit became reachable
  (the 10 closed records were already reachable Bard class features; this
  cycle changed only the atlas's own attribution of an already-computed or
  newly-computed value, not what a player can select).
- **Instrument-correction:** 0 — `F1` (5401) and every other population
  figure moved by exactly the closure count and nothing else; no
  measurement-method change this cycle.

## Remainder — 267 units, named by sub-cause

Re-derived fresh against the post-cycle inventory (populations sum exactly
to 267):

| Sub-cause | Units | Notes |
|---|---|---|
| `Domain Power` (the 56 domains `DOMAIN_POWER_CATALOG` carries no formula for) | 56 | Unchanged. Real new-formula work per domain. |
| `Domain Base` | 33 | Unchanged (cycles 2-3's own investigation: genuinely computable DC formula, genuinely never consumed by an enemy-facing domain power `DOMAIN_POWER_CATALOG`'s own module doc names as deliberately excluded for a safety reason — not a bounded one-cycle fix). |
| Wizard arcane-school cluster (remaining 7 schools' own powers, plus both Evocation's and Abjuration's own top-level/opposition-school recognition records) | 38 | Unchanged from cycle 4. Needs either a genuinely new per-power formula for 7 schools this engine has none for, or a new prohibited-school recognition record neither built school has either — see cycle 4's own receipt for the exact boundary. |
| `Draconic Bloodline Choice` | 10 | Unchanged. Sorcerer bloodline sub-choice roster; **checked this cycle**, unlike Bardic Performance, each color's own corpus record carries ONLY the shared, non-numeric spell-list/resistance-type identity tokens (`Pool_ArcaneOppositionSchool`-shaped, no per-color numeric formula) — a genuinely different shape from Bardic Performance's per-record DC/count formulas, not the same lever. |
| `Secret Lore` | 10 | Unchanged. Oracle Lore mystery's sub-roster; no per-record consumer (not re-investigated this cycle — no reason yet to suspect the Bardic Performance shape applies). |
| `New Arcana` | 9 | RULED OUT by cycle 3 (see that receipt): no single canonical per-record value the engine ever commits to; do not re-investigate without a genuinely new consumer. |
| Small (2-3 unit) per-class roster groups (Damage Reduction, Divine Bond, Favored Class Bonus, Hunter's Bond, Ki Stat Choice, Nature's Bond, Physical Enhancement, Precise Strike, Wildshape 2/3) | 22 | Unchanged. |
| Long tail (single-unit groups, non-school) | 89 | Unchanged. |

56+33+38+10+10+9+22+89 = 267.

## Next-cycle plan

1. **`Secret Lore` (10)** — read each of the 10 Oracle Lore sub-records'
   `BONUS:VAR` tokens directly (the same first move this cycle made for
   Bardic Performance) before assuming it needs the same "no consumer"
   treatment `Draconic Bloodline Choice` got this cycle — it has not been
   checked record-by-record yet.
2. **One additional wizard school, built end-to-end (e.g. Conjuration or
   Necromancy, 4 units each)** — cycle 4's own next-cycle lever, still
   open.
3. **`Domain Base`'s real DC consumer (33)** — still not built, out of scope
   for a single bounded cycle unless deliberately taken on as its own
   dedicated cycle (cycles 2-3's own finding, re-confirmed unchanged).
4. Fix (or hand to the `class_feature_option_pool_record_not_held_by_engine`
   mechanism's next cycle) the unrelated pre-existing
   `tests/v06_work_inventory.rs` failure named in Discoveries.

- **Status:** partial
