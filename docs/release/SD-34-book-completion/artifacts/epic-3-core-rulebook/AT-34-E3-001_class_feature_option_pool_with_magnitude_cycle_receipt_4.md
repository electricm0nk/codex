# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine` mechanism, cycle 4)

- **Commit SHA:** (set below at push time)
- **Files touched:** `src/bin/v06_work_inventory.rs` (one new probe
  `probe_wizard_arcane_school_wiring`, one new `EngineFacts` field, one new
  `classify()` branch, 2 new tests), `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs`
  (F1 population re-pin, 5402 -> 5401 — a real movement this cycle caused, not a bad
  re-derivation), `scripts/completion_atlas.py` (10 shifted `file:line` citations, re-derived),
  `scripts/missing_engine_tables.py` (2 shifted `file:line` citations, re-derived),
  `docs/work-inventory.json` (regenerated, guarded), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
  `.../missing-engine-tables.json` (regenerated by their own `--check` runs),
  `docs/release/SD-34-book-completion/progress.md` (self-healed one inherited
  denominator-gate violation from cycle 3's own committed text, plus this cycle's own
  entry), `docs/release/SD-34-book-completion/kanban.md`, this receipt,
  `docs/retro/events/sd34-at-34-e3-001.jsonl` (one `correction` event).
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS`
- **Wired-integration audit result:** `OK_NO_TOKENS` (the wider file-touch-set audit
  surfaces matches of the word "placeholder" — all pre-existing, from earlier committed
  cycles' own `vacuous-placeholder` sub-cause work and real PCGen "no selection" CHOOSE-menu
  prose, never a stub this cycle introduced; reviewed, not self-healed, the same finding
  cycles 2 and 3's own receipts each already made)
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**970** Core Rulebook units
  whose table exists but which are not in it. Evidence: the atlas reporting bucket B at zero
  for `core_rulebook`, and the mechanism that placed them named — by mechanism, not per
  record." (970 is `epic-breakdown.md`'s own stale, whole-bucket figure; this cycle owns
  exactly ONE of the nine mechanisms `decisions.md §14` names, and does not itself close the
  bucket.)
- **Status:** partial

Bucket B for `core_rulebook` is nine distinct mechanisms (`decisions.md §14`). This cycle owns
exactly one: `class_feature_option_pool_record_with_magnitude_not_held_by_engine` — continuing
cycle 3's own work (`..._cycle_receipt_3.md`, 324 -> 282).

## Population, re-derived (not quoted)

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
```
Before this cycle: **282** of 1,006 `core_rulebook` bucket-B units (re-derived at this
cycle's own start SHA `3e68073423`, matching cycle 3's own closing figure exactly — no drift
to correct). Corpus-wide (37 books) before: 3,000.

## Mechanism-specific direction followed

Read cycle 3's own receipt in full before touching anything (`artifacts/epic-3-core-rulebook/
AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_3.md`), continuing from
its own named remainder rather than re-deriving it. Its own "Next-cycle plan" named the
Wizard arcane-school cluster as the cheapest remaining lever, sharing its root cause with the
sibling `class_feature_option_pool_record_not_held_by_engine` mechanism's own cycle 1 finding.
That sibling's own cycle 1 receipt investigated the SAME cluster and explicitly declined the
FULL cluster (37 units at the time) as needing "genuinely new engine subsystems (spell-school
restriction tracking, ...)" — this cycle re-confirmed that constraint still holds for most of
the cluster (read directly, never assumed), but found a real, already-computed, previously
mis-attributed subset within it.

## The real fix: read the engine's own arcane-school code directly before building anything

`src/rules_core/pilot_compute/mod.rs` already carries real, tested, non-fabricated per-power
formulas for exactly TWO of the wizard's nine arcane schools — confirmed by reading the
function directly, never assumed:

1. **Evocation** (`canonical_seeds_for("wizard")`'s own standard canonical seed already selects
   it): `class_chassis.wizard.intense_bonus_damage` computes Intense Spells' `max(1,
   EvocationSchoolLVL/2)`, matching the corpus's own `BONUS:VAR|EvocationIntenseSpellsBonus|
   max(1,EvocationSchoolLVL/2)` token verbatim (`evocation_school/intense_spells.json`).
   `class_chassis.wizard.force_missile_uses_per_day` computes Force Missile's `3 + Intelligence
   modifier`, matching `BONUS:VAR|EvocationForceMissileTimes|ArcaneSchoolPowerTimes` ->
   `ArcaneSchoolPowerTimes|INT+3` (`evocation_school/force_missile.json`).
2. **Abjuration** (`wizard_has_canonical_abjuration_selection`'s own gate — Task #66, already
   built, never wired to `classify()`): `class_feature.school.abjuration.resistance` (stepped
   5/10, matching `BONUS:VAR|AbjurationResistanceBonus|5` x2), `...protective_ward_uses_per_day`
   / `...protective_ward_duration` / `...protective_ward_deflection_bonus` (three flat
   sub-magnitudes for the one `Abjuration School ~ Protective Ward` corpus record), and
   `...energy_absorption` (`AbjurationSchoolLVL*3`, matching `BONUS:VAR|
   AbjurationEnergyAbsorption|AbjurationSchoolLVL*3`).

Every one of these 5 corpus records (`"Evocation School ~ Intense Spells"`, `"Evocation School
~ Force Missile"`, `"Abjuration School ~ Resistance"`, `"Abjuration School ~ Protective Ward"`,
`"Abjuration School ~ Energy Absorption"`) was still `engine-does-not-hold` before this cycle
because their `group` prefix (`"Evocation School"` / `"Abjuration School"`) can never equal
`"wizard"`, so `class_feature_owner` and its `type_facet` fallback (whose marker match needs a
LITERAL space before `"Class Feature(s)"`, and these facets are unspaced camelCase —
`"WizardClassFeatures.SpecialQuality...."`) can never resolve an owner — the same shape Domain
Power / Weapon Training / Favored Enemy/Terrain Bonus (cycles 2-3) already established.

**Deliberately NOT claimed, named here so no future cycle re-investigates without a genuinely
new formula:** the other seven schools (Conjuration, Divination, Enchantment, Illusion,
Necromancy, Transmutation, Universal) have no per-power formula built at all — only the
shared, non-numeric `Pool_ArcaneOppositionSchool`/`OppositionalSchool` prohibited-school
tokens every school's top-level record carries identically. Both Evocation's and Abjuration's
OWN top-level `"<School> School"` recognition record and every `"<School> Opposition School"`
record are ALSO not claimed — their magnitude tokens (`Pool_ArcaneOppositionSchool|2`,
`OppositionalSchool|2`) are the same shared, uncredited prohibited-school bookkeeping every
school carries, and no explanation id exists for them. Crediting either would be exactly the
"plausible-looking but not actually observed" shape the Sorcerer New Arcana finding (cycle 3's
own receipt) already ruled out for this mechanism.

## The probe

Built `probe_wizard_arcane_school_wiring` (`src/bin/v06_work_inventory.rs`), the same shape as
`probe_domain_power_effect_wiring` / `probe_ranger_favored_enemy_bonus_wiring`: for each of the
5 SWEEP_LEVELS, runs `compute_pilot_base_chassis` once with the standard wizard seed
(Evocation, already canonical) and once with the specialization choice swapped to Abjuration
(same Necromancy/Transmutation opposed pair `wizard_has_canonical_abjuration_selection`
requires), and keeps only the exact corpus_key strings whose own explanation id was genuinely
observed. `classify()`'s `Kind::ClassFeature` arm gained one new check (mirroring the Domain
Power / Favored Enemy checks exactly): `facts.wizard_arcane_school_wired.contains(&unit.key)`.

## RED -> GREEN

RED (confirmed for the intended reason): before this cycle's `classify()` edit, all 5 target
records fell through the same owner-resolution path every sibling record does (`group` can
never equal `"wizard"`, and the `type_facet` fallback's marker match fails on wizard's own
unspaced camelCase facets), landing at `engine-does-not-hold` /
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` — the live corpus data
itself is the RED proof (`docs/work-inventory.json` at this cycle's start SHA lists exactly
this verdict for all 5).

GREEN: two new tests added to `class_feature_text_complete_rung_tests`:
- `a_wizard_arcane_school_record_the_probe_observed_reaches_grounded` — probe fact seeded with
  `"Evocation School ~ Force Missile"`; asserts `status == "grounded"`, the new
  probe-specific evidence string.
- `a_wizard_arcane_school_record_the_probe_never_observed_is_unaffected` — negative control,
  empty `EngineFacts`, using `"Conjuration School ~ Acid Dart"` (a school the probe
  deliberately never claims); asserts the pre-existing `engine-does-not-hold` verdict is
  unchanged, doubling as proof the probe's own bound is honored.

```
cargo test --locked --bin v06_work_inventory wizard_arcane
running 2 tests
test class_feature_text_complete_rung_tests::a_wizard_arcane_school_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_arcane_school_record_the_probe_never_observed_is_unaffected ... ok
test result: ok. 2 passed; 0 failed
```

Live regen confirms exactly the predicted 5 corpus-wide records moved (all `core_rulebook`):

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
w = [x for x in d['units'] if x.get('evidence','')=='wizard_arcane_school_probe_observed_a_real_computed_magnitude']
print(sorted((x['book'], x['corpus_key']) for x in w))
print(len(w))
"
[('core_rulebook', 'Abjuration School ~ Energy Absorption'), ('core_rulebook', 'Abjuration School ~ Protective Ward'), ('core_rulebook', 'Abjuration School ~ Resistance'), ('core_rulebook', 'Evocation School ~ Force Missile'), ('core_rulebook', 'Evocation School ~ Intense Spells')]
5
```

## Discoveries

**Instrument correction (real movement, not a bad re-derivation).** Cycle 3's own F1-population
re-pin (`formula_interpreter_corpus_wide.rs`, 5,402) was TRUE at cycle 3's own start and end.
This cycle's own fix moved it: of the 5 newly-grounded records, exactly ONE
(`Abjuration School ~ Resistance`, `BONUS:VAR|AbjurationResistanceBonus|5`) carries F1's own
defining shape (a bare-literal magnitude token); the other four carry per-level
(`AbjurationSchoolLVL*3`), ability-modifier (`3 + Intelligence modifier`), or
referenced-variable formulas, never F1-shaped. `shape_ledger.py`'s population is scoped to
NOT-DONE units, so a unit leaving bucket B for `DONE` legitimately leaves the F1 count too if
it was F1-shaped. 5402 - 1 = 5401, confirmed by re-running `python3 scripts/shape_ledger.py
--inventory docs/work-inventory.json --corpus-root data/corpus` against the regenerated file:
F1 = 5401 exactly. Re-pinned with the doc comment explaining the delta by mechanism, not
silently. Retro `correction` event filed (`docs/retro/events/sd34-at-34-e3-001.jsonl`,
`--verified-by "python3 scripts/shape_ledger.py ..."`).

**Inherited pre-existing denominator-gate violation, self-healed.** `docs/release/
SD-34-book-completion/progress.md` line 202 (committed by cycle 3, `0302b44e`) read
`` (`/` at 100%) `` — a bare percentage describing disk fullness, no denominator marker on
the line, and not the exempted "false 100%" idiom. `python3 scripts/denominator_gate.py
--check 'docs/release/SD-34-book-completion/*.md'` found it as this cycle's own first run's
`violations=1`. Confirmed via `git log -1 -- .../progress.md` that the line predates this
cycle (already committed at `0302b44e`, before this cycle touched anything) — a real,
pre-existing gate violation, not one this cycle introduced. Self-healed inline (single-token
fix, in a file this cycle already had to touch for its own progress entry): `` (`/` at 100%) ``
-> `` (`/` full) ``, removing the bare-percentage token without changing the sentence's
meaning. `denominator_gate.py --check` now reports `violations=0`.

**Unrelated pre-existing test failure, NOT this cycle's to fix, re-flagged for the owning
cycle.** `cargo test --locked --test v06_work_inventory` still fails one test,
`the_committed_inventory_is_well_formed_and_uses_only_declared_statuses`, on
`core_rulebook:class_feature:empty_selection_standard_barbarian` (same finding cycle 3's own
receipt already made, unchanged this cycle: a different sibling mechanism's own cycle 3 fix,
confirmed present in `docs/work-inventory.json` before this cycle touched anything). Out of
THIS mechanism's scope; this cycle did not touch `VACUOUS_PLACEHOLDER_CLASS_FEATURES` or any
of the three affected units.

`apps/desktop/src-tauri`: not run this cycle (time budget spent on the workspace-wide build
scope, the class_feature/lib/bin test suites, and the full regenerate-and-verify chain; the
separate crate's own full `cargo test` was not attempted this cycle either — same gap cycle 3's
own receipt named).

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| Mechanism population before | 282 | `python3 -c "..."` (above) against `docs/work-inventory.json` at start SHA `3e68073423` | of 1,006 `core_rulebook` bucket-B units |
| Mechanism population after | 277 | same command against the regenerated `docs/work-inventory.json` | of 1,006 `core_rulebook` bucket-B units (pre-cycle denominator) |
| Corpus-wide mechanism population before | 3,000 | same command, no `book` filter | of 49,438 units |
| Corpus-wide mechanism population after | 2,995 | same command, no `book` filter | of 49,438 units (delta -5, exactly this cycle's 5 closures) |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) before | 692 | `python3 scripts/completion_atlas.py --by-book` | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) after | 687 | same | of 6,701 `core_rulebook` units |
| Units closed (corpus-wide) | 5 | `evidence == "wizard_arcane_school_probe_observed_a_real_computed_magnitude"` | of 282 (core_rulebook), 3,000 (corpus-wide) |
| `completion_atlas.py --check` (corpus-wide) | `unclassified=0 overlap=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| `missing_engine_tables.py --check` | `citation_failures=0` | `python3 scripts/missing_engine_tables.py --check` | of 449 |
| `corpus_literal_sweep` | `48708 examined of 51482 read, 0 findings` (before and after — no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/release/corpus_literal_sweep" --json-out ...` | of 51,482 |
| `derived_evaluator_fixture_check` | `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/release/derived_evaluator_fixture_check" --json-out ...` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `393 passed; 0 failed` | `cargo test --locked --bin v06_work_inventory` | of 393 |
| `cargo test --locked --lib` | `2881 passed; 0 failed; 14 ignored` | `cargo test --locked --lib` | of 2895 |
| `cargo test --locked --no-run` (workspace) | exit 0 | `cargo test --locked --no-run` | — |
| `cargo test --locked --test v06_work_inventory` | `15 passed; 1 failed (pre-existing, unrelated — see Discoveries); 1 ignored` | `cargo test --locked --test v06_work_inventory` | of 17 |
| `F1` (`shape_ledger.py` rollup) | 5402 -> 5401 | `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` | of 27,230 not-done units before this cycle |
| `box_ledger.py --check` | 6 stale-count WARNINGs (inherited THE-BOX.md drift, unowned by this mechanism, pre-existing), `uncovered=20021 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` (1 inherited violation self-healed this cycle — see Discoveries) | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
277
```
Not zero. **Status: partial**, remainder named below.

## Build scope verified

`cargo test --locked --no-run` exit 0, workspace-wide, run at this cycle's own commit SHA (see
`commit_sha` in the structured return). `cargo test --locked --lib`: 2881 passed, 0 failed, 14
ignored. `cargo test --locked --bin v06_work_inventory`: 393 passed, 0 failed. `cargo test
--locked --test v06_work_inventory`: 15 passed, 1 failed (pre-existing, unrelated to this
mechanism — see Discoveries), 1 ignored. `apps/desktop/src-tauri` (separate cargo workspace):
**not run this cycle** — time budget spent on the workspace-wide build scope and the full
regenerate-and-verify chain (corpus_literal_sweep, derived_evaluator_fixture_check, the guarded
regen, F1 re-pin, citation re-derivation).

## Sweep population

`corpus_literal_sweep`: 48708 examined before -> 48708 examined after, delta 0 (no
`data/corpus/**` file touched or regenerated this cycle — only `src/rules_core/pilot_compute/
mod.rs`'s ALREADY-EXISTING formulas were consumed; only `src/bin/` and
`src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` Rust source changed, plus the
two Python citation files).

## Movement, four buckets

- **Closure:** 5 — `Evocation School ~ {Intense Spells, Force Missile}` and `Abjuration School
  ~ {Resistance, Protective Ward, Energy Absorption}` (all `core_rulebook`) -> `DONE`
  (`grounded`, `wiring_class: computed`). Every one carries a REAL, live-computed magnitude a
  Wizard's own sheet already renders, proven by a live probe against the real compute pipeline
  for that exact school selection, not asserted.
- **Reclassification:** 0 — no unit changed bucket without a genuine holds change.
- **Reachability:** 0 — no previously-unreachable unit became reachable (the 5 closed records
  were already reachable Wizard class features; this cycle changed only the atlas's own
  attribution of an already-computed value, not what a player can select).
- **Instrument-correction:** 1 — the F1 population re-pin (5402 -> 5401), a REAL movement this
  cycle's own fix caused (see Discoveries), corrected with its own doc comment and a retro
  `correction` event, not silently carried or silently changed.

## Remainder — 277 units, named by sub-cause

Re-derived fresh against the post-cycle inventory (populations sum exactly to 277):

| Sub-cause | Units | Notes |
|---|---|---|
| `Domain Power` (the 56 domains `DOMAIN_POWER_CATALOG` carries no formula for) | 56 | Unchanged. Real new-formula work per domain. |
| `Domain Base` | 33 | Unchanged (cycles 2-3's own investigation: genuinely computable DC formula, genuinely never consumed by an enemy-facing domain power `DOMAIN_POWER_CATALOG`'s own module doc names as deliberately excluded for a safety reason — not a bounded one-cycle fix). |
| Wizard arcane-school cluster (remaining 7 schools' own powers, plus both Evocation's and Abjuration's own top-level/opposition-school recognition records) | 38 | Investigated THIS cycle, partially closed (5 of 43 total). The remaining 38 need either a genuinely new per-power formula for 7 schools this engine has none for, or a new prohibited-school recognition record neither built school has either — see "The real fix" above for the exact boundary. |
| `Bardic Performance` | 10 | Unchanged. Bard performance-type roster; no per-performance-type magnitude consumer. |
| `Draconic Bloodline Choice` | 10 | Unchanged. Sorcerer bloodline sub-choice roster; no per-type magnitude consumer. |
| `Secret Lore` | 10 | Unchanged. Oracle Lore mystery's sub-roster; no per-record consumer. |
| `New Arcana` | 9 | RULED OUT by cycle 3 (see that receipt): no single canonical per-record value the engine ever commits to; do not re-investigate without a genuinely new consumer. |
| Small (2-3 unit) per-class roster groups (Damage Reduction, Divine Bond, Favored Class Bonus, Hunter's Bond, Ki Stat Choice, Nature's Bond, Physical Enhancement, Precise Strike, Wildshape 2/3) | 22 | Unchanged. |
| Long tail (single-unit groups, non-school) | 89 | Unchanged. |

56+33+38+10+10+10+9+22+89 = 277.

## Next-cycle plan

1. **One additional wizard school, built end-to-end (e.g. Conjuration or Necromancy, 4 units
   each)** — the cheapest remaining lever in the school cluster: each school's 3 powers plus
   its own top-level record need real per-power formulas built (not just attribution, unlike
   this cycle's Evocation/Abjuration fix, which only needed attribution because the formulas
   were ALREADY built by earlier tasks #55/#66). Read each school's own corpus record's
   `BONUS:VAR` tokens directly before building — several are non-flat (e.g. dice rolls) and may
   stay unproven the same way Force Missile's own 1d4 damage roll does.
2. **`Domain Base`'s real DC consumer (33)** — still not built, out of scope for a single
   bounded cycle unless deliberately taken on as its own dedicated cycle (cycles 2-3's own
   finding, re-confirmed unchanged).
3. **`Weapon Training`'s remaining 48** — flagged by cycle 3, now folded into the sibling
   `class_feature_owner_matched` mechanism's own count per that mechanism's latest cycle;
   confirm which mechanism currently owns it before dispatching.
4. **`Bardic Performance`/`Draconic Bloodline Choice`/`Secret Lore` (30)** — each its own scoped
   "new per-record consumer" investigation, unchanged from cycle 3's own plan.
5. Fix (or hand to the `class_feature_option_pool_record_not_held_by_engine` mechanism's next
   cycle) the unrelated pre-existing `tests/v06_work_inventory.rs` failure named in Discoveries
   — 3 `vacuous_placeholder_row_no_corpus_content_to_render` units violate the
   "deferred-with-reason always quotes `engine_diagnostic:`" invariant.

- **Status:** partial
