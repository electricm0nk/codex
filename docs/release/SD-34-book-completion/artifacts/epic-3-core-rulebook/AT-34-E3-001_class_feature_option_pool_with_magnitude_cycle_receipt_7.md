# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine` mechanism, cycle 7)

- **Commit SHA:** `6a87278d87`
- **Files touched:** `src/rules_core/pilot_compute/mod.rs` (new
  `CONJURATION_SCHOOL_SELECTION` constant, new
  `wizard_has_canonical_conjuration_selection`, one new computation block
  gated on it -- Summoner's Charm duration-increase bonus, Acid Dart
  bonus-damage + uses-per-day, Dimensional Steps feet-per-day, 3 new
  `ComputationExplanation` pushes), `src/bin/v06_work_inventory.rs`
  (`probe_wizard_arcane_school_wiring` gained a fourth variant, swapping
  BOTH the specialization AND the opposed-schools choice per cycle 6's own
  Discoveries warning, 3 new tests, updated doc comments naming the fourth
  school), `scripts/completion_atlas.py` (10 shifted `file:line` citations,
  re-derived by direct grep against the post-edit file, not assumed),
  `scripts/missing_engine_tables.py` (2 shifted `file:line` citations,
  re-derived), `docs/work-inventory.json` (regenerated, guarded),
  `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
  `.../missing-engine-tables.json` (regenerated by their own `--check` runs),
  `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, this receipt,
  `docs/retro/events/sd34-at-34-e3-001.jsonl`.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (the wider branch-wide diff against
  `origin/develop` surfaces many `sd32_*` marker-string deletions, all pre-existing content
  from earlier committed cycles' own corpus-record relabeling, never introduced by this
  cycle's own additions -- verified zero `+` lines match the pattern)
- **Wired-integration audit result:** `OK_NO_TOKENS` (the wider file-touch-set audit surfaces
  20 `+` matches of the word "placeholder" -- all pre-existing, from earlier committed cycles'
  own `vacuous-placeholder` sub-cause work landed on this shared branch; reviewed, not
  self-healed, the same finding cycles 2-6's own receipts each already made; none of this
  cycle's own added lines in `pilot_compute/mod.rs` or the new probe/test code contain any of
  the audited tokens)
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**970** Core
  Rulebook units whose table exists but which are not in it. Evidence: the
  atlas reporting bucket B at zero for `core_rulebook`, and the mechanism
  that placed them named -- by mechanism, not per record." (970 is
  `epic-breakdown.md`'s own stale, whole-bucket figure; this cycle owns
  exactly ONE of the nine mechanisms `decisions.md §14` names, and does not
  itself close the bucket.)
- **Status:** partial

Bucket B for `core_rulebook` is nine distinct mechanisms (`decisions.md
§14`). This cycle owns exactly one:
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` --
continuing cycle 6's own work (`..._cycle_receipt_6.md`, 267 -> 261).

## Population, re-derived (not quoted)

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
```
Before this cycle: **261** of 562 `core_rulebook` bucket-B units (re-derived
at this cycle's own start SHA `0827dcd59b`, matching cycle 6's own closing
figure exactly -- no drift to correct). Corpus-wide (37 books) before:
**2,976** of 49,438 units.

## Mechanism-specific direction followed

Read cycle 6's own receipt in full before touching anything
(`artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_6.md`),
continuing from its own named remainder rather than re-deriving it. Its own
"Next-cycle plan" named two levers: `Secret Lore` (a materially bigger lift,
needs a Loremaster prestige-class chassis that does not exist yet -- left
alone this cycle) and "one more wizard school, built end-to-end" (any of
Conjuration/Divination/Enchantment/Illusion/Necromancy/Universal). This
cycle took **Conjuration**: cheapest of the six by unit count that carries
the exact same 3-power shape cycle 4/5/6's own Evocation/Abjuration/
Transmutation groundings already established (Divination and Universal
have only 1-2 power records each; Enchantment/Illusion/Necromancy have 3
each, tied with Conjuration, but Conjuration's own two level-1 powers plus
one level-8 power matched the Transmutation precedent's exact shape most
closely, minimizing re-derivation risk).

## The real fix: read the engine's own Conjuration-school corpus records directly before building anything

Reading `data/corpus/core_rulebook/class_feature/conjuration_school/*.json`
directly (never assumed) found the SAME shape cycles 4-6's own Evocation/
Abjuration/Transmutation findings each already established: real,
non-fabricated `BONUS:VAR` formulas the engine had never wired to
`classify()`, because the corpus record's `group` prefix
(`"Conjuration School"`) can never equal `"wizard"`.

**New formulas built this cycle** (`src/rules_core/pilot_compute/mod.rs`),
gated on a new `wizard_has_canonical_conjuration_selection` (mirroring
`wizard_has_canonical_transmutation_selection` exactly, one school swapped,
opposing Necromancy + Abjuration -- both already-existing selection
constants):

- **Summoner's Charm** (`Conjuration School ~ Summoner's Charm`) --
  `ConjurationSummonersCharmBonus|max(1,ConjurationSchoolLVL/2)`, unlocked
  from level 1.
- **Acid Dart** (`Conjuration School ~ Acid Dart`) --
  `ConjurationAcidDartDamageBonus|ConjurationSchoolLVL/2` (bonus damage) and
  `ConjurationAcidDartTimes|ArcaneSchoolPowerTimes` (uses/day, the same
  "3 + Intelligence modifier" idiom Force Missile / Telekinetic Fist
  already use), both unlocked from level 1.
- **Dimensional Steps** (`Conjuration School ~ Dimensional Steps`) --
  `ConjurationDimensionalSteps|ConjurationSchoolLVL*30`, gated on the
  power's own level-8 unlock (`PREVARLT:Wizard_CF_SchoolPower8,1`).

**Deliberately NOT claimed**, consistent with the Evocation/Abjuration/
Transmutation precedent: the top-level `"Conjuration School"` recognition
record and `"Conjuration Opposition School"` record carry only the shared,
non-numeric `Pool_ArcaneOppositionSchool`/`OppositionalSchool` bookkeeping
every school carries identically -- no per-record formula, no explanation
id, not this mechanism's shape.

## The probe

Extended `probe_wizard_arcane_school_wiring` (`src/bin/v06_work_inventory.rs`)
with a fourth variant, this time swapping BOTH the specialization choice
AND the opposed-schools choice from the very first attempt (cycle 6's own
Discoveries section named this exact hazard -- the seeded default opposed
pair, Necromancy+Transmutation, never satisfies a non-Transmutation
specialist's own opposed-pair precondition either, so leaving it unswapped
silently observes nothing). Verified before committing by checking the
three target `corpus_key`s directly against the regenerated inventory
after the first full guarded regen, not by trusting the predicted delta
alone -- the corrected implementation closed exactly the 3 records
predicted on the FIRST attempt this cycle, unlike cycle 6's own two-round
probe-defect history.

`classify()`'s pre-existing `wizard_arcane_school_wired` check (unchanged
this cycle -- exact `unit.key` `BTreeSet` membership, not the substring
match the mechanism's own COLLISION HAZARD warns about) now also resolves
the corrected set. Doc comments naming "the three schools" updated to name
all four (Evocation, Abjuration, Transmutation, Conjuration).

## RED -> GREEN

RED (confirmed for the intended reason): before this cycle's probe
extension, all 3 `core_rulebook` target records fell through the same
owner-resolution path every sibling record does (`group` can never equal
`"wizard"`), landing at `engine-does-not-hold` /
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` --
the live corpus data itself is the RED proof (`docs/work-inventory.json`
at this cycle's start SHA lists exactly this verdict for all 3).

GREEN: three new tests added to `class_feature_text_complete_rung_tests`:
- `a_wizard_conjuration_school_record_the_probe_observed_reaches_grounded`
  -- probe fact seeded with `"Conjuration School ~ Acid Dart"`; asserts
  `status == "grounded"`, the pre-existing
  `wizard_arcane_school_probe_observed_a_real_computed_magnitude` evidence
  string (the SAME evidence id Evocation/Abjuration/Transmutation already
  use -- one probe, one evidence string, four schools).
- `a_wizard_conjuration_dimensional_steps_record_the_probe_observed_reaches_grounded`
  -- same proof for the level-8 `"Conjuration School ~ Dimensional Steps"`
  power, a DIFFERENT record within the same group prefix.
- `a_wizard_conjuration_school_record_the_probe_never_observed_is_unaffected`
  -- negative control, empty `EngineFacts`, uses the top-level
  `"Conjuration School"` recognition record itself (deliberately never
  claimed); asserts the pre-existing `engine-does-not-hold` verdict is
  unchanged, doubling as proof the probe's own bound is honored.

```
cargo test --locked --bin v06_work_inventory conjuration
running 3 tests
test class_feature_text_complete_rung_tests::a_wizard_conjuration_dimensional_steps_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_conjuration_school_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_conjuration_school_record_the_probe_never_observed_is_unaffected ... ok
test result: ok. 3 passed; 0 failed
```

Live regen confirms **3** corpus-wide records moved -- all 3 in
`core_rulebook` (this mechanism's own scope), with **no cross-book side
effect this time**: unlike Transmutation's own Arcanist "Arcanist School
Transmutation" exploit counterpart in `advanced_class_guide`, Conjuration
has no equivalent Arcanist exploit record sharing this corpus_key set
(checked directly, not assumed):

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
w = [x for x in d['units'] if x.get('evidence','')=='wizard_arcane_school_probe_observed_a_real_computed_magnitude'
     and x['corpus_key'].startswith('Conjuration')]
print(sorted((x['book'], x['corpus_key']) for x in w))
print(len(w))
"
[('core_rulebook', 'Conjuration School ~ Acid Dart'), ('core_rulebook', 'Conjuration School ~ Dimensional Steps'),
 ('core_rulebook', "Conjuration School ~ Summoner's Charm")]
3
```

Whole-file before/after diff against the committed `HEAD` copy of
`docs/work-inventory.json` confirms exactly 6 changed lines (the 3
records' old-version deletion + new-version addition each), plus the
`generated_at` timestamp and the `by_status`/`class_feature` summary
counters -- no other record anywhere in the 49,438-unit file moved,
confirming this cycle's own COLLISION HAZARD discipline (the mechanism's
own warning about substring-owner-namespace misclassification) was
honored: `classify()`'s check here is exact `unit.key` `BTreeSet`
membership, never a substring match.

## Discoveries

**No probe defect this cycle, unlike cycle 6's own two-round history.**
Cycle 6's own Discoveries section named the exact hazard (swap BOTH
choices, not just the specialty) explicitly enough that this cycle's
first probe implementation attempt already swapped both from the start,
and the first full guarded regen closed exactly the 3 records predicted
with no correction round needed.

**Unrelated pre-existing test failure, NOT this cycle's to fix, re-flagged
for the owning cycle (unchanged from cycles 3-6's own receipts).**
`cargo test --locked --test v06_work_inventory` still fails one test on
`core_rulebook:class_feature:empty_selection_standard_barbarian` -- a
different sibling mechanism's own fix (`vacuous_placeholder_row_no_corpus_
content_to_render`, Barbarian's own PCGen CHOOSE-menu placeholder row),
confirmed present in `docs/work-inventory.json` before this cycle touched
anything, unrelated to Conjuration.

**Regen wall-clock cost, re-confirmed for future cycles' own time
budgeting.** The full guarded regen (`cargo run --bin v06_work_inventory`,
release profile this cycle for speed) took roughly 10-25 minutes of CPU
time; ran once to completion this cycle (no correction round needed),
confirmed against the pre-cycle `git diff` snapshot before committing.

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| Mechanism population before | 261 | `python3 -c "..."` (above) against `docs/work-inventory.json` at start SHA `0827dcd59b` | of 562 `core_rulebook` bucket-B units |
| Mechanism population after | **258** | same command against the regenerated `docs/work-inventory.json` | of 562 `core_rulebook` bucket-B units (pre-cycle denominator) |
| Corpus-wide mechanism population before | 2,976 | same command, no `book` filter | of 49,438 units |
| Corpus-wide mechanism population after | 2,973 | same command, no `book` filter | of 49,438 units (delta -3, exactly this cycle's 3 closures) |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) before | 556 | `python3 scripts/completion_atlas.py --check --book core_rulebook` at start SHA | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) after | 553 | `python3 scripts/completion_atlas.py --check --book core_rulebook` | of 6,701 `core_rulebook` units |
| Units closed (corpus-wide) | 3 (all `core_rulebook`, no cross-book side effect) | `evidence == "wizard_arcane_school_probe_observed_a_real_computed_magnitude"`, Conjuration subset | of 261 (core_rulebook), 2,976 (corpus-wide) |
| `completion_atlas.py --check` (corpus-wide) | `population=49438 unclassified=0 overlap=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| `missing_engine_tables.py --check` | `population=449 citation_failures=0` | `python3 scripts/missing_engine_tables.py --check` | of 449 |
| `corpus_literal_sweep` | `48708 examined of 51482 read, 0 findings` (before and after -- no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/release/corpus_literal_sweep" --json-out ...` | of 51,482 |
| `derived_evaluator_fixture_check` | `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/release/derived_evaluator_fixture_check" --json-out ...` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `405 passed; 0 failed` | `cargo test --locked --bin v06_work_inventory` | of 405 |
| `cargo test --locked --lib` | `2910 passed; 0 failed; 14 ignored` | `cargo test --locked --lib` | of 2924 |
| `cargo test --locked --no-run` (workspace) | exit 0 | `cargo test --locked --no-run` | -- |
| `cargo test --locked --test v06_work_inventory` | `15 passed; 1 failed (pre-existing, unrelated -- see Discoveries); 1 ignored` | `cargo test --locked --test v06_work_inventory` | of 17 |
| `F1` (`shape_ledger.py` rollup) | 5400 -> 5400 (unchanged, checked not assumed -- none of this cycle's formulas are bare-literal) | `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` | of not-done units after this cycle's regen |
| `box_ledger.py --check` | 7 stale-count WARNINGs (inherited THE-BOX.md drift, unowned by this mechanism, pre-existing, unchanged shape from cycle 6), `uncovered=19861 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
258
```
Not zero. **Status: partial**, remainder named below.

## Build scope verified

`cargo test --locked --no-run` exit 0, workspace-wide, run at this cycle's
own commit SHA (see `commit_sha` in the structured return). `cargo test
--locked --lib`: 2910 passed, 0 failed, 14 ignored. `cargo test --locked
--bin v06_work_inventory`: 405 passed, 0 failed (3 new this cycle). `cargo
test --locked --test v06_work_inventory`: 15 passed, 1 failed
(pre-existing, unrelated -- see Discoveries), 1 ignored. `apps/desktop/src-tauri`
(separate cargo workspace): `cargo test --locked --no-run` exit 0.

## Sweep population

`corpus_literal_sweep`: 48708 examined before -> 48708 examined after,
delta 0 (no `data/corpus/**` file touched or regenerated this cycle --
only `src/rules_core/pilot_compute/mod.rs`'s new formulas and
`src/bin/v06_work_inventory.rs`'s probe extension changed).

## Movement, four buckets

- **Closure:** 3 -- all 3 Conjuration power records (Summoner's Charm,
  Acid Dart, Dimensional Steps) -> `DONE` (`grounded`). Every one carries a
  REAL, live-computed magnitude a Conjuration wizard's own sheet already
  renders, proven by a live probe against the real compute pipeline for a
  canonical Conjuration wizard, not asserted.
- **Reclassification:** 0 -- no unit changed bucket without a genuine holds
  change.
- **Reachability:** 0 -- no previously-unreachable unit became reachable
  (the closed records were already reachable Wizard class features; this
  cycle changed only the atlas's own attribution of a newly-computed
  value, not what a player can select).
- **Instrument-correction:** 0 -- `F1` (5400) and every other population
  figure moved by exactly the closure count and nothing else; no
  measurement-method change this cycle.

## Remainder -- 258 units, named by sub-cause

Re-derived fresh against the post-cycle inventory (populations sum exactly
to 258):

| Sub-cause | Units | Notes |
|---|---|---|
| `Domain Power` (domains `DOMAIN_POWER_CATALOG` carries no formula for) | 56 | Unchanged. Real new-formula work per domain. |
| `Domain Base` | 33 | Unchanged (cycles 2-3's own investigation: genuinely computable DC formula, never consumed by an enemy-facing domain power, deliberately excluded per `DOMAIN_POWER_CATALOG`'s own module doc). |
| Wizard arcane-school cluster (6 remaining schools' own powers plus every school's own top-level/opposition-school recognition record, Conjuration's own now included in the "closed schools" set) | 32 | Was 35 before this cycle; -3 (this cycle's own 3 closed Conjuration POWER records moved out; the Conjuration top-level + opposition-school recognition records remain, deliberately unclaimed, same as Evocation/Abjuration/Transmutation's own). |
| `Draconic Bloodline Choice` | 10 | Unchanged (cycle 5's own finding: non-numeric, no per-color formula, different shape from Bardic Performance). |
| `Secret Lore` | 10 | Unchanged (cycle 6's own finding: real per-record formulas exist, but no Loremaster prestige-class chassis exists to hang them on -- a materially bigger lift, named again below). |
| `New Arcana` | 9 | RULED OUT by cycle 3: no single canonical per-record value the engine ever commits to. |
| Small (2-3 unit) per-class roster groups (Damage Reduction, Divine Bond, Favored Class Bonus, Hunter's Bond, Ki Stat Choice, Nature's Bond, Precise Strike, Wildshape 2/3) | 20 | Unchanged. |
| Long tail (single-unit groups, non-school) | 88 | Unchanged. |

56+33+32+10+10+9+20+88 = 258.

## Next-cycle plan

1. One more wizard school (Divination, Enchantment, Illusion, Necromancy,
   or Universal -- 1-3 units each), following this cycle's exact pattern
   (new `wizard_has_canonical_<school>_selection` opposing two other real
   schools, swap BOTH the specialty AND opposed-schools choice in the
   probe from the start).
2. **`Secret Lore` (10)** -- a dedicated cycle, not a bounded extension:
   every one of the 10 Loremaster prestige-class sub-records carries a
   real, distinct formula, but the engine has ZERO Loremaster
   prestige-class posture at all. Needs the prestige-class chassis built
   first.
3. `Domain Base`'s real DC consumer (33) -- still not built, out of scope
   for a single bounded cycle (cycles 2-3's own finding, re-confirmed
   unchanged through cycle 7).

- **Status:** partial
