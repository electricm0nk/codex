# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-003 (bucket `M` — EQUIPMENT sub-causes)

- **Commit SHA:** `d2cd685ced00cfca12658c1f6503db619f2b3f4a` (this receipt's own commit is on
  top of it). Started this cycle's rebase from `origin/tranche/14`'s tip at the time,
  `f2c13e12b90e6dc0eed033cd447fb730629ccb91`.

- **Files touched:**
  - `src/bin/v06_work_inventory.rs` — `equipment_key_is_wired` (the shared function
    `probe_equipment_effect_wiring` calls per `(engine_book, key)`, whose result feeds
    `classify()`'s `Kind::Equipment`/`Kind::EquipmentModifier` "observed" rung) widened with a
    second, independent check: after the existing eight `compute_equipment_effects` fields
    (armor-class/max-dex/spell-failure/ACP/skill/ability/weapon-enhancement/spell-resistance)
    all come back `None`, the function now also consults
    `damage_total::resolve_base_damage_dice(key, corpus)`. This is **not a new compute path** —
    `resolve_base_damage_dice` already reads a weapon's real `DAMAGE:` corpus token into a
    structured `DiceExpression`, and is already the entry gate
    `damage_total::resolve_weapon_damage_breakdown` uses to build the `WeaponDamageBreakdown`
    the desktop app's `character_hub.rs` renders on the real character sheet (confirmed:
    `grep -n 'resolve_base_damage_dice' src/rules_core/damage_total.rs` shows it gating
    `resolve_weapon_damage_breakdown` at line 798; `grep -n 'WeaponDamageBreakdown'
    apps/desktop/src-tauri/src/character_hub.rs` finds it wired). Four new tests:
    `equipment_probe_promotes_a_real_weapon_with_a_real_damage_token` (real on-disk CRB corpus,
    `Bastard Sword (Base)`), `equipment_probe_promotes_a_hand_built_weapon_with_only_a_damage_token`
    (fixture, pins the mechanism to the `DAMAGE:` token alone),
    `equipment_probe_does_not_promote_a_record_with_no_damage_token_and_no_other_effect`
    (negative control), `a_real_damage_token_promotes_the_equipment_unit_to_grounded_end_to_end`
    (`classify()`-level, same shape as the pre-existing
    `an_observed_computed_delta_outranks_the_text_complete_rung`).
  - `scripts/completion_atlas.py` — 10 `BUCKET_DEFINITIONS` `file:line` citations re-derived and
    corrected. This cycle's insertion into `equipment_key_is_wired` (+20 net lines) shifted
    every citation below it by exactly +20 — measured via `sed -n '<old+20>p'
    src/bin/v06_work_inventory.rs` against each citation's own `must_contain` literal before
    writing the new line number (never guessed by offset alone). `citation_failures=0` after.
  - `scripts/missing_engine_tables.py` — 2 `ENGINE_SURFACE_CITATIONS` (`companion`, `power`),
    same reason, same method. `citation_failures=0` after.
  - This receipt, `docs/release/SD-34-book-completion/progress.md`,
    `docs/release/SD-34-book-completion/kanban.md`.
  - **Deliberately NOT touched/committed:** `docs/work-inventory.json` and
    `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` (and, by
    the same reasoning, `missing-engine-tables.json`) — this wave's dispatch brief reserves their
    regeneration to a single shared end-of-wave cycle. This receipt's figures below were derived
    by running the guarded three-pass regen pipeline **locally**, reading the result, and then
    `git restore`-ing all three generated files back to HEAD before the final commit — every
    figure ships with the command that reproduces it, but the regenerated bytes themselves are
    not shipped in this cycle's commits.

- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` on this cycle's own diff —
  `git diff --unified=0 f2c13e12b90e6dc0eed033cd447fb730629ccb91...HEAD -- src/rules_core/
  src/bin/ scripts/oracle_harness/ artifacts/epic-3-core-rulebook/ ':!**/__tests__/**'
  ':!**/*.test.*' | grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` → zero matches.
  The wider epic-file-touch-set diff since the tranche cut
  (`ea2b3396f2fde9223dde93522bd2288b463a21ee...HEAD`, same paths) carries 20 matches — confirmed
  by diffing `ea2b3396f2...f2c13e12b9` (the state immediately before this cycle started) against
  the same paths: also exactly 20 matches, i.e. **all 20 pre-date this cycle**, none introduced
  by it.

- **Wired-integration audit result:** `OK_NO_TOKENS` on this cycle's own diff (same command,
  second pattern) → zero matches. The wider epic-diff carries 20 matches (the word
  "placeholder" in its real PF1/PCGen domain sense, already-landed); the same before/after check
  against `ea2b3396f2...f2c13e12b9` shows the identical 20, confirming none are new.

- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "AT-34-E3-003 — buckets M, V, D, U, X
  close" (per-bucket, atlas reporting zero for `core_rulebook`, movement in four buckets). This
  cycle closes part of bucket `M`'s two EQUIPMENT sub-causes only
  (`equipment_table_entry_with_corpus_magnitude`,
  `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does`) — the criterion as a whole
  stays open (`M`'s other six sub-causes, and buckets V/D/U/X, are untouched by design; the
  territory boundary was stated in this cycle's own dispatch brief to avoid two lanes editing
  the shared classifier at once).

- **Re-derived at cycle start (never trusting the inherited figure):** `python3 -c "import json,
  collections; d=json.load(open('docs/work-inventory.json')); u=[x for x in d['units'] if
  x['book']=='core_rulebook' and x['status']=='ingested-magnitude']; print(len(u));
  print(collections.Counter(x['evidence'] for x in u))"` at rebased HEAD
  (`f2c13e12b9`) → `core_rulebook` M = **972**, split exactly as the dispatch brief stated:
  `equipment_table_entry_with_corpus_magnitude` 276,
  `ability_content_table_holds_record_magnitude_not_yet_computed` 217,
  `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` 147,
  `race_trait_generic_table_holds_record_magnitude_not_yet_computed` 119,
  `template_content_table_holds_record_magnitude_not_yet_computed` 96,
  `in_catalog_with_corpus_magnitude_but_no_observed_consumer` 47,
  `domain_content_table_holds_record_magnitude_not_yet_computed` 34,
  `skill_content_table_holds_record_magnitude_not_yet_computed` 19,
  `spell_list_entry_with_resolved_level` 15,
  `race_trait_states_a_universal_sheet_modifier_pending_compute` 2. This cycle's assigned
  territory (276 + 147 = 423) confirmed exactly before any code was written.

- **Figures + their re-derive commands (post-fix, from the local regen described above):**
  - `core_rulebook` bucket `M`: **972 → 958** (−14). Re-derive:
    `python3 scripts/completion_atlas.py --book core_rulebook --check` (after re-running the
    three-pass regen pipeline below; the committed `docs/work-inventory.json` at HEAD still
    reads 972 until the wave's shared regen cycle lands this cycle's source change).
  - `core_rulebook` DONE: **4330 → 4344** (+14). Same command.
  - `core_rulebook` `equipment_table_entry_with_corpus_magnitude`: **276 → 262** (−14).
    Re-derive: the same `json.load`/`Counter` one-liner above, run against the local regen
    output — denominator 972 (this cycle's own re-derived `core_rulebook` M population at
    cycle start, confirmed above before any edit).
  - `core_rulebook` `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does`:
    **147 → 147** (0 closed). **The two equipment shapes are genuinely different, as the
    dispatch brief warned to check for.** Every one of this cycle's 14 real closures resolved a
    `DAMAGE:` token sitting on the unit's OWN corpus row (the
    `equipment_table_entry_with_corpus_magnitude` shape). Zero came from the closure-only shape:
    `resolve_base_damage_dice` reads a `DAMAGE:` token off the record `equipment_id_resolve`
    returns for the exact key handed in, and none of the 147 closure-only alias rows in this
    population happen to resolve to a base record that itself carries a `DAMAGE:` token.
    **One fix did not cover both shapes.**
  - Corpus-wide bucket `M`: **5002 → 4971** (−31), out of population 49,438 (unchanged — 0
    added, 0 removed). Re-derive: `python3 scripts/completion_atlas.py --check`. Corpus-wide
    DONE: **24278 → 24309** (+31), same command.
  - **Whole-corpus before/after diff by unit id** (script written this cycle, comparing the
    pre-cycle committed `docs/work-inventory.json` at `50790d6bf9` — the last commit that
    touched that file before this cycle's rebase base — against the local post-fix regen):
    **49,438 units before, 49,438 after, 0 added, 0 removed, 37 changed.** 31 real status
    transitions (`ingested-magnitude → grounded`), by book:
    `core_rulebook` 14, `ultimate_equipment` 5, `bestiary_3` 4, `inner_sea_races` 2,
    `ultimate_psionics` 2, `advanced_class_guide` 1, `advanced_players_guide` 1, `bestiary_2` 1,
    `ultimate_combat` 1 (sum 14+5+4+2+2+1+1+1+1 = 31). The other 6 changed units are
    **reclassification, not closure**: all 6 in `ultimate_equipment`, already `literal-verified`
    (bucket `V`, not `M`) both before and after — their `evidence` string changed from
    `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` to
    `equipment_effect_probe_observed_computed_delta` because the same probe widening now
    observes a real `DAMAGE:`-token weapon a separate, already-passing oracle-verification pass
    had already stamped `literal-verified` by proxy. Named here per `decisions.md §12` L1 even
    though it moves no bucket count. **The fix is generic by construction** — the same
    `equipment_key_is_wired` function every book's probe pass calls — and the movement proves
    it: 31 units closed across **9 books**, not just `core_rulebook`.
  - `corpus_literal_sweep`: **48,708 examined of 51,482 read**, CLEAN, 0 findings, both before
    and after this cycle's own regen (no `data/corpus/**` file added or changed — only the
    engine classifier and the derived inventory changed).

- **Row-count command output (this cycle's own local regen, the artifact this cycle's status is
  set from):**
  ```
  $ python3 -c "
  import json, collections
  d = json.load(open('docs/work-inventory.json'))
  units = d['units']
  m = [u for u in units if u.get('book')=='core_rulebook' and u.get('status')=='ingested-magnitude']
  print('core_rulebook M count:', len(m))
  print(collections.Counter(u.get('evidence') for u in m))
  "
  core_rulebook M count: 958
  Counter({'ability_content_table_holds_record_magnitude_not_yet_computed': 217,
  'equipment_table_entry_with_corpus_magnitude': 262,
  'equipment_own_line_has_no_magnitude_but_closure_wiring_class_does': 147,
  'race_trait_generic_table_holds_record_magnitude_not_yet_computed': 119,
  'template_content_table_holds_record_magnitude_not_yet_computed': 96,
  'in_catalog_with_corpus_magnitude_but_no_observed_consumer': 47,
  'domain_content_table_holds_record_magnitude_not_yet_computed': 34,
  'skill_content_table_holds_record_magnitude_not_yet_computed': 19,
  'spell_list_entry_with_resolved_level': 15,
  'race_trait_states_a_universal_sheet_modifier_pending_compute': 2})
  ```
  Sum 217+262+147+119+96+47+34+19+15+2 = **958**, matching
  `python3 scripts/completion_atlas.py --book core_rulebook --check`'s own live `M: 958`
  (run against the local regen) exactly. This cycle's own two sub-causes: 262+147 = **409**
  (down from 423 — this cycle's assigned territory), −14 exactly, 14+409 = 423.

- **Build scope verified (at the final commit SHA, `d2cd685ced`):**
  - `cargo test --locked --bin v06_work_inventory` — **446/446 pass** (4 new tests; 442
    pre-existing at this cycle's rebase base, all still passing).
  - `cargo test --locked --lib rules_core::damage_total::` — **30/30 pass** (no source change
    in `damage_total.rs` itself; confirms the module's own suite is undisturbed by a new
    caller).
  - `python3 scripts/completion_atlas.py --check` (local regen) — exit 0, `citation_failures=0`,
    `done_evidence_violations=0`, `missing_clearing_mechanisms=0`, `stale_derived_at=False`.
  - `python3 scripts/missing_engine_tables.py --check` — exit 0, `citation_failures=0`.
  - `cargo test --locked --no-run` (full workspace, `CARGO_TARGET_DIR=/tmp/cargo-sd34-at-34-e3-003`)
    — **exit 0** at commit `d2cd685ced`.
  - `apps/desktop/src-tauri` (separate cargo workspace,
    `CARGO_TARGET_DIR=/tmp/cargo-sd34-at-34-e3-003-desktop`):
    `cargo test --locked --no-run --manifest-path apps/desktop/src-tauri/Cargo.toml` — tested
    explicitly because `src/bin/v06_work_inventory.rs`'s changed function calls
    `damage_total::resolve_base_damage_dice`, a function the desktop crate's own
    `character_hub.rs` also calls transitively (`codex = { path = "../../.." }`); the change
    adds a read-only consultation inside a `src/bin/` binary the desktop crate does not link, so
    no desktop-visible type signature changed. **Exit 0** (confirmed:
    `Executable unittests src/main.rs`, `Finished \`test\` profile`, `[exited with code 0]`).

- **Sweep population:** no `data/corpus/**` records added or regenerated — only the engine
  classifier and the derived inventory changed. `corpus_literal_sweep`: **48,708 examined of
  51,482 read**, CLEAN, 0 findings, unchanged before/after (`decisions.md §12` L8 — 0 delta
  expected, 0 delta confirmed).

- **Denominator gate against this package:**
  `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` →
  `files_checked=15 violations=7`, all 7 pre-existing verbatim-quoted corpus prose in
  `progress.md` (`FRT_HVY`'s "75% chance..."), already flagged by the already-merged
  `AT-34-E3-004` cycle and every `AT-34-E3-003` cycle since; this cycle's own new prose (this
  receipt, the `progress.md` entry, the `kanban.md` row) contains no bare percentage.

- **Oracle pin:** `PCGEN_ORACLE_SHA` per `scripts/pcgen-oracle-pin.env` — not consulted this
  cycle (no oracle-pinned corpus read; the `DAMAGE:` token this cycle reads comes from the
  repo's own `data/corpus/core_rulebook/`, already ingested).

- **Status:** partial

- **Movement, four buckets:** closure (31 units corpus-wide, 14 `core_rulebook`, genuinely
  newly promoted `ingested-magnitude → grounded` — a real, already-wired compute path
  (`damage_total::resolve_base_damage_dice`, feeding the desktop app's own
  `WeaponDamageBreakdown`) now backs their magnitude, not a reclassification of the same
  evidence); reclassification (6 units, `ultimate_equipment`, evidence string corrected on
  already-`literal-verified` units, bucket `V` not `M`, described above); reachability 0;
  instrument-correction (10 `completion_atlas.py` + 2 `missing_engine_tables.py` citation line
  numbers re-pinned after this cycle's own +20-line insertion shifted them).

- **Notes:**
  - **Both equipment shapes were investigated with real evidence, not assumed to share a fix.**
    Before writing code, this cycle read the real, on-disk `Bastard Sword (Base)` corpus record
    (`data/corpus/core_rulebook/equipment/arms_armor/bastard_sword_base.json`) and confirmed it
    carries `DAMAGE:1d10`, resolves against the corpus, and was classified `ingested-magnitude`
    / `equipment_table_entry_with_corpus_magnitude` before this cycle — the RED state the fix
    then turned GREEN. The 147-unit closure-only shape was checked the same way: its own
    `equipment_id_resolve` target rows were inspected, and none carry a `DAMAGE:` token — the
    fix genuinely does not reach that shape, confirmed rather than assumed.
  - **Why the remainder (409) is not more of the 423, per real sub-shape (categorized this
    cycle, not exhaustively):** equipment modifiers granting an AC-type bonus
    (`Special Ability ~ Bonus AC / <School>`) have no field on `ResolvedEquipmentEffect` at all
    — building one is new bucket-`B`-shaped engineering, out of this cycle's "instrument already
    exists" scope. Modifiers whose bonus chain is `%CHOICE`-gated need the choice-selection
    mechanism a sibling lane owns this cycle (explanation-id/choice wiring — explicitly off
    limits). Internal-plumbing modifier records (`Charged Item`, `Enhancement Cost`, and
    similar — `BONUS:VAR`/`BONUS:ITEMCOST`) carry a real token but are not a player-facing
    mechanical effect; no compute path exists or obviously should under the current promotion
    ladder. Named artifacts (`Holy Avenger`, `Nine Lives Stealer`, and similar) state their
    magnitude in prose alongside a `BONUS:` chain but resolve to record shapes this cycle did
    not build a probe for. None of these four shapes has an existing, already-wired compute
    path this cycle could consult without new engineering.
  - **Independent re-derivation, not inherited from the unreviewed salvage checkpoint.** An
    earlier, uncommitted checkpoint existed on `origin/salvage/wave13-lane3` (rescued after a
    host kill mid-run) making the same claims this receipt makes. This cycle treated it as a
    claim to verify, not a fact to inherit: rebased onto the current `tranche/14` tip (9 commits
    ahead of the salvage branch's own base), re-derived the 972/276/147 starting figures fresh
    against the rebased HEAD, re-applied the code and test changes by hand (confirming each one
    compiles and is internally consistent with the rebased file, not merged wholesale), reran
    the full three-pass regen pipeline itself, and independently re-derived the whole-corpus
    before/after diff from this cycle's own commits rather than citing the salvage branch's
    numbers. The independently-derived figures (14 `core_rulebook` closures, 31 corpus-wide, 6
    reclassifications, identical per-book breakdown) matched the salvage checkpoint's claims
    exactly — a genuine confirmation, not an assumption. Nothing was carried forward unverified.
  - **Generated-artifact scope discipline.** `docs/work-inventory.json` and
    `completion-atlas.json` were regenerated locally to derive every figure in this receipt, then
    `git restore`-d before each commit — this cycle's dispatch brief reserves committing those
    two files to a single shared end-of-wave regeneration cycle, a rule new since (and a direct
    answer to) the wave-13 collision this same dispatch brief names. The figures above are
    therefore reproducible by anyone re-running the same three commands, but the committed
    `docs/work-inventory.json` at this cycle's own HEAD still reads the PRE-cycle numbers (972 /
    276 / 147) until that shared regen cycle runs.
  - **A pre-existing, unrelated instrument defect found and NOT fixed** (out of this criterion's
    file-touch set): `python3 scripts/shape_engine_boundary.py --check` reports
    `STALE_CITATION` for its `PROMOTION_LADDER_ANCHOR_LINE` (10857) — confirmed by inspecting
    `src/bin/v06_work_inventory.rs` at both this cycle's own rebase base (`f2c13e12b9`) and the
    pre-cycle tranche cut (`ea2b3396f2`): the citation was already stale before this cycle
    touched the file, from an earlier, unrelated insertion. Not in this criterion's declared
    scope (`decisions.md §3`'s Epic 3 file-touch set does not include re-pinning every citation
    instrument in the repo); named here per AT-34-E3-006's "an atlas defect gets recorded, not
    silently fixed or silently ignored" posture, left for the owning cycle.

- **Remainder — every unit in this cycle's two EQUIPMENT sub-causes, named, at HEAD:**

  | Sub-cause (evidence string) | Population before | Closed this cycle | Population after | Clearing mechanism for the rest |
  |---|---:|---:|---:|---|
  | `equipment_table_entry_with_corpus_magnitude` | 276 | 14 | **262** | per real sub-shape: an unmeasured number of AC-bonus equipmods need a new compute path (bucket-B-shaped, not this cycle's scope); an unmeasured number are `%CHOICE`-gated (sibling lane's scope); an unmeasured number are internal PCGen plumbing (VAR/ITEMCOST — a future `decisions.md §17`-shaped ruling); an unmeasured number are named artifacts needing a description-linked probe not built this cycle |
  | `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` | 147 | 0 | **147** | same four sub-shapes as above, applied to the closure-only (alias-row) population; zero of this shape happened to resolve to a `DAMAGE:`-bearing base record this cycle |

  Sum: 262 + 147 = **409**, matching this cycle's own live re-derive exactly. This cycle's
  assigned territory was 423 (276+147); 14 closed, 409 remain, 14+409 = 423 exactly.

  **Every other `M` sub-cause, and buckets V/D/U/X, are untouched by this cycle** (out of
  territory by the dispatch brief's own no-collision rule — `ability_content` and sibling
  `race_trait`/`template`/`domain`/`skill_content` compute probes belong to a sibling lane):

  | Bucket / sub-cause | `core_rulebook` population (post-cycle, local regen) |
  |---|---:|
  | `M` `ability_content_table_holds_record_magnitude_not_yet_computed` | 217 |
  | `M` `race_trait_generic_table_holds_record_magnitude_not_yet_computed` | 119 |
  | `M` `template_content_table_holds_record_magnitude_not_yet_computed` | 96 |
  | `M` `in_catalog_with_corpus_magnitude_but_no_observed_consumer` | 47 |
  | `M` `domain_content_table_holds_record_magnitude_not_yet_computed` | 34 |
  | `M` `skill_content_table_holds_record_magnitude_not_yet_computed` | 19 |
  | `M` `spell_list_entry_with_resolved_level` | 15 |
  | `M` `race_trait_states_a_universal_sheet_modifier_pending_compute` | 2 |
  | `V` (whole bucket, local regen) | 87 |
  | `D` (whole bucket) | 366 |
  | `U` (whole bucket) | 10 |
  | `X` (whole bucket) | 115 |

- **Next-cycle plan:**
  1. **This cycle's own remainder (409) needs real new engineering, not more wiring** — an
     AC-bonus-from-equipmod compute path (bucket-B-shaped) is a legitimate next equipment
     sub-cycle but is explicitly out of "instrument already exists" scope; scope and cost it
     before committing to the full population.
  2. **A finer real-corpus categorization of the 409** (exactly how many are `%CHOICE`-gated vs.
     internal plumbing vs. named-artifact-prose vs. genuinely uncomputed AC/save bonuses) would
     let a future cycle pick the cheapest remaining shape first — this cycle categorized at a
     coarse level (Notes, above) but did not exhaustively classify every one of the 409.
  3. **`ability_content` (217, a sibling lane's territory)** is the largest `M` sub-cause
     overall.
  4. **The shared regen cycle** must pick up this cycle's source change
     (`equipment_key_is_wired`'s widening) the next time it runs `docs/work-inventory.json`'s
     three-pass pipeline — this receipt's figures predict exactly what that regen will show
     (`core_rulebook` M 972→958, corpus-wide M 5002→4971, DONE +31), so the shared cycle should
     treat a mismatch as a real regression to investigate, not noise.
  5. **Buckets `V` (87), `D` (366), `U` (10), `X` (115) are entirely untouched by this cycle**.
