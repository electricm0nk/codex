# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-002 (bucket C, "held and computed, never surfaced"), cycle 2

- **Commit SHA:** `PENDING`
- **Files touched:** `src/bin/v06_work_inventory.rs` (three grounding mechanisms added to
  `classify()`'s `Kind::ClassFeature` arm and one new probe -- see "Mechanisms closed" below),
  `docs/work-inventory.json` (regenerated, guarded, env-var stamped), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`
  (regenerated by its own `--check` run), `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, `docs/work-inventory.json`, this receipt.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS on this cycle's own working-tree diff
  (`git diff --unified=0 -- src/bin/v06_work_inventory.rs` -- zero matches). The epic-scoped diff
  (`git diff --unified=0 <merge-base>...HEAD -- src/rules_core/ src/bin/ scripts/oracle_harness/
  ':!**/__tests__/**' ':!**/*.test.*'`) surfaces 20 matches, ALL pre-existing `sd13_*`/`sd25_*`
  test-filename references from an earlier AT-34-E3 cycle already committed to this branch --
  confirmed none originate from this cycle's own diff (same check, this cycle's file only, zero
  matches).
- **Wired-integration audit result:** OK_NO_TOKENS on this cycle's own working-tree diff (same
  command, same file). The epic-scoped diff surfaces 17 pre-existing matches from earlier
  AT-34-E3 cycles' own committed content (same disposition as cycle 1's own receipt: none from
  this cycle's diff).
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**370** units the engine holds
  and computes but never surfaces. **Evidence:** per unit, the explanation or display path
  that now carries it. A unit the player still cannot see is not cleared, whatever the engine
  holds." (370 is `epic-breakdown.md`'s own stale figure, unchanged since cycle 1's own receipt
  flagged it; not carried forward here either.)
- **Status:** partial

## Population, re-derived (not quoted)

Cycle 1 closed bucket C 414 -> 372 (`AT-34-E3-002_cycle_receipt.md` cycle 1). Between that
receipt and this cycle's start, unrelated lanes (`AT-34-E3-003` bucket-M `skill_content`
classifier wiring and its own wave-9 regeneration) moved the `core_rulebook` bucket-C population
further, from a DIFFERENT mechanism than either AT-34-E3-002 cycle -- confirmed by reading
`progress.md`'s own most recent entry before this cycle (the wave-9 regeneration cycle), not
assumed. This cycle's OWN start-of-cycle re-derive:

```
python3 scripts/completion_atlas.py --book core_rulebook --check
```

At this cycle's start (HEAD `1ea93e99ce`, fast-forwarded to `tranche/14`):

```
book=core_rulebook population=6701 unclassified=0 overlap=0
  DONE: 4330
  A: 0
  B: 470
  C: 357
  D: 366
  M: 972
  V: 81
  U: 10
  X: 115
  Z: 0
```

Bucket C for `core_rulebook` at this cycle's start: **357**, not 372. **Confirmed the prior
cycle's own remainder mechanism-shapes still hold** (`decisions.md §12` L12: a method carried
past its limit is corrected and everything it judged re-run) -- re-grouped the live 357 units by
the SAME sub-cause categories cycle 1 named and found every category still present with the same
shape, only populations shifted by unrelated lanes' work (base-class-standalone dropped 47->36,
`other_named_group_or_standalone` dropped 83->75, `bloodline_power_or_bloodline_feat_not_computed`
rose 73->77 -- net -15, matching 372-357 exactly). None of cycle 1's eight named sub-causes were
disproven; all eight are still real, still unstarted (except this cycle's own three slices below).

## Mechanisms closed this cycle: three paired-record/generic-suffix grounding fixes, all territory-scoped to explanation-id/diagnostic-naming wiring

**1. Domain display record, sibling of `AT-34-E3-001`'s own `"Domain Power ~ <X>"` chassis
check.** `data/corpus/core_rulebook/class_feature/<domain>/*.json` (`"<Domain> Domain ~ <granted
power name>"`, e.g. `"Strength Domain ~ Strength Surge"`) is a DIFFERENT corpus record from
`data/corpus/core_rulebook/class_feature/domain_power/*.json` (`"Domain Power ~ <granted power
name>"`), the chassis record `probe_domain_power_effect_wiring` already proves wired for exactly
the five domains `domain_power::DOMAIN_POWER_CATALOG` carries a real formula for (Good/War/
Strength/Destruction/Glory). Confirmed by reading both files directly. Same shape cycle 1's
Favored Enemy/Terrain fix used: `group` (`"<Domain Name> Domain"`) can never equal `"cleric"`, so
ordinary owner resolution can never ground this record; the sibling chassis record's already-
proven wiring is the only real attribution path. New rung in `classify()`, reuses
`domain_power_effect_wired`, **no new probe**.

Corpus-wide population check (not just `core_rulebook`) before implementing, to confirm the fix's
real reach:

```
python3 -c "
import json
inv = json.load(open('docs/work-inventory.json'))
target_powers = {'Touch of Good','Battle Rage','Strength Surge','Destructive Smite','Touch of Glory'}
units = [u for u in inv['units'] if u.get('kind')=='class_feature'
         and u.get('status')=='engine-does-not-hold'
         and u.get('evidence')=='no_explanation_id_and_no_diagnostic_names_this_feature']
matches = [u for u in units if ' ~ ' in (u.get('corpus_key') or '')
           and (u['corpus_key'].split(' ~ ',1)[0]).endswith('Domain')
           and u['corpus_key'].split(' ~ ',1)[1] in target_powers]
print(len(matches))
"
```

Result: **3**, all `core_rulebook` (`Strength Domain ~ Strength Surge`, `Destruction Domain ~
Destructive Smite`, `Glory Domain ~ Touch of Glory` -- Good/War have no bucket-C sub-power record
of their own; every other domain's sub-powers are genuinely uncomputed, no false-positive risk
from the 79 other `<X> Domain` groups the corpus carries). Confirmed generic (fires for any
book/domain sharing this exact paired-record shape and one of the five wired power names), not
book-specific.

**2. `CLASS_FEATURE_ID_MAGNITUDE_SUFFIXES` += `"size"`.** A single-word addition to the existing,
already-audited magnitude-suffix allowlist `id_matches_feature_slug_after_known_magnitude_suffix_
strip` uses. The engine already computes and emits a real, PF1-Core-Rulebook-verbatim formula
(`level/2 + Wisdom modifier`) at `"class_chassis.monk.ki_pool_size"`
(`src/rules_core/pilot_compute/mod.rs`), but its own trailing dot-segment (`ki_pool_size`) never
matched `"Monk ~ Ki Pool"`'s feature slug (`ki_pool`) because `"size"` was not yet a recognized
magnitude suffix word -- the SAME gap shape `"count"`/`"amount"`/`"total"`/`"value"` already close
for other records, not a new mechanism. Checked corpus-wide for false-positive risk before
adding: only 3 explanation ids in the whole codebase end in `_size`
(`class_chassis.monk.ki_pool_size`, `class_feature.acg.investigator.inspiration_pool_size`,
`class_feature.acg.skald.rage_powers_pool_size`, plus one unrelated `psychic_strike.die_size`) --
the ACG ids have no matching `corpus_key` in `docs/work-inventory.json` (`grep`-confirmed 0
hits for `"Inspiration Pool"`/`"Rage Powers Pool"`), so this addition's real corpus-wide effect is
exactly the one `core_rulebook` unit. No new rung -- the pre-existing generic
`explanation_id_observed_after_known_magnitude_suffix_strip` path does the crediting.

**3. New probe `probe_monk_unarmed_damage_medium_wiring`: `"Monk Unarmed Damage LVL <N>
(Medium)"`.** Cycle 1's own next-cycle plan named this cluster (54 units total in the
`other_named_group_or_standalone` sub-cause) as the next-cheapest lever, citing
`monk_unarmed_strike_damage_die` as an existing formula. Investigated fully this cycle:
`explain_monk_level1_chassis` (`src/rules_core/pilot_compute/mod.rs`) genuinely computes and
emits `"class_chassis.monk.unarmed_strike_damage_die"` for EVERY monk at EVERY level -- but,
confirmed by reading the function directly, it NEVER branches on race size at all; the
size-aware helper (`monk_unarmed_strike_damage_die_for_size`, which does cover Medium and Small)
is called ONLY by `ground_unchained_monk_unarmed_strike_damage`, an Unchained-Monk-specific path
feeding a DIFFERENT corpus record family (`class_feature.pu.unchained_monk.*`). So the real,
live-computed value behind this corpus group exists for the **Medium column only** -- 6 of the
54 units (LVL 1/4/8/12/16/20 (Medium)); the other 48 (Small plus 7 other size categories) have no
engine-computed value behind them at all, regardless of this fix.

Because the SAME explanation id fires at every level with a DIFFERENT value (the die steps up
through the table), mere id-presence -- sufficient for every other paired-record mechanism in
this file, where each type/tier/power has its own id -- cannot tell one corpus `LVL <N>` record's
claimed die face from another's here. The new probe therefore VERIFIES the observed value at each
of the six canonical levels against the exact face this level's own corpus `UDAM:` token names
(read directly from the six `data/corpus/core_rulebook/class_feature/monk_unarmed_damage_lvl_
{1,4,8,12,16,20}_medium/*.json` files, not assumed from the compute function's doc comment, though
they agree), and only credits a level when the two agree.

## The fix

Three independent additions to `src/bin/v06_work_inventory.rs`:

1. `CLASS_FEATURE_ID_MAGNITUDE_SUFFIXES` gains `"size"` (one array entry).
2. New `EngineFacts` field `monk_unarmed_damage_medium_wired: BTreeSet<u8>`, filled by new probe
   `probe_monk_unarmed_damage_medium_wiring` (loops the 6 canonical levels, runs
   `class_sweep_input(fixture, "monk", level)` through the real `compute_pilot_base_chassis`
   pipeline inside a caught panic hook -- same discipline every other probe in this file uses --
   and inserts the level only when the observed `class_chassis.monk.unarmed_strike_damage_die`
   value equals the hardcoded expected face for that level).
3. Two new `classify()` rungs inserted in the SAME `if group == "X" { ... }` chain cycle 1's
   Favored Enemy/Terrain display rungs occupy (immediately after the Favored Terrain display
   check, before the Wizard Arcane School check): the domain-display rung (`group.ends_with("
   Domain") && unit.key.contains(" ~ ")`, checks `domain_power_effect_wired`) and the Monk
   Unarmed Damage Medium rung (parses `"Monk Unarmed Damage LVL <N> (Medium)"` via
   `strip_prefix`/`strip_suffix`, checks `monk_unarmed_damage_medium_wired`).

## RED -> GREEN

RED (confirmed for the intended reason, each mechanism independently): temporarily reverted each
of the three fixes in turn (the suffix-list entry; the domain rung, via `if false &&`; the monk
rung, via `if false &&`) and re-ran that mechanism's own positive-proof test -- all three failed
with `left: "engine-does-not-hold", right: "grounded"` (the exact pre-fix fallthrough), confirming
each test fails because its own fix is absent, not for an unrelated reason. Restored all three;
diffed the restored file against the pre-RED-test backup byte-identical.

```
cargo test --locked --bin v06_work_inventory domain
running 7 tests ... test result: ok. 7 passed; 0 failed
cargo test --locked --bin v06_work_inventory monk
running 6 tests ... test result: ok. 6 passed; 0 failed
cargo test --locked --bin v06_work_inventory suffix_word
running 3 tests ... test result: ok. 3 passed; 0 failed
```

11 new tests total (3 domain display positive/negative/bare-record-untouched, 3 monk-unarmed
positive/negative/small-untouched, 1 ki-pool positive via the new suffix word, 1 suffix-strip
unit test, plus this receipt counts the pre-existing domain-power-chassis positive/negative pair
exercised alongside as regression coverage, unchanged).

Full `class_feature`-scoped suite: `cargo test --locked --bin v06_work_inventory class_feature`
-- **135 passed, 0 failed** (124 pre-existing from cycle 1 + 11 new this cycle; no regression in
any pre-existing rung).

Full binary suite: `cargo test --locked --bin v06_work_inventory` -- **443 passed, 0 failed**
(432 pre-existing at this cycle's start + 11 new).

## Territory confirmation

All three mechanisms are `class_feature`-kind explanation-id/diagnostic-naming wiring in
`v06_work_inventory.rs`'s `classify()`/`EngineFacts` -- none touch `data/corpus/equipment/**`,
`src/rules_core/pilot_compute/mod.rs`'s trait/ability formulas, or `CharacterInput`'s own
struct/fields. `probe_monk_unarmed_damage_medium_wiring` constructs its fixture via the
pre-existing `class_sweep_input` helper (unchanged), the same way every other probe in this file
already does -- it does not add, remove, or reinterpret any `CharacterInput` field.

## Live regen

Ran the guarded three-pass pipeline (`corpus_literal_sweep --json-out` ->
`derived_evaluator_fixture_check --json-out` -> `CORPUS_LITERAL_SWEEP_REPORT`/
`DERIVED_FIXTURE_CHECK_REPORT`-pointed regen), same protocol cycle 1 documented.

`corpus_literal_sweep --json-out`: `PENDING`
`derived_evaluator_fixture_check --json-out`: `1,839 unit(s) cleared over 2,580 fixture row(s); 0 failed`

`PENDING -- regen in progress`

## Figures + their re-derive commands

`PENDING`

## Row-count command output (this cycle's own artifact)

`PENDING`

## Build scope verified

`PENDING`

## Sweep population

`PENDING`

## Oracle pin

N/A -- no figure in this receipt came from the pinned PCGen oracle corpus.

## Movement, four buckets

`PENDING`

## Remainder -- named by sub-cause, populations sum exactly

`PENDING`

## Notes

`PENDING`

## Next-cycle plan

`PENDING`
