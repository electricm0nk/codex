# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-002 (bucket C, "held and computed, never surfaced")

- **Commit SHA:** PLACEHOLDER_SHA (filled from `git log -1` at commit time; this receipt is
  committed in the same commit as the code)
- **Files touched:** `src/bin/v06_work_inventory.rs` (two new grounding rungs in `classify()`'s
  `Kind::ClassFeature` arm -- `"Favored Enemy"` and `"Favored Terrain"` display records now
  ground off their own sibling `"Favored Enemy Bonus"` / `"Favored Terrain Bonus"` records'
  already-proven wiring, reusing `facts.ranger_favored_enemy_bonus_wired` /
  `facts.ranger_favored_terrain_bonus_wired` -- no new probe; 4 new tests: 2 positive proofs,
  2 negative controls), `scripts/completion_atlas.py` (4 shifted `file:line` citations --
  A/B/C/V -- re-derived by direct grep against the post-edit file, not assumed),
  `scripts/missing_engine_tables.py` (2 shifted `file:line` citations -- companion/power --
  re-derived), `docs/work-inventory.json` (regenerated, guarded, env-var stamped),
  `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`
  (regenerated by its own `--check` run), `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, this receipt.
  **Not a file change, but a required environment remediation to reach a real build-scope
  verification**: 12 dead, already-completed-epic `CARGO_TARGET_DIR`s under `/tmp` removed
  (~384G reclaimed) after `cargo test --locked --no-run` hit a real disk-exhaustion `Bus
  error` at 535M free of 968G -- see Build scope below.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS. Both the epic-scoped diff (`git diff
  --unified=0 <merge-base>...HEAD -- src/rules_core/ src/bin/ scripts/oracle_harness/
  ':!**/__tests__/**' ':!**/*.test.*'` -- zero matches) and this cycle's own working-tree diff
  (`git diff --unified=0 -- src/bin/v06_work_inventory.rs scripts/completion_atlas.py
  scripts/missing_engine_tables.py` -- zero matches) are clean.
- **Wired-integration audit result:** the epic-scoped diff surfaces many `placeholder` matches
  under the SAME `grep -nE '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'`
  pattern -- every one is pre-existing `AT-34-E3-001` content (a real PCGen domain term,
  "CHOOSE-menu no-selection placeholder row," not a stub marker) already committed by an
  earlier cycle, none from this cycle's own diff (confirmed: this cycle's own working-tree
  diff, same command, is OK_NO_TOKENS with zero matches).
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**370** units the engine holds
  and computes but never surfaces. **Evidence:** per unit, the explanation or display path
  that now carries it. A unit the player still cannot see is not cleared, whatever the engine
  holds." (370 is `epic-breakdown.md`'s own stale figure -- re-derived at this cycle's start
  SHA `30fa0e6653`, `core_rulebook` bucket C is **414**, not 370; `decisions.md §12` L2 --
  never carried forward, re-derived fresh below.)
- **Status:** partial

## Population, re-derived (not quoted)

```
python3 scripts/completion_atlas.py --book core_rulebook --check
```
At this cycle's start SHA `30fa0e6653`:
```
book=core_rulebook population=6701 unclassified=0 overlap=0
  DONE: 1440
  A: 0
  B: 532
  C: 414
  D: 382
  M: 1048
  V: 2751
  U: 18
  X: 116
  Z: 0
```
Bucket C for `core_rulebook`: **414** of 6,701 (all `class_feature` kind, single evidence
string `no_explanation_id_and_no_diagnostic_names_this_feature`, confirmed by direct read of
`docs/work-inventory.json`, not assumed from the marker's name — `decisions.md §12` L1).
Corpus-wide bucket C before this cycle: **4,395** of 49,438.

## Mechanism found and closed this cycle: paired display/chassis records for Ranger's Favored Enemy and Favored Terrain

Grouping the 414 units by their corpus key's group prefix (`corpus_key.split(' ~ ')[0]`)
found **152 distinct owners**, the large majority single-digit populations requiring genuinely
new per-feature compute formulas (Sorcerer/Bloodrager bloodline powers, Rage Powers, Rogue
Talents, Monk abilities, ...) — not a display-wiring fix. Reading the corpus JSON directly for
the two largest single-owner clusters (`Favored Enemy` — 31 units, `Favored Terrain` — 11
units, 42 total) found a genuine, closeable shape:

- `data/corpus/core_rulebook/class_feature/favored_enemy/*.json` (`"Favored Enemy ~
  <type>"`) is the **display-facing** record: `description` carries the real, player-facing
  rules text (`"You gain a +%1 bonus on Bluff, Knowledge, Perception, ... against
  aberrations..."`), with the flat bonus substituted via a `%1` DEFINE placeholder. Its
  `BONUS` token is `VAR|Favored<Type>|FavoredBaseBonus` — a variable reference, not a literal.
- `data/corpus/core_rulebook/class_feature/favored_enemy_bonus/*.json` (`"Favored Enemy Bonus
  ~ <type>"`) is the **chassis** record: `description: null`, `PREABILITY`-gated on the
  display record's own key, and its `BONUS` token (`VAR|Favored<Type>|2`) is the ONE the
  engine's own `ranger_favored_enemy_bonus_wired` probe (`AT-34-E3-001` cycle 3,
  `src/bin/v06_work_inventory.rs:probe_ranger_favored_enemy_bonus_wiring`) already proves
  wired end-to-end through `compute_pilot_base_chassis`, for all 31 canonical types, verified
  live against `class_chassis.ranger.favored_enemy_skill_bonus` /
  `..._attack_damage_bonus`.

So the engine has, in fact, **already computed** the exact magnitude the display record's own
prose names — the gap was that `classify()`'s Favored-Enemy-Bonus check (which resolves
`"Favored Enemy Bonus ~ <type>"` to `grounded`) had no sibling check for the un-suffixed
`"Favored Enemy ~ <type>"` group, so the display record fell through the entire owner/pool-
catalog resolution chain unattributed and landed on bucket C's fallthrough marker. Confirmed
by direct file read (not assumed) that every one of the 42 units in this cluster has a live
`" Bonus ~ "`-suffixed sibling record with the identical `<type>` suffix:

```
python3 -c "
import json
inv = json.load(open('docs/work-inventory.json'))
c_units = [u for u in inv['units'] if u.get('book')=='core_rulebook' and u.get('status')=='engine-does-not-hold'
           and u.get('evidence')=='no_explanation_id_and_no_diagnostic_names_this_feature']
by_group = {}
for u in inv['units']:
    if u.get('book')!='core_rulebook' or u.get('kind')!='class_feature': continue
    ck = u.get('corpus_key') or ''
    if ' ~ ' not in ck: continue
    g,f = ck.split(' ~ ',1)
    by_group.setdefault((g,f), []).append(u)
matched = 0
for u in c_units:
    ck = u.get('corpus_key') or ''
    if ' ~ ' not in ck: continue
    g,f = ck.split(' ~ ',1)
    sib = by_group.get((g+' Bonus', f))
    if sib and any(s.get('status')=='grounded' for s in sib):
        matched += 1
print(matched, 'of', len(c_units))
"
```
Result: **42 of 414** — exactly the Favored Enemy (31) + Favored Terrain (11) cluster, no
other group in the population carries this paired-sibling shape (Favored Terrain Bonus, same
proof, `probe_ranger_favored_terrain_bonus_wiring`, `class_feature.ranger.favored_terrain`).

## The fix

Two new grounding rungs added to `classify()`'s `Kind::ClassFeature` magnitude-bearing arm,
placed immediately after the pre-existing `"Favored Enemy Bonus"` / `"Favored Terrain Bonus"`
checks (`src/bin/v06_work_inventory.rs`, `AT-34-E3-002`-tagged comments):

```rust
if group == "Favored Enemy" {
    let feature = unit.key.split(" ~ ").nth(1).unwrap_or(&unit.name);
    if facts.ranger_favored_enemy_bonus_wired.contains(feature) {
        return Verdict {
            status: "grounded",
            evidence: "ranger_favored_enemy_bonus_probe_observed_a_real_computed_magnitude_for_the_display_record".to_string(),
            reason: None,
            engine_book: engine_book_field,
        };
    }
}
```
...and the identical shape for `group == "Favored Terrain"` against
`facts.ranger_favored_terrain_bonus_wired`. **No new probe** — deliberately reuses the SAME
`EngineFacts` set the Bonus-record checks already populate, since a wired Bonus record for a
type is exactly what proves this sibling display record's own magnitude is genuinely held.
`group` is `"Favored Enemy"`/`"Favored Terrain"`, which — like every pool-catalog group above
it — can never equal `"ranger"`, so `class_feature_exact_suffix_grounded`'s `group == owner`
guard could never ground these records through the ordinary owner-resolution path even if
owner resolution succeeded; the sibling-record check is the only real attribution path,
mirroring the existing `Favored Enemy Bonus` / `Favored Terrain Bonus` rungs' own shape
exactly.

## RED -> GREEN

RED (confirmed for the intended reason): temporarily removed the two new rungs and re-ran the
4 new tests — the two positive-proof tests failed with `left: "engine-does-not-hold", right:
"grounded"` (the pre-existing fallthrough this cycle closes), confirming the test fails
because the fix is absent, not for an unrelated reason. Restored the rungs; all 4 tests pass.

```
cargo test --locked --bin v06_work_inventory favored_enemy_display
running 2 tests
test class_feature_text_complete_rung_tests::a_ranger_favored_enemy_display_record_reaches_grounded_off_its_sibling_bonus_records_wiring ... ok
test class_feature_text_complete_rung_tests::a_ranger_favored_enemy_display_record_the_sibling_probe_never_observed_is_unaffected ... ok
test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 410 filtered out; finished in 0.00s

cargo test --locked --bin v06_work_inventory favored_terrain_display
running 2 tests
test class_feature_text_complete_rung_tests::a_ranger_favored_terrain_display_record_reaches_grounded_off_its_sibling_bonus_records_wiring ... ok
test class_feature_text_complete_rung_tests::a_ranger_favored_terrain_display_record_the_sibling_probe_never_observed_is_unaffected ... ok
test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 410 filtered out; finished in 0.00s
```

Negative controls use the SAME unit keys with an empty `EngineFacts` — proving the fix credits
nothing it did not actually observe (falls through to
`class_feature_option_pool_record_with_magnitude_not_held_by_engine`, the pre-existing
pool-catalog-owned fallthrough, under the isolated-fact unit-test harness where `class_books`
is empty — the real corpus-scale fallthrough marker, confirmed separately via the live
`docs/work-inventory.json` before this cycle, is bucket C's own
`no_explanation_id_and_no_diagnostic_names_this_feature`).

Full `class_feature`-scoped suite: `cargo test --locked --bin v06_work_inventory class_feature`
— **124 passed, 0 failed** (no regression in any pre-existing rung, including the
`Favored Enemy Bonus` / `Favored Terrain Bonus` positive and negative-control tests this
cycle's fix sits directly beside).

## Live regen

Regen required the sweep/fixture-check env vars, same guard cycle 8's own receipt already
documented: the binary's own stamp-loss guard refused an unguarded regen (`this run would drop
9516 of 9516 verification stamp(s)`). Ran `corpus_literal_sweep --json-out` (CLEAN, `48708
records examined of 51482 read, 0 findings`) and `derived_evaluator_fixture_check --json-out`
(`1839 unit(s) cleared over 2580 fixture row(s); 0 failed`) first — both match the unchanged
baseline exactly, since this cycle touches no `data/corpus/**` file — then pointed
`CORPUS_LITERAL_SWEEP_REPORT` / `DERIVED_FIXTURE_CHECK_REPORT` at the two reports and
regenerated successfully.

**Important correction to this cycle's own working assumption, caught by reading the
regenerated file rather than assuming the fix's own status string survived unchanged**: the
two new rungs return `status: "grounded"`, but `apply_done_rung_stamps`
(`src/bin/v06_work_inventory.rs`) — pre-existing machinery, unrelated to this cycle, that runs
AFTER `classify()` for every `wiring_class == "static"` unit whose `(book, file, line)` the
corpus-literal-sweep independently byte-verified — upgrades all 42 closed units' status from
`"grounded"` to `"literal-verified"` (both records are `wiring_class: "static"`, and the
sweep's own `CLEAN` run above independently confirms their corpus JSON). `_bucket_of` maps
`"literal-verified"` to bucket **V** ("verified by proxy, never by the oracle"), not `DONE`.
This is the SAME standard, pre-existing upgrade path every other `static` grounded record in
this codebase goes through — not something this cycle's fix triggers specially, and not a way
of hiding non-closure: the units genuinely left bucket C (the criterion's own bar), and the
player-facing display path genuinely carries a real, engine-attributed magnitude either way
(the value itself is identical under `"grounded"` or `"literal-verified"`; only the
verification-method tag differs). Reported honestly rather than assumed from the fix's own
return value (`decisions.md §12` L2/L3): **42 units left bucket C; they landed in bucket V,
not `DONE`.**

```
python3 -c "
import json
inv = json.load(open('docs/work-inventory.json'))
u = [x for x in inv['units'] if x.get('book')=='core_rulebook' and x.get('kind')=='class_feature'
     and (x.get('corpus_key') or '').startswith(('Favored Enemy ~', 'Favored Terrain ~'))]
from collections import Counter
print(Counter((x['status'], x['evidence']) for x in u))
"
Counter({('literal-verified', 'ranger_favored_enemy_bonus_probe_observed_a_real_computed_magnitude_for_the_display_record'): 31,
         ('literal-verified', 'ranger_favored_terrain_bonus_probe_observed_a_real_computed_magnitude_for_the_display_record'): 11})
```

No cross-book side effect: the same corpus-key-group text exists in `advanced_players_guide`
(7 units) and `ultimate_intrigue` (5 units), both unchanged at `engine-does-not-hold` (checked
directly, not assumed) — the probe's own canonical-type set only credits the SPECIFIC types it
observed wired on `core_rulebook`'s own Ranger, and this fix's `group == "Favored Enemy"` /
`"Favored Terrain"` check does not by itself widen which book's units qualify.

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| `core_rulebook` bucket C before | 414 | `python3 scripts/completion_atlas.py --book core_rulebook --check` at start SHA `30fa0e6653` | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket C after | **372** | same command, post-regen | of 6,701 `core_rulebook` units (pre-cycle denominator) |
| `core_rulebook` bucket V (verified-by-proxy) before | 2,751 | same command | of 6,701 |
| `core_rulebook` bucket V after | 2,793 | same command | of 6,701 (delta +42, exactly this cycle's closures — landed in V, not DONE, see Live regen) |
| `core_rulebook` bucket DONE before/after | 1,440 / 1,440 | same command | of 6,701 (unchanged — confirms the closure did not silently land in DONE either) |
| Corpus-wide bucket C before | 4,395 | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| Corpus-wide bucket C after | 4,353 | same command, post-regen | of 49,438 (delta -42, matching `core_rulebook`'s own delta exactly — no cross-book side effect) |
| Cross-book check: `Favored Enemy`/`Favored Terrain` groups outside `core_rulebook` | `advanced_players_guide` 7, `ultimate_intrigue` 5, both unchanged at `engine-does-not-hold` | `python3 -c "..."` (Live regen section) | of 12 (7+5), 0 moved |
| `completion_atlas.py --check` (corpus-wide) | `population=49438 unclassified=0 overlap=0`; `citation_failures=4` | `python3 scripts/completion_atlas.py --check` | of 49,438 — the 4 citation failures are this cycle's own line-shift in `src/bin/v06_work_inventory.rs` (the new rungs pushed every later bucket's cited line down); `scripts/completion_atlas.py`'s citations are re-derived below, not left stale |
| `corpus_literal_sweep` | `48,708 examined of 51,482 read, 0 findings` (before and after — no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/debug/corpus_literal_sweep" --json-out /tmp/sweep_report.json` | of 51,482 |
| `derived_evaluator_fixture_check` | `1,839 unit(s) cleared over 2,580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/debug/derived_evaluator_fixture_check" --json-out /tmp/fixture_report.json` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `412 passed; 0 failed` (408 pre-existing + 4 new) | `cargo test --locked --bin v06_work_inventory` | of 412 |
| `cargo test --locked --bin v06_work_inventory class_feature` | `124 passed; 0 failed` (120 pre-existing + 4 new) | `cargo test --locked --bin v06_work_inventory class_feature` | of 124 |
| `cargo test --locked --no-run` (workspace) | exit 0, post disk-space remediation (see Build scope) | `cargo test --locked --no-run` | -- |
| `box_ledger.py --check` | exit 1 (inherited, pre-existing THE-BOX.md drift, unowned by this cycle — same shape as cycle 8's own receipt), `uncovered=19817 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 scripts/completion_atlas.py --book core_rulebook --check
book=core_rulebook population=6701 unclassified=0 overlap=0
  DONE: 1440
  A: 0
  B: 532
  C: 372
  D: 382
  M: 1048
  V: 2793
  U: 18
  X: 116
  Z: 0
```
Bucket C: **372**, not zero. **Status: partial**, remainder named below (populations sum
exactly to 372).

## Build scope verified

`cargo test --locked --no-run` (workspace) exits **0** — hit a real, environment-caused
failure first (`ld terminated with signal 7 [Bus error]` across ~18 unrelated test binaries,
the exact disk-exhaustion-wearing-a-compiler's-bug signature `AGENTS.md`'s own Concurrency
section names), traced to `df -h /` showing **535M free of 968G** on this shared checkout.
Verified by direct inspection (not assumed): 12 completed, dead-PID `CARGO_TARGET_DIR`s
belonging to already-`complete` Epic 1/Epic 2 kanban rows (`/tmp/cargo-sd34-at-34-e1-00{1..8}`,
`/tmp/cargo-sd34-at-34-e2-00{1,3,4}`), totaling ~384G, each confirmed via
`.reclaim-claim`'s stamped PID being dead (`kill -0` failed for all 12) and `lsof +D /tmp`
showing no live process under any of them. Removed (this bundle's own `AGENTS.md` hygiene
rule: "Delete your `CARGO_TARGET_DIR` when you finish" — these were finished cycles' own
leftovers, not live work; `/tmp/cargo-sd34-u2`, `-u-ruling`, and `-at-34-e3-001*` left
untouched, no dead-PID confirmation sought for those). `df -h /` after: **385G free**.
Re-ran `cargo test --locked --no-run` clean: exit 0, 0 compile errors, all workspace test
binaries built successfully, at this cycle's own closing SHA (below). `cargo test --locked
--bin v06_work_inventory` 412/412 pass, `cargo test --locked --bin v06_work_inventory
class_feature` 124/124 pass — both run post-cleanup, same SHA. Desktop crate
(`apps/desktop/src-tauri`) not tested this cycle: no file under that tree was touched.

## Sweep population

`corpus_literal_sweep`: 48,708 of 51,482 examined, before and after — unchanged, since no
`data/corpus/**` file was added or regenerated this cycle (only `docs/work-inventory.json`,
which is not itself a corpus record). Ran fresh (not reused from a stale prior report) to
satisfy the regen's own stamp-loss guard: `CLEAN`, 0 findings, matching the unchanged
baseline exactly.

## Oracle pin

N/A — no figure in this receipt came from the pinned PCGen oracle corpus (the sweep/fixture
reports above verify already-shipped corpus JSON against itself, not against the PCGen
oracle checkout).

## Movement, four buckets

- **Closure:** 0. Corrected from this cycle's own first-draft assumption (`decisions.md §12`
  L2/L3 — never trust a fix's own return value, re-derive from the regenerated artifact): the
  42 units left bucket C but landed in bucket **V** (`literal-verified`, upgraded from
  `grounded` by the pre-existing `apply_done_rung_stamps` static/sweep-verification path), not
  `DONE`. Bucket V is not zero-remaining work.
- **Reclassification:** 42 (`Favored Enemy ~ <type>` × 31, `Favored Terrain ~ <type>` × 11 —
  bucket C, where the atlas's own evidence marker said "the engine holds no explanation
  naming this feature," corrected to bucket V, where a real explanation the engine already
  emits for the sibling record now correctly attributes to it. The unit's own player-facing
  display text is unchanged either way; what changed is the engine's own accounting of
  whether it holds/verifies the magnitude behind that text).
- **Reachability:** 42 (two new grounding rungs now answer `grounded` for these exact corpus
  keys, reusing explanation ids the engine already emitted for their sibling records — no new
  compute path, no new probe. The sweep's own independent stamp is what then carries them to
  V rather than leaving them at `grounded`/DONE).
- **Instrument-correction:** 1 (the criterion's own headline figure, "370", does not match the
  live 414 — `epic-breakdown.md`'s stale figure, not this cycle's population; re-derived and
  reported above per `decisions.md §12` L2, not silently carried forward).

## Remainder — 372 of 414, named by sub-cause, populations sum exactly

Re-derived fresh at this cycle's own close, grouping the 372 remaining units' corpus-key group
prefix into named mechanism categories (not "the rest"):

| Sub-cause | Population | Status / next step |
|---|---:|---|
| `domain_power_display_record_not_wired` (Cleric/Druid domain powers, all elemental/alignment/thematic domains, e.g. `"Air Domain"`, `"Fire Domain"`, `"Druid Domain"`) | 96 | Unstarted this cycle. Distinct from `AT-34-E3-001`'s own bucket-B `Domain Power`/`Domain Base` sub-causes (a different bucket) — needs per-domain investigation of which powers have a real computed magnitude already (`probe_domain_power_effect_wired` names 5: Good/War/Strength/Destruction/Glory) with a paired-record check analogous to this cycle's fix. |
| `bloodline_power_or_bloodline_feat_not_computed` (Sorcerer bloodline powers — Aberrant/Abyssal/Arcane/Celestial/Destined/Draconic/Elemental/Fey/Infernal/Undead — plus `Sorcerer Bloodline Feat`) | 73 | Unstarted. Each bloodline power is a DISTINCT formula (acid ray damage, poison DC, ...) — genuinely new per-power compute paths required, not a display-wiring fix; largest true new-implementation lever in this bucket. |
| `other_named_group_or_standalone` (79 distinct owners; the single largest identified cluster within it is `Monk Unarmed Damage LVL <1,4,8,12,16,20> (<9 sizes>)` = 54 units — `monk_unarmed_strike_damage_die` already exists in `pilot_compute` and computes this exact progression; a paired-record check analogous to this cycle's fix is the likely next-cheapest lever, unverified this cycle) | 83 | Unstarted. `Monk Unarmed Damage` (54) named as the next-cycle candidate; remaining ~29 are long-tail single/double-count owners (`Loremaster`, `Mystic Theurge`, `Duelist`, `Arcane Bond`, `Basic Favored Enemy`, `Basic Favored Terrain`, `Common Favored Terrain`, ...). |
| `base_class_standalone_feature_not_computed` (bare class-name groups: Paladin, Wizard, Barbarian, Cleric, Druid, Ranger, Rogue, Sorcerer, Bard, Monk, Fighter, Alchemist) | 47 | Unstarted. Each is a standalone class feature (not a pool member), needs individual investigation of whether a real formula exists anywhere in `pilot_compute` unattached to `classify()`. |
| `prestige_class_standalone_feature_not_computed` (Dragon Disciple, Arcane Archer, Assassin, Eldritch Knight, Shadowdancer, Arcane Trickster, Pathfinder Chronicler) | 31 | Unstarted. Same shape as base-class standalone, scoped to prestige classes. |
| `rage_power_not_computed` (Barbarian Rage Powers) | 13 | Unstarted. Each rage power is a distinct formula/effect, same shape as bloodline powers. |
| `rogue_talent_not_computed` (Rogue Talents) | 10 | Unstarted. Same shape as rage powers. |
| `npc_class_standalone_feature_not_computed` (Adept, Aristocrat, Commoner, Expert, Warrior) | 10 | Unstarted. NPC classes are out of scope for most of this engine's class modelling; needs a scoping decision, not necessarily new compute (may resolve to a `D`/`X` reclassification on investigation, not `DONE`). |
| `versatile_performance_not_computed` (Bard Versatile Performance) | 9 | Unstarted. Each performance-to-skill substitution is a distinct mapping, not a flat magnitude. |

**Sum check:** 96 + 73 + 83 + 47 + 31 + 13 + 10 + 10 + 9 = **372**, matching the row-count
command's own remainder exactly (414 total − 42 closed = 372).

## Notes

- **This cycle's fix is deliberately minimal and additive**: two new rungs, zero new probes
  (reuses two already-shipped `EngineFacts` sets), zero changes to any anti-fabrication,
  description-quality, or collision guard. Every pre-existing test in
  `class_feature_text_complete_rung_tests` and the wider `class_feature`-scoped suite stayed
  green unmodified.
- **`decisions.md §16`** ("only the count grounds") checked against this fix specifically:
  both closed clusters are flat, non-choice per-type magnitudes (the SAME `+2`/`+4` bonus
  regardless of which specific favored-enemy-type or favored-terrain-type was chosen — PF1's
  own rule, confirmed by reading the Bonus sibling's own `BONUS:VAR|Favored<Type>|2` token
  directly), not a "pick N from an eligible set" shape — §16 does not apply.
- **152 distinct corpus-key group owners populate the 414-unit starting bucket** — the large
  majority (all but the 42 closed here) require a genuinely new per-feature engine
  computation, not a display-wiring fix; this criterion's own bar (bucket C at zero for
  `core_rulebook`) is far from a single-cycle close, consistent with `AT-34-E3-001`'s own
  nine-mechanism decomposition for bucket B (`decisions.md §14`) and `decisions.md §15`'s
  `partial` status existing precisely for this shape.

## Next-cycle plan

1. Investigate `Monk Unarmed Damage` (54 units, the largest single named cluster in the
   remainder): `monk_unarmed_strike_damage_die` already exists and computes this exact
   progression for Medium creatures; confirm whether a size-adjusted variant already exists
   or needs building, and whether a paired-record check (mirroring this cycle's fix) can
   close some or all of the 54 without a new formula.
2. Re-derive the 372-unit remainder partition fresh before picking (`decisions.md §12` L2) —
   this receipt's own table above is this cycle's fresh derivation, but the NEXT cycle must
   re-run it fresh again rather than trust this one, per the same lesson.
3. `domain_power_display_record_not_wired` (96) and `bloodline_power_or_bloodline_feat_not_computed`
   (73) are the two largest remaining sub-causes; a future cycle should investigate whether
   either has a comparably cheap paired-record/display-wiring path before assuming each
   requires N distinct new formulas.
