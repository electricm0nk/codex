# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-001 (`class_feature_option_pool_record_with_magnitude_not_held_by_engine` mechanism, cycle 6)

- **Commit SHA:** `df94dd024c`
- **Files touched:** `src/rules_core/pilot_compute/mod.rs` (new
  `wizard_has_canonical_transmutation_selection`, one new computation block
  gated on it -- Telekinetic Fist bonus-damage + uses-per-day, Physical
  Enhancement flat bonus, Change Shape rounds-per-day, 4 new
  `ComputationExplanation` pushes), `src/bin/v06_work_inventory.rs`
  (`probe_wizard_arcane_school_wiring` gained a third variant, swapping
  BOTH the specialization AND the opposed-schools choice, 3 new tests,
  updated doc comments), `scripts/completion_atlas.py` (10 shifted
  `file:line` citations, re-derived twice -- see Discoveries),
  `scripts/missing_engine_tables.py` (2 shifted `file:line` citations,
  re-derived -- also self-healed a PRE-EXISTING 8-line drift in this
  script's own citations, present at this cycle's own start HEAD,
  unrelated to this cycle's edits), `docs/work-inventory.json`
  (regenerated, guarded), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`,
  `.../missing-engine-tables.json` (regenerated by their own `--check` runs),
  `docs/release/SD-34-book-completion/progress.md`,
  `docs/release/SD-34-book-completion/kanban.md`, this receipt,
  `docs/retro/events/sd34-at-34-e3-001.jsonl`.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS`
- **Wired-integration audit result:** `OK_NO_TOKENS` (the wider file-touch-set
  audit surfaces 6 matches of the word "placeholder" -- all pre-existing, from
  earlier committed cycles' own `vacuous-placeholder` sub-cause work; never a
  stub this cycle introduced; reviewed, not self-healed, the same finding
  cycles 2-5's own receipts each already made)
- **Acceptance criterion (verbatim, `epic-breakdown.md`):** "**970** Core
  Rulebook units whose table exists but which are not in it. Evidence: the
  atlas reporting bucket B at zero for `core_rulebook`, and the mechanism
  that placed them named -- by mechanism, not per record." (970 is
  `epic-breakdown.md`'s own stale, whole-bucket figure; this cycle owns
  exactly ONE of the nine mechanisms `decisions.md §14` names, and does not
  itself close the bucket.)
- **Status:** partial

Bucket B for `core_rulebook` is nine distinct mechanisms (`decisions.md
§14`). This cycle owns exactly one:
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` --
continuing cycle 5's own work (`..._cycle_receipt_5.md`, 277 -> 267).

## Population, re-derived (not quoted)

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
```
Before this cycle: **267** of 562 `core_rulebook` bucket-B units (re-derived
at this cycle's own start SHA `2829c89e18`, matching cycle 5's own closing
figure exactly -- no drift to correct; 562, not the stale 1,006 the brief
quotes, is the atlas-real `core_rulebook` bucket-B denominator, confirmed
via `python3 scripts/completion_atlas.py --check --book core_rulebook`).
Corpus-wide (37 books) before: **2,985** of 49,438 units.

## Mechanism-specific direction followed

Read cycle 5's own receipt in full before touching anything
(`artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_5.md`),
continuing from its own named remainder rather than re-deriving it. Its own
"Next-cycle plan" named two levers: `Secret Lore` (10, briefly investigated
this cycle by reading all 10 corpus records directly -- each carries a
real, distinct `BONUS:VAR` formula, but the engine has ZERO existing
Loremaster prestige-class chassis to hang them on, a materially bigger lift
than either Bardic Performance or a wizard school; deferred, named again in
Next-cycle plan below) and "one additional wizard school, built
end-to-end". This cycle took the second, cheaper lever: **Transmutation**.

## The real fix: read the engine's own Transmutation-school corpus records directly before building anything

Reading `data/corpus/core_rulebook/class_feature/transmutation_school/*.json`
directly (never assumed) found the SAME shape cycle 4's own Wizard
Evocation/Abjuration finding and cycle 5's own Bardic Performance finding
both already established: real, non-fabricated `BONUS:VAR` formulas the
engine had never wired to `classify()`, because the corpus record's `group`
prefix (`"Transmutation School"` / `"Physical Enhancement"`) can never
equal `"wizard"`.

**New formulas built this cycle** (`src/rules_core/pilot_compute/mod.rs`),
gated on a new `wizard_has_canonical_transmutation_selection` (mirroring
`wizard_has_canonical_abjuration_selection` exactly, one school swapped,
opposing Necromancy + Evocation -- both already-existing selection
constants):

- **Telekinetic Fist** (`Transmutation School ~ Telekinetic Fist`) --
  `TransmutationTelekineticFistBonus|TransmutationSchoolLVL/2` (bonus
  damage) and `TransmutationTelekineticFistTimes|ArcaneSchoolPowerTimes`
  (uses/day, the same "3 + Intelligence modifier" idiom Force Missile /
  Protective Ward already use), both unlocked from level 1.
- **Physical Enhancement** (`Transmutation School ~ Physical Enhancement`,
  plus the three ability-score sub-choice records it auto-grants --
  `Physical Enhancement ~ Constitution` / `~ Dexterity` / `~ Strength`) --
  `TransmutationPhysicalEnhancementBonus|min(5,(TransmutationSchoolLVL/5)+1)`,
  unlocked from level 1. The three sub-choice records apply this SAME
  magnitude to whichever one physical ability score the player chose that
  day; the choice of WHICH stat is not modeled (the same
  "standalone, not wired into `ability_modifiers`" idiom Abjuration's
  Protective Ward deflection bonus already uses), but the shared bonus
  magnitude genuinely is, so all four records ground on one explanation id.
- **Change Shape** (`Transmutation School ~ Change Shape`) --
  `TransmutationChangeShapeRounds|TransmutationSchoolLVL`, gated on the
  power's own level-8 unlock (`PREVARLT:Wizard_CF_SchoolPower8,1`).

**Deliberately NOT claimed**, consistent with the Evocation/Abjuration
precedent: the top-level `"Transmutation School"` recognition record and
`"Transmutation Opposition School"` record carry only the shared,
non-numeric `Pool_ArcaneOppositionSchool`/`OppositionalSchool` bookkeeping
every school carries identically -- no per-record formula, no explanation
id, not this mechanism's shape.

## The probe

Extended `probe_wizard_arcane_school_wiring` (`src/bin/v06_work_inventory.rs`)
with a third variant. **Real defect found and fixed within this cycle's own
work, before committing** -- see Discoveries.

`classify()`'s pre-existing `wizard_arcane_school_wired` check (unchanged
this cycle -- exact `unit.key` `BTreeSet` membership, not the substring
match the mechanism's own COLLISION HAZARD warns about) now also resolves
the corrected set. Doc comments naming "the two schools" updated to name
all three (Evocation, Abjuration, Transmutation).

## RED -> GREEN

RED (confirmed for the intended reason): before this cycle's probe
extension, all 6 `core_rulebook` target records fell through the same
owner-resolution path every sibling record does (`group` can never equal
`"wizard"`), landing at `engine-does-not-hold` /
`class_feature_option_pool_record_with_magnitude_not_held_by_engine` -- the
live corpus data itself is the RED proof (`docs/work-inventory.json` at
this cycle's start SHA lists exactly this verdict for all 6).

GREEN: three new tests added to `class_feature_text_complete_rung_tests`:
- `a_wizard_transmutation_school_record_the_probe_observed_reaches_grounded`
  -- probe fact seeded with `"Transmutation School ~ Telekinetic Fist"`;
  asserts `status == "grounded"`, the pre-existing
  `wizard_arcane_school_probe_observed_a_real_computed_magnitude` evidence
  string (the SAME evidence id Evocation/Abjuration already use -- one
  probe, one evidence string, three schools).
- `a_transmutation_physical_enhancement_stat_choice_record_the_probe_observed_reaches_grounded`
  -- same proof for `"Physical Enhancement ~ Strength"`, a DIFFERENT
  `corpus_key` group prefix grounded on the SAME shared magnitude
  explanation id.
- `a_wizard_transmutation_school_record_the_probe_never_observed_is_unaffected`
  -- negative control, empty `EngineFacts`, uses the top-level
  `"Transmutation School"` recognition record itself (deliberately never
  claimed); asserts the pre-existing `engine-does-not-hold` verdict is
  unchanged, doubling as proof the probe's own bound is honored.

```
cargo test --locked --bin v06_work_inventory transmutation
running 3 tests
test class_feature_text_complete_rung_tests::a_transmutation_physical_enhancement_stat_choice_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_transmutation_school_record_the_probe_observed_reaches_grounded ... ok
test class_feature_text_complete_rung_tests::a_wizard_transmutation_school_record_the_probe_never_observed_is_unaffected ... ok
test result: ok. 3 passed; 0 failed
```

Live regen confirms **9** corpus-wide records moved -- 6 in `core_rulebook`
(this mechanism's own scope) plus **3 in `advanced_class_guide`** (an
honestly-reported side effect, NOT double-counted against this criterion:
Arcanist's own "Arcanist School Transmutation" exploit's corpus record
`ABILITY`-grants the identical `"Transmutation School ~ Physical
Enhancement"` key, which auto-grants the identical
`"Physical Enhancement ~ {Constitution,Dexterity,Strength}"` sub-choice
records `advanced_class_guide` also carries under the SAME corpus_key
strings -- the probe/`classify()` check is `unit.key`-keyed, not
book-scoped, the same idiom every prior cross-book side effect in this
mechanism's own history already used, e.g. `companion_absent` cycle 5's
own `ultimate_magic`/`book_of_the_damned_volume_1` side effect):

```
python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
w = [x for x in d['units'] if x.get('evidence','')=='wizard_arcane_school_probe_observed_a_real_computed_magnitude'
     and (x['corpus_key'].startswith('Transmutation School') or x['corpus_key'].startswith('Physical Enhancement'))]
print(sorted((x['book'], x['corpus_key']) for x in w))
print(len(w))
"
[('advanced_class_guide', 'Physical Enhancement ~ Constitution'), ('advanced_class_guide', 'Physical Enhancement ~ Dexterity'),
 ('advanced_class_guide', 'Physical Enhancement ~ Strength'), ('core_rulebook', 'Physical Enhancement ~ Constitution'),
 ('core_rulebook', 'Physical Enhancement ~ Dexterity'), ('core_rulebook', 'Physical Enhancement ~ Strength'),
 ('core_rulebook', 'Transmutation School ~ Change Shape'), ('core_rulebook', 'Transmutation School ~ Physical Enhancement'),
 ('core_rulebook', 'Transmutation School ~ Telekinetic Fist')]
9
```

## Discoveries

**Shared checkout collision hazard: an unrelated concurrent lane's WIP was
present in the working tree at this cycle's start, in files this mechanism
also edits.** `git status` at the very start of this cycle showed
`src/bin/v06_work_inventory.rs`, `scripts/completion_atlas.py`, and
`docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`
already dirty with an operator-ruling U-bucket lever (`decisions.md §17`)
another concurrent lane (`sd34-u-done`) was actively building, last
modified minutes before this cycle started. Never discarded: backed up each
file's own diff as a patch to the scratchpad, restored each file to its
committed `HEAD` content via a read-only `git show`/copy path (never
`git checkout --`, which this session's own auto-mode classifier correctly
refused as a destructive-discard pattern), did this mechanism's own work on
the clean base, and this receipt intentionally leaves the other lane's WIP
un-restored and untouched in this cycle's own commit -- it remains for that
lane's own session to find and commit, exactly as it stood before this
cycle started (verified: `git diff` against this cycle's own commit touches
only the files this receipt names).

**Probe defect, caught by this cycle's own before/after diff before
committing (the mechanism's own COLLISION HAZARD discipline, applied to a
different failure shape than the one it names).** The first probe variant
only swapped the specialization choice (mirroring the Abjuration variant's
own code shape exactly), which left the opposed-schools choice at
`canonical_seeds_for("wizard")`'s own seeded default (Necromancy +
Transmutation) -- nonsensical once Transmutation itself becomes the
specialty (a school can never oppose itself), and this NEVER satisfied
`wizard_has_canonical_transmutation_selection`'s own opposed-pair
precondition, so the probe silently observed nothing. A first full guarded
regen (`CORPUS_LITERAL_SWEEP_REPORT`/`DERIVED_FIXTURE_CHECK_REPORT`
supplied) confirmed the mechanism population held at 267, not the predicted
261 -- caught BEFORE committing by checking the six target `corpus_key`s
directly against the regenerated inventory, never by trusting the
predicted delta alone. Root-caused (the opposed-schools choice was never
swapped) and fixed by ALSO retaining/re-pushing
`"choice:wizard_opposed_schools"` with Necromancy + Evocation; verified
with a throwaway scratch test (`scratch_transmutation_probe_against_real_fixture`,
calling the REAL probe against the REAL fixture directly -- deleted before
committing, never shipped) before spending another ~11 minutes on a full
regen to confirm. The corrected regen closed exactly the 9 records
predicted (6 core_rulebook + the honest 3-unit `advanced_class_guide` side
effect, see RED -> GREEN above). This also caused a SECOND round of
citation drift in `completion_atlas.py` (the probe fix added 20 lines
beyond the first version's 39, and the later-deleted scratch test's
temporary presence had shifted a first round of re-derived citations that
then had to be re-derived AGAIN after the scratch test's own removal) --
both rounds caught by `--check`'s own `citation_failures` count before
committing, never assumed correct from having "already fixed it once".

**Regen wall-clock cost, named for future cycles' own time budgeting.**
The full guarded regen (`cargo run --bin v06_work_inventory`, debug
profile, workspace-scale) took roughly 10-13 minutes of single-core CPU
time each of the three times this cycle ran it to completion (a bare run
that hit the stamp-loss guard, a corrected re-run that revealed the probe
defect, and the final corrected re-run) -- markedly longer than earlier
cycles' own receipts imply, and NOT this cycle's own regression (the very
first, UNMODIFIED attempt at this cycle's own start SHA took the same
order of magnitude before any of this cycle's own edits existed). Named
here as a real, load-bearing planning fact for future cycles' own §2.5
"generous timeout" budgeting; not investigated further, out of this
mechanism's own scope.

**`--allow-stamp-loss` guard fired on this cycle's own first regen attempt,
self-healed per its own documented remediation.** Neither
`CORPUS_LITERAL_SWEEP_REPORT` nor `DERIVED_FIXTURE_CHECK_REPORT` was set on
the first attempt, and the guard correctly refused to write (would have
dropped 9,516 of 9,516 verification stamps). Generated both reports fresh
(`corpus_literal_sweep --json-out`, `derived_evaluator_fixture_check --json-out`,
both built `--release` this cycle for speed) and re-ran with both env vars
set; the guard passed cleanly on every subsequent regen.

**Unrelated pre-existing test failure, NOT this cycle's to fix, re-flagged
for the owning cycle (unchanged from cycles 3-5's own receipts).**
`cargo test --locked --test v06_work_inventory` still fails one test on
`core_rulebook:class_feature:empty_selection_standard_barbarian` -- a
different sibling mechanism's own fix, confirmed present in
`docs/work-inventory.json` before this cycle touched anything, unrelated to
Transmutation.

## Figures + their re-derive commands

| Figure | Value | Command | Denominator |
|---|---|---|---|
| Mechanism population before | 267 | `python3 -c "..."` (above) against `docs/work-inventory.json` at start SHA `2829c89e18` | of 562 `core_rulebook` bucket-B units |
| Mechanism population after | **261** | same command against the regenerated `docs/work-inventory.json` | of 562 `core_rulebook` bucket-B units (pre-cycle denominator) |
| Corpus-wide mechanism population before | 2,985 | same command, no `book` filter | of 49,438 units |
| Corpus-wide mechanism population after | 2,976 | same command, no `book` filter | of 49,438 units (delta -9, exactly this cycle's 9 closures) |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) before | 562 | `python3 scripts/completion_atlas.py --check --book core_rulebook` at start SHA | of 6,701 `core_rulebook` units |
| `core_rulebook` bucket B (whole book, all 9 mechanisms) after | 556 | `python3 scripts/completion_atlas.py --check --book core_rulebook` | of 6,701 `core_rulebook` units |
| Units closed (corpus-wide) | 9 (6 core_rulebook + 3 advanced_class_guide side effect) | `evidence == "wizard_arcane_school_probe_observed_a_real_computed_magnitude"`, Transmutation subset | of 267 (core_rulebook), 2,985 (corpus-wide) |
| `completion_atlas.py --check` (corpus-wide) | `population=49438 unclassified=0 overlap=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` | of 49,438 |
| `missing_engine_tables.py --check` | `citation_failures=0` | `python3 scripts/missing_engine_tables.py --check` | of 449 |
| `corpus_literal_sweep` | `48708 examined of 51482 read, 0 findings` (before and after -- no `data/corpus/**` file touched this cycle) | `"$CARGO_TARGET_DIR/release/corpus_literal_sweep" --json-out ...` | of 51,482 |
| `derived_evaluator_fixture_check` | `1839 unit(s) cleared over 2580 fixture row(s); 0 failed; 0 not ingested` | `"$CARGO_TARGET_DIR/release/derived_evaluator_fixture_check" --json-out ...` | of 2,580 fixture rows |
| `cargo test --locked --bin v06_work_inventory` | `400 passed; 0 failed` | `cargo test --locked --bin v06_work_inventory` | of 400 |
| `cargo test --locked --lib` | `2896 passed; 0 failed; 14 ignored` | `cargo test --locked --lib` | of 2910 |
| `cargo test --locked --no-run` (workspace) | exit 0 | `cargo test --locked --no-run` | -- |
| `cargo test --locked --test v06_work_inventory` | `15 passed; 1 failed (pre-existing, unrelated -- see Discoveries); 1 ignored` | `cargo test --locked --test v06_work_inventory` | of 17 |
| `F1` (`shape_ledger.py` rollup) | 5400 -> 5400 (unchanged, checked not assumed -- none of this cycle's formulas are bare-literal) | `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` | of not-done units after this cycle's regen |
| `box_ledger.py --check` | 6 stale-count WARNINGs (inherited THE-BOX.md drift, unowned by this mechanism, pre-existing, unchanged shape from cycle 5), `uncovered=19873 oracle_disagreement=0 unverifiable_done=0 stale=False` | `python3 scripts/box_ledger.py --check` | of 49,438 |
| `denominator_gate.py --check` | `files_checked=15 violations=0` | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` | of 15 files |

## Row-count command output (this cycle's own artifact)

```
$ python3 -c "
import json
d = json.load(open('docs/work-inventory.json'))
u = [x for x in d['units'] if x['book']=='core_rulebook' and x['status']=='engine-does-not-hold'
     and x['evidence']=='class_feature_option_pool_record_with_magnitude_not_held_by_engine']
print(len(u))
"
261
```
Not zero. **Status: partial**, remainder named below.

## Build scope verified

`cargo test --locked --no-run` exit 0, workspace-wide, run at this cycle's
own commit SHA (see `commit_sha` in the structured return). `cargo test
--locked --lib`: 2896 passed, 0 failed, 14 ignored. `cargo test --locked
--bin v06_work_inventory`: 400 passed, 0 failed (3 new this cycle). `cargo
test --locked --test v06_work_inventory`: 15 passed, 1 failed
(pre-existing, unrelated -- see Discoveries), 1 ignored. `apps/desktop/src-tauri`
(separate cargo workspace): `cargo test --locked --no-run` exit 0.

## Sweep population

`corpus_literal_sweep`: 48708 examined before -> 48708 examined after,
delta 0 (no `data/corpus/**` file touched or regenerated this cycle --
only `src/rules_core/pilot_compute/mod.rs`'s new formulas and
`src/bin/v06_work_inventory.rs`'s probe extension changed).

## Movement, four buckets

- **Closure:** 9 -- all 9 Transmutation-related records (6 `core_rulebook`,
  3 `advanced_class_guide` honest side effect) -> `DONE` (`grounded`).
  Every one carries a REAL, live-computed magnitude a Transmutation
  wizard's own sheet already renders, proven by a live probe against the
  real compute pipeline for a canonical Transmutation wizard, not
  asserted.
- **Reclassification:** 0 -- no unit changed bucket without a genuine holds
  change.
- **Reachability:** 0 -- no previously-unreachable unit became reachable
  (the closed records were already reachable Wizard/Arcanist class
  features; this cycle changed only the atlas's own attribution of a
  newly-computed value, not what a player can select).
- **Instrument-correction:** 0 -- `F1` (5400) and every other population
  figure moved by exactly the closure count and nothing else; no
  measurement-method change this cycle.

## Remainder -- 261 units, named by sub-cause

Re-derived fresh against the post-cycle inventory (populations sum exactly
to 261):

| Sub-cause | Units | Notes |
|---|---|---|
| `Domain Power` (domains `DOMAIN_POWER_CATALOG` carries no formula for) | 56 | Unchanged. Real new-formula work per domain. |
| `Domain Base` | 33 | Unchanged (cycles 2-3's own investigation: genuinely computable DC formula, never consumed by an enemy-facing domain power, deliberately excluded per `DOMAIN_POWER_CATALOG`'s own module doc). |
| Wizard arcane-school cluster (7 remaining schools' own powers plus every school's own top-level/opposition-school recognition record, Transmutation's own now included in the "closed schools" set) | 35 | Was 38 before this cycle; -3 (this cycle's own 3 closed Transmutation POWER records moved out; the Transmutation top-level + opposition-school recognition records remain, deliberately unclaimed, same as Evocation/Abjuration's own). Verified directly (`Conjuration`/`Enchantment`/`Illusion`/`Necromancy` 4 each, `Divination`/`Universal` 3 each, `Evocation`/`Abjuration`/`Transmutation` 1-2 each of top-level/opposition remainder, `Arcane School Tracker` 1). |
| `Draconic Bloodline Choice` | 10 | Unchanged (cycle 5's own finding: non-numeric, no per-color formula, different shape from Bardic Performance). |
| `Secret Lore` | 10 | Unchanged in count, but investigated this cycle (see Mechanism-specific direction above): real per-record formulas exist, but no Loremaster prestige-class chassis exists to hang them on -- a materially bigger lift than this cycle's own lever, named again below. |
| `New Arcana` | 9 | RULED OUT by cycle 3: no single canonical per-record value the engine ever commits to. |
| Small (2-3 unit) per-class roster groups (Damage Reduction, Divine Bond, Favored Class Bonus, Hunter's Bond, Ki Stat Choice, Nature's Bond, Precise Strike, Wildshape 2/3) | 20 | Unchanged. |
| Long tail (single-unit groups, non-school) | 88 | Unchanged. |

56+33+35+10+10+9+20+88 = 261.

## Next-cycle plan

1. **`Secret Lore` (10)** -- a dedicated cycle, not a bounded extension:
   every one of the 10 Loremaster prestige-class sub-records
   (`data/corpus/core_rulebook/class_feature/secret_lore/*.json`) carries a
   real, distinct formula (6 flat saves/HP/attack/AC bonuses, 4 choice-shaped
   grants -- Applicable Knowledge/feat, Instant Mastery/skill ranks,
   Newfound Arcana + More Newfound Arcana/bonus spells), but the engine has
   ZERO Loremaster prestige-class posture at all (no `class_sweep_input`
   support, no `LoremasterSecretsLVL` tracking). Needs the prestige-class
   chassis built first.
2. One more wizard school (Conjuration, Divination, Enchantment,
   Illusion, Necromancy, or Universal -- 3-4 units each), following this
   cycle's exact pattern (new `wizard_has_canonical_<school>_selection`
   opposing two other real schools -- REMEMBER to swap the opposed-schools
   choice too, not just the specialty, per this cycle's own Discoveries).
3. `Domain Base`'s real DC consumer (33) -- still not built, out of scope
   for a single bounded cycle (cycles 2-3's own finding, re-confirmed
   unchanged through cycle 6).

- **Status:** partial
