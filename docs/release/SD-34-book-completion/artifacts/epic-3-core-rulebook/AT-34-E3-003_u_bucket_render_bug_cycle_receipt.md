# Cycle — Epic 3 (Core Rulebook to zero) / AT-34-E3-003 (bucket `U` cycle 2 — render_pcgen_desc fix)

- **Commit SHA:** `<filled after commit>`
- **Files touched:**
  - `src/rules_core/pcgen_desc.rs` — `render_pcgen_desc_with_values`'s bare-`%`
    branch gains the same digit-preceded exemption `leaked_pcgen_syntax`
    already had (`!(i > 0 && chars[i - 1].is_ascii_digit())`): a `%`
    immediately preceded by a digit ("75% chance...") is now preserved as a
    literal percent sign instead of dropped into `dropped_args`. One new
    test, `a_digit_preceded_percent_sign_is_a_literal_sign_not_a_drop`
    (RED→GREEN pair: the digit-preceded case now renders clean; the
    pre-existing non-digit-preceded case, `"Cast % 1/day"`, still drops,
    proving the exemption is scoped and does not swallow real leaks).
  - `docs/work-inventory.json` (regenerated at HEAD, guarded regeneration
    path — plain `cargo run --locked --release --bin v06_work_inventory`,
    `CORPUS_LITERAL_SWEEP_REPORT`/`DERIVED_FIXTURE_CHECK_REPORT` set from
    this cycle's own fresh `corpus_literal_sweep --json-out`/
    `derived_evaluator_fixture_check --json-out` runs; no
    `--allow-stamp-loss` used or needed — the run completed cleanly with no
    `RESIDUAL`/stamp-loss warning).
  - `scripts/tests/test_completion_atlas.py` —
    `test_bucket_u_matches_named_population` re-pinned 211 → 202 (a count
    change requires a sweep, not just a build — grepped `tests/`, `src/`,
    `apps/`, `scripts/` for the literal `211`; the only other hardcoded hit
    outside this file is `docs/work-inventory.json` itself, which the
    regeneration already updated, and unrelated numeric coincidences —
    `source_line`/`source_page` values, `sd17_b_races_and_abilities.rs`'s
    LST-file count, `feat_prereqs.rs`'s comment, generated
    `web_atoms`/`named_entities.rs` build output).
  - `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`
    — regenerated by `completion_atlas.py --check`'s own write path (counts
    only, no hand edits). `missing-engine-tables.json` unaffected — bucket
    A moved 0 units this cycle, confirmed with a diff.

- **Identifier audit result:** OK_NO_BUNDLE_TAGS (checked against this
  cycle's own diff — `git diff --unified=0 -- src/rules_core/pcgen_desc.rs
  scripts/tests/test_completion_atlas.py`)
- **Wired-integration audit result:** OK_NO_TOKENS (same diff)

- **Acceptance criterion:** `AT-34-E3-003` — "buckets M, V, D, U, X close"
  (`epic-breakdown.md` §"AT-34-E3-003 — buckets M, V, D, U, X close"). This
  cycle continues bucket `U`'s remainder from the prior cycle's own
  `AT-34-E3-003_u_bucket_ruling_cycle_receipt.md`, closing the 9-unit
  `render_pcgen_desc` defect that receipt named and explicitly deferred as
  "a real, narrow renderer bug... out of this cycle's scope." **This is
  closure, not reclassification**: a real defect in a shared rendering
  function was fixed and the affected records now render their real,
  already-shipped corpus prose correctly — nothing was reclassified by
  ruling, only corrected.

- **Figures + their re-derive commands:**
  - `core_rulebook` bucket `U` before this cycle: **10** were reachable
    only after re-deriving at HEAD before starting — the atlas showed 18 at
    the top of this cycle (`python3 scripts/completion_atlas.py --book
    core_rulebook --check`), matching the prior cycle's own closing figure.
  - Corpus-wide `unmeasurable` before: **211** —
    `git show HEAD~0:docs/work-inventory.json` (this cycle's own parent
    commit, `45c5aead16`) piped through
    `json.load(...)['totals']['by_status']['unmeasurable']`.
  - Corpus-wide `unmeasurable` after: **202** — same command against the
    regenerated file. Delta: **9**, exactly the population the prior
    cycle's deferral named for this defect.
  - `core_rulebook` bucket `U` after: **10** —
    `python3 scripts/completion_atlas.py --book core_rulebook --check`.
    (Delta 8 for `core_rulebook` specifically — 9 units moved corpus-wide,
    8 of them `core_rulebook`, 1 `ultimate_equipment`; see full breakdown
    below.)
  - `core_rulebook` `DONE` before → after: **1440 → 1448** (+8), matching
    the 8 `core_rulebook` units this cycle moved.
  - Total units moved this cycle (whole-corpus before/after diff by unit
    id, not a total-count subtraction): **10** — 9 in the named
    `equipment_modifier`/`unmeasurable` population, plus **1 discovery**:
    `advanced_class_guide:equipment_modifier:burdenless`
    (`ingested-magnitude → text-complete`) — its corpus description
    ("Carrying capacity increased by 50%") carries the identical
    digit-preceded-`%` shape and was also silently mis-rendered before this
    fix, but its `status` was `ingested-magnitude`, not `unmeasurable`, so
    it fell outside the 30-unit remainder the prior cycle enumerated. Not
    an atlas defect (no new *category* was missed — this is the SAME
    defect touching a unit the prior cycle's book/kind-scoped grep of
    `unmeasurable` records did not examine, because it wasn't a
    `U`-bucket unit at all), but reported here per `decisions.md §12` L18
    ("a remaining step the atlas did not predict is recorded"): logged as
    a bonus finding, not a new bucket-clearing mechanism.
  - By book, units moved: `core_rulebook` 8 (`FRT_HVY`, `FRT_LGHT`,
    `FRT_MOD`, and their 3 prose siblings `Special Ability ~ Fortification
    {Heavy,Light,Moderate} ~ Armor`, plus the 2 `Special Ability ~ Ghost
    Touch ~ {Amulet of Mighty Fists,Weapon}` records), `ultimate_equipment`
    1 (`Special Ability ~ Deathless ~ Armor`), `advanced_class_guide` 1
    (`burdenless`) — sum 10.
  - Named remainder (bucket `U`, corpus-wide, still open): **21** —
    `python3 -c "import json; from collections import Counter;
    inv=json.load(open('docs/work-inventory.json'));
    rem=[u for u in inv['units'] if u['status']=='unmeasurable' and
    u['kind']=='equipment_modifier']; print(len(rem),
    Counter(u['book'] for u in rem))"` →
    `21 Counter({'core_rulebook': 10, 'ultimate_psionics': 6,
    'advanced_class_guide': 4, 'ultimate_equipment': 1})`. All 21 carry an
    unresolved `%CHOICE`/`%d<N>` PCGen substitution and, per the prior
    cycle's own conclusion, need a further operator ruling on whether that
    shape can read `DONE` or belongs nearer bucket `X` — **not** a
    render-path defect, and out of this cycle's scope for the same reason
    the prior cycle named.

- **Row-count command output** (whole-corpus before/after diff by unit id):
  ```
  $ python3 -c "
  import json
  before = json.load(open('/tmp/sd34-e3-003-work/wi_before.json'))   # git show HEAD:docs/work-inventory.json, this cycle's parent
  after = json.load(open('docs/work-inventory.json'))
  bu = {u['id']: u for u in before['units']}
  au = {u['id']: u for u in after['units']}
  print('id sets equal:', set(bu.keys()) == set(au.keys()))
  changed = [(uid, bu[uid]['kind'], bu[uid]['book'], bu[uid]['status'], au[uid]['status'])
             for uid in bu if bu[uid]['status'] != au[uid]['status'] or bu[uid]['evidence'] != au[uid]['evidence']]
  print('TOTAL CHANGED:', len(changed))
  from collections import Counter
  print('by kind:', Counter(c[1] for c in changed))
  print('by book:', Counter(c[2] for c in changed))
  print('by transition:', Counter((c[3], c[4]) for c in changed))
  "
  id sets equal: True
  TOTAL CHANGED: 10
  by kind: Counter({'equipment_modifier': 10})
  by book: Counter({'core_rulebook': 8, 'advanced_class_guide': 1, 'ultimate_equipment': 1})
  by transition: Counter({('unmeasurable', 'text-complete'): 9, ('ingested-magnitude', 'text-complete'): 1})
  ```
  Same 49,438-unit id set before and after; exactly 10 changed, all one
  kind, both destined for `text-complete`.

  Completion-atlas cross-check:
  ```
  $ python3 scripts/completion_atlas.py --check
  population=49438 buckets=10 unclassified=0 overlap=0
  U: 202   (was 211)
  $ python3 scripts/completion_atlas.py --book core_rulebook --check
  population=6701 unclassified=0 overlap=0
  DONE: 1448  (was 1440)
  U: 10       (was 18)
  ```

- **Build scope verified:**
  - `cargo test --locked --lib rules_core::pcgen_desc::` — **36 passed, 0
    failed** (includes the new RED→GREEN pair).
  - `cargo test --locked --bin v06_work_inventory` — **412 passed, 0
    failed**.
  - `python3 -m unittest scripts.tests.test_completion_atlas -v` — **38
    passed, 0 failed** (includes the re-pinned `test_bucket_u_matches_
    named_population`).
  - `cargo test --locked --no-run` (full workspace) — **exit 0**; all test
    binaries compiled cleanly, confirmed by the run's own `[exited with
    code 0]` marker and `grep -c '^error'` returning `0` on the captured
    log (`/tmp/sd34-e3-003-work/norun.log`).
  - `apps/desktop/src-tauri` — **not touched this cycle**, not run
    (`decisions.md §10`/`workflow-instruction.md §2.5`: this crate's own
    suite is only required when it is touched; neither
    `src/rules_core/pcgen_desc.rs` nor `docs/work-inventory.json` is
    consumed by the desktop crate's own compiled test binaries in a way
    this cycle's scope reaches — no desktop-crate file was edited).
  - All runs at commit `<filled after commit>`, run **after** the last
    figure-moving commit in this cycle (there is only one).

- **Sweep population:** no `data/corpus/**` records were added or
  regenerated this cycle — only the shared renderer and the derived
  inventory changed. The guarded `docs/work-inventory.json` regeneration
  required a fresh `corpus_literal_sweep --json-out`/
  `derived_evaluator_fixture_check --json-out` pair to clear the
  stamp-loss guard: `corpus_literal_sweep` reported **48708 records
  examined of 51482 read, 413336 tokens compared (9 synthesized), 51469
  digests checked, 0 findings, CLEAN** (identical population before and
  after this cycle — no corpus record was added or removed, so the
  examined-population correctly did not move; `decisions.md §12` L8's
  "delta must equal the record delta" is satisfied trivially at
  delta=0=0). `derived_evaluator_fixture_check` reported **1839 unit(s)
  cleared over 2580 fixture row(s); 0 failed; 0 not ingested** — both
  reports fed the regen via `CORPUS_LITERAL_SWEEP_REPORT`/
  `DERIVED_FIXTURE_CHECK_REPORT`, and the regen completed with no
  `--allow-stamp-loss` needed.

- **Oracle pin:** N/A — no figure in this cycle depends on the pinned
  PCGen oracle corpus.

- **Status:** **partial** — this cycle closes bucket `U`'s
  `render_pcgen_desc` sub-cause (9 named + 1 discovered = 10 units) but
  `core_rulebook`'s own bucket `U` is **not** zero: 10 remain, all one
  named sub-cause (`%CHOICE`/`%d<N>` unresolved substitution, awaiting an
  operator ruling per the prior cycle's own conclusion). Buckets `M`
  (1,048), `V` (2,793), `D` (382), and `X` (116) are **untouched** by this
  cycle and remain the overwhelming majority of `AT-34-E3-003`'s remaining
  scope. See "Remainder" below for the full named breakdown.

- **Movement, four buckets:**
  - **Closure:** **10** — a real rendering defect was fixed and verified
    (RED→GREEN), and the affected records now correctly surface their
    already-shipped corpus prose to a player. This is closure, not
    reclassification: no rule changed what counts as `DONE`; a bug that
    prevented already-correct data from being recognized as complete was
    corrected.
  - **Reclassification:** 0 this cycle (the prior cycle's 110-unit
    reclassification under `decisions.md §17` is not repeated here).
  - **Reachability:** 0.
  - **Instrument-correction:** 0 — `corpus_json_description_leaks_pcgen_
    syntax`'s own logic is unchanged; only `render_pcgen_desc`, the shared
    renderer it calls, was corrected.

- **Notes / judgment calls:**
  - The fix mirrors an existing, already-trusted exemption
    (`leaked_pcgen_syntax`'s own `!(i > 0 &&
    chars[i - 1].is_ascii_digit())` check) rather than inventing a new
    rule — the two functions previously disagreed about the same shape,
    which is what caused the defect; they now agree.
  - The `burdenless` discovery (1 extra unit, not in the prior cycle's
    named 30) is reported transparently rather than silently absorbed,
    per `decisions.md §12` row 18's standing lesson that an unpredicted
    step is worth naming even when it is welcome.
  - This fix is corpus-wide by construction (`render_pcgen_desc` serves
    every book's equipment/spell/monster-ability descriptions), so no
    other book's digit-preceded-`%` records were silently missed — the
    before/after diff above is a whole-corpus scan, not a
    `core_rulebook`-filtered one.

- **Remainder — every unit named by sub-cause, populations summing to the
  stated total (`AT-34-E3-003`'s own "buckets M, V, D, U, X close" bar,
  for `core_rulebook`, at HEAD after this cycle):**

  | Bucket | Population (core_rulebook, live) | Clearing mechanism | Status |
  |---|---:|---|---|
  | `M` | 1,048 | running the compute path (`decisions.md §2`) | untouched this cycle |
  | `V` | 2,793 | the SD-33 oracle harness (`scripts/oracle_harness/`) | untouched this cycle |
  | `D` | 382 | per named sub-cause (not yet enumerated by this cycle) | untouched this cycle |
  | `U` | 10 | operator ruling on the `%CHOICE`/`%d<N>` shape (all 10 named units, all one sub-cause) | escalation candidate, not yet ruled |
  | `X` | 116 | per `decisions.md §17`'s new clearing mechanism — build the per-character choice filter (`apps/desktop/src-tauri/src/class_feature_pool_picker.rs` + `src/rules_core/feat_prereqs.rs`, the missing join named in §17) | untouched this cycle |

  Sum: 1,048 + 2,793 + 382 + 10 + 116 = **4,349**, matching
  `python3 scripts/completion_atlas.py --book core_rulebook --check`'s own
  live sum of those five buckets (re-run above). No unit in these five
  buckets is unaccounted for.

- **Next-cycle plan:**
  1. **Escalation candidate, not filed this cycle as an `## Open
     blockers` entry (that pauses the whole bundle) but flagged for the
     operator:** whether `core_rulebook`'s 10 remaining `U` units
     (`%CHOICE`/`%d<N>` unresolved substitution — a real, unmodelled
     player choice or dice-multiplier reference) can read `DONE` as
     "plumbing whose choice a different subsystem resolves," or must move
     to bucket `X` under `decisions.md §17`'s own new `X` clearing
     mechanism (the per-character choice filter). This is genuinely a
     policy question, not an engineering one — the prior cycle already
     concluded it needs its own ruling and this cycle found no new
     information that settles it.
  2. **`M` (1,048) and `V` (2,793) are the dominant remaining populations**
     and neither has had a per-mechanism enumeration cycle yet for
     `core_rulebook` specifically under `AT-34-E3-003`'s own scope (as
     opposed to Epic 3's earlier bucket-B/C/A cycles). The next cycle
     should measure per-unit cost on a small sample of each **before**
     committing to a population-scoped run, per `decisions.md §12` L11 —
     neither bucket's cost has been established this epic.
  3. **`D` (382)** needs its sub-causes enumerated before any clearing
     work starts (`decisions.md §2`: "admitted only with its sub-causes
     enumerated").
  4. **`X` (116)** is now unblocked from being a dead end by
     `decisions.md §17`'s new clearing mechanism (the choice-filter join),
     but that capability does not yet exist in this codebase — building
     it is a real, multi-file engineering task, not a documentation
     change, and belongs in its own scoped cycle (or is named to
     `AT-34-E5-002`'s capability register if SD-34 does not build it).
