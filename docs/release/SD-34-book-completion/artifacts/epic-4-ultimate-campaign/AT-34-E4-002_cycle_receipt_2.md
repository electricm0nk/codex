# Cycle salvage-2026-08-30 — Epic 4 / AT-34-E4-002 (continuation)

**Provenance.** This cycle is a salvage of an isolated worktree's uncommitted diff (session
exited without committing). The diff implemented `AT-34-E4-002_cycle_receipt.md`'s own
next-cycle plan item 1: *"widen `closure_has_real_description` for the 3 `ability`
`ASPECT:`-only records (root cause already diagnosed this cycle)."* No other artifact from that
session survived (no receipt, no kanban/progress entry) — this receipt reconstructs the record
from the merged diff plus a fresh independent re-derivation of every figure below.

- **Commit SHA:** see this cycle's own commit (`fix(sd34): salvage...`).
- **Files touched:** `src/bin/v06_work_inventory.rs` (new `closure_has_real_aspect_description`
  / `aspect_display_text_is_real_description` functions + 7 unit tests + one call-site wire in
  `main()`'s `has_real_description` computation), `docs/work-inventory.json` (regenerated,
  this cycle's own run), `scripts/completion_atlas.py` (10 citation line numbers, re-verified
  against the merged file, unchanged from the salvaged patch — `citation_failures=0` both before
  and after this cycle's own additional insertions), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` (auto-regenerated by `completion_atlas.py --check`).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (no `sd[0-9]+_`/`t_[0-9a-f]{8,}` token in the
  new code).
- **Wired-integration audit result:** OK_NO_TOKENS (no STUB/MOCK/placeholder/todo/fixme/hack
  token in the new code).
- **Acceptance criterion (verbatim, epic-breakdown.md AT-34-E4-002):** `python3
  scripts/completion_atlas.py --book ultimate_campaign --check` exits 0 with `DONE=265 of 265`,
  every other bucket zero. **Not met.** Live: `DONE=133 of 265`, remainder `D=2 M=89 V=18 U=21
  X=2`. This cycle closes 3 of the 5 `D` units this criterion's own first cycle left named; the
  remaining 132-unit population is real engine-compute-path work (`M`), an oracle-harness run
  (`V`), and two already-adjudicated buckets (`U`/`X`, per `AT-34-E4-001`) — out of this cycle's
  scope, unchanged from the first cycle's own "Next-cycle plan" items 2-3.

## Figures + their re-derive commands

| Figure | Value | Command |
|---|---|---|
| Whole-corpus unit id-set | 49,438 → 49,438, 0 added/removed | before/after diff by `id` over `docs/work-inventory.json` (`git show HEAD:docs/work-inventory.json` vs. this cycle's regen) |
| Whole-corpus units with a changed `status`/`evidence` | 131 of 49,438 | same diff |
| — of those, real closure (`engine-does-not-hold` → `text-complete`) | 123 | same diff, filtered on `before.status=='engine-does-not-hold'` |
| — of those, same-bucket evidence-string relabel (`grounded` → `text-complete`, both already `DONE`-clearing statuses per `decisions.md §2`) | 8 | same diff, filtered on `before.status=='grounded'` |
| Closure by book | `ultimate_equipment` 40, `core_rulebook` 39, `bestiary_2` 19, `bestiary_4` 16, `ultimate_campaign` 3, `bestiary_5` 2, `advanced_players_guide` 2, `bestiary_3` 1, `inner_sea_races` 1 | same diff, grouped by `book` |
| Evidence-string churn by book (no bucket crossed) | `bestiary` 4, `bestiary_2` 3, `ultimate_wilderness` 1 | same diff |
| `ultimate_campaign` closures, named | `blood_of_dragons_saving_throw`, `deathtouched_mind_affecting`, `loyalty_across_lifetimes_eidolon_bonus` — exactly the 3 the salvaged diff's own doc comment names | `python3` filter on `docs/work-inventory.json`, `book=='ultimate_campaign' and evidence=='ability_content_table_resolve_returned_a_real_record_with_description'` |
| `ultimate_campaign` bucket state | `DONE 130→133, D 5→2` | `python3 scripts/completion_atlas.py --book ultimate_campaign --check` |
| `core_rulebook` bucket state | `DONE 4215→4254, D 405→366` | `python3 scripts/completion_atlas.py --book core_rulebook --check` |
| Corpus-wide bucket state | `DONE 17453→17576, D 3078→2955`, all other buckets unchanged (incl. `V` 6846 unchanged — see the sibling receipt on why) | `python3 scripts/completion_atlas.py --check` |
| `completion_atlas.py --check` corpus-wide | `population=49438 unclassified=0 overlap=0 done_evidence_violations=0 missing_clearing_mechanisms=0 citation_failures=0` | `python3 scripts/completion_atlas.py --check` |
| `denominator_gate.py --check` | `files_checked=15 violations=6` — unchanged pre-existing `FRT_HVY` baseline (verified line-for-line against the pre-cycle baseline; no 7th added) | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` |
| `box_ledger.py --check` | pre-existing failure, unchanged in kind: `exit 1`, `uncovered` 22,346 → 22,223 (an improvement — same pre-existing gap `AT-34-E3-005`'s own apply cycle left, `THE-BOX.md` never updated for `oracle-agree`/`oracle-unverifiable`; `oracle_disagreement=0` throughout) | `python3 scripts/box_ledger.py --check` |

## Sweep population

Three-pass pipeline, in order, **no `--allow-stamp-loss`**:

1. `corpus_literal_sweep --json-out` → `clean:true records_examined:48708` (unchanged — no
   `data/corpus/**` file touched this cycle).
2. `derived_evaluator_fixture_check --json-out` → `1839 unit(s) cleared over 2580 fixture
   row(s); 0 failed`.
3. `CORPUS_LITERAL_SWEEP_REPORT=... DERIVED_FIXTURE_CHECK_REPORT=... cargo run --locked --bin
   v06_work_inventory --release` → exit 0, `real 2m26.098s`, no stamp-loss refusal.

## Oracle pin

Not applicable — no figure here came from the pinned PCGen oracle checkout; the fix reads only
the live repo's `data/corpus/` tree via the token-closure the classifier already builds.

## Build scope verified

`cargo test --locked --no-run`: full workspace exit 0 (`real 2m18.144s`); `apps/desktop/src-tauri`
(separate cargo workspace, tested explicitly per `decisions.md §10`) exit 0 (`5m57s`). Both run at
this cycle's own merged HEAD, after the last commit that can move a figure an assertion depends
on. `cargo test --bin v06_work_inventory`: 433 passed, 0 failed (includes the 7 new
`closure_has_real_aspect_description_tests`). Full 184-target `sd13_*`/`sd25_*` gate set, run
together as one `cargo test --locked --test ... --test ...` invocation: 184/184 pass, 0 failed.

## Status: complete (this cycle's own scope only — the criterion itself stays open)

## Movement, four buckets (`decisions.md §9`)

- **Closure:** 123 units, corpus-wide (D → DONE via a real, verified `ASPECT:` tooltip
  description — leaked-syntax and bare-number/template text explicitly refused, same discipline
  as every other `has_real_description` signal). 3 of these are the named `ultimate_campaign`
  units this criterion's own first cycle deferred.
- **Reclassification:** 0.
- **Reachability:** 0.
- **Instrument-correction:** 0.
- **Evidence-string churn, no bucket crossed:** 8 (`grounded` → `text-complete`, both `DONE`
  statuses per `decisions.md §2` — the description signal now fires before the structural-match
  signal for these 8 pre-existing `DONE` units, changing which evidence string is recorded, not
  their bucket).

## Notes

- This function is corpus-wide by construction (reads the same `row_refs` token closure every
  other `has_real_description` signal reads), not book-scoped — consistent with `MEMORY`'s
  "generic pass, not per-object lanes" standing lesson. The 123-unit real closure is
  larger than the 3-unit `ultimate_campaign` example in the function's own doc comment because
  the doc comment names only its motivating case, not its full reach.
- `AT-34-E4-002`'s own bar (`DONE=265 of 265`) is **not met** and is not claimed met. The
  remaining 132-unit `ultimate_campaign` population needs real engine compute-path work (`M`,
  89 units) and an oracle-harness run (`V`, 18 units) — Epic-3-scale work applied to a shallower
  book, per the criterion's own first-cycle receipt.
- `docs/release/SD-34-book-completion/artifacts/epic-5-forward-plan/forward-plan.json` and
  `capability-register.json` were both re-derived this cycle (their own `build_*.py` scripts,
  live against the regenerated `docs/work-inventory.json`) because this fix moved `D`-bucket
  counts corpus-wide, which those frozen artifacts cite. Both verifiers (`verify_forward_plan.py`,
  `verify_capability_register.py`) pass at HEAD.

## Next-cycle plan

Unchanged from `AT-34-E4-002_cycle_receipt.md`'s own next-cycle plan items 2-3: measure per-unit
cost on a small sample of `M` (89) and `V` (18) before any population-scoped run, then re-run
`completion_atlas.py --book ultimate_campaign --check` after each sub-wave.
