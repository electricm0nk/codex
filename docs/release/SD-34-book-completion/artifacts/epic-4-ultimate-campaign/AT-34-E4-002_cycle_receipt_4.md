# Cycle 4 — Epic 4 (Ultimate Campaign) / AT-34-E4-002 — widen the trait/drawback spine to the 5 fixed-choice `%LIST` traits, land the desktop picker end to end

- **Commit SHAs:** `4da3693d51` (compute path + classifier wiring + desktop DTO/request plumbing, 5 units DONE), `34bd97559a` (instrument-correction: re-derive all 10 `completion_atlas.py` citation pins after a rebase-induced second shift), `8e69cdc4ff` (desktop `CreateCharacterForm.tsx` skill picker + wire-through, closing the compute-path-with-no-UI gap). All three pushed to `tranche/14`.
- **Provenance.** This cycle starts from `origin/tranche/14` at `58521a54ef` (wave 16), which already contains cycle 3's landed capability build (`e8ac310280`, merged before this cycle started — the dispatch prompt's stated `HEAD 651966b83e` was stale; re-verified at the real HEAD before writing any code, per the dispatch's own instruction not to trust a carried figure). Re-derived `ultimate_campaign` split at real HEAD: `DONE=182, M=58 (trait 28 + ability 30), U=21, D=2, X=2, V=0` — matches cycle 3's own stated end-state exactly (`python3 scripts/completion_atlas.py --book ultimate_campaign --check`). Whole-tree grep re-confirmed no *second* trait/drawback capability was independently built elsewhere (`grep -rniE 'selected_traits|character_traits|CharacterTrait\b' src/ apps/desktop/src-tauri/src/ apps/desktop/src/` shows only cycle 3's own machinery). No salvage branch was needed or read — cycle 3's work was already committed history, not an uncommitted checkpoint.
- **Files touched:** `src/rules_core/trait_effects.rs` (+365/-13: `SKILL_CHOICE_TRAIT_BONUSES` table, 5 entries; `skill_choice_bonuses_from_traits`; `skill_choice_trait_magnitude_is_grounded_for_corpus_key`; 9 new tests), `src/rules_core/skill_allocation.rs` (+12/-1: folds the choice-based bonus map into the same `trait_skill_bonuses` map the flat table already populates), `src/bin/v06_work_inventory.rs` (+58/-16: `Kind::Trait` tries the choice-based grounding as an `.or_else` fallback; 1 new positive test, 1 negative-control comment update), `scripts/completion_atlas.py` (+18/-12, twice: instrument-correction, all 10 bucket citation line pins re-derived after two successive rebase-induced shifts), `docs/work-inventory.json` (regenerated, this cycle's own three-pass run — Epic 4 is the sequential writer, unchanged from cycle 3's own `§3` disposition), `apps/desktop/src-tauri/src/trait_picker.rs` (+153/-32: `TraitSkillOptionDto`, widened `CharacterTraitOptionDto`, `list_available_character_traits` now chains both tables, 4 new/updated tests), `apps/desktop/src-tauri/src/character_hub.rs` (+20: `CreateCharacterRequest.trait_skill_choices`, one construction-site fix), `apps/desktop/src-tauri/src/pf1_adapter.rs` (+16: `compose_character_input` folds `request.trait_skill_choices` into `selected_choices`, one construction-site fix), `apps/desktop/src-tauri/src/{rule_system_adapter,stub_adapter,characterHub/appendToCharacter,characterHub/reSaveCharacter}.rs` (+1 each: construction-site fixes for the new required field), `apps/desktop/src/boundary/loadCharacterTraits.ts` (+21/-7: `TraitSkillOptionDto`, widened `CharacterTraitOptionDto`), `apps/desktop/src/boundary/loadCreateCharacter.ts` (+19: `TraitSkillChoiceDto`, `CreateCharacterRequest.traitSkillChoices`), `apps/desktop/src/boundary/loadCreateCharacter.test.ts` (+1: construction-site fix), `apps/desktop/src/characterHub/composeCreateCharacterRequest.ts` (+10/-1: threads `traitSkillChoices` through, optional), `apps/desktop/src/characterHub/composeCreateCharacterRequest.test.ts` (+41: new default/pass-through test), `apps/desktop/src/characterHub/CreateCharacterForm.tsx` (+101/-34: `traitSkillChoices` state, `toggleTrait` seeds/clears a default choice, real `<select>` rendered for a checked choice-based option, submit resolves the recorded choices into the request).
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` on every commit's own diff (`git diff --unified=0 <base> -- src/rules_core/ src/bin/ apps/desktop/src-tauri/src apps/desktop/src docs/work-inventory.json ':!**/__tests__/**' ':!**/*.test.*'`, run against `ea2b3396f2` for the first commit and against `HEAD~1` for each subsequent one).
- **Wired-integration audit result:** `OK_NO_TOKENS`. No `STUB`/`MOCK`/`placeholder`(-as-stub)/`not yet implemented`/`todo`/`fixme`/`hack` token in shipping code. Two legitimate context matches for the literal substring `placeholder` in `trait_effects.rs`'s own module doc comment — PCGen's `%LIST` player-chosen-target *placeholder token* is a corpus-format term (naming what the flat/choice tables do NOT yet cover: the 4 open-subtype `TYPE=Craft/Perform/Profession` traits), the same doctrine-compliant "what this deliberately does not cover" pattern cycle 3's own receipt already established and was ruled clean.
- **Acceptance criterion (verbatim, epic-breakdown.md AT-34-E4-002):** `python3 scripts/completion_atlas.py --book ultimate_campaign --check` exits 0 with `DONE=265 of 265`, every other bucket zero, plus `artifacts/epic-4-ultimate-campaign/ultimate-campaign-completion-manifest.json`. **Not met this cycle** — real, incremental progress: `DONE=187 of 265` (was `182 of 265` at this cycle's start), remainder `M:53 U:21 X:2 D:2` = 78. The completion manifest artifact remains out of this cycle's scope (Epic 4's closing artifact once all buckets clear).

## Figures + their re-derive commands

| Figure | Value | Command / denominator |
|---|---:|---|
| `ultimate_campaign` bucket split, re-derived at cycle start | `DONE=182, M=58 (trait 28 + ability 30), U=21, D=2, X=2, V=0` of 265 | `python3 scripts/completion_atlas.py --book ultimate_campaign --check` at real HEAD `58521a54ef` (wave 16) — the dispatch prompt's stated `HEAD 651966b83e`/`DONE 151` was stale by two whole cycles; cycle 3 (`e8ac310280`) had already landed and merged |
| `trait_content` (28 remaining, of the original 59) BONUS-shape census | 5 fixed-list `%LIST` (this cycle's scope: `trait_criminal`, `trait_fiend_blood`, `trait_harvester`, `trait_influence`, `trait_style_sage`) + 4 open-subtype `%LIST` (`trait_artisan`, `trait_mentored`, `trait_simple_disciple`, `trait_talented` — `CHOOSE:SKILL|TYPE=Craft/Perform/Profession`, out of scope, named not built) + 3 ability-formula (`max(INT,CHA)-CHA` etc.) + 15 mixed non-`SKILL` bonus types + 1 corpus data gap (`trait_shadow_whispers`) | direct read of every `data/corpus/ultimate_campaign/trait_generic/<key>.json` record for the 28 `ingested-magnitude` `kind:trait` `ultimate_campaign` units listed by `docs/work-inventory.json`, `data.raw_tokens` filtered on `key in {BONUS, CHOOSE}` |
| Units genuinely promoted M → DONE (`grounded`), this cycle | **5**, all in `ultimate_campaign` — no shared-corpus-`KEY` payoff in another book this time (checked, not assumed) | id-set diff of `docs/work-inventory.json` before (`git show <cycle-start-sha>:docs/work-inventory.json`) vs. after (this cycle's own regen): `0 added, 0 removed`, exactly 5 changed, all `ingested-magnitude → grounded`: `trait_criminal`, `trait_fiend_blood`, `trait_harvester`, `trait_influence`, `trait_style_sage` |
| `ultimate_campaign` bucket state after this cycle | `DONE 182→187, M 58→53`, all other buckets unchanged (`U:21 D:2 X:2 V:0`) | `python3 scripts/completion_atlas.py --book ultimate_campaign --check` (post-regen, post-final-rebase) |
| `completion_atlas.py --check` corpus-wide | `population=49438 unclassified=0 overlap=0 done_evidence_violations=0 missing_clearing_mechanisms=0 citation_failures=0` — `DONE:24314 A:449 B:11769 C:4332 D:2955 M:4966 V:262 U:202 X:170 Z:19` | `python3 scripts/completion_atlas.py --check`, at final HEAD after both rebases; `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`'s regenerated-timestamp side effect `git restore`d before every commit, per the wave's `GENERATED_FILE_BAN` |
| `denominator_gate.py --check` | `files_checked=15 violations=7` — pre-existing `FRT_HVY`/quoted-corpus-prose baseline in `progress.md`, grown from 6 (cycle 3's own recorded count) to 7 by intervening merged cycles' own `progress.md` entries, none of them this cycle's; this cycle's own new prose (this receipt + the progress/kanban entries below) adds no new bare-percentage line | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` |
| `box_ledger.py --check` | pre-existing failure, unchanged in kind (inherited read-only per `decisions.md §2`) | `python3 scripts/box_ledger.py --check` |

## Sweep population

Three-pass pipeline, in order, **no `--allow-stamp-loss`**:

1. `corpus_literal_sweep --json-out` → `clean:true records_examined:48708` (unchanged from cycle 3's own baseline — no `data/corpus/**` file touched this cycle; `decisions.md §12` L8 does not apply).
2. `derived_evaluator_fixture_check --json-out` → `1839 unit(s) cleared over 2580 fixture row(s); 0 failed` (unchanged).
3. `CORPUS_LITERAL_SWEEP_REPORT=... DERIVED_FIXTURE_CHECK_REPORT=... cargo run --locked --release --bin v06_work_inventory` → exit 0, no stamp-loss refusal, `docs/work-inventory.json` regenerated with `0 added, 0 removed` units; id-set stable, only `status`/`evidence` moved on 5 units.

## Oracle pin

Not applicable — no figure here came from the pinned PCGen oracle checkout; every figure was derived from the live repo's `data/corpus/` tree and this cycle's own executed fixture tests (`trait_effects.rs`'s `every_choice_entry_is_genuinely_grounded_by_fixture_execution`, which actually builds a fixture character, records a real recognized choice, and runs it through `allocate_skill_ranks`).

## Build scope verified

`cargo build --locked --lib`: exit 0 (only pre-existing warnings, no new ones). `cargo test --locked --lib -- trait_effects`: **20/20 passed** (9 new tests for the choice-based table/compute path, including `every_choice_entry_is_genuinely_grounded_by_fixture_execution`, which actually executes the engine for all 5 entries, and `no_trait_id_appears_in_both_tables`, which proves the flat and choice tables never overlap and so are never double-applied when summed). `cargo test --locked --lib -- selected_traits skill_allocation trait_content local_store`: **36/36 passed** (unchanged from cycle 3, confirming the choice-based fold-in did not regress the flat path). `cargo test --locked --bin v06_work_inventory`: **446/446 passed** (includes the new `a_fixed_choice_skill_trait_bonus_promotes_a_held_trait_record_to_grounded` positive test, proving the `.or_else` fallback reaches `grounded` via the choice-based entry point specifically). `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml -- trait_picker`: **3/3 new + 2 updated passed** (`returns_every_flat_and_choice_skill_trait` (36 total), `every_flat_option_id_is_recognized_by_the_compute_path`, `every_choice_option_round_trips_through_the_compute_path`, `criminal_option_carries_its_real_corpus_choice_data`); the one pre-existing desktop-crate failure in the same run (`race_trait_picker::...the_menu_command_carries_all_fourteen_adopted_race_options...`) is confirmed pre-existing and unrelated — reproduced identically with `docs/work-inventory.json` swapped back to its pre-cycle-3 content, and is outside `race_trait_picker.rs`, a file this cycle never touched. `cargo build --locked --manifest-path apps/desktop/src-tauri/Cargo.toml`: exit 0 (after fixing 6 `CreateCharacterRequest` construction sites the new required field broke). `apps/desktop`: `npx tsc --noEmit`: exit 0, no errors. `npm test`: **96/100 test files passed**; both touched test files (`composeCreateCharacterRequest.test.ts`, `loadCreateCharacter.test.ts`) pass; the 4 failures (`raceCreationCoverage.test.ts`, `release/buildVersionTriple.test.ts`, `releaseChecks/buildVersionTriple.test.ts`, `releaseChecks/buildLabelFixtureFreshness.test.ts`) are confirmed pre-existing corpus-count/version-triple drift by reading each failure's own error message — none mention traits, choices, or any file this cycle touched.

`cargo test --locked --no-run` (full workspace, widest build scope, `decisions.md §10`): run at this cycle's final HEAD (`8e69cdc4ff`, post both mid-cycle rebases) after the last commit that can move a figure an assertion depends on (`decisions.md §12` L7 — the docs/kanban/receipt commit that follows this run touches no `.rs` file) — `Finished 'test' profile [unoptimized + debuginfo] target(s) in 14m 54s`, **0 `error[` / `error:` lines**, every test binary in the workspace linked successfully. `apps/desktop/src-tauri` (separate cargo workspace, tested explicitly, full run not just the scoped `trait_picker` slice): `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml`: **526 passed, 28 failed**, `finished in 40.01s` — the identical 28-test failure set cycle 3's own receipt recorded (`companion_catalog.rs` 15, `feat_catalog.rs` 1, `race_trait_picker.rs` 1, `reach_gate.rs` 11), confirmed by name-for-name comparison against that receipt's own list; none of the 28 are in `trait_picker.rs`, `character_hub.rs`, `pf1_adapter.rs`, or any other file this cycle touched.

## Status: partial

## Movement, four buckets (`decisions.md §9`)

- **Closure:** 5 units, all `ultimate_campaign`, `M → DONE`, via a real, fixture-executed choice-based `BONUS:SKILL|%LIST` compute path — genuine closure, not a relabelling: every one of the 5 entries is re-verified by `every_choice_entry_is_genuinely_grounded_by_fixture_execution`, which builds a real character that both selects the trait AND records a genuine, corpus-legal skill choice, then runs it through the real skill-allocation engine.
- **Reclassification:** 0.
- **Reachability:** 0 (this cycle widens the compute path itself and its desktop selection surface, not a display/explanation wire onto an already-computed value).
- **Instrument-correction:** all 10 of `completion_atlas.py`'s `BUCKET_DEFINITIONS` citation line pins, twice. This cycle's own doc-comment edit to `v06_work_inventory.rs` shifted the `V` pin (fixed once, before the first rebase); rebasing onto `AT-34-E3-002`'s own concurrently-landed WIP cycle (Cleric Domain, `a26d8b3`) shifted every one of the other nine pins a second time, which `--check` caught immediately (`citation_failures=10`) — the same mechanism `AT-34-E1-002`'s own archived note in `progress.md` already proved catches this class of regression on its first live opportunity. Re-derived every line fresh against the post-rebase file: `A/B/C/D` point at the exact literal call site each comment already names; `DONE/M/U/X/Z` (no specific call site named) point at the first `status: "<value>"` construction-site occurrence, never a test-assertion occurrence — the same convention `DONE`'s own comment already documents. No bucket population moved by this correction; it is purely instrument upkeep.

## Notes

- **This is a second slice of a capability build, not a book-scoped patch.** The 5 new table entries, `skill_choice_bonuses_from_traits`, `trait_skill_choice_id`, `CreateCharacterRequest.trait_skill_choices`, and the desktop `<select>` are all general machinery — any book's fixed-list-choice `BONUS:SKILL` trait record benefits the moment its corpus `KEY` is added to `SKILL_CHOICE_TRAIT_BONUSES`, the same kind-keyed payoff cycle 3 proved for the flat table (this cycle's own inventory diff shows no *other* book currently shares one of these 5 exact records' corpus `KEY`, so the corpus-wide count is 5 this time — checked, not assumed).
- **No stub, no half-wired compute path.** The dispatch brief's own doctrine bar — "a selector that renders but grants nothing is a doctrine violation, and so is a compute path with no UI reaching it" — is met on both halves: `skill_choice_bonuses_from_traits` genuinely computes and applies (proven by fixture execution), and the desktop `CreateCharacterForm.tsx` genuinely renders a picker whose selection reaches `compose_character_input`'s `selected_choices` and from there the real engine, not a static list with no wire behind it.
- **What is left named, not guessed, for the next cycle** (`M:53` remaining, of the original 58): (a) 4 open-subtype `%LIST` trait records (`trait_artisan`, `trait_mentored`, `trait_simple_disciple`, `trait_talented`) — need an open Craft/Perform/Profession subtype chooser (free player-named text, not an enumerable list), a materially different input shape than this cycle's closed-list picker; (b) 3 ability-score-difference-formula trait records — need a formula evaluator this crate does not have; (c) 15 mixed-bonus-type trait records (`BONUS:VAR/SAVE/SITUATION/ABILITYPOOL/COMBAT/CONCENTRATION`) — different pillars, each its own compute path; (d) 1 corpus data gap (`trait_shadow_whispers`); (e) 17 narrative Drawback + 1 cross-skill-guarded Drawback + 12 `Retrain` `ability_content` records — all unchanged from cycle 3's own naming, none of this cycle's scope.
- **U(21), D(2), X(2) were not touched, reopened, or reclassified.** Verified by the inventory diff: zero `ultimate_campaign` units with those starting statuses appear in the 5-unit changed set.
- **`git status --porcelain` before every write; no `git add -A`; no `git stash`.** Two rebases onto `origin/tranche/14` mid-cycle (once before the first push, once before the second) — both fast-forwarded cleanly with `git rebase origin/tranche/14`, no manual conflict resolution.
- **Checkpoint discipline:** three commits, each pushed within the same turn it was made, per the box's hard-kill risk.

## Next-cycle plan

Pick the next cheapest named sub-cause from the "what is left" list above. The 4 open-subtype `%LIST` records are the most structurally similar to this cycle's own work (same `BONUS:SKILL` family, needs a free-text/open-subtype chooser generalizing this cycle's closed-list `SelectedChoice` pattern — the same gap `feat_effects.rs` and cycle 3's own doc comment already name for Skill Focus-shaped choices). Re-run `python3 scripts/completion_atlas.py --book ultimate_campaign --check` after each sub-wave; current remainder is `M:53 U:21 X:2 D:2` summing to `78 of 265` non-DONE.
