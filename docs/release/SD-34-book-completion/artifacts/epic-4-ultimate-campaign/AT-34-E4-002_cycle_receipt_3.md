# Cycle 3 — Epic 4 (Ultimate Campaign) / AT-34-E4-002 — build the trait/drawback capability, land the flat `BONUS:SKILL` spine

- **Commit SHA:** see this cycle's own commit (`fix(sd34): AT-34-E4-002 cycle 3 -- build the character trait capability, land the flat BONUS:SKILL spine, 31 units DONE`).
- **Provenance.** This cycle starts from `origin/salvage/wave14-lane1` (`git diff 1ea93e99ce origin/salvage/wave14-lane1`), an unreviewed, unverified checkpoint rescued after the host killed the prior lane's box mid-run. Every file in it was read in full before being trusted. **What was kept, verbatim, after review:** `src/rules_core/trait_effects.rs` (new — the compute module), `apps/desktop/src-tauri/src/trait_picker.rs` (new — the desktop command), `apps/desktop/src/boundary/loadCharacterTraits.ts` (new — the frontend boundary), `CharacterInput.chosen.selected_traits` (new field on `character_input.rs`), the `skill_allocation.rs` wiring that folds `trait_effects::skill_bonuses_from_traits` into `misc_modifier`, the `v06_work_inventory.rs` classifier wire-up (`Kind::Trait` arm now calls `flat_skill_trait_magnitude_is_grounded_for_corpus_key`), the full desktop DTO/boundary/UI plumbing (`character_hub.rs`, `main.rs`, `pf1_adapter.rs`, `rule_system_adapter.rs`, `stub_adapter.rs`, `CreateCharacterForm.tsx`, `CharacterSheet.tsx`, `characterSheetRefresh.ts`, `composeCreateCharacterRequest.ts`, `previewData.ts`, `loadCreateCharacter.ts`/`.test.ts`, `loadSavedCharacterDetail.ts`), the save/load round-trip in `saved_character/local_store.rs`, and every fixture-site `selected_traits: Vec::new()` addition across `tests/sd19_*` / `tests/sd20_*` / `tests/sd24_*` / `tests/sd25_*` / `tests/sd27_*` (86 test files) plus `composed_input.rs`, `feat_prereqs.rs`, `level_up.rs`, `pilot_compute/prestige_class_entry_gate.rs`, `pilot_compute_corpus.rs`, `fixture_verified_oracle_probe.rs`. **What was discarded, and why:** `apps/desktop/package-lock.json`'s version-string edit (out of this cycle's scope — restored to HEAD); `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`'s regenerated copy (GENERATED — forbidden to hand-commit per this cycle's own dispatch; restored to HEAD and re-derived properly, see below); the salvage branch's own edits to `docs/release/SD-34-book-completion/artifacts/sd-34-dispatch.workflow.js` (orchestrator-owned file, outside this criterion's scope, never merged in). Applied via `git merge --no-ff --no-commit origin/salvage/wave14-lane1` (zero conflicts against tranche/14 HEAD `b939abcd4b`) followed by `git restore` on the two forbidden/out-of-scope files — not a blind inherit.
- **Files touched:** (this cycle's own merge, all reviewed) `src/rules_core/trait_effects.rs` (new, 372 lines), `src/rules_core/character_input.rs` (+81), `src/rules_core/skill_allocation.rs` (+67), `src/rules_core/mod.rs` (+1), `src/bin/v06_work_inventory.rs` (+70), `src/bin/fixture_verified_oracle_probe.rs`, `src/rules_core/{composed_input,feat_prereqs,level_up,pilot_compute_corpus}.rs`, `src/rules_core/pilot_compute/prestige_class_entry_gate.rs`, `src/saved_character/local_store.rs`, `apps/desktop/src-tauri/src/trait_picker.rs` (new, 121 lines), `apps/desktop/src-tauri/src/{character_hub,main,pf1_adapter,rule_system_adapter,stub_adapter}.rs`, `apps/desktop/src-tauri/src/characterHub/{appendToCharacter,reSaveCharacter,recomputeCharacter}.rs`, `apps/desktop/src/boundary/loadCharacterTraits.ts` (new, 39 lines), `apps/desktop/src/boundary/{loadCreateCharacter,loadCreateCharacter.test,loadSavedCharacterDetail}.ts`, `apps/desktop/src/characterHub/{CreateCharacterForm.tsx (+111), CharacterSheet.tsx, characterSheetRefresh.ts, composeCreateCharacterRequest.ts, previewData.ts}`, 86 `tests/sd{19,20,24,25,27}_*.rs` fixture-site updates, `docs/work-inventory.json` (regenerated, this cycle's own three-pass run — Epic 4 is the writer this cycle per `workflow-instruction.md §3`, sequential after Epic 3).
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` on this cycle's own diff (`git diff --unified=0 b939abcd4b -- src/rules_core/ src/bin/ apps/desktop/src-tauri/src apps/desktop/src ':!**/__tests__/**' ':!**/*.test.*'`). Note: the SAME grep run against the wider `BASE_BRANCH=$(git merge-base HEAD origin/develop)=ea2b3396f2` (the whole bundle's history, per §6 step 2's literal instruction) surfaces 20 pre-existing hits, all in `src/rules_core/pilot_compute/class_feature_grant_consumer.rs` (untouched by this cycle) citing real test filenames (`sd13_bard_level4..8_progression`, `sd25_druid_level_up_explanation_filter_audit.rs`) in doc comments — legitimate test-file citations from an earlier cycle, not bundle-tag leakage, and outside this cycle's own diff.
- **Wired-integration audit result:** `OK_NO_TOKENS` is the honest instrument-level answer, with three cited context matches on this cycle's own diff for the literal substring `placeholder` — all three are `trait_effects.rs`'s own doc comment describing PCGen's `%LIST` **player-chosen-target placeholder** token syntax (a corpus-format term, not a code stub), explaining exactly what this cycle does **not** yet cover. Read in context, none marks incomplete shipped code; the module's own doctrine-compliant "what this deliberately does NOT cover" section is what the no-stub doctrine asks every real cut to state.
- **Acceptance criterion (verbatim, epic-breakdown.md AT-34-E4-002):** `python3 scripts/completion_atlas.py --book ultimate_campaign --check` exits 0 with `DONE=265 of 265`, every other bucket zero, plus `artifacts/epic-4-ultimate-campaign/ultimate-campaign-completion-manifest.json`. **Not met this cycle** — real, substantial, non-fabricated progress: `DONE=182 of 265` (was `151 of 265` this cycle's own start), remainder `M:58 U:21 X:2 D:2` = 83. The completion manifest artifact is not yet produced (out of this cycle's scope — it is Epic 4's closing artifact once all buckets clear; premature to write against a partial `DONE`).

## Figures + their re-derive commands

| Figure | Value | Command / denominator |
|---|---:|---|
| `ultimate_campaign` bucket split, re-derived at cycle start | `DONE=151, M=89 (trait 59 + ability 30), U=21, D=2, X=2, V=0` of 265 | `python3 scripts/completion_atlas.py --book ultimate_campaign --check` at HEAD `b939abcd4b`, matches this criterion's own stated split exactly |
| Whole-tree grep confirming no trait/drawback selection capability existed | zero matches for `selected_traits\|character_traits\|CharacterTrait\b` before this cycle's merge | `grep -rniE 'selected_traits|character_traits|CharacterTrait\b' src/ apps/desktop/src-tauri/src/ apps/desktop/src/` at `b939abcd4b`; confirms `AT-34-E4-002_cycle_receipt.md`'s finding still held (re-verified, not re-derived from scratch, per this cycle's own instruction) |
| `ultimate_campaign` `ability_content` M-bucket (30), split by real mechanism | 18 `Drawback` records + 12 `Retrain` records | `docs/work-inventory.json` filtered `book=='ultimate_campaign' and status=='ingested-magnitude' and kind=='ability'`, ids grouped on `drawback`/`retrain` substring. The 12 `Retrain` records (character-progression rebuild rules, e.g. `Retrain ~ Feat`) are a **different mechanic** than trait/drawback selection and out of this cycle's scope; of the 18 real `Drawback` records, 17 have no `BONUS` token at all (pure narrative/GM-adjudicated penalties, e.g. `Drawback ~ Attached`'s conditional Will-save penalty) and 1 (`drawback_meticulous`) has a `BONUS:SKILL` token but guarded by a per-skill `PRESKILL` (untrained-only) condition across all ~90 skills — a materially different shape than the flat trait bonus this cycle covers, named here rather than silently included or silently dropped |
| `trait_content` (59) shape census, by BONUS token shape | 31 flat named-skill `BONUS:SKILL` (this cycle's scope) + ~12 `%LIST` player-choice placeholder + ~3 ability-difference formula + ~15 mixed `VAR/SAVE/SITUATION/ABILITYPOOL/COMBAT/CONCENTRATION` + 1 record whose corpus file could not be located under `trait_generic/` (`trait_shadow_whispers`, pre-existing corpus gap, unrelated to this cycle, named not chased) | direct read of every `data/corpus/ultimate_campaign/trait_generic/*.json` record named in the 59 `ingested-magnitude` `kind:trait` `ultimate_campaign` units, cross-checked against `trait_effects.rs`'s own transcribed `FLAT_SKILL_TRAIT_BONUSES` table (31 entries, `table_has_exactly_thirty_one_entries` test). **Correction of this cycle's own dispatch brief**: the brief stated "44 of 59 are pure BONUS:SKILL" — the real, corpus-verified figure this cycle's compute path actually covers end-to-end is **31 of 59**; logged as a `retro.py correction` below |
| Units genuinely promoted M → DONE (`grounded`), this cycle | **31** in `ultimate_campaign`, **+5** in `advanced_players_guide` (same shared corpus `KEY`, e.g. `Trait ~ Caretaker` exists verbatim in both books) = **36 of 49,438 corpus-wide** | id-set diff of `docs/work-inventory.json` before (`git show b939abcd4b:docs/work-inventory.json`) vs. after (this cycle's own regen): `0 added, 0 removed` units; 36 changed `status`/`evidence`, all `ingested-magnitude → grounded`, zero elsewhere. This is the kind-keyed corpus-wide payoff the dispatch named: the classifier's `Kind::Trait` fix reads `unit.key` (the corpus `KEY` token), not the book, so any other book sharing an identical Trait record benefits automatically |
| `ultimate_campaign` bucket state after this cycle | `DONE 151→182, M 89→58`, all other buckets unchanged | `python3 scripts/completion_atlas.py --book ultimate_campaign --check` (post-regen) |
| `advanced_players_guide` bucket state after this cycle | `DONE +5, M -5` (exact book-level figures not separately re-derived here — out of this cycle's own book scope, but the 5-unit id-level movement is proven by the inventory diff above) | same `docs/work-inventory.json` diff |
| `completion_atlas.py --check` corpus-wide | `population=49438 unclassified=0 overlap=0 done_evidence_violations=0 missing_clearing_mechanisms=0` (`citation_failures=10` is `--check`'s own re-derived-timestamp side effect on the atlas file, not a real citation break — this cycle never commits that regenerated atlas copy, per the wave's `GENERATED_FILE_BAN`; `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` was `git restore`d to HEAD before commit) | `python3 scripts/completion_atlas.py --check` |
| `denominator_gate.py --check` | `files_checked=15 violations=6` — unchanged pre-existing `FRT_HVY` baseline (verified line-for-line identical to `AT-34-E4-002_cycle_receipt_2.md`'s own recorded baseline; no 7th violation added) | `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'` |
| `box_ledger.py --check` | pre-existing failure, unchanged in kind: `exit 1`, `uncovered=28813` (THE-BOX.md staleness warnings on 7 groups — inherited read-only per `decisions.md §2`, no SD-34 cycle writes to it; `oracle_disagreement=0` throughout) | `python3 scripts/box_ledger.py --check` |

## Sweep population

Three-pass pipeline, in order, **no `--allow-stamp-loss`**:

1. `corpus_literal_sweep --json-out` → `clean:true records_examined:48708` (unchanged from this cycle's own start baseline — no `data/corpus/**` file touched this cycle; `decisions.md §12` L8 does not apply, no records added).
2. `derived_evaluator_fixture_check --json-out` → `1839 unit(s) cleared over 2580 fixture row(s); 0 failed` (unchanged).
3. `CORPUS_LITERAL_SWEEP_REPORT=... DERIVED_FIXTURE_CHECK_REPORT=... cargo run --locked --release --bin v06_work_inventory` → exit 0, no stamp-loss refusal, `docs/work-inventory.json` regenerated with `0 added, 0 removed` units (id-set stable; only `status`/`evidence` moved on 36 units).

## Oracle pin

Not applicable — no figure here came from the pinned PCGen oracle checkout; every figure was derived from the live repo's `data/corpus/` tree and this cycle's own executed fixture tests (`trait_effects.rs`'s `flat_skill_trait_magnitude_is_grounded_for_corpus_key`, which genuinely builds a fixture character and runs it through `allocate_skill_ranks`).

## Build scope verified

`cargo build --locked --lib`: exit 0 (only pre-existing warnings, no new ones from this cycle's code). `cargo test --locked --lib -- trait_effects`: **11/11 passed** (including `every_flat_entry_is_genuinely_grounded_by_fixture_execution`, which actually executes the engine for all 31 entries). `cargo test --locked --lib -- selected_traits skill_allocation trait_content local_store`: **36/36 passed** (character_input fixture parsing, skill_allocation class-skill grounding untouched/still-green, `saved_character::local_store` round-trip of `selected_traits`). `cargo test --locked --bin v06_work_inventory`: **437/437 passed** (includes the new `a_flat_skill_trait_bonus_promotes_a_held_trait_record_to_grounded` positive test and `a_trait_outside_the_flat_slice_stays_ingested_magnitude` negative control). `cargo test --locked --no-run`: full workspace **exit 0**. `apps/desktop/src-tauri` (separate cargo workspace, tested explicitly): `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml`: **524 passed, 28 failed** — every failing test is in `companion_catalog.rs`, `reach_gate.rs`, `feat_catalog.rs`, or `race_trait_picker.rs`, **none of which this cycle touched** (confirmed via `git status --porcelain` against this cycle's own diff); attributed as pre-existing failures unrelated to this cycle's change, not re-verified against a from-scratch baseline run at this exact SHA (no established desktop-crate baseline exists in this cycle's brief — the pre-launch checklist's item 10 explicitly did not reach the desktop crate within its time bound). All runs at this cycle's own merged HEAD, after the last commit that can move a figure an assertion depends on (the `docs/work-inventory.json` regen ran after all Rust test runs above, per `decisions.md §12` L7 — no Rust code changed after the regen).

## Status: partial

## Movement, four buckets (`decisions.md §9`)

- **Closure:** 36 units corpus-wide (`M → DONE`, via a real, fixture-executed flat `BONUS:SKILL` compute path — 31 `ultimate_campaign`, 5 `advanced_players_guide` sharing the identical corpus `KEY`). This is genuine compute-and-apply closure, not a relabelling: every one of the 31 transcribed entries is re-verified by `every_flat_entry_is_genuinely_grounded_by_fixture_execution`, which builds a real character selecting exactly that trait and runs it through the real skill-allocation engine.
- **Reclassification:** 0.
- **Reachability:** 0 (this cycle builds the compute path itself, not a display/explanation wire onto an already-computed value).
- **Instrument-correction:** 0.

## Notes

- **This is a capability build, not a book-scoped patch.** `CharacterInput.chosen.selected_traits`, `trait_effects.rs`, `trait_picker.rs`, and the desktop selection UI are all new, general machinery — any book's flat `BONUS:SKILL` trait record benefits the moment its corpus `KEY` is added to `FLAT_SKILL_TRAIT_BONUSES` (or, for full coverage, once a future cycle widens the table's derivation past hand transcription). The 5-unit `advanced_players_guide` payoff this cycle already produced with zero additional code is the proof.
- **What is left named, not guessed, for the next cycle:** (a) 12 `%LIST` player-choice trait records — need a per-character skill-choice slot generalizing `SelectedChoice`/`selected_traits` (`trait_criminal`, `trait_artisan`, etc.); (b) 3 ability-score-difference-formula trait records (`trait_bruising_intellect`'s `max(INT,CHA)-CHA`) — need a formula evaluator this crate does not have for that shape; (c) 15 mixed-bonus-type trait records (`BONUS:VAR/SAVE/SITUATION/ABILITYPOOL/COMBAT/CONCENTRATION`) — different pillars entirely, each its own compute path; (d) 1 corpus data gap (`trait_shadow_whispers` — `ultimate_campaign:trait:trait_shadow_whispers` is a real `ingested-magnitude` unit but no matching file exists under `data/corpus/ultimate_campaign/trait_generic/` by any name/key search tried — a pre-existing corpus gap, not something this cycle's compute path can ground regardless of scope, flagged here rather than silently absorbed); (e) 17 of the 18 `Drawback` `ability_content` records (narrative/GM-adjudicated, no clean formulaic trigger) plus `drawback_meticulous`'s cross-skill `PRESKILL`-guarded penalty — a materially different compute shape than flat trait bonuses; (f) 12 `Retrain` `ability_content` records — a different mechanic (character-progression rebuild), not trait/drawback selection at all, should probably be re-keyed to a different sub-cause in a future atlas defect note rather than carried under this criterion's population.
- **U(21), D(2), X(2) were not touched, reopened, or reclassified.** Verified by the inventory diff: zero `ultimate_campaign` units with those starting statuses appear in the 31-unit changed set.
- **Desktop crate's 28 pre-existing failures** are reported, not silenced or excluded from the count — see Build scope row for the file-level attribution that shows none are in files this cycle touched.
- **`git status --porcelain` before every write; no `git add -A`; no `git stash`** — the merge from `origin/salvage/wave14-lane1` was applied via `git merge --no-ff --no-commit` (reviewed diff-by-diff before trusting), never a blind stash-pop or checkout.

## Next-cycle plan

Pick the next cheapest named sub-cause from the "what is left" list above — the 12 `%LIST` records are the most structurally similar to this cycle's own work (same `BONUS:SKILL` shape, needs one new `CharacterInput` slot: a per-trait chosen-skill selection, the same generalization `feat_effects.rs`'s own Skill Focus gap already names). Re-run `python3 scripts/completion_atlas.py --book ultimate_campaign --check` after each sub-wave; current remainder is `M:58 U:21 X:2 D:2` summing to `83 of 265` non-DONE.
