export const meta = {
  name: 'sd34-wave50',
  description: 'Wave 50: close as much of the ENTIRE Core Rulebook + Ultimate Campaign actionable remainder as possible in ONE pass (1,781 units across buckets B/C/D/M), single end-of-cycle verification',
  phases: [{ title: 'Build' }],
}

const buildPrompt = [
  'You are working in /home/ubuntu/workspace/repos/codex on branch tranche/14 (a PF1e rules-engine repo, SD-34 bundle, docs/release/SD-34-book-completion/). This is a shared checkout -- set CARGO_TARGET_DIR=/tmp/claude-1000/-home-ubuntu-workspace-repos-codex-docs-release-SD-33-computed-value-verification/f34e35e3-6405-4a87-b612-555220a48600/scratchpad/cargo-target-wave50 for every cargo command you run (export it once at the start of your session).',
  '',
  'IMPORTANT: wave 49 (a separate dispatch targeting 42 prestige classes) may ALSO be editing src/rules_core/pilot_compute/mod.rs and src/bin/v06_work_inventory.rs on this same branch, and may commit while you are working. Run `git log --oneline -5` and `git status --porcelain` before you start. If wave 49 has already committed, you are working from its updated tree -- fine, proceed. If you see unexpected uncommitted changes to those two files that are clearly not yours mid-cycle, do not discard them -- note it and consider whether to pause rather than overwrite another agent\'s in-progress work.',
  '',
  '=== THE OPERATOR HAS TWICE CORRECTED THE SCOPE OF THIS WAVE UPWARD -- READ THIS ===',
  '',
  'This wave was originally scoped to only 309 units (one bucket, "D"). The operator said "why only 309 + 2. there is tones more than that to do on those two books. think bigger." The REAL actionable remainder for these two books, across every bucket that represents genuine unfinished engine work (not a deliberately-deferred or an unmeasurable case), is **1,781 units**. Your job is to make a serious, large dent in ALL of it, not just one narrow slice. The operator has ALSO said (separately, about pacing generally): "do all of them at once - then test them all at once. This thing where you only get 1 item done an hour is not going to work." Internalize both: BIG SCOPE, ONE VERIFY PASS.',
  '',
  '=== SCOPE: Core Rulebook + Ultimate Campaign ONLY, buckets B + C + D + M ===',
  '',
  'Do not touch any other book. Freshly re-derive this yourself before trusting these numbers (query `docs/work-inventory.json`, classify each unit\'s bucket using the SAME logic as `scripts/completion_atlas.py`\'s own `_bucket_of()` function -- read that function directly, it is short and authoritative) -- the corpus/classifier can shift:',
  '',
  '**Core Rulebook -- 1,743 actionable units:**',
  '- bucket M ("magnitude ingested, never computed or applied" -- a real formula token IS present and recognized, no compute function written yet): 778 units. This is the CLEANEST, highest-yield bucket -- treat it as your first priority. Break down by kind and tackle the largest kinds first: class_feature, ability, template, equipment_modifier, race_trait, feat, skill, domain, language, equipment, class, spell (exact per-kind counts will differ slightly from bucket D\'s own breakdown -- re-derive fresh).',
  '- bucket B ("table exists, record not in it" -- the engine has a real lookup table for this kind but this specific record was never added to it): 467 units. Find the table (likely a static array/map keyed by class or record slug in `src/rules_core/pilot_compute/mod.rs` or `src/bin/v06_work_inventory.rs`) and add the missing entries following the exact pattern already used for sibling entries in the same table.',
  '- bucket C ("held and computed, never surfaced" -- the value IS computed somewhere in the engine but never reaches wherever this unit\'s status check looks): 191 units. This is a WIRING gap, not a missing-formula gap -- find where the computed value lives and why it is not surfacing to the classifier/dispatch chain that would mark this unit as done, and fix the wiring (this is the same shape as several prior waves\' own class-feature classifier-collision fixes).',
  '- bucket D ("other engine gap"): 309 units, split: 130 template + 109 ability + 22 language + 17 class + 15 skill + 9 class_feature + 5 race_trait. Of these, 276 (template/ability/language/skill/race_trait, plus most of the class_feature/race_trait ones) share the evidence code `<kind>_content_table_holds_zero_magnitude_record_pending_wiring_class_review` -- search `src/bin/v06_work_inventory.rs` for the literal string `table_holds_zero_magnitude_record_pending_wiring_class_review` (around line 12550, verify current) and read that function\'s full logic BEFORE fixing anything in this shape: it fires when a record is `text_only == true` but failed one of three promotion checks (`has_real_description`, `is_display_wiring_class_for_promotion(wc_class)`, `!universal_sheet_modifier`). Sample a handful across each kind to characterize which of the three blocking reasons applies before writing fixes -- some genuinely need real computation (if `universal_sheet_modifier` is true), some need a classifier fix, some may be blocked by a real missing/redacted description that cannot be closed. The remaining 33 D-bucket units are smaller named shapes (`class_modelled_but_no_observed_delta_on_the_rendered_snapshot` 17, `class_feature_no_dedicated_magnitude_id_matched_the_record_slug` 6, `class_feature_of_unmodelled_corpus_class` 3, `race_trait_record_loaded_but_never_applies` 2, `race_trait_generic_table...` 3) -- handle each using this bundle\'s own established precedented-formula approach (read the real corpus record, find or write a compute function, test it).',
  '',
  '**Ultimate Campaign -- 38 actionable units:**',
  '- bucket M: 36 units (30 ability, 6 unaccounted -- re-derive by kind).',
  '- bucket D: 2 units (trait).',
  '',
  '=== HOW TO SEQUENCE YOUR OWN WORK GIVEN THE SCALE ===',
  '',
  'This is a big pool with four structurally different fix shapes (M=write missing formulas, B=add missing table entries, C=fix wiring/surfacing gaps, D=mixed, investigate). Prioritize in this order for throughput: M first (most units, most mechanical, most similar to prior waves\' own work), then B (mechanical table-entry additions once you find the pattern), then D\'s dominant shape (276 units, but needs the investigation described above first), then C (fewest units, most investigation-heavy per unit), then D\'s smaller named shapes. Work through as much of the total 1,781 as you reasonably can in this one session. Do not stop after a small slice "to keep it manageable" -- that has been explicitly rejected. If you must stop before finishing everything, stop after a CLEAN checkpoint (verified, committed) rather than mid-investigation.',
  '',
  '=== STANDING RULES (these override any instinct to go slow/small/redundant) ===',
  '',
  '1. **BATCH BIG.** See above.',
  '2. **VERIFY ONCE.** Write ALL your code/tests/table-entries/wiring-fixes for everything you are closing FIRST. Then run the full verification sequence (lib tests, full integration suite, clippy, guarded regen, completion_atlas.py check, denominator_gate.py) ONE time, at the very end. Do NOT run the full suite after every kind or every few units. Only re-run a second time if that one pass actually finds something broken -- fix it, re-verify once more, stop there.',
  '3. Before grounding ANY corpus record shaped `# <X> choices` / `BONUS:ABILITYPOOL|<X>|1`, check whether it is a real one-of-N choice needing `choice_selection(input, <CHOICE_ID>)` gating rather than an unconditional grant -- the single most common real bug this bundle has found.',
  '4. `cargo clippy --locked --tests -j 6` enforces a ZERO-warning ceiling -- run it as part of your one verification pass and fix anything flagged.',
  '5. Commit as soon as your one verification pass is genuinely clean -- do not sit on a large uncommitted diff.',
  '6. Given the SCALE of this wave, consider committing in a SMALL number of logical checkpoints (e.g., one commit after M is done and independently verified, another after B, etc.) rather than one giant commit at the very end IF that helps you avoid losing a huge amount of work to a single mistake -- but each checkpoint commit still only happens after that checkpoint\'s own work is genuinely complete and passes at least a targeted test check; do not commit broken/untested intermediate states, and do not multiply this into the same "many small full-suite-verify cycles" pattern that was already rejected -- a full integration-suite verification still only needs to happen ONCE, covering everything, at the true end.',
  '',
  'REQUIRED READING (quick, not busywork): docs/release/SD-34-book-completion/decisions.md section 22\'s most recent few WAVE UPDATEs, and progress.md\'s two most recent entries, for house style.',
  '',
  '=== VERIFICATION (ONE PASS, at the end) ===',
  '',
  '1. `cargo test --locked --lib -j 6` AND the FULL `cargo test --locked --no-fail-fast -j 6`, ONE time.',
  '2. `cargo clippy --locked --tests -j 6` as part of the same pass, fix anything flagged.',
  '3. If both clean: proceed. If not: fix, re-run ONE more time, stop there once clean.',
  '4. If your changes touch anything F1-shaped or otherwise change the corpus-wide formula-family population, re-derive the pin in `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` via `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus`.',
  '5. Guarded regen: `cargo run --locked --bin v06_work_inventory` (generate `CORPUS_LITERAL_SWEEP_REPORT` / `DERIVED_FIXTURE_CHECK_REPORT` prerequisites first).',
  '6. Before/after: `python3 scripts/completion_atlas.py --check` on both snapshots plus a direct id->status join over the specific units you targeted, confirming zero collateral movement. Report real bucket deltas.',
  '7. `python3 scripts/denominator_gate.py --check` directly.',
  '8. Re-run `completion_atlas.py --check` and the citation checks ONE final time AFTER any late fix (including a late clippy fix) -- this must be the LAST thing checked before commit (wave 48\'s own lesson: a late `.rs` edit shifts line-number citations across completion_atlas.py/shape_engine_boundary.py/missing_engine_tables.py/test_shape_engine_boundary.py).',
  '9. Update `scripts/verify-baselines.env` if lib/full test counts moved (raise only).',
  '10. Write ONE cycle receipt at `docs/release/SD-34-book-completion/artifacts/bucket-d-mining/wave50_core_rulebook_ultimate_campaign_cycle_receipt.md` -- given the scale, a table of bucket/kind/units-closed/units-skipped-with-reason is the right format, not exhaustive per-unit prose.',
  '11. Prepend ONE cycle entry to progress.md, update kanban.md if used.',
  '12. Add ONE dated update to decisions.md section 22 ("WAVE 50 UPDATE, <date>: <summary>") -- name the real root-cause breakdown you found for each bucket, especially the dominant D-bucket "zero magnitude pending wiring class review" shape and whatever you found wiring bucket C\'s "computed but never surfaced" gap.',
  '13. Commit on tranche/14. Do NOT push.',
  '',
  'NOTE ON SHARED CHECKOUT: another concurrent actor (or wave 49) may touch shared files -- if you see unexpected changes to files like docs/retro/events/*.jsonl that you did not touch, leave them uncommitted.',
  '',
  'Report back: exact commit SHA(s), the fresh Core Rulebook + Ultimate Campaign actionable population you derived (by bucket), the root-cause breakdown of bucket D\'s dominant shape and bucket C\'s wiring gap, units closed by bucket/kind, confirmation the FULL integration suite passed and was run only once (or twice if you fixed something), confirmation clippy showed 0 warnings, whether the F1 pin needed updating, and what remains named for a future wave.',
].join('\n')

const build = await agent(buildPrompt, {
  label: 'build-wave50-core-rulebook-ultimate-campaign',
  phase: 'Build',
  model: 'claude-sonnet-5',
})

return { buildResult: build }
