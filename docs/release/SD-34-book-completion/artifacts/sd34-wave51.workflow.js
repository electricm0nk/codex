export const meta = {
  name: 'sd34-wave51',
  description: 'Wave 51: close as much of the ENTIRE Core Rulebook + Ultimate Campaign actionable remainder as possible in ONE pass (1,537 units across buckets B/C/D/M), single end-of-cycle verification, OPERATOR-DIRECTED opus build agent',
  phases: [{ title: 'Build' }],
}

const buildPrompt = [
  'You are working in /home/ubuntu/workspace/repos/codex on branch tranche/14 (a PF1e rules-engine repo, SD-34 bundle, docs/release/SD-34-book-completion/). This is a shared checkout -- set CARGO_TARGET_DIR=/tmp/claude-1000/-home-ubuntu-workspace-repos-codex-docs-release-SD-33-computed-value-verification/f34e35e3-6405-4a87-b612-555220a48600/scratchpad/cargo-target-wave51 for every cargo command you run (export it once at the start of your session).',
  '',
  'Run `git log --oneline -5` and `git status --porcelain` before you start -- this is a shared checkout and another concurrent lane may have committed or left uncommitted work. If you see unexpected uncommitted changes to files you did not touch (especially docs/retro/events/*.jsonl), do not discard them, leave them uncommitted, and proceed.',
  '',
  '=== YOU ARE RUNNING ON OPUS -- THIS IS A DELIBERATE OPERATOR OVERRIDE ===',
  '',
  'This bundle\'s standing model tier for a build agent is sonnet. The operator has EXPLICITLY requested opus for this wave specifically, because a large share of this wave\'s scope is genuinely new engine-chassis work, not filling in an existing table: 347 of the CR bucket-M units (ability/template/domain) have NO existing `grounded` precedent anywhere in this bundle to copy from, and CR bucket C (191 units / 103 option-pool groups: Rage Power, Rogue Talent, Dragon Disciple, Druid Domain, Arcane Archer, Elemental Bloodline, Monk, and ~96 more) is a GENERIC pool-group wiring gap that has only ever been solved twice before (Cleric Domain, Sorcerer Bloodline) and never generalized. Use the extra reasoning budget on those two shapes; the mechanical shapes (precedented M, table-entry B) do not need it as much.',
  '',
  '=== SCOPE: Core Rulebook + Ultimate Campaign ONLY, buckets B + C + D + M ===',
  '',
  'Wave 50 closed 343 units across these two books (commits 1a07f08bdc + 138f6bb288, independently re-confirmed at its own wave-end gate, commit 5f6b18f4e3). This is the fresh remainder AFTER that closure. Freshly re-derive it yourself before trusting these numbers (query `docs/work-inventory.json`, classify each unit\'s bucket using the SAME logic as `scripts/completion_atlas.py`\'s own `_bucket_of()` function -- read that function directly, it is short and authoritative) -- the corpus/classifier can shift between this dispatch and when you actually run:',
  '',
  '**Core Rulebook -- 1,499 actionable units (re-derived 2026-09-07 against docs/work-inventory.json generated_at 2026-09-07T08:00:34Z, n=6,701 CR units total):**',
  '- bucket M ("magnitude ingested, never computed or applied"): 778 units, by kind: ability 217, equipment_modifier 210, race_trait 121, template 96, feat 47, domain 34, equipment 19, skill 19, spell 15. Of these:',
  '  - **221 units (race_trait 121 + feat 47 + skill 19 + equipment 19 + spell 15) have an established `grounded` precedent elsewhere in this bundle** -- the mechanical, highest-confidence slice. Do these first.',
  '  - **347 units (ability 217 + template 96 + domain 34) have NO existing `grounded` precedent anywhere in this bundle** -- verify that claim fresh (grep for a `grounded` unit of the same kind before assuming), and where it still holds, this is genuinely new compute-path chassis work, not a missing formula in an existing path.',
  '  - **210 equipment_modifier units split by evidence**: 111 carry `equipment_table_entry_with_corpus_magnitude` (a real formula token IS present, recognized, just never computed -- mechanical) and 99 carry `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` (the magnitude lives on the CLOSURE record, not the modifier\'s own line -- these are player-choice-gated, not a lookup fix; apply the choice-gating check from standing rule 3 below before writing any of these).',
  '- bucket B ("table exists, record not in it"): 392 units, all `class_feature`, by evidence: `class_feature_option_pool_record_with_magnitude_not_held_by_engine` 205, `class_feature_owner_matched_by_name_but_record_not_held_by_engine` 162, `class_feature_option_pool_record_not_held_by_engine` 25. The 230 option-pool-shaped units (205+25) span 86 distinct pool-name groups (by corpus_key prefix before " ~ "; e.g. Domain Power 58, Domain Base 33, Draconic Bloodline Choice 10, Secret Lore 10, New Arcana 9 are the largest) -- find the table (likely a static array/map keyed by class or record slug in `src/rules_core/pilot_compute/mod.rs` or `src/bin/v06_work_inventory.rs`) and add the missing entries following the exact pattern already used for sibling entries in the same table.',
  '- bucket C ("held and computed, never surfaced"): 191 units, all `class_feature`, single evidence shape `no_explanation_id_and_no_diagnostic_names_this_feature`, across 103 distinct pool-name groups (Rage Power 13, Rogue Talent 10, Dragon Disciple 9, Druid Domain 7, Arcane Archer 6, Elemental Bloodline 6, Monk 5, Arcane Trickster 4, Barbarian 4, Fey Bloodline 4, and ~93 smaller groups). This is the SAME generic pool-group wiring shape already proven twice (Cleric Domain, Sorcerer Bloodline per `decisions.md` prior WAVE UPDATEs) but never generalized to the rest of these groups -- this is where a genuinely reusable fix (not another one-group patch) earns its keep; look hard for the chance to write ONE generic wiring path that clears many groups at once rather than 103 individual patches.',
  '- bucket D ("other engine gap"): 138 units, by kind/shape: ability 109 (evidence `ability_content_table_holds_zero_magnitude_record_pending_wiring_class_review` -- same shape wave 50\'s own gate found dominant; read `src/bin/v06_work_inventory.rs`\'s `table_holds_zero_magnitude_record_pending_wiring_class_review` function BEFORE fixing anything in this shape, it fires when a record is `text_only == true` but failed one of three promotion checks), class 17 (`class_modelled_but_no_observed_delta_on_the_rendered_snapshot`), class_feature 9 (6 `class_feature_no_dedicated_magnitude_id_matched_the_record_slug` + 3 `class_feature_of_unmodelled_corpus_class:animal`), race_trait 2 (`race_trait_record_loaded_but_never_applies`), template 1 (`template_content_table_holds_zero_magnitude_record_pending_wiring_class_review`).',
  '',
  '**Ultimate Campaign -- 38 actionable units (re-derived 2026-09-07, n=265 UC units total):**',
  '- bucket M: 36 units (ability 30, trait 6).',
  '- bucket D: 2 units (trait, `trait_content_table_holds_zero_magnitude_record_pending_wiring_class_review`).',
  '',
  'Grand total this wave\'s scope: **1,537 units**.',
  '',
  '=== HOW TO SEQUENCE YOUR OWN WORK GIVEN THE SCALE ===',
  '',
  'Four structurally different fix shapes (M=write missing formulas, B=add missing table entries, C=fix a wiring/surfacing gap -- ideally generically, D=mixed, investigate first). Prioritize for throughput: M\'s 221-unit precedented slice first (fastest, most mechanical), then B (mechanical once you find the pattern), then M\'s 347-unit new-chassis slice and the 210 equipment_modifier units, then D\'s dominant 109-unit ability shape (investigate the three-check function first), then C (fewest units but the highest-leverage fix if you can generalize it), then D\'s smaller named shapes, then UC\'s 38. Work through as much of the total 1,537 as you reasonably can in this one session. Do not stop after a small slice "to keep it manageable" -- that has been explicitly rejected by the operator repeatedly across this bundle. If you must stop before finishing everything, stop after a CLEAN checkpoint (verified, committed) rather than mid-investigation, and name exactly what remains by bucket/kind/shape for the next wave.',
  '',
  '=== STANDING RULES (these override any instinct to go slow/small/redundant) ===',
  '',
  '1. **BATCH BIG.** See above -- target the full 1,537, not a slice.',
  '2. **VERIFY ONCE.** Write ALL your code/tests/table-entries/wiring-fixes for everything you are closing FIRST. Then run the full verification sequence (lib tests, full integration suite, clippy, guarded regen, completion_atlas.py check, denominator_gate.py) ONE time, at the very end. Do NOT run the full suite after every kind or every few units. Only re-run a second time if that one pass actually finds something broken -- fix it, re-verify once more, stop there.',
  '3. **CHOICE-GATING CHECK.** Before grounding ANY corpus record shaped `# <X> choices` / `BONUS:ABILITYPOOL|<X>|1`, check whether it is a real one-of-N choice needing `choice_selection(input, <CHOICE_ID>)` gating rather than an unconditional grant -- the single most common real bug this bundle has found. This applies especially to the 99 choice-gated equipment_modifier units named above.',
  '4. `cargo clippy --locked --tests -j 6` enforces a ZERO-warning ceiling -- run it as part of your one verification pass and fix anything flagged.',
  '5. Commit as soon as your one verification pass is genuinely clean -- do not sit on a large uncommitted diff.',
  '6. Given the SCALE of this wave, consider committing in a SMALL number of logical checkpoints (e.g., one commit after M is done and independently verified, another after B, etc.) rather than one giant commit at the very end IF that helps you avoid losing a huge amount of work to a single mistake -- but each checkpoint commit still only happens after that checkpoint\'s own work is genuinely complete and passes at least a targeted test check; do not multiply this into the same "many small full-suite-verify cycles" pattern already rejected -- a full integration-suite verification still only needs to happen ONCE, covering everything, at the true end.',
  '7. **CITATION LINE NUMBER MUST BE LAST.** A late `.rs` edit (including a late clippy fix) shifts line-number citations across `completion_atlas.py`/`shape_engine_boundary.py`/`missing_engine_tables.py`/`test_shape_engine_boundary.py` (wave 48\'s own lesson, re-confirmed wave 50). Re-run `completion_atlas.py --check` and the citation checks ONE final time AFTER any late fix -- this must be the LAST thing checked before commit, never something checked once early and trusted.',
  '8. **DENOMINATOR MUST BE ON THE SAME RAW LINE.** Wave 50\'s own wave-end gate found `denominator_gate.py --check` failing 3 times for a bare percentage figure whose real denominator was stated on an adjacent markdown-wrapped line rather than the SAME raw line the gate scans (receipt, decisions.md, and progress.md each had one). When you write any `N/M` or `X%` figure in your receipt/decisions.md/progress.md entries, state the denominator on the exact same raw text line as the figure itself, not just nearby -- and run `denominator_gate.py --check` before you consider your docs done, not just the code.',
  '',
  'REQUIRED READING (quick, not busywork): docs/release/SD-34-book-completion/decisions.md section 22\'s most recent few WAVE UPDATEs (including the wave 50 one), and progress.md\'s two most recent entries (the wave 50 wave-end gate entry and the wave 50 cycle entry), for house style and to confirm exactly what wave 50 already closed so you do not re-derive stale precedent claims.',
  '',
  '=== VERIFICATION (ONE PASS, at the end) ===',
  '',
  '1. `cargo test --locked --lib -j 6` AND the FULL `cargo test --locked --no-fail-fast -j 6`, ONE time.',
  '2. `cargo clippy --locked --tests -j 6` as part of the same pass, fix anything flagged.',
  '3. If both clean: proceed. If not: fix, re-run ONE more time, stop there once clean.',
  '4. If your changes touch anything F1-shaped or otherwise change the corpus-wide formula-family population, re-derive the pin in `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` via `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus`.',
  '5. Guarded regen: `cargo run --locked --bin v06_work_inventory` (generate `CORPUS_LITERAL_SWEEP_REPORT` / `DERIVED_FIXTURE_CHECK_REPORT` prerequisites first).',
  '6. Before/after: `python3 scripts/completion_atlas.py --check` on both snapshots plus a direct id->status join over the specific units you targeted, confirming zero collateral movement. Report real bucket deltas.',
  '7. `python3 scripts/denominator_gate.py --check` directly (see standing rule 8 above).',
  '8. Re-run `completion_atlas.py --check` and the citation checks ONE final time AFTER any late fix (including a late clippy fix) -- this must be the LAST thing checked before commit (standing rule 7 above).',
  '9. Update `scripts/verify-baselines.env` if lib/full test counts moved (raise only).',
  '10. Write ONE cycle receipt at `docs/release/SD-34-book-completion/artifacts/bucket-d-mining/wave51_core_rulebook_ultimate_campaign_cycle_receipt.md` -- given the scale, a table of bucket/kind/units-closed/units-skipped-with-reason is the right format, not exhaustive per-unit prose.',
  '11. Prepend ONE cycle entry to progress.md, update kanban.md if used. Update progress.md\'s frontmatter `date:` field to the real date of your commit.',
  '12. Add ONE dated update to decisions.md section 22 ("WAVE 51 UPDATE, <date>: <summary>") -- name whatever generic fix you found (or attempted and why it did not generalize) for bucket C\'s pool-group wiring gap, and the real root-cause breakdown for whatever of bucket D/M\'s new-chassis shapes you closed.',
  '13. Commit on tranche/14. Do NOT push.',
  '',
  'NOTE ON SHARED CHECKOUT: another concurrent actor may touch shared files -- if you see unexpected changes to files like docs/retro/events/*.jsonl that you did not touch, leave them uncommitted.',
  '',
  'Report back: exact commit SHA(s), the fresh Core Rulebook + Ultimate Campaign actionable population you derived (by bucket), whatever generic fix (or attempted-and-rejected generic fix) you found for bucket C\'s wiring gap, units closed by bucket/kind, confirmation the FULL integration suite passed and was run only once (or twice if you fixed something), confirmation clippy showed 0 warnings, whether the F1 pin needed updating, confirmation denominator_gate.py passed with same-line denominators, and what remains named for a future wave.',
].join('\n')

const build = await agent(buildPrompt, {
  label: 'build-wave51-core-rulebook-ultimate-campaign',
  phase: 'Build',
  model: 'opus',
})

return { buildResult: build }
