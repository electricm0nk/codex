export const meta = {
  name: 'sd34-wave44',
  description: 'Wave 44: fix census_prestige_class_entry_requirements.py name-collision bug + 4 cheap classifier-collision fixes',
  phases: [{ title: 'Build' }],
}

const buildPrompt = [
  'You are working in /home/ubuntu/workspace/repos/codex on branch tranche/14 (a PF1e rules-engine repo, SD-34 bundle, docs/release/SD-34-book-completion/). This is a shared checkout -- set CARGO_TARGET_DIR=/tmp/claude-1000/-home-ubuntu-workspace-repos-codex-docs-release-SD-33-computed-value-verification/f34e35e3-6405-4a87-b612-555220a48600/scratchpad/cargo-target-wave44 for every cargo command you run (export it once at the start of your session).',
  '',
  'REQUIRED READING FIRST: docs/release/SD-34-book-completion/decisions.md section 22 (read the whole section, including the "WAVE 43 UPDATE" at the end -- it is the audit that scoped this wave) and docs/release/SD-34-book-completion/progress.md (the two most recent entries, "Wave 43 wave-end gate" and "Wave 43", for house style and the exact process lesson from wave 42: any wave that adds compute or changes classification MUST run the FULL `cargo test --locked --no-fail-fast` integration suite itself, not just lib tests, before claiming done -- wave 42 skipped this and caused a real regression that had to be recovered.',
  '',
  'This wave has TWO independent pieces of work. Do them both, in either order, but verify each on its own before moving to guarded regen.',
  '',
  '=== PIECE 1: fix scripts/census_prestige_class_entry_requirements.py (lines 97-98) ===',
  '',
  'The audit found a REAL BUG, not a classifier-tuning issue: `prestige_names.setdefault(name, path)` keys purely by display name across the FULL 158-book oracle. When a class name also appears in an older, un-ingested oracle book (a predecessor of an ingested book -- e.g. psionics_unleashed/psionics_expanded predecessors of the ingested ultimate_psionics), `os.walk` iteration order can let the non-ingested file win the race, and the real ingested entry is silently dropped forever (the script`s own `if matched_book is None: continue` then discards it).',
  '',
  'Confirmed affected: Phrenic Slayer, Thrallherd, Psychic Fist, War Mind, Elocater, Psion Uncarnate, Pyrokineticist, Metamind, Cerebremancer, Pathfinder Savant, Student of War, Pathfinder Delver, Gifted Blade -- 13 classes, 191 units total, all currently missing from `tests/fixtures/rules_core/prestige-class-entry-requirements.json`.',
  '',
  'Fix: change the script`s matching logic to prefer an INGESTED book`s file over a non-ingested one when the same display name collides (check the script`s own definition of "ingested" -- it likely already has a concept of which books are in the corpus vs. the wider oracle; use that, don`t invent a new one). Do NOT just take the first match differently -- make the preference explicit and correct: an ingested-book match must always win over a non-ingested one, regardless of walk order.',
  '',
  'After fixing: regenerate `tests/fixtures/rules_core/prestige-class-entry-requirements.json` by re-running the script. Verify:',
  '  - All 13 previously-missing classes now appear with correct PRE* entry-requirement data (spot-check at least 3 against their real corpus `.lst` source lines directly, e.g. Phrenic Slayer against `up_classes.lst:922-926`).',
  '  - None of the 62 currently-registered classes regressed, changed incorrectly, or disappeared (diff the fixture before/after -- every pre-existing entry should be either unchanged or, if it happened to be one of the 13 collision victims, now correct).',
  '  - Run whatever test suite covers this fixture/script (search for its own test file, likely something like scripts/tests/test_census_prestige_class_entry_requirements.py or similar -- check scripts/tests/ directory) and confirm it passes, or add/update a test if none exists that would have caught this exact bug (a real regression test: the collision scenario, an ingested vs non-ingested book sharing a class name).',
  '',
  '=== PIECE 2: 4 confirmed cheap classifier-collision fixes ===',
  '',
  'These are the SAME misclassification SHAPE already fixed for Turn Undead/Fighter/Psychic earlier this bundle (see decisions.md §22 for those precedents) -- a corpus record`s own "class" field collides with an unrelated bestiary/pseudo-class name, so the classifier in `src/bin/v06_work_inventory.rs`\'s `classify()` function fails to resolve the true owner. Each of these 4 needs BOTH a classifier-recognition fix (so the unit resolves to its real owner) AND, where the owner doesn\'t yet ground the specific fact, a small precedented compute function (same shape as wave 42/43\'s antipaladin-template-copy pattern).',
  '',
  '1. `core_rulebook:class_feature:power_over_undead_turn_undead` -- corpus record`s "class" field is literally "Undead" (collides with the bestiary\'s unmodelled Kind::Class "Undead" pseudo-class). Real owner is Cleric. Cleric already grounds `channel_energy_dice`/`channel_energy_uses_per_day` (mod.rs:47287-47310) -- only the DC (`10+level/2+CHA`) is missing. `warpriest_channel_energy_dc` (mod.rs:20198) is a literal template for this exact formula shape -- read it and copy the pattern for Cleric.',
  '',
  '2. `advanced_players_guide:class_feature:order_of_the_dragon` -- corpus record`s "class" field is "Dragon" (collides with bestiary "Dragon"). Real owner is Cavalier. `cavalier_generic_order_description_pass_grounds_order_of_the_beast` (mod.rs:79499) and `cavalier_order_of_the_sword_sense_motive_bonus` (mod.rs:2838, formula shape `CavalierLVL/4`, `max(1,CavalierLVL/2)`) are near-identical templates -- read the actual corpus record for Order of the Dragon`s real formula before assuming it matches exactly; verify against the corpus token, don`t just copy blindly.',
  '',
  '3. `adventurers_guide:class_feature:padfe_construct` and `padfe_ooze` (and check for a `favored_enemy_undead` sibling too) -- corpus records have `SOURCELONG: "Core Rulebook"`, "class" field "Construct"/"Ooze" (collide with bestiary type names). Real owner is Ranger. Ranger`s `choice:ranger_favored_enemy` is explicitly built as an open-ended, non-restricted-list recognizer (mod.rs:1043 doc comment says so) -- these should already be reachable through it once the classifier correctly resolves the owner; verify they actually flow through end-to-end rather than assuming the doc comment guarantees it.',
  '',
  '4. `inner_sea_magic:class_feature:eidolon_companion_progression_standard` ("class" field "Eidolon", collides with bestiary) and `occult_adventures:class_feature:phantom_emotional_focus_despair` ("class" field "Phantom Emotional Focus") -- both companion-pseudo-classes already have real, wired compute: `ground_summoner_eidolon` (mod.rs:14203) and `ground_spiritualist_class_features` (mod.rs:30080, already emitting `spiritualist.phantom.master_level`/`shared_consciousness.focus_pool`). These are misrouted, not unmodelled -- verify the classifier fix alone is sufficient (no new compute needed) by checking whether the existing functions already produce the exact fact these misrouted units need.',
  '',
  'ALSO CHECK (mentioned in the audit but not fully confirmed): `occult_adventures:class_feature:psychic_detective_expanded_arcana_1x8` -- the corpus`s own `CLASS:Psychic Detective` is `VISIBLE:NO`; it`s an Investigator ARCHETYPE, not a distinct class (mirrors the archetype-owner-recognition shape §22 already fixed for Fighter/Psychic). If this is straightforward given the existing archetype-recognition precedent, fix it too as a 5th item; if it turns out more involved, it`s fine to leave it named and unclosed for a future wave -- do not force it.',
  '',
  'For EVERY unit you touch in Piece 2: read the actual corpus record directly before writing any code (never trust the audit`s prose description of the formula -- verify against the real `.lst` token). Write direct pure-formula tests for each new compute function (following this bundle`s own established test-naming conventions -- look at `wave43_prestige_class_new_compute_tests` or similar recent test modules for the pattern), plus reachability tests proving the classifier fix actually resolves the record end-to-end through the real pipeline, not just a synthetic fixture.',
  '',
  '=== VERIFICATION (both pieces) ===',
  '',
  '1. Run `cargo test --locked --lib -j 6` AND the FULL `cargo test --locked --no-fail-fast -j 6` integration suite. Both must pass clean. This is not optional -- it is the exact step wave 42 skipped.',
  '2. If your changes touch anything F1-shaped (a bare-literal magnitude, no per-level/ability/pool expression) or otherwise change the corpus-wide formula-family population, re-derive the pinned census test in `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` (`f1_population_matches_the_current_true_formula_bearing_count_...` and any sibling F-family tests) via `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` and update the pin following its own established doc-comment convention if it moved -- do this BEFORE the guarded regen, not after.',
  '3. Guarded regen: run `cargo run --locked --bin v06_work_inventory` (it will refuse until `CORPUS_LITERAL_SWEEP_REPORT` and `DERIVED_FIXTURE_CHECK_REPORT` prerequisites are generated fresh -- generate both first, the same guard every prior wave has hit).',
  '4. Before/after: `python3 scripts/completion_atlas.py --check` on both snapshots, plus a direct Python id->status join over the specific units you targeted, confirming exactly the units you intended moved and nothing else did (zero collateral movement). Report the real DONE/D/V bucket deltas.',
  '5. Update `scripts/verify-baselines.env` if lib/full test counts moved (raise only, following the file`s own dated-comment-block convention).',
  '6. Write a cycle receipt at `docs/release/SD-34-book-completion/artifacts/bucket-d-mining/wave44_census_bug_and_classifier_collisions_cycle_receipt.md` (follow the format of the wave 43 receipt in the same directory).',
  '7. Prepend a cycle entry to `docs/release/SD-34-book-completion/progress.md` (house style -- see wave 43`s own entry for the pattern) and update `kanban.md` if this bundle uses one (check for its existence first).',
  '8. Add a dated update to `docs/release/SD-34-book-completion/decisions.md` §22 (append at the end, "WAVE 44 UPDATE, <date>: <summary>") documenting what closed and what, if anything, remains open or deferred (e.g. Psychic Detective if not attempted, Cerebremancer`s manifester-stacking case which is explicitly out of THIS wave`s scope per the audit).',
  '9. Commit your work on `tranche/14` in as few commits as make sense (a feat commit for the code+tests, a docs commit for any receipt-SHA fill-in, following this bundle`s own established two-commit pattern from waves 41-43). Do NOT push -- the orchestrator runs the final isolated verify.sh gate and handles the merge.',
  '',
  'Report back: exact commit SHA(s), units closed (id list with before/after bucket), confirmation the FULL integration suite passed (paste the actual pass/fail counts, not just "it passed"), whether the F1/shape_ledger pin needed updating and by how much, and anything you found that should scope a future wave (e.g. Psychic Detective status, Cerebremancer, or any other class-collision pattern you notice while in there).',
].join('\n')

const build = await agent(buildPrompt, {
  label: 'build-wave44-census-bug-and-classifier-collisions',
  phase: 'Build',
  model: 'claude-sonnet-5',
})

return { buildResult: build }
