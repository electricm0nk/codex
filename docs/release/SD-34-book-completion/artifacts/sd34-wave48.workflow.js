export const meta = {
  name: 'sd34-wave48',
  description: 'Wave 48: registered-prestige-class magnitude formulas from sub-mechanism-5 remainder (385 units across non-manifester-level classes)',
  phases: [{ title: 'Build' }],
}

const buildPrompt = [
  'You are working in /home/ubuntu/workspace/repos/codex on branch tranche/14 (a PF1e rules-engine repo, SD-34 bundle, docs/release/SD-34-book-completion/). This is a shared checkout -- set CARGO_TARGET_DIR=/tmp/claude-1000/-home-ubuntu-workspace-repos-codex-docs-release-SD-33-computed-value-verification/f34e35e3-6405-4a87-b612-555220a48600/scratchpad/cargo-target-wave48 for every cargo command you run (export it once at the start of your session).',
  '',
  'IMPORTANT: this VM was JUST rebooted and its RAM allocation was JUST lowered (176GB -> 143GB) by the operator to relieve host-side memory overcommit. There is no known reason this should affect a normal single-lane cargo build, but if you see anything unusual (OOM-kill, an unexpectedly slow build, a process silently dying) do not assume it is your own bug -- check `free -h` and report it honestly rather than guessing.',
  '',
  'REQUIRED READING FIRST: docs/release/SD-34-book-completion/decisions.md section 22 in full (the WAVE 44 through WAVE 47 UPDATEs at the end are the most important -- they scope this wave) and docs/release/SD-34-book-completion/progress.md\'s two most recent entries ("Wave 47 wave-end gate" and "Wave 47 -- Divine Scion...") for house style and process lessons.',
  '',
  'CRITICAL LESSONS FROM THIS BUNDLE\'S OWN HISTORY (apply throughout, not optional caution):',
  '- Wave 43\'s own audit made 4 classifier-collision "real owner" claims; 3 of the 4 turned out WRONG when wave 44 actually read the corpus records. NEVER trust any prior wave\'s prose characterization of a unit\'s owner, formula shape, or "already wired" status without reading the actual corpus record yourself first.',
  '- Wave 44\'s own cycle ran its full integration suite once, hit a failure, fixed it, but never re-ran the full suite end-to-end afterward. If you make ANY code change after your first full-suite pass, re-run the FULL suite a SECOND time end-to-end, not just a lib-only recheck.',
  '- Waves 45 and 46 both caught their own miscounts via their own tests\' first assertion failing for the right reason (RED before GREEN) -- write your own tests to actually verify counts/populations against real files, don\'t just assert what you expect.',
  '- Wave 46\'s own audit found that at least 9 Ultimate Psionics prestige classes share the IDENTICAL cross-class-manifester-level shape already excluded for Phrenic Slayer. **Skip ALL of these** (Sighted Seeker, Thrallherd, Psion Uncarnate, Cerebremancer, Metamind, Elocater, Psicrystal Imprinter, Soul Archer, Metaforge) plus Phrenic Slayer\'s own remainder -- one harder subsystem question, deliberately out of scope here.',
  '- Wave 47\'s original build agent (a Divine Scion cycle) wrote real, substantial code, then STALLED for ~48 minutes without committing, writing a receipt, or running verification -- a recovery cycle found the work and, critically, found a real correctness bug the stalled draft\'s own code had introduced: it ground two `ABILITYPOOL`-gated one-of-N choice records (Domain Specialization, Opposition Alignment) UNCONDITIONALLY for every character at once, instead of gating them on the character\'s actual recorded selection. **Before grounding ANY corpus record shaped `# <X> choices` in its section header, or granted via a `BONUS:ABILITYPOOL|<X>|1` token, check whether the SAME class\'s other already-excluded units share the identical shape** -- this is exactly the defect class this lesson exists to catch. If you find a genuine `ABILITYPOOL` one-of-N choice, gate it with `choice_selection(input, <CHOICE_ID>)` the way ~30 existing call sites in this file already do (Cleric Domain, Sorcerer Bloodline, Divine Scion\'s own Domain Specialization/Opposition Alignment, etc) -- never ground all N branches simultaneously.',
  '- Wave 47\'s own wave-end gate caught a genuinely NEW kind of failure this bundle had not hit before: `cargo clippy --locked --tests -j 6` enforces a ZERO-warning ceiling, and even a behaviorally-correct, fully-tested change can trip it (3 `collapsible_if` warnings from ordinary nested `if let` blocks). **Run `cargo clippy --locked --tests -j 6` yourself before claiming done and fix any warning it reports** -- do not rely solely on the orchestrator\'s own final gate to catch this, it costs a whole extra recovery cycle.',
  '- Wave 47 also survived a mid-gate SERVER REBOOT with zero data loss because all real work was already committed to git before the reboot happened, and only a disposable `/tmp` scratch build cache was lost. **Commit your work as soon as it is genuinely complete and verified -- do not sit on a large uncommitted diff for extended periods**, since git commits are the only thing that reliably survives an unexpected host-level restart on this box.',
  '',
  'SCOPE: sub-mechanism-5 (`class_feature_of_unmodelled_corpus_class`, evidence code emitted at `src/bin/v06_work_inventory.rs`\'s `classify()` function -- search for the literal string to find the current line number, it shifts every wave). Freshly re-derived at this wave\'s own start: **591 total units**, split as follows -- **88 units are NOT registered** in the 74-entry prestige-class registry fixture and are OUT OF SCOPE by standing doctrine (named by slug/count: psychic_detective 18, animal 17, eidolon 16, phantom 9, plant 9, undead 8, dragon 8, gifted_blade 3); **116 units belong to the excluded cross-class-manifester-level group** named above (Sighted Seeker 21, Thrallherd 19, Psion Uncarnate 13, Elocater 13, Psicrystal Imprinter 12, Metamind 10, Phrenic Slayer 11, Cerebremancer 9, Soul Archer 4, Metaforge 4); **2 units are Divine Scion\'s own True Scion remainder** (a real `ABILITYPOOL|True Scion|1` mutually-exclusive choice this engine does not yet track selection state for -- do not attempt unless you find a genuinely new, clean way to track it). That leaves **385 units across the remaining ~50 registered prestige classes** as this wave\'s real working pool -- re-derive this fresh yourself before trusting these numbers, the registry fixture and corpus can shift between waves.',
  '',
  'The largest remaining candidates by raw count (re-verify yourself, do not trust blindly): cyphermage (17), twilight_talon (17), golden_legionnaire (16), psychic_fist (16), asavir (15), metamorph (15), war_mind (15), hellknight (14), adaptive_warrior (14), aldori_swordlord (13), sanguine_angel (13), body_snatcher (13), steel_falcon (12), lantern_bearer (11), magaambyan_arcanist (11), storm_kindler (11), westcrown_devil (11), pyrokineticist (11), aspis_agent (9), gray_corsair (9), pathfinder_savant (9), rivethun_emissary (9), telekinetic_weaponmaster (9), and roughly 25 more smaller classes (4-8 units each). NOTE: wave 47\'s own stalled build agent was seen investigating Hellknight (`HellknightArmorLVL`) and Aldori Swordlord in the PCGen oracle before it pivoted to Divine Scion -- there may be a little prior scoping context there, but verify everything from scratch, do not assume anything it half-investigated is correct or complete.',
  '',
  'YOUR JOB, in order:',
  '',
  '1. Re-derive the CURRENT sub-mechanism-5 population and its registered/not-registered/excluded-manifester-level/Divine-Scion-remainder split fresh -- do not trust this prompt\'s own 591/88/116/2/385 numbers at face value; the registry fixture and corpus can shift.',
  '2. Pick 2-5 prestige classes whose remaining units look cheapest/most precedented (same formula shapes already built repeatedly this bundle: favored-enemy-style choices, companion/order/discipline progressions, `bonus_pp_level`-style manifester-point totals, sneak-attack-dice-by-level, save-DC "10 + level-factor + ability modifier" idioms, `ABILITYPOOL`-gated one-of-N choices per the lesson above, etc). Close as many units as you can safely, correctly verify this cycle -- no fixed target count, prioritize correctness over volume.',
  '3. For EVERY unit: read its real corpus record directly (never trust a summary or a prior wave\'s characterization), find the literal formula token (BONUS:/DEFINE:/etc.), and check for an existing precedented compute function of the same shape in `src/rules_core/pilot_compute/mod.rs` before writing new code.',
  '4. Skip (name, do not attempt) anything needing genuinely new subsystem modeling with no precedent, or where the corpus record contradicts its own DESC prose in a way you cannot resolve by reading the raw token directly (authoritative-token-over-DESC-prose ruling, precedent: `warpriest_channel_energy_dc`).',
  '5. Write a pure-formula test for every new compute function (direct formula tests covering edge cases, PLUS a real-pipeline reachability test proving the classifier/dispatch chain resolves each record end-to-end -- follow wave 43-47\'s own test modules for the established pattern).',
  '',
  '=== VERIFICATION (mandatory) ===',
  '',
  '1. Run `cargo test --locked --lib -j 6` AND the FULL `cargo test --locked --no-fail-fast -j 6` integration suite. Both must pass clean. If you make ANY code change after your first full-suite run, re-run the full suite a SECOND time end-to-end before claiming done.',
  '2. Run `cargo clippy --locked --tests -j 6` yourself and fix any warning it reports (see the lesson above -- this bundle enforces a ZERO-warning ceiling).',
  '3. If your changes touch anything F1-shaped (a bare-literal magnitude, no per-level/ability/pool expression) or otherwise change the corpus-wide formula-family population, re-derive the pinned census test in `src/rules_core/pilot_compute/formula_interpreter_corpus_wide.rs` via `python3 scripts/shape_ledger.py --inventory docs/work-inventory.json --corpus-root data/corpus` and update the pin following its own established doc-comment convention BEFORE the guarded regen. Current pin: 5155.',
  '4. Guarded regen: run `cargo run --locked --bin v06_work_inventory` (generate `CORPUS_LITERAL_SWEEP_REPORT` and `DERIVED_FIXTURE_CHECK_REPORT` prerequisites first).',
  '5. Before/after: `python3 scripts/completion_atlas.py --check` on both snapshots, plus a direct Python id->status join over the specific units you targeted, confirming exactly the units you intended moved and nothing else did (zero collateral movement). Report the real DONE/D/B/V bucket deltas against this wave\'s own gate baseline (DONE 25458, D 2398, V 349, population=49438).',
  '6. Run `python3 scripts/denominator_gate.py --check` directly before finishing -- any percentage in your receipt or progress.md entry MUST carry its denominator on the same line.',
  '7. Update `scripts/verify-baselines.env` if lib/full test counts moved (raise only, following the file\'s own dated-comment-block convention -- current baseline is `BASELINE_ROOT_LIB_TESTS=3134` / `BASELINE_ROOT_FULL_TESTS=8562`).',
  '8. Write a cycle receipt at `docs/release/SD-34-book-completion/artifacts/bucket-d-mining/wave48_registered_prestige_magnitude_formulas_cycle_receipt.md` (follow the format of the wave 43-47 receipts in the same directory).',
  '9. Prepend a cycle entry to `docs/release/SD-34-book-completion/progress.md` (house style) and update `kanban.md` if this bundle uses one.',
  '10. Add a dated update to `docs/release/SD-34-book-completion/decisions.md` §22 (append at the end, "WAVE 48 UPDATE, <date>: <summary>") documenting the fresh sub-mechanism-5 population re-derivation, which classes you closed, what you skipped and why, and what is genuinely left for a future wave.',
  '11. Commit your work on `tranche/14` in as few commits as make sense (a feat commit for the code+tests, a docs commit for any receipt-SHA fill-in, following this bundle\'s own established two-commit pattern) AS SOON AS it is genuinely complete and verified -- do not sit on a large uncommitted diff (see the mid-gate-reboot lesson above). Do NOT push -- the orchestrator runs the final isolated verify.sh gate and handles the merge.',
  '',
  'NOTE ON SHARED CHECKOUT: another concurrent actor may be running verify.sh/reclaim.sh on this same shared checkout during your cycle -- if you see unexpected changes to files like docs/retro/events/*.jsonl that you did not touch, leave them uncommitted.',
  '',
  'Report back: exact commit SHA(s), the fresh sub-mechanism-5 population count you derived, which prestige classes you closed and how many units each, units closed (id list with before/after bucket), confirmation the FULL integration suite passed (paste actual pass/fail counts, confirm it was re-run a second time if you made post-first-run fixes), confirmation clippy showed 0 warnings, whether the F1/shape_ledger pin needed updating, and what remains named for a future wave.',
].join('\n')

const build = await agent(buildPrompt, {
  label: 'build-wave48-registered-prestige-magnitude',
  phase: 'Build',
  model: 'claude-sonnet-5',
})

return { buildResult: build }
