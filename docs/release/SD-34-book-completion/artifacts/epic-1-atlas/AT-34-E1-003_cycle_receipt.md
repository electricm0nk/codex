# Cycle 3 — Epic 1 Completion Atlas / AT-34-E1-003

- **Commit SHA:** `2ec0462736`
- **Files touched:** `scripts/missing_engine_tables.py` (new), `scripts/tests/test_missing_engine_tables.py` (new), `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/missing-engine-tables.json` (new, generated)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** "Bucket A is **8,463** units across **9** kinds with no engine table. Each is named with its population, **which books contain it**, and which books it would unblock. **Evidence:** `artifacts/epic-1-atlas/missing-engine-tables.json` — per kind: unit count, the per-book breakdown, the engine surface a table would attach to, and the books that reach zero bucket-A once it exists. **This is the direct answer to "do we need to build something after the shape engine runs".** **The book-coverage half is load-bearing**, not decoration: it is what identified `ultimate_campaign` as an almost-single-bucket book and corrected an earlier miscount of how many tables the Core Rulebook exercises (`decisions.md §7`)." (`epic-breakdown.md`, verbatim)
- **Figures + their re-derive commands:**
  - `population=8463 kinds=9` — `python3 scripts/missing_engine_tables.py --check` (denominator: bucket-A units of 49,438 total units in `docs/work-inventory.json`)
  - Per-kind counts (of the 8,463-unit bucket-A population): `ability=4337 template=2248 trait=487 deity=459 power=421 domain=183 skill=149 language=136 companion=43` — same command, `kinds.<kind>.count` in `missing-engine-tables.json`
  - `citation_failures=0` (denominator: 9 kind→engine-surface citations checked) — same command
  - Core Rulebook slice of bucket A, matching `technical-design.md §4` exactly: `ability=471 template=262 skill=110 domain=34 language=22 deity=21 companion=14` (of `core_rulebook`'s own 6,701-unit population; `core_rulebook` total bucket-A = 934, i.e. these seven sum to 934) — `python3 scripts/missing_engine_tables.py --check` then read `kinds.<kind>.by_book.core_rulebook`; cross-checked independently via `python3 scripts/completion_atlas.py --by-book | grep '^core_rulebook'` → `A=934(13.9%)` of `n=6701`
  - `ultimate_campaign` bucket-A slice: `ability=88 trait=154` (sum 242, of `ultimate_campaign`'s own 265-unit population) — same command; cross-checked via `python3 scripts/completion_atlas.py --by-book | grep '^ultimate_campaign'` → `A=242(91.3%)` of `n=265`, with `U=21 X=2` the only other non-zero buckets — confirms the epic-breakdown's "almost-single-bucket book" claim
  - Books whose entire bucket-A population zeroes out on one kind's table (`zero_bucket_a_books`, of that kind's own book set): `ability` → `inner_sea_faiths` (1 of 28 books); `language` → `inner_sea_temples` (1 of 16 books); `template` → `inner_sea_intrigue`, `ultimate_intrigue` (2 of 29 books); all other 6 kinds → 0 (every book they touch also carries at least one other bucket-A kind) — same command, `kinds.<kind>.zero_bucket_a_books`
- **Row-count command output:**
  ```
  $ python3 -c "import json; d=json.load(open('docs/release/SD-34-book-completion/artifacts/epic-1-atlas/missing-engine-tables.json')); print('rows=',len(d['kinds']),'population=',d['population'])"
  rows= 9 population= 8463
  ```
  9 rows (one per kind) matches the criterion's stated "9 kinds" exactly; `population=8463` matches the criterion's stated bucket-A total exactly.
- **Build scope verified:** `cargo test --locked --no-run` exit 0, run at `2ec0462736` (after this cycle's only commit — a Python-only change, no Rust source touched); `cargo test --locked --lib` not re-run this cycle (no Rust change to verify; inherited from AT-34-E1-002's last-verified state); `apps/desktop/src-tauri` not touched, not run.
- **Sweep population:** N/A — this cycle adds no corpus records and regenerates none; `docs/work-inventory.json` is read-only this cycle.
- **Oracle pin:** N/A — no figure in this cycle came from the pinned PCGen corpus; every figure is derived from `docs/work-inventory.json`.
- **Status:** complete
- **Movement, four buckets:** reclassification — no unit moved bucket; this cycle re-partitions the already-fixed 8,463-unit bucket-A population by `kind` and `book`, a finer view of an existing partition, not new closure work.
- **Notes:**
  - `missing_engine_tables.py` deliberately re-derives bucket A from the raw `(status, evidence)` rule (`status == not-ingested` and `evidence` contains `has_no_engine_table`) rather than importing `completion_atlas.py`'s `_bucket_of` and filtering, so the two instruments stay independently checkable against each other — both this cycle's `population=8463` and `completion_atlas.py`'s committed `buckets.A.count=8463` were compared and agree.
  - Verified live that a 10th `Kind::MonsterAbility` arm exists in `v06_work_inventory.rs` emitting the same `has_no_engine_table` marker shape, but it contributes **zero** live bucket-A units — its 3,806 units are `text-complete` (2,092), `grounded` (1,453), `literal-verified`/`fixture-verified` (248), and 13 `not-ingested` units that land in bucket B (`*_absent_from_*`), not A. This is dead-but-defensive code for the current corpus, correctly excluded from the 9-kind, 8,463-unit population — confirmed by reading the corpus, not the code path alone (`decisions.md §12` L1's own lesson, applied here).
  - `UnknownKindError` is the fail-closed guard specified for this instrument: a kind reaching bucket A with no `ENGINE_SURFACE_CITATIONS` entry raises rather than silently omitting units from the report, mirroring `completion_atlas.py` condition 6. Exercised by `test_unclassified_kind_is_a_defect_not_silently_dropped`.
  - "The engine surface a table would attach to" is reported as the exact `not_ingested(...)` match-arm citation (`file:line:must_contain`) in `v06_work_inventory.rs` that currently emits each kind's bucket-A marker — the arm a real per-kind table lookup would replace. `citation_failures()` re-verifies all 9 against the live file content (not just path/line) every run, the same posture `completion_atlas.py` condition 6 established (`risks-and-open-questions.md §10`).
- **Next-cycle plan:** AT-34-E1-004 — state the shape-engine boundary as a committed fact (`artifacts/epic-1-atlas/shape-engine-boundary.md`), re-verifying the 26,396/13,119 magnitude counts and the promotion-ladder line number at HEAD.
