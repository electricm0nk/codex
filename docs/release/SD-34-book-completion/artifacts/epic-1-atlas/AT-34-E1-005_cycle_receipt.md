# Cycle 1 — Epic 1 Completion Atlas / AT-34-E1-005

- **Commit SHA:** (this cycle's commit — see push step; parent `0d9fcc8ec89abe3f1608a59886a83c2ba6e918bc`)
- **Files touched:** 79 total —
  `src/bin/v06_work_inventory.rs` (the field's definition site, `not_ingested`/`not_ingested_owned`
  closures and every `status: "not-ingested"` literal); `docs/work-inventory.json` (26,239 of 26,239
  live occurrences of the `"not-ingested"` string, in `status_vocabulary`, per-unit `status`, and
  per-book `by_status` breakdowns); `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json`
  (regenerated by `scripts/completion_atlas.py --check`, which re-derives `derived_at` at current
  HEAD); every consumer under `tests/`, `src/`, `apps/`, `scripts/` — full list:
  `git diff --name-only HEAD~1 -- tests/ src/ apps/ scripts/ docs/work-inventory.json 'docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json'`
  (78 files; the 79th is the new test below). Full enumeration reachable from the pre-rename sweep:
  `grep -rl 'not-ingested\|not_ingested' tests/ src/ apps/ scripts/ 2>/dev/null | wc -l` → `76` (the
  starting file count; two more — `docs/work-inventory.json` and `completion-atlas.json` — sit
  outside those four directories and are named explicitly by the criterion).
  New: `scripts/tests/test_legacy_not_ingested_string_swept.py`.
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** "The field renamed to state what it means (e.g. `engine-does-not-hold`)
  across `src/bin/v06_work_inventory.rs`, `docs/work-inventory.json`, and every consumer, with
  RED→GREEN. A count sweep across `tests/`, `src/`, `apps/`, `scripts/` for the old string,
  reported. The atlas's A/B/C/D arms key on this string and are updated in the same cycle."
  (`epic-breakdown.md`, `### AT-34-E1-005`, verbatim.)

- **Figures + their re-derive commands:**
  - Old-string live-use count across the four scanned directories, **before** this cycle's rename:
    `grep -rl 'not-ingested\|not_ingested' tests/ src/ apps/ scripts/ | wc -l` → `76` (of the full
    repo tree under those four roots — the sweep's own denominator; historical `docs/retro/` and
    other bundles' `docs/release/SD-2x.../` package docs are out of scope by the criterion's own
    directory list and were left untouched).
  - Old-string live-use count, **after**: `python3 -m unittest scripts.tests.test_legacy_not_ingested_string_swept -v`
    → both tests `ok` (0 hits; see row-count row below for the literal sweep output).
  - `docs/work-inventory.json` occurrences renamed: `grep -c '"not-ingested"' docs/work-inventory.json`
    → `0` (was `26239` of `26239` total field-name occurrences before the rename, same command
    pre-edit).
  - New status label present and consistent: `grep -c '"engine-does-not-hold"' docs/work-inventory.json`
    → `26239` (exact parity with the pre-rename count — a pure relabel, no unit reclassified).
  - Population unaffected (this is a label rename, not a reclassification):
    `python3 scripts/completion_atlas.py --check` →
    `population=49438 buckets=10 unclassified=0 overlap=0` with bucket `D: 1230` (the bucket the
    renamed status feeds) unchanged from the pre-cycle atlas.
  - Denominator gate on this package: `python3 scripts/denominator_gate.py --check 'docs/release/SD-34-book-completion/*.md'`
    → `files_checked=15 violations=0`.

- **Row-count command output:**
  ```
  $ python3 -c "
  import sys; sys.path.insert(0, '.')
  from scripts.tests.test_legacy_not_ingested_string_swept import sweep
  hits = sweep()
  print('legacy_not_ingested_live_uses =', len(hits))
  print(hits)"
  legacy_not_ingested_live_uses = 0
  {}
  ```
  Population scanned: every `.rs`/`.py`/`.ts`/`.tsx`/`.js`/`.json`/`.md`/`.html`/`.env` file under
  `tests/`, `src/`, `apps/`, `scripts/` (this test file's own path excluded, since its docstring
  and regex necessarily name the retired spelling). Count is `0` of that population — the
  criterion's own bar ("returning 0 live uses").

- **Build scope verified:**
  - `cargo build --bin v06_work_inventory` — exit 0 (warnings only, pre-existing dead-code lints,
    no new errors), run at this cycle's working tree.
  - `cargo test --locked --no-run` (full workspace) — **exit 0**, run at commit
    `0d9fcc8ec89abe3f1608a59886a83c2ba6e918bc` plus this cycle's uncommitted diff (log:
    `/tmp/ws_norun.log`, `EXIT:0`).
  - `apps/desktop/src-tauri` — `cargo check --locked` — **exit 0** (separate cargo workspace,
    tested explicitly per `decisions.md §10`; touched here via `character_hub.rs`,
    `spell_catalog.rs`, `reach_gate.rs`).
  - Targeted suites, same tree: `cargo test --locked --test v06_work_inventory --test
    equipment_gap_tables --test feat_gap_tables --test derived_evaluator_fixture_check --test
    derived_evaluator_fixture_check_class_feature_description` → 16 passed, 1 pre-existing
    `#[ignore]` (the 2-minute double-run determinism test), 0 failed.
  - Python consumers exercised directly: `python3 -m unittest scripts.tests.test_completion_atlas
    scripts.tests.test_missing_engine_tables scripts.tests.test_shape_ledger
    scripts.tests.test_shape_engine_boundary scripts.tests.test_row17_census
    scripts.tests.test_reachability_audit scripts.tests.test_audit_fallback_join_identity
    scripts.tests.test_ingest_generic_kind scripts.tests.test_ingest_race_trait_generic
    scripts.tests.test_ingest_companion_idempotent_rerun scripts.tests.test_sample_ground_truth_units
    scripts.tests.test_pf1e_dashboard_producer` → all `OK`.
    `scripts.tests.test_transcribe_monster_tables` — 1 pre-existing failure
    (`test_an_ability_no_bundle_names_stays_an_orphan_and_is_not_shipped`), **confirmed pre-existing
    and unrelated to this cycle**: reproduced identically by temporarily restoring both
    `scripts/transcribe_monster_tables.py` and its test to `HEAD` (`git checkout HEAD --
    <both files>`, re-run, same failure, then the rename diff restored from a saved copy) —
    the assertion fails on an unrelated concatenated-ability-text shape, with no `not-ingested`
    string anywhere in the failing assertion or its traceback.
  - `pytest` is not installed in this environment (`ModuleNotFoundError` if invoked); every Python
    suite above ran via `python3 -m unittest`, this repo's standing convention
    (`scripts/tests/test_completion_atlas.py` itself imports only `unittest`).

- **Sweep population:** N/A — this cycle renamed a status label; it did not add or regenerate any
  `data/corpus/` record, so `corpus_literal_sweep`'s examined-population is untouched
  (`decisions.md §12` L8 does not apply here).

- **Oracle pin:** N/A — no figure in this cycle was derived from the pinned PCGen oracle checkout;
  `docs/work-inventory.json`'s data content (which units exist, which status each carries) is
  unchanged, only the status label's spelling is.

- **Status:** complete

- **Movement, four buckets:**
  - **Closure:** the `not-ingested` misnomer itself — the field now states what it means
    (`engine-does-not-hold`), closing `decisions.md §2b` and `workflow-instruction.md §12` row 17.
  - **Reclassification:** none — every unit keeps its exact prior status; only the label's
    spelling changed (verified by the before/after occurrence-count parity above,
    `26239` → `26239`).
  - **Reachability:** none.
  - **Instrument-correction:** the atlas's bucket-D citation (`scripts/completion_atlas.py`,
    `BUCKET_DEFINITIONS["D"]["citation"]["must_contain"]`) was updated from `"not-ingested"` to
    `"engine-does-not-hold"` in the same cycle, so its condition-6 citation (`decisions.md §2` /
    `AT-34-E1-002` condition 6) keeps resolving against the renamed line rather than going stale.

- **Notes:**
  - `docs/work-inventory.json` was relabeled via a validated, whole-file string substitution
    (`sed 's/not-ingested/engine-does-not-hold/g'`, verified `python3 -m json.tool` before and
    after, and occurrence-count parity before/after) rather than by re-running
    `cargo run --bin v06_work_inventory` against a live corpus checkout. Regenerating fresh would
    require also supplying `CORPUS_LITERAL_SWEEP_REPORT` and `DERIVED_FIXTURE_CHECK_REPORT`
    fixtures matching the exact prior run to avoid silently reverting the committed
    `literal-verified`/`fixture-verified` units back to their unverified ordinary status — a much
    larger, unrelated regression risk than a verified label substitution carries. The existing
    test `the_committed_inventory_is_well_formed_and_uses_only_declared_statuses`
    (`tests/v06_work_inventory.rs`) passed against the edited file, confirming every unit's
    status still resolves against a declared `status_vocabulary` key post-rename.
  - New/underscore identifier choice: `not_ingested`/`not_ingested_owned` (the Rust closures) →
    `engine_does_not_hold`/`engine_does_not_hold_owned`; the JSON/status string
    `"not-ingested"` → `"engine-does-not-hold"` — matching the epic-breakdown's own example
    verbatim.
  - Scope was bounded to `tests/`, `src/`, `apps/`, `scripts/` plus the two files the criterion
    names explicitly. Historical package docs elsewhere under `docs/release/SD-2x.../` and
    `docs/retro/events/*.jsonl` were left untouched deliberately — they are dated records of what
    was true when written, not live consumers, and rewriting them would falsify history rather
    than rename a field.
  - `docs/retro/events/sd31-transcribe.jsonl` (pre-existing dirty in the shared checkout at cycle
    start) was not touched, per the dispatch's explicit exclusion.

- **Next-cycle plan:** AT-34-E1-006 (figure-provenance `verify.sh` stage + denominator-gate
  default-scope widening) and AT-34-E1-007 (corpus-trap-audit stage) are next in Epic 1's
  sequence; both are currently `not-started` on `kanban.md`.
