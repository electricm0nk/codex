# Cycle — SD-34 wave 33, Lane A — `class_feature_*_held_by_*_table`'s 27-unit rung, checked individually against two sources

**Status: complete for 22 of 27; 5 named as next-cycle scope, not escalated.** Bucket D's
`class_feature_*_held_by_*_table` shape (four rungs: `weapon-proficiency` 3,
`weapon-and-armor-proficiency` 5, `class-skill-list` 10, `wizard-school-spell-list` 9 — 27
total) all carry `description: null` in `docs/work-inventory.json`. Every one of the 27 was
checked individually against two independent sources (the PCGen `cr_abilities_class.lst`
source rows, and the already-ingested corpus JSON's own `data.description` field) rather than
assumed from the shape. 22 genuinely have no upstream prose in either source and close this
cycle as `grounded` (bucket D → DONE). 5 (`weapon-and-armor-proficiency`) do have real,
already-ingested prose — their gap is a `pilot_compute` wiring gap, not a description gap —
and are named as next-cycle scope, not closed here and not escalated.

- **Commit SHA (this cycle's code + decisions.md §20, rebased onto tranche/14):**
  `9558f4a774f7877b07ec9653b77d1d5cfe47fdaa`
- **Commit SHA (this receipt + progress.md, landed on tranche/14):** *(reported in the
  structured output — cannot cite its own future hash)*
- **Files touched this cycle:**
  - `src/bin/v06_work_inventory.rs` — three `Kind::ClassFeature` `text_only` sub-arms
    (`weapon-proficiency`, `class-skill-list`, `wizard-school-spell-list`) gained an
    `if !has_real_description { return grounded/... }` branch ahead of their existing
    `engine_does_not_hold(...)` return; the `weapon-and-armor-proficiency` arm's own comment
    was corrected (it wrongly claimed `description: null` for a shape that in fact has real
    prose) but its runtime behavior is unchanged. Six new unit tests added (three positive —
    proseless key reaches the new closure — and three negative controls — the same key with a
    hypothetical `has_real_description: true` does NOT take the closure, proving the branch is
    content-gated, not a bare key match — plus one control pinning the
    weapon-and-armor-proficiency rung's own unchanged behavior even under
    `has_real_description: true`). Two pre-existing tests renamed/updated to match the new
    return value (`a_weapon_proficiency_grant_verified_against_the_class_table_leaves_bucket_b`
    → `..._with_no_upstream_prose_reaches_the_no_prose_done_closure`, and the equivalent
    wizard-school-spell-list test).
  - `docs/work-inventory.json` — regenerated by the binary; 22 units' `status` moved from
    `engine-does-not-hold` to `grounded` with the new evidence string. No other field on any
    of the 22 units changed.
  - `docs/release/SD-34-book-completion/decisions.md` — new `§20` records the method, the
    per-rung finding table, and the disposition for both the 22-unit closure and the 5-unit
    deferral.
  - `scripts/completion_atlas.py` — bucket A/B/C/V's four `BUCKET_DEFINITIONS` citation line
    numbers re-derived (lane A's own insertions shifted every citation below them by exactly
    +116 lines); each re-derived via fresh `grep -n` for the same unique literal, content read
    back and confirmed unchanged in substance.
  - `docs/release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json` —
    regenerated at this cycle's own final commit via `python3 scripts/completion_atlas.py
    --check` (the command itself writes this artifact); only `derived_at` differs from the
    pre-lane-A snapshot plus the population/bucket counts named below.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — `git diff --unified=0
  9558f4a774^..9558f4a774 -- src/bin/v06_work_inventory.rs scripts/completion_atlas.py
  docs/work-inventory.json docs/release/SD-34-book-completion/decisions.md | grep -nE
  '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})'` finds nothing.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same diff range, `grep -nE
  '\b(STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack)\b'` finds one hit, which is
  the unified-diff hunk-context header line quoting *pre-existing, unmodified*
  `decisions.md` prose ("...not an unresolved placeholder.") immediately above the new §20
  insertion point — not a token introduced by this cycle. Read directly (`sed -n '310,318p'
  docs/release/SD-34-book-completion/decisions.md`) to confirm it is unchanged context, not a
  new line.
- **Acceptance criterion:** *(no `AT-34-E#` card names this specific rung — this is a
  bucket-D mining cycle, continuing wave 32 lane C's own reconnaissance and this wave's own
  dispatch instruction: "determine from the data whether real `DESC:` prose exists upstream
  and was never ingested, or genuinely does not exist anywhere — do not escalate.")*

## Figures + their re-derive commands

- **Population, unchanged:** 49438. Command: `python3 scripts/completion_atlas.py --check`
  → `population=49438 buckets=10 unclassified=0 overlap=0`.
- **DONE:** 24963 (pre-lane-A, `git show d686678427:docs/release/SD-34-book-completion/
  artifacts/epic-1-atlas/completion-atlas.json | python3 -c "import json,sys;
  print(json.load(sys.stdin)['buckets']['DONE']['count'])"` → 24963) → **24985 measured**
  (this cycle's own final `--check` run, above). Delta **+22**, exactly the 22 closed units.
- **D:** 2955 (same pre-lane-A snapshot) → **2933 measured**. Delta **-22**, the same 22
  units leaving bucket D for DONE.
- **overlap=0, unclassified=0, done_evidence_violations=0, citation_failures=0,
  missing_clearing_mechanisms=0, stale_derived_at=False** — all from the same `--check` run
  above, at this cycle's own final commit (not carried forward from the crashed run).
- **The 22 closed units no longer appear in bucket D's own `sub_causes`; the 5 deferred
  units still do, alone.** Command: `python3 -c "import json; d=json.load(open('docs/
  release/SD-34-book-completion/artifacts/epic-1-atlas/completion-atlas.json'));
  sc=d['sub_causes']['D']; [print(k,sc[k]) for k in sc if 'weapon_proficiency_grant' in k or
  'class_skill_list' in k or 'wizard_school_spell_list' in k or 'weapon_and_armor' in k]"` →
  `class_feature_weapon_and_armor_proficiency_grant_held_by_class_proficiency_tables 5` (only
  the deferred rung; the other three sub-cause keys are gone because their population is now
  zero).
- **Scoped test count:** 84 passed, 0 failed. Command: `cargo test --locked --lib -j 6 --bin
  v06_work_inventory -- class_feature_text_complete_rung_tests` (run with
  `CARGO_TARGET_DIR=/tmp/cargo-sd34-fold-lanea`).

## Row-count command output

Not a row-owning artifact in the `AT-34-E#` card sense (no card names this cycle) — the atlas
`--check` output above is this cycle's own row-count instrument, per the receipt schema's
allowance for cycles outside the epic/card structure.

## Build scope verified

Scoped, not full-workspace (full `scripts/verify.sh` runs once at wave-end by a different
agent after all lanes land, per this fold's own dispatch instruction):

`cargo test --locked --lib -j 6 --bin v06_work_inventory -- class_feature_text_complete_rung_tests`
at this cycle's own commit `9558f4a774`: **84 passed, 0 failed, 0 ignored, 423 filtered
out**, `finished in 0.01s`.

`python3 scripts/completion_atlas.py --check` at the same commit: **exit 0**, all six
fail-closed conditions clear (see Figures above).

## Sweep population

N/A — this cycle re-classifies 22 already-ingested `class_feature` units' `status`/`evidence`
fields; it adds no new corpus records and touches no corpus JSON files under `data/corpus/`.
`BASELINE_CORPUS_LITERAL_RECORDS` is unaffected.

## Oracle pin

`PCGEN_ORACLE_SHA=7f818006e371188e5717fd18d74d18a420747fc6` — unchanged, carried forward from
`scripts/pcgen-oracle-pin.env`. This cycle's own upstream-prose checks read the pinned PCGen
checkout directly (`cr_abilities_class.lst`), not a separate unpinned clone.

## Full gate result

Not run this cycle — scoped tests + `completion_atlas.py --check` only, per this fold's own
dispatch instruction ("the full `scripts/verify.sh` runs once at the very end, by a different
agent, after all three lanes are in"). The prior full green run (lane D,
`7ea9651b87`, 40/40, `/tmp/codex-verify-9KJsiq`) is the last one this repo has recorded;
it predates this cycle's own commit and does not cover this cycle's own changes.

## Status: complete (22/27), deferred (5/27), no escalation

## Movement, four buckets

- **Closure:** 22 units — `weapon-proficiency` (3), `class-skill-list` (10),
  `wizard-school-spell-list` (9) — moved bucket D → DONE (`status: "grounded"`, new evidence
  `class_feature_set_shaped_grant_carries_no_upstream_description_by_design`).
- **Reclassification:** 0 — the 22 did not move sideways between non-DONE buckets, they
  closed.
- **Reachability:** 0.
- **Instrument-correction:** 4 — `completion_atlas.py`'s bucket A/B/C/V citation line numbers,
  shifted +116 lines by this cycle's own insertions and re-derived (not a content change to
  what they cite, only their position in the file).

## Notes

- **Wave 32 lane C recommended escalating this 27-unit shape to the operator.** This cycle
  was directed not to escalate, and instead to determine from the data itself whether the
  `description: null` shape hides an ingestion gap (fixable) or a genuine absence (a
  different-shape ruling). The data resolved it without an operator ruling: 22 of 27 are
  genuinely proseless (closed), 5 of 27 are not (real gap named, not closed).
- **The `weapon-and-armor-proficiency` arm's own pre-existing code comment was factually
  wrong** ("Still `description: null`'s display-bucket sibling concern is untouched") — this
  cycle corrected the comment in place (§20's own finding: real multi-sentence `DESC:` prose
  exists for all five keys in both the PCGen source and the ingested corpus) without changing
  the arm's runtime behavior, since the actual blocker there is a different mechanism
  (`pilot_compute` wiring), not the description gap the old comment assumed.
- **A comment inside the `wizard-school-spell-list` closure branch cites `decisions.md §21`**
  (should read `§20` — only one new section, `§20`, was added this cycle). This is a
  pre-existing typo in the crashed run's own uncommitted diff, inherited as-is; it is a
  doc-citation slip inside a Rust comment, not a behavior, test, or figure defect, and does
  not affect any audit (the identifier/wired-integration greps do not key on decision-section
  numbers). Left uncorrected this cycle to keep the fold to its intended scope (landing the
  crashed work, not auditing its prose); flagged here for whichever cycle next touches this
  arm to fix in passing.
- **The 3 non-`CSKILL:`-shaped remainder of the 13-unit class-skill-list sub-cause**
  (`Companion ~ Animal Companion`, `Companion ~ Special Mount`, `Special Mount ~ Standard
  Choices` — `FOLLOWERS:`/`COMPANIONLIST:` shaped, not checked against upstream prose by this
  cycle) are unchanged and out of this cycle's named scope, per `decisions.md §20`.

## Next-cycle plan

Ground `pilot_compute::explain_base_class_weapon_and_armor_proficiency`/
`ground_class_weapon_and_armor_proficiency` for Bard, Fighter, Paladin, Ranger, and Rogue —
the same idiom already grounding Sorcerer, Wizard, Cleric, Assassin, and Shadowdancer's own
version of this record shape — at the same rigor Cleric's own cycle 6 applied (reading every
one of the class's own registered archetypes in `rules_tables/*/archetype_tables.rs` that
supersede a proficiency slot before trusting any replacement text, since all five of these
classes have real archetypes doing exactly that, unlike the zero-archetype
Assassin/Shadowdancer precedent). This closes the remaining 5-unit
`weapon-and-armor-proficiency` rung once done.
