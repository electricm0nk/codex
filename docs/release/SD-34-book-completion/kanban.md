---
canonical: true
owner: god-emporer
bundle_id: SD-34
date: 2026-08-26
board: local-file (Hermes board retired 2026-08-01, SD-30 Decision 14a)
---

# SD-34 Kanban

One row per acceptance criterion. **27 rows covering 28 criteria** — the last row carries both remaining closure criteria. A cycle marks its row `complete` from inside the dispatched agent (`workflow-instruction.md §6` step 8), and only when the row-count on its own artifact says so (`decisions.md §4`).

**Status vocabulary:** `not-started` | `in-progress` | `complete` | `blocked-escalated`.

A cycle that closes part of its population and **names every remaining unit by sub-cause, with
populations that sum exactly** leaves its row at `in-progress` and reports `partial` — the
dispatch continues and a later cycle takes the remainder (`decisions.md §15`). **Needing more
cycles is never `blocked-escalated`.**

**There is no `returned-to-backlog` and no `deferred`.** A blocker on the Definition of Done is cleared or escalated (`../../governance/blocker-closure-doctrine.md`). `blocked-escalated` pauses the bundle and does not satisfy AT-34-E6-001.

**Row hygiene:** per-cycle narrative goes in `progress.md` and the cycle receipt, **not the Notes column.** Notes here are a pointer, never a story.

| # | Card | Epic | Criterion | Status | Notes (pointer only) |
|---|---|---|---|---|---|
| 1 | `atlas-one-bucket-per-unit` | 1 | AT-34-E1-001 | complete | `artifacts/epic-1-atlas/AT-34-E1-001_cycle_receipt.md` |
| 2 | `atlas-fails-closed` | 1 | AT-34-E1-002 | complete | `artifacts/epic-1-atlas/AT-34-E1-002_cycle_receipt.md`, re-verified at HEAD by `artifacts/epic-1-atlas/AT-34-E1-002_re-verification_receipt.md`; citation gate re-derived a tenth time (all ten, not just DONE's) at wave 32 by `artifacts/epic-1-atlas/AT-34-E1-002_wave32_citation-repair_cycle_receipt.md` |
| 3 | `missing-tables-and-book-coverage` | 1 | AT-34-E1-003 | complete | `artifacts/epic-1-atlas/AT-34-E1-003_cycle_receipt.md`, re-verified at HEAD by `artifacts/epic-1-atlas/AT-34-E1-003_re-verification_receipt.md` |
| 4 | `shape-engine-boundary-stated` | 1 | AT-34-E1-004 | complete | `artifacts/epic-1-atlas/AT-34-E1-004_cycle_receipt.md`, re-verified at HEAD by `artifacts/epic-1-atlas/AT-34-E1-004_re-verification_receipt.md` |
| 5 | `rename-not-ingested-field` | 1 | AT-34-E1-005 | complete | `artifacts/epic-1-atlas/AT-34-E1-005_cycle_receipt.md`, re-verified at HEAD by `artifacts/epic-1-atlas/AT-34-E1-005_re-verification_receipt.md` |
| 6 | `figure-provenance-gate` | 1 | AT-34-E1-006 | complete | `artifacts/epic-1-atlas/AT-34-E1-006_cycle_receipt.md`, re-verified at HEAD by `artifacts/epic-1-atlas/AT-34-E1-006_re-verification_receipt.md` (found and fixed a real 14-violation regression in Epic 3's own receipts) |
| 7 | `corpus-trap-audit-stage` | 1 | AT-34-E1-007 | complete | `artifacts/epic-1-atlas/AT-34-E1-007_cycle_receipt.md`, `artifacts/epic-1-atlas/AT-34-E1-007_re-verification_receipt.md` |
| 8 | `wiring-class-mismatch-to-zero` | 1 | AT-34-E1-008 | complete | `artifacts/epic-1-atlas/AT-34-E1-008_G1_cycle_receipt.md` .. `_G4_cycle_receipt.md`, verified by `artifacts/epic-1-atlas/AT-34-E1-007_re-verification_receipt.md`, reconfirmed at HEAD by `artifacts/epic-1-atlas/AT-34-E1-008_reconfirmation_receipt.md` |
| 9 | `build-eight-tables` | 2 | AT-34-E2-001 | complete | `artifacts/epic-2-tables/AT-34-E2-001_cycle_receipt.md`, `artifacts/epic-2-tables/AT-34-E2-001_table_transcript.txt`, reconfirmed at HEAD by `artifacts/epic-2-tables/AT-34-E2-001_reconfirmation_receipt.md` |
| 10 | `tables-fail-closed` | 2 | AT-34-E2-002 | complete | `artifacts/epic-2-tables/fail-closed-proofs.md`, `artifacts/epic-2-tables/AT-34-E2-002_cycle_receipt.md`, reconfirmed at HEAD by `artifacts/epic-2-tables/AT-34-E2-002_reconfirmation_receipt.md` |
| 11 | `table-build-rate-measured` | 2 | AT-34-E2-003 | complete | `artifacts/epic-2-tables/table-build-rate.json`, `artifacts/epic-2-tables/AT-34-E2-003_cycle_receipt.md`, reconfirmed at HEAD by `artifacts/epic-2-tables/AT-34-E2-003_reconfirmation_receipt.md` |
| 12 | `bucket-a-zero-both-books` | 2 | AT-34-E2-004 | complete | `artifacts/epic-2-tables/AT-34-E2-004_cycle_receipt.md`, reconfirmed at HEAD by `artifacts/epic-2-tables/AT-34-E2-004_reconfirmation_receipt.md` |
| 13 | `core-bucket-b-zero` | 3 | AT-34-E3-001 | in-progress | Escalation cleared (`decisions.md §14`, 9 mechanisms, 1006 total). `artifacts/epic-3-core-rulebook/AT-34-E3-001_cycle_receipt.md` — 29/1035 (mechanism: reattribution). `artifacts/epic-3-core-rulebook/AT-34-E3-001_domain_cycle_receipt.md` — 1/1 (mechanism: `domain`). `artifacts/epic-3-core-rulebook/AT-34-E3-001_race_trait_absent_cycle_receipt.md` — 9/9 (mechanism: `race_trait_absent_from_race_traits`). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_absent_cycle_receipt.md` — 17/17 (mechanism: `class_absent_from_ClassId_ALL_and_book_class_id_enums`). `artifacts/epic-3-core-rulebook/AT-34-E3-001_deity_absent_cycle_receipt.md` — 21/21 (mechanism: `deity_content_absent_from_deity_table_in_core_rulebook`). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` — Cycle 1 6/63, Cycle 2 2/57 (multi-DESC ingest truncation sub-cause), NOT closed (mechanism: `class_feature_option_pool_record_not_held_by_engine`; 55 remain, 5 named sub-causes, see receipt). `artifacts/epic-3-core-rulebook/AT-34-E3-001_companion_absent_cycle_receipt.md` — 72/100 (mechanism: `companion_absent_from_core_rulebook_companion_tables`; Shape 7 book-wide-grant ownership added to `companion_chassis`'s transcriber; 28 remain, 3 named sub-causes — 12 zero-content, 2 monster-class definitions, 14 cross-book familiar-pool rows), NOT closed. `artifacts/epic-3-core-rulebook/AT-34-E3-001_race_trait_race_not_modelled_cycle_receipt.md` — 132/132 (mechanism: `race_trait_race_not_modelled`; `classify()`'s `Kind::RaceTrait` arm gained a `race_trait_generic/` table fallback, reusing `simple_kind_verdict`; corpus-wide side effect 1413→90), CLOSED. `core_rulebook` bucket B (atlas-real partition) 762/6701 remains (`python3 scripts/completion_atlas.py --by-book`), 5 mechanisms fully closed, 2 partially closed (55 of 63, 28 of 100 remain), 2 not started. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt.md` — 0/346 (mechanism: `class_feature_owner_matched_by_name_but_record_not_held_by_engine`; re-derived and named an EXACT 7-way sub-cause partition, proven by a committed passing regression test — 143 null-description internal bookkeeping, 121 real engine-effect tokens, 67 catalog-served-but-wiring-class-gated, 6 class-level-scaled phrase, 5 dropped pcgen args, 3 unregenerated multi-DESC branches, 1 bare percent reference; every gate refusing these is pre-existing Decision-7 safety architecture, none a narrow catalog-widening bug — all need real engine wiring or new ingest work), NOT closed, 0 units moved. `core_rulebook` bucket B unchanged at 762/6701; 1 mechanism newly fully named but unstarted (this cycle's 346), 1 unstarted (`class_feature_option_pool_record_with_magnitude_not_held_by_engine`, 333). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt.md` — 5/333 (mechanism: `class_feature_option_pool_record_with_magnitude_not_held_by_engine`; new `probe_domain_power_effect_wiring` live-probes cleric's 5 real-formula domains, closing `Domain Power ~ {Battle Rage, Destructive Smite, Strength Surge, Touch of Good, Touch of Glory}`; 328 remain, sum-exact 129-group sub-cause partition, largest named: `Domain Power` 56, `Domain Base` 33, `Favored Enemy/Terrain Bonus` 42, `Bardic Performance`/`Draconic Bloodline Choice`/`Secret Lore`/`New Arcana` 39, wizard-school cluster ~34), NOT closed. `core_rulebook` bucket B (atlas-real partition) 757/6701 remains, 5 mechanisms fully closed, 3 partially closed (55 of 63, 28 of 100, 328 of 333 remain), 1 unstarted (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`, 346). `artifacts/epic-3-core-rulebook/AT-34-E3-001_companion_absent_cycle_receipt_2.md` — cycle 2 on `companion_absent_from_core_rulebook_companion_tables`: dispatch instruction to re-derive (not inherit) the 28-unit remainder's judgement, take a narrower fix if one closes it. Re-derivation found none: the 2 monster-class rows confirmed a genuinely different (level-progression) record shape; the 14 familiar-pool rows' true owners (11 familiar creatures) confirmed already shipping as registered `CompanionRecord`s under `beastiary`, not `core_rulebook` — a real cross-book split baked into the actual books, needing Shape 8 (a corpus-wide invariant widening) not a narrow fix. 0/28 closed, judgement confirmed correct with new corpus proof, partition now backed by a committed passing regression test. `core_rulebook` bucket B (atlas-real partition) unchanged at 757/6701. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` — Cycle 3, 3/55 (mechanism: `class_feature_option_pool_record_not_held_by_engine`; filed `artifacts/epic-3-core-rulebook/atlas-defects.md` entry 1 first per `decisions.md §2` — 3 vacuous PCGen placeholder rows, `Empty Selection ~ Standard {Barbarian, Monk, Rogue}`, null description and no mechanical token, an unpredicted verdict shape — then closed them via a new closed 3-key named list, `class_feature_pool_catalog::VACUOUS_PLACEHOLDER_CLASS_FEATURES`, reclassifying to `deferred-with-reason` (bucket X); isolation check confirms exactly these 3 moved corpus-wide; 52 remain, 4 named sub-causes — proficiency/grant possession-tracking 28, class-skill/companion-mount attribution 13, wizard opposition-school tracking 9, Domain Power registration gap 2), NOT closed. `core_rulebook` bucket B (atlas-real partition) 754/6701 remains, 5 mechanisms fully closed, 3 partially closed (52 of 63, 28 of 100, 328 of 333 remain), 1 unstarted (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`, 346). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_2.md` — cycle 2 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: investigated `Domain Base` (33) sub-cause -- genuinely computable DC formula, but genuinely never consumed by any real domain-power computation, so left named rather than force-closed with an unobserved claim. Closed the `Weapon Training` sub-cause's 4 canonical records instead: new `probe_fighter_weapon_training_wiring` (mirrors `probe_domain_power_effect_wiring`) selects the engine's own 4 hardcoded canonical (tier, group) pairs over the real compute pipeline; `Weapon Training {1 Blades Heavy, 2 Bows, 3 Pole Arms, 4 Hammers}` -> DONE, other 48 of 52 need real new per-group arithmetic. 4/328 closed, 324 remain, 10-family sub-cause partition re-derived fresh (largest: `Domain Power` 56, `Domain Base` 33, `Favored Enemy Bonus` 31, wizard-school cluster 18, `Favored Terrain Bonus` 11, `Bardic Performance`/`Draconic Bloodline Choice`/`Secret Lore` 10 each, `New Arcana` 9, `Weapon Training` remainder 48, small/long-tail 88), NOT closed. `core_rulebook` bucket B (atlas-real partition) 750/6701 remains, 5 mechanisms fully closed, 3 partially closed (52 of 63, 28 of 100, 324 of 333 remain), 1 unstarted (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`, 346). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_2.md` — cycle 2 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`251ad7929a`), same 346, same 7-way sum-exact partition (re-ran the prior cycle's committed regression test, unchanged output). Tested and falsified one new hypothesis (duplicate-unit/atlas-defect check on `Arcane Bond ~ Bonded Object` and its siblings — none is a duplicate of an already-DONE unit). 0/346 closed, 0 movement in all four buckets; remainder unchanged and still needs either real engine wiring (5 shapes), new ingest work, or an operator-scoped ruling on the 143-unit zero-description sub-cause. `core_rulebook` bucket B (atlas-real partition) unchanged at 750/6701. `artifacts/epic-3-core-rulebook/AT-34-E3-001_companion_absent_cycle_receipt_3.md` — cycle 3 on `companion_absent_from_core_rulebook_companion_tables`: built Shape 8 (cross-book ownership) that cycle 2 declined — `companion_chassis` widened with `cross_book_owners`, verified against all 16 registered companion books. The 14 familiar-pool rows now ship, owned by the 11 already-registered `beastiary` familiar creatures. 14/28 closed, 14 remain (12 zero-content — now recorded as atlas defect 3, 461/51,482 corpus-wide, not reclassified; 2 monster-class rows, re-confirmed). `core_rulebook` bucket B (atlas-real partition) 750 → 736/6701, 6 mechanisms fully closed, 1 mechanism (`class_feature_option_pool_record_not_held_by_engine`) partially closed at 52 of 63, this mechanism partially closed at 14 of 100 remain, 1 partially closed (`class_feature_option_pool_record_with_magnitude_not_held_by_engine`, 324 of 333 remain), 1 unstarted (`class_feature_owner_matched_by_name_but_record_not_held_by_engine`, 346). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` — Cycle 4 on `class_feature_option_pool_record_not_held_by_engine`: re-derived the 52-unit remainder fresh, independently confirmed Cycle 3's own 28/13/9/2 sub-cause split is EXACT. Read every one of the 52 live corpus records against the existing engine-effect-token / render-and-refuse safety gates and `has_real_description`: 44 of 52 carry `description: null` (no text to serve, structural chassis rows needing a real new consumer — proficiency lists, class-skill lists, wizard known-cantrips, companion registration); the other 8 (`Domain Power ~ Leadership`, `~ Sun's Blessing`, 6 `Prof` group members) are correctly refused by an existing safety gate for a real, still-uncomputed mechanical effect. Confirmed via grep that no proficiency-tracking probe, spell-known-per-school consumer, or `Domain Power` class-feature-grant fact exists anywhere in the engine for any of the four sub-causes. 0/52 closed (declined to force an unsafe closure per `AGENTS.md` rule 6 / no-stub-mvp-doctrine); `cargo test --locked --no-run` re-run attempted but hit environmental disk exhaustion (`ld` Bus error, `df -h /` 590M/968G free, stale sibling `CARGO_TARGET_DIR` cleanup blocked by this session's own permission classifier) — 0 code changed this cycle so HEAD's last-verified widest-scope result (Cycle 3's own, exit 0) stands unmodified. `core_rulebook` bucket B (atlas-real partition) unchanged at 736/6701. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_3.md` — cycle 3 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: built `probe_ranger_favored_enemy_bonus_wiring`/`probe_ranger_favored_terrain_bonus_wiring` (mirrors `probe_domain_power_effect_wiring`), grounding all 31 Favored Enemy Bonus + 11 Favored Terrain Bonus canonical types (open-ended recognition, unlike Weapon Training's hardcoded subset). 43/324 closed corpus-wide (42 core_rulebook + 1 advanced_players_guide), 282 remain. Investigated and ruled out New Arcana (9, a genuine free chooser, no canonical value to credit). Real instrument-correction: F1 population 5445->5402 (43 newly-grounded records were F1-shaped), re-pinned with a retro correction event. Flagged (not fixed, out of scope): 3 vacuous-placeholder units break `tests/v06_work_inventory.rs`'s deferred-with-reason invariant, a different sibling mechanism's own cycle-3 fix. `core_rulebook` bucket B (atlas-real partition) 736 -> 694/6701, 6 mechanisms fully closed, 3 partially closed (52 of 63, 14 of 100, 282 of 1006 remain), 1 unstarted (346). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_3.md` — cycle 3 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`c3202a90ce`), same 346. Broke the previously-flat `engine_effect_token_present` (121) into a real 13-shape sum-exact partition (temporary test instrumentation, reverted before commit, no shipped code changed): 87 `Sorcerer Bloodline Feat`, 16 `Ranger Combat Style Feat`, 18 long-tail. Confirmed the 103-unit majority is the SAME already-ratified "count grounds, choice not modelled" pattern used for Fighter/Cavalier/Brawler/Arcane-bloodline bonus feats (`pilot_compute/mod.rs:1837-1844`), and that `ABILITY` in `ENGINE_EFFECT_TOKEN_KEYS` means widening `REGISTERED_POOL_GROUPS` would not close them — the catalog's real serving path (`load_pool_catalog`) would still correctly refuse them. Names a second operator-scoped question (parallel to the 143-unit bookkeeping one): should a "count grounds, choice not modelled" per-option enumeration record ever be bucket-B eligible? The two questions together now cover 246/346 (71%). 0/346 closed, 0 movement in all four buckets. `core_rulebook` bucket B (atlas-real partition) unchanged at 694/6701. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_4.md` — cycle 4 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`16c772cca9`), same 346, independently confirmed via a fresh from-scratch re-derivation before reading prior receipts. Picked up cycle 3's real-engine-wiring path: new `pilot_compute::explain_base_class_weapon_and_armor_proficiency` (mirrors the shipped `class_slayer.rs` grant-only-identity-record precedent) grounds Sorcerer's and Wizard's own "Weapon and Armor Proficiency" class features via `classify()`'s EXISTING generic explanation-id check — 2/346 closed (TDD RED->GREEN), both reaching `text-complete`. Cleric (archetype-supersession complexity) and Assassin/Shadowdancer (no prestige-class chassis exists) deferred with a named retro event, not silently dropped. 344 remain, sub-cause partition re-derived fresh and unchanged in shape from cycle 3 (118 null-description, 118 engine-effect-token of which 87 Sorcerer Bloodline Feat + 16 Ranger Combat Style Feat + 15 long tail, 67 catalog-served-but-gated, 6 class-level-scaled phrase, 5 dropped args, 3 multi-DESC, 1 bare percent). `core_rulebook` bucket B (atlas-real partition) 694 -> 692/6701, 6 mechanisms fully closed, 2 partially closed (14 of 100, 282 of 1006 remain), this mechanism now partially closed at 344 of 346 remain. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_4.md` — cycle 4 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: read cycle 3's own arcane-school lever directly against `pilot_compute/mod.rs` before building anything, found real ALREADY-COMPUTED (never wired to `classify()`) formulas for exactly 2 of 9 wizard schools (Evocation: Task #55's own `intense_bonus_damage`/`force_missile_uses_per_day`; Abjuration: Task #66's own `resistance`/`protective_ward_*`/`energy_absorption`). New `probe_wizard_arcane_school_wiring` (mirrors `probe_domain_power_effect_wiring`) grounds exactly those 5 corpus records — `Evocation School ~ {Intense Spells, Force Missile}`, `Abjuration School ~ {Resistance, Protective Ward, Energy Absorption}` — never claiming the other 7 schools or either school's own top-level/opposition-school recognition record (no formula exists for those). 5/282 closed, 277 remain, sub-cause partition re-derived fresh and sums exactly (`Domain Power` 56, `Domain Base` 33, wizard-school cluster remainder 38, `Bardic Performance`/`Draconic Bloodline Choice`/`Secret Lore` 10 each, `New Arcana` 9 — ruled out, small 2-3-unit groups 22, long tail 89). Real instrument-correction: F1 population 5402->5401 (1 newly-grounded record, `Abjuration School ~ Resistance`, was F1-shaped), re-pinned with a retro correction event. Self-healed one inherited `denominator_gate.py` violation from cycle 3's own committed `progress.md` text (a bare "100%" describing disk fullness, not the exempted idiom). Re-derived and fixed 10 + 2 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (`citation_failures=0` on both after). Flagged, not fixed (out of this mechanism's own scope, unchanged from cycle 3): the same pre-existing `cargo test --locked --test v06_work_inventory` failure on 3 `vacuous_placeholder_row_no_corpus_content_to_render` units. `core_rulebook` bucket B (atlas-real partition) 692 -> 687/6701 (this mechanism 282 -> 277 of 1,006). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` — Cycle 5 on `class_feature_option_pool_record_not_held_by_engine`: re-derived at HEAD, still 52 of 687. Cycle 4's own grep for a proficiency-tracking probe (`"proficiency.*wired\|weapon_prof.*wired"`) was too narrow — a wider search found `weapon_tables::CLASS_WEAPON_PROFICIENCIES`, a real, already-shipped, already-tested class-weapon-proficiency table combat's own `character_is_proficient_with` already consumes, never wired to the atlas. Verified 3 of the 28-unit proficiency sub-cause's own `"Weapon Proficiencies ~ {Bard,Druid,Rogue}"` records as an EXACT byte-for-byte set match against that table (Cleric's deity-weapon and Monk's 16/17 near-match individually investigated and correctly excluded); new `weapon_proficiency_grant_class_id` closed-list lookup moves the 3 from bucket B to bucket D (`engine-does-not-hold`, new evidence, `description: null` so still not `text-complete` — a shelf, not a display fix). 3/52 closed, 49 remain, sub-cause partition re-derived and sums exactly (weapon-flavored generic/combined/excluded 15, armor/shield/other-grant 10, class-skill/companion-mount 13, wizard opposition-school 9, Domain Power 2). Fixed 6 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (`citation_failures=0` on both after). `core_rulebook` bucket B (atlas-real partition) 687 -> 684/6701 (this mechanism 52 -> 49 of 687). `artifacts/epic-3-core-rulebook/AT-34-E3-001_companion_absent_cycle_receipt_4.md` — cycle 4 on `companion_absent_from_core_rulebook_companion_tables`: re-derived at HEAD (`dbf97940fd`), still 14. Closed the 12 grant-token-only rows via a per-record, corpus-wide VERIFIED predicate (never a shape-only reclassification — a corpus-wide re-check of defect 3's own bare shape query found only 171 of 461 safe, 104 unsafe, 280 target-missing, proving shape-only unsafe): a new closed 12-key `companion_chassis::GRANT_TOKEN_ONLY_DISPATCH_ROWS` list, each entry verified against the LIVE `docs/work-inventory.json` (every `ABILITY:` target is already `grounded`/`text-complete`/`literal-verified`), moved to `deferred-with-reason` (bucket X). The 2 monster-class rows (`Companion`, `Shadow Companion`) NOT closed — genuine level-progression record type needed; confirmed its 2 real second/third consumers this cycle (`ultimate_magic` 3 rows, `book_of_the_damned_volume_1` 2 rows) but did not build/verify the type against them within this cycle. 12/14 closed, 2 remain (1 named sub-cause: monster-class definitions). Fixed 2 shifted `file:line` citations in `completion_atlas.py` (bucket A 10583->10601, bucket V 11234->11252; `citation_failures=0` after). `core_rulebook` mechanism 14 -> 2. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_5.md` — cycle 5 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`c4e6ac92f9`), still 344. Applied `decisions.md §16`'s ratified "only the count grounds" precedent (written for this cycle, unblocking the 103-unit majority cycles 2-4 each declined as "operator-scoped"): new `ground_sorcerer_bloodline_feat_pool`/`ground_ranger_combat_style_feat_pool` ground a bloodline/style-invariant slot COUNT magnitude, name the full corpus-wide eligible set (87 Sorcerer names, 16 Ranger names), and emit a non-claim-blocking diagnostic per option, no default choice seeded. Found and fixed a real defect in this cycle's OWN work: the first regeneration's diagnostics incidentally misattributed 4 unrelated units (`Sorcerer Domain ~ Sun/Knowledge/Magic`, `Sorcerer Bonus Spell L3 ~ Fly`) via `diagnostic_id_names_feature`'s substring match; a 10-name `SORCERER_BLOODLINE_FEAT_POOL_DIAGNOSTIC_EXCLUSIONS` list (found complete only after a corpus-wide 431+282-unit cross-check, verified by a third regeneration showing zero unintended movement) fixes it. 93/344 closed (77 Sorcerer + 16 Ranger), all bucket B -> bucket X (`deferred-with-reason`), 251 remain, NOT closed. `core_rulebook` bucket B (atlas-real partition) 579/6701 remains, this mechanism 344 -> 251. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_5.md` — cycle 5 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: re-derived at HEAD (`3ebc08b451`), still 277. Read cycle 4's own remainder table, which named a `Bardic Performance` cluster (10 units, untouched); reading the live corpus records directly found every one carries a real `BONUS:VAR` formula, not a roster pick. `pilot_compute/mod.rs` already computed 7 of 10 (Fascinate, Inspire Courage, Inspire Competence, Soothing Performance, Frightening Tune, Deadly Performance, Inspire Heroics) — same owner-resolution failure (`group` "Bardic Performance" != "bard") every prior sub-cause (Domain Power / Weapon Training / Favored Enemy / Wizard Arcane School) already established. New `probe_bard_bardic_performance_wiring` (mirrors `probe_wizard_arcane_school_wiring`) grounds them; this cycle also built the 3 remaining formulas the engine lacked (Suggestion DC, Mass Suggestion DC, Inspire Greatness allies-count), each level-gate verified directly against the ingested corpus record's own `PREVARGTEQ` token. 10/277 closed, all 10 -> DONE, 267 remain, sub-cause partition re-derived fresh and sums exactly (`Domain Power` 56, `Domain Base` 33, wizard-school cluster remainder 38, `Draconic Bloodline Choice` 10 — checked this cycle, genuinely different shape, `Secret Lore` 10 — not yet checked, `New Arcana` 9 ruled out, small 2-3-unit groups 22, long tail 89). No instrument movement: F1 stayed 5401 (checked each closed record's formula shape directly, none bare-literal). Re-derived and fixed 10 + 2 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (`citation_failures=0` on both after). `core_rulebook` bucket B (atlas-real partition) 579 -> 569/6701, this mechanism 277 -> 267. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` — Cycle 6 on `class_feature_option_pool_record_not_held_by_engine`: re-derived at HEAD (`16aea9b4dd`), still 49 of 569. Checked cycle 5's own next-cycle plan (an armor-proficiency table "the exact same way `CLASS_WEAPON_PROFICIENCIES` was built") against the live corpus first: the standalone `Armor Prof ~ {Light,Medium,Heavy}`/`Shield Prof`/`Shield Prof ~ Tower` records are generic indirection targets with no per-class grant, the same unclosable shape as the weapon-flavored generics cycle 5 already left unclosed — the real per-class data lives in each class's own DISPLAY-bearing `"Weapon and Armor Proficiency ~ <Class>"` combined record instead (a different corpus key from cycle 5's own 3 weapon-only matches). Built new `weapon_tables::CLASS_ARMOR_PROFICIENCIES` and, for 7 combined records (Bard/Druid/Fighter/Monk/Paladin/Ranger/Rogue), verified BOTH weapon AND armor content byte-for-byte against the live corpus and both tables. 5 matched exactly (Bard/Fighter/Paladin/Ranger/Rogue); Druid excluded (its own `AUTO:WEAPONPROF` list is missing `Scythe` against both the table and its own dedicated `"Weapon Proficiencies ~ Druid"` record — a real corpus-internal discrepancy) and Monk excluded (repeats cycle 5's own established `Flurry of Blows`/`Unarmed Strike` mismatch). New `weapon_and_armor_proficiency_grant_class_id` closed-list lookup moves the 5 from bucket B to bucket D (new evidence string, `description` real but display-bucket concern is a separate mechanism's). 5/49 closed, 44 remain, sub-cause partition re-derived and sums exactly (weapon-flavored generic indirection targets 8, `Weapon and Armor Proficiency ~ {Druid,Monk}` excluded 2, armor/shield-flavored generic indirection targets plus non-weapon extras 10, class-skill/companion-mount 13, wizard opposition-school 9, Domain Power 2). Checked `decisions.md §16` against the remainder — does not apply (no "pick N" choice shape among these 44). Fixed 6 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (`citation_failures=0` on both after). `core_rulebook` bucket B (atlas-real partition) 569 -> 564/6701, this mechanism 49 -> 44. | `artifacts/epic-3-core-rulebook/AT-34-E3-001_companion_absent_cycle_receipt_5.md` — cycle 5 on `companion_absent_from_core_rulebook_companion_tables`: re-derived at HEAD (`50a5785592`), still 2. Built `companion_chassis::CompanionClassRecord` (a genuine new level-progression record type this chassis had no fields for, per SD-29's own round-8 screen and this mechanism's cycle 4 deferral), registered it against all 3 real corpus-wide consumers named by cycle 4 (`core_rulebook` 2, `ultimate_magic` 3, `book_of_the_damned_volume_1` 2 — 7 rows total). Fixed a pre-existing determinism bug in `transcribe_companion_tables.py` (an un-sorted `set` iteration reordered `ability_keys` run to run) found by diffing two back-to-back regenerations before trusting the regen. 2/2 closed — **mechanism reaches 0**; 5 of the other 7 rows (`ultimate_magic` 3, `book_of_the_damned_volume_1` 2) also closed as a legitimate side effect, each book's own separate bucket-B mechanism, not double-counted here. Re-pinned one stale hardcoded F1-population test (5401->5400, retro correction event) and fixed 10 shifted `file:line` citations in `completion_atlas.py` (`citation_failures=0` after). `core_rulebook` bucket B (atlas-real partition) corpus-wide 12002 -> 11995/49438; `core_rulebook`-scoped B 562/6701 remains (owned by the 8 other AT-34-E3-001 mechanisms). AT-34-E3-001 itself remains open. `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_6.md` -- cycle 6 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`2829c89e18`), still 251. Extended cycle 4's `explain_base_class_weapon_and_armor_proficiency` with a shared `ground_class_weapon_and_armor_proficiency` helper mirroring `class_slayer.rs`'s archetype-supersession primitive for Cleric (real Ecclesitheurge archetype, 4 slot-id spellings), Assassin and Shadowdancer (no registered archetype, corrects cycle 4's own deferral reason). 3/251 closed (bucket B -> DONE directly), 248 remain, whole-corpus before/after diff confirms zero collision. Live concurrent-write collision (another lane's uncommitted `v06_work_inventory.rs` diff) caught and worked around via an isolated worktree, never touched. `core_rulebook` bucket B (atlas-real partition) 559/6,701 remains, 8 mechanisms with cycles run, this mechanism 248 remain (total verified fresh, wiring_class split verified; finer sub-cause partition inherited not re-derived, see receipt). ption_pool_with_magnitude_cycle_receipt_6.md` — cycle 6 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: re-derived at HEAD (`2829c89e18`), still 267. Built the Transmutation wizard-school lever (Telekinetic Fist, Physical Enhancement + its 3 stat sub-choices, Change Shape), same pattern as cycle 4's Evocation/Abjuration; a probe defect (opposed-schools choice never swapped) was found and fixed before committing, caught by this cycle's own before/after regen diff. 9/267 closed corpus-wide (6 core_rulebook + 3 advanced_class_guide honest side effect), 261 remain. `core_rulebook` bucket B (atlas-real partition) 562 -> 556/6701. AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 261 remain). | `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` -- Cycle 7 on `class_feature_option_pool_record_not_held_by_engine`: re-derived at HEAD (`94705a4149`), still 44 of 564. Task brief named class-skill/companion-mount attribution (13) a genuine new-subsystem investment; per-record read split it into a 10-unit `CSKILL:`-shaped class-skill-list group (9 CRB base classes + Jack of All Trades) and a 3-unit `FOLLOWERS:`/`COMPANIONLIST:`-shaped companion/mount registration group -- genuinely different subsystems. Built new `rules_tables::crb::class_skill_tables::CLASS_SKILL_LISTS` (10-row closed table, each verified byte-for-byte against the live corpus `CSKILL` token) + `class_feature_pool_catalog::class_skill_list_grant_owner_id` lookup, wired into `v06_work_inventory.rs` immediately after cycle 6's own weapon-and-armor rung. 10/44 closed (bucket B -> D, `description: null` unchanged, no live consumer required -- same precedent cycle 6's own `CLASS_ARMOR_PROFICIENCIES` set), 34 remain, sub-cause partition re-derived and sums exactly (weapon-flavored generic 8, `Weapon and Armor Proficiency ~ {Druid,Monk}` excluded 2, armor/shield-flavored generic + extras 10, companion/mount registration 3 [newly split out], wizard opposition-school 9, Domain Power 2). Before/after whole-corpus diff confirms exactly these 10 keys moved, zero collision (the task brief's own named cycle-5 hazard). Fixed 6 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (`citation_failures=0` on both after). `core_rulebook` bucket B (atlas-real partition) 564 -> 543/6701, this mechanism 44 -> 34. AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 34 remain). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_cycle_receipt.md` -- cycle 8 on `class_feature_option_pool_record_not_held_by_engine`: re-derived at HEAD (`2c56ac5a71`), still 34 of 543. Task brief pointed at proficiency/grant possession-tracking (20 units) and wizard opposition-school tracking (9 units) as the two named genuine-new-subsystem groups; independently verified (corpus JSON + engine grep, not re-quoted) that all 3 non-sibling sub-causes -- companion/mount registration (3, Druid's own Animal Companion path is wired but Special Mount has zero Paladin-side engine computation and the shared indirection target has 3 other unwired owners), wizard opposition-school spell tracking (9, no consumer anywhere in `rules_core`), and proficiency/mechanical-grant possession-tracking (20, unchanged from cycles 5/6) -- genuinely need new subsystems, not a same-shape table extension. 0/34 closed this cycle (no unit moved into X/U on this cycle's own authority); remainder re-derived and named exactly (8 + 2 + 10 + 3 + 9 + 2 = 34) with what must be built per sub-cause. `core_rulebook` bucket B (atlas-real partition) unchanged at 543/6,701 (`completion_atlas.py --check --book core_rulebook`), this mechanism unchanged at 34. Movement: 0 closure, 0 reclassification, 0 reachability, 0 instrument-correction (`decisions.md §9` -- a zero-unit wave is a legitimate deliverable). AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 34 remain). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_7.md` — cycle 7 on `class_feature_owner_matched_by_name_but_record_not_held_by_engine`: re-derived at HEAD (`2ae06fe4cd`), still 248. Re-derived cycle 6's inherited sub-cause split fresh (105 real-description / 143 null-description-with-tokens / 0 neither), and grouping by owning class found 218 of 248 already blocked by the pre-existing `ANTI_FABRICATION_GATE_EXCLUDED_CLASSES` gate (Sorcerer/Cleric/Monk/Wizard/Paladin/Bard/Druid, `OPEN-ISSUES.md` rows 330/338, OPEN, not this cycle's to decide). Of the remaining 30, traced why Assassin/Shadowdancer/Duelist/Arcane Trickster/Dragon Disciple/Pathfinder Chronicler records with real descriptions and real grant facts still failed to ground: `compute_pilot_base_chassis`'s generic class_feature grant-roster call site used `chassis_supported` alone as its "is this a real class" precondition, and `compute_class_chassis` always returns `None` for a CRB prestige class's chassis regardless of whether the class id is real. New `prestige_class_entry_gate::is_registered` accessor + a widened `||` at the call site (zero changes to any anti-fabrication/description-quality/collision gate) lets the same already-shipped, already-unit-tested generic roster fire for a prestige-class-only character. TDD RED (3 of 4 new tests failing for the intended reason) -> GREEN. 6/248 closed (`assassin_hidden_weapons`, `assassin_true_death`, `duelist_deflect_arrows`, `shadowdancer_darkvision`, `shadowdancer_defensive_roll`, `shadowdancer_shadow_power`), bucket B -> DONE directly, whole-corpus before/after diff confirms 35 changed ids total (all `core_rulebook:class_feature:*`, zero cross-book movement; 29 of the 35 are same-status evidence relabelling on sibling prestige-class records, not bucket moves — verified none carries this mechanism's own evidence string). 242 remain, named by sub-cause (218 excluded-class blocker, ~20 non-excluded-class internal bookkeeping, ~4 not yet individually re-verified). `core_rulebook` bucket B (atlas-real partition) 543 -> 537/6701. `box_ledger.py --check` exits 1 both before and after (pre-existing since prior cycles' status-string rename, confirmed against the untouched pre-cycle snapshot; `uncovered` 19870 -> 19864, an improvement not a regression). AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 242 remain). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_7.md` — cycle 7 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: re-derived at HEAD (`0827dcd59b`), still 261 of 562 `core_rulebook` bucket-B units, matching cycle 6's own closing figure exactly. Took cycle 6's own next-cycle-plan lever 2 (one more wizard school): built Conjuration, same shape as Evocation/Abjuration/Transmutation — new `CONJURATION_SCHOOL_SELECTION` constant + `wizard_has_canonical_conjuration_selection` (opposing Necromancy + Abjuration), one new compute block in `pilot_compute::mod.rs` grounding Summoner's Charm (`max(1,ConjurationSchoolLVL/2)`, level 1), Acid Dart (bonus-damage `ConjurationSchoolLVL/2` + `ArcaneSchoolPowerTimes` uses/day, level 1), and Dimensional Steps (`ConjurationSchoolLVL*30`, level 8) — all read directly off the live `conjuration_school/*.json` corpus records, never assumed. `probe_wizard_arcane_school_wiring` gained a fourth variant, swapping BOTH the specialization AND the opposed-schools choice (per cycle 6's own Discoveries warning). TDD: RED (3 target records fell through the same `group` != `"wizard"` owner-resolution path before this cycle) -> GREEN, 3 new tests. **3/261 closed** (`Conjuration School ~ Summoner's Charm`, `~ Acid Dart`, `~ Dimensional Steps`, all `engine-does-not-hold` -> `grounded`), verified by a whole-file before/after diff against git HEAD: exactly 6 changed lines (3 records' old+new versions), no cross-book side effect this time (Conjuration has no Arcanist-exploit counterpart in `advanced_class_guide`, unlike Transmutation's own 3-unit side effect), no collision-hazard misclassification. `core_rulebook` bucket B (atlas-real partition, all 9 mechanisms) 556 -> 553/6701 (`completion_atlas.py --check --book core_rulebook`), this mechanism 261 -> 258. Fixed 10 + 2 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (this cycle's own probe/doc-comment insertion shifted every one; `citation_failures=0` on both after). AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 258 remain). `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_8.md` -- cycle 8 on `class_feature_option_pool_record_with_magnitude_not_held_by_engine`: re-derived at HEAD (`617c1d3b40`), still 258 of 553 `core_rulebook` bucket-B units. Took cycle 7's own next-cycle-plan lever (one more wizard school) and picked Universal (the "no specialization" arm, 2 power records, cheapest remaining) -- genuinely different choice shape from every specialist school this mechanism already grounded (PF1's own rule: a universalist selects zero opposed schools, not two), verified against the corpus's raw tokens before writing the new `wizard_has_canonical_universal_selection` gate. New computation block grounds Hand of the Apprentice (shared `ArcaneSchoolPowerTimes` idiom) and Metamagic Mastery (`(UniversalSchoolLVL-8)/2+1`); `probe_wizard_arcane_school_wiring` gained a fifth variant clearing the opposed-schools choice entirely, no probe-defect round needed. **2/258 closed** (`Universal School ~ Hand of the Apprentice`, `~ Metamagic Mastery`), 256 remain, no cross-book side effect (`advanced_class_guide` carries no `Universal School` record at all, checked directly). `core_rulebook` bucket B (atlas-real partition, all 9 mechanisms) 553 -> 552/6701, this mechanism 258 -> 256. Fixed 10 + 2 shifted `file:line` citations in `completion_atlas.py`/`missing_engine_tables.py` (all +40, this cycle's own probe-variant/test insertion; `citation_failures=0` on both after). Regen required `CORPUS_LITERAL_SWEEP_REPORT`/`DERIVED_FIXTURE_CHECK_REPORT` env vars pointed at the sweep/fixture-check's own fresh `--json-out` reports (the binary's stamp-loss guard refuses an unguarded regen; no `--allow-stamp-loss` used). AT-34-E3-001 itself remains open (8 other mechanisms + this mechanism's own 256 remain). Cycle 9 (`AT-34-E3-001_class_feature_option_pool_with_magnitude_cycle_receipt_9.md`) took the Weapon Training sub-cause (48 units) and built it GENERICALLY: `WEAPON_TRAINING_GROUPS` names all 14 real PF1 weapon-training groups instead of the 4 hardcoded canonical ones, `fighter_weapon_training_canonical_catalog` widened to a 56-entry catalog (4 tiers x 14 groups), `probe_fighter_weapon_training_wiring` tests each combination in isolation, and `explain_fighter_class_features`'s 4 tier blocks now accept any of the 14 groups (the folded-into-total `fighter_weapon_training_attack_bonus` stays unchanged, gated on Heavy Blades only). Cross-book check confirms `ultimate_combat`/`ultimate_wilderness`/`advanced_players_guide`'s own differently-named Weapon-Training-shaped records are unaffected. `docs/work-inventory.json` NOT regenerated this cycle (wave rule). Expected: this mechanism 256 -> 208 (48 closed) once the wave's shared regeneration lands; an expectation, not a measured count.  `artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_9.md` -- owner_matched mechanism, non-excluded remainder: re-derived the excluded/non-excluded split at 218/24 (dispatch stated 161/81, wrong), of this lane's own 24: 18 null-description (OPEN bucket-B question, untouched), 6 real-description but correctly gate-refused (needs real grant/formula engine wiring, out of this cycle's scope). Zero units closed; new characterization test pins the split. Status: partial. **Wave-9 shared regeneration** (`AT-34-E3-001_wave9_regen_receipt.md`): the three-pass pipeline (`corpus_literal_sweep` 3m25s, `derived_evaluator_fixture_check` 13s, `v06_work_inventory` 11m00s — this last pass paying the throughput cost the four lanes above deferred) confirmed the four lanes' engine changes against a whole-corpus before/after diff by unit id: 49,438 ids, 0 added/removed, 79 changed, all `core_rulebook`. Attribution: **with-magnitude** (48 closed to `grounded` exactly as predicted, 256->208, plus 4 more Fighter-Weapon-Training-"Monk"-weapon-group units the lane's own receipt said would NOT move this cycle — a better-than-predicted outcome, 52 real closures not 48); **option-pool** (9 units B->D reclassification, `class_feature_wizard_school_spell_list_held_by_wizard_spell_list_and_spell_list_join`, exactly as its own commit message described — **but this cycle's own regen build found the with-magnitude lane's rebase had silently DELETED this fix's entire wiring block and both its tests from `v06_work_inventory.rs` — commit `534c9c2a61`'s diff removes it under an unrelated "generalize Weapon Training" subject with no mention of the deletion. Restored in this cycle, re-verified 419/419 `v06_work_inventory` tests green, re-regenerated.**); **owner-matched** (0 of its 24-unit non-excluded population moved, confirmed exactly as predicted); **gate-widening** (predicted "a floor of 5" reclassification for 5 NAMED units — `Bard ~ Bardic Knowledge`, `~ Lore Master`, `Paladin ~ Holy Champion`, `~ Lay on Hands`, `Sorcerer ~ Spells` — **refuted**: those 4 Bard/Paladin units were already `grounded`/`verified` before this wave even started, unaffected either way; only `Sorcerer ~ Spells` changed, and it reclassified C->D alongside 4 OTHER, un-named units — `wizard_arcane_bond`, `cleric_aura`, `paladin_detect_evil`, `wizard_bonus_feats` — 5 total reclassified but not the 5 the receipt named; separately, 2 units DID close to DONE via this same widening — `wizard_cantrips`, `cleric_spontaneous_casting` — a real closure the lane's own receipt did not claim; 0 Druid/Monk movement confirmed, as predicted; plus 1 unrelated `bard_bardic_performance` X->D reclassification). Movement, four buckets: **closure 54** (48 weapon-training + 4 weapon-training-Monk-group + 2 owner-matched-widened), **reclassification 16** (9 wizard-school-join + 5 gate-widening C->D + 1 owner-matched B->D + 1 bard-performance X->D), **evidence-string churn, no bucket crossed, 9** (already-DONE `text-complete` units whose evidence id changed from the old hand-wired path to the new citation-based path — gate-widening's rewrite, not movement), **instrument-correction 0**. `completion_atlas.py --check` corpus-wide: population=49438, citation_failures=0 (10 `file:line` citations re-derived and fixed by direct grep against `v06_work_inventory.rs`'s post-regen line numbers, twice — once after the shared regen, again after restoring the deleted wiring shifted them a second time). `--book core_rulebook --check`: B 481->472, D 3062->3071 (net of the reclassifications above). `--book ultimate_campaign --check`: unaffected (0 changed units, all movement is `core_rulebook`). `denominator_gate.py --check`: `violations=5`, all pre-existing verbatim corpus-prose quotes in `progress.md`, unchanged by this cycle. `cargo test --locked --no-run` (workspace) and `--manifest-path apps/desktop/src-tauri/Cargo.toml` both exit 0, run at this cycle's final HEAD after the restoration commit. Cycle 10 (bucket-B batch): owner_matched 239 (215 excl/24 non-excl, re-derived, not 218/24 -- wave-9 regen moved 3); removed class_feature_grant_consumer.rs LEVEL_UP_PILLAR_FILTERED_CLASSES (Druid/Monk class-wide exclusion) entirely -- stale reason (LevelUpPlan-reachability, irrelevant to classify()); expected 2 closures (Monk Flurry of Blows/Unarmed Strike) + 6 bonus (different mechanism). option_pool_with_magnitude 208 and option_pool 25 unchanged, both re-derived and reported plainly as not narrow work this cycle. Found+fixed 8 pre-existing stale sd13_*/sd25_* gates (4 Bard, 2 Sorcerer progression files, plus the level1 exhaustive checks) unrelated to this cycle's own code, via first full 184-target sd13_*/sd25_* run; also corrected a stale class_feature_pool_catalog.rs pin (excluded 218->215). cargo test --locked --no-run (workspace + desktop) exit 0; --lib 2917/2917; --bin v06_work_inventory 419/419; all 184 sd13_*/sd25_* targets 0 failed after fixes. artifacts/epic-3-core-rulebook/AT-34-E3-001_class_feature_owner_matched_cycle_receipt_10.md. Wave-10 regen+attribution measured cycle 10's bucket-B batch: whole-corpus diff 10 changed (all core_rulebook), 0 elsewhere. `class_feature_owner_matched_by_name_but_record_not_held_by_engine` 239->237 count confirmed, membership refuted (only `Monk ~ Unarmed Strike` closed to DONE; `Monk ~ Flurry of Blows` reclassified B->D, not closed). "6 bonus closures" in `no_explanation_id_and_no_diagnostic_names_this_feature` refuted entirely and in the opposite direction: population 363->357 (-6, not +6), all 6 named units (5 Monk + Druid Nature Bond) reclassified C->D to a single un-named evidence string (`class_feature_no_dedicated_magnitude_id_matched_the_record_slug`, core_rulebook 22->29), zero closed. Net: lane claimed 8 closures (2+6), measured result is 1. option_pool_with_magnitude 208 and option_pool 25 both confirmed unchanged, byte-identical membership. Movement: closure 1, reclassification 7, evidence-string churn 2, instrument-correction 0 (sum 10). Bucket-V (2,712-unit disposition list) cross-checked, zero overlap -- confirmed inert. `completion_atlas.py --check` citation_failures=0 (no src/** touched this cycle). `denominator_gate.py --check` violations=6, not the prior-reported 5 -- verified directly, all 6 pre-existing verbatim corpus-prose quotes, count climbs because each cycle's own report re-quotes the flagged text; nothing this cycle's to fix. `cargo test --locked --no-run` workspace (130s) and desktop crate (211s) both exit 0. artifacts/epic-3-core-rulebook/AT-34-E3-001_wave10_regen_receipt.md. `artifacts/epic-3-core-rulebook/AT-34-E3-001_wave9_regen_receipt.md` -- wave-9 shared regeneration cycle: 3-pass pipeline re-run corpus-wide (24m31s wall time), 0 units moved -- the M and V-ledger lanes had already regenerated and committed `docs/work-inventory.json` inline with their own engine/data commits (`4d27d70551`, `cfd9c6d3d9`), and this cycle's independent full re-run confirms byte-identical agreement (0 drift). `completion_atlas.py --check` citation_failures=0 both scopes. `cargo test --locked --no-run` workspace + desktop crate both exit 0.
| 14 | `core-bucket-c-zero` | 3 | AT-34-E3-002 | in-progress | `artifacts/epic-3-core-rulebook/AT-34-E3-002_cycle_receipt.md` — this cycle: wired the already-shipped generic pool-group-selection pass (`push_generic_pool_group_selection_magnitude`) into `classify()` for Cleric Domain, which had never once consulted it. `core_rulebook` bucket C 351->296 (55 closed: 38 to DONE, 17 reclassified to V per `apply_done_rung_stamps`'s static-wiring-class guard — reported as reclassification, not closure). 11 sub-causes re-derived fresh, summing exactly to 296 (`domain_power_display_record_not_wired` shrinks 96->41: 33 domain headers + 7 Druid sub-choices + 1 zero-token record, all named, none force-closed). Prior cycle closed 6/357 (Monk Unarmed Damage Medium); population re-derived fresh each cycle per `decisions.md §12` L2, not carried forward. NOT closed — `docs/work-inventory.json` regen for this cycle is local/uncommitted only, per this dispatch's file-ownership rule assigning it to the wave's shared regeneration cycle; isolation confirmed by a whole-inventory id-keyed diff (0 added, 0 removed, exactly 55 changed, all `core_rulebook`). Confirmed by the wave-16 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fourth section): 55/55 exactly, 38 to DONE + 17 to V, `core_rulebook` C 351→296, no drift. **Cycle 4**: reused the SAME generic pool-group pass for Sorcerer Bloodline (new probe `probe_sorcerer_bloodline_generic_member_wiring`, one new `classify()` rung, book-scoped to `core_rulebook` after this cycle's own scan found 3 real cross-book corpus-key collisions with `advanced_class_guide`/`ultimate_magic` the Cleric Domain cycle's scan did not have). `core_rulebook` C 296→242 (54 closed: 44 to DONE, 10 reclassified to V). Whole-inventory id-diff: 0 added/removed, exactly 54 changed, all `core_rulebook`, all `bloodline_power_or_bloodline_feat_not_computed` members. Remainder 242, 11 sub-causes summing exactly (`bloodline_power_or_bloodline_feat_not_computed` shrinks 77→23: 16 generic-pass-formula-chain refusals + 3 feat-pool-catalog-scope stragglers + 4 elemental-choice-flag records, none force-closed). 8 new tests (4 probe + 3 classify() + RED/GREEN proof), `class_feature` suite 138/138, full bin suite 461/461, `cargo test --locked --no-run` exit 0. `scripts/completion_atlas.py` citation pins re-derived (10→0 failures). `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. **Cycle 5**: closed the prior cycle's own named `versatile_performance_not_computed` sub-cause (9/9), a DIFFERENT engine mechanism than cycles 3/4's generic pool-group pass — Bard's own Versatile Performance slot-recognition record (`pilot_compute::mod.rs` SD13-E5, fixed explanation id, member identity read from `detail` text via a new probe-local bridge, not the reusable `generic_pool_group_selection_observed_keys`). Found and fixed a real corpus-vs-engine display-name mismatch (3 of 9 Perform types: `Percussion/String/Wind Instruments` vs. the engine's own shorter `Percussion/String/Wind`) with a probe-local mapping table, `pilot_compute.rs` itself untouched. `core_rulebook` C 242(true-reachable)→233 (9 closed, all straight to DONE — every member is `wiring_class: computed`, none reclassified to V). Whole-inventory id-diff against committed HEAD: 0 added/removed, 67 changed total, cleanly decomposed into 3 independently-attributed groups (54 cycle-4 bloodline, not this cycle's; 4 sibling AT-34-E4-002 trait-compute closures, not this cycle's; 9 this cycle's own, isolated by evidence string). Remainder 233, 11 sub-causes summing exactly (`versatile_performance_not_computed` 9→0, closed; `Bard ~ Versatile Performance` header record, a different non-`~`-pool corpus shape, deliberately excluded from this sub-cause both cycles, folded into `other_named_group_or_standalone` for the next cycle). 8 new tests (5 probe + 3 classify(), RED/GREEN proof), `class_feature` suite 142/142, full bin suite 470/470, `cargo test --locked --no-run` exit 0. `scripts/completion_atlas.py` citation pins re-derived a fifth time (10→0 failures). `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. Confirmed by the wave-18 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fifth section): cycle 4's 54 (44 DONE + 10 V, wave 17's own stranded/never-regenerated debt) and cycle 5's 9 (all DONE) both reproduced exactly, `core_rulebook` C 296→233. **Cycle 6**: closed 32 of the 33 bare `"<Domain> Domain"` HEADER records (cycle 5's own next-cycle-plan candidate, `domain_power_display_record_not_wired`) via the SAME paired display/chassis pattern the Favored Enemy/Favored Terrain checks establish, spanning TWO already-wired sibling shapes — `cleric_domain_generic_member_wired` prefix match (30 domains) and `domain_power::domain_power_probe_catalog()`'s own `"domain:<slug>"` selection-id lookup into `domain_power_effect_wired` (2 net-new: Good, War). Zero new probes, zero new `EngineFacts` fields, zero changes to `pilot_compute::mod.rs`/`domain_power.rs` — one new 73-line `classify()` rung. A genuine discovery: a pre-check proxy filtering candidate siblings by JSON `status == "grounded"` undercounted this rung's real reach (predicted 29, real 32) — the live probe credits a sibling regardless of whether it has since been restamped to `literal-verified` (bucket V); trusting the real live regen over the proxy caught 3 extra real closures (`Charm`, `Liberation`, `Sun`). `core_rulebook` C 233→**201** (32 closed, all straight to DONE, `wiring_class: "computed"` throughout, none reclassified to V). Whole-inventory id-diff: 0 added/removed, exactly 32 changed, all `core_rulebook`, all this cycle's own two evidence strings. 4 new tests (2 proof + 2 negative control), `class_feature` suite 146/146, full bin suite 477/477, `cargo test --locked --no-run` exit 0. A first regen attempt (`v06_work_inventory` alone) was correctly refused by its own stamp-loss guard (9,591 of 9,591 stamps at risk) — fixed by running the real three-stage pipeline (`corpus_literal_sweep` → `derived_evaluator_fixture_check` → `v06_work_inventory`), `--allow-stamp-loss` never passed. `scripts/completion_atlas.py` citation pins re-derived a sixth time (4→0 failures; one, the `V`-bucket pin, needed re-deriving twice after a rebase onto `AT-34-E4-002` cycle 7's own concurrent insertion into the same file shifted it again). `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. Remainder 201, 12 sub-causes summing exactly (`domain_power_display_record_not_wired` narrowed 41→2: the one remaining header [`Nobility Domain`] plus its own zero-token record; new `druid_nature_bond_domain_selection_not_computed` 7: confirmed genuine engine gap, no `DRUID_DOMAIN_CHOICE_ID` seam exists at all, per `pilot_compute::mod.rs`'s own Task #64 comment). Confirmed by the wave-19 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, sixth section): cycle 6's own 32 reproduced exactly, `core_rulebook` C 233→201; `engine-does-not-hold`→`grounded` is CLOSURE, not the "reclassification" label M cycle 4's own receipt applied to it (corrected there, not here). **Cycle 7**: re-derived cycle 6's own 12-sub-cause remainder table fresh before writing any code (`decisions.md §12` L2) and found one real count error: `monk_unarmed_damage_no_formula_in_engine` was stated 42, live corpus carries **48** (the 6 `(Small)`-size records were missing) — logged as a `correction` retro event. Closed 2 of the 201 via the SAME paired display/chassis pattern (Favored Enemy/Terrain, Domain header): `"Ranger Combat Style ~ Archery"` / `"~ Two-Weapon Combat"` are chooser-eligibility marker records whose group (`"Ranger Combat Style"`) can never resolve to owner `"ranger"`, so the generic suffix-matcher could never ground them even though `explain_ranger_level1_chassis_and_class_feature_separation`'s own `class_chassis.ranger.combat_style_choice` explanation already names exactly which style was chosen. New probe `probe_ranger_combat_style_wiring` mirrors the Favored Enemy/Terrain probes exactly, with one honest difference documented inline: no companion numeric magnitude exists to cross-check (the style choice itself is a +0 recognition), so a genuinely-observed `choice_observed` naming the exact selection is the whole bar. Checked for cross-book key collision before shipping (none found: `data/corpus/` declares this exact key only in `core_rulebook`). `core_rulebook` C 201→**199** (2 closed, both straight to DONE, `wiring_class: "computed"`). Whole-inventory id-diff: 0 added/removed, exactly 2 changed, both this cycle's own two ids. Corpus-wide C 4182→4180, DONE 24724→24726. 4 new tests (2 proof + 2 negative control), `class_feature` suite 150/150, full bin suite 486/486, `cargo test --locked --no-run` exit 0. `scripts/completion_atlas.py` citation pins re-derived a seventh time (10→0 failures, this cycle's own 186-line insertion having shifted every one). Also split cycle 6's vague `base_class_standalone_feature_not_computed`/`prestige_class_standalone_feature_not_computed` into more precise mechanism names after direct corpus reads: `"<Class> ~ Class"` and bare prestige-class-name records are `completeness: "chassis_only"` internal PCGen `DEFINE`/pool-tracker bookkeeping (new sub-causes `class_chassis_internal_tracker` 16, `prestige_class_chassis_internal_tracker` 10) with no player-facing explanation surface — confirmed genuine, not naming-only, by direct read; a promising-looking `class_chassis.<class>.base_attack_bonus` lead was checked and DECLINED once verified: it explains the DIFFERENT `Kind::Class` record (already `grounded`), not the `Kind::ClassFeature` bookkeeping record these candidates are — crediting one off the other would have been the "broadened matcher" doctrine forbids. `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. Remainder 199, 13 sub-causes summing exactly (48+35+26+25+16+13+11+10+10+5+7+2+0). Full detail in `AT-34-E3-002_cycle_receipt.md`. Confirmed by the wave-20 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, seventh section): cycle 7's own 2 reproduced exactly, `core_rulebook` C 201→199, corpus-wide C 4182→4180, DONE 24724→24726. **Cycle 8**: re-derived cycle 7's own 13-row remainder table from scratch by exhaustive per-key categorization (every one of the 199 live bucket-C `core_rulebook` keys assigned to exactly one sub-cause, script-verified sum, zero uncategorized) — found and corrected two real errors, both logged as `correction` retro events: cycle 7's own 13 listed values summed to 208, not its claimed 199; `base_class_standalone_feature_not_computed` corrected 35→25; `versatile_performance_not_computed` corrected 0→1 (`"Bard ~ Versatile Performance"`'s bare header record was never actually closed, contrary to cycle 7's "closed cycle 5" claim). Closed the 11-unit `favored_class_bonus_choice_not_wired` sub-cause's 6 closable members: Fighter's own already-shipped SD13-E5 explanation (`explain_fighter_favored_class_bonus_choice`) had never been consulted by `classify()`; generalized the SAME rule/choice-id/+1-magnitude to Barbarian, Monk, Paladin, Rogue, Wizard via one new `pilot_compute::mod.rs` function (`explain_other_classes_favored_class_bonus_choice`) reusing each class's own already-existing bounded level-1 chassis seam, rather than duplicating Fighter's function five times. The other 5 (Adept, Aristocrat, Commoner, Expert, Warrior) have no `supported_<class>_level` seam at all — genuine engine gap, named in the remainder, not force-closed. `core_rulebook` C 199→**193** (6 closed, all straight to DONE, `wiring_class: "computed"`, none reclassified to V). Whole-inventory id-diff: 0 added/removed, 21 changed total, cleanly decomposed into this cycle's own 6 (evidence-string-filtered) plus 15 unrelated `X`→`X` evidence-string-only fixes from an already-landed wave-21 classifier fix (`38e10d066b`) this cycle's regen was simply the first to fold in. Corpus-wide C 4180→4174, DONE 24731→24737. Cross-book collision check performed before shipping (none found: each of the six bare class-name keys is declared exactly once, only in `core_rulebook`, across the full 49,438-unit corpus). 4 new tests (2 proof + 2 negative control), `class_feature` suite 154/154, full bin suite 493/493 pre-rebase (496/496 post-rebase after a concurrent sibling lane's cycle landed), `cargo test --locked --no-run` exit 0 both times. `scripts/completion_atlas.py` citation pins re-derived (10→0 failures), resolved through one rebase conflict against a concurrently-landed `AT-34-E4-002` cycle 9 citation re-pin (their post-rebase-derived values taken, verified 0 failures after). `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. Remainder 193, 13 sub-causes summing exactly (48+26+25+25+16+13+10+10+7+5+5+2+1). Full detail in `AT-34-E3-002_cycle_receipt.md`. Confirmed by the wave-21 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, ninth section): cycle 8's own 6 (all `engine-does-not-hold`→`grounded`, `core_rulebook` bucket C) reproduced exactly, `core_rulebook` C 199→193, corpus-wide C 4180→4174. **Cycle 9**: re-checked cycle 8's own 13-row remainder rather than inheriting it (two smallest sub-causes re-verified directly against live code, both held). Found and closed one real case inside `ranger_favored_x_chassis_or_wild_empathy` (5): `"Ranger ~ Favored Enemy"`'s bare bookkeeping header had no exact-slug explanation id (its 3 pre-existing ids all carry an unmatched suffix), unlike its own already-grounded sibling `"Ranger ~ Favored Terrain"`. Added one new `pilot_compute::mod.rs` explanation, `class_feature.ranger.favored_enemy`, carrying the SAME already-triple-verified magnitude — no new formula, no new `classify()` rung (the classifier's pre-existing GENERIC exact-suffix path grounds it unaided once the id exists). `core_rulebook` C 193→**192** (1 closed, straight to DONE). Whole-inventory id-diff: 0 added/removed, exactly 1 changed, corpus-wide. Corpus-wide C 4174→4173, DONE 24961→24962; every other bucket unchanged. Investigated the sub-cause's other member, `Ranger ~ Wild Empathy` (no compute at all; formula would mirror Druid's already-shipped `class_chassis.druid.wild_empathy`), and correctly declined it: its `wiring_class: "static"` routes doneness through `AT-34-E3-005`'s separate oracle-agree/bucket-V mechanism (confirmed via Druid's own sibling record's real disposition), not this criterion's classify()-explanation path — named in the remainder, logged as a `deferral` retro event, not force-closed. 3 new tests (1 integration proof + 1 classify()-level proof + 1 negative control), widened `RANGER_PER_PILLAR_RECORD_IDS` 7→8 (free leakage-guard extension). `cargo test --locked --test sd13_ranger_level1_chassis_and_class_feature_separation`: 19/19. Full bin suite 501/501 (497+4), `class_feature` suite 156/156. `cargo test --locked --no-run` exit 0. `docs/work-inventory.json` regen local/uncommitted only, per file-ownership rule. NOT closed. Remainder 192, 13 sub-causes summing exactly (48+26+25+25+16+13+10+10+7+5+4+2+1). Full detail in `AT-34-E3-002_cycle_receipt.md`. Confirmed by the wave-22 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, tenth section): cycle 9's own claimed 1 (`Ranger ~ Favored Enemy`, `engine-does-not-hold`→`grounded`) reproduced exactly, `core_rulebook` C 193→192 — regen-verified, not just locally measured. |
| 15 | `core-buckets-m-v-d-u-x-zero` | 3 | AT-34-E3-003 | in-progress | `artifacts/epic-3-core-rulebook/AT-34-E3-003_u_bucket_ruling_cycle_receipt.md` — bucket `U` ruling cycle 1 (`decisions.md §17`): 110/140 corpus-wide `equipment_modifier` `unmeasurable` units → `text-complete` (40/58 `core_rulebook`), 18 `core_rulebook` remain named by sub-cause (21 genuine unresolved PCGen substitution, 9 a separate `render_pcgen_desc` bug); U 321→211 corpus-wide. `artifacts/epic-3-core-rulebook/AT-34-E3-003_u_bucket_render_bug_cycle_receipt.md` — bucket `U` cycle 2: fixed the named `render_pcgen_desc` bare-percent-after-digit defect, closing 9 named + 1 discovered (`burdenless`) = 10 units corpus-wide (8 `core_rulebook`); U 211→202 corpus-wide, `core_rulebook` U 18→10, DONE 1440→1448. `core_rulebook` U remainder: 10, all one sub-cause (`%CHOICE`/`%d<N>` unresolved substitution, awaiting operator ruling). `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_skill_cycle_receipt.md` — bucket `M`, `skill_content` sub-cause: 76 `core_rulebook` units `ingested-magnitude`→`grounded` (simple_kind_verdict's new `grounded_magnitude` parameter, `Kind::Skill`'s real fixture-executed probe); `skill_content` 95→19, `core_rulebook` M 1048→972, corpus-wide M 5114→5038. Live re-derive via `python3 scripts/completion_atlas.py --book core_rulebook --check`: M=972 (9 named sub-causes), V=81, D=366, U=10, X=115 — sum 1544. NOT closed. `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt.md` — bucket `M`, EQUIPMENT sub-causes (`equipment_table_entry_with_corpus_magnitude` + `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does`, 423 of 972): 14 `core_rulebook` closures (`equipment_table_entry_with_corpus_magnitude` 276→262; the closure-only shape, 147, got 0 — genuinely different, one fix does not cover both), via `equipment_key_is_wired`'s new `damage_total::resolve_base_damage_dice` consultation (already-wired compute path, no new subsystem). Generic by construction: 31 units closed corpus-wide across 9 books (whole-corpus id-diff: 49438→49438, 0 added/removed, 37 changed — 31 real closures + 6 `ultimate_equipment` bucket-`V` reclassifications). `core_rulebook` M 972→958, corpus-wide M 5002→4971, DONE 24278→24309. Remainder 409 (262+147) named by 4 real sub-shapes (AC-bonus equipmods = new compute path, `%CHOICE`-gated = sibling scope, internal-plumbing VAR/ITEMCOST, named-artifact prose) — none has an existing wired compute path. `docs/work-inventory.json`/`completion-atlas.json` deliberately NOT committed this cycle (shared end-of-wave regen owns them per this wave's dispatch brief); figures derived from a local regen, restored before commit. NOT closed. Confirmed exactly by the wave-15 shared regen cycle (`AT-34-E3-001_wave9_regen_receipt.md`, third section): whole-corpus id-diff shows precisely 37 changed (31 closures matching this row's own 9-book breakdown unit-for-unit, 6 `ultimate_equipment` bucket-`V` evidence reclassifications), `core_rulebook` M 972→958 and DONE 4330→4344 exactly as stated, `citation_failures=0`. `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_2.md` — bucket `M`, EQUIPMENT sub-causes, cycle 2: re-derived the same 409-unit remainder fresh (not inherited) and, before writing code, exhaustively classified all 409 by real corpus shape into 9 named mechanisms (choice-gated 99 = sibling scope; `VAR` cross-subsystem 121 = new engineering, not "not player-facing" as previously framed — several genuinely are player-facing (e.g. Phylactery of Negative Channeling really does widen a Cleric's Channel Energy dice), they lack cross-subsystem wiring, not mechanical relevance; chassis/plumbing 100; prose-only named-artifact magnitude 71; ITEMCOST/EQM/EQMWEAPON/WEAPON no-compute-field shapes 9+3+3+2+1 = 18), summing to exactly 409. Found and closed 1 more real unit the prior cycle's instrument could not reach: `resolve_base_damage_dice` widened with a one-hop `BASEITEM:` chase (same `equipment_id_resolve` call, not a new mechanism) — `Crossbow (Light)` → `BASEITEM:Light Crossbow (Base)` (`DAMAGE:1d8`) closes, `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` 147→146, `core_rulebook` M 958→957, DONE 4344→4383 (+1 this cycle, +38 the separately-already-committed `AT-34-E3-002` Cleric Domain fix this same regen also picked up, not this cycle's work). Corpus-wide: M 4966→4965 (−1, isolated from the co-mingled Cleric Domain delta by re-running the atlas against the pre-regen file directly), whole-corpus id-diff 49438→49438 0 added/removed 72 changed (55 Cleric Domain, not this cycle; 17 `equipment` this cycle's own — 1 real closure + **16 bucket-`V` evidence-string corrections found as a side effect**: `Axe (Throwing)`, `Battleaxe`, `Club`, `Dart`, `Falchion`, `Greataxe`, `Greatclub`, `Hammer (Light)`, `Handaxe`, `Pick (Heavy)`, `Pick (Light)`, `Sap`, `Scimitar`, `Scythe`, `Sickle`, `Warhammer` were already `literal-verified` under the stale M-shaped evidence string and now carry the accurate one, bucket unchanged). 4 new tests (`damage_total.rs`) + 1 new test (`v06_work_inventory.rs`) against the real on-disk corpus, RED confirmed then GREEN; 33/33 and 454/454 pass; `cargo test --locked --no-run` and the desktop crate both exit 0. Remainder 408 (262+146), same 9-shape census, no closable-by-existing-instrument units left in this territory. NOT closed. Confirmed by the wave-16 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fourth section): 1 closure (`crossbow_light`) + 16 evidence-string reclassifications exactly as this cycle's own local regen predicted, `core_rulebook` M 958→957. `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_3.md` — bucket `M`, EQUIPMENT sub-causes, cycle 3: found a real mechanism cycle 2's exhaustive census missed — `TEMPBONUS:` tokens (PCGen's temporary/consumable-triggered sibling of `BONUS:`), never read by `compute_general_effect`/`compute_magic_items_effect` (both only parse `record.bonus_chains`, populated from `BONUS:` tokens only). `Potion of Bull's Strength` carries no `BONUS:STAT` chain at all, only `TEMPBONUS:ANYPC|STAT|STR|4|TYPE=Enhancement` — its entire real effect. Widened both resolvers with a `TEMPBONUS:` fallback (fires only when no explicit `BONUS:` chain wins); `compute_equipment_effects` already calls both unconditionally on every record, so the fix is corpus-wide by construction with no dispatch change. Caught and excluded PCGen's `ALL` skill wildcard (`Setting Stone (Invigoration)`, `TEMPBONUS:PC|SKILL|ALL|2|TYPE=Morale`) before shipping — would have fabricated a bonus to a skill literally named "ALL". 10 new tests (4 negative controls + 6 positive/edge fixtures) against real corpus records, RED then GREEN; 82/82 (`equipment_effects`) and 473/473 (`v06_work_inventory`) pass; full-workspace and desktop-crate `--no-run` both exit 0; 5 pre-existing unrelated failures confirmed (already-named `AT-34-E3-005` regression). Closed **20 units corpus-wide** across 3 books (13 `core_rulebook` + 5 `advanced_class_guide` + 2 `advanced_race_guide`), whole-corpus id-diff 49438→49438 0 added/removed 90 changed (20 this cycle's own equipment closures + 70 co-mingled already-committed sibling-cycle changes, named and not claimed: 63 `AT-34-E3-002` cycle 5 Bard fix, 7 `AT-34-E4-002` cycle 6 trait fix). `core_rulebook` M 957→944 (−13), `equipment_table_entry_with_corpus_magnitude` 262→249, `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` unchanged at 146 (fix reaches only the same-line shape — confirms cycle 2's "two shapes stay different" finding a third time). `docs/work-inventory.json`/`completion-atlas.json` deliberately NOT committed (shared regen owns them); figures from a local regen, restored before commit. Remainder 395 (`core_rulebook`-scoped, 249+146) named by 9 real mechanisms in the receipt (choice-gated 99 = sibling scope, `VAR` cross-subsystem 121, `TEMPBONUS` compound/wildcard 7 [new discovery], chassis/plumbing 99, prose-only named-artifact 71 [next cycle's best-ROI candidate], `ITEMCOST`/`EQM`/`EQMWEAPON`/`WEAPON` no-field shapes 9+3+3+2+1). NOT closed. Confirmed by the wave-18 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fifth section): this cycle's own 20 (13+5+2) reproduced exactly, `core_rulebook` M 957→944 — but this row's own "63 = cycle 5 Bard fix" / "7 = cycle 6 trait fix" sub-labels each conflate two cycles (63 = 9 cycle-5 + 54 cycle-4; 7 = 3 cycle-6 + 4 cycle-5 UC, wave 17's own stranded debt); the corrected per-cycle table is in that section.  `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_4.md` -- bucket `M`, EQUIPMENT sub-causes, cycle 4: found `general::compute_var_effect`/`apply_eqmod_var_bonus` already existed (oracle-validated by SD-33's `e5_var_shape_ours.rs`) but was never called from `compute_equipment_effects` -- wired it in, correcting cycle 2's own "VAR needs new engineering" finding. Closes BOTH shapes this time: `equipment_table_entry_with_corpus_magnitude` 249->164 (-85), `equipment_own_line_has_no_magnitude_but_closure_wiring_class_does` 146->99 (-47); `core_rulebook` M 944->812 (-132). Corpus-wide 256 closures across 9 books (whole-corpus id-diff 49438->49438, 0 added/removed, 337 changed: 256 this cycle's own + 46 this cycle's own DONE->DONE reclass + 32 co-mingled `AT-34-E3-002` cycle 6 (not mine, matches its own stated 32 exactly) + 3 co-mingled fixture-check pass, not mine). 18 units (formula-valued, `PRE`-gated `Intelligent Item ~ Alignment` chassis rows) correctly declined, named as a real boundary. Remainder 263 (164+99), fresh 13-row qualifier-shape census in the receipt. 3 new tests, RED->GREEN; 83/83 + 482/482 + 2977/5-pre-existing-failed; workspace + desktop crate `--no-run` both exit 0. NOT closed. Confirmed by the wave-19 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, sixth section): this cycle's own 337-unit reconciliation (256+46+32+3) reproduced digit-for-digit; `core_rulebook` M 944→812, corpus-wide M 4938→4679, DONE 24433→24724 exactly as stated — but this row's own "46 ... DONE->DONE reclass" and "32 co-mingled ... not mine" both used the label "reclass(ification)"; under `decisions.md §9`'s own four-bucket taxonomy the 32 (`engine-does-not-hold`→`grounded`) is CLOSURE (a non-DONE bucket reaching DONE, just not this cycle's own), and the 46 (`text-complete`→`grounded`, both DONE) is INSTRUMENT-CORRECTION (no bucket boundary crossed at all) — corrected bucketing, not corrected counts, in that section. `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_5.md` — bucket `M`, EQUIPMENT sub-causes, cycle 5: re-derived `core_rulebook` M fresh (812, matching cycle 4/wave-19 exactly) and the 263-unit territory (164+99) independently, then re-classified all 263 by real corpus shape by script (not inherited) — matched cycle 4's own 13-row census exactly. Investigated cycle 4's own next-cycle-plan item 1 (COMBAT/STAT/SKILL "already wired, worth investigating"): all 20 records read by hand carry a `%CHOICE` magnitude (player-chosen, no fixed literal), the SAME correctness boundary as cycle 4's own VAR finding, not a resolver bug — correction logged via retro. Found and closed the one real generic gap: `TEMPBONUS:<target>|COMBAT|AC|<n>` (cycle 3's own TEMPBONUS idiom, never extended to arms_armor's AC field) — `Cloak of the Manta Ray`'s real +3 natural armor bonus, 1 unit corpus-wide (a corpus-wide sweep confirms no other record carries this shape today). New `tempbonus_combat_ac_fallback`, RED->GREEN, 3 new unit tests + 1 integration test; 89/89 + 30/30 pass; workspace + desktop crate `--no-run` both exit 0. `core_rulebook` M 812->811, `equipment_table_entry_with_corpus_magnitude` 164->163, closure-shape unchanged at 99 (fix reaches only same-line). Whole-inventory id-diff 0 added/0 removed/3 changed (this cycle's own 1 + 2 co-mingled AT-34-E3-002 cycle 7 closures, already verified by that cycle's own receipt, not claimed here). Remainder 262 (163+99), independently re-confirmed no closable-by-existing-instrument units remain: 34 %CHOICE-gated + 18 VAR/PRE-gated = 52 correctness boundaries, 182 COST/WT-only nothing-to-compute, 31 scattered new-subsystem shapes each too small alone. NOT closed. Confirmed by the wave-20 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, seventh section): cycle 5's own 1 closure (`cloak_of_the_manta_ray`) reproduced exactly, `core_rulebook` M 812→811; the 2 co-mingled units in this row's own id-diff are AT-34-E3-002 cycle 7's own (row 14), not double-counted here. `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_6.md` — bucket `M`, EQUIPMENT sub-causes, cycle 6: disproves cycle 5's own "generically exhausted" premise. `encumbrance::compute_encumbrance` already reads a record's own `WT:`/`COST:` tokens and is already a real, wired consumer (called by `pilot_compute_corpus`/`contract`, rendered on the desktop character sheet's Gear tab); the wiring probe (`equipment_key_is_wired`) simply never consulted it. Two composable fixes: (1) `encumbrance::equipment_key_resolves_a_carried_weight` wired into the probe, gated on `WT:`/weight specifically (matches `compute_encumbrance`'s own "weight required, cost supplementary" rule — `COST:` alone is not sufficient); (2) `corpus_loader::equipment_record_from_json` synthesizes `WT:`/`COST:` tokens from the already-ingested `weight_lbs`/`cost_gp` fields for "thin" records with no `raw_tokens` at all (same idiom as its existing `KEY:` synthesis; confirmed corpus-wide, 4,470 enriched records, 0 mismatches). The full regen genuinely did not finish inside this cycle's turn budget (killed after ~13 min CPU, `docs/work-inventory.json` confirmed byte-identical to its pre-run state) — the same wall `AT-34-E4-002` cycle 9 already hit and named (this table, row 19–21). In its place, measured `equipment_key_is_wired` directly against the real on-disk corpus, one book at a time (the literal function `classify()`'s equipment arm consults first, unconditionally, for both `Kind::Equipment` and `Kind::EquipmentModifier`): **221 of 763 territory units corpus-wide now resolve wired (33 of 262 `core_rulebook`)** — fix 1 alone (already-enriched records) 109 corpus-wide (23 `core_rulebook`); fix 2's own marginal reach (thin records) +112 corpus-wide (+10 `core_rulebook`). Per-book: `advanced_class_guide` 41/50, `advanced_players_guide` 4/11, `advanced_race_guide` 3/12, `adventurers_guide` 61/88, `bestiary` 3/4, `core_rulebook` 33/262, `inner_sea_gods` 14/14, `inner_sea_magic` 6/64, `inner_sea_temples` 32/35, `occult_adventures` 2/3, `ultimate_equipment` 15/120, `ultimate_intrigue` 1/1, `ultimate_psionics` 5/66, `ultimate_wilderness` 1/1, remaining 7 books 0. 3+3+4 new tests (`encumbrance.rs`/`v06_work_inventory.rs`/`corpus_loader.rs`), RED→GREEN against real corpus fixtures; `rules_core::encumbrance::` 9/9, `rules_core::corpus_loader::` 10/10, `--bin v06_work_inventory equipment` 33/33, `rules_core::equipment_effects::` 86/86; `cargo test --locked --no-run` and the desktop crate both exit 0 at `acd6a6a5e3`. Retro `correction` event (cycles 2/3/5's "COST/WT-only means nothing to compute" disposition) + retro `deferral` event (the incomplete regen, revisit condition named) both logged. **This cycle's 221-unit (33 `core_rulebook`) closure is real and proven but NOT YET reflected in `docs/work-inventory.json`** — `core_rulebook` M stays reported at 811 until a full regen completes and applies it; movement this cycle is 0 committed / 221 proven closure-eligible pending the next regen. Remainder after the fix applies: `core_rulebook` 229 (262−33), dominated by the already-declined `%CHOICE`-gated (34) and `VAR`/`PRE`-gated (18) families plus `COST:`-only-no-`WT:` records and small new-subsystem shapes cycle 5's own census named — to be re-derived fresh once the regen actually applies this cycle's closures. NOT closed. Confirmed by the wave-21 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, ninth section): cycle 6's own 221 corpus-wide (33 `core_rulebook`) reproduced exactly by book, `core_rulebook` M 811→778, plus 2 `mythic_adventures` bucket-DONE evidence-string corrections (`literal-verified`→`literal-verified`, no bucket movement) found as a side effect, not claimed by this cycle. | `artifacts/epic-3-core-rulebook/AT-34-E3-003_m_bucket_equipment_cycle_receipt_7.md` — bucket `M`, EQUIPMENT sub-causes, cycle 7: re-derived the 229-unit remainder (`core_rulebook`, post wave-21 regen) fresh and, per the wave's own instruction to disprove a stated reason where warranted, re-checked cycle 6's own next-cycle-plan characterization against every candidate compute-path consumer before writing code. Finding: cycle 6's characterization holds and is now exact — 7 named mechanisms sum to 229 (choice-gated 104, cost-only-no-weight-deliberately-excluded 44, untraced-closure 28, VAR-formula-needs-evaluator 18, thin-no-raw-tokens 17, ITEMCOST-pricing-formula-no-consumer 9, real-type-excluded-or-new-mechanic 9), none closable by an existing wired consumer the probe was blind to. No code changed; no bucket moved. Retro `correction` logged against the dispatch brief's own stale 423-unit split. NOT closed. Confirmed by the wave-22 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, tenth section): 0 movement reproduced exactly — `core_rulebook` M unchanged at 778, corpus-wide M unchanged.
| 16 | `core-step-cost-ledger` | 3 | AT-34-E3-004 | complete | `artifacts/epic-3-core-rulebook/step-cost-ledger.json` (3 of 7 non-DONE buckets have a real cost entry: B/C/U; D/M/V/X named in `buckets_not_yet_cleared`, no dedicated clearing cycle yet), `artifacts/epic-3-core-rulebook/step-cost-ledger-raw-commits.json`, `artifacts/epic-3-core-rulebook/AT-34-E3-004_cycle_receipt.md` |
| 17 | `core-rulebook-zero-remaining` | 3 | AT-34-E3-005 | in-progress | Gated on AT-34-E3-001/002/003 reaching zero (not yet: B 470, C 357, D 405, M 1048, V 81, U 10, X 115 remain, `completion_atlas.py --book core_rulebook --check`). Built this criterion's own evidence artifact `artifacts/epic-3-core-rulebook/core-rulebook-completion-manifest.json` (one row per unit, current bucket + evidence pointer, `complete: false`); `artifacts/epic-3-core-rulebook/AT-34-E3-005_cycle_receipt.md`. Bucket-V lane (`sd34-bucket-v`): cross-referenced all 2,793 `core_rulebook` bucket-V units against SD-33's own 24 committed `oracle-results.json` files + `AT-33-E1-003`'s no-probe-surface census — 2,712 of 2,793 (97.1% of 2,793) dispositioned with a real, freshness-checked oracle verdict (0 disagreements), 81 named remainder by sub-cause; `artifacts/epic-3-core-rulebook/bucket-v/bucket-v-consolidated.oracle-results.json`, `artifacts/epic-3-core-rulebook/AT-34-E3-005_bucket_v_consolidation_cycle_receipt.md`. **Apply cycle (`decisions.md §19`, operator ruling extending `§17`):** wired the consolidated ledger into `classify()`'s status-promotion rung (`v06_work_inventory.rs::apply_bucket_v_oracle_disposition_stamps`, keyed only on the ledger's own per-unit verdict) and regenerated `docs/work-inventory.json` — `core_rulebook` bucket V now reads **81 of 2,793** (2,712 moved V→DONE: 385 `oracle-agree`, 2,327 `oracle-unverifiable`; SET-verified against `bucket-v-remainder.json`'s 81 named ids), 0 units outside `core_rulebook` moved. `no_probe_surface`'s 130 carried forward into `artifacts/epic-5-forward-plan/capability-register.json` as `oracle_probe_surface_for_no_table_kinds` (10 → 11 capabilities). `artifacts/epic-3-core-rulebook/bucket-v/AT-34-E3-005_bucket_v_apply_cycle_receipt.md`. **Corpus-wide widen cycle (`bucket-v-widen`):** an earlier salvage cycle landed the loader's multi-path widening (tested, generic) but its own corpus-wide ledger data file was lost uncommitted — the loader was a verified no-op, corpus-wide V unchanged at 6,846. This cycle built and **committed** that ledger: cross-referenced all 6,765 non-`core_rulebook` bucket-V units against SD-33's own final, 100%-complete, 0-disagree combined oracle ledger (`AT-33-E5-003.combined-oracle-results.json`) plus `AT-33-E1-003`'s no-probe-surface census — 6,590 of 6,765 (97.4% of 6,765) dispositioned (426 `oracle-agree`, 6,164 `oracle-unverifiable`; 0 disagreements; 30-row freshness-checked sample, 0 drift), 175 named remainder by book:kind sub-cause (SET-verified, not just counted); `artifacts/bucket-v-widen/bucket-v-corpus-wide-consolidated.oracle-results.json`, `artifacts/bucket-v-widen/bucket-v-widen-remainder.json`, `artifacts/bucket-v-widen/AT-34-E3-005_bucket_v_widen_cycle_receipt.md`. Corpus-wide V **6,846 → 256** (81 `core_rulebook`, unchanged, + 175 this cycle's own remainder); corpus-wide DONE **17,576 → 24,166**. `oracle_probe_surface_for_no_table_kinds` capability grew live (no code change) from 130/1 book to **2,062/30 books**. Zero Rust source changed — the loader and apply rung were already landed; this cycle supplied only the data. |
| 18 | `atlas-defects-recorded` | 3 | AT-34-E3-006 | complete | `artifacts/epic-3-core-rulebook/atlas-defects.md` (3 entries, pre-existing from AT-34-E3-001) verified this cycle by a mechanical structural checker (scratch, RED→GREEN, not committed — see `artifacts/epic-3-core-rulebook/AT-34-E3-006_cycle_receipt.md`): `entries=3 violations=0` — each entry carries a resolvable `**Retro event:**` (correction/deferral) and either an `**Atlas re-derivation:**` or an explicit `decisions.md §16`-sanctioned "not settled" statement. Cross-checked every later `AT-34-E3-*` receipt referencing the open no-content-record question; none raised a fourth, unrecorded defect. |
| 19 | `uc-non-a-tail-resolved` | 4 | AT-34-E4-001 | complete | `artifacts/epic-4-ultimate-campaign/AT-34-E4-001_cycle_receipt.md` — 21 `U` (all `feat`/Story, `[Not Implemented]` marker + real joined BENEFIT text): proven the `unmeasurable` verdict is a deliberate corpus-wide-consistent decision (`SD31-E2-F3-002`), not an instrument gap, via a new mechanically-checked test; stale "text-complete" doc claim in `ultimate_campaign::feat_tables` corrected; marker-stripping filed as a forward capability candidate (`AT-34-E5-002`), not decided here. 2 `X` (`Fearless Zeal`, `Magnum Opus`): splice/truncation defects checked against the pinned oracle and confirmed real, current, unrepairable — `X` is correct and final. No bucket movement (`U:21 X:2` unchanged); resolution is by proof per the criterion's own Evidence clause. |
| 20 | `ultimate-campaign-zero-remaining` | 4 | AT-34-E4-002 | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt.md` — bucket `B` (5) closed via a missing PI-coordinate wire-up in `Kind::Trait` (same mechanism `decisions.md §14` already built for `Domain`/`Deity`); `DONE 127->130`, `B 5->0` (3 DONE, 1 M, 1 D — split by real corpus shape, not forced). Remainder named exactly: `M:89 V:18 D:5 U:21 X:2` = 135; `U`/`X` correctly not reopened (already proven final by AT-34-E4-001, and `§17`'s 2026-08-28 ruling targets a different shape). `DONE=130 of 265` — bar not met. |
| 21 | `second-cost-measurement` | 4 | AT-34-E4-003 | complete | `artifacts/epic-4-ultimate-campaign/step-cost-ledger.json` (1 of 5 non-DONE `ultimate_campaign` buckets has a real cost entry: `B`, 1 cycle, 108.0 min, 5->0, 3 DONE/2 reclassified; `U`/`X` resolved by proof, no rate — `decisions.md §9`; `D`/`M`/`V` untouched), `artifacts/epic-4-ultimate-campaign/step-cost-ledger-raw-commits.json`, `artifacts/epic-4-ultimate-campaign/AT-34-E4-003_cycle_receipt.md` — comparison vs Epic 3's bucket-B rate (5.8 units/hr blended, 22.2-617.4 min/cycle range): Epic 4's single mechanism (108 min/5 units) sits inside that range (agreement); its blended rate (1.667/hr) is 3.5x slower (divergence), explained as **sample-size noise (n=1 cycle)**, not book shape — the one shape-linked fact supported is mechanism count (1 vs 29), not per-unit cost. Epic 5 warned not to price M/D/V off this bucket-B point alone. |
| 22 | `forward-plan-per-book-per-bucket` | 5 | AT-34-E5-001 | complete | `artifacts/epic-5-forward-plan/forward-plan.json` (35 books, per-bucket unit counts, mechanism, rate+sample size on every row), `artifacts/epic-5-forward-plan/build_forward_plan.py` (re-derive script), `artifacts/epic-5-forward-plan/verify_forward_plan.py` (RED->GREEN structural check), `artifacts/epic-5-forward-plan/AT-34-E5-001_cycle_receipt.md`. 11,919 of 29,364 units priced to DONE, 3,981 priced only to V, 13,464 (45.9%) unpriced (buckets D/M/V/X/Z have zero dedicated clearing cycles in either vehicle book) — named honestly, not blended into a false total. |
| 23 | `capability-register` | 5 | AT-34-E5-002 | complete | `artifacts/epic-5-forward-plan/capability-register.json` (10 named capabilities, `built_by_sd34: false` on every row), `artifacts/epic-5-forward-plan/build_capability_register.py` (re-derive script), `artifacts/epic-5-forward-plan/verify_capability_register.py` (RED->GREEN structural + live-population check), `artifacts/epic-5-forward-plan/AT-34-E5-002_cycle_receipt.md`. 8 of 10 capabilities carry a live-re-derived population (`power_engine_table` 421, `companion_table_shape_widening` 28, `per_character_choice_filter` 113, `companion_mount_advancement_table` 9, `class_feature_deep_subsystem_modelling` 32 — the bucket-X reconciliation sums exactly to the live 171); 2 are cited from an already-verified AT-34-E3-001 receipt (monster-class modelling 2, familiar-pool ownership 14) with a flagged re-derivation gap; 2 are named with an explicitly unsized population (`atlas-defects.md` #2's two unresolved content shapes) rather than guessed. Ordinary bucket-B placement work (634 `companion_absent` units, already priced by `AT-34-E5-001`) deliberately excluded — not new capability. |
| 24 | `power-table-costed` | 5 | AT-34-E5-003 | complete | `artifacts/epic-5-forward-plan/power-table-cost.json` — 421 units priced (2-7 marginal lines/49-172s ESTIMATE, matched-directory tier only), `AT-34-E5-003_cycle_receipt.md` |
| 25 | `plan-ordered-single-bucket-flagged` | 5 | AT-34-E5-004 | complete | `artifacts/epic-5-forward-plan/ordered-plan.json` (35 books: 19 ranked ascending by priced-to-DONE cost midpoint, 16 unrankable-by-cost listed alphabetically, 1 single-bucket book flagged by name — `beginner_box`), `build_ordered_plan.py` (re-derive script), `verify_ordered_plan.py` (RED->GREEN structural + live cross-check), `AT-34-E5-004_cycle_receipt.md`. |
| 26 | `final-acceptance-scan` | 6 | AT-34-E6-001 | not-started | |
| 27 | `retro-sweep-archdocs-pr` | 6 | AT-34-E6-002 + AT-34-E6-003 | not-started | |
| 28 | `salvage-2026-08-30` | cross-cutting (recovers two isolated worktrees' uncommitted diffs) | AT-34-E4-002 (continuation) + bucket-v-widen infra | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_2.md` — corpus-wide `closure_has_real_aspect_description` widening closes 123 of 49,438 units (`D`→`DONE`), 3 of them the named `ultimate_campaign` units its own first cycle deferred (`ultimate_campaign` `DONE` 130→133, `D` 5→2; criterion's own `DONE=265 of 265` bar NOT met, 132 remain). `artifacts/epic-5-forward-plan/bucket_v_widen_infra_cycle_receipt.md` — landed the bucket-V oracle-ledger loader's multi-path widening (tested, generic, safe) but the actual corpus-wide ledger data file the salvaged diff claimed (6,590-row disposition, `core_rulebook` V 6,846→256) was never captured by the patch (untracked file, lost with the uncommitted session) — **dropped, not landed**; corpus-wide bucket V unchanged at 6,846. A third salvaged patch (`bucket-m.patch`, docs-only) was also dropped: it cited a receipt and a retro event that likewise do not exist in the repo. `forward-plan.json`/`capability-register.json` re-derived live and both verifiers pass. |
| 29 | `trait-drawback-capability-built` | 4 | AT-34-E4-002 (cycle 3) | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_3.md` — the character trait/drawback selection capability is BUILT (real `CharacterInput.chosen.selected_traits`, a real fixture-executed compute-and-apply path in `trait_effects.rs`/`skill_allocation.rs`, and a real desktop checkbox picker), landing the pure flat `BONUS:SKILL` spine: **31 of 59** `ultimate_campaign` trait records (corrected from the criterion's own "44 of 59" estimate, retro-logged). Corpus-wide, kind-keyed payoff: **36 of 49,438** units moved `ingested-magnitude → grounded` (31 `ultimate_campaign` + 5 `advanced_players_guide` sharing the same corpus `KEY`). `ultimate_campaign`: `DONE 151->182, M 89->58`; `U:21 D:2 X:2 V:0` untouched. Remainder named by sub-cause: 12 `%LIST` choice traits, 3 ability-formula traits, 15 mixed-bonus-type traits, 1 corpus data gap, 17 narrative Drawbacks, 1 cross-skill-guarded Drawback, 12 `Retrain` records (different mechanic, flagged for re-keying) = 58+21+2+2=83 non-DONE. `DONE=182 of 265` — bar not met. Confirmed by the wave-15 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, third section): this cycle's own inline self-regen (`e8ac310280`) was already correct — the 36 corpus-wide `grounded` trait units are byte-identical before and after an independent from-scratch pipeline re-run. |
| 30 | `trait-choice-spine-and-picker` | 4 | AT-34-E4-002 (cycle 4) | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_4.md` — widens the trait/drawback capability to its second slice: the **5 of 28** remaining `trait_content` records whose `BONUS:SKILL|%LIST` names a fixed, closed list of concrete skills (`trait_criminal`, `trait_fiend_blood`, `trait_harvester`, `trait_influence`, `trait_style_sage`), via the same generic `SelectedChoice` channel `archetype_resolver.rs` already established — not a new mechanism. Landed **end to end**: desktop `CreateCharacterForm.tsx` now renders a real `<select>` for a checked choice-based trait and submits the pick through `CreateCharacterRequest.trait_skill_choices` into `compose_character_input`'s `selected_choices`, closing the "compute path with no UI reaching it" gap the dispatch doctrine flags. `ultimate_campaign`: `DONE 182->187, M 58->53`; `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff in another book this time (checked). `DONE=187 of 265` — bar not met. Instrument-correction, not content movement: all 10 of `completion_atlas.py`'s citation line pins re-derived after this cycle's rebase onto `AT-34-E3-002`'s own concurrent WIP cycle shifted every one a second time (`citation_failures` 10→0, no bucket population moved). Remainder named by sub-cause, unchanged from cycle 3 except the 5 now closed: 4 open-subtype `%LIST` traits (need a free-text Craft/Perform/Profession chooser — a materially different, out-of-scope input shape), 3 ability-formula traits, 15 mixed-bonus-type traits, 1 corpus data gap, 17 narrative + 1 cross-skill-guarded Drawback, 12 `Retrain` records = 53+21+2+2=78 non-DONE. Confirmed by the wave-16 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fourth section): this cycle's own inline self-regen was already correct — `ultimate_campaign` byte-identical before/after this cycle's independent from-scratch pipeline run, 0 drift. |
| 31 | `trait-family-choice-spine` | 4 | AT-34-E4-002 (cycle 5) | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_5.md` — widens the trait/drawback capability to a third slice: the **4 of 28** remaining `trait_content` records whose `BONUS:SKILL|%LIST` names an open `TYPE=Craft/Perform/Profession` subtype family (`trait_artisan`, `trait_mentored`, `trait_simple_disciple`, `trait_talented`), via `FAMILY_CHOICE_TRAIT_BONUSES` + `family_choice_skill_options` resolving the crate's own pre-existing closed Craft/Perform/Profession rosters (`skill_allocation::skill_family_member_ids`, made `pub(crate)`) — not a new mechanism, and **corrects cycle 4's own doc comment** (retro-logged) that this shape needed a "materially different" open-text UI and was out of scope. Landed **end to end with zero frontend changes**: `CreateCharacterForm.tsx`'s picker already renders any trait option generically off `skillOptions.length > 0`/`choiceSetId`, so the larger family-resolved option list reaches the UI for free; `trait_picker.rs` gets a third `.chain()` in the same DTO shape. `ultimate_campaign`: `DONE 187->191, M 53->49`; `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff in another book this time (checked). `DONE=191 of 265` — bar not met. Also repoints a `v06_work_inventory.rs` negative-control fixture off `Trait ~ Artisan` (now covered) onto `Trait ~ Bruising Intellect` (retro-logged, caught by the test suite immediately) and re-derives `completion_atlas.py`'s `V`-bucket citation pin (`citation_failures` 1->0, this cycle's own line-count shift, no bucket population moved). Remainder named by sub-cause, unchanged from cycle 4 except the 4 now closed: 3 ability-formula traits, 15 mixed-bonus-type traits, 1 corpus data gap, 17 narrative + 1 cross-skill-guarded Drawback, 12 `Retrain` records = 49+21+2+2=74 non-DONE. `cargo test --locked --no-run` exits 0 at `5e3c000c8e`; desktop crate 528/28 pass/fail, identical pre-existing failure set to cycles 3/4 (+2 vs. cycle 4's 526, exactly this cycle's own new tests). Confirmed by the wave-18 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fifth section): this cycle's own claimed 4 reproduced exactly, all `ultimate_campaign` — but wave 17 (this cycle's own wave) was killed by the host before its closing regeneration ever ran, so these 4 units sat unmeasured for a full day and were only actually verified by wave 18's own closing cycle, not this one. |
| 32 | `trait-flat-save-slice` | 4 | AT-34-E4-002 (cycle 6) | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_6.md` — widens the trait/drawback capability to a fourth slice, a genuinely new pillar (saving throws, not skills): `ultimate_campaign`'s **2 of 21** remaining `trait_content` records whose corpus token is a single flat, named-save `BONUS:SAVE` bonus (`trait_life_of_toil`/Fortitude, `trait_indomitable_faith`/Will), via new `trait_effects::SAVE_TRAIT_BONUSES` + `save_bonuses_from_traits`, folded into the real `pilot_compute::compute_total_saves` consumer alongside `feat_save_bonuses`/`alternate_trait_saves` — reusing the identical shape `feat_effects::save_bonuses_from_feats` already grounds for Great Fortitude/Iron Will/Lightning Reflexes, not a new pathway. Landed **end to end**: `CharacterTraitOptionDto` gains a real `save: Option<String>` field (desktop `trait_picker.rs`, `loadCharacterTraits.ts`, `CreateCharacterForm.tsx`'s trait-row display), the compute path already reaches `CharacterSheet.tsx` through the pre-existing `total_saves` pipeline. `ultimate_campaign`: functional `DONE 191->193, M 49->47` (fixture-verified, not yet baked into the committed, regeneration-owned `docs/work-inventory.json`, which still reads `DONE=187 M=53` — honestly reported unchanged this cycle); `U:21 D:2 X:2 V:0` untouched. Corpus-wide, kind-keyed payoff: **3 units**, not 2 — `Trait ~ Indomitable Faith` shares its corpus `KEY` with `advanced_players_guide`. `DONE=187 of 265` (committed) / `193` (functional) — bar not met. Instrument-correction: `completion_atlas.py`'s bucket-V citation line pin re-derived after this cycle's own insertions shifted it (`citation_failures` 1->0, no bucket population moved); regenerated `completion-atlas.json` timestamp `git restore`d before commit. Remainder named by sub-cause, unchanged from cycle 5 except the 2 now closed: 3 ability-formula traits, 13 mixed-bonus-type traits split by real corpus shape (1 `COMBAT`-only, 1 `COMBAT`+`CONCENTRATION`, 1 `CONCENTRATION`-only, 3 `VAR`-only, 1 `CASTERLEVEL`-only, 3 `SITUATION`-only, 2 `ABILITYPOOL`-only, 1 corpus data gap), 17 narrative + 1 cross-skill-guarded Drawback, 12 `Retrain` records = 47+21+2+2=72 non-DONE. `cargo test --locked --no-run` exits 0; desktop crate 531/28 pass/fail, identical pre-existing 28-name failure set to cycles 3/4/5 (+3 vs. cycle 5's 528, exactly this cycle's own new tests). Confirmed by the wave-18 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, fifth section): this cycle's own claimed 3 reproduced exactly (2 `ultimate_campaign` + 1 `advanced_players_guide`, `ingested-magnitude`→`grounded`); committed `ultimate_campaign` now reads `DONE=193 M=47` for real, matching this row's own "functional" figure. |
| 33 | `trait-initiative-concentration-slice` | 4 | AT-34-E4-002 (cycle 7) | partial | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_7.md` — this cycle's worktree started at the stale `tranche/14` cut point and had to be reset onto `origin/tranche/14` before any work; re-derived the split fresh (`DONE=193 M=47`, not the dispatch brief's stale `DONE=151`) and confirmed six prior cycles had already built the trait/drawback capability the brief claimed was un-started. Widens the compute path to two genuinely new pillars at once (initiative checks and concentration checks, neither computed anywhere in this engine before): `ultimate_campaign`'s **2 of 17** remaining `trait_content` records whose corpus token includes a flat `BONUS:COMBAT|INITIATIVE` (`trait_tactician`, `trait_arcane_temper`) and 2 whose token includes a flat `BONUS:CONCENTRATION|ALLSPELLS` (`trait_arcane_temper` again — its record carries both — and `trait_desperate_resolve`), via new `trait_effects::{INITIATIVE_TRAIT_BONUSES,CONCENTRATION_TRAIT_BONUSES}` + a new `pilot_compute::ground_orphan_trait_facts`, the same standalone-fact idiom `ground_orphan_feat_facts` already established for Improved Initiative/Endurance/Fleet (this engine computes no integrated initiative or concentration-check total to layer onto). **3 distinct records close** (Arcane Temper counted once); its own grounding check requires BOTH pillars to fixture-execute correctly before reporting it grounded — the first record in this module with two independently-pillared `BONUS` tokens. `ultimate_campaign`: functional `DONE 193->196, M 47->44` (local regen, not committed); `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff in another book this time (checked). `DONE=196 of 265` (functional) — bar not met. Retro-logged correction: cycle 6's own remainder census silently omitted `trait_precise_treatment` (a 17th `trait_content` M-record, mixing a flat and a formula `SKILL` token) — ability-formula count corrected 3→4. Instrument-correction: `completion_atlas.py`'s bucket-V citation pin re-derived (`12914→12924`, `citation_failures` 1→0, no bucket population moved). `cargo test --locked --lib -- trait_effects` 40/40; `cargo test --locked --bin v06_work_inventory` 475/475; `cargo test --locked --no-run` exits 0 at `5e13b79821` (re-verified after this cycle's own rebase onto a concurrent, different-territory `AT-34-E3-002` commit); desktop crate not run this cycle (no file under `apps/desktop/` touched, honestly reported skipped). Remainder named by sub-cause, unchanged from cycle 6 except the 3 now closed: 4 ability-formula traits (corrected count), 10 further mixed `VAR`/`SITUATION`/`ABILITYPOOL`/`CASTERLEVEL` records (each a separate future pillar), 1 corpus data gap, 30 `ability_content` records (Drawback/Retrain, out of scope) = 44+21+2+2=69 non-DONE. Confirmed by the wave-19 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, sixth section): this cycle's own claimed 3 reproduced exactly, all `ultimate_campaign`, `ingested-magnitude`→`grounded`; committed `ultimate_campaign` now reads `DONE=196 M=44` for real, matching this row's own "functional" figure. |
| 34 | `trait-ability-diff-formula-slice` | 4 | AT-34-E4-002 (cycle 8) | in-progress | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_8.md` — dispatch brief matched the real HEAD split exactly this time (`DONE=196 M=44`), wave 19's shared regen having already folded cycle 7 in. Closes cycle 7's own next-cycle-plan sub-cause entirely: **4 of 14** remaining `trait_content` records whose `BONUS:SKILL` magnitude is an ability-score-difference `max(A,B)-B` formula (`trait_bruising_intellect`, `trait_planar_savant`, `trait_pragmatic_activator`, `trait_precise_treatment`), via new `trait_effects::ABILITY_DIFF_SKILL_TRAIT_BONUSES` + `ability_diff_skill_bonuses_from_traits`, evaluated by the crate's own real `formula_interpreter::PcgenFormulaEvaluator`, folded into the SAME `skill_allocation::allocate_skill_ranks` consumer the first three slices already established. **Corrects cycle 7's own "no formula evaluator exists in this crate" finding** (retro-logged): `PcgenFormulaEvaluator` already exists and is already wired into 5 other consumers crate-wide, never reached from `trait_effects.rs`. `Trait ~ Precise Treatment` carries two `BONUS:SKILL|Heal` tokens on the SAME skill (flat `+1` + formula); both summed and genuinely applied, proven by the fixture-executed expected value (`5`). `ultimate_campaign`: functional `DONE 196->200, M 44->40` (local regen, not committed); `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff elsewhere (checked both `trait_generic/` and the duplicate `ability/` directory — the latter is a different `Kind`, already `text-complete`). `DONE=200 of 265` (functional) — bar not met. Instrument-correction: `completion_atlas.py`'s bucket-V citation pin re-derived twice (`13004→13017` for this cycle's own insertions, then `13017→13116` resolving the one real rebase conflict against `AT-34-E3-002` cycle 7 wave-21's concurrent insertions). `cargo test --locked --lib -- trait_effects skill_allocation` 63/63; `cargo test --locked --bin v06_work_inventory` 488/488 post-rebase; `cargo test --locked --no-run` exits 0 post-rebase; desktop crate not run this cycle (no file under `apps/desktop/` touched). Out-of-territory finding, not fixed: workspace integration suite's `the_committed_inventory_is_well_formed...` fails on a `core_rulebook` barbarian class-feature deferral, unrelated to this cycle's trait/ability territory. Remainder named by sub-cause, unchanged from cycle 7 except the 4 now closed: 3 `VAR`-only, 3 `SITUATION`-only, 2 `ABILITYPOOL`-only, 1 mixed `CASTERLEVEL`+`SKILL`, 1 corpus data gap = 10 `trait_content`, 30 `ability_content` records (out of scope) = 40+21+2+2=65 non-DONE. Confirmed by the wave-20 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, seventh section): this cycle's own 4 reproduced exactly, `ultimate_campaign` DONE 196→200, M 44→40 — `DONE=200 of 265` now regen-verified, not just locally measured. |
| 35 | `trait-situational-skill-slice` | 4 | AT-34-E4-002 (cycle 9) | in-progress | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_9.md` — dispatch brief matched the real HEAD split exactly (`DONE=200 M=40`), wave 20's shared regen having already folded cycle 8 in. Closes the `SITUATION`-only sub-cause entirely: **3 of 10** remaining `trait_content` records whose corpus `BONUS` token set includes a `BONUS:SITUATION` clause (`trait_almost_human`, `trait_self_taught_scholar`, `trait_trustworthy`), via new `trait_effects::SITUATIONAL_SKILL_TRAIT_BONUSES` + `situational_skill_facts_from_traits` (standalone-fact producer, the SAME `pilot_compute::ground_orphan_trait_facts` idiom initiative/concentration established) + `situational_flat_skill_bonuses_from_traits` (Trustworthy's separate flat Diplomacy token, folded into the SAME `skill_allocation::allocate_skill_ranks` consumer the first three slices established). `Trait ~ Trustworthy` carries two independently-pillared tokens on different skills and is only grounded once BOTH fixture-execute. **Also wired the 3 new traits into the desktop picker** (`list_available_character_traits` gains a fifth chained iterator), and found — but did not fix, out of scope — that the fifth/sixth slices' 7 traits are still NOT selectable through the picker at all for a new character (retro-logged `incident`). `ultimate_campaign`: functional `DONE 200->203, M 40->37` (classify()-level bin tests, NOT a completed local whole-corpus regen — the regen pipeline was still running after 6+ minutes and was killed to free the shared cargo slot the required desktop verification also needed); `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff elsewhere (checked). `DONE=203 of 265` (functional) — bar not met. Instrument-correction: `completion_atlas.py`'s ten citation pins re-derived (a concurrently-landed `AT-34-E3-002` cycle 8 commit had already shifted them, uncorrected by that lane, plus this cycle's own shift); `citation_failures` 10→0. `cargo test --locked --lib -- trait_effects skill_allocation` 75/75; `cargo test --locked --bin v06_work_inventory` 492/492; `cargo test --locked --no-run` exits 0 at `ab5008c2d8`; desktop crate tested explicitly (separate workspace): `trait_picker` 33/34 passed — 34/34 own tests, the 1 failure the identical pre-existing `race_trait_picker` adopted-race-count failure every prior cycle already attributed. One retro `correction`: this cycle's own first desktop test checked the wrong field for Almost Human's situational circumstance text, caught immediately, fixed. Remainder named by sub-cause, unchanged from cycle 8 except the 3 now closed: 3 `VAR`-only (now the largest group), 2 `ABILITYPOOL`-only, 1 mixed `CASTERLEVEL`+`SKILL`, 1 corpus data gap = 7 `trait_content`, 30 `ability_content` records (out of scope) = 37+21+2+2=62 non-DONE. Confirmed by the wave-21 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, ninth section): cycle 9's own 3 (`Trait ~ Almost Human`, `Trait ~ Self-Taught Scholar`, `Trait ~ Trustworthy`, all `ingested-magnitude`→`grounded`) reproduced exactly, `ultimate_campaign` DONE 200→203, M 40→37 — `DONE=203 of 265` now regen-verified. |
| 36 | `trait-picker-gap-fix-and-caster-level-skill-slice` | 4 | AT-34-E4-002 (cycle 10) | in-progress | `artifacts/epic-4-ultimate-campaign/AT-34-E4-002_cycle_receipt_10.md` — worktree opened stale (`ea2b3396f2`); `git reset --hard origin/tranche/14` onto wave 22 (`15485e5197`) before any work. Re-derived split fresh: `DONE=203 M=37`, matching the dispatch brief's own stated baseline exactly (wave 21's shared regen had already folded cycle 9 in). **Fixed the picker gap FIRST, as the dispatch brief required**: `list_available_character_traits` chained only 5 of 7 `trait_effects` tables (cycle 9's own seventh slice was already in); this cycle chains the remaining 2 (`INITIATIVE_TRAIT_BONUSES`/`CONCENTRATION_TRAIT_BONUSES` merged by `trait_id` into a new `otherPillars` DTO field, `ABILITY_DIFF_SKILL_TRAIT_BONUSES` into a new `abilitySubstitution` field), making Tactician, Arcane Temper, Desperate Resolve, Bruising Intellect, Planar Savant, Pragmatic Activator, and Precise Treatment genuinely selectable for the first time — **retracting cycles 7/8's own false claim** ("the picker already surfaces every selected trait generically," true only for a trait selected some other way). Retro-logged `resolution` against cycle 9's own `incident`. Moves **0** DONE-bucket units (all 7 were already `grounded` at the classify() level since cycle 8) — a doctrine fix, not a closure, reported separately. **Then took the next slice**: re-checked cycle 9's own "needs a per-subschool caster-level pillar this crate does not have" characterization of the remaining 7 `trait_content` records and found it FALSE for 1 of them — `trait_eldritch_delver`'s caster-level-by-subschool bonus is the same shape `feat_effects::spell_focus_facts_from_choices` already grounds for Spell Focus's per-school DC bonus (retro-logged `correction`). New `trait_effects::CASTER_LEVEL_SKILL_TRAIT_BONUSES` (1 record, kept OUT of `FLAT_SKILL_TRAIT_BONUSES` so `classify()`'s `.or_else` chain cannot short-circuit on the skill half alone) + `caster_level_subschool_facts_from_traits` (standalone fact, the SAME `pilot_compute::ground_orphan_trait_facts` idiom) + `caster_level_skill_bonuses_from_traits` (flat two-skill token, folded into the SAME `skill_allocation::allocate_skill_ranks` consumer). Also chained this ninth table into the desktop picker so the cycle's own new record does not repeat the gap it just fixed. `ultimate_campaign`: functional `DONE 203->204, M 37->36` (classify()-level bin test, not yet in the committed inventory per this dispatch's file-ownership rule); `U:21 D:2 X:2 V:0` untouched; no shared-corpus-`KEY` payoff elsewhere (checked — `Trait ~ Eldritch Delver` appears only under `ultimate_campaign/trait_generic/`). `DONE=204 of 265` (functional) — bar not met. `cargo test --locked --lib -- trait_effects skill_allocation` 84/84 (9 new); `cargo test --locked --bin v06_work_inventory` 502/502 (1 new); `cargo test --locked --no-run` exits 0 at `7714a6a5ef`; desktop crate tested explicitly (separate workspace, own `--no-run` also exit 0): `trait_picker` own 23/23 tests pass (18 new/updated), `race_trait_picker` 19/20 (the 1 failure the identical pre-existing adopted-race-count failure every prior cycle already attributed); TypeScript typecheck clean; frontend suite 96/100 files (4 pre-existing failures, none touching this cycle's 3 changed files). Remainder named by sub-cause, corrected from cycle 9's lumped characterization: 3 `VAR`-only (genuinely THREE different problems — a cross-cutting luck-bonus modifier this crate has no concept of, an eidolon mechanic this crate does not model at all, and 6 missing channel-energy DC totals), 2 `ABILITYPOOL`-only (need a new heterogeneous-choice mechanism), 1 corpus data gap = 6 `trait_content`, 30 `ability_content` records (out of scope) = 36+21+2+2=61 non-DONE. Confirmed by the wave-22 shared regen (`AT-34-E3-001_wave9_regen_receipt.md`, tenth section): cycle 10's own claimed 1 (`Trait ~ Eldritch Delver`, `ingested-magnitude`→`grounded`) reproduced exactly, `ultimate_campaign` DONE 203→204, M 37→36 — `DONE=204 of 265` now regen-verified. |
| 37 | `mine-bucket-d` | 3 | wave 32, lane C (no AT-34-E# card yet) | partial | `artifacts/bucket-d-mining/wave32_laneC_reconnaissance_cycle_receipt.md` — re-derived bucket D fresh (`population=49438`, `D: 2955`, matching the brief exactly); independently hit and fixed the same `AT-34-E1-002` condition-6 citation-gate defect lane B was dispatched at (`65c891e277`, confirmed byte-identical by lane B's own independent audit `0eaba444bc`) — instrument-correction, not a closure. Decomposed D's full population into 6 named mechanisms summing exactly to 2,955 (1,727 Epic-2 simple-kind-table zero-magnitude fallthrough / 931 unmodelled-class chassis gaps, NOT zero-magnitude / 179 magnitude-bearing class_feature slug mismatches / 53 race-trait "Shape 8" cross-book-ownership blocker / 38 class-level snapshot-delta gaps / 27 "held by table, description: null" rungs). Tested the brief's "text-only, zero-magnitude, description-shown = COMPLETE" ruling against the one genuinely zero-magnitude shape (1,727 units): already the live gate on `simple_kind_verdict`; of 427 units (25% of 1,727) carrying a real corpus `DESC:`, only 3 pass `wiring_class == "display"`, and all 3 are correctly excluded by the shipped `universal_sheet_modifier` test — **zero closable units found**. Ran a full guarded `docs/work-inventory.json` regeneration to test (and falsify) a stale-cache hypothesis for 401 `deity`-kind units stuck at `wiring_class: computed` despite carrying zero `MAGNITUDE_TOKENS` fields — `git diff --stat` came back byte-identical (only `generated_at` moved), so the committed inventory is confirmed fresh, saving the next lane the same regen. Movement: closure 0, reclassification 0, reachability 0, instrument-correction 10 citation pins + 1 falsified hypothesis. No card ID exists yet for generic "mine bucket D" work; next-cycle plan in the receipt is cheapest-first over the 6 named mechanisms, starting with the 27-unit "held by table" rungs (smallest, already fully diagnosed, needs an operator ruling on disposition for description-less-but-held records). **Wave 35, lane C** (reconnaissance only, `artifacts/bucket-d-mining/wave35_laneC_reconnaissance_cycle_receipt.md`): mined the two largest untouched shapes from wave 32's own table, re-confirmed both populations unchanged (`931`, `179`); corrected a stale "75 classes" figure to `70` (retro correction, `docs/retro/events/sd34-wave35-lanec.jsonl`); split the 931 into 5 sub-mechanisms — 19 units where a real chassis already exists but the classifier's own matcher discards the match (`psychic_warrior` 18 via an underscore-vs-space key mismatch in `modelled_class_books()`, `rogue` 1 via the final fallback never re-checking `class_books` membership; both traced to source, not live-verified this cycle), 63 units mismatched onto unrelated same-named creature-type corpus records (Animal/Undead/Dragon/Construct/Plant/Ooze), 16 Eidolon companion-progression records, 1 feat-chain miscategorization, and 832 genuine new-chassis units across 60 classes (full per-class table in the receipt, largest-yield-first). Split the 179 by 36 owning classes and flagged a 25-unit zero-magnitude sub-population inside it for the same `DESC:`/`universal_sheet_modifier` check wave 32 already proved out on the 1,727-shape. Movement: closure 0, reclassification 0, reachability 0, instrument-correction 1 (the class-count figure). Still `partial` — no code/corpus touched this cycle by design. **Wave 36, lane A** (`artifacts/bucket-d-mining/wave36_laneA_sub_mechanism_1_matcher_fix_cycle_receipt.md`): closed wave 35 lane C's own named Sub-mechanism 1 (19 units, `psychic_warrior` 18 + `rogue` 1) — fixed both traced matcher bugs (space-joined `bare_name` in `modelled_class_books()`'s untabled-registry loop; added a `facts.class_books` membership check to the final `corpus_class_names`-only owner-resolution fallback), RED→GREEN (4 new tests, 3 proof + 1 negative control), guarded regen confirmed `population=49438` unchanged, whole-inventory id-diff 0 added/0 removed/**20 changed** (19 named + 1 found outside the shape — `ultimate_psionics:class:psychic_warrior`, a `Kind::Class`-level record the same underscore bug also mislabeled, confirming the dispatch brief's own flagged concern). **Real movement: 4 units close to DONE** (`psychic_warrior_martial_power`/`_psionic_proficiency`/`_secondary_path`/`_twisting_path`, via the already-shipped `class_feature_pool_catalog` text-complete rung, unblocked once the owner resolves correctly), **16 reclassified** to a more precise non-DONE bucket (B or C, naming the true remaining per-feature-magnitude gap instead of a false "class not modelled" claim), 0 pure instrument-corrections. Found and named (not fixed — no live unit affected, checked two ways) a dormant twin of the same bug shape in `PuClassId`'s own 4 multi-word class names. `D: 2891→2873` (−18), `DONE +4`, `B +2` net, `C +12`. `cargo test --locked --no-run` (full workspace) exit 0; `completion_atlas.py --check` `citation_failures=0` (4 lines re-derived after this cycle's own `+21`-line shift). Movement this row's own cumulative total across all three cycles: closure 4, reclassification 16, reachability 0, instrument-correction 11 (10 wave-32 citation pins + 1 wave-32 falsified hypothesis + 1 wave-35 class-count correction — wave 36 added 0 pure instrument-corrections). Still `partial` — Sub-mechanisms 2-5 (80 + 832 units) and this cycle's own 16-unit reclassified remainder are all real, named, un-closed work. **Wave 36, lane B** (`artifacts/bucket-d-mining/wave36_laneB_shape2_zero_magnitude_subsplit_cycle_receipt.md`): ran wave 35 lane C's own named next-cycle check — the `DESC:`/`universal_sheet_modifier` cross-reference against Shape 2's 17 `display`-wiring-class, zero-magnitude units. **Zero of 17 promotable**, the same outcome wave 32 found on the 1,727-unit shape: every one fails `has_real_description` under all three sources the gate checks (raw `DESC:` token, `.COPY`-inherited description, real `ASPECT:` tooltip) — 7 are "Class Skills" header records with no prose to promote, 10 are internal-chassis records (`VISIBLE:NO`/`ABILITY:` pointers) whose real text lives on a sibling corpus record the gate does not follow. Re-derived `population=49438`, `D: 2891` (down from `2955`, a real shift from wave 33's own closures elsewhere in D — Shape 2 itself unchanged at 179). Movement: closure 0, reclassification 0, reachability 0, instrument-correction 1 (resolves wave 35's flagged-but-unverified item to a definitive negative result). Also caught and corrected a base-branch hazard: `origin/tranche/14` was 3 commits stale relative to the local `tranche/14` tip (unpushed) — flagged in `progress.md` for the next lane. Still `partial` — Shape 2's remaining 154 magnitude-bearing units and the 931-shape's 19-unit sub-mechanism 1 remain open, named in the receipt's next-cycle plan. **Wave 36, lane C** (`artifacts/bucket-d-mining/wave36_laneC_creature_type_collision_disposition_cycle_receipt.md`): dispositioned all 80 units of wave 35's own sub-mechanisms 2–4 — none left as "the rest". 1 real matcher bug found and FIXED: `"Order of the Dragon"`'s 5 units (real owner Cavalier) were short-circuited by a corpus-wide collision with the bestiary's own unmodelled `"Dragon"` pseudo-class BEFORE the text-only pool-catalog promotion every non-colliding sibling order already reaches `text-complete` through; guarded the short-circuit (RED→GREEN + negative control, `521/521` bin tests, `--no-run` exit 0). 75 units are case (b) genuinely-unbuilt mechanisms (several confirmed against the engine's own doc comments — Power Over Undead's channel DC, Eidolon's evolution-slot table — named precisely, not built). 5 units (Undead Savant Subschool ×2, PaDFE ×3) are case (c) real misattributions traced to source but not safely fixable this cycle (unreliable corpus signal / zero achievable bucket movement). Guarded regen confirms the ONE matcher fix closes **215 units** to DONE (`D: 2891→2676`), **13 in this cycle's own 80-unit scope, 202 outside it** (sub-mechanism 5's own 60-class population's zero-magnitude sub-features, magnitude-bearing siblings spot-checked and confirmed still correctly D) — honestly reported as an unavoidable emergent effect of the fix, not chosen scope. Retro correction: wave 35's own "Animal Companion plausibly already exists" hypothesis refuted (Hunter's Bond/Nature's Bond's own real granting records are ALSO D). Movement: closure 215, reclassification 0, reachability 0, instrument-correction 1 + 4 citation pins. Worktree self-heal: started 30 commits behind local `tranche/14`'s real tip, rebased cleanly before any commit landed. **Wave 37, lane A** (`artifacts/bucket-d-mining/wave37_laneA_domain_granted_power_death_s_kiss_cycle_receipt.md`): closed wave 36 lane C's own named item 5 (domain-vs-class_feature dual representation) — re-derived the real 7-unit population from the live tree (Dragonbreath's own class_feature records had already closed as an emergent side effect of wave 36's own fix). Found and reused the existing `domain_power::DOMAIN_POWER_CATALOG` precedent (live since SD-31 wave 25/26), extending it with Undead Subdomain's Death's Kiss via a new `grounds_self_application: bool` guard (its own corpus formula is an effect DURATION in rounds, not a flat bonus — the guard stops the shared "a +{magnitude} bonus" sentence from misrepresenting it, while its real uses-per-day count is still honestly grounded). Added a matching subdomain-sibling classify() check, matched by the catalog's own `(domain_display_name, granted_power_name)` pair (a bare feature-name match would wrongly credit two real unrelated collisions, proven by a negative-control test). **2 units close to DONE** (`domain_power_death_s_kiss`, `undead_subdomain_death_s_kiss`). **5 of 7 honestly NOT closed**, each named precisely: Animate Servant ×3 needs a per-spec uses-per-day-formula catalog widening (its own formula genuinely differs from the shared `3+WIS`); Dragon Subdomain's header + Venomous Stare are correctly excluded by the catalog's own pre-existing enemy-facing/multi-DESC-token design boundary. Also fixed a real prose-citation bug in the Inquisitor diagnostic (a multi-word domain name would have cited a non-existent corpus token). `population=49438`, `engine-does-not-hold: 19067→19065`, `grounded: 4341→4343`, 4 citation pins re-derived (this cycle's own insertion shifted them). RED→GREEN: 19/19 `domain_power.rs` (2 new), 56/56 domain-related `pilot_compute` (2 new), 1029/1029 full `pilot_compute` module, 527/527 `v06_work_inventory` bin (2 new), `--no-run` exit 0 full workspace. Movement: closure 2, reclassification 0, reachability 0, instrument-correction 4 citation pins + 1 prose-citation fix. Next-cycle plan: widen `DomainPowerSpec` with a per-spec uses-per-day formula to close Animate Servant's 3 units; domain-kind (`Kind::Domain`) header grounding is a separate, larger question. | B** (`artifacts/bucket-d-mining/wave37_laneB_sentinel_correction_and_submech5_rederive_cycle_receipt.md`): two small independent items from wave 36 lane C's own next-cycle plan, both honestly dispositioned with 0 closures. Item 1 (Sentinel, 1 unit) — investigated the requested ingest-time `Kind::feat` re-tag before touching anything and found it factually wrong: `Sentinel Style Feat ~ Improved Sense Intruder` is a genuine `Kind::ClassFeature` (Ranger's own 10th-level combat-style-feat-chain slot via the Sentinel archetype), structurally identical to ~180 correctly-classified `RangerBonusFeat` siblings including wave 35's own cited analogy (`ranger_combat_style_feat/vital_strike.json`, itself filed `Kind::ClassFeature`). The real D-bucket cause is a short-word matcher collision with the unmodelled `inner_sea_gods:class:sentinel` prestige class — same shape as Order of the Dragon, not a `Kind` bug — but even a correct owner-attribution fix would not close it (`magnitude_token_count: 1`, not zero; all 151 real Ranger-combat-style-feat siblings are still `engine-does-not-hold`, mechanism genuinely unbuilt, Epic 4/5 scope). No corpus edit made; retro-logged a correction against wave 35/36 lane C's carried-forward wrong claim (`docs/retro/events/sd34-wave37-laneb.jsonl`). Item 2 (read-only) — re-ran wave 35 lane C's own `Counter` method against the current committed inventory: sub-mechanism 5's stale `832`-unit/`60`-class figure is now `634`/`60` (same 60 classes, `198` units closed by wave 36's matcher fixes), full per-class table in the receipt, `divine_scion` 45 and `phrenic_slayer` 43 still highest-yield. Flagged a small unreconciled 4-unit gap against wave 36 lane C's own claimed 202-unit figure (this cycle independently measures 198) — does not affect the corrected 634 remaining population. Movement: closure 0, reclassification 0, reachability 0, instrument-correction 1. Worktree self-heal: started at the SD-33 PR #377 merge commit, far behind local `tranche/14`'s real tip, rebased cleanly before any analysis. |

## Gating

```
Epic 1 — Completion Atlas (rows 1-8)          THE DELIVERABLE; gates everything
   |
   +--> Epic 2 — Build 8 of 9 tables (rows 9-12)
            |
            +--> Epic 3 — Core Rulebook to zero (rows 13-18)   deep book, every bucket
            |
            +--> Epic 4 — Ultimate Campaign to zero (rows 19-21)  shallow book, one bucket
                     |
            (3 and 4 both) --> Epic 5 — Price 35 books (rows 22-25)
                                   |
                                   +--> Epic 6 — Closure (rows 26-27)
```

**Epics 3 and 4 are the only pair that could run concurrently** — different books, disjoint corpus subtrees, both gated only on Epic 2. Whether they do is decided after Epic 2 closes by `workflow-instruction.md §4`'s **disjointness check** (the `git diff --name-only` block there): both touch `src/rules_core/` and `src/bin/`, so unless that check proves **file-level** disjointness, they run **sequentially, Core Rulebook first**. If they do run in parallel, each agent gets `isolation: 'worktree'`.

**Everything else is strictly sequential.** Each epic's output is the next one's input: the atlas names the tables, the tables unblock the books, the books measure the rates, the rates price the plan.

**Row 18 (`atlas-defects-recorded`) is the operator's "three more things" guard.** An empty defects file is an excellent result. An absent one is a failure.
