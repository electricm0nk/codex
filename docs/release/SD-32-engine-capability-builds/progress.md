# SD-32 Progress Log

## Split from SD-30 (2026-08-14)

Package created by operator ruling 2026-08-14 ("split phase 3 and phase 4 into their own SD's; SD-31
and SD-32"). Epic 12 (race chassis) and Epic 13 (verdict-path capability) moved from
`SD-30-class-feature-archetype-bundle/` (renumbered 1 and 2 here), plus a scoped copy of Epic 14
(cloud fan-out) as Epic 3. Both moved cards were `READY` (unclaimed, no gate) at split time — verified
against SD-30's `kanban.md` immediately before the move.

**Number-reuse note recorded here too (not just README.md):** this package is not
`SD-32-instrument-coverage-and-consumer-wiring`, which was deleted from the repo tree earlier the same
day (`SD-30-.../decisions.md §50`).

SD-30's own per-cycle receipts for this scope, recorded before the split, remain in
`SD-30-class-feature-archetype-bundle/progress.md` as history — not copied here. Cycles claiming a card
in this package's `kanban.md` from this point forward append their receipts below.

No cycles claimed yet.
