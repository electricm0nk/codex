# SD-32 Artifacts

Per-cycle receipts and finding logs land here as cycles run, including Epic 2-F1's committed
hand-labelled sample (the ground-truth labels the classifier is judged against — this is the single
most load-bearing artifact this package produces; treat it as canonical once committed, do not
regenerate it to fit a classifier's output).

## Shared instrument tooling

This package does not duplicate the corpus-wide instrument scripts — they remain canonical under
`../SD-30-class-feature-archetype-bundle/artifacts/`. See that directory's `README.md` for each
script's current status.
