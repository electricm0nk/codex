# SD-32 Forward-Scope Register

Successor work depending on this package's output.

| ID | Item | Owner |
|---|---|---|
| H1.1 | Once Epic 1 lands the race chassis, `SD-31-corpus-closure-grind`'s Epic 4-F3/F4 must re-derive its ingest ceiling and run the newly-unblocked cycles. | `SD-31-corpus-closure-grind` |
| H1.2 | Once Epic 2 lands or closes-at-gate, `SD-31-corpus-closure-grind`'s Epic 1-F4/Epic 3-F3 must re-run `class_feature`'s `unknown`-bucket disposition against the new verdict paths. | `SD-31-corpus-closure-grind` |
| H1.3 | Any race the Epic 1 design decision (F1) finds cannot be chassis-built within this epic's bound (e.g., a genuinely bespoke racial subsystem) is a named finding, not silently dropped. | Unassigned pending Epic 1-F1's actual finding — do not pre-assign a successor for a population not yet named |

No item above is left without a stated home (H1.3's "unassigned pending finding" is explicit, not a
tidiness gap — the population it would cover does not exist yet as a named finding).
