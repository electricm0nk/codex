# Cycle barbarian-residue-audit — Epic 7 (Deferred Per-Class Work & Coverage Backlog) / Criterion 7.5

- **Card ID:** t_e979d996
- **Commit SHA:** `60a0b98` (code + test + this receipt); `d77b2f7` (progress.md status-matrix + register A6 update, follow-up commit per `§5`'s protocol)
- **Files touched:**
  - `src/rules_core/level_up/barbarian.rs` (doc-comment-only addition documenting the verified negative finding; no functional filter change — see Notes)
  - NEW `tests/sd25_barbarian_level_up_explanation_filter_audit.rs` (the audit test)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/barbarian-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + cycle log update, in a follow-up commit per `§5`'s protocol)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (documented exception) — scoped to this cycle's own staged diff (`git diff --unified=0 --cached` against the canonical `loop-instruction.md §6` audited glob list: `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`, excluding `**/__tests__/**` and `**/*.test.*`). One hit: `barbarian.rs`'s own new doc comment cites the real, existing new test file path `tests/sd25_barbarian_level_up_explanation_filter_audit.rs` — a real `tests/`-file citation, not a bundle-tag leak, matching the exact exclusion precedent already established by criterion 1.1 and repeated verbatim by the sibling 7.2 (Cleric) and 7.3 (Rogue) receipts. `tests/*.rs` itself is not in the canonical audited glob at all, but the new test file's own body was checked anyway (informational): also clean (no `sd\d+_`/`Sd\d+`-shaped hits beyond real `tests/sd25_*`/`tests/sd20_*`/`tests/sd13_*` file-path citations, all pointing at files verified to exist).
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own staged diff: zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` in either changed file.
- **Acceptance criterion (verbatim, `progress.md`'s `## DISCOVERED` register A6 sub-row for Barbarian):** "**Barbarian** — `src/rules_core/level_up/barbarian.rs` — filter admits `class_chassis.barbarian.` / `class_feature.barbarian.` only. No `class_spell.barbarian.*` id exists in `pilot_compute.rs` (non-caster) — audit should confirm no live gap exists. suggested criterion: **7.5**." Dispatcher's brief for this cycle: audit `barbarian.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **RED → GREEN evidence:**
  - **Audit performed:** grepped every `barbarian`-containing `id: "..."` literal in `src/rules_core/pilot_compute.rs` (144 mentions total) and confirmed by direct inspection that every explanation id grounding a Barbarian fact is prefixed either `class_chassis.barbarian.` or `class_feature.barbarian.` — no `class_spell.barbarian.*` family and no third prefix family of any kind exists (consistent with Barbarian being a non-caster in PF1 Core Rulebook). Read the two `diagnostics.push(...)` calls too (`class_chassis.barbarian.rage_rounds_per_day.unsupported` at a non-positive-Constitution-modifier edge case, and `class_feature.barbarian.bounded_progression.rage_execution.unsupported`, the standing rage-execution-engine claim-block) and confirmed both are `ComputationDiagnostic`s pushed to the structurally separate `diagnostics` `Vec`, never merged into `explanations` — the same diagnostic-vs-explanation separation criterion 7.2 (Cleric) established as the reason a `.unsupported`-suffixed id is not automatically a live filter gap. Cross-checked against `barbarian.rs`'s own `append_class_feature_grants` filter (`is_barbarian_class_feature_id = id.starts_with("class_chassis.barbarian.") || id.starts_with("class_feature.barbarian.")`): the two admitted prefixes already cover the full live set.
  - **RED test written:** `tests/sd25_barbarian_level_up_explanation_filter_audit.rs`, two tests:
    1. `every_barbarian_explanation_id_from_pilot_compute_is_covered_by_barbarians_own_two_admitted_prefixes` — sweeps `compute_pilot_base_chassis` outputs across all 20 supported Barbarian levels (with every numbered rage-power choice-slot pre-populated, so the rage-power-choice explanation ids are grounded too, not left named-but-unselected) and asserts every `barbarian`-containing explanation id starts with one of the two admitted prefixes.
    2. `every_newly_granted_barbarian_explanation_surfaces_as_a_real_levelup_grant_at_every_transition` — sweeps all 19 level-up transitions (1->2 through 19->20) and asserts every real (non-"correctly absent", non-class-table-covered) explanation that newly becomes granted between `from_level` and `to_level` shows up as a grant in `compute_barbarian_level_up_grants`'s `automatic_features` output — the literal Wizard-bug-shaped check. Rage rounds per day is excluded from this sweep (it is a `ResourcePoolDelta`, not a `Grant`, by `barbarian.rs`'s own design) and instead asserted separately via `plan.resource_pool_change.pools`.
  - **Confirmed RED capability live:** temporarily edited `barbarian.rs`'s filter (after `cp barbarian.rs /tmp/barbarian.rs.bak` to preserve a byte-identical restore point) to `is_barbarian_class_feature_id = to_explanation.id.starts_with("class_chassis.barbarian.")` only (dropping the `class_feature.barbarian.` admission — the exact Wizard-bug shape, applied to Barbarian's second prefix). Re-ran the new test suite: **FAILED** as expected — `cargo test --test sd25_barbarian_level_up_explanation_filter_audit` reported `1 passed; 1 failed`, with `every_newly_granted_barbarian_explanation_surfaces_as_a_real_levelup_grant_at_every_transition` failing at `level 1 -> 2: expected explanation "class_feature.barbarian.uncanny_dodge" ... to surface as a grant ... but it did not`. (Check 1 still passed under the injected bug, correctly — it audits `pilot_compute.rs` directly, independent of `barbarian.rs`'s own filter, exactly mirroring the Rogue precedent.) This proves check 2 is load-bearing, not a rubber-stamp: it would have caught the exact Wizard-shaped bug (Uncanny Dodge, Trap Sense, Improved Uncanny Dodge, Damage Reduction, and Indomitable Will would all have vanished from every plan) had it existed in Barbarian's module.
  - **Reverted the injected bug** (`cp /tmp/barbarian.rs.bak src/rules_core/level_up/barbarian.rs`, confirmed byte-identical via `diff`) and re-ran: **GREEN** — both tests pass (`test result: ok. 2 passed; 0 failed`).
  - **Verified negative finding, not a skipped check:** `barbarian.rs`'s explanation-id filter was already correct — no functional code fix was needed or made. A doc-comment addition documents the audit (mirroring how `wizard.rs`'s own SD-24 fix doc comment, and the sibling 7.2/7.3 receipts' identical convention, record their findings).
- **Test results:**
  - `cargo test --test sd25_barbarian_level_up_explanation_filter_audit`: 2 passed, 0 failed (baseline, post-revert).
  - Full pre-existing Barbarian test surface (regression safety, all 22 Barbarian-named binaries run explicitly by name — `cargo test barbarian`'s substring filter alone under-matched several binaries whose fn names don't literally contain "barbarian", so each was also run directly by `--test` name to confirm real coverage): `sd20_levelup_barbarian` (3), `sd13_barbarian_level1_chassis_baseline` (10), `sd13_barbarian_level2_progression` (13), `sd13_barbarian_level3_progression` (15), `sd13_barbarian_level4_progression` (15), `sd13_barbarian_level5_progression` (14), `sd13_barbarian_level6_progression` (16), `sd13_barbarian_level8_progression` (15), `sd13_barbarian_level9_progression` (16), `sd13_barbarian_level10_progression` (12), `sd13_barbarian_rage_power_slots` (7), `sd18_barbarian_level11_greater_rage` (10), `sd18_barbarian_level12_widening` (11), `sd18_barbarian_level13_widening` (11), `sd18_barbarian_level14_widening` (11), `sd18_barbarian_level15_widening` (11), `sd18_barbarian_level16_widening` (10), `sd18_barbarian_level17_widening` (11), `sd18_barbarian_level18_widening` (11), `sd18_barbarian_level19_widening` (10), `sd18_barbarian_level20_widening` (12), `sd25_barbarian_level_up_explanation_filter_audit` (2, this cycle's own) — all 0 failed, 245 total assertions across all binaries pass.
  - `cargo test --lib rules_core::level_up` (multiclass dispatch unit tests): 3 passed, 0 failed, unaffected (no dispatch code touched).
- **Notes:**
  - This audit's scope per the file-touch grant was `src/rules_core/level_up/barbarian.rs` only (plus its own test file/module) — respected; no sibling class file touched, `pilot_compute.rs` read-only (the same read-only seam every other Epic 7 per-class residue cycle documents reading).
  - Unlike Sorcerer/Bard/Druid/Paladin/Ranger (real spellcasting or partial-casting classes the 7.1 intake flags as likely having a live `class_spell.<class>.*` gap), Barbarian, like Rogue, is a non-caster — this audit independently confirms Barbarian has no such gap, exactly as the intake's own "audit should confirm no live gap exists" instruction required. This is a real, distinct verification, not simply trusting the intake's own prefix-string claim.
  - No `## DISCOVERED` entries added this cycle — the intake's existing register A6 entry already names this exact finding as a to-be-confirmed suggestion; this cycle confirms it rather than discovering something new.
- **Discovery forwards:** none (confirms, does not add to, the existing register A6 `## DISCOVERED` entry).
- **Next-cycle plan:** the next per-class residue cycle should pick up whichever of 7.4 (Sorcerer), 7.6 (Bard), 7.7 (Druid), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger) is still unclaimed — per the register A6 intake and criterion 7.2's own forwarded correction, each of the real/partial spellcasters (Sorcerer/Bard/Druid/Paladin/Ranger) should be independently re-verified rather than assumed to have a live gap (Cleric turned out not to, despite being flagged the same way); Monk, like Rogue and Barbarian, is a non-caster and likely mirrors this cycle's negative-finding shape, but should still be read directly rather than assumed.
