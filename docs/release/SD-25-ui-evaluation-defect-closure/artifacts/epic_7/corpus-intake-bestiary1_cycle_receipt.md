# Cycle 7.N (bestiary1) — Epic 7 (Equipment/spell corpus intake) / Criterion 7.N item 4

- **Card ID:** `t_83e4b64b`
- **Commit SHA:** `9824776` (pushed to `tranche/5-3`; full SHA `982477681e4846c767e3e58ba341553147e0dae6`; this receipt's own card-ID backfill lands in a small follow-up commit, per the established self-reference-backfill pattern used by criteria 6.1/7.O/7.1/7.N-crb-description/7.N-apg-description)
- **Files touched:**
  - NEW `src/rules_core/rules_tables/beastiary1/equipment_tables.rs`
  - NEW `src/rules_core/rules_tables/beastiary1/equipment_data.rs`
  - `src/rules_core/rules_tables/beastiary1/mod.rs` (register the 2 new submodules + a short doc-comment addendum)
  - `tests/sd24_equipment_coverage_audit.rs` (replaced the "Bestiary 1 is not covered by this test file" note with a real coverage test; updated the module-doc comment)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/corpus-intake-bestiary1_cycle_receipt.md` (this file)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped re-check (`git diff BASE_BRANCH...HEAD -- src/rules_core/rules_tables/beastiary1/** tests/sd24_equipment_coverage_audit.rs` where `BASE_BRANCH=$(git merge-base HEAD origin/develop)=7f07d85`) is clean: the only grep hits are the `diff --git a/tests/sd24_equipment_coverage_audit.rs ...` header lines (the pre-existing SD-24-era filename itself, not a new identifier this cycle introduced). The unscoped whole-tranche-diff form of the same grep (as literally written in `loop-instruction.md §6`) surfaces many prior, already-reviewed cycles' hits (criterion 1.1's `SD13_*`→bare-identifier rename, `Sd11*`/`Sd12*` interfaces, `t_b7833349`, etc.) — pre-existing history between `origin/develop` and `tranche/5-3`, not contributed by this cycle.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; this cycle's diff adds zero `STUB`/`MOCK`/`placeholder`/`not yet implemented`/`todo`/`fixme`/`hack` tokens (verified directly against the touched files only). **Register A9 check performed and clean:** none of the 4 new record literals or their doc comments contain any of the four-check tokens — no named-bucket exclusion was needed this cycle (unlike the sibling CRB-description cycle, this book's tiny 4-record corpus happened not to collide). Ran the standing `tests/sd24_wired_integration_audit.rs` directly: it still reports its 2 pre-existing, unrelated failures (`apps/desktop/src-tauri/src/stub_adapter.rs` and `characterHub/{appendToCharacter,reSaveCharacter,recomputeCharacter}.rs`'s `"Would render for system {}; not yet implemented"` pattern) — confirmed out of this cycle's file-touch grant (`src/rules_core/rules_tables/beastiary1/**` + one test file only) and not introduced here; already flagged by the sibling `corpus-intake-crb-description` cycle's `## DISCOVERED` entry, not re-duplicated.
- **Acceptance criterion (verbatim, `cycles/7_N.md` item 4):** "Bestiary 1 equipment + spells — plain scope gap, NOT a corpus ceiling: no `beastiary1` equipment module exists; ~7 real records unclaimed (`b1_equip_general.lst` ×2, `b1_equip_arms_armor.lst` ×3, `b1_equip_magic_items.lst` ×2). Should mechanically reach whatever ceiling the LST supports. `EquipmentTableEntry` already has `weight`/`description` fields (register A12) — populate, don't re-add. Confirm no spell-list concept exists before treating spells as a gap (register A13)."
- **Status:** complete
- **Notes:**

  ## Correction to the cycle doc's estimated record count

  The cycle doc estimates "~7 real records" (general ×2, arms_armor ×3, magic_items ×2). Direct inspection of the real corpus at
  `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary/{b1_equip_general,b1_equip_arms_armor,b1_equip_magic_items}.lst`
  finds **4 real records total**: 1 in `b1_equip_general.lst` (`Poison (Black Smear)`), 2 in `b1_equip_arms_armor.lst` (1 `###BLOCK: Armor` row — `Rag Armor (Dark Creeper)` — and 1 `###BLOCK: Weapons` row — `Aklys`), 1 in `b1_equip_magic_items.lst` (`Heartstone (Night Hag)`). Verified by `wc -l` + a comment/blank/`SOURCELONG:`-excluding grep over each file (7/12/6 lines respectively, mostly headers/comments/blanks). Per the task's own drift-correction instruction ("use the real analogous path/count and document the correction rather than blocking or inventing"), this receipt ships the real 4, not a fabricated 7th record to match the estimate.

  ## Register A8 (shared codegen path decision)

  **Decision: did NOT build the shared `pcgen_import`-backed codegen path this cycle.** Reasoning: Bestiary 1's real equipment corpus is 4 records total — smaller than the time it would take to wire up, validate, and cross-check a one-off generator against `lst_parser::equipment` for a single-digit record count. Hand-transcribed instead, matching the same hand-authored precedent this book's own `monster_subset_01`..`08` modules already established (SD-22 Epic 5). Each of the 4 records was individually checked field-by-field against the raw LST line (`key`/`name` from the raw name + `OUTPUTNAME:`, `cost_gp` from `COST:`, `weight_lbs` from `WT:`, `description` from `SPROP:` or the web second source) rather than run through any transformation script, so there is no transcription-tooling risk at this scale. If Bestiary 1's equipment scope widens later (e.g. more `.MOD` companion records from later splatbooks), that is the point to revisit building the shared path — flagged for whoever picks that up next.

  ## Register A10 (Bestiary-1 `DESC:`/`SPROP:`-equivalent convention check)

  Checked first, before any web fetch, per the register's own instruction. Bestiary 1's equipment corpus has **zero `DESC:` tokens** across all 3 files (direct inspection). **3 of the 4 records do carry a `SPROP:` token** (`Poison (Black Smear)`, `Aklys`, `Heartstone (Night Hag)`) — used as `description` per the same convention `rules_tables::acg::equipment_data` established for ACG ("hit 98.1% via `SPROP:` alone"). None of this book's 3 `SPROP:` values contain a trailing `|<directive>` qualifier, so no stripping was needed (unlike some ACG rows). Only 1 of 4 records (`Rag Armor (Dark Creeper)`) has neither `DESC:` nor `SPROP:` — this is the one true gap this cycle's web pass closed.

  ## Register A11 (`SOURCELONG:`-exclusion, `.COPY=`-merge rule, full-category sweep)

  Applied from the start. Each of the 3 corpus files carries exactly one `SOURCELONG:Bestiary ...` header line, excluded from the 4-record count (same off-by-one-per-file methodology `rules_tables::apg::equipment_data` documents for its own 341→338 correction). Checked directly for `.COPY=`/`.MOD` rows across all 3 files (`grep -n '\.COPY=\|\.MOD'`) — **zero matches in any file**, so the `.COPY=`-merge rule has no records to apply to in this book; a real finding (confirmed, not skipped for time), not a gap. All non-header/non-comment/non-blank rows in all 3 files (the full category sweep: General + ArmsArmor's 2 blocks + MagicItems) were transcribed — no held-back remainder.

  ## Register A13 (spell-list existence check)

  Confirmed before writing any code: no `b1_spells.lst` (or any `*spell*`-named file) exists anywhere under
  `~/workspace/repos/pcgen/data/pathfinder/paizo/roleplaying_game/bestiary/` — unlike CRB (`cr_spells.lst`) and APG (`apg_spells.lst`), which each carry a dedicated player-facing spell-list corpus file, Bestiary 1 has none. `grep -l 'SPELLS:\|SPELLLEVEL:\|CLASSSPELL'` over the directory does surface hits, but only as inline `SPELLS:`/spell-like-ability tokens embedded on individual monster rows in `b1_abilities_race.lst`/`b1_races.lst`/`b1_templates.lst`/`b1_kits_race.lst` (a monster's innate "Spell-Like Abilities" special quality) — the same kind of monster-intrinsic data `beastiary1::MonsterStatBlock`'s own doc comment already scopes out of this book's chassis-data surface (AC/HP/saves/spells are PCGen-computed/derived, not literal chassis tokens the existing monster-resolve surface captures). There is no standalone spell-list table to ingest for this book. "Spells" is correctly N/A for this item — not an unclaimed gap, and nothing was stubbed or left as a fake `None`-returning function to paper over it; there is simply no spell-list concept/module for this book to add.

  ## Web second-source pass (operator directive 2026-07-21)

  **1 record required a web second source: `Rag Armor (Dark Creeper)`** (`b1_equip_arms_armor.lst`, `###BLOCK: Armor`, `COST:0 WT:15 SOURCEPAGE:p.53 PRERACE:1,Dark Creeper`; no `DESC:`/`SPROP:` token). Identity-matched: name ("Rag Armor") + category (armor) + the `PRERACE:1,Dark Creeper` qualifier + Bestiary 1 source page (p.53) all point to the Dark Creeper monster's own "Rag Armor" (Ex) special quality — this is literally the special quality the LST row encodes, not a same-named cross-book/edition cousin (there is no other Pathfinder "Rag Armor" concept). Fetched 2026-07-22, one page per source (no bulk scraping), reasonable pacing:
  - <https://www.d20pfsrd.com/bestiary/monster-listings/humanoids/dark-creeper/>
  - <https://www.aonprd.com/MonsterDisplay.aspx?ItemName=Dark+Creeper>

  Both sources carry identical text for the special quality: "A dark creeper's multiple layers of filthy rags function as leather armor when worn by one of their kind." Used verbatim as `description`. The other 3 records needed no web source (register A10's `SPROP:` convention covered them).

  **This closes the "Bestiary-1" portion of SD-24's carry-forward reasoning the same way the sibling CRB-description/APG-description cycles' successful web passes closed Open Blockers #1/#2** — flagging here so SD-24's record can note that all 3 web-second-source-eligible corpus-intake items (CRB-desc, APG-desc, Bestiary-1) have now landed real, cited progress.

  ## RED → GREEN evidence

  - **RED:** temporarily removed the 2 new `pub mod` lines from `beastiary1/mod.rs` with the new test (`beastiary1_equipment_is_fully_record_ingested_with_full_description_coverage`) already added to `tests/sd24_equipment_coverage_audit.rs`, then ran `cargo test --test sd24_equipment_coverage_audit`. Confirmed failure for the intended reason: `error[E0433]: cannot find equipment_tables in beastiary1` — proves the module genuinely did not exist before this cycle (matching the acceptance criterion's own "plain scope gap... no `beastiary1` equipment module exists" framing), not a false RED.
  - **GREEN:** restored the 2 `pub mod` lines; `cargo test --test sd24_equipment_coverage_audit` → 9/9 passed, including the new `beastiary1_equipment_is_fully_record_ingested_with_full_description_coverage` (`records_expected=4`, `total_records=4`, `has_cost=4`, `has_weight=4`, `has_description=4`).
  - **Full test run:** `cargo test --test sd24_equipment_coverage_audit` → 9/9 passed. `cargo build --lib` → clean, no warnings. `cargo test --no-run` (whole-repo compile check across all test binaries) → exit 0, all green to compile. `cargo test --test sd24_wired_integration_audit` → 3/5 passed, 2 pre-existing unrelated failures (see Wired-integration audit result above) — confirmed by `git diff --stat` that this cycle touches only the 4 files listed above, none of which are `stub_adapter.rs`/`characterHub/*.rs`.

- **Discovery forwards:** none new this cycle (the 2 pre-existing `stub_adapter.rs`/`characterHub` wired-integration findings and the CRB `equipmods.rs` duplicate-key finding were already surfaced by the sibling `corpus-intake-crb-description` cycle's receipt; not re-duplicated here).
- **Next-cycle plan:** this item (Bestiary-1) is now closed. The remaining sibling item in this criterion's 4-item set, `apg-spell-text` (36/297 APG spells lacking a `.MOD` full-text record or `SCHOOL:`/`CLASSES:` token), is a disjoint worktree cycle running in parallel — unaffected by this cycle's changes (no shared files). Once all 4 items land, criterion 7.N as a whole is closure-ready for Epic 7.
