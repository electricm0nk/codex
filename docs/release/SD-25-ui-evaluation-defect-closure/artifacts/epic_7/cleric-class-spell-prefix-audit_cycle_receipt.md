# Cycle 7.2 — Epic 7 (Per-class residue) / Criterion 7.2

- **Card ID:** t_a7aa9573
- **Commit SHA:** `009abfc`
- **Files touched:**
  - NEW `tests/sd25_cleric_level_up_explanation_coverage.rs` (RED → GREEN regression proof + verified-negative-finding documentation)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/cleric-class-spell-prefix-audit_cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + `## DONE`, in a follow-up commit per `§5`'s protocol)
  - **`src/rules_core/level_up/cleric.rs` — NOT touched.** The file-touch grant covers it, but no bug was found there (see Notes/Acceptance below) — per the brief's own instruction, "do not invent a fix for a bug that isn't there."
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff (`git diff --unified=0 --cached` against the audited globs `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`); the new test file lives under `tests/`, outside every audited glob, and no `src/**/*.rs` file was changed. Manually re-checked the test file's own body too (not required by the glob, but as extra hygiene): the only `sd\d+_`/`Sd\d+`-shaped matches are three real `tests/sd24_..._.rs` / `tests/sd13_..._.rs` file-path citations (existing files, verified to exist), the same "real tests/-file citation" exclusion criterion 1.1 already established as non-violating.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` anywhere in the new test file.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` entry, Cleric sub-row):** "`src/rules_core/level_up/cleric.rs` — filter is `CLERIC_EXPLANATION_PREFIX = "class_chassis.cleric."` (plus the single `CLERIC_RECOGNITION_ID` constant) — no `class_spell.cleric.` prefix admitted. `pilot_compute.rs:15841` grounds `class_spell.cleric.prepared_divine.unsupported`, which is silently dropped from `LevelUpPlan` today — the same bug shape as Wizard, applied to Cleric's honest 'unsupported' diagnostic rather than a computed value. suggested criterion: **7.2**." Dispatcher's brief for this cycle: audit `cleric.rs` against Wizard's fixed module as reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **Notes:**
  - **Verified finding: the 7.1 intake's characterization for Cleric is factually incorrect, and there is no live bug.** Read `pilot_compute.rs`'s `explain_cleric_level1_spell_baseline` (the sole function that grounds any Cleric explanation) end-to-end and traced every `id: "..."` it emits (`grep -n 'id: "class_.*cleric' src/rules_core/pilot_compute.rs`, 14 hits total). All 12 hits that are pushed via `explanations.push(...)` carry either the exact recognition id `class_chassis.spell_baseline.cleric` or the `class_chassis.cleric.` prefix — the identical set `cleric.rs`'s current `CLERIC_EXPLANATION_PREFIX`/`CLERIC_RECOGNITION_ID` already admits. The 2 remaining hits (`class_feature.cleric.domain_powers.unsupported`, `class_spell.cleric.prepared_divine.unsupported`) are pushed via `diagnostics.push(...)` — a structurally separate `Vec<ComputationDiagnostic>`, never merged into `explanations` anywhere in `compute_pilot_base_chassis` (confirmed by reading the function's full body: `explanations` and `diagnostics` are threaded as two independent `&mut Vec<_>` parameters throughout every helper call, and the final `PilotBaseChassisComputation { explanations, diagnostics, .. }` construction keeps them as two distinct struct fields). `cleric.rs`'s own `cleric_chassis_explanations` helper reads only `.explanations`. Therefore `class_spell.cleric.prepared_divine.unsupported` is **structurally unreachable** by `append_class_feature_grants` today, regardless of what prefix the filter admits — there is nothing for a wider filter to "catch" here, because the id never enters the `Vec` the filter operates over in the first place.
  - **Why this differs from the real Wizard bug (verified by re-reading `wizard.rs` and its own SD-24 fix commit as the reference pattern, per the dispatch brief):** Wizard's dropped ids (`class_spell.wizard.spellbook_contents`, `.daily_preparation`, `.base_spells_per_day.spell_level_N`, etc.) were real `ComputationExplanation` records — genuinely pushed onto `.explanations` by `ground_wizard_prepared_spellbook` once its posture was met — that `wizard.rs`'s filter (at the time) simply didn't recognize by prefix. That is a real silent-drop: the fact reached `.explanations` and was thrown away by the filter. Cleric's `prepared_divine.unsupported` never reaches `.explanations` at all; it is, and always was, a `ComputationDiagnostic` (the honest "this burden is still unproven" marker), which `cleric.rs` was never wired to read in the first place. Widening `CLERIC_EXPLANATION_PREFIX` to admit `"class_spell.cleric."` would therefore have **zero observable effect today** (there is no explanation carrying that prefix for it to newly admit) — and doing it anyway would be actively wrong per this cycle's own no-fabrication instruction: it would create a latent trap where a future refactor that accidentally started threading diagnostic-shaped ids through the explanation path could get silently turned into a fabricated `Grant` (reporting a burden that is explicitly claim-blocked-as-unproven as though it were a granted mechanical fact). The correct fix for a diagnostic-vs-explanation confusion is a regression test proving the separation holds, not a filter widening.
  - **RED → GREEN, performed for real (not skipped):** RED = temporarily narrowed `CLERIC_EXPLANATION_PREFIX` from `"class_chassis.cleric."` to `"class_chassis.cleric.c"` in a scratch copy of `cleric.rs` (`cp cleric.rs /tmp/cleric.rs.bak` first) to simulate the real Wizard bug shape (a filter that fails to admit real, changed pillar ids) — `cargo test --test sd25_cleric_level_up_explanation_coverage` then failed 2 of 3 tests exactly as expected: `every_changed_cleric_explanation_produces_a_grant` (`base_attack_bonus` changed level 1→2 with zero matching grants) and `known_cleric_pillars_each_produce_a_grant_across_their_own_documented_threshold` (`domain_spell_slot` at level 2→3). GREEN = restored the real, unmodified `cleric.rs` from the scratch backup (byte-identical `diff` confirmed) — all 3 tests pass. This proves the test suite **would** have caught the Wizard-shaped bug had one existed in Cleric, and that it passes cleanly against the real, currently-correct module — a verified negative finding, not a skipped check.
  - **Test suite added (`tests/sd25_cleric_level_up_explanation_coverage.rs`, 3 tests):**
    1. `every_changed_cleric_explanation_produces_a_grant` — generalized completeness proof (not a hand-picked allowlist): walks every adjacent level 1→20 transition, diffs `compute_pilot_base_chassis`'s `.explanations` by id, and asserts every Cleric-pillar id whose value changed produces a matching `Grant` in `compute_cleric_level_up_grants`'s output. This is the test shape that generalizes catching any future Wizard-shaped regression, not just today's specific suspect id.
    2. `known_cleric_pillars_each_produce_a_grant_across_their_own_documented_threshold` — names the concrete real pillars (`base_attack_bonus`, all 3 base saves, `channel_energy_dice`, `domain_spell_slot`, `domain_power_good_touch_of_good_bonus`) and confirms each produces a grant at its own real documented rise, found dynamically rather than assumed at a fixed level (initial draft assumed Reflex changes 1→2 — it does not, per Cleric's poor-save cadence — corrected to scan for the real transition instead of hardcoding a wrong one, an example of verifying-by-running rather than assuming).
    3. `prepared_divine_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — the verified-negative-finding proof: across all 20 supported levels, confirms the diagnostic stays present and claim-blocking, never appears in `.explanations`, and never appears as a `Grant.source_table.column_key` in any `LevelUpPlan`.
  - **Test results (final, GREEN, real module):** `cargo test --test sd25_cleric_level_up_explanation_coverage` → `3 passed; 0 failed`. Also re-ran the full pre-existing Cleric surface for regression safety: `cargo test --test sd20_levelup_cleric --test sd13_cleric_level1_spell_baseline --test sd13_cleric_domain_powers --test sd13_cleric_base_attack_and_saves` → all pass (5 + 17 + 6 + 7 = 35 tests, 0 failed). `cargo test --lib rules_core::level_up` (multiclass dispatch unit tests) → 3 passed, unaffected (no dispatch code touched).
  - **Correction to forward to sibling cycles 7.4/7.6/7.7/7.9/7.10 (Sorcerer/Bard/Druid/Paladin/Ranger — the other spellcasters the 7.1 intake flagged by the identical "`.unsupported` diagnostic" reasoning):** the 7.1 intake's blanket claim that these `.unsupported`-suffixed ids are "silently dropped from `LevelUpPlan` today — the same bug shape as Wizard" should be **independently re-verified per class, not assumed** — if (as with Cleric) those ids are `ComputationDiagnostic`s rather than `ComputationExplanation`s in their own class's `explain_*` function, the same "no live gap, diagnostic-vs-explanation separation, add a regression test rather than widen the filter" finding likely applies there too. Each sibling cycle should read its own class's `explain_<class>_level1_...` function directly (per this cycle's method) rather than trusting the intake's prefix-string pattern-match alone.
  - **File-touch grant note:** the grant named `src/rules_core/level_up/cleric.rs only (plus its own test file/module)` was interpreted as covering a new top-level `tests/sd25_*.rs` integration test file (the established convention this crate already uses for every `level_up/<class>.rs` — e.g. `tests/sd20_levelup_cleric.rs`, `tests/sd24_wizard_level_up_spell_coverage.rs` — none of these per-class level-up tests live as an inline `#[cfg(test)] mod tests` inside the `level_up/<class>.rs` file itself). No sibling class's file was read or touched beyond `pilot_compute.rs` (read-only, to trace explanation/diagnostic provenance — the same read-only seam every `level_up/<class>.rs` module already documents reading).
- **Discovery forwards:** the correction note above (re-verify, don't assume, for 7.4/7.6/7.7/7.9/7.10) — not a new `## DISCOVERED` top-level entry (would risk pushing the queue further over the already-flagged 10-entry cap per 7.1's own note); recorded here and cross-referenced from `progress.md`'s 7.1 entry instead.
- **Next-cycle plan:** 7.3 (Rogue), 7.4 (Sorcerer), 7.5 (Barbarian), 7.6 (Bard), 7.7 (Druid), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger) remain to audit, each against this cycle's and Wizard's method: read the class's own `explain_<class>_level1_...` function in `pilot_compute.rs` directly, classify every candidate id as `ComputationExplanation` (real, filter-relevant) vs. `ComputationDiagnostic` (never filter-relevant), and only widen the class's filter if a real explanation-side gap is found.
