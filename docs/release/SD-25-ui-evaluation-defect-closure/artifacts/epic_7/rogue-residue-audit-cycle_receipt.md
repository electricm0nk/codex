# Cycle rogue-residue-audit — Epic 7 (Deferred Per-Class Work & Coverage Backlog) / Criterion 7.3

- **Card ID:** t_8b99ec21
- **Commit SHA:** (recorded post-commit; see progress.md update in the same push)
- **Files touched:**
  - `src/rules_core/level_up/rogue.rs` (doc-comment-only addition documenting the verified negative finding; no functional filter change — see Notes)
  - `tests/sd25_rogue_level_up_explanation_filter_audit.rs` (new; the audit test)
  - `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/rogue-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + cycle log update)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff on `src/rules_core/level_up/rogue.rs` (the canonical `loop-instruction.md §6` audited glob list): clean. The bundle-wide `BASE_BRANCH=$(git merge-base HEAD origin/develop)` diff surfaces a large volume of pre-existing hits from earlier, already-reviewed cycles on this branch (SD-11/SD-12/SD-13 in-flight identifier renames, etc.) — none contributed by this cycle, matching the sibling `corpus-intake-apg-spell-text` receipt's own precedent for re-scoping. `tests/*.rs` is not in the canonical audited glob at all, but was checked anyway (informational): also clean.
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own diff on `src/rules_core/level_up/rogue.rs`: clean. Checked the new test file too (informational, outside the canonical glob): clean.
- **Acceptance criterion (verbatim, `progress.md`'s `## DISCOVERED` register A6 sub-row for Rogue):** "**Rogue** — `src/rules_core/level_up/rogue.rs` — filter admits `class_chassis.rogue.` / `class_feature.rogue.` only. No `class_spell.rogue.*` id exists in `pilot_compute.rs` (Rogue is a non-caster in CRB) — audit should confirm no live gap exists rather than assume, and check for any other prefix family the filter might miss. suggested criterion: **7.3**."
- **Status:** complete
- **RED → GREEN evidence:**
  - **Audit performed:** grepped every `rogue`-containing line in `src/rules_core/pilot_compute.rs` (357 hits) and confirmed by direct inspection that every explanation `id: "..."` literal grounding a Rogue fact is prefixed either `class_chassis.rogue.` or `class_feature.rogue.` — no `class_spell.rogue.*` family and no third prefix family of any kind exists. Cross-checked against `rogue.rs`'s own `append_class_feature_grants` filter (`is_rogue_class_feature_id = id.starts_with("class_chassis.rogue.") || id.starts_with("class_feature.rogue.")`): the two sets match exactly.
  - **RED test written:** `tests/sd25_rogue_level_up_explanation_filter_audit.rs`, two tests:
    1. `every_rogue_explanation_id_from_pilot_compute_is_covered_by_rogues_own_two_admitted_prefixes` — sweeps `compute_pilot_base_chassis` outputs across all 20 supported Rogue levels and asserts every `rogue`-containing explanation id starts with one of the two admitted prefixes.
    2. `every_newly_granted_rogue_explanation_surfaces_as_a_real_levelup_grant_at_every_transition` — sweeps all 19 level-up transitions (1->2 through 19->20) and asserts every real (non-"correctly absent", non-class-table-covered) explanation that newly becomes granted between `from_level` and `to_level` shows up as a grant in `compute_rogue_level_up_grants`'s output — the literal Wizard-bug-shaped check.
  - **Confirmed RED capability live:** temporarily edited `rogue.rs`'s filter to `is_rogue_class_feature_id = to_explanation.id.starts_with("class_chassis.rogue.")` only (dropping the `class_feature.rogue.` admission — the exact Wizard-bug shape, applied to Rogue's second prefix). Re-ran the new test suite: **FAILED** as expected — `level 1 -> 2: expected explanation "class_feature.rogue.evasion" ... to surface as a grant ... but it did not`. This proves check 2 is load-bearing, not a rubber-stamp: it would have caught the exact Wizard-shaped bug had it existed in Rogue's module.
  - **Reverted the injected bug** (confirmed via `git diff --stat` showing zero uncommitted changes to `rogue.rs` before the revert-plus-doc-comment edit) and re-ran: **GREEN** — both tests pass (`test result: ok. 2 passed; 0 failed`).
  - **Verified negative finding, not a skipped check:** `rogue.rs`'s explanation-id filter was already correct — no code fix was needed or made. Only a doc-comment addition documents the audit (mirroring how `wizard.rs`'s own doc comment records its SD-24 fix).
- **Test results:**
  - `cargo test --test sd25_rogue_level_up_explanation_filter_audit`: 2 passed, 0 failed (baseline, post-revert).
  - `cargo test rogue` (full-workspace filter): all Rogue-related test binaries pass (`sd13_rogue_level1_chassis_baseline`: 13 passed; `sd20_levelup_rogue`: 3 passed; `sd24_multiclass_fighter_wizard_split`'s `multiclass_fighter_rogue_mix_is_still_unsupported`: 1 passed; `sd25_rogue_level_up_explanation_filter_audit`: 2 passed; all other filtered binaries: 0 tests matched, 0 failed).
  - `cargo test --test sd20_levelup_rogue --test sd13_rogue_level1_chassis_baseline`: 16 passed, 0 failed (explicit re-run for this receipt).
- **Notes:**
  - This audit's scope per the file-touch grant was `src/rules_core/level_up/rogue.rs` only (plus its own test file/module) — respected; no sibling class file touched.
  - Unlike Cleric/Sorcerer/Bard/Druid/Paladin/Ranger (all real spellcasting or partial-casting classes with a live `class_spell.<class>.*` family already grounded in `pilot_compute.rs` and silently dropped today per the same register A6 intake), Rogue and Barbarian/Monk are non-casters — this audit independently confirms Rogue has no such gap, exactly as the intake's own "audit should confirm no live gap exists rather than assume" instruction required. This is a real, distinct verification from simply reading the intake's own claim.
  - No `## DISCOVERED` entries added this cycle — the intake's existing entry (register A6) already names this exact finding as a to-be-confirmed suggestion; this cycle confirms it rather than discovering something new.
- **Discovery forwards:** none (confirms, does not add to, the existing register A6 `## DISCOVERED` entry).
- **Next-cycle plan:** the next per-class residue cycle should pick up **7.4 (Sorcerer)** — per the register A6 intake, Sorcerer's filter (`SORCERER_EXPLANATION_PREFIX = "class_chassis.sorcerer."`) does NOT admit `class_spell.sorcerer.spontaneous.unsupported` (grounded at `pilot_compute.rs:14334`), which the intake flags as a live, real gap (not a "confirm no gap" case like Rogue) — this is expected to require an ACTUAL filter fix mirroring Wizard's, not just a verification test.
