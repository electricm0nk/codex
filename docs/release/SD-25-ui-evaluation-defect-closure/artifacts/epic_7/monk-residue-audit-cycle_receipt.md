# Cycle monk-residue-audit — Epic 7 (Deferred Per-Class Work & Coverage Backlog) / Criterion 7.8

- **Card ID:** `t_4473c8b2`
- **Commit SHA:** `b1a89bba66d8cbe069743d1c8e757929766a0bb9` (test + this receipt); `b4ff3b1` (progress.md status-matrix + register A6 update)
- **Files touched:**
  - NEW `tests/sd25_monk_level_up_explanation_filter_audit.rs` (RED → GREEN regression proof + verified-negative-finding documentation)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/monk-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix, follow-up commit per `§5`'s protocol)
  - **`src/rules_core/level_up/monk.rs` — NOT touched.** The file-touch grant covers it, but no bug was found there (see Notes/Acceptance below) — per the brief's own instruction, "do not invent a fix for a bug that isn't there."
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own staged diff (`git diff --unified=0 --cached` against the canonical `loop-instruction.md §6` audited glob list: `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`, mirroring the Bard/Druid/Sorcerer precedent scoping). Zero hits — `src/**/*.rs` has no diff at all (monk.rs untouched). The new test file (`tests/sd25_monk_level_up_explanation_filter_audit.rs`) sits outside every audited glob entirely (it lives under `tests/`); checked informationally anyway — its only `sd\d+_`/`Sd\d+`-shaped hits are real `tests/sd20_levelup_monk.rs`/`tests/sd24_wizard_level_up_spell_coverage.rs`/`tests/sd25_druid_level_up_explanation_filter_audit.rs` doc-comment citations and this file's own `sd25_monk_level_up_explanation_filter_audit` `case_id`/`source_package_id` string literals, not bundle-tag leaks in shipping code.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` anywhere in the (empty) `src/**/*.rs` diff.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` register A6 Monk sub-row):** "**Monk** — `src/rules_core/level_up/monk.rs` — filter admits `class_chassis.monk.` / `class_feature.monk.` only. No `class_spell.monk.*` id exists in `pilot_compute.rs` (non-caster) — audit should confirm no live gap exists. suggested criterion: **7.8**." Dispatcher's brief for this cycle: audit `monk.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **RED → GREEN evidence:**
  - **Intake claim independently re-verified, confirmed accurate this time** (unlike Bard's 7.6, where the intake's cited diagnostic id turned out to be a red herring but a DIFFERENT real bug was found by the fuller reference-module comparison): read `explain_monk_level1_chassis` end-to-end in `src/rules_core/pilot_compute.rs` (26 `explanations.push(...)` call sites traced by line number). Every real (non-diagnostic) id pushed uses either the `"class_chassis.monk."` or `"class_feature.monk."` prefix — both already admitted by `monk.rs`'s `is_monk_pillar_id`. `grep -n '"class_spell.monk'` returns zero hits anywhere in `pilot_compute.rs` (also independently asserted by this cycle's own `no_class_spell_monk_prefixed_id_exists_anywhere` test): Monk is not a spellcaster, so there is no later-landing spell-family grounding of the Wizard-bug shape that could have been missed.
  - **Second, independent check per Bard's 7.6 forward-note** ("also check for a `<CLASS>_RECOGNITION_ID`-shaped whitelist gap, not just the cited prefix"): Monk's own recognition explanation, `"class_chassis.monk.bounded_progression"` (`pilot_compute.rs:11961`), already has its second dotted segment as `monk` — unlike Bard's/Wizard's/Sorcerer's own `"class_chassis.spell_baseline.<class>"` recognition ids, it was never at risk of the Bard-shaped "different second segment" bug either, and it already passes `is_monk_pillar_id`'s `"class_chassis.monk."` prefix directly. No missing whitelist entry needed or added.
  - **One claim-blocking diagnostic checked and confirmed structurally unreachable** (mirroring Druid's/Bard's own diagnostic-vs-explanation check): `"class_feature.monk.bounded_progression.bonus_feat.unsupported"` (`pilot_compute.rs:12684`) is pushed via `diagnostics.push(...)`, a structurally separate `Vec<ComputationDiagnostic>`, never merged into `.explanations`. `monk.rs`'s own `monk_chassis_explanations` probe helper reads only `.explanations`, so this id is structurally unreachable by `append_class_feature_grants` regardless of what prefix the filter admits.
  - **RED test written:** `tests/sd25_monk_level_up_explanation_filter_audit.rs`, 6 tests (mirrors `tests/sd25_druid_level_up_explanation_filter_audit.rs`'s coverage-sweep method, generalized rather than hand-picking one suspect id):
    1. `every_real_monk_explanation_id_survives_the_level_up_filter_except_the_documented_exclusions` — enumerates every real explanation id `pilot_compute.rs` emits for Monk across the full grounded level 1..=12 range (`MAX_SUPPORTED_MONK_LEVEL`) directly from the public `compute_pilot_base_chassis` seam, drives every adjacent level transition through the public `compute_monk_level_up_grants` entry point, and asserts every id surfaces somewhere in the resulting `LevelUpPlan` (either as an `automatic_features` grant's source id, or — for the ki pool, a resource-count magnitude — as a `resource_pool_change` entry's source id), except three documented exclusions.
    2. `the_flat_bounded_progression_recognition_id_is_deliberately_never_granted` — confirms the flat +0 recognition id's exclusion is real and structural (present in the raw explanation set at value 0 every level, never itself a grant source).
    3. `the_flat_ac_bonus_id_passes_the_filter_but_is_verified_constant_across_the_whole_range` — a SECOND finding, surfaced live by this test's own first RED run against the real, unmodified module (not anticipated in advance, mirroring Druid's own "second finding, surfaced by this test's own RED run" precedent): `class_chassis.monk.ac_bonus` passes the filter (matches the admitted prefix) but `pilot_compute.rs` grounds it as `ability_modifiers.wisdom.max(0)` only — a pure Wisdom-modifier readout with NO level term in its formula at all — so for this audit's fixed-ability-score fixture it is provably constant across the whole 1..=12 sweep and can never itself drive a value-change grant. Verified (not assumed) by asserting the raw value stays `3` (WIS 17 → +3) at every level.
    4. `the_class_table_covered_ids_are_reported_once_under_a_different_provenance_not_dropped` — confirms the four base-attack/base-save ids `monk.rs`'s own `CLASS_TABLE_COVERED_EXPLANATION_IDS` deliberately excludes are reported once under a DIFFERENT, plain column key (`"base_attack_bonus"`/`"fort_save"`/`"ref_save"`/`"will_save"`, sourced from `class_tables()` by `append_class_table_grants`) every time the dotted `pilot_compute.rs` id's underlying value changes — a real fact under a different provenance, not a silent drop.
    5. `no_class_spell_monk_prefixed_id_exists_anywhere` — verified-negative-finding proof: confirms zero `class_spell.monk.*`-prefixed explanations exist anywhere in the raw explanation set across the full level range.
    6. `the_bonus_feat_unsupported_diagnostic_is_never_read_as_an_explanation` — verified-negative-finding regression proof for the one diagnostic id, mirroring Druid's/Bard's own diagnostic negative controls.
  - **Confirmed RED (bug-shape simulation) against a deliberately narrowed filter:** backed up first (`cp monk.rs /tmp/monk.rs.bak`), then temporarily narrowed `is_monk_pillar_id` in `src/rules_core/level_up/monk.rs` from `id.starts_with("class_chassis.monk.") || id.starts_with("class_feature.monk.")` to `id.starts_with("class_chassis.monk.")` only, simulating the exact Wizard bug shape (a filter that fails to admit a real, changed pillar prefix). `cargo test --test sd25_monk_level_up_explanation_filter_audit` then **FAILED** exactly as expected: `5 passed; 1 failed`, with `every_real_monk_explanation_id_survives_the_level_up_filter_except_the_documented_exclusions` reporting 3 silently-dropped ids (`class_feature.monk.evasion`, `class_feature.monk.improved_evasion`, `class_feature.monk.still_mind`). This proves the test **would** have caught a Wizard-shaped bug had one existed in Monk.
  - **Restored the real, unmodified `monk.rs`** from the backup — `diff /tmp/monk.rs.bak src/rules_core/level_up/monk.rs` confirmed byte-identical after restore. **Re-ran: GREEN** — `cargo test --test sd25_monk_level_up_explanation_filter_audit`: `test result: ok. 6 passed; 0 failed`.
- **Test results:**
  - `cargo test --test sd25_monk_level_up_explanation_filter_audit`: 6 passed, 0 failed.
  - Full pre-existing Monk test surface (regression safety, all 17 Monk-named `tests/*.rs` binaries run explicitly by name via `find tests -iname '*monk*'`, plus this cycle's own new binary): `sd13_monk_bonus_feat_choice`, `sd13_monk_level1_chassis_baseline`, `sd13_monk_level2_progression` .. `sd13_monk_level10_progression`, `sd13_monk_bonus_feats_three_and_four`, `sd13_monk_second_bonus_feat`, `sd18_monk_level11_diamond_body`, `sd18_monk_level12_widening`, `sd20_levelup_monk`, `sd25_monk_level_up_explanation_filter_audit` (this cycle's own) — all 0 failed.
  - `cargo build --tests` (full workspace): clean build, no warnings introduced, finished in 2m06s.
- **Notes:**
  - This is a genuine, real, verified negative finding — not a skipped check, and not a fabricated fix. `monk.rs`'s explanation-id filter already correctly admits both real prefixes; Monk grounds no spell-bearing posture at all, so it was never at risk of the Wizard bug's specific "spell-family grounding lands after the filter is authored" shape, and its recognition id's naming (`"class_chassis.monk.bounded_progression"`) was never at risk of the different Bard-shaped "different second segment" bug either.
  - The `ac_bonus` finding (exclusion #1a) is a genuinely new observation this cycle's own comprehensive sweep surfaced (not anticipated by the 7.1 intake, not present in any prior sibling's receipt) — it is a correctly-included, correctly-inert fact (mirrors Druid's own `nature_sense` "second finding" exactly in structure): the filter does admit it, but `pilot_compute.rs`'s own formula for it has no level term, so no `from_level -> to_level` transition this codebase models can ever produce a value change to grant. This is NOT a defect in `monk.rs`; widening anything here would change nothing observable and isn't warranted.
  - File-touch grant note: the grant named `src/rules_core/level_up/monk.rs only (plus its own test file/module)` was interpreted as covering a new top-level `tests/sd25_*.rs` integration test file, mirroring the established convention this crate already uses for every `level_up/<class>.rs` (`tests/sd20_levelup_monk.rs`, `tests/sd24_wizard_level_up_spell_coverage.rs`, `tests/sd25_druid_level_up_explanation_filter_audit.rs`, `tests/sd25_bard_level_up_explanation_coverage.rs`) — none of these per-class level-up tests live as an inline `#[cfg(test)] mod tests` inside the `level_up/<class>.rs` file itself. No sibling class's file was read or touched beyond `pilot_compute.rs` (read-only, to trace explanation/diagnostic provenance) and `wizard.rs` (read-only, reference pattern) and `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/druid-residue-audit-cycle_receipt.md`/`bard-residue-audit-cycle_receipt.md` (read-only, prior-cycle method reference).
  - **Concurrent-write note:** this cycle's initial fetch+rebase landed cleanly on `origin/tranche/5-3` at `b0049de` (7.6 Bard progress.md update) with zero conflicts before any of this cycle's own work began.
- **Discovery forwards:** none new. Confirms Druid's/Bard's own forward-note: the 7.1 intake's characterization holds for Monk (correctly predicted "audit should confirm no live gap exists" — the one intake sub-row among the batch that already anticipated a negative finding), and this cycle's own independent comprehensive sweep (not just the cited-id check) surfaced one additional, genuinely-inert (not a bug) flat-formula finding (`ac_bonus`) worth noting for future cycles: **remaining sibling cycles (7.9 Paladin, 7.10 Ranger) should continue to independently re-verify per class** (read the class's own `explain_<class>_level1_...` function directly; classify each candidate id as `ComputationExplanation` vs. `ComputationDiagnostic`; check for a `<CLASS>_RECOGNITION_ID`-shaped whitelist gap; and separately check whether any filter-admitted id's underlying `pilot_compute.rs` formula has NO level term at all, which — for a fixed-ability-score sweep fixture — will always appear as "never granted" and needs its own documented exclusion rather than being mistaken for a silent drop).
- **Next-cycle plan:** the next per-class residue cycle should pick up whichever of 7.9 (Paladin), 7.10 (Ranger) is still unclaimed. Per this cycle's own finding, both remaining classes should be checked for: (a) the cited-id diagnostic-vs-explanation shape, (b) a missing `<CLASS>_RECOGNITION_ID`-shaped whitelist entry, and (c) any filter-admitted-but-level-formula-independent id (the `ac_bonus`-shaped false-positive risk) before concluding a coverage-sweep failure is a real bug.
