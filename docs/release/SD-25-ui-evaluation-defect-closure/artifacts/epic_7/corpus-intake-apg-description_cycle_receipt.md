# Cycle corpus-intake-apg-description — Epic 7 (Equipment/spell corpus intake) / Criterion 7.N (item 2: "APG equipment description field")

- **Card ID:** t_apgdesc01 (kanban card minted at end of cycle; see below)
- **Commit SHA:** (recorded at push time — see `progress.md`)
- **Files touched:**
  - `src/rules_core/rules_tables/apg/equipment_data.rs` (populated `description` on 331/338 records; updated module doc comment)
  - `src/rules_core/rules_tables/apg/equipment_tables.rs` (updated `description`/`has_description` doc comments to match the new ceiling; no struct/logic change)
  - `tests/sd24_equipment_coverage_audit.rs` (updated `apg_equipment_gained_weight_field_and_is_fully_record_ingested`'s `has_description` assertion from `0` to `331`, with updated doc comment — this is the file's one APG-scoped test function; the sibling CRB/ACG functions in the same file were not touched)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff on the audited globs (`src/**/*.rs`; `tests/**/*.rs` is not in the audited glob list at all). The bundle-wide `BASE_BRANCH=$(git merge-base HEAD origin/develop)` diff surfaces a large volume of pre-existing hits from earlier, already-reviewed cycles in this same branch (SD-24/SD-25 identifier-cleanup renames, `sd13_*`/`Sd11*`/`Sd12*` in-flight renames, etc.) — none contributed by this cycle. Re-ran the grep scoped to just this cycle's two touched `src/**/*.rs` files: clean.
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own diff: clean, both against the full-branch base and re-scoped to this cycle's own files. Register A9 note: several sourced descriptions use ordinary combat/craft prose (e.g. "chopping blade", "cutting edge") but the literal forbidden token `hack` does not appear anywhere in this cycle's diff — no exclusion or self-heal was needed.
- **Acceptance criterion (verbatim, `cycles/7_N.md` item 2):** "APG description field — 0/338 rows carry `DESC:`; hardest ceiling. Web-second-source pass required."
- **Status:** complete
- **RED → GREEN evidence:** RED — reverted only `tests/sd24_equipment_coverage_audit.rs` to its pre-cycle content (`git stash push` on that one file) and ran `cargo test --test sd24_equipment_coverage_audit apg_equipment_gained_weight_field_and_is_fully_record_ingested` against the already-updated `equipment_data.rs`: **FAILED** — `assertion left == right failed: ... left: 331 right: 0` (the old test's hard-coded `has_description == 0` assertion, proving the source change is real and load-bearing). GREEN — restored the updated test (`git stash pop`) and re-ran the same test plus the full `sd24_equipment_coverage_audit` suite: **8 passed, 0 failed**. Also ran the full workspace `cargo test`: 443 passed, 2 failed — both pre-existing failures in `sd24_wired_integration_audit.rs` (`stub_adapter.rs`/`characterHub/*.rs`, an in-flight Epic 3 scaffold this cycle's file-touch grant does not cover), confirmed identical on the pre-change baseline via the same stash technique.
- **Notes:**
  - **Register A10 (checked before falling back to web):** re-confirmed via direct inspection of `apg_equip_general.lst`/`apg_equip_arms_armor.lst`/`apg_equip_magic_items.lst` that APG's equipment corpus has zero `DESC:` tokens and no `SPROP:`-equivalent convention (unlike ACG, register A10) — the web second-source pass was the only path, as the cycle doc anticipated.
  - **Register A8 (codegen-path decision, recorded deliberately):** did **not** build the shared `pcgen_import`-backed codegen path (`lst_parser::{equipment,spell}` → semantic map → Rust literals) for this item. That path's entire purpose is extracting a semantic value from an LST corpus *token* (`COST:`/`WT:`/`DESC:`); this cycle's gap has no `DESC:` token in the corpus at all to extract — the sourced data comes from `legacy.aonprd.com`/`aonprd.com`/`d20pfsrd.com` web pages, which an LST-token parser cannot help with. Used a one-off Python merge script instead (same shape the prior `apg-content-completion-cycle` used for the `weight` field), preserved at
    `/tmp/claude-1000/-home-ubuntu-workspace-repos-codex/4a431fbd-9267-40d2-9cbe-491d592b6c32/scratchpad/apg_desc_fill.py`
    for reproducibility (not checked into the repo — it is a merge tool, not a build-time codegen step; `equipment_data.rs` remains hand-maintainable per its own doc comment, same posture the `weight` field already established).
  - **Web second-source citation table.** All sources are `legacy.aonprd.com`/`www.aonprd.com` (both `aonprd.com` per the rule) or `d20pfsrd.com`. Identity was matched by exact record **name** cross-checked against **cost** (gp) and, where the corpus's own `COST:` token diverged from the real confirmed market price, against **weight** instead (see the corpus-quirk finding below). Fetched pages (10 category-aggregate pages covered nearly the whole 338-record corpus in a small, reasonably-paced number of requests — no bulk/per-item scraping):
    - `https://legacy.aonprd.com/advancedPlayersGuide/advancedGear.html` — mundane weapons, armor, adventuring gear, special substances, tools/skill kits, clothing, animal gear, entertainment items (covers nearly all of `General` + the mundane half of `ArmsArmor`).
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/wondrousItems.html` — ~65 wondrous items.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/rings.html` — 6 rings.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/staves.html` — all 17 staves.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/rods.html` — all 14 metamagic rod types (Lesser/Normal/Greater = 42 records + Rod of Arson).
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/cursedItems.html` — 8 matched cursed items (Buffoon's Sword, Cannibal Ring, Ring of Truth, Cloak of Immolation, Crown of Blindness, Hat of Hatreds, Girdle of Opposite Gender, Riot Pipes) + Rod of Arson.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/minorArtifacts.html` — 6 minor artifacts.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/majorArtifacts.html` — 5 major artifacts.
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/armor.html` — specific magic armors/shields (Mistmail, Soothsayer's Raiment ×2, Boneless Leather, Buccaneer's Breastplate, Daystar Half-Plate, Folding Plate, Forsaken Banded Mail, Giant Hide ×8, Murderer's Blackcloth, Armor of Insults, Battlement Shield, Fortress Shield).
    - `https://legacy.aonprd.com/advancedPlayersGuide/magicItems/weapons.html` — specific magic weapons (Beaststrike Club, Mace (Boulderhead), Guarding Blade, Hammer (Ricochet), Starknife (Sparkwake), Axe (Undercutting), Trident of Stability, Blade of Binding, Lance (Jousting), Lance (Shieldsplitter), Spirit Blade, Arrow (Searing), Bolt (Tangle), Bullet (Dustburst)).
    - `https://www.d20pfsrd.com/magic-items/magic-weapons/specific-magic-weapons/sizzling-arrow/` and `.../searing-arrow/` — used only to **verify** a price conflict (see below), not as a source for a written description.
  - **Corpus quirk discovered this cycle (`## DISCOVERED` entry filed in `progress.md`):** for a subset of `ArmsArmor`-category "named specific magic weapon/armor" records, this corpus's own `COST:` token is far below the real market price the web sources confirm (e.g. `Beaststrike Club` corpus `Some(0.0)` vs. real 7,300 gp; `Guarding Blade` corpus `Some(10.0)` vs. real 65,310 gp), while `weight` matches exactly every time (11/11 checked). Rings, staves, rods, and wondrous items showed **no** such divergence — their `COST:` tokens matched the real price exactly in every case checked. Identity for the divergent `ArmsArmor` subset was confirmed via distinctive name + exact weight cross-check rather than cost. `cost_gp` itself was **not** touched — out of this cycle's field-touch scope (only `description` was granted) — so this remains a real, separate, documented gap for a future cost-focused cycle, not silently fixed here.
  - **Honest, non-fabricated gaps kept as `None` (7 of 338):**
    - `Formula Book`, `Bomb` — corpus records with no `COST:`/`WT:` token at all (not sold items; `Bomb` is a to-hit-bonus template per this file's own pre-existing doc note). No confident single-item source found; left `None`.
    - `Arrow (Sizzling)` — the corpus's `COST:` token (1,350 gp) disagrees with the real, independently-confirmed price (1,516 gp — verified via both `d20pfsrd.com` and a second `aonprd.com`-sourced search result, and matching this same corpus's own `Arrow (Searing)` record). Per the identity-match rule ("reject cross-book or edition-cousin false matches"), a cost mismatch with no weight cross-check available (the source page gives no weight for either arrow) was treated as an unconfirmed identity rather than silently accepted — left `None` rather than guessed.
    - `Cauldron of Seeing (See Invisibility)` / `(Detect Thoughts)` / `(Telepathy)` / `(True Seeing)` — the base `Cauldron of Seeing` (42,000 gp) matched and was populated; these 4 higher-priced variants have no distinct sourced text beyond the base item's generic scrying description, so their specific added divination mechanic was not fabricated. Left `None`.
  - **Two entries fixed on a second pass** after an initial merge-script run: `Arrow (Searing)`, `Bolt (Tangle)`, `Bullet (Dustburst)`, `Agile Breastplate`, `Agile Half-Plate` were confirmed matches gathered during research but omitted from the first script pass by oversight; added and re-run (see RED→GREEN below for the two-pass count 326 → 331).
  - **SD-24 Open Blocker #2 retroactively closed.** Per the cycle doc's directive, this pass closes SD-24's Open Blocker #2 (register A16, "APG equipment description field, 0/338, hardest ceiling") — SD-24's own record should be annotated that criterion 6.4's APG description gap is now 331/338 (97.9%) sourced, not a corpus-limited 0%.
  - **Register A9:** no forbidden-token/real-SRD-text collision was hit in this cycle's diff (checked via the dual-audit re-run above); no exclusion mechanism needed.
- **Discovery forwards:** one `## DISCOVERED` entry added to `progress.md` — "APG `ArmsArmor` specific-magic-weapon/armor `COST:` token systematically understates the real market price (name+weight-confirmed against 11 web-sourced records); `cost_gp` itself unchanged this cycle — candidate for a future targeted `apg-cost-correction` cycle, scoped to `ArmsArmor` only."
- **Next-cycle plan:** the sibling `apg-spell-text` item (APG full spell text, 36/297 gap) and `bestiary1` item (new equipment module) are disjoint cycles running in parallel per the cycle doc; no dependency on this cycle's output. If a future cycle wants to push APG equipment description past 331/338, the 7 remaining gaps are individually documented above with the specific blocker for each (no LST token, unconfirmed cost identity, or no distinct sourced sub-variant text) — none are a "just look harder" gap.
