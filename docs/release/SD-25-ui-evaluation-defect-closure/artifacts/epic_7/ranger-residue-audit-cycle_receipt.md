# Cycle 7.10 — Epic 7 (Per-class residue) / Criterion 7.10

- **Card ID:** `t_687cc086`
- **Commit SHA:** `8a3e7ee8d03bb0498cd8376682256d4f60c5dc50`
- **Files touched:**
  - `src/rules_core/level_up/ranger.rs` — added `RANGER_RECOGNITION_ID` whitelist entry (`"class_chassis.hybrid_baseline.ranger"`) to `append_class_feature_grants`'s `is_ranger_class_feature_id` check, plus a matching `friendly_name` trim and doc-comment correction. A REAL bug found and fixed (see Notes below) — mirrors Bard's 7.6 fix exactly, applied to Ranger's own recognition-id shape.
  - NEW `tests/sd25_ranger_level_up_explanation_coverage.rs` (RED → GREEN regression proof, 5 tests)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/ranger-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix row + `## DISCOVERED` Ranger sub-row update, in a follow-up commit per `§5`'s protocol)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own staged diff (`git diff --unified=0 --cached` against the audited globs `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`; `tests/**` is outside every audited glob so the new test file itself is never scanned). One hit inside `src/rules_core/level_up/ranger.rs`'s own doc comment: a literal citation of the real, existing new test file path `tests/sd25_ranger_level_up_explanation_coverage.rs` — the same "real `tests/`-file citation" exclusion criterion 1.1 (and every sibling 7.x cycle) already established as non-violating, not a fabricated bundle-tag identifier. No `sd\d+_`/`SD\d+_`/`Sd\d+`/`t_[0-9a-f]{8,}`-shaped identifier (variable, const, function, or type name) appears anywhere in `ranger.rs`.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` anywhere in the staged diff.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` entry, Ranger sub-row):** "**Ranger** — `src/rules_core/level_up/ranger.rs` — filter admits `class_chassis.ranger.` / `class_feature.ranger.` only — no `class_spell.ranger.` (or `class_spell.hybrid.ranger.`) prefix. `pilot_compute.rs:7614` grounds `class_spell.hybrid.ranger.unsupported`, silently dropped today. suggested criterion: **7.10**." Dispatcher's brief for this cycle: audit `ranger.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **Notes:**
  - **The 7.1 intake's own cited id is a verified negative, but a REAL bug of a different shape was found — mirrors Bard's 7.6 outcome, not Cleric's/Rogue's/Sorcerer's/Barbarian's/Druid's/Monk's own verified-negative outcomes.** Read `pilot_compute.rs`'s `explain_ranger_level1_chassis_and_class_feature_separation` (the sole function that grounds Ranger's own per-level chassis/spellcasting pillars, lines 8772-10963) end-to-end and traced every `id: "..."` it emits, including the full `class_chassis.ranger.partial_caster.*` spellcasting-burden family (`effective_caster_level`, `spell_level_access`, `base_spells_per_day.spell_level_N`, `bonus_spells_per_day.spell_level_N`, `total_spells_per_day.spell_level_N`, `spell_save_dc.spell_level_N`, newly opening at ranger level 4 per `RANGER_FIRST_LEVEL_SPELLS_BEGIN_AT_CLASS_LEVEL`). Every single one of these already carries the `class_chassis.ranger.` prefix `ranger.rs`'s filter already admitted — confirmed by `grep -oE '"[a-z_.{}0-9]*ranger[a-z_.{}0-9]*"'` across the whole of `pilot_compute.rs` and manual classification of every hit. There is genuinely no `class_spell.ranger.*`-shaped defect (the literal bug shape the 7.1 intake's blanket characterization named) — that part of the intake row is a verified negative, same as every prior sibling's own finding about its own row.
  - **But comparing `ranger.rs` directly against Wizard's/Sorcerer's/Bard's already-fixed reference filter shape (not just re-checking the intake's own cited id) surfaced a genuine, different gap, per Bard's 7.6 own forward-note instruction to check "whether the class's own `<CLASS>_RECOGNITION_ID`-shaped whitelist entry for `"class_chassis.spell_baseline.<class>"` is present."** Ranger has no `class_chassis.spell_baseline.ranger` id at all (Ranger is not a level-1 caster, unlike Wizard/Sorcerer/Bard/Cleric/Druid) — but it DOES have an analogous recognition-shaped seam: `pilot_compute.rs`'s `explain_hybrid_level1_chassis` (the SAME shared function that also grounds Paladin's `class_chassis.hybrid_baseline.paladin`, confirmed by reading its body directly, `pilot_compute.rs:7587-7651`) pushes a bounded +0 `class_chassis.hybrid_baseline.ranger` explanation onto `.explanations` (via `explanations.push(...)`, confirmed a real `ComputationExplanation`, not a diagnostic) whenever the input is a single-class Human Ranger at level 1 (`HYBRID_BASELINE_LEVEL = 1`). This function is called UNCONDITIONALLY inside `compute_pilot_base_chassis` (`pilot_compute.rs:4627`, `explain_hybrid_level1_chassis(input, &mut explanations, &mut diagnostics);`), alongside (not instead of) `explain_ranger_level1_chassis_and_class_feature_separation`, so both fire for the same level-1 Ranger probe `ranger_chassis_explanations` constructs. The id's own second segment is `hybrid_baseline`, not `ranger` — it never matched `ranger.rs`'s two admitted prefixes (`"class_chassis.ranger."` / `"class_feature.ranger."`). Result: on the very first Ranger level transition (0 -> 1, i.e. initial class entry), this id changes `None -> Some(0)` — a genuine change under this module's own diffing rule — yet produced no `Grant` in the `LevelUpPlan` before this cycle's fix. This is the exact Wizard SD-24 bug shape, in Bard's own 7.6-shaped variant (a missing recognition-id whitelist entry, not a missing prefix family).
  - **The two hybrid diagnostics the SAME shared seam also emits (`class_feature.hybrid.ranger.unsupported`, `class_spell.hybrid.ranger.unsupported` — the literal id the 7.1 intake cited) are pushed via `diagnostics.push(...)`, a structurally separate `Vec<ComputationDiagnostic>`, never merged into `.explanations` anywhere in `compute_pilot_base_chassis`** (confirmed by reading the function's full body: `explanations` and `diagnostics` are threaded as two independent `&mut Vec<_>` parameters throughout, and `explain_hybrid_level1_chassis`'s own signature takes both separately). `ranger.rs`'s `ranger_chassis_explanations` helper reads only `.explanations`. So the 7.1 intake's own literally-cited id (`class_spell.hybrid.ranger.unsupported`) was never reachable regardless of what prefix the filter admitted — a verified negative on that SPECIFIC claim, same shape as Cleric's/Sorcerer's/Druid's/Monk's own `.unsupported`-diagnostic findings — but the SIBLING explanation from the same function (`class_chassis.hybrid_baseline.ranger`) genuinely was reachable and genuinely was dropped.
  - **RED → GREEN, performed for real (not skipped):** RED = temporarily reverted `is_ranger_class_feature_id` in `src/rules_core/level_up/ranger.rs` from `to_explanation.id == RANGER_RECOGNITION_ID || starts_with("class_chassis.ranger.") || starts_with("class_feature.ranger.")` back to the pre-fix `starts_with("class_chassis.ranger.") || starts_with("class_feature.ranger.")` only (backed up first: `cp ranger.rs /tmp/ranger.rs.bak`). `cargo test --test sd25_ranger_level_up_explanation_coverage` then failed exactly 2 of 5 tests as expected: `every_changed_ranger_explanation_produces_a_grant` (`explanation 'class_chassis.hybrid_baseline.ranger' changed from None to 0 but no matching Grant was found`) and `ranger_recognition_explanation_produces_a_grant_on_first_level` (the targeted named-id assertion). The other 3 tests (the partial-caster family test, both hybrid-diagnostic-never-leaks tests) passed even pre-fix, confirming they test genuinely separate, already-correct behavior, not the bug this cycle fixes. GREEN = restored the real, fixed `ranger.rs` (`cp /tmp/ranger.rs.bak src/rules_core/level_up/ranger.rs`, byte-identical `diff` confirmed) — all 5 tests pass.
  - **Test suite added (`tests/sd25_ranger_level_up_explanation_coverage.rs`, 5 tests):**
    1. `every_changed_ranger_explanation_produces_a_grant` — generalized completeness proof (not a hand-picked allowlist): starts at `from_level = 0` (uncovering the missed 0 -> 1 recognition transition) and walks the full supported level 1..20 range, diffing `compute_pilot_base_chassis`'s `.explanations` by id and asserting every Ranger-pillar id whose value changed produces a matching `Grant`. This is the test shape that generalizes catching any future Wizard-shaped regression, not just this cycle's specific finding.
    2. `ranger_recognition_explanation_produces_a_grant_on_first_level` — names the recognition record explicitly by id, so a future accidental narrowing of the whitelist is caught by name too.
    3. `ranger_partial_caster_family_newly_grants_at_the_level_4_spell_access_gate` — names the concrete real spellcasting-burden pillars (`effective_caster_level`, `spell_level_access`, `base_spells_per_day.spell_level_1`, `spell_save_dc.spell_level_1`) and confirms each produces a grant at ranger level 3 -> 4, the real documented spell-access-opening transition — proving the intake's suspected `class_spell.ranger.*` bug shape genuinely does not exist for this family.
    4. `hybrid_feature_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — verified-negative-finding proof for `class_feature.hybrid.ranger.unsupported`: confirms it stays claim-blocking, never appears in `.explanations`, and never appears as a `Grant.source_table.column_key`.
    5. `hybrid_spell_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — the identical verified-negative-finding proof for the 7.1 intake's own literally-cited id, `class_spell.hybrid.ranger.unsupported`.
  - **Test results (final, GREEN, real fixed module):** `cargo test --test sd25_ranger_level_up_explanation_coverage` → `5 passed; 0 failed`. Also re-ran the full pre-existing Ranger surface for regression safety: 32 test binaries (`sd20_levelup_ranger`, `sd13_ranger_*` ×20, `sd18_ranger_level11_quarry` through `sd18_ranger_level20_widening` ×10, plus this cycle's new file) → all pass, 0 failed across every binary.
  - **File-touch grant note:** the grant named `src/rules_core/level_up/ranger.rs only (plus its own test file/module)` was interpreted as covering a new top-level `tests/sd25_*.rs` integration test file, mirroring the established convention this crate already uses for every `level_up/<class>.rs` (`tests/sd20_levelup_ranger.rs`, `tests/sd24_wizard_level_up_spell_coverage.rs`, `tests/sd25_bard_level_up_explanation_coverage.rs`) — none of these per-class level-up tests live as an inline `#[cfg(test)] mod tests` inside the `level_up/<class>.rs` file itself. No sibling class's file was read or touched beyond `pilot_compute.rs` (read-only, to trace explanation/diagnostic provenance) and `wizard.rs`/`bard.rs` (read-only, reference pattern for the recognition-id whitelist shape).
  - **Concurrent-write note:** this cycle's initial fetch+rebase landed cleanly on `origin/tranche/5-3` with zero conflicts before any of this cycle's own work began (see commit SHA above for the exact parent).
  - **Cross-check against Paladin (7.9), landed concurrently on `tranche/5-3` and picked up by this cycle's own pre-push rebase (`d74dee2`, parent of this cycle's commit):** independent confirmation, not coordination — Paladin's own 7.9 cycle found and fixed the IDENTICAL bug shape via the IDENTICAL `explain_hybrid_level1_chassis` seam (`class_chassis.hybrid_baseline.paladin`, same function, same unconditional call site), adding its own `PALADIN_HYBRID_BASELINE_RECOGNITION_ID` whitelist entry mirroring this exact fix. Both cycles independently derived the same finding shape by directly comparing against Wizard's/Sorcerer's/Bard's reference filter pattern rather than trusting the 7.1 intake's own cited ids, which is the intended convergence the audit method (per Bard's own 7.6 forward-note) was designed to produce.
- **Discovery forwards:** none new — this cycle's own finding shape (a `<CLASS>_RECOGNITION_ID`-shaped gap distinct from the 7.1 intake's own cited ids) is already independently confirmed by Paladin's concurrent 7.9 cycle, so no further forward note is needed.
- **Next-cycle plan:** with 7.2-7.10 all now complete (Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger), the 7.1 intake's full 9-class batch is closed. Two real bugs were found and fixed (Bard 7.6, Paladin 7.9) plus this cycle's own Ranger 7.10 fix — three total, all the identical `<CLASS>_RECOGNITION_ID`-shaped gap on the shared `explain_hybrid_level1_chassis`/`explain_<class>_level1_spell_baseline` recognition seam, none the literal `class_spell.<class>.*`-prefix-family shape the intake's blanket characterization originally suspected. Remaining Epic 7 work per `progress.md §Remaining`: 7.O (blocked on operator design decision) and any newly-spawned `## DISCOVERED` items.
