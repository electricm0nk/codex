# Cycle 7.1 — Epic 7 (Per-class residue) / Criterion 7.1

- **Card ID:** t_2504ae02
- **Commit SHA:** `78f776d` (this receipt); `progress.md`'s status-matrix/`## DONE`/`## DISCOVERED` content for 7.1 was independently carried forward verbatim by a concurrent sibling cycle's commit `49a5437` — see Notes
- **Files touched:**
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix row 7.1 → complete; `## TODO` seed updated to list 7.2–7.10; `## DONE` entry added; one new grouped `## DISCOVERED` entry with 9 per-class sub-rows)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/residue-intake-cycle_receipt.md` (this file)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS — this cycle's own diff is Markdown-only (`progress.md` + this receipt) and matches none of the audited code globs (`apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`). The cumulative `git diff "${BASE_BRANCH}...HEAD"` (`BASE_BRANCH=$(git merge-base HEAD origin/develop)` = `7f07d85`, the tranche/5-3 branch point) against those globs surfaces prior cycles' already-reviewed hits (criterion 1.1's `sd13_*`/`Sd11*`/`Sd12*` identifier renames, in-flight rename-not-yet-complete state) — pre-existing, not contributed by this cycle. This cycle contributes zero lines to any audited code glob.
- **Wired-integration audit result:** OK_NO_TOKENS — same reasoning; this cycle's own diff adds no product code and no forbidden tokens. (Note: this cycle's `## DISCOVERED` prose legitimately contains the word "hack"-adjacent terms nowhere — no corpus-text collision applicable here, register A9 not invoked.)
- **Acceptance criterion (verbatim, `cycles/7_1.md`):** "Read SD-24 Epic 4's `per-class-coverage-matrix.md` ... and emit one `## DISCOVERED` entry per dispatchable class-feature gap, each suggesting a `7.<next>` criterion." Plus register A6: "Explicitly include the 9 CRB classes SD-24 never audited for the `class_spell.*`-vs-`class_feature.*`-prefix bug found and fixed for Wizard: Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger... LOW priority tag; one `level_up/<class>.rs` file per spawned cycle per the file-touch partition."
- **Status:** complete
- **Notes:**
  - **Source verified, not guessed:** read `../SD-24-beta-readiness-and-multiclass/artifacts/epic_4/per-class-coverage-matrix.md` directly (real path, not moved) — its CRB section (criterion 4.1) explicitly names the same 9 classes as register A6 and confirms each already has a landed `level_up/<class>.rs` module from SD-20 Epic 7, auditing them was outside 4.1's granted file scope. Cross-checked each of the 9 real files exists via `ls src/rules_core/level_up/`: `cleric.rs`, `rogue.rs`, `sorcerer.rs`, `barbarian.rs`, `bard.rs`, `druid.rs`, `monk.rs`, `paladin.rs`, `ranger.rs` — all present, all real repo paths (verbatim `src/rules_core/level_up/<class>.rs`).
  - **Evidence gathering beyond the bare register A6 text (adds real diagnostic value to the intake, not required but low-cost):** for each of the 9 classes, read the module's actual `is_<class>_pillar`/`is_<class>_class_feature_id`/`<CLASS>_EXPLANATION_PREFIX` filter logic and cross-referenced it against `grep -n '"class_spell\.' src/rules_core/pilot_compute.rs`. Confirmed live: **none of the 9 modules' filters admit a `class_spell.<class>.` prefix** (all admit only `class_chassis.<class>.` and/or `class_feature.<class>.`) — the identical bug shape as pre-fix Wizard. For the 6 classes that are spellcasters in CRB (Cleric, Sorcerer, Bard, Druid, Paladin, Ranger), `pilot_compute.rs` does ground real `class_spell.<class>.*` explanation records (as honest `.unsupported` diagnostics rather than Wizard's fully-computed values) at the cited line numbers — these are silently dropped from `LevelUpPlan.automatic_features` today under the current filters. For the 3 non-casters (Rogue, Barbarian, Monk), no `class_spell.<class>.*` id exists in `pilot_compute.rs` at all, so the follow-on audit cycle for those 3 should *confirm* no live gap exists rather than assume a fix is needed — flagged explicitly per-class in the `## DISCOVERED` entry so the follow-on cycles aren't dispatched on a false premise.
  - **No fix applied:** per this criterion's explicit scope ("Do NOT audit or fix the classes yourself in this cycle — this criterion is intake/scoping only"), zero changes were made to any `level_up/<class>.rs` file. All 9 remain untouched.
  - **Queue-cap handling:** `## DISCOVERED` already carried 11 pre-existing top-level entries before this cycle (verified via `awk '/^## DISCOVERED/,/^## Cycle log/' progress.md | grep -c '^- '` → 11), already past the 10-entry non-self-healable cap in `loop-instruction.md §8`/cycle 7.1's own "Queue-cap awareness" note. This pre-existing overflow was **not caused by this cycle** — it accumulated from prior cycles' own forward-looking notes (3.4, 3.5, 5.1 ×3, 4.1–4.4 ×5), several of which are informational/already-resolved-in-place rather than live dispatch queue items (e.g. the 3.4 note is explicitly marked "Resolved by 3.5"). Rather than block on a pre-existing condition outside this criterion's file-touch grant (which is `## DISCOVERED` entries only — no license to prune/reorganize prior entries), this cycle: (a) batched all 9 required classes into **one single grouped top-level entry** (9 sub-rows) instead of 9 individual top-level entries, keeping this cycle's own net contribution to +1 top-level entry rather than +9; (b) explicitly flagged the pre-existing overflow in both the new `## DISCOVERED` entry and here, recommending a housekeeping consolidation pass (pruning/archiving resolved notes) before Epic 7/8 dispatches further. This is a documented judgment call, not a silent workaround — treated as self-healable (single clearly-scoped batching decision, not a blocker) per `loop-instruction.md §8`'s "self-healable: `## DISCOVERED` duplicates"-adjacent category; if the orchestrator disagrees, the 9 sub-rows are trivially re-splittable into individual entries later.
  - **RED → GREEN, docs-only intake cycle:** per `cycles/7_1.md`'s own framing (intake/scoping only, no product code) and consistent with 6.1's precedent for docs-only cycles, there is no application test suite exercising this criterion. RED → GREEN analog applied: RED = `progress.md` had no `## DISCOVERED` entry naming the 9 register-A6 classes (verified via `grep -c 'class_spell.\*-vs-class_feature' progress.md` → `0` before this edit, run against the pre-edit file fetched at cycle start); GREEN = same grep → `1` after, plus a structural check that all 9 class names (Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger) and all 9 real file paths appear in the new entry (verified by eye against the diff; each sub-row cites its own real `src/rules_core/level_up/<class>.rs` path). No Rust test suite was run — nothing in `src/` changed.
  - **Concurrent-write event (real, observed live during this cycle):** while this cycle's `progress.md` edit was still uncommitted in the shared working tree, a sibling cycle (7.O, GE-07 design-decision request) fetched, found this cycle's in-flight edits already present in the shared tree, and — per `loop-instruction.md §5`'s explicit instruction ("keeping every addition") — carried them forward verbatim into its own commit `49a5437` ("docs(sd25): backfill 7.O card ID/commit SHA; carry forward concurrent 7.1 update") alongside its own 7.O changes, then pushed to `origin/tranche/5-3`. This cycle's subsequent `git fetch && git rebase` picked up `49a5437` as already containing this cycle's exact intended `progress.md` content (verified byte-identical to what this cycle had staged). No conflict, no rework needed — this cycle's own commit (`78f776d`: this receipt file, plus the commit-SHA/card-ID backfill in `progress.md`) lands as a small follow-up on top of `49a5437`, matching the established self-reference-backfill pattern already used by criteria 6.1 and 7.O in this bundle's own history.
- **Discovery forwards:** one grouped `## DISCOVERED` entry (9 sub-rows) added — see `progress.md`'s `## DISCOVERED` section, "Criterion 7.1 (register A6 carry-forward...)". Suggested criteria 7.2 (Cleric), 7.3 (Rogue), 7.4 (Sorcerer), 7.5 (Barbarian), 7.6 (Bard), 7.7 (Druid), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger).
- **Next-cycle plan:** each of 7.2–7.10 is a single-file-grant cycle (`level_up/<class>.rs`) that (a) confirms live whether `pilot_compute.rs` grounds any `class_spell.<class>.*` (or other mis-filtered) explanation id for that class, (b) if so, RED = a failing test asserting the missing explanation surfaces in `LevelUpPlan.automatic_features`, GREEN = widening the class's filter (mirroring `wizard.rs`'s fix) to admit the missing prefix, (c) for the 3 non-caster classes (Rogue 7.3, Barbarian 7.5, Monk 7.8) the audit may legitimately close with "no gap found" if no `class_spell.<class>.*` id exists in `pilot_compute.rs` at audit time — that is a valid GREEN outcome, not a failure to find one. The orchestrator should also consider, outside 7.1's own scope, a housekeeping pass to consolidate/prune the now-12-entry `## DISCOVERED` section before further Epic 7/8 dispatch (see Notes).
