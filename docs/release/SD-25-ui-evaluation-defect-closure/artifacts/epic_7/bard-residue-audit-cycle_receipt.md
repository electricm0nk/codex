# Cycle bard-residue-audit — Epic 7 (Deferred Per-Class Work & Coverage Backlog) / Criterion 7.6

- **Card ID:** t_f2801389
- **Commit SHA:** `<pending — see push section of this receipt / progress.md log>`
- **Files touched:**
  - `src/rules_core/level_up/bard.rs` (functional fix: added `BARD_RECOGNITION_ID` whitelist entry to `append_class_feature_grants`'s filter, plus a matching `friendly_name` trim — see Notes)
  - NEW `tests/sd25_bard_level_up_explanation_coverage.rs` (the RED→GREEN audit test)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/bard-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + register A6 update, follow-up commit per `§5`'s protocol)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own staged diff (`git diff --unified=0 --cached` against the canonical `loop-instruction.md §6` audited glob list). Zero hits in `src/**/*.rs` (the only glob this cycle's code change falls under). The new test file (`tests/sd25_bard_level_up_explanation_coverage.rs`) sits outside the canonical audited glob entirely (mirrors the `barbarian-residue-audit` / `sorcerer-residue-audit` precedent); checked informationally anyway — its only `sd\d+_`/`Sd\d+`-shaped hits are real `tests/sd24_*`/`tests/sd25_*` file citations in doc comments and the `sd25_bard_residue_audit` test-fixture `case_id`/`source_package_id` string literals, not bundle-tag leaks in shipping code.
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own staged diff: zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` in `bard.rs`.
- **Acceptance criterion (verbatim, `progress.md`'s `## DISCOVERED` register A6 sub-row for Bard):** "**Bard** — `src/rules_core/level_up/bard.rs` — filter admits `class_chassis.bard.` / `class_feature.bard.` only — no `class_spell.bard.` prefix. `pilot_compute.rs:17445` grounds `class_spell.bard.spontaneous_known_and_per_day.unsupported`, silently dropped today. suggested criterion: **7.6**." Dispatcher's brief for this cycle: audit `bard.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **RED → GREEN evidence:**
  - **Intake claim independently re-verified, found inaccurate (same shape as Cleric's 7.2 resolution):** read `explain_bard_level1_spell_baseline` end-to-end in `src/rules_core/pilot_compute.rs`. The cited id, `class_spell.bard.spontaneous_known_and_per_day.unsupported` (line 17445), is pushed via `diagnostics.push(...)` — a `ComputationDiagnostic` on the structurally separate `diagnostics` `Vec`, never merged into `.explanations`. `bard_chassis_explanations` (`bard.rs`'s own probe helper) reads only `.explanations`, so this id is structurally unreachable by `append_class_feature_grants` regardless of what prefix the filter admits — not a live gap, and not the Wizard bug shape. Same true of the sibling diagnostic `class_feature.bard.bardic_performance_execution.unsupported` (line 17125), also checked.
  - **A different, real bug found by direct comparison against Wizard's/Sorcerer's already-fixed reference shape:** `pilot_compute.rs` pushes a `"class_chassis.spell_baseline.bard"` recognition explanation (a bounded +0 "Recognized deterministic Human Bard ... spell-bearing baseline" record — the identical shape as `WIZARD_RECOGNITION_ID`/`SORCERER_RECOGNITION_ID`) onto `.explanations` at line 16512. That id's second segment is `spell_baseline`, not `bard`, so it never matches either of `bard.rs`'s two admitted prefixes (`"class_chassis.bard."` / `"class_feature.bard."`). Unlike Wizard's own bug (a later-landing grounding whose prefix was never added), `wizard.rs` and `sorcerer.rs` both already carry an explicit `== <CLASS>_RECOGNITION_ID` whitelist entry for this exact non-prefix-matching id; `bard.rs` was the one module in the reference set missing it. Confirmed the effect: on the 0 -> 1 bard-level transition (initial class entry), the recognition id changes `None -> Some(0)` — a genuine change under this module's own diffing rule — yet produced no `Grant`.
  - **RED test written:** `tests/sd25_bard_level_up_explanation_coverage.rs`, four tests:
    1. `every_changed_bard_explanation_produces_a_grant` — sweeps `from_level` 0 through 19 (deliberately starting at 0, not 1, to exercise the initial-class-entry transition where the recognition id changes) and asserts every real changed, filter-admitted Bard explanation surfaces as a matching `Grant` or `ResourcePoolDelta`.
    2. `bard_recognition_explanation_produces_a_grant_on_first_level` — names the recognition id explicitly by id, mirroring `tests/sd24_wizard_level_up_spell_coverage.rs`'s own named-id assertions.
    3. `bardic_performance_execution_diagnostic_never_leaks_into_explanations_or_grants` — verified-negative-finding regression proof for the first diagnostic id.
    4. `spontaneous_known_and_per_day_diagnostic_never_leaks_into_explanations_or_grants` — verified-negative-finding regression proof for the intake-cited diagnostic id.
  - **Confirmed RED against the pre-fix module:** ran `cargo test --test sd25_bard_level_up_explanation_coverage` against `bard.rs` before adding the `BARD_RECOGNITION_ID` whitelist entry: **FAILED** — `2 passed; 2 failed`, with `every_changed_bard_explanation_produces_a_grant` and `bard_recognition_explanation_produces_a_grant_on_first_level` both failing with `explanation 'class_chassis.spell_baseline.bard' changed from None to 0 but no matching Grant ... was found`, proving the test catches the bug and is not vacuous.
  - **Applied the fix**, mirroring `wizard.rs`'s `WIZARD_RECOGNITION_ID` / `sorcerer.rs`'s `SORCERER_RECOGNITION_ID` exactly:
    - Added `const BARD_RECOGNITION_ID: &str = "class_chassis.spell_baseline.bard";`.
    - Widened `append_class_feature_grants`'s filter: `is_bard_class_feature_id = to_explanation.id == BARD_RECOGNITION_ID || to_explanation.id.starts_with("class_chassis.bard.") || to_explanation.id.starts_with("class_feature.bard.")`.
    - Added `.trim_start_matches("class_chassis.spell_baseline.")` to `friendly_name`, mirroring `wizard.rs`'s/`sorcerer.rs`'s own identical trim (so the recognition grant's display name reads `"bard"` rather than the un-trimmed dotted id).
  - **Re-ran: GREEN** — `cargo test --test sd25_bard_level_up_explanation_coverage`: `test result: ok. 4 passed; 0 failed`.
- **Test results:**
  - `cargo test --test sd25_bard_level_up_explanation_coverage`: 4 passed, 0 failed.
  - Full pre-existing Bard test surface (regression safety, all 31 Bard-named `tests/*.rs` binaries run explicitly by name via `find tests -iname '*bard*'`): `sd13_bard_base_attack_and_saves`, `sd13_bard_bonus_spells`, `sd13_bard_fascinate_dc`, `sd13_bard_level10_progression`, `sd13_bard_level1_spell_baseline`, `sd13_bard_level2_progression` .. `sd13_bard_level9_progression`, `sd13_bard_spell_level_thresholds`, `sd13_bard_spell_save_dcs`, `sd13_bard_spells_known_counts`, `sd13_bard_spells_per_day_counts`, `sd13_bard_total_spells_per_day`, `sd13_bard_versatile_performance_slots`, `sd18_bard_level11_inspire_widening` .. `sd18_bard_level20_widening`, `sd20_levelup_bard`, `sd25_bard_level_up_explanation_coverage` (this cycle's own) — all 0 failed across 31 binaries (sum well over 300 assertions).
  - `cargo test --lib rules_core::level_up` (multiclass dispatch unit tests): 3 passed, 0 failed, unaffected (no dispatch code touched).
  - `cargo build --tests` (full workspace): clean build, no warnings introduced.
- **Notes:**
  - This audit's scope per the file-touch grant was `src/rules_core/level_up/bard.rs` only (plus its own test file/module) — respected; no sibling class file touched, `pilot_compute.rs` read-only.
  - This is a genuine, real fix — not a negative finding. The intake register's own characterization of the suspect id was inaccurate (it named a diagnostic, which is structurally unreachable regardless of prefix, exactly like Cleric's 7.2 resolution) — but comparing `bard.rs` directly against Wizard's/Sorcerer's already-fixed reference shape (not just re-checking the intake's own cited id) surfaced a real, different gap: the missing recognition-id whitelist entry. This confirms 7.2's own forwarded correction ("re-verify per-class whether the cited id is a diagnostic or an explanation before assuming the same bug shape applies — do not trust the prefix-string match alone") while also showing that a clean bill on the *cited* id does not guarantee a clean bill on the *whole* filter — the fuller reference-module diff was what surfaced this.
  - No functional behavior beyond the recognition grant changes: the recognition grant carries value 0 (a "+0 recognition record" per its own detail text) and fires exactly once, on the transition into a bard's first supported level — it does not affect any other grant, resource pool, or the capstone-threshold flag.
- **Discovery forwards:** none new. This resolves (rather than adds to) the existing register A6 Bard sub-row; the sub-row's own characterization is corrected in `progress.md` alongside the resolution note (mirroring Cleric's 7.2 precedent format).
- **Next-cycle plan:** the next per-class residue cycle should pick up whichever of 7.7 (Druid), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger) is still unclaimed. Per this cycle's own finding, each remaining spellcasting/partial-casting class (Druid, Paladin, Ranger) should be checked not only for the intake's cited `class_spell.<class>.*`-prefix suspicion, but also — independently — for whether it carries its own `<CLASS>_RECOGNITION_ID`-shaped whitelist entry for a `"class_chassis.spell_baseline.<class>"` id, mirroring this cycle's own two-part check (diagnostic-vs-explanation AND full-filter-vs-reference-module comparison), rather than re-checking only the specific id the intake happened to name.
