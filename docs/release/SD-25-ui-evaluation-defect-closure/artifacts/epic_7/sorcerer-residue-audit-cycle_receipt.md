# Cycle 7.4 — Epic 7 (Per-class residue) / Criterion 7.4

- **Card ID:** (to be backfilled after kanban mint — see Notes)
- **Commit SHA:** (to be backfilled after commit+push — see Notes)
- **Files touched:**
  - NEW `tests/sd25_sorcerer_level_up_explanation_coverage.rs` (RED → GREEN regression proof + verified-negative-finding documentation)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/sorcerer-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + `## DONE`, in a follow-up commit per `§5`'s protocol)
  - **`src/rules_core/level_up/sorcerer.rs` — NOT touched.** The file-touch grant covers it, but no bug was found there (see Notes/Acceptance below) — per the brief's own instruction, "do not invent a fix for a bug that isn't there."
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff (`git diff --unified=0 --cached` against the audited globs `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`); the new test file lives under `tests/`, outside every audited glob, and no `src/**/*.rs` file was changed.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` anywhere in the new test file.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` entry, Sorcerer sub-row):** "`src/rules_core/level_up/sorcerer.rs` — filter is `SORCERER_EXPLANATION_PREFIX = "class_chassis.sorcerer."` — no `class_spell.sorcerer.` prefix admitted. `pilot_compute.rs:14334` grounds `class_spell.sorcerer.spontaneous.unsupported`, silently dropped today. suggested criterion: **7.4**." Dispatcher's brief for this cycle: audit `sorcerer.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **Notes:**
  - **Verified finding: the 7.1 intake's characterization for Sorcerer is factually incorrect, and there is no live bug — mirrors Cleric's own (7.2) and Rogue's own (7.3) findings exactly, per Cleric's own forward-note advising each sibling cycle to independently re-verify rather than assume.** Read `pilot_compute.rs`'s `explain_sorcerer_level1_spell_baseline` (the sole function that grounds any Sorcerer explanation) end-to-end (lines 13689-14349) and traced every `id: "..."` it emits. All real facts (`class_chassis.spell_baseline.sorcerer` recognition, `base_attack_bonus`, `base_save.fortitude/reflex/will`, `eschew_materials`, `bloodline_choice`, `bloodline_class_skill_choice`, and every `spontaneous.spell_level_access` / `spontaneous.base_spells_per_day.spell_level_N` / `spontaneous.spell_save_dc.spell_level_N` / `spontaneous.spells_known.spell_level_N` / `spontaneous.bonus_spells_per_day.spell_level_N` / `spontaneous.total_spells_per_day.spell_level_N` record) are pushed via `explanations.push(...)` and carry either the exact recognition id `class_chassis.spell_baseline.sorcerer` or the `class_chassis.sorcerer.` prefix — the identical set `sorcerer.rs`'s current `SORCERER_EXPLANATION_PREFIX`/`SORCERER_RECOGNITION_ID` already admits. Two ids do carry a DIFFERENT prefix (`class_spell.sorcerer.spontaneous.unsupported` at `pilot_compute.rs:14334`, and `class_feature.sorcerer.arcane_bond_and_bloodline_progression.unsupported` at `pilot_compute.rs:13928`), but both are pushed via `diagnostics.push(...)` — a structurally separate `Vec<ComputationDiagnostic>`, never merged into `explanations` anywhere in `compute_pilot_base_chassis`. `sorcerer.rs`'s own `sorcerer_chassis_explanations` helper reads only `.explanations`. Therefore neither diagnostic id is reachable by `append_class_feature_grants` today, regardless of what prefix the filter admits — there is nothing for a wider filter to "catch" here, because neither id ever enters the `Vec` the filter operates over in the first place. Confirmed via `grep -rn "class_spell.sorcerer\|class_feature.sorcerer" src/` that no OTHER Sorcerer-namespaced explanation id exists anywhere in the source tree.
  - **Why this differs from the real Wizard bug (verified by re-reading `wizard.rs` and its own SD-24 fix as the reference pattern, per the dispatch brief):** Wizard's dropped ids (`class_spell.wizard.spellbook_contents`, `.daily_preparation`, `.base_spells_per_day.spell_level_N`, etc.) were real `ComputationExplanation` records — genuinely pushed onto `.explanations` by `ground_wizard_prepared_spellbook` once its posture was met — that `wizard.rs`'s filter (at the time) simply didn't recognize by prefix. That is a real silent-drop: the fact reached `.explanations` and was thrown away by the filter. Sorcerer's two `.unsupported` ids never reach `.explanations` at all; they are, and always were, `ComputationDiagnostic`s (the honest "this burden is still unproven" marker), which `sorcerer.rs` was never wired to read in the first place. Widening `SORCERER_EXPLANATION_PREFIX` to admit `"class_spell.sorcerer."` or `"class_feature.sorcerer."` would therefore have **zero observable effect today** — and doing it anyway would be actively wrong per the no-fabrication instruction: it would create a latent trap where a future refactor that accidentally started threading diagnostic-shaped ids through the explanation path could get silently turned into a fabricated `Grant` (reporting a burden that is explicitly claim-blocked-as-unproven as though it were a granted mechanical fact). The correct fix for a diagnostic-vs-explanation confusion is a regression test proving the separation holds, not a filter widening.
  - **RED → GREEN, performed for real (not skipped):** RED = temporarily narrowed `SORCERER_EXPLANATION_PREFIX` from `"class_chassis.sorcerer."` to `"class_chassis.sorcerer.eschew"` directly in `src/rules_core/level_up/sorcerer.rs` (backed up first: `cp sorcerer.rs /tmp/sorcerer_backup.rs`) to simulate the real Wizard bug shape (a filter that fails to admit real, changed pillar ids — chose a narrowing that specifically excludes the level-varying `spontaneous.*` records, not just the flat/never-changing `eschew_materials`/`bloodline_choice` records, so the injected regression is actually exercised by a value that changes across levels). `cargo test --test sd25_sorcerer_level_up_explanation_coverage` then failed 2 of 4 tests exactly as expected: `every_changed_sorcerer_explanation_produces_a_grant` (`spontaneous.base_spells_per_day.spell_level_1` changed level 1→2 with zero matching grant) and `known_sorcerer_pillars_each_produce_a_grant_across_their_own_documented_threshold` (`spell_level_access` at level 3→4). GREEN = restored the real, unmodified `sorcerer.rs` from the backup (byte-identical `diff` confirmed) — all 4 tests pass. This proves the test suite **would** have caught the Wizard-shaped bug had one existed in Sorcerer, and that it passes cleanly against the real, currently-correct module — a verified negative finding, not a skipped check.
  - **Test suite added (`tests/sd25_sorcerer_level_up_explanation_coverage.rs`, 4 tests):**
    1. `every_changed_sorcerer_explanation_produces_a_grant` — generalized completeness proof (not a hand-picked allowlist): walks every adjacent level 1→20 transition, diffs `compute_pilot_base_chassis`'s `.explanations` by id, and asserts every Sorcerer-pillar id whose value changed produces a matching `Grant` (either directly by explanation id, or — for the 4 class-table-covered ids — under `append_class_table_grants`'s own `base_attack_bonus`/`fort_save`/`ref_save`/`will_save` column keys, confirmed separately by `tests/sd20_levelup_sorcerer.rs`). This is the test shape that generalizes catching any future Wizard-shaped regression, not just today's specific suspect ids.
    2. `known_sorcerer_pillars_each_produce_a_grant_across_their_own_documented_threshold` — names the concrete real pillars (`spell_level_access`, `base_spells_per_day.spell_level_2`, `spells_known.spell_level_2`, `spell_save_dc.spell_level_2`, `total_spells_per_day.spell_level_2` at the level 3→4 2nd-spell-level-access threshold; `spells_known.spell_level_0` at level 1→2; all 4 class-table-covered pillars found dynamically across the full range) and confirms each produces a grant at its own real documented rise.
    3. `spontaneous_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — the verified-negative-finding proof for the 7.1-flagged suspect id: across all 20 supported levels, confirms the diagnostic stays present and claim-blocking, never appears in `.explanations`, and never appears as a `Grant.source_table.column_key` in any `LevelUpPlan`.
    4. `arcane_bond_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — the same structural proof applied to the second candidate diagnostic id found during this cycle's own read of `pilot_compute.rs` (not flagged by 7.1's intake, but the identical shape).
  - **Test results (final, GREEN, real module):** `cargo test --test sd25_sorcerer_level_up_explanation_coverage` → `4 passed; 0 failed`. Also re-ran the full pre-existing Sorcerer surface for regression safety: `cargo test --test sd20_levelup_sorcerer --test sd18_sorcerer_level20_widening --test sd13_sorcerer_level1_spell_baseline --test sd25_sorcerer_level_up_explanation_coverage` → all pass (4 + 14 + (sd13 suite) + 4, 0 failed).
  - **File-touch grant note:** the grant named `src/rules_core/level_up/sorcerer.rs only (plus its own test file/module)` was interpreted as covering a new top-level `tests/sd25_*.rs` integration test file, mirroring the established convention this crate already uses for every `level_up/<class>.rs` (`tests/sd20_levelup_sorcerer.rs`, `tests/sd24_wizard_level_up_spell_coverage.rs`, `tests/sd25_cleric_level_up_explanation_coverage.rs`) — none of these per-class level-up tests live as an inline `#[cfg(test)] mod tests` inside the `level_up/<class>.rs` file itself. No sibling class's file was read or touched beyond `pilot_compute.rs` (read-only, to trace explanation/diagnostic provenance).
  - **Concurrent-write note:** this cycle's initial fetch+rebase (start of cycle) landed on top of `41d7ec8` (7.3 Rogue progress.md update); a second fetch immediately before this receipt's own commit found `origin/tranche/5-3` had advanced further (`009abfc`/`59a772d`/`d2ebe43` — the 7.2 Cleric residue-audit cycle landing concurrently), rebased cleanly with zero conflicts (this cycle's file-touch set — `tests/sd25_sorcerer_level_up_explanation_coverage.rs` and this receipt — never overlapped Cleric's).
- **Discovery forwards:** none new. Confirms Cleric's (7.2) own forward-note: the 7.1 intake's blanket "`.unsupported` diagnostic ⇒ same Wizard bug shape" characterization does NOT hold for Sorcerer either — the remaining sibling cycles (7.5 Barbarian, 7.6 Bard, 7.7 Druid, 7.8 Monk, 7.9 Paladin, 7.10 Ranger) should continue to independently re-verify per class (read the class's own `explain_<class>_level1_...` function directly) rather than trust the intake's prefix-string pattern-match alone.
- **Next-cycle plan:** 7.5 (Barbarian), 7.6 (Bard), 7.7 (Druid), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger) remain to audit, each against Wizard's/Cleric's/Rogue's/this cycle's method: read the class's own `explain_<class>_level1_...` function in `pilot_compute.rs` directly, classify every candidate id as `ComputationExplanation` (real, filter-relevant) vs. `ComputationDiagnostic` (never filter-relevant), and only widen the class's filter if a real explanation-side gap is found.
