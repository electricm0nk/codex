# Cycle 7.7 — Epic 7 (Per-class residue) / Criterion 7.7

- **Card ID:** `t_TBD` (see Notes — minted after commit; backfilled if the receipt lands before the kanban call completes)
- **Commit SHA:** `TBD` (filled in at commit time below)
- **Files touched:**
  - NEW `tests/sd25_druid_level_up_explanation_filter_audit.rs` (RED → GREEN regression proof + verified-negative-finding documentation)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/druid-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + `## DONE`, in a follow-up commit per `§5`'s protocol)
  - **`src/rules_core/level_up/druid.rs` — NOT touched.** The file-touch grant covers it, but no bug was found there (see Notes/Acceptance below) — per the brief's own instruction, "do not invent a fix for a bug that isn't there."
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff (`git diff --unified=0 --cached` against the audited globs `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`); the new test file lives under `tests/`, outside every audited glob, and no `src/**/*.rs` file was changed.
- **Wired-integration audit result:** `OK_NO_TOKENS` — same scoping; zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` anywhere in the new test file.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` entry, Druid sub-row):** "**Druid** — `src/rules_core/level_up/druid.rs` — `is_druid_pillar_id` admits `class_chassis.druid.` / `class_feature.druid.` only — no `class_spell.druid.` prefix. `pilot_compute.rs:16365` grounds `class_spell.druid.prepared_divine.unsupported`, silently dropped today. suggested criterion: **7.7**." Dispatcher's brief for this cycle: audit `druid.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **Notes:**
  - **Verified finding: the 7.1 intake's characterization for Druid is factually incorrect, and there is no live bug — mirrors Cleric's (7.2), Rogue's (7.3), and Sorcerer's (7.4) own findings exactly**, per Cleric's own forward-note advising each sibling cycle to independently re-verify rather than assume the same bug shape applies. Read `pilot_compute.rs`'s `explain_druid_level1_spell_baseline` (the sole function that grounds any Druid explanation, lines 15978-16374) end-to-end and traced every `id: "..."` it emits. All real facts (`class_chassis.spell_baseline.druid` recognition, `base_attack_bonus`, `base_save.fortitude/reflex/will`, `wild_empathy`, `nature_sense`, `nature_bond_choice`, `woodland_stride`, `trackless_step`, `resist_natures_lure`, `venom_immunity`, `a_thousand_faces`, `timeless_body`) are pushed via `explanations.push(...)` and carry either the exact recognition id or the `class_chassis.druid.`/`class_feature.druid.` prefixes — the identical set `druid.rs`'s current `is_druid_pillar_id` already admits. Two ids do carry a DIFFERENT prefix (`class_feature.druid.animal_companion.unsupported` at `pilot_compute.rs:16342`, and `class_spell.druid.prepared_divine.unsupported` at `pilot_compute.rs:16365` — the exact id the 7.1 intake cites), but both are pushed via `diagnostics.push(...)` — a structurally separate `Vec<ComputationDiagnostic>`, never merged into `explanations` anywhere in `compute_pilot_base_chassis`. `druid.rs`'s own `druid_chassis_explanations` helper reads only `.explanations`. Therefore the cited diagnostic id is not reachable by `append_druid_chassis_grants` today, regardless of what prefix the filter admits — there is nothing for a wider filter to "catch" here, because the id never enters the `Vec` the filter operates over in the first place. Confirmed via `grep -n '"class_spell\.druid' src/rules_core/pilot_compute.rs` that this is the ONLY `class_spell.druid`-prefixed id anywhere in the source tree, and via `grep -n 'ground_druid_prepared_spellbook'` that (unlike Wizard's SD-21 E6b.2 grounding) no later-landing real Druid spellbook grounding exists anywhere in this codebase — Druid's prepared divine spell posture has simply never been grounded, full stop, so there is no real fact for any filter to have ever dropped.
  - **Why this differs from the real Wizard bug** (verified by re-reading `wizard.rs` and its own SD-24 fix as the reference pattern, per the dispatch brief): Wizard's dropped ids (`class_spell.wizard.spellbook_contents`, `.daily_preparation`, `.base_spells_per_day.spell_level_N`, etc.) were real `ComputationExplanation` records — genuinely pushed onto `.explanations` by `ground_wizard_prepared_spellbook` once its posture was met — that `wizard.rs`'s filter (at the time) simply didn't recognize by prefix. That is a real silent-drop: the fact reached `.explanations` and was thrown away by the filter. Druid's `class_spell.druid.prepared_divine.unsupported` never reaches `.explanations` at all; it is, and always was, a `ComputationDiagnostic` (the honest "this burden is still unproven" marker, kept claim-blocking by `tests/sd13_druid_level1_spell_baseline.rs`'s own `druid_level1_stays_blocked_on_prepared_divine_spell_posture_burden` test), which `druid.rs` was never wired to read in the first place. Widening `is_druid_pillar_id` to admit `"class_spell.druid."` would therefore have **zero observable effect today** — and doing it anyway would be actively wrong per the no-fabrication instruction: it would create a latent trap where a future refactor that accidentally started threading diagnostic-shaped ids through the explanation path could get silently turned into a fabricated `Grant` (reporting a burden that is explicitly claim-blocked-as-unproven as though it were a granted mechanical fact).
  - **Second, independently-surfaced finding (not in the 7.1 intake, found by this cycle's own comprehensive sweep, also not a bug):** `class_chassis.druid.nature_sense` DOES pass `is_druid_pillar_id` (matches the real, admitted `class_chassis.druid.` prefix) but never itself produces a grant anywhere across the full level 1..=15 sweep, because Nature Sense is a flat, level-independent +2 (PF1 Core Rulebook) already granted at Druid level 1 — there is no `from_level >= 1 -> to_level` transition this codebase models in which its value or presence ever changes (the identical structural boundary the flat recognition id hits: both are "granted at character creation," not "granted by leveling up"). `tests/sd20_levelup_druid.rs`'s own `druid_level_1_to_2_grants_bab_rise_fort_will_rise_and_woodland_stride` test already asserts this exact behavior is intentional ("nature sense is flat and level-independent; it must not re-fire on every level"). Verified (not assumed) by a dedicated test confirming the raw `pilot_compute.rs` value is constant `2` at every level 1-15. Correctly-included, correctly-inert — not a silent drop.
  - **RED → GREEN, performed for real (not skipped):** RED = temporarily narrowed `is_druid_pillar_id` in `src/rules_core/level_up/druid.rs` from `id.starts_with("class_chassis.druid.") || id.starts_with("class_feature.druid.")` to `id.starts_with("class_chassis.druid.")` only (backed up first: `cp druid.rs /tmp/druid.rs.bak`) to simulate the real Wizard bug shape (a filter that fails to admit a real, changed pillar prefix). `cargo test --test sd25_druid_level_up_explanation_filter_audit` then failed exactly as expected: the coverage test reported 6 silently-dropped ids (`class_feature.druid.a_thousand_faces`, `.resist_natures_lure`, `.timeless_body`, `.trackless_step`, `.venom_immunity`, `.woodland_stride` — every level-gated class feature). GREEN = restored the real, unmodified `druid.rs` from the backup (byte-identical `diff` confirmed) — all 4 tests pass. This proves the test suite **would** have caught the Wizard-shaped bug had one existed in Druid, and that it passes cleanly against the real, currently-correct module — a verified negative finding, not a skipped check.
  - **Test suite added (`tests/sd25_druid_level_up_explanation_filter_audit.rs`, 4 tests):**
    1. `every_real_druid_explanation_id_survives_the_level_up_filter_except_the_documented_recognition_exclusion` — generalized completeness proof (not a hand-picked allowlist): collects every real (non-diagnostic) explanation id `pilot_compute.rs` emits for Druid across the full level 1..=15 range directly from the public `compute_pilot_base_chassis` seam, drives every adjacent level transition through the public `compute_druid_level_up_grants` entry point, and asserts every real id (except the two documented, separately-verified exclusions) surfaces as a grant's source id at least once. This is the test shape that generalizes catching any future Wizard-shaped regression, not just today's specific suspect id.
    2. `the_flat_recognition_only_id_is_deliberately_never_granted` — confirms the flat +0 recognition id's exclusion is real and deliberate (present in the raw explanation set, absent from every granted set).
    3. `the_flat_nature_sense_id_passes_the_filter_but_is_verified_constant_across_the_whole_range` — the second finding's own verification: confirms Nature Sense's value is truly constant (2) at every level 1-15, justifying its exclusion from the "must eventually be granted" requirement rather than assuming it.
    4. `the_unsupported_prepared_divine_spell_diagnostic_is_never_read_as_an_explanation` — the verified-negative-finding proof for the 7.1-flagged suspect id: confirms it never appears in `.explanations` anywhere in the source tree, and stays a claim-blocking diagnostic.
  - **Test results (final, GREEN, real module):** `cargo test --test sd25_druid_level_up_explanation_filter_audit` → `4 passed; 0 failed`. Also re-ran the full pre-existing Druid surface for regression safety: `cargo test --test sd20_levelup_druid --test sd13_druid_base_attack_and_saves --test sd13_druid_level1_spell_baseline --test sd13_druid_level2_progression --test sd13_druid_level3_progression --test sd13_druid_level4_progression --test sd13_druid_level5_progression --test sd13_druid_level6_progression --test sd13_druid_level7_progression --test sd13_druid_level8_progression --test sd13_druid_level9_progression --test sd13_druid_level10_progression --test sd18_druid_level11_widening --test sd18_druid_level12_widening --test sd18_druid_level13_widening --test sd18_druid_level14_widening --test sd18_druid_level15_widening --test sd25_druid_level_up_explanation_filter_audit` → all pass, 0 failed.
  - **File-touch grant note:** the grant named `src/rules_core/level_up/druid.rs only (plus its own test file/module)` was interpreted as covering a new top-level `tests/sd25_*.rs` integration test file, mirroring the established convention this crate already uses for every `level_up/<class>.rs` (`tests/sd20_levelup_druid.rs`, `tests/sd24_wizard_level_up_spell_coverage.rs`, `tests/sd25_sorcerer_level_up_explanation_coverage.rs`) — none of these per-class level-up tests live as an inline `#[cfg(test)] mod tests` inside the `level_up/<class>.rs` file itself. No sibling class's file was read or touched beyond `pilot_compute.rs` (read-only, to trace explanation/diagnostic provenance) and `wizard.rs` (read-only, reference pattern).
  - **Concurrent-write note:** this cycle's initial fetch+rebase landed cleanly on `origin/tranche/5-3` at `33a6d4f` (7.4 Sorcerer progress.md update) with zero conflicts before any of this cycle's own work began.
- **Discovery forwards:** none new. Confirms Cleric's/Sorcerer's/Barbarian's own forward-note: the 7.1 intake's blanket "`.unsupported` diagnostic ⇒ same Wizard bug shape" characterization does NOT hold for Druid either — the remaining sibling cycles (7.6 Bard, 7.8 Monk, 7.9 Paladin, 7.10 Ranger) should continue to independently re-verify per class (read the class's own `explain_<class>_level1_...` function directly, classify each candidate id as `ComputationExplanation` vs. `ComputationDiagnostic`) rather than trust the intake's prefix-string pattern-match alone.
- **Next-cycle plan:** 7.5 (Barbarian) already complete per `progress.md`. Remaining: 7.6 (Bard), 7.8 (Monk), 7.9 (Paladin), 7.10 (Ranger), each against Wizard's/Cleric's/Sorcerer's/Barbarian's/this cycle's method.
