# Cycle paladin-residue-audit — Epic 7 (Deferred Per-Class Work & Coverage Backlog) / Criterion 7.9

- **Card ID:** `<pending — see kanban section of this receipt / progress.md log>`
- **Commit SHA:** `<pending — see push section of this receipt / progress.md log>`
- **Files touched:**
  - `src/rules_core/level_up/paladin.rs` (functional fix: added `PALADIN_HYBRID_BASELINE_RECOGNITION_ID` whitelist entry to `append_class_feature_grants`'s filter, plus a matching `friendly_name` trim and a corrective doc-comment addendum — see Notes)
  - NEW `tests/sd25_paladin_level_up_explanation_coverage.rs` (the RED→GREEN audit test)
  - NEW `docs/release/SD-25-ui-evaluation-defect-closure/artifacts/epic_7/paladin-residue-audit-cycle_receipt.md` (this file)
  - `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (status matrix + register A6 Paladin sub-row update, follow-up commit per `§5`'s protocol)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (documented exception) — scoped to this cycle's own staged diff (`git diff --unified=0 --cached` against the canonical `loop-instruction.md §6` audited glob list: `apps/desktop/**/*.ts*`, `apps/desktop/src-tauri/**/*.rs`, `src/**/*.rs`, `scripts/**/*.sh`, `scripts/**/*.py`, excluding `**/__tests__/**` and `**/*.test.*`). One hit: `paladin.rs`'s own new doc comment cites the real, existing new test file path `tests/sd25_paladin_level_up_explanation_coverage.rs` — a real `tests/`-file citation, not a bundle-tag leak, matching the exact exclusion precedent already established by criterion 1.1 and repeated verbatim by the sibling 7.2 (Cleric), 7.3 (Rogue), 7.5 (Barbarian), and 7.6 (Bard) receipts. `tests/*.rs` itself is not in the canonical audited glob at all, but the new test file's own body was checked anyway (informational): also clean (no `sd\d+_`/`Sd\d+`-shaped hits beyond the real `tests/sd25_*`/`tests/sd24_*`/`tests/sd20_*`/`tests/sd13_*` file-path citations in doc comments and the `sd25_paladin_residue_audit` test-fixture `case_id`/`source_package_id` string literals — all pointing at files verified to exist, none a stray bundle-tag literal).
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own staged diff: zero hits for `STUB|MOCK|placeholder|not yet implemented|todo|fixme|hack` in either changed file.
- **Acceptance criterion (verbatim, `progress.md`'s 7.1 `## DISCOVERED` entry, Paladin sub-row):** "**Paladin** — `src/rules_core/level_up/paladin.rs` — filter admits `class_chassis.paladin.` / `class_feature.paladin.` only — no `class_spell.paladin.` (or `class_spell.hybrid.paladin.`) prefix. `pilot_compute.rs:8592` grounds `class_spell.paladin.partial_caster.unsupported` and `pilot_compute.rs:7606` grounds `class_spell.hybrid.paladin.unsupported` — both silently dropped today. suggested criterion: **7.9**." Dispatcher's brief for this cycle: audit `paladin.rs` against Wizard's fixed module as the reference pattern, RED→GREEN, fix only if a real bug is found.
- **Status:** complete
- **RED → GREEN evidence:**
  - **Intake claim independently re-verified, found inaccurate (same shape as Cleric's 7.2 / Sorcerer's 7.4 / Barbarian's 7.5 resolutions)**: read `pilot_compute.rs` end-to-end. Both ids the 7.1 intake cited — `class_spell.paladin.partial_caster.unsupported` (line 8592, inside the main paladin chassis function) and `class_spell.hybrid.paladin.unsupported` (line 7606, inside `explain_hybrid_level1_chassis`) — are pushed via `diagnostics.push(...)`, landing on `PilotBaseChassisComputation.diagnostics` (`Vec<ComputationDiagnostic>`), a structurally separate `Vec` from `.explanations` that is never merged into it anywhere in `compute_pilot_base_chassis`. `paladin.rs`'s own `paladin_chassis_explanations` helper reads only `.explanations`, so neither cited id was ever reachable by the filter regardless of what prefix it admits — not a live gap. Also checked the sibling diagnostic `class_feature.hybrid.paladin.unsupported` (also pushed inside `explain_hybrid_level1_chassis`): same verdict.
  - **A different, real bug found by direct comparison against Wizard's/Bard's already-fixed reference shape (mirroring Bard's 7.6 finding exactly)**: `pilot_compute.rs`'s `explain_hybrid_level1_chassis` — called unconditionally from `compute_pilot_base_chassis` for every input, internally gated on `hybrid_level1_class` (matches a single-class Paladin or Ranger at EXACTLY class level 1) — pushes a bounded +0 recognition record `"class_chassis.hybrid_baseline.paladin"` onto `.explanations` for a level-1 Human Paladin (line 7603). That id's second segment is `hybrid_baseline`, not `paladin`, so it matched NEITHER of `paladin.rs`'s two admitted prefixes (`"class_chassis.paladin."` / `"class_feature.paladin."`). Confirmed the effect: on the 0 -> 1 paladin-level transition (initial class entry), the id changes `None -> Some(0)` — a genuine change under this module's own diffing rule — yet produced no `Grant`. This is the exact Wizard SD-24 bug shape, just on a hybrid-baseline recognition id rather than a `class_spell.*` value family, and structurally identical to Bard's own 7.6 finding (a recognition id shaped like `WIZARD_RECOGNITION_ID`/`SORCERER_RECOGNITION_ID`/`BARD_RECOGNITION_ID` that the module's filter never whitelisted).
  - **RED test written:** `tests/sd25_paladin_level_up_explanation_coverage.rs`, four tests:
    1. `every_changed_paladin_explanation_produces_a_grant` — sweeps `from_level` 0 through 19 (deliberately starting at 0, not 1, to exercise the initial-class-entry transition where the hybrid-baseline recognition id changes) and asserts every real changed, filter-admitted Paladin explanation surfaces as a matching `Grant` or `ResourcePoolDelta`.
    2. `hybrid_baseline_recognition_explanation_produces_a_grant_on_first_level` — names the recognition id explicitly by id, mirroring `tests/sd24_wizard_level_up_spell_coverage.rs`'s / `tests/sd25_bard_level_up_explanation_coverage.rs`'s own named-id assertions.
    3. `partial_caster_unsupported_diagnostic_never_leaks_into_explanations_or_grants` — verified-negative-finding regression proof for the intake-cited `class_spell.paladin.partial_caster.unsupported` diagnostic.
    4. `hybrid_spell_and_feature_unsupported_diagnostics_never_leak_into_explanations_or_grants` — verified-negative-finding regression proof for the intake-cited `class_spell.hybrid.paladin.unsupported` diagnostic and its sibling `class_feature.hybrid.paladin.unsupported`.
  - **Confirmed RED against the pre-fix module:** ran `cargo test --test sd25_paladin_level_up_explanation_coverage` against `paladin.rs` before adding the `PALADIN_HYBRID_BASELINE_RECOGNITION_ID` whitelist entry: **FAILED** — `2 passed; 2 failed`, with `every_changed_paladin_explanation_produces_a_grant` and `hybrid_baseline_recognition_explanation_produces_a_grant_on_first_level` both failing (`explanation 'class_chassis.hybrid_baseline.paladin' changed from None to 0 but no matching Grant ... was found`), proving the test catches the bug and is not vacuous. The two verified-negative diagnostic tests already passed pre-fix, as expected (they test a structurally-unreachable-regardless-of-fix property).
  - **Applied the fix**, mirroring `wizard.rs`'s `WIZARD_RECOGNITION_ID` / `bard.rs`'s `BARD_RECOGNITION_ID` exactly:
    - Added `const PALADIN_HYBRID_BASELINE_RECOGNITION_ID: &str = "class_chassis.hybrid_baseline.paladin";`.
    - Widened `append_class_feature_grants`'s filter: `is_paladin_class_feature_id = to_explanation.id == PALADIN_HYBRID_BASELINE_RECOGNITION_ID || to_explanation.id.starts_with("class_chassis.paladin.") || to_explanation.id.starts_with("class_feature.paladin.")`.
    - Added `.trim_start_matches("class_chassis.hybrid_baseline.")` to `friendly_name`, mirroring `wizard.rs`'s/`bard.rs`'s own identical trim (so the recognition grant's display name reads `"paladin"` rather than the un-trimmed dotted id).
    - Added an `SD-25 Epic 7 correction` doc-comment addendum (mirroring `wizard.rs`'s own SD-24 correction addendum) recording the finding and the two verified-negative intake ids.
  - **Re-ran: GREEN** — `cargo test --test sd25_paladin_level_up_explanation_coverage`: `test result: ok. 4 passed; 0 failed`.
- **Test results:**
  - `cargo test --test sd25_paladin_level_up_explanation_coverage`: 4 passed, 0 failed.
  - Full pre-existing Paladin test surface (regression safety, all 29 Paladin-named `tests/*.rs` binaries run explicitly by name): `sd13_paladin_base_attack_and_saves`, `sd13_paladin_bonus_spells`, `sd13_paladin_level10_progression`, `sd13_paladin_level1_chassis_and_spell_burden_separation`, `sd13_paladin_level2_lay_on_hands_divine_grace`, `sd13_paladin_level3_mercy`, `sd13_paladin_level4_progression` .. `sd13_paladin_level9_progression`, `sd13_paladin_mercies_two_and_three`, `sd13_paladin_partial_caster_effective_caster_level`, `sd13_paladin_spell_level_thresholds`, `sd13_paladin_spell_save_dcs`, `sd13_paladin_spells_per_day_counts`, `sd13_paladin_total_spells_per_day`, `sd18_paladin_level11_aura_of_justice` .. `sd18_paladin_level20_widening`, `sd20_levelup_paladin` — all 0 failed (well over 150 assertions).
  - `cargo build --lib` and `cargo test --lib`: clean, 157 passed, 0 failed.
  - `cargo test --tests` (full workspace integration-test sweep): only 2 failures, both pre-existing and already documented in `progress.md`'s `## DISCOVERED` section (`tests/sd24_wired_integration_audit.rs`'s `no_would_strings_in_shipping_source` / `no_zero_tolerance_forbidden_tokens_in_shipping_source`, tripped by `stub_adapter.rs` / `characterHub/*.rs`'s honest "Would render... not yet implemented" strings — confirmed unrelated to and not touched by this cycle's diff, which changes only `src/rules_core/level_up/paladin.rs` and one new test file).
- **Notes:**
  - This audit's scope per the file-touch grant was `src/rules_core/level_up/paladin.rs` only (plus its own test file/module) — respected; no sibling class file touched, `pilot_compute.rs` read-only.
  - This is a genuine, real fix — not a negative finding. The intake register's own characterization of both suspect ids was inaccurate (both named diagnostics, structurally unreachable regardless of prefix, exactly like Cleric's 7.2 resolution) — but comparing `paladin.rs` directly against Wizard's/Bard's already-fixed reference shape (not just re-checking the intake's own two cited ids), per 7.6's own forwarding note, surfaced a real, different gap: the missing hybrid-baseline recognition-id whitelist entry.
  - **A second, distinct finding was surfaced by this cycle's own comprehensive sweep test, verified but deliberately NOT fixed (out of this cycle's scope)**: `append_resource_pool_change` (the function backing Paladin's two resource pools, `smite_evil_uses_per_day` and `lay_on_hands_uses_per_day`) requires BOTH the from-side and to-side explanation values to be `Some` before pushing a `ResourcePoolDelta`. Whenever a pool's explanation id doesn't exist yet on the from-side — `smite_evil_uses_per_day` at the 0 -> 1 initial-class-entry transition (`supported_paladin_level`'s own gate excludes level 0), and `lay_on_hands_uses_per_day` at the 1 -> 2 transition (below its own level-2 gate the id is instead the flat `class_chassis.paladin.level_gate.lay_on_hands` "correctly absent" marker, not the real uses/day id) — the pool never emits a delta on that one transition, even though the underlying explanation value genuinely changed `None -> Some(N)`. This is a real, verified gap, but a DIFFERENT bug mechanism than the explanation-id filter this cycle (7.9) targets: the id correctly passes the filter and is correctly excluded from `automatic_features` (handled elsewhere, by design); the drop happens one step later, inside the pool-diff's own both-sides-`Some` requirement, which this cycle's file-touch grant and brief ("fix the explanation-id filter... exactly as Wizard's was fixed... do not invent a fix for a bug that isn't there") scope to leaving alone. Documented as an explicit exception in the new test (`is_documented_newly_introduced_pool_gap`) rather than silently excluded, and forwarded below.
  - No functional behavior beyond the recognition grant changes: the recognition grant carries value 0 (a "+0 recognition record" per its own detail text) and fires exactly once, on the transition into a paladin's first supported level — it does not affect any other grant, resource pool, or the capstone-threshold flag.
- **Discovery forwards:** the register A6 Paladin sub-row in `progress.md`'s `## DISCOVERED` section is updated in place with a `RESOLVED 2026-07-22` note (mirroring Cleric's/Bard's precedent format) rather than adding a new top-level entry (respecting the pre-existing >10-entry-cap overflow already flagged as a known, tracked condition by the 7.1 entry itself). One new forwarding note (folded into the same sub-row, not a new top-level entry) for the resource-pool 0->1/1->2 gap described above, for a future cycle with `paladin.rs` in its own grant to pick up.
- **Next-cycle plan:** the next per-class residue cycle should pick up whichever of 7.8 (Monk), 7.10 (Ranger) is still unclaimed. Per this cycle's own finding (mirroring Bard's 7.6), Ranger (the remaining hybrid partial-caster, sharing `explain_hybrid_level1_chassis`'s `HybridClass::Ranger` arm and its own `class_chassis.hybrid_baseline.ranger` recognition id at `pilot_compute.rs:7611`) should be checked specifically for the identical hybrid-baseline whitelist gap, not just its own intake-cited `class_spell.hybrid.ranger.unsupported` diagnostic id.
