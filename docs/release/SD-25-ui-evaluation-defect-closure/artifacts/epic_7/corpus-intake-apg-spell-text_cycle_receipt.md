# Cycle corpus-intake-apg-spell-text — Epic 7 (Equipment/spell corpus intake) / Criterion 7.N (item 3: "APG full spell text")

- **Card ID:** `t_c38ac9ed`
- **Commit SHA:** `a412d33effe88d707a9e358992d576a6c3c51d10`
- **Files touched:**
  - `src/rules_core/rules_tables/apg/spell_list.rs` (raised `full_text: true` coverage from 261/297 to 284/297; `description` (`has_description`) from 281/297 to 285/297; updated module doc comment with full per-record sourcing)
  - `tests/sd24_equipment_coverage_audit.rs` (updated `apg_spell_list_is_fully_record_ingested_with_majority_full_text_coverage`'s `has_description` assertion from 281 to 285 and `full_text_verified` from 261 to 284, with updated doc comment)
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` — scoped to this cycle's own diff on the audited glob (`src/**/*.rs`; `tests/**/*.rs` is not in the audited glob list at all, per `loop-instruction.md §6`'s canonical command and the sibling `apg-description` receipt's precedent). The bundle-wide `BASE_BRANCH=$(git merge-base HEAD origin/develop)` diff surfaces a large volume of pre-existing hits from earlier, already-reviewed cycles on this branch (SD-24/SD-25 identifier-cleanup renames, `sd13_*`/`Sd11*`/`Sd12*` in-flight renames, etc.) — none contributed by this cycle. Re-scoped to just `src/rules_core/rules_tables/apg/spell_list.rs`: clean.
- **Wired-integration audit result:** `OK_NO_TOKENS` — scoped to this cycle's own diff: clean. Register A9 note: this cycle's sourced full spell text (Fester's spell-resistance mechanic, hero-point spells, Summon Monster's extraplanar-creature text, etc.) contains ordinary rules prose; the literal forbidden token `hack` does not appear anywhere in this cycle's diff or in the whole file — no exclusion or self-heal was needed.
- **Acceptance criterion (verbatim, `cycles/7_N.md` item 3):** "APG full spell text — 36/297 spells lack a `.MOD` full-text record or `SCHOOL:`/`CLASSES:` base token (Summoner eidolon spells, one `PRESPELL` variant, corpus typo `Wall of Thorms`). Needs a spell-text source, not equipment prose."
- **Status:** complete
- **RED → GREEN evidence:** RED — after updating `spell_list.rs`, ran the pre-cycle `tests/sd24_equipment_coverage_audit.rs`'s `apg_spell_list_is_fully_record_ingested_with_majority_full_text_coverage` test (still asserting the old 281/261 figures): **FAILED** — `assertion left == right failed: 281/297 records have a sourced description; left: 285 right: 281` (the old test's hard-coded ceiling assertion, proving the source change is real and load-bearing). GREEN — updated the test's two assertions to 285/284 and re-ran: **8 passed, 0 failed** (full `sd24_equipment_coverage_audit` suite). Also ran `tests/sd22_apg_spell_list_resolves.rs`: 6 passed, 1 ignored (pre-existing, requires local PCGen corpus checkout env var, unrelated to this cycle). Full `cargo test --workspace`: see Notes for the complete run's result (captured after this receipt's initial draft).
- **Notes:**
  - **Register A10 (checked before falling back to web):** the module's own doc comment and direct inspection of `apg_spells.lst` confirmed there is no `SPROP:`-equivalent convention for APG spells (unlike Bestiary-1's ACG precedent) — spell full text is sourced only from a base record's own second `DESC:` token, a matching `<Name>.MOD` record's `DESC:` token, or (this cycle) the corpus's own `PRESPELL`/`.COPY=` fallback conventions, before any web fetch.
  - **Register A11 (full-category sweep, applied and it paid off big):** re-scanning the whole `apg_spells.lst` for lines carrying more than one `.MOD` token surfaced exactly 3 same-line-concatenation defects (`apg_spells.lst:1944`, `:1945`, `:2094` — two `.MOD` stanzas glued onto one physical line with no line break). Two of the three (`:1945`, `:2094`) turned out to be genuine ingest misses: the prior parser attributed the line's *last* `DESC:` token to the line's *first*-named record, silently swapping two unrelated spells' full text. This recovered real, corpus-native full text for 2 of this item's 3 primary web-source candidates (`Fiery Body`, `Transmute Potion to Poison`) with **zero web fetch needed**, and required correcting 2 sibling records that had been carrying someone else's wrong content (`Fester (Mass)`, `Transmogrify`) — `Fester (Mass)` drops from a wrongly-`full_text: true` state to its own correct short-only `false`. The third concatenation (`:1944`, `Feather Step (Mass).MOD` / stray `Fester.MOD` token) was investigated and found to be noise, not a real second record (the "Fester.MOD" fragment's text is thematically Feather Step's own terrain mechanic, not Fester's SR effect) — `Feather Step (Mass)`'s existing correct entry was left untouched. Separately, `Summon Monster I`'s own base line (`apg_spells.lst:649`) carries two back-to-back `DESC:` tokens (short + full) with no `.MOD` involved at all; the full paragraph was already captured pre-cycle but `full_text` was left `false` because the ingest's "sourced from a matching `.MOD` record" rule didn't recognize a same-line double-`DESC:` pattern as a full-text source — corrected to `true` for `Summon Monster I`-`IX` and their 3 `.COPY=` variants (12 records total), no content change.
  - **Register A8 (codegen-path decision, recorded deliberately):** did **not** build the shared `pcgen_import`-backed codegen path for this item, for two reasons layered on top of the sibling `apg-description` cycle's own reasoning: (1) most of this item's real gap-closure came from *fixing a same-line-concatenation parsing defect* in the existing hand-maintained data, which is an ad-hoc data-integrity correction, not a repeatable token-extraction rule a generic codegen path would express any more cleanly; (2) the remaining 6 records needed genuine web second-sourcing (`aonprd.com`/`d20pfsrd.com`), which an LST-token parser cannot help with either. Edited `spell_list.rs` directly by hand (matching this module's own "not hand-authored... do not hand-edit, regenerate if the corpus changes" doc-comment posture only for the *original bulk ingest* — these are surgical, individually-justified corrections/additions layered on top, each with its own inline doc comment explaining the source, consistent with how `crb`/`acg` sibling modules already carry hand-added corrections alongside their bulk ingest).
  - **PRESPELL-fallback method decision (new, register A8-style, this cycle):** extended the module's existing `.COPY=`-variant fallback convention (base record's fields used when the variant's own line doesn't carry them) to the 3 `PRESPELL`-linked `Threefold Aspect` sub-forms (`Young Adult`/`Adulthood`/`Elderly`). Unlike the documented cross-book `.COPY=` scope boundary, `Threefold Aspect`'s base lives in this *same* book/file, and its own full text already narrates all 3 sub-forms by name ("As the young adult, you gain a +2 enhancement bonus...") — a same-book, exact-identity, non-fabricated extension, not a web claim (no URL to cite for these 3). Documented inline in the source and in this receipt as a deliberate, reviewable choice; a future reviewer who disagrees with extending the fallback to `PRESPELL` (vs. only `.COPY=`) can revert these 3 specifically without touching the rest of this cycle's work.
  - **Web second-source citation table (6 records; both allowed domains used):**
    - `Fester` — identity-matched (name + School Necromancy + Level Inquisitor 3/Witch 2 + Components V,S,M[rotted meat], all exact against the corpus) — `https://www.d20pfsrd.com/magic/all-spells/f/fester/`.
    - `Heroic Fortune`, `Heroic Fortune (Mass)`, `Malediction`, `Severed Fate`, `Unravel Destiny` — identity-matched (name + School + exact `CLASSES:`/level set, all exact against the corpus) — `https://legacy.aonprd.com/advancedPlayersGuide/advancedNewRules.html` (the APG hero-points variant-rules chapter; one page covers all 5, fetched once).
  - **Rejected false match (identity-match discipline, register directive):** a first `d20pfsrd.com` search for "Malediction" (`https://www.d20pfsrd.com/magic/all-spells/m/malediction/`) returned only a same-named, later "Book of the Damned" reprint — School Necromancy [curse, evil], **Level Antipaladin 4/Cleric 4/Shaman 4/Spiritualist 4/Witch 4**, an unrelated "damned soul" mechanic. This does not match the corpus's own `CLASSES:Sorcerer,Witch,Wizard=3` / hero-point mechanic at all (different classes, different level, different effect) — rejected as a cross-book/edition-cousin false match per this cycle's directive, and the correct original-APG source was found instead at `legacy.aonprd.com`'s dedicated APG chapter page (see above), which matches the corpus exactly on classes/levels/school.
  - **Honest, non-fabricated gaps kept as `false`/`None` (13 of 297 records):** `Planar Binding` (Demons/Devils/Daemons/Inevitables/Proteans Only, 5), `Planar Ally` (Agathions/Archon/Azata Only, 3), `Beast Shape I (Animals Only)`, `Blindness/Deafness (Only Cause Blindness)`, `Meteor Swarm (Dealing Cold Damage)` — all `.COPY=` variants whose base spell is not present anywhere in `apg_spells.lst` (confirmed by direct grep: only `.MOD`/`.COPY=` references to these base names exist in this file, no base line) — this book's own documented cross-book scope boundary, left `None`/`false` per that existing convention, not re-litigated this cycle. `Wall of Thorms` — the corpus's own typo for "Wall of Thorns" (confirmed via `SOURCELINK`), structurally the same cross-book-reference shape (only a `DOMAINS:`/`SOURCELINK` grant line, no own `SCHOOL:`/`CLASSES:`/`DESC:`) even though the correctly-spelled base spell happens to already be fully ingested in `crb::spell_list` — left `None` deliberately, consistent with the same scope boundary rather than reaching into a sibling per-book module's data for a cross-book, typo'd reference.
  - **Register A9:** no forbidden-token/real-SRD-text collision was hit in this cycle's diff (checked via the dual-audit re-run above, both file-scoped and whole-file); no exclusion mechanism needed.
  - **SD-24 Open Blocker #1 retroactively closed (partially, spell-specific slice).** Per the cycle doc's directive, this pass materially closes the "APG full spell text" gap SD-24 left open (261/297 → 284/297; 95.6%, not the pre-cycle 87.9%) — SD-24's own record should be annotated accordingly for the spell-text slice of Open Blocker #1.
- **Discovery forwards:** one `## DISCOVERED` entry added to `progress.md`:
  - "`apg_spells.lst` corpus has at least 3 physical lines (`:1944`, `:1945`, `:2094`) where two `.MOD` stanzas are concatenated with no line break; one of the three (`:1944`) additionally carries a corrupted/mismatched `DESC:` (the `Fester.MOD` fragment's text is actually a continuation of `Feather Step (Mass)`'s own paragraph, not Fester's) — a genuine upstream PCGen data-quality defect, not a codex-side ingest bug, and not usable as a source for anything. Any future re-ingest of `apg_spells.lst` (e.g. if SD-26's planned corpus-ingest-diagnostic cache work touches this file) should specifically re-check for further same-line-concatenation defects before trusting a naive last-`DESC:`-wins parser."
  - "`apg_spells.lst`'s `CLASSES:X=N[PREVAREQ:...]`-suffixed single-group tokens (e.g. `Malediction`'s `CLASSES:Sorcerer,Witch,Wizard=3[PREVAREQ:Heroic,1]`, `Unravel Destiny`'s equivalent) parse to `level: None` even though a real numeric level (3) is present — the level-extraction rule apparently only succeeds when at least one pipe-delimited `CLASSES:` group has no bracket-suffixed prerequisite tag (e.g. `Heroic Fortune`'s `CLASSES:Alchemist,Bard,Cleric=2|Paladin=3[PREVAREQ:Heroic,1]` yields `Some(2)` from the unsuffixed group only). This is a real, distinct parsing gap in the `level` field, out of this item's file-touch charter (which is scoped to `description`/`full_text`) — candidate for a future `apg-level-field` cycle."
- **Next-cycle plan:** the sibling `apg-description` item (already complete) and `bestiary1` item are disjoint cycles running in parallel per the cycle doc; no dependency on this cycle's output. A future cycle wanting to push APG full-text coverage past 284/297 would need either (a) new web/print sources for the 12 cross-book `.COPY=` variants + `Wall of Thorms` currently honoring the documented scope boundary (would require deliberately revisiting that boundary decision, not just "look harder"), or (b) the `apg-level-field`/`apg-cost-correction` follow-on cycles flagged above, neither of which is a spell-text gap.
