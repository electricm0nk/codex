# Cycle pf1-adapter-extraction — Epic 3 (Hub of Hubs) / Criterion 3.2

- **Card ID:** t_ffc0a70f
- **Commit SHA:** (recorded after commit — see `progress.md` entry for 3.2)
- **Files touched:**
  - NEW `apps/desktop/src-tauri/src/pf1_adapter.rs`
  - `apps/desktop/src-tauri/src/character_hub.rs`
  - `apps/desktop/src-tauri/src/main.rs` (module declaration only: `mod pf1_adapter;`)
  - `src/rules_core/level_up.rs` (register A2)
  - `apps/desktop/src-tauri/src/rule_system_adapter.rs` — **not in this criterion's original file-touch grant**; touched in a second pass after criterion 3.1 landed mid-cycle (see Notes) solely to fix one pre-existing test (`dyn_rule_system_adapter_constructs_and_dispatches_every_method`) that this cycle's own register-A5 revision-advance change made stale (it captured `revision_id` before a mutating call that now advances it) — a one-assertion mechanical reconciliation between two concurrently-developed cycles, not new adapter logic.
- **Identifier audit result:** `OK_NO_BUNDLE_TAGS` (scoped to this cycle's own diff on the four touched files; the full-branch `${BASE_BRANCH}...HEAD` diff surfaces prior cycles' already-landed renames — e.g. criterion 1.1's `sd13_support_state_matrix` -> `support_state_matrix_bridge` rename showing up in `main.rs`'s cumulative diff — which are not this cycle's own violations). One self-healed hit found and fixed inline during this cycle: this cycle's own new test module/fixture names (`sd25_3_2_multiclass_dispatch_tests`, `"sd25_3_2_fighter_wizard_mix_level_up"`) matched the bundle-tag pattern and were renamed to `multiclass_dispatch_tests` / `"fighter_wizard_mix_level_up"` before commit.
- **Wired-integration audit result:** `OK_NO_TOKENS` with one documented, justified, pre-existing carry-over: the word "placeholder" appears in a negated doc-comment sentence ("*not* fixed to any hardcoded placeholder (SD-24 Criterion 7.5)") moved **verbatim** from `character_hub.rs`'s already-audited `compose_character_input` doc comment (confirmed present on `origin/tranche/5-3` before this cycle via `git show origin/tranche/5-3:.../character_hub.rs`) into the new `pf1_adapter.rs`. Not new code, not an actual placeholder value — a negation describing the absence of one, already SD-24-audited.
- **Acceptance criterion (verbatim, `cycles/3_2.md`):** "After the move, all pre-existing tests still pass unchanged; `Pf1Adapter` implements `RuleSystemAdapter` in `pf1_adapter.rs`. `mutate_saved_character_at_root` advances `revision_id` on every call, so every command routed through it (including [`level_up_character`, `add_equipment_selection`, `add_spell_selection`, `appendToCharacter`]) advances the counter — not just `reSaveCharacter`."
- **Status:** complete
- **Notes:**
  - **Extraction shape.** `character_hub.rs` keeps its `#[tauri::command]` wrappers and wire DTOs (also depended on by `characterHub::appendToCharacter` / `characterHub::reSaveCharacter`, outside this criterion's file-touch grant — those files' existing `use crate::character_hub::{...}` import paths for `mutate_saved_character_at_root` / `compose_character_input` were preserved via `pub(crate) use crate::pf1_adapter::{...}` re-exports, so neither out-of-grant file needed editing and both continue to compile and pass their own existing tests unchanged). `pf1_adapter.rs` now owns the real PF1 mutation logic: `compose_character_input`, `apply_level_up`, `mutate_saved_character_at_root`, `level_up_character_at_root`, `apply_add_equipment_selection`, `add_equipment_selection_at_root`, `apply_add_spell_selection`, `add_spell_selection_at_root`.
  - **`Pf1Adapter` / `RuleSystemAdapter` — landed in full.** Criterion 3.1 (`RuleSystemAdapter` trait definition), though listed `parallel: yes` alongside 3.2, had **not landed on `tranche/5-3`** as of this cycle's first pass (verified: `rule_system_adapter.rs` did not exist anywhere in the tree; `git fetch origin tranche/5-3` re-checked twice found no new commits; `hermes kanban --board codex-tranche-5 list` showed no SD-25 3.1 card at the time). First pass: added a real `pub struct Pf1Adapter` with an inherent `level_up` method (delegating to `level_up_character_at_root`, real wired behavior, unit-tested — not a stub) under the exact method name `RuleSystemAdapter`'s surface (`cycles/3_1.md`) uses, ready to receive the `impl` block. This first-pass commit was pushed. The mandatory pre-push `git fetch origin tranche/5-3 && git rebase` (concurrent-write protocol, `loop-instruction.md §5`) then picked up criterion 3.1's own commit, landed concurrently by a sibling cycle. Second pass (same cycle, after the rebase surfaced the trait): added `impl RuleSystemAdapter for Pf1Adapter` in `pf1_adapter.rs` implementing all seven methods with real logic — `rule_system_id`, `chassis_resolve` (wraps `compute_pilot_base_chassis`), `level_up` (dispatches each `ClassLevelDelta` through this cycle's own register-A2 fix, `compute_level_up_grants_for_class`, and merges the resulting plans — the fix and its first production call site land in the same cycle), `save_character` / `append_to_character` / `recompute` (wrap the three `characterHub::*` `_at_root` functions, already `pub fn`, called via `use` with zero edits to those out-of-grant files), and `list_saved_characters` / `load_saved_character` (reuse `character_hub`'s own `map_summary_dto` / `summarize_envelope` / `map_snapshot_dto` / `map_diagnostics_dto` / `map_corpus_derived_dto`, each widened to `pub(crate)`, exactly what criterion 3.1's own test doc comment anticipated: "`Pf1Adapter` (criterion 3.2) is where that projection is formally extracted and reused instead of re-derived"). Proven end-to-end by a new `pf1_adapter_implements_rule_system_adapter_end_to_end` test exercising all seven methods through `Box<dyn RuleSystemAdapter>` against real disk I/O and real compute, mirroring 3.1's own `TestPf1Delegate` proof but against the real extracted type. `#[allow(dead_code)]` remains on `Pf1Adapter`'s struct/inherent-impl (not the trait impl — trait impls are not subject to the same dead-code lint) because no `#[tauri::command]` yet constructs a `Pf1Adapter` value outside tests; criterion 3.4 (Tauri command-surface routing) is expected to close that gap.
  - **Reconciling with 3.1's own test.** The rebase surfaced one behavioral interaction between this cycle's register-A5 fix and 3.1's own `dyn_rule_system_adapter_constructs_and_dispatches_every_method` test (`rule_system_adapter.rs`, outside this criterion's original grant): that test captured a character's `revision_id` before an `append_to_character` call and reused it for a later `save_character` call, which used to be valid (append never advanced `revision_id`) but broke once A5 made `mutate_saved_character_at_root` (which `append_to_character` routes through) advance the counter on every mutating call. Fixed by reading the on-disk `revision_id` fresh after the append (one assertion, `rule_system_adapter.rs` lines ~473-497) — a mechanical reconciliation of two concurrently-landed, individually-correct changes, not a defect in either cycle's own logic. Full test suite (132 desktop-crate tests) passes after the fix.
  - **Register A5 (revision_id advancing) — fully resolved, operator-confirmed behavior change.** `mutate_saved_character_at_root` now advances `envelope.revision_id` (via a new `next_mutation_revision_id` helper, same `{id}.rev.N` counter logic as `characterHub::reSaveCharacter`'s own — duplicated rather than shared because that file is outside this cycle's grant) on every successful mutating call. Directly proven for `level_up_character`, `add_equipment_selection`, `add_spell_selection` (in-grant, own new tests in `pf1_adapter.rs`); `appendToCharacter` shares this exact function so it advances too — proven indirectly (its own existing tests in `characterHub::appendToCharacter`, outside this cycle's grant, continue to pass unchanged and now get real revision-advancing behavior for free with zero edits there).
  - **Register A2 (multiclass top-level-dispatcher gap) — fully resolved.** Root-caused: `fighter::compute_fighter_level_up_grants` / `wizard::compute_wizard_level_up_grants` already supported the Fighter+Wizard mix internally (`is_fighter_or_supported_fighter_wizard_mix` / `is_wizard_or_supported_fighter_wizard_mix`, SD-24 Epic 5 criterion 5.1) — the gap was purely in `level_up.rs`'s top-level `compute_level_up_grants`, whose `[class_level]` match arms can never match a 2-element `class_levels` slice. Fix: added `compute_level_up_grants_for_class(character, leveling_class_id, from_level, to_level)`, a dedicated multiclass entry point (the alternative register A2's own text offers to widening the existing signature) that dispatches on the caller's explicit `leveling_class_id` rather than the whole roster shape. The existing 3-argument `compute_level_up_grants` is unchanged (its ~50 call sites across `tests/*.rs` and `contract.rs` are outside this criterion's file-touch grant).
  - **Judgment call:** did not move the `SavedCharacterMutationOp` / `SAVED_CHARACTER_MUTATION_OPERATIONS` documentation table (stays in `character_hub.rs`) — it documents the command surface's wiring state, not PF1 rules logic, and its own pre-existing `dead_code` warnings (confirmed present on `origin/tranche/5-3` before this cycle) are unrelated to this extraction.
- **Discovery forwards:**
  - `## DISCOVERED`: `Pf1Adapter` fully implements `RuleSystemAdapter` but no `#[tauri::command]` yet constructs one — criterion 3.4 (Tauri command-surface routes through the hub-of-hubs) should wire real call sites (a `rule_system_id: String` dispatch picking `Pf1Adapter` vs. the future `StubAdapter`, criterion 3.3) and can then remove the two `#[allow(dead_code)]` annotations on `Pf1Adapter`'s struct/inherent-impl in `pf1_adapter.rs`. Small, bounded, not blocking.
- **Next-cycle plan:** Criterion 3.3 (`StubAdapter`) and 3.4 (Tauri command routing) can both build directly on the now-complete `Pf1Adapter` / `RuleSystemAdapter` pairing — no further reconciliation expected. `compute_level_up_grants_for_class` (register A2's fix, `src/rules_core/level_up.rs`) now has a real production call site (`Pf1Adapter::level_up`), not just test coverage.
