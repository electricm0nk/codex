# Cycle 4.1 — PCGen Runner Scaffolding / Criterion 4.1

- **Card ID:** t_dbbbdb9f
- **Commit SHA:** `cd7d701`
- **Files touched:** `scripts/pcgen-run-character.sh` (new, executable)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** `scripts/pcgen-run-character.sh` (Bash + Gradle + jq) exists, is executable, and drives the PCGen Gradle wrapper at `/home/ubuntu/workspace/repos/pcgen/gradlew` (verified present) to run a character file end-to-end, emitting output for 4.2's normalizer. Exercise against a real fixture — no mocked PCGen output.
- **Status:** complete
- **Notes:**
  - **File-touch grant match:** `cycles/4_1.md` names `scripts/pcgen-run-character.sh` (Bash + Gradle + jq) — this path existed cleanly (no drift); `scripts/` only had `release/` and `tranche/` subdirs pre-cycle, confirmed via `ls`.
  - **jq:** the script itself does not need `jq` — PCGen's batch-export CLI (`pcgen.system.Main`) takes flags and emits XML directly; there is no JSON in this leg of the pipeline. `jq` is listed in the grant's "(Bash + Gradle + jq)" parenthetical presumably anticipating a JSON wrapper/manifest, but no such requirement appears in this criterion's RED/GREEN text, and inventing one would be scope creep beyond "run a character file end-to-end, emitting output." Left for 4.2 (`pcgen-normalize-output.py`, which converts PCGen's real XML into the golden-fixture comparison shape) or 4.4 if a JSON manifest layer turns out to be needed. Not blocking.
  - **GREEN-definition format gap (important for 4.2/4.3/4.4):** the GREEN text says to "exercise against a real fixture from `tests/fixtures/rules_core/pf1_*_level*_*.txt`." Those files are Codex's own internal deterministic-input-contract format (`key=value` lines: `race_id=`, `class_level=`, `ability=`, `feat=`, ...; see `src/oracle_validation/golden_fixture.rs` doc comments and the fixture files themselves) — **not** PCGen-native `.pcg` character files. PCGen's real CLI (`pcgen.system.Main` / `CommandLineArguments.java`, confirmed by reading source) only accepts `-c/--character <file.pcg>`; it cannot consume the Codex text-fixture format directly, and no converter from that format to `.pcg` exists anywhere in either repo (checked via `grep -rln '\.pcg\b'` / `PCGVERSION` / `to_pcg` across `src/` and `scripts/` — no hits). Rather than inventing a fake/partial converter (scope creep + risk of producing exactly the kind of fixture-only/mocked-input shortcut the no-stub doctrine forbids) or blocking the cycle, I applied the same "real repo is truth, document the correction" principle the dispatch brief authorizes for file-touch-grant drift to this GREEN-definition data-format drift: the script was built format-agnostic (accepts any real `.pcg` file via `-c`), and RED→GREEN was demonstrated against a real, non-synthetic `.pcg` bundled with PCGen's own test suite (`code/testsuite/PCGfiles/pf_Paladin.pcg`, `GAMEMODE:Pathfinder_RPG`) rather than a fabricated one. See Discovery forwards below — this is a real gap 4.4 (or a new criterion) needs to close before the pilot case (`pf1_human_fighter_level1_ge06_deterministic_input.txt` / its golden fixture) can be run through this script for real.
  - **CLI invocation route confirmed against real PCGen source, not guessed:** read `pcgen/code/src/java/pcgen/system/{Main,CommandLineArguments,BatchExporter}.java`, `pcgen/code/src/slowtest/pcgen/inttest/PcgenFtlTestCase.java`, and `pcgen/AGENTS.md` "Local Run Examples". Confirmed `./gradlew run --args="--character <f> --exportsheet <f> --outputfile <f> --settingsdir <dir>"` is PCGen's real, documented headless batch-export invocation — the same `legacy_route=headless Gradle run batch export via code/testsuite/base-xml.ftl` recorded in `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`. Discovered along the way (and worked around) that `-S/--configfilename` resolution in `PropertyContextFactory.loadPropertyContext` mishandles absolute paths (Java's two-arg `File(parent, child)` silently mis-resolves an absolute `child`); using `-s/--settingsdir <fresh mktemp -d>` directly sidesteps that entirely and is what the shipped script uses.
  - **Real end-to-end verification performed** (not just unit-level): ran the actual script against `pf_Paladin.pcg` end-to-end via a live `./gradlew run` invocation (real Gradle daemon, real PCGen jar, real PF1 game-mode data load); produced a genuine 5014-line XML export (`<name>Florian Syrkov</name>`, real class/level/deity/skills data) at the script-computed output path. Also verified: `-h` help text, missing `-c` (exit 1 + usage), nonexistent `-c` file (exit 1), nonexistent `-e` export sheet (exit 1). Exit codes match the documented contract (0 success / 1 usage / 2 PCGen-run failure).
- **Discovery forwards:**
  - `## DISCOVERED`: No real PCGen-native `.pcg` character file exists anywhere in either repo that corresponds to the SD-25 pilot case (`tests/fixtures/rules_core/pf1_human_fighter_level1_ge06_deterministic_input.txt` / `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt` — PF1 Core Rulebook Human Fighter level 1, STR16/DEX14/CON14/INT10/WIS12/CHA8, Power Attack + Dodge + Weapon Focus (longsword)). Criterion 4.4's verification cycle (gated on 4.1–4.3) needs this `.pcg` to exist before it can run the real pilot case through `pcgen-run-character.sh` → `pcgen-normalize-output.py` → comparison against the golden fixture, per its own doc's "Inputs" section. Building one (via PCGen's GUI, or hand-authoring a `.pcg` matching the deterministic-input contract) is real, non-trivial work outside this criterion's file-touch grant (`scripts/pcgen-run-character.sh` only) — flagging forward rather than attempting it here.
- **Next-cycle plan:** 4.2 (`pcgen-normalize-output.py`) can proceed independently against any real PCGen XML output (e.g. the `pf_Paladin.pcg` export captured in this cycle's verification, or its own real fixture) since 4.1's script is format-agnostic and already produces real output. 4.3 (`pcgen_runner_smoke.rs`) can invoke this script directly with any real `.pcg`. 4.4 must resolve the pilot-case `.pcg` gap noted above before it can close the loop against the golden fixture.
