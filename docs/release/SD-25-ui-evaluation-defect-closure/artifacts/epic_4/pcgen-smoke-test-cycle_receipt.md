# Cycle 4.3 — PCGen Runner Scaffolding / Criterion 4.3

- **Card ID:** t_fdf81197
- **Commit SHA:** `93003f67cd2dc5ebe72b8e040ee3511b5bb27021`
- **Files touched:** `tests/pcgen_runner_smoke.rs` (new)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (scoped to `tests/pcgen_runner_smoke.rs`; the unscoped whole-tranche `BASE_BRANCH...HEAD` diff surfaces pre-existing `sd13_`/`SD11`/`SD12`-named identifiers from prior, already-landed cycles — not this cycle's diff — so the audit was scoped to this cycle's own touched path per the dispatch brief's "final diff scoped to the paths you touched" instruction)
- **Wired-integration audit result:** OK_NO_TOKENS (same scoping rationale; the unscoped diff surfaces legitimate prior-cycle `StubAdapter` "not yet implemented" honesty strings from criterion 3.3, not this cycle's work)
- **Acceptance criterion:** `tests/oracle_validation/pcgen_runner_smoke.rs` (root-crate integration test) exists and asserts the 4.1 script + 4.2 normalizer are invocable and produce parseable output for the pilot case; passes when run against the real scripts (may be `#[ignore]`-gated if 4.1/4.2 haven't landed yet, per this criterion's own doc).
- **Status:** complete
- **Notes:**
  - **File-touch grant path drift (documented per dispatch brief):** `cycles/4_3.md` names `tests/oracle_validation/pcgen_runner_smoke.rs`. That path does not match this crate's real integration-test convention: verified via `find`/`ls` that `Cargo.toml`'s `[package] name = "codex"` crate has **no** `tests/<subdir>/main.rs` nested-integration-test layout anywhere (confirmed no existing test uses one — the one `tests/<dir>/` that exists, `tests/sd16-e5-f1/`, holds only a shell script, not a Rust test binary), and Cargo's own discovery rule only auto-compiles `.rs` files placed **directly** under `tests/` as separate test-binary targets — a file at `tests/oracle_validation/pcgen_runner_smoke.rs` would never be discovered or run by `cargo test`, making it permanently-vacuously "RED" and undermining the whole point of the smoke test. Corrected to the real, flat convention every other `tests/*.rs` integration test in this crate uses: `tests/pcgen_runner_smoke.rs`. No `sd25`/bundle-tag prefix was added to the filename or any identifier inside it (consistent with `ge06_`/`golden_case_fixture_schema.rs`-style non-bundle-tagged test names already in the crate, and with the identifier-discipline audit's intent).
  - **4.1 landed, 4.2 has not (as of this cycle):** confirmed via `ls scripts/` — `scripts/pcgen-run-character.sh` exists (criterion 4.1, commit `4c5d8d8`); `scripts/pcgen-normalize-output.py` does not exist on `tranche/5-3` yet (criterion 4.2 is `not-started` per `progress.md`, being worked in a parallel sibling worktree at cycle-authoring time). Per this criterion's own doc ("the test may be authored `#[ignore]`-gated with the ignore removed by 4.4's verification cycle — state which in the receipt"): the smoke test is split into (a) an always-run test that verifies 4.1's real script exists, is executable, and reports its real documented usage contract via a live `-h` invocation (no mocking), and (b) the actual runner→normalizer pipeline test, `#[ignore]`-gated with a `#[ignore = "..."]` reason string naming criterion 4.2 and 4.4 explicitly.
  - **Real end-to-end verification of the `#[ignore]`d test, not just a plausible-looking placeholder:** to prove test (b) is a genuine, wired smoke test (not fixture-only/self-satisfying scaffolding forbidden by the no-stub doctrine), I temporarily copied the in-flight, not-yet-committed `scripts/pcgen-normalize-output.py` from the sibling parallel worktree (`.claude/worktrees/wf_69a19687-7a4-3`, actively implementing criterion 4.2) into this worktree's `scripts/` directory, ran `cargo test --test pcgen_runner_smoke -- --ignored`, confirmed it passes end to end (real `./gradlew run` PCGen batch-export against the real bundled `pf_Paladin.pcg` fixture, real Python normalizer invocation, real JSON-shape assertions on the result — 36.35s wall time), then deleted the temporary copy before committing (confirmed via `git status --short` showing only `tests/pcgen_runner_smoke.rs` before commit). No 4.2 code is committed by this cycle; the copy was verification-only and never touched the working tree's git-tracked state.
  - **Pilot-case `.pcg` gap (register, forwarded from 4.1):** 4.1's own `## DISCOVERED` note established that no real PCGen-native `.pcg` file exists anywhere in either repo for the actual SD-25 pilot case (`pf1-crb-human-fighter-level1` / PF1 CRB Human Fighter level 1). This smoke test therefore substitutes PCGen's own bundled real fixture `code/testsuite/PCGfiles/pf_Paladin.pcg` (same substitute 4.1's receipt used for its own real verification) rather than inventing a fake pilot `.pcg`. Criterion 4.4's "Inputs" section names the real pilot fixtures (`tests/fixtures/rules_core/pf1_human_fighter_level1_ge06_deterministic_input.txt`, `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`) and is gated on 4.1–4.3, i.e. it inherits responsibility for closing this gap before comparing against the golden fixture — not this cycle's file-touch grant.
  - **Dual-audit scoping:** ran both greps first unscoped (`BASE_BRANCH...HEAD` across the full path set per `loop-instruction.md §6` literal text) and confirmed the many resulting hits are exclusively pre-existing, already-landed prior-cycle work (criterion 3.3's `StubAdapter` honest "not yet implemented" strings; historical `sd13_`/`SD11`/`SD12` identifiers from earlier renames), not anything introduced by this cycle's diff. Re-ran scoped to `tests/pcgen_runner_smoke.rs` only (this cycle's sole touched path) per the dispatch brief's explicit "final diff scoped to the paths you touched" instruction — both `OK_*`.
- **Discovery forwards:**
  - `## DISCOVERED`: `cycles/4_3.md`'s file-touch grant path (`tests/oracle_validation/pcgen_runner_smoke.rs`) is unusable as literally written — this crate's Cargo integration-test discovery only picks up `.rs` files directly under `tests/`, never files in a subdirectory unless that subdirectory contains its own `main.rs`. Future cycles authoring new Rust integration tests in this bundle/crate should use the flat `tests/<name>.rs` convention directly, not a subdirectory path copied from the grant text.
- **Next-cycle plan:** 4.4 (verification cycle, gated on 4.1–4.3) must (1) resolve the pilot-case `.pcg` gap (build or hand-author a real `.pcg` for `pf1-crb-human-fighter-level1`, or explicitly re-scope the pilot case to the `pf_Paladin.pcg` substitute), (2) remove this file's `#[ignore]` attribute, and (3) capture the real end-to-end command transcript in its own receipt per its RED/GREEN text.
  - **Post-commit discovery:** criterion 4.2 (`scripts/pcgen-normalize-output.py`) landed on `tranche/5-3` concurrently, mid-way through this cycle's own push-retry loop (rebase pulled in commits `a9b28da`/`0395d40`/`1002c2c`). Re-ran `cargo test --test pcgen_runner_smoke -- --ignored` against the now-real, committed 4.2 script (not a temporary copy this time): **passes end to end** (35.64s). This is left `#[ignore]`d per 4.3's own criterion doc and because criterion 4.4's file-touch grant explicitly names "removing a 4.3 `#[ignore]` gate" as 4.4's own permitted action — but 4.4 can now flip it immediately without waiting on anything further; the pilot-`.pcg` gap above is 4.4's only remaining real blocker.
