# Cycle pcgen-normalize — Epic 4 PCGen Runner Scaffolding / Criterion 4.2

- **Card ID:** t_265eb8be
- **Commit SHA:** `a9b28da1d8be1d4a0f85b08059aabad474f84a0a` (rebased onto `origin/tranche/5-3` after 4.1 landed concurrently; original pre-rebase SHA was `1908edb147ff5bc036bf6b5e64c7afcf8f47f040`)
- **Files touched:** `scripts/pcgen-normalize-output.py` (NEW, executable, 292 lines)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (scoped to `scripts/pcgen-normalize-output.py`; see Notes for full-tranche-diff caveat)
- **Wired-integration audit result:** OK_NO_TOKENS (scoped to `scripts/pcgen-normalize-output.py`)
- **Acceptance criterion:** (verbatim GREEN, `cycles/4_2.md`) "Script normalizes real PCGen output (the legacy `code/testsuite/base-xml.ftl` route noted in `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`) into the golden-fixture comparison shape used by `src/oracle_validation/{golden_fixture,selected_parity_dimensions}.rs`. Test against real captured PCGen output, not a hand-written sample."
- **Status:** complete
- **Notes:**
  - **File-touch grant:** matched the cycle doc exactly — only `scripts/pcgen-normalize-output.py` was created/touched. No other file was added to the repo for this cycle.
  - **Dual-audit scoping:** `loop-instruction.md §6`'s literal grep command (`BASE_BRANCH=$(git merge-base HEAD origin/develop)` then diff `BASE_BRANCH...HEAD`) spans the *entire* tranche/5-3 history since it branched from develop, not just this cycle's own commit — running it unscoped surfaces pre-existing `sd*_`/`Sd*` renames and deletions from earlier already-audited cycles (e.g. criterion 1.1's identifier cleanup renaming `sd13_support_state_matrix.rs` → `support_state_matrix_bridge.rs`, and prior SD-1x file removals). Per the task brief's "final diff scoped to the paths you touched," both greps were additionally re-run with the path filter narrowed to `scripts/pcgen-normalize-output.py` only (this cycle's sole touched file) and both return clean (`OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`). The unscoped full-tranche run was inspected and contains no violations attributable to this cycle's diff.
  - **RED→GREEN, TDD:** RED confirmed first — `python3 scripts/pcgen-normalize-output.py` failed with `No such file or directory` (exit 2) before the file existed. GREEN — script implemented as a general-purpose XML normalizer (uses `xml.etree.ElementTree`, no hand-coded per-case branching) and exercised end-to-end.
  - **Real-vs-sample data honesty (per dispatch brief) — superseded by genuine real-data proof:** at the time this criterion's implementation began, criterion 4.1 (`scripts/pcgen-run-character.sh`) had **not landed yet** (`progress.md` showed 4.1 = not-started at cycle start; confirmed via `git log origin/tranche/5-3` before work began), and no captured live PCGen XML output existed anywhere in the repo or `/tmp` — the golden fixture's own `legacy_raw_output_ref` is explicitly `local_generated_only` retention and absent from disk. Per the dispatch brief's fallback instruction, a realistic sample XML was first hand-rendered from the real legacy template `/home/ubuntu/workspace/repos/pcgen/code/testsuite/base-xml.ftl`'s actual tag structure, populated with values computed by hand under standard PF1 rules for the GE-06 pilot input contract (kept only in the session scratchpad, never committed, not referenced by production code — see below for its continued role as an edge-case fixture). **4.1 then landed concurrently in this same parallel wave** (picked up by this cycle's pre-push rebase). Rather than stopping at the sample, this cycle went further than the brief's minimum bar and **actually invoked 4.1's real script against PCGen**: ran `scripts/pcgen-run-character.sh -c /home/ubuntu/workspace/repos/pcgen/code/testsuite/PCGfiles/pf_Paladin.pcg` end-to-end (real `./gradlew run`, real PCGen jar, real PF1 game-mode data load, ~34s build), producing a genuine 5014-line captured XML export (`<name>Florian Syrkov</name>`, matching 4.1's own receipt's verification run). **This normalizer was then run directly against that real captured output** (`python3 scripts/pcgen-normalize-output.py <real-export.xml> --case-id pf1-testsuite-paladin --source-package-id pf1.testsuite`) and produced all 9 dimensions correctly with an **empty diagnostics list**, exit code 0. Cross-checked every emitted value directly against the raw XML: `combat.baseline_melee_attack_bonus`=10 (from `<attack><melee><total>+10/+5</total>` — the regex correctly takes the primary/first iterative-attack figure), `defense.baseline_armor_class`=22 (raw `<armor_class><total>22</total>`, exact match), `skill.selected_modifier.climb`=-1 / `.intimidate`=2 / `.swim`=-1 (raw `<skill_mod>` values -1/2/-1, exact match), `character.identity`="Florian Syrkov" (raw `<name>`, exact match). This is **not** the SD-25 pilot case's own character (4.1's own receipt documents that no real `.pcg` matching the pilot's exact ability-score/feat/skill selections exists in either repo yet — flagged there as a gap for 4.4 to resolve) — but it is genuine, non-fabricated, non-hand-written PCGen runtime output flowing through the real `base-xml.ftl` legacy route end to end, directly satisfying the dispatch brief's core instruction ("test against real captured PCGen output, not a hand-written sample") ahead of criterion 4.4. **Full proof against the pilot case itself specifically** (matching the golden fixture's exact character) still requires 4.4 to resolve the pilot-`.pcg` gap 4.1 flagged forward.
  - **Verification performed against the hand-rendered sample (secondary, retained for edge-case coverage):** the earlier hand-rendered sample XML (`/tmp/.../scratchpad/pf1_human_fighter_level1_pcgen_sample.xml`, not committed) was kept as the source for exercising failure/partial-input paths not present in the one real export captured above: full run produced all 9 dimensions matching hand-computed PF1 values for the pilot's own ability scores/skills (`combat.baseline_melee_attack_bonus`=4, `defense.baseline_armor_class`=16, `defense.total_save.{fortitude=4,reflex=2,will=1}`, `skill.selected_modifier.{climb=7,intimidate=3,swim=7}`) with an empty `diagnostics` list. Additional edge cases exercised against hand-built inputs: unreadable input path (exit 1), malformed/non-well-formed XML (exit 2, hard failure — nothing silently normalized), partial export missing several tags (exit 0, per-dimension `diagnostics` messages naming the missing XPath, remaining dimensions still emitted), `-o`/`--output` file-write mode.
  - **RED→GREEN, TDD, restated:** RED — script absent, invocation fails (`No such file or directory`, exit 2). GREEN — script implemented as a general-purpose XML normalizer (`xml.etree.ElementTree`, no per-case branching, no hardcoded pilot values) and proven to work correctly against **genuine real PCGen output**, not only against synthetic input.
  - **Output shape:** JSON object with `case_id`, `source_package_id`, `legacy_route`, `claim_tier_floor: "computed"`, `dimensions: [{id, value_string, value_i16, source_package_id}, ...]` (field-for-field mirror of `SelectedDimension` in `selected_parity_dimensions.rs`) and a `diagnostics` list for any dimension the input didn't contain. `case_id`/`source_package_id`/`legacy_route` are CLI-overridable (`--case-id`, `--source-package-id`, `--legacy-route`), defaulting to the pilot case's values so the script generalizes beyond this one fixture.
  - **No-stub-MVP compliance:** the script contains no `success: true`-style fake success, no inline mocks, and does not hardcode the pilot's expected values anywhere in its parsing/emission logic — every value in its output is read from the input XML at runtime; missing values surface as explicit diagnostics rather than being silently defaulted.
- **Discovery forwards:** none.
- **Next-cycle plan:** `scripts/pcgen-run-character.sh` (criterion 4.1) landed on `tranche/5-3` during this cycle's pre-push rebase and was chained directly into this normalizer (see above) — 4.3/4.4 can reuse the same `pcgen-run-character.sh -c <x.pcg> -o <out.xml>` → `pcgen-normalize-output.py <out.xml>` pipeline verbatim. Criterion 4.3 (`tests/oracle_validation/pcgen_runner_smoke.rs`) can invoke this script directly (`scripts/pcgen-normalize-output.py <xml> [-o out.json]`) against any real `.pcg`-derived export, e.g. `pf_Paladin.pcg` as used here, or `#[ignore]`-gate on the pilot-`.pcg` gap. Criterion 4.4 still needs a real `.pcg` matching the SD-25 pilot case's exact selections (Human Fighter 1, STR16/DEX14/CON14/INT10/WIS12/CHA8, Power Attack + Dodge + Weapon Focus) — 4.1's receipt already flags this gap forward — before it can confirm this normalizer's output against `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt` for the pilot case specifically. Note: the golden fixture's own `dimension=` lines use coarser categories (`loaded_content_summary`/`derived_values`/`choice_or_prerequisite_outcome`/`exportable_summary`) than this normalizer's per-metric `SelectedDimension` ids; 4.4 should treat this normalizer's output as feeding the `selected_parity_dimensions.rs` surface specifically, with the golden fixture's own dimension lines remaining the separate coarse-grained claim-status ledger.
