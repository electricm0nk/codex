# Cycle pcgen-normalize — Epic 4 PCGen Runner Scaffolding / Criterion 4.2

- **Card ID:** t_265eb8be
- **Commit SHA:** `1908edb147ff5bc036bf6b5e64c7afcf8f47f040`
- **Files touched:** `scripts/pcgen-normalize-output.py` (NEW, executable, 292 lines)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (scoped to `scripts/pcgen-normalize-output.py`; see Notes for full-tranche-diff caveat)
- **Wired-integration audit result:** OK_NO_TOKENS (scoped to `scripts/pcgen-normalize-output.py`)
- **Acceptance criterion:** (verbatim GREEN, `cycles/4_2.md`) "Script normalizes real PCGen output (the legacy `code/testsuite/base-xml.ftl` route noted in `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`) into the golden-fixture comparison shape used by `src/oracle_validation/{golden_fixture,selected_parity_dimensions}.rs`. Test against real captured PCGen output, not a hand-written sample."
- **Status:** complete
- **Notes:**
  - **File-touch grant:** matched the cycle doc exactly — only `scripts/pcgen-normalize-output.py` was created/touched. No other file was added to the repo for this cycle.
  - **Dual-audit scoping:** `loop-instruction.md §6`'s literal grep command (`BASE_BRANCH=$(git merge-base HEAD origin/develop)` then diff `BASE_BRANCH...HEAD`) spans the *entire* tranche/5-3 history since it branched from develop, not just this cycle's own commit — running it unscoped surfaces pre-existing `sd*_`/`Sd*` renames and deletions from earlier already-audited cycles (e.g. criterion 1.1's identifier cleanup renaming `sd13_support_state_matrix.rs` → `support_state_matrix_bridge.rs`, and prior SD-1x file removals). Per the task brief's "final diff scoped to the paths you touched," both greps were additionally re-run with the path filter narrowed to `scripts/pcgen-normalize-output.py` only (this cycle's sole touched file) and both return clean (`OK_NO_BUNDLE_TAGS` / `OK_NO_TOKENS`). The unscoped full-tranche run was inspected and contains no violations attributable to this cycle's diff.
  - **RED→GREEN, TDD:** RED confirmed first — `python3 scripts/pcgen-normalize-output.py` failed with `No such file or directory` (exit 2) before the file existed. GREEN — script implemented as a general-purpose XML normalizer (uses `xml.etree.ElementTree`, no hand-coded per-case branching) and exercised end-to-end.
  - **Real-vs-sample data honesty (per dispatch brief):** criterion 4.1 (`scripts/pcgen-run-character.sh`, the script that drives a real PCGen Gradle run) has **not landed yet** in this parallel wave (`progress.md` shows 4.1 = not-started at cycle start; confirmed via `git log origin/tranche/5-3`). No captured live PCGen XML output exists anywhere in the repo or `/tmp` at authoring time — the golden fixture's own `legacy_raw_output_ref` (`/tmp/codex-ge05-e1-f2/pf1-crb-human-fighter-level1-attempt3.xml`) is explicitly `local_generated_only` retention and is not present on disk. Per the dispatch brief's explicit fallback instruction, a realistic sample XML was hand-rendered directly from the real legacy template `/home/ubuntu/workspace/repos/pcgen/code/testsuite/base-xml.ftl`'s actual tag structure (`<basics><name>`, `<attack><melee><total>`, `<armor_class><total>`, `<saving_throws><saving_throw><name><short>`/`<total>`, `<skills><skill><name>`/`<skill_mod>`), populated with values computed by hand under standard PF1 rules for the GE-06 deterministic pilot input contract (`tests/fixtures/rules_core/pf1_human_fighter_level1_ge06_deterministic_input.txt`: Human Fighter 1, STR16/DEX14/CON14/INT10/WIS12/CHA8, 1 rank each Climb/Intimidate/Swim, chain shirt equipped). This sample lives only at `/tmp/claude-1000/.../scratchpad/pf1_human_fighter_level1_pcgen_sample.xml` (session scratchpad) — it was **not** committed to the repo and is not referenced by the script's production code path; the script's parsing logic is generic (element/attribute lookups by real base-xml.ftl tag names, no fixture-specific branching or hardcoded values). **Full end-to-end proof against 4.1's live PCGen output is deferred to criterion 4.4** per the dispatch brief, as documented there already ("GREEN: `pcgen-run-character.sh` → real PCGen run → `pcgen-normalize-output.py` → output comparable against the golden fixture → `pcgen_runner_smoke.rs` passes un-ignored").
  - **Verification performed against the realistic sample:** full run produced all 9 dimensions (`character.identity`, `combat.baseline_melee_attack_bonus`=4, `defense.baseline_armor_class`=16, `defense.total_save.{fortitude=4,reflex=2,will=1}`, `skill.selected_modifier.{climb=7,intimidate=3,swim=7}`) with an empty `diagnostics` list, exit code 0 — matching hand-computed PF1 values. Additional edge cases exercised: unreadable input path (exit 1), malformed/non-well-formed XML (exit 2, hard failure — nothing silently normalized), partial export missing several tags (exit 0, per-dimension `diagnostics` messages naming the missing XPath, remaining dimensions still emitted), `-o`/`--output` file-write mode.
  - **Output shape:** JSON object with `case_id`, `source_package_id`, `legacy_route`, `claim_tier_floor: "computed"`, `dimensions: [{id, value_string, value_i16, source_package_id}, ...]` (field-for-field mirror of `SelectedDimension` in `selected_parity_dimensions.rs`) and a `diagnostics` list for any dimension the input didn't contain. `case_id`/`source_package_id`/`legacy_route` are CLI-overridable (`--case-id`, `--source-package-id`, `--legacy-route`), defaulting to the pilot case's values so the script generalizes beyond this one fixture.
  - **No-stub-MVP compliance:** the script contains no `success: true`-style fake success, no inline mocks, and does not hardcode the pilot's expected values anywhere in its parsing/emission logic — every value in its output is read from the input XML at runtime; missing values surface as explicit diagnostics rather than being silently defaulted.
- **Discovery forwards:** none.
- **Next-cycle plan:** Criterion 4.3 (`tests/oracle_validation/pcgen_runner_smoke.rs`) can invoke this script directly (`scripts/pcgen-normalize-output.py <xml> [-o out.json]`) once it has real or `#[ignore]`-gated sample input. Criterion 4.4 must run `pcgen-run-character.sh`'s real captured PCGen XML output through this normalizer as the actual end-to-end proof this cycle deferred, and confirm the output's dimension values are structurally comparable to `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt` (note: the golden fixture's own `dimension=` lines use coarser categories — `loaded_content_summary`/`derived_values`/`choice_or_prerequisite_outcome`/`exportable_summary` — than this normalizer's per-metric `SelectedDimension` ids; 4.4 should treat this normalizer's output as feeding the `selected_parity_dimensions.rs` surface specifically, with the golden fixture's own dimension lines remaining the separate coarse-grained claim-status ledger).
