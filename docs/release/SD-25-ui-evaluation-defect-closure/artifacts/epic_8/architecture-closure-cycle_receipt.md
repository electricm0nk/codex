# Cycle 8.2 — Closure Epilogue / Criterion 8.2 (Architecture closure pipeline)

- **Card ID:** t_feac3495
- **Commit SHA:** d7c1aee (truth-up commit; this receipt's own card-ID/SHA backfill lands in the immediately following commit, same as prior cycles)
- **Files touched:**
  - `docs/architecture/README.md` — index Source-dirs (`support_state_matrix_bridge.rs`, `testerWorkbench/feedback/`), desktop-app scope adds adapter seam, provenance-note example refreshed off the now-renamed `sd13_*` file, `Last verified`.
  - `docs/architecture/status.md` — corpus-completeness row (real SD-25 numbers), four new "Real today" rows (adapter seam, corpus-ingest diagnostic, PCGen runner, recompute), stub-graduation edits (Sheet ☰ Menu, recompute reachable, oracle-parity comparator now has a runner half), Bestiary 1 equipment.
  - `docs/architecture/desktop-app.md` — boundary layer 26→27, `recompute_character` wrapper+UI caller, command count recount to 38, `corpus_ingest_diagnostic` table row, adapter-dispatch notes on the three characterHub commands, new "Rule-system adapter seam (hub-of-hubs)" section, CorpusIngestDiagnosticPanel direct-invoke exception, Mode-union `corpusIngestDiagnostic`, `sd11/`→`testerWorkbench/` / `sd15/`→`operatorTriage/` / `sd22/`→`releaseChecks/` frontend-map renames, `Sd11TesterWorkbenchSurface`→`TesterWorkbenchSurface`.
  - `docs/architecture/rules-data-tables.md` — Equipment/spell completeness table + Bestiary 1 row (real audited numbers), Bestiary 1 directory now has `equipment_data.rs`/`equipment_tables.rs`.
  - `docs/architecture/rules-engine.md` — `compute_level_up_grants` single-class dispatch limitation + adapter `level_up`'s explicit `ClassLevelDelta` seam.
  - `docs/architecture/support-state-matrix.md` — `sd13_support_state_matrix.rs`→`support_state_matrix_bridge.rs` (2 citations).
  - `docs/architecture/update-and-feedback.md` — `sd11/feedback/`→`testerWorkbench/feedback/`, `Sd11TesterWorkbenchSurface`→`TesterWorkbenchSurface`.
  - `docs/architecture/overview.md` — bridge-rename in data-flow diagram + task-index source-dir.
  - `docs/architecture/conventions.md` — provenance-example refresh + note that criterion 1.1 renamed several `sdNN` frontend surfaces.
  - `docs/architecture/testing.md` — bridge rename in inline-test list (now 19 files incl. the 4 new adapters/diagnostic), `sd25_*` + `pcgen_runner_smoke.rs` added, test counts 441→453 (Rust) and 48→62 (frontend), `TesterWorkbenchSurface`.
  - `docs/architecture/release-pipeline.md` — `sd22/`→`releaseChecks/` test paths, `formatSd11WorkbenchBuildLabel`→`formatWorkbenchBuildLabel`, latent-CI-gap note (rename left `check-release-manifest.yml`'s `paths:` filter pointing at dead `sd11/`/`sd15/` globs).
  - `graphify-out/.truth-up-run-2026-07-22T15-32-08Z.log` — graphify-update receipt (Phase A + B both exit 0).
- **Identifier audit result:** OK_NO_BUNDLE_TAGS — docs-only diff carries no *code-side* bundle-tag leaks; the arch docs necessarily *name* `sdNN` files as proper nouns / rename provenance, which is the doc set's documented convention, not an identifier-discipline violation in shipping code.
- **Wired-integration audit result:** OK_NO_TOKENS / OK_NO_NOOP_HANDLERS / OK_NO_MOCK_LEAKS / OK_NO_WOULD_STRINGS — the dual-audit grep is scoped to source globs (`apps/desktop/**/*.ts*`, `**/*.rs`, `scripts/**`), not `docs/`; this cycle touched only `docs/` + a graphify log, so it introduces no shipping-code tokens. The docs deliberately *describe* the governed `StubAdapter` "not yet implemented" exception (registry entry 0002), which is the correct current-state truth, not a stub the audit should catch.
- **Acceptance criterion:** "Architecture docs updated to describe the post-SD-25 system truthfully (hub-of-hubs adapter pattern + governed stub-adapter exception, PCGen runner pipeline, corpus-ingest diagnostic, expanded CRB/APG/Bestiary-1 corpus coverage); graphify-update run; adversarially verify docs/architecture/ describes current state. PR/merge deferred to 8.5."
- **Status:** complete
- **Notes:**
  - **Truth-up done MANUALLY (register C3):** the known-broken `architecture_truth_up.py` (single-pipe vs `||` regex → false "no impact") was NOT trusted. Deltas were found by `git diff origin/develop...HEAD --stat` and each mapped to its owning doc via the README index Source-dirs column.
  - **graphify-update SUCCEEDED this cycle** (unlike the pre-existing errored `cluster-only`-only run): ran Phase A (`graphify update --no-cluster`, exit 0, 1199 files, 16461 nodes) then Phase B (`GRAPHIFY_FORCE=1 cluster-only --no-label --no-viz`, exit 0, 1131 communities). This is the zero-token budget-mode pipeline and is independent of the broken truth-up regex. Only the run-log is committed (per SD-23 precedent); the 18 MB `graph.json`/`GRAPH_REPORT.md`/`manifest.json`/`cache/` stay untracked, matching how graphify-out has always been handled here.
  - **Adversarial verification (Opus tier):** re-ran both README §Maintenance-contract verification one-liners after editing — **cited-path existence check clean, relative-link check clean**. Every corpus number is quoted from the audit-test assertions (`tests/sd24_equipment_coverage_audit.rs` / `tests/sd24_equipment_field_completion.rs`), not estimated: CRB `description` 2021/2977, APG `description` 331/338, APG spell 285/297 & full-text 284/297, Bestiary 1 equipment 4/4. Registered-command count re-derived by parsing the real `generate_handler!` list (38, correcting the doc's prior undercount of 36). The stub-adapter exception is confirmed present as registry entry 0002.
- **Discovery forwards:**
  - **DISCOVERED (CI):** SD-25 criterion 1.1's `sd11/`→`testerWorkbench/` and `sd15/`→`operatorTriage/` renames did NOT update `.github/workflows/check-release-manifest.yml`'s `paths:` filter, so its `apps/desktop/src/sd11/update/**` and `sd15/**` globs now match nothing — changes under those dirs no longer trip that PR gate. Documented in `release-pipeline.md`; a follow-up should repoint the filter (out of this docs-only cycle's file-touch grant).
  - **DISCOVERED (backend):** `beastiary1/mod.rs` still has no public `ALL`/count constant for `MonsterId` (unlike `ClassId::ALL`), forcing `corpus_ingest_diagnostic.rs` to hand-maintain `ALL_BESTIARY1_MONSTERS`; a future cycle adding a real `ALL` const removes that hand-sync hazard. (Carried from criterion 5.1's own receipt.)
- **Next-cycle plan:** 8.3 (release-notes, Haiku) → 8.4 (version bump 0.5.97→0.5.98, Haiku) → 8.5 (open `tranche/5-3 → develop` PR + resolve any merge conflicts). PR/merge is explicitly NOT this cycle's job (SD-24 D7 lesson). At 8.5's merge, the architecture docs are already truthful for the develop integration target.
