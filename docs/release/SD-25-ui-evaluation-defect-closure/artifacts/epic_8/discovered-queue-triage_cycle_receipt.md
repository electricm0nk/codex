# Cycle discovered-queue-triage — Epic 8 (precursor) / Non-numbered (queue-cap remediation)

- **Card ID:** t_c04c67e3
- **Commit SHA:** `12fdbd1`
- **Files touched:** `docs/release/SD-25-ui-evaluation-defect-closure/progress.md` (only)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (docs-only diff; no `apps/desktop/**`, `src/**`, or `scripts/**` touched)
- **Wired-integration audit result:** OK_NO_TOKENS (docs-only diff; no shipping source touched)
- **Acceptance criterion:** N/A — precursor work for Epic 8 closure (criterion 8.1's final-criterion scan), triggered by `loop-instruction.md §8`'s "`## DISCOVERED` queue > 10 entries" non-self-healable hard stop. Not a numbered criterion; receipt lives under `artifacts/epic_8/` per the dispatch instruction's explicit direction.
- **Status:** complete
- **Notes:**
  - **Trigger:** `## DISCOVERED` had grown to 18 top-level entries (counting only lines starting with `- ` directly under the heading; criterion 7.1's 9-class grouped batch already correctly counted as ONE entry per its own in-file batching rationale). This breached the >10 non-self-healable cap.
  - **Method:** every one of the 18 top-level entries was individually re-verified against the live codebase and against `## DONE`/status-matrix state — not assumed from the entries' own text. Verification commands run live during this cycle:
    - `grep -n "allow(dead_code)" apps/desktop/src-tauri/src/pf1_adapter.rs` → still present at lines 88/91 (3.4 dead-code entry: genuinely still open).
    - `grep -n "struct CharacterSummaryDto" -A15 apps/desktop/src-tauri/src/character_hub.rs` → no `revision_id` field (3.5 revisionId entry: genuinely still open).
    - `grep -rn "MonsterId" src/rules_core/rules_tables/beastiary1/mod.rs` → no `ALL` constant (5.1 entry: genuinely still open).
    - `grep -n '"version"' apps/desktop/package.json apps/desktop/src-tauri/tauri.conf.json` + `grep -n '^version' apps/desktop/src-tauri/Cargo.toml` → confirmed `0.5.98` vs `0.5.97` drift still present (5.1 buildVersionTriple entry: genuinely still open, expected to self-resolve at criterion 8.4, which is `not-started`).
    - `git log --oneline -- src/rules_core/rules_tables/crb/equipment_data/equipmods.rs` and the APG `ArmsArmor` records → only the original discovery commits touch these files; no later criterion addressed either (7.N cost-understatement and duplicate-key entries: genuinely still open).
    - `grep -rn "recomputeCharacter\|recompute_character" apps/desktop/src/` (excluding tests) → real non-test call sites now exist in `boundary/recomputeCharacter.ts` / `characterHub/characterHubRuntime.ts` (3.4 carry-forward-note entry: confirmed resolved by 3.5, as the entry's own text already asserted).
    - `cargo test --test sd24_wired_integration_audit` → **5/5 tests pass** (crb-description stub_adapter entry: confirmed resolved — independently, a concurrent session's commit `42f972b` had already landed the registry-0002 exclusion and self-annotated the entry `**RESOLVED 2026-07-22**` with card `t_6ffc2b84` and receipt `artifacts/epic_8/sd24-audit-registry-0002-exclusion_cycle_receipt.md` before this triage's own fetch+rebase; this triage's `cargo test` run is an independent re-confirmation, not the original fix).
    - Status matrix rows 7.2–7.10 (all `complete`) confirm criterion 7.1's entire 9-class grouped batch is now closed; the Druid sub-bullet was missing its sibling `RESOLVED` annotation (an inconsistency, not a live gap) — backfilled verbatim from criterion 7.7's own already-existing status-matrix row/receipt text (`artifacts/epic_7/druid-residue-audit-cycle_receipt.md`, card `t_42e830d8`), no new investigation or fabricated receipt.
  - **Disposition (18 → 10 top-level `## DISCOVERED` entries, honest triage, no under-counting):**
    - **6 entries moved to a new `## Resolved` section** (created between `## DISCOVERED` and `## Cycle log`), each annotated with the resolving criterion/commit/evidence, full original wording preserved:
      1. 3.4's carry-forward note → resolved by 3.5 (real frontend caller now exists).
      2. 4.3's grant-path correction note → archived as informational (self-contained within 4.3's own commit, no forward action).
      3. 4.3's concurrent-landing note (predicted 4.4 would remove `#[ignore]`) → resolved by 4.4 (status matrix confirms it was removed).
      4. Criterion 7.1's entire 9-class grouped entry → resolved (all 9 sub-items, 7.2–7.10, are `complete`; Druid's missing annotation backfilled from its own existing receipt).
      5. The crb-description item's `sd24_wired_integration_audit.rs` failure note → resolved (registry-0002 exclusion already landed, confirmed via live `cargo test`, 5/5 passing).
      6. The bestiary1 item's "~7 vs 4 records" estimate-correction note → archived as informational (the entry's own original text already says "No further action needed").
    - **3 entries (4.1's original `.pcg`-gap discovery, 4.2's reconfirmation, 4.4's re-scope note) consolidated into ONE grouped live entry** under `loop-instruction.md §8`'s "`## DISCOVERED` duplicates" self-heal category — all describe the same still-genuinely-open pilot-`.pcg` gap; no finding was dropped, all three original texts are preserved verbatim as sub-bullets in chronological order. This entry **stays live** in `## DISCOVERED` per the dispatch instruction's explicit "do NOT unilaterally defer the pilot-.pcg gap" directive — it is a real, unresolved gap, not resolved by consolidation.
    - **10 entries remain live** in `## DISCOVERED`: 3.4 dead-code annotations, 3.5 revisionId, 5.1 `MonsterId::ALL`, 5.1 `last_ingested_at` (SD-26 forward note), 5.1 `buildVersionTriple.test.ts` drift (pending 8.4), the consolidated 4.1/4.2/4.4 pilot-`.pcg` gap, 7.N APG `ArmsArmor` cost understatement, 7.N CRB `equipmods.rs` duplicate keys, 7.N `apg_spells.lst` `.MOD`-concatenation sweep candidate, 7.N `apg_spells.lst` `CLASSES:` level-parsing gap. All 10 were individually re-verified live (see above) as genuinely still open — none deferred or archived to hit the cap number; the cap is met honestly.
  - No code, test, or non-`progress.md` file was touched — this cycle is documentation-only triage/reorganization; nothing to RED/GREEN.
- **Discovery forwards:** none new — this cycle only re-triaged pre-existing entries; it did not discover new defects.
- **Next-cycle plan:** Epic 8's criterion 8.1 (final criterion scan) can now proceed — the `## DISCOVERED` queue-cap hard stop is cleared with an honest 10-entry live count and a preserved, annotated audit trail in the new `## Resolved` section. The 10 remaining live entries (listed above) are candidates for future dispatch (SD-26 or a dedicated Epic-1/Epic-7 follow-on bundle) but are out of SD-25's own remaining scope (Epic 8 is closure-only, no new feature/fix cycles).
