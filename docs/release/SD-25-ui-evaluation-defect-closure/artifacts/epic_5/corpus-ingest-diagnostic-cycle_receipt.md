# Cycle corpus-ingest-diagnostic — Epic 5 (Corpus Ingest Diagnostic) / Criterion 5.1

- **Card ID:** (filled in after kanban mint — see below)
- **Commit SHA:** (filled in after commit — see below)
- **Files touched:**
  - NEW `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs`
  - NEW `apps/desktop/src/characterHub/CorpusIngestDiagnosticPanel.tsx`
  - `apps/desktop/src-tauri/src/main.rs` (mod declaration, import, `invoke_handler` registration)
  - `apps/desktop/src/characterHub/CharacterHubPage.tsx` (routing: new `corpusIngestDiagnostic` mode)
  - `apps/desktop/src/characterHub/LandingScreen.tsx` (real affordance: "Corpus Ingest Diagnostic" link button)
- **Identifier audit result:** OK_NO_BUNDLE_TAGS (scoped to added lines in the five touched files; two pre-existing `-` deletion lines referencing `sd13_support_state_matrix` in `main.rs`'s cumulative bundle diff are leftovers from commit `9206ad0` (SD-21 identifier-cleanup rename), not additions from this cycle — verified via `git log -p`)
- **Wired-integration audit result:** OK_NO_TOKENS
- **Acceptance criterion:** `corpus_ingest_diagnostic` Tauri command + UI panel route (Epic 5), per `cycles/5_1.md`: command returns `Vec<BookIngestStatus>` (`book_id`, `status`, `last_ingested_at`, `content_kind_counts`) computed from the REAL ingested-corpus state; panel renders the real result via a boundary wrapper + `invoke()`.
- **Status:** complete
- **Notes:**
  - **File-touch grant scope respected as written** — no drift found; all four granted paths (`corpus_ingest_diagnostic.rs`, `CorpusIngestDiagnosticPanel.tsx`, `main.rs`, a routing touch under `apps/desktop/src/characterHub/`) exist and were used exactly as named. The routing touch spans two files (`CharacterHubPage.tsx` for the mode/route, `LandingScreen.tsx` for the actual clickable affordance) — both are under the granted `apps/desktop/src/characterHub/` directory.
  - **"Real ingested-corpus state" interpretation:** the PCGen corpus itself is never vendored into this repo (only reachable via `PCGEN_CORPUS_ROOT` at test time — see `docs/architecture/corpus-ingest.md`). The actually-shipped, actually-queryable "ingested" state at runtime is the `rules_tables` book-partitioned store (`docs/architecture/rules-data-tables.md`): four populated books, `RuleSetId::{Crb,Apg,Acg,Bestiary1}`. `content_kind_counts` per book is computed by calling the real public APIs each book already exposes (`ClassId::ALL.len()`, `RaceId::ALL.len()`, `feats::feat_tables().len()`, `SPELL_LIST.len()`, `equipment_tables().len()`, `ApgClassId::ALL.len()`, `AcgClassId::ALL.len()`) — never a fixture/hand-guessed number. This satisfies the A1 no-stub-doctrine lesson the cycle doc calls out explicitly.
  - **`beastiary1` monster count exception:** `beastiary1::mod.rs`'s `MonsterId` enum has no public `ALL`/count constant (unlike the other three books' class-roster enums), and this criterion's file-touch grant does not extend to that module. Rather than either fabricate a count or block, `corpus_ingest_diagnostic.rs` carries its own exhaustive 41-entry `ALL_BESTIARY1_MONSTERS: &[MonsterId]` list, compiler-checked against the real enum (a variant add/remove/rename that isn't mirrored here fails to compile, so it can't silently drift wrong — though it can go stale-but-still-compiling if a future cycle adds new monsters without touching this file). Forwarded to `## DISCOVERED` below: `beastiary1::mod.rs` should grow its own `MonsterId::ALL` constant, mirroring `ClassId`/`ApgClassId`/`AcgClassId`, so this diagnostic (and any future consumer) doesn't need a duplicate list.
  - **`last_ingested_at` sourcing:** computed at runtime via `git log -1 --format=%cI -- <book-dir>` against the repo root (derived from `env!("CARGO_MANIFEST_DIR")`, no `build.rs` edit needed — stayed inside the file-touch grant). This mirrors the existing `git_short_sha()` idiom in `apps/desktop/src-tauri/build.rs` (used for `CODEX_GIT_SHA`/`load_backend_health`), including its graceful-degradation shape: returns `None` when `git`/`.git` isn't reachable (e.g. a packaged production build shipping only the compiled binary, no `.git` checkout). This is real, verifiable data (the actual last commit date touching that book's directory) — not a fixture timestamp. Flagged to `## DISCOVERED`: SD-26's planned JSON ingest cache should replace this git-shell-out with a persisted ingest-time timestamp that survives packaging, since production builds will report `null` today.
  - **`status` field:** computed (`"populated"` if the book's `content_kind_counts` sum to > 0, else `"empty"`), not hand-set per book. All four books evaluate to `"populated"` today, which is true and real.
  - **`corpus_fixtures.rs` was NOT reused or touched** — that module bundles a small, fixed set of corpus records as a Tauri *resource* for a different purpose (proving `compute_pilot_with_corpus` end-to-end in the live UI), explicitly documented as "not a general corpus provider." Using it here would have been exactly the kind of fixture-in-production-path the cycle doc warns against.
  - **Pre-existing, unrelated test failure observed and NOT caused by this change:** `apps/desktop/src/sd21/buildVersionTriple.test.ts` fails both with and without this cycle's diff (`Cargo.toml version must match package.json version: expected 0.5.98, got 0.5.97`) — confirmed via `git stash`/`npm test`/`git stash pop` before and after. Pre-existing version-drift issue, out of this cycle's scope (Epic 8's version-increment-cycle owns this).
- **Discovery forwards:**
  - `beastiary1::mod.rs` should gain a public `MonsterId::ALL` constant (mirroring `ClassId::ALL`/`ApgClassId::ALL`/`AcgClassId::ALL`) so consumers don't need to hand-duplicate the 41-monster list — this file currently does, out of file-touch-grant necessity.
  - SD-26's JSON ingest cache should replace `corpus_ingest_diagnostic.rs`'s `git log`-based `last_ingested_at` with a persisted ingest-time timestamp, since a packaged production build (no `.git` checkout shipped) will report `null` for every book today.
  - Pre-existing `apps/desktop/src/sd21/buildVersionTriple.test.ts` failure (`Cargo.toml` 0.5.97 vs `package.json` 0.5.98 drift) — unrelated to this cycle, already failing before this cycle's diff; belongs to whichever cycle/epic owns the version-increment-cycle.
- **Next-cycle plan:** Epic 5 has no further named criteria in this bundle beyond 5.1 per `artifacts/README.md`'s epic table (`epic_5` concurrency: serial, single criterion). Next work in the loop resumes at whichever epic/criterion the orchestrator schedules next.
