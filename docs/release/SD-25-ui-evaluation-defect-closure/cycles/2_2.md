# Criterion 2.2 — `tranche/5-3` pushed to origin (Epic 2: Operator Pre-Launch)

- **Concurrency:** serial (gating epic)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_2/branch-pushed-cycle_receipt.md`
- **Gated on:** none

## File-touch grant (register C1)

No code writes. Receipt + `progress.md` row update only.

## Check (RED → GREEN semantics)

- RED: `git ls-remote origin tranche/5-3` returns nothing, or local HEAD is ahead of/diverged from `origin/tranche/5-3`.
- GREEN: branch exists on origin and local HEAD matches `origin/tranche/5-3` (or is fast-forwardable); capture `git ls-remote` + `git status -sb` output in the receipt.

## RE-AUDIT

No diff expected; if any file changed, the cycle is out of scope — return to backlog.
