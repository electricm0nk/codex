# Criterion 4.1 — `scripts/pcgen-run-character.sh` (Epic 4: PCGen Runner Scaffolding)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_4/pcgen-run-script-cycle_receipt.md`
- **Gated on:** E3.4 (per loop-instruction §3 epic gating)

## File-touch grant (register C1)

- NEW `scripts/pcgen-run-character.sh` (Bash + Gradle + jq)

## RED

`scripts/pcgen-run-character.sh` does not exist; invoking it fails.

## GREEN

Script exists, is executable, and drives the PCGen Gradle wrapper at `/home/ubuntu/workspace/repos/pcgen/gradlew` (verified present, loop-instruction §4) to run a character file end-to-end, emitting output for 4.2's normalizer. Exercise against a real fixture from `tests/fixtures/rules_core/pf1_*_level*_*.txt` — no mocked PCGen output.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
