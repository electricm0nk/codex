# Criterion 1.1 — Source-code identifier audit (Epic 1: Identifier Cleanup)

- **Concurrency:** serial (single cycle; fires FIRST)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_1/identifier-audit-cycle_receipt.md`
- **Gated on:** none

## File-touch grant (from epic-breakdown.md §Epic 1 — never copied forward; register C1)

- Renames of bundle-tagged identifiers (files, modules, constants, resource dirs) under `apps/desktop/`, `apps/desktop/src-tauri/`, `src/`, `scripts/`, plus every call-site/import the rename touches.
- The receipt artifact above.
- Note: `apps/desktop/src-tauri/src/sd13_support_state_matrix.rs` is a known live example — the RED grep will return real hits.

## RED

`git grep -nE '\b(sd[0-9]+_|SD[0-9]+_|Sd[0-9]+|t_[0-9a-f]{8,})' apps/desktop/ apps/desktop/src-tauri/ src/ scripts/` returns ≥1 hit. (Trailing `\b` deliberately omitted; pattern generalized per register A7 — the old `\b`-suffixed form silently missed `_`-joined identifiers.)

## GREEN

Renames land; the same `git grep` returns 0 hits; full test suite still passes (renames are behavior-neutral).

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
