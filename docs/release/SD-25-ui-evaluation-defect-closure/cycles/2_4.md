# Criterion 2.4 — Working tree clean on `tranche/5-3` (Epic 2: Operator Pre-Launch)

- **Concurrency:** serial (gating epic)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_2/tree-clean-cycle_receipt.md`
- **Gated on:** none

## File-touch grant (register C1)

No code writes. Receipt + `progress.md` row update only.

## Check (RED → GREEN semantics)

- RED: `git status --porcelain` is non-empty, or current branch ≠ `tranche/5-3`.
- GREEN: empty porcelain output on `tranche/5-3`; capture output in the receipt.
- Dirty tree is **self-healable** per `loop-instruction.md §8` (stash/commit stray planning-doc edits via §5's protocol); a tree diverged from `tranche/5-3` needing manual rebase is NOT — write `## Open blockers` and exit FAIL.

## RE-AUDIT

No diff expected beyond any self-heal commit; if product code changed, the cycle is out of scope — return to backlog.
