# Criterion 4.2 — `scripts/pcgen-normalize-output.py` (Epic 4: PCGen Runner Scaffolding)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_4/pcgen-normalize-cycle_receipt.md`
- **Gated on:** E3.4 (per loop-instruction §3 epic gating)

## File-touch grant (register C1)

- NEW `scripts/pcgen-normalize-output.py` (Python)

## RED

`scripts/pcgen-normalize-output.py` does not exist; invoking it fails.

## GREEN

Script normalizes real PCGen output (the legacy `code/testsuite/base-xml.ftl` route noted in `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`) into the golden-fixture comparison shape used by `src/oracle_validation/{golden_fixture,selected_parity_dimensions}.rs`. Test against real captured PCGen output, not a hand-written sample.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
