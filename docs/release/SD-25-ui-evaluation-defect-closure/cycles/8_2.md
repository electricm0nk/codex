# Criterion 8.2 — Architecture closure pipeline (Epic 8; fires LAST)

- **Concurrency:** serial
- **Subagent tier:** **Opus** (template §2 adversarial-verification tier)
- **Receipt:** `../artifacts/epic_8/architecture-closure-cycle_receipt.md`
- **Gated on:** 8.1 complete

## File-touch grant (register C1)

- `docs/architecture/**` (truth-up per the living-architecture doctrine — every SD closure must refresh it)
- Graphify outputs wherever the graphify-update skill writes them
- Receipt + `progress.md`

## Steps

1. **Architecture truth-up — MANUAL (register C3).** `~/.hermes/profiles/god-emporer/skills/devops/architecture-truth-up/scripts/architecture_truth_up.py` is known-broken: its `parse_source_dirs_index` regex needs literal `||` delimiters, the real `docs/architecture/README.md` uses single-pipe rows, so it always reports a false-negative "no architecture impact". Either fix the regex first (out-of-repo, operator's profile — only with operator consent) or diff `tranche/5-3` against develop and update `docs/architecture/` by hand as SD-24's closing session did. **Never trust the script's no-impact output.** This bundle's real architecture deltas at minimum: `RuleSystemAdapter`/`Pf1Adapter`/`StubAdapter` (E3), PCGen runner scaffolding (E4), corpus-ingest diagnostic (E5).
2. **Graphify-update** per its skill.
3. Adversarially verify (this is why the cycle is Opus-tiered): does `docs/architecture/` now describe the post-SD-25 system truthfully, including the stub-adapter's registered exception?

Note: PR + merge is criterion 8.5, NOT this cycle (SD-24's D7 lesson — its 8.2 write scope couldn't open the PR).

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit (docs-only diff expected).
