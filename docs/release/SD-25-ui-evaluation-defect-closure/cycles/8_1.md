# Criterion 8.1 — Final criterion scan (Epic 8: Closure Epilogue; fires LAST)

- **Concurrency:** serial
- **Subagent tier:** Sonnet (the follow-up completeness pass `8.1.final` is Opus per dispatcher override)
- **Receipt:** `../artifacts/epic_8/final-scan-cycle_receipt.md`
- **Gated on:** E7 complete (all declarative + all spawned dynamic criteria terminal)

## File-touch grant (register C1)

Read-only over the whole bundle; writes limited to the closure-readiness report artifact + receipt + `progress.md`.

## Deliverable

Scan every criterion (declarative AND dynamically-spawned) for terminal state: each is `complete` with a receipt, or has an `## Open blockers` entry. Cross-check three sources against each other: `progress.md` status matrix, `../artifacts/epic_*/` receipts on disk, and kanban done-receipts on `codex-tranche-5`. Any three-way mismatch = not closure-ready.

Also verify the SD-24 carry-forward custody claim: every register §A item's assigned criterion is terminal, and §B's batch (7.P) is terminal. GREEN = closure-readiness report written with zero unaccounted criteria.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit (docs-only diff expected).
