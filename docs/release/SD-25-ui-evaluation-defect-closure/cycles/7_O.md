# Criterion 7.O — GE-07 pilot-shell-snapshot real implementation (Epic 7; SD-24 carry-forward A1)

- **Concurrency:** serial
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_7/ge07-snapshot-cycle_receipt.md`
- **Gated on:** **open question Q5** (`risks-and-open-questions.md §4`) — an operator design decision. **Until Q5 is answered, the ONLY dispatchable action is surfacing the design-decision request** (a receipt noting the question was raised, plus an `## Open blockers`-adjacent note if it stalls the epic). Do NOT implement on a guessed contract.

## File-touch grant (register C1) — implementation phase only, post-Q5

- `apps/desktop/src-tauri/src/` — the `load_pilot_shell_snapshot` Tauri command (locate its defining file with `git grep -n load_pilot_shell_snapshot`)
- `apps/desktop/src/boundary/loadPilotShellSnapshot.ts`
- The SD-11 tester-workbench consumer, only as far as the changed contract requires

## Background (register A1)

Both ends unconditionally return hardcoded GE-07-era fixture data (`case_id: "ge07-e1-scaffold-placeholder"`) regardless of real character state — fixture-only data in a production command path, a genuine no-stub-doctrine violation. Legacy scaffolding predating the bundle-tag convention; consumed only by the SD-11 internal tester workbench, which labels it honestly.

## RED → GREEN (post-Q5)

- RED: a test asserting the snapshot reflects real character state (per Q5's answered contract) fails against the hardcoded fixture.
- GREEN: headless-core-backed snapshot computed per the Q5 contract; fixture constants deleted; workbench still renders.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`. The `ge07-e1-scaffold-placeholder` string's removal should itself clear a `placeholder` token hit.
