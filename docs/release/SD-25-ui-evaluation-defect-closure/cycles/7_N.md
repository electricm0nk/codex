# Criterion 7.N — Equipment/spell corpus intake (Epic 7; SD-24 carry-forward)

- **Concurrency:** parallel: yes across the 4 items below → `isolation: worktree` (dispatcher `PARALLEL_OVERRIDE["7.N"]`)
- **Subagent tier:** Sonnet
- **Receipts:** `../artifacts/epic_7/corpus-intake-<item>_cycle_receipt.md` (one per item)
- **Gated on:** E6 (bundle ordering); no hard technical dependency
- **Register custody:** A8, A10–A17; SD-24 Open Blockers #1–#3

## The 4 items (disjoint file-touch; one worktree cycle each)

1. **CRB description field** — ceiling 61.2% (1821/2977); the 1156 remainder (mostly `cr_equipmods.lst` + slot-type markers) have no `DESC:` in the LST corpus. Web-second-source pass required.
2. **APG description field** — 0/338 rows carry `DESC:`; hardest ceiling. Web-second-source pass required.
3. **APG full spell text** — 36/297 spells lack a `.MOD` full-text record or `SCHOOL:`/`CLASSES:` base token (Summoner eidolon spells, one `PRESPELL` variant, corpus typo `Wall of Thorms`). Needs a spell-text source, not equipment prose.
4. **Bestiary 1 equipment + spells** — plain scope gap, NOT a corpus ceiling: no `beastiary1` equipment module exists; ~7 real records unclaimed (`b1_equip_general.lst` ×2, `b1_equip_arms_armor.lst` ×3, `b1_equip_magic_items.lst` ×2). Should mechanically reach whatever ceiling the LST supports. `EquipmentTableEntry` already has `weight`/`description` fields (register A12) — populate, don't re-add. Confirm no spell-list concept exists before treating spells as a gap (register A13).

## File-touch grant per item (register C1)

Each item touches ONLY its own book's table module(s) under the rules-tables tree (locate the real paths — e.g. `git grep -l EquipmentTableEntry` — before writing; register B1–B3 proved per-book layouts differ). Item 4 additionally creates the new `beastiary1` equipment module + its `mod.rs` registration.

## Web second-source rules (operator directive 2026-07-21 — verbatim obligations)

- Sources: **d20pfsrd.com** and/or **aonprd.com** only, where the LST corpus genuinely lacks the data.
- Identity-match first: name + level/school (spells) or name + category/cost (equipment) against the LST record; a same-named cross-book or 3.5e/PF2e cousin is a false match.
- Cite the source URL in the receipt for every web-sourced field.
- Reasonable request pacing; fetch only the specific pages for identified gap records — no bulk scraping.
- No confident match → leave `None`, keep the ceiling. Never fabricate.
- A successful pass retroactively closes SD-24's Open Blockers #1/#2 — note it in the receipt so SD-24's record can be annotated.

## Method notes (register A8, A10, A11)

- **A8:** before writing another ad-hoc script, decide deliberately whether to build the shared `pcgen_import`-backed codegen path (`src/pcgen_import/lst_parser/{equipment,spell}.rs` → semantic map → Rust literals). Record the choice + reasoning in the receipt.
- **A10:** for Bestiary-1, check for a `DESC:`/`SPROP:`-equivalent convention first (ACG hit 98.1% via `SPROP:` alone).
- **A11:** apply the `SOURCELONG:`-header exclusion, `.COPY=`-merge rule, and full-category sweep (equipmods equivalent) from the start.

## RED → GREEN

- RED per item: coverage measurement showing the documented gap (capture the count).
- GREEN per item: coverage at the honest achievable ceiling, every filled field traceable to an LST token or cited URL; tests green.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit. **Expect A9-class collisions:** real SRD text may contain forbidden tokens (`hack`, `todo`-like words). Handle via the standing audit test's named-bucket/corpus-field exclusion and document in the receipt — never edit rules text to satisfy the grep.
