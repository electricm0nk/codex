# Criterion 8.4 — Build version increment → 0.5.98 (Epic 8; fires LAST)

- **Concurrency:** serial
- **Subagent tier:** **Haiku** (mechanical housekeeping)
- **Receipt:** `../artifacts/epic_8/version-bump-cycle_receipt.md`
- **Gated on:** 8.1 complete

## File-touch grant (register C1 — exactly these three, nothing else)

- `apps/desktop/package.json`
- `apps/desktop/src-tauri/tauri.conf.json`
- `apps/desktop/src-tauri/Cargo.toml`

## RED → GREEN

- RED: all three files read `0.5.97` (develop's value at bundle cut; verify — a concurrent bundle may have moved it, in which case increment from the LIVE value and note it).
- GREEN: all three read the same incremented value (first concrete: `0.5.98`); `cargo metadata` / a build parses cleanly; the three values are byte-identical.
- Reminder (bundle doctrine): the TRANCHE digit only bumps when a new `tranche/N` branch is cut — this cycle bumps only the patch build number.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit; both greps must return `OK_*`.
