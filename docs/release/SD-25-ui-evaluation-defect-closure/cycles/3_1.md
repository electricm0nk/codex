# Criterion 3.1 — `RuleSystemAdapter` trait definition (Epic 3: Hub of Hubs)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_3/rule-system-adapter-trait-cycle_receipt.md`
- **Gated on:** E2 complete

## File-touch grant (register C1)

- NEW `apps/desktop/src-tauri/src/rule_system_adapter.rs`
- `apps/desktop/src-tauri/src/main.rs` — module declaration only (the crate entrypoint; there is no `lib.rs`)
- Tests inline (`#[cfg(test)] mod tests`) per this crate's convention (register B9 — no `tests/` dir exists in this crate)

## RED

A test that constructs a `dyn RuleSystemAdapter` (or a trait-bound generic) and calls each method fails to compile — the trait does not exist.

## GREEN

Trait defined with the full surface from epic-breakdown.md §3.1: `chassis_resolve`, `level_up`, `save_character`, `append_to_character`, `recompute`, `list_saved_characters`, `load_saved_character`. Compiles; trait-surface test passes.

## Carry-forward notes

- Register A2 (shared with 3.2): design `level_up`'s signature so it CAN express a multiclass mix (explicit per-class from/to sub-levels or a dedicated multiclass entry point) — do not bake in the single-class-only shape that made `compute_level_up_grants` return an empty plan for any mix.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
