# Criterion 5.1 — `corpus_ingest_diagnostic` Tauri command + UI panel route (Epic 5)

- **Concurrency:** serial (one cycle)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_5/corpus-ingest-diagnostic-cycle_receipt.md`
- **Gated on:** E3.4 (per loop-instruction §3 epic gating)

## File-touch grant (register C1)

- NEW `apps/desktop/src-tauri/src/corpus_ingest_diagnostic.rs`
- NEW `apps/desktop/src/characterHub/CorpusIngestDiagnosticPanel.tsx`
- `apps/desktop/src-tauri/src/main.rs` — command registration
- Minimal routing touch in `apps/desktop/src/characterHub/` to make the panel reachable (a real affordance — no orphan component)

## RED

No `corpus_ingest_diagnostic` command exists; the panel route 404s / does not render.

## GREEN

Command returns `Vec<BookIngestStatus>` (`book_id`, `status`, `last_ingested_at`, `content_kind_counts`) computed from the REAL ingested-corpus state — fixture data in this path is a no-stub-doctrine violation (this bundle carries the A1 lesson). Panel renders the real result via a boundary wrapper + `invoke()`.

**Sketch-only boundary:** SD-26 fans out the full status table + flags + ETA once the JSON cache lands. Keep the surface to exactly the four fields above; anything more goes to `## DISCOVERED`.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
