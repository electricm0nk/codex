# Criterion 4.3 — `tests/oracle_validation/pcgen_runner_smoke.rs` (Epic 4: PCGen Runner Scaffolding)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_4/pcgen-smoke-test-cycle_receipt.md`
- **Gated on:** E3.4 (per loop-instruction §3 epic gating)

## File-touch grant (register C1)

- NEW `tests/oracle_validation/pcgen_runner_smoke.rs` (root-crate integration test; if the root crate turns out to use a different integration-test layout, follow the crate's real convention and record the deviation in the receipt — do NOT invent a new layout)

## RED

The smoke test does not exist / fails: it asserts the 4.1 script + 4.2 normalizer are invocable and produce parseable output for the pilot case.

## GREEN

Smoke test passes when run against the real scripts. If 4.1/4.2 land after this cycle (parallel dispatch), the test may be authored `#[ignore]`-gated with the ignore removed by 4.4's verification cycle — state which in the receipt.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
