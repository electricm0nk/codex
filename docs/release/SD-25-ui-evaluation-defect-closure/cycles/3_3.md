# Criterion 3.3 — `StubAdapter` future-system stub (Epic 3: Hub of Hubs)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_3/stub-adapter-cycle_receipt.md`
- **Gated on:** E2 complete; trait from 3.1 available (compile-time dependency)

## File-touch grant (register C1)

- NEW `apps/desktop/src-tauri/src/stub_adapter.rs`
- `apps/desktop/src-tauri/src/main.rs` — module declaration
- `governance/wired-integration-stubs-registry.md` — REQUIRED registry entry (see below)

## RED

A test asserting a non-PF1 `rule_system_id` resolves to an adapter fails — no stub adapter exists.

## GREEN

`StubAdapter` implements the trait, returning "Would render for system X; not yet implemented" results. **The wired-integration doctrine forbids "Would …" strings in shipping code — this stub is only GREEN if its entry lands in `governance/wired-integration-stubs-registry.md` in the same commit**, with the operator-granted justification (future-system rollout is operator-pinned). Without the registry entry, the §6 dual-audit failure is NOT self-healable for this cycle.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit. The "Would" hit inside `stub_adapter.rs` is expected and covered by the registry entry — document it in the receipt as a registered exclusion, not a violation. Everything else must be `OK_*`.
