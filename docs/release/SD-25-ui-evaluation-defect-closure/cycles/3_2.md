# Criterion 3.2 — `Pf1Adapter` extraction from `character_hub.rs` (Epic 3: Hub of Hubs)

- **Concurrency:** parallel: yes → `isolation: worktree`
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_3/pf1-adapter-extraction-cycle_receipt.md`
- **Gated on:** E2 complete (trait shape coordination with 3.1 if both live concurrently — conflicting-file check per §8)

## File-touch grant (register C1)

- NEW `apps/desktop/src-tauri/src/pf1_adapter.rs` (existing logic moves here)
- `apps/desktop/src-tauri/src/character_hub.rs` (extraction source)
- `apps/desktop/src-tauri/src/main.rs` — module declaration
- `src/rules_core/level_up.rs` — resolving register A2 in this cycle (see below)

## RED

Existing character-hub tests pass before the move (baseline capture); a test asserting the trait's PF1 implementation lives at `pf1_adapter.rs` fails. Additionally: a test asserting that `level_up_character`, `add_equipment_selection`, `add_spell_selection`, and `appendToCharacter` each advance `revision_id` on the saved character fails against current behavior (only `reSaveCharacter` advances it today).

## GREEN

After the move, all pre-existing tests still pass unchanged; `Pf1Adapter` implements `RuleSystemAdapter` in `pf1_adapter.rs`. `mutate_saved_character_at_root` advances `revision_id` on every call, so every command routed through it (including the four named above) advances the counter — not just `reSaveCharacter`.

## Carry-forward notes (register A2, A5 — verbatim obligations from epic-breakdown.md §3.2)

- **A2:** the adapter's `level_up` must NOT silently inherit `level_up::compute_level_up_grants`'s top-level-dispatcher gap (returns honestly-empty `LevelUpPlan::default()` for any 2+-class mix, including Fighter+Wizard). Widen the signature to accept explicit per-class from/to sub-levels, or add a dedicated multiclass entry point, before or as part of this extraction. Add a RED test: top-level entry point with a Fighter+Wizard mix must NOT return an empty plan.
- **A5 — operator-confirmed 2026-07-21 (`decisions.md §11`):** fold revision-advancing into `mutate_saved_character_at_root` itself as part of this extraction. This is a behavior change (not a pure move) and is explicitly approved — do not defer to `## DISCOVERED`.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
