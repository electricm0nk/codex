# Criterion 3.4 — Tauri command surface routes through the hub-of-hubs (Epic 3)

- **Concurrency:** parallel: no (multi-file; depends on 3.1–3.3)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_3/command-routing-cycle_receipt.md`
- **Gated on:** 3.1, 3.2, 3.3 complete

## File-touch grant (register C1)

- `apps/desktop/src-tauri/src/append_to_character.rs`
- `apps/desktop/src-tauri/src/recompute_character.rs`
- `apps/desktop/src-tauri/src/re_save_character.rs`
- `apps/desktop/src-tauri/src/main.rs` — only if command registration signatures change

## RED

A test invoking each command with `rule_system_id: "pf1"` and asserting dispatch through the `RuleSystemAdapter` trait fails — commands call PF1 logic directly today.

## GREEN

All three commands accept a `rule_system_id: String` argument and dispatch through the trait; existing behavior for PF1 unchanged (pre-existing inline tests still pass); an unknown `rule_system_id` routes to `StubAdapter`.

## Carry-forward notes (register A3)

These three commands have **zero frontend callers** today — no `boundary/*.ts` wrapper, no `invoke()` call site in `apps/desktop/src`. Routing them through the trait is necessary but not sufficient; criterion 3.5 owns closing the caller gap. Note in the receipt's next-cycle plan.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
