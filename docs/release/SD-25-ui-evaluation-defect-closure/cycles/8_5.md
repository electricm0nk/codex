# Criterion 8.5 — `tranche/5-3 → develop` PR + merge (Epic 8; fires LAST)

- **Concurrency:** serial (terminal criterion)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_8/closure-pr-cycle_receipt.md`
- **Gated on:** 8.1–8.4 complete

## File-touch grant (register C1)

No new content authoring. Permitted: merge-conflict resolutions per template §6 step 4 (resolve toward develop's truth for files this bundle didn't intentionally change; toward the bundle's truth for its own deliverables — record every resolution in the receipt).

## Procedure

1. §5's fetch+rebase; full test suite green on the rebased head.
2. `gh pr create` from `tranche/5-3` into `develop` — title `SD-25 — UI-Eval Defect Closure + Hub-of-Hubs + PCGen Runner + Ingest Diagnostic`, body linking `release-notes.md`, the closure-readiness report (8.1), and `sd24-carry-forward-register.md` disposition.
3. **The operator merges the PR** (standing repo convention — do not self-merge). GREEN for this cycle = PR open, checks green, merge-conflict-free (or conflicts resolved and re-pushed), operator notified.
4. Post-merge (operator-confirmed): mint the final kanban done-receipt and close the bundle in `progress.md`.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the full `merge-base(develop)...HEAD` diff one last time — this is the whole-bundle final gate, not just the last cycle's diff.
