# Criterion 4.4 — Verification cycle: run all three against the pilot case (Epic 4)

- **Concurrency:** parallel: no (multi-artifact verification)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_4/pcgen-runner-verification-cycle_receipt.md`
- **Gated on:** 4.1, 4.2, 4.3 complete

## File-touch grant (register C1)

- No new production files. Permitted: removing a 4.3 `#[ignore]` gate; small fixes to 4.1/4.2/4.3's own files if verification exposes defects (each fix noted in the receipt).

## Inputs (verbatim from epic-breakdown.md §4.4)

- `tests/fixtures/rules_core/pf1_human_fighter_level1_ge06_deterministic_input.txt`
- `tests/fixtures/oracle_validation/pf1_human_fighter_level1_golden_fixture.txt`

## RED → GREEN

- RED: any of the three artifacts fails end-to-end against the pilot case.
- GREEN: `pcgen-run-character.sh` → real PCGen run → `pcgen-normalize-output.py` → output comparable against the golden fixture → `pcgen_runner_smoke.rs` passes un-ignored. Capture the actual command transcript in the receipt — this is the epic's no-stub proof.

## RE-AUDIT

Run `loop-instruction.md §6` dual-audit on the final diff; both greps must return `OK_*`.
