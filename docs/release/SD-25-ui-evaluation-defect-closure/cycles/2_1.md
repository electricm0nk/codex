# Criterion 2.1 — Kanban board `codex-tranche-5` reachable (Epic 2: Operator Pre-Launch)

- **Concurrency:** serial (gating epic)
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_2/board-reachable-cycle_receipt.md`
- **Gated on:** none

## File-touch grant (register C1)

No code writes. Receipt + `progress.md` row update only.

## Check (RED → GREEN semantics)

- RED: `hermes kanban boards` (per template §1 item 1 — NOT `list-boards`) does not list `codex-tranche-5`, or the command errors.
- GREEN: board listed and reachable; capture the command output verbatim in the receipt.
- Per the assignee/daemon-hazard doctrine: cards on this board are minted `--assignee operator`, receipt-only lifecycle — verify the daemon has not auto-claimed anything.

## RE-AUDIT

No diff expected; if any file changed, the cycle is out of scope — return to backlog.
