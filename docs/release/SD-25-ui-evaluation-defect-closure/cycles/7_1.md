# Criterion 7.1 — Per-class residue intake (Epic 7: Coverage Backlog)

- **Concurrency:** serial
- **Subagent tier:** Sonnet
- **Receipt:** `../artifacts/epic_7/residue-intake-cycle_receipt.md`
- **Gated on:** E6 (per loop-instruction §3 epic gating)

## File-touch grant (register C1)

- `progress.md` (`## DISCOVERED` entries only) + receipt. Intake criterion — no product code.

## Deliverable

Read SD-24 Epic 4's `per-class-coverage-matrix.md` (in `../SD-24-beta-readiness-and-multiclass/artifacts/`; locate with `find` if moved) and emit one `## DISCOVERED` entry per dispatchable class-feature gap, each suggesting a `7.<next>` criterion.

## Carry-forward obligation (register A6 — verbatim from epic-breakdown.md §7.1)

Explicitly include the 9 CRB classes SD-24 never audited for the `class_spell.*`-vs-`class_feature.*`-prefix bug found and fixed for Wizard: **Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger**. Each has a landed `level_up/<class>.rs` module; the bug class (a later `pilot_compute.rs` grounding never added to the module's explanation-id filter) could recur in any of them. LOW priority tag; one `level_up/<class>.rs` file per spawned cycle per the file-touch partition.

**Queue-cap awareness:** `## DISCOVERED` > 10 entries is non-self-healable (§8). If intake would exceed 10, batch related features into grouped entries rather than overflowing the queue, and say so in the receipt.

## RE-AUDIT

Docs-only diff; run the dual-audit anyway.
