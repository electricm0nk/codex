# SD-24 → SD-25 Carry-Forward Register

Full custody of every `## DISCOVERED` entry and every `## TODO` remainder from SD-24's `progress.md` — **all 41 DISCOVERED entries + 4 TODO items**, closed 2026-07-21 — so nothing found during SD-24's run gets lost at the `tranche/5-2 → develop` merge. Source of truth for each item's full original text: `../SD-24-beta-readiness-and-multiclass/progress.md ## DISCOVERED` and `## Open blockers`. Every one of the 41 is mapped below (sections A-D); none are omitted, including the ones with no SD-25 action at all — those are recorded for audit-trail completeness, not silently dropped.

Each row: **origin** (SD-24 epic/criterion) · **finding** (condensed) · **disposition** (already-fixed-in-cycle / informational-no-action / real-follow-on) · **SD-25 assignment** (epic/criterion, or "register only" if no dispatchable work remains).

## A. Real follow-on work — assigned to an SD-25 epic/criterion

| # | Origin | Finding | SD-25 assignment |
|---|---|---|---|
| A1 | epic-3 / 3.1 | `load_pilot_shell_snapshot` Tauri command + `loadPilotShellSnapshot.ts` unconditionally return hardcoded GE-07 fixture data regardless of real character state — fixture-only data in a production path (no-stub-doctrine violation). Legacy scaffolding, consumed only by the SD-11 tester workbench. | **Epic 7**, new criterion (added below) — needs an operator design decision first (what a headless-core-backed snapshot computes, from what input contract), then a real implementation. Also register in `risks-and-open-questions.md §4` as an open question. |
| A2 | epic-5 / 5.1 | `level_up::compute_level_up_grants`'s top-level dispatcher (`src/rules_core/level_up.rs`) still returns an honestly-empty `LevelUpPlan::default()` for any 2+-class mix, including Fighter+Wizard — it never routes to the per-class functions for a mix. SD-24's tests called the per-class functions directly (correct per-class, but any caller using only the top-level entry point sees an empty plan for a multiclass character). | **Epic 3**, criterion 3.1/3.2 (`RuleSystemAdapter.level_up` / `Pf1Adapter` extraction) — the adapter's `level_up` method must not inherit this gap silently. Either widen `compute_level_up_grants`'s signature to accept explicit per-class from/to sub-levels, or add a dedicated multiclass entry point, before or as part of the extraction. |
| A3 | epic-7 / 7.1-7.3 | `append_to_character`, `recompute_character`, `re_save_character` (SD-24's 3 new Tauri commands) are registered and tested but have **zero frontend callers** — no `boundary/*.ts` wrapper, no `invoke()` call site anywhere in `apps/desktop/src`. | **Epic 3**, criterion 3.4/3.5 — when these route through the hub-of-hubs trait, give at least one of them a real UI call site (matching 7.4's own Add-Weapon/Add-Armor/Add-Spell precedent), not just refactored-but-still-unused plumbing. |
| A4 | epic-7 / 7.4 | `CharacterSheet.tsx`'s top-menu "Open"/"Save"/"Clone" items (`menuItems` array) are genuine no-op handlers (`onSelect: () => {}`) — real accidental stub debt. The dual-audit forbidden-token grep doesn't flag bare `() => {}`, so this passed the gate clean. | **Epic 3** (the entry's own suggestion: "Epic 3 remediation backlog") — wire Open/Save/Clone to real behavior or remove them if genuinely out of scope, while `CharacterSheet.tsx` is already in-scope for 3.5's adapter-aware UI work. |
| A5 | epic-7 / 7.3 | `mutate_saved_character_at_root` and every command routed through it (`level_up_character`, `add_equipment_selection`, `add_spell_selection`, `appendToCharacter`) preserve whatever `revision_id` was already on disk — none of them advance it. Only the new `reSaveCharacter` command advances the counter. | **Epic 3**, criterion 3.2 (`Pf1Adapter` extraction touches this exact file) — fold revision-advancing into `mutate_saved_character_at_root` itself so every mutation command advances `revision_id`, not just an explicit re-save call. Operator confirms this is wanted before folding it in (it's a behavior change, not a pure extraction). |
| A6 | epic-4 / 4.1 | The other 9 CRB classes (Cleric, Rogue, Sorcerer, Barbarian, Bard, Druid, Monk, Paladin, Ranger) each have a landed `level_up/<class>.rs` module that hasn't had the `class_spell.*`-vs-`class_feature.*`-prefix coverage audit applied — the same bug class found and fixed for Wizard (a later `pilot_compute.rs` grounding landing after the module and never being added to its explanation-id filter) could recur. LOW priority — not gating Epic 5, which closed Fighter+Wizard only. | **Epic 7**, per-class residue intake (criterion 7.1 already reads SD-24's `per-class-coverage-matrix.md`) — explicitly include this 9-class audit as one of the dynamically-spawned 7.2..7.M cycles, one `level_up/<class>.rs` file at a time per the file-touch partition. |
| A7 | epic-1 / 1.1 | The canonical identifier-discipline grep (`` \b(sd(16\|19\|22\|23\|24)_\|...)\b `` ) has a trailing-`\b` boundary bug — `\b` never matches between `_` and a following word char, so it silently misses real hits like `sd19_class_catalog` and only catches a bare standalone token. Live-proven: returned 0 hits against a repo that had 6 bundle-tagged modules, a resource dir, and 13 constants. | **Already fixed** in `/governance/loop-instruction-template.md` and in this bundle's own `epic-breakdown.md`/`loop-instruction.md` (2026-07-21, same session that wrote this register). Register-only entry — no further SD-25 action needed on the regex itself. Recommend re-running SD-24's own `git grep` with the corrected pattern once more against `develop` post-merge, in case anything landed after SD-24's own 1.1 cycle. |
| A8 | epic-6 / 6.2 | Multiple Epic-6 cycles (6.1, `crb-field-completion-cycle`, `apg-content-completion-cycle`) each independently wrote their own one-off ad-hoc script against the real PCGen corpus, rather than reusing the existing, tested in-repo LST tokenizer (`src/pcgen_import/lst_parser/equipment.rs`/`spell.rs`, SD-17 Slice B-5). That tokenizer emits raw tag tokens, not the semantic `cost_gp`/`weight`/`description` mapping needed; wiring it as codegen was judged out of each cycle's time budget. Risk: independently-written scripts silently diverging in `.COPY=`/`.MOD` handling, float formatting, entity decoding. | **Epic 7**, equipment/spell corpus intake criterion (already added) — before dispatching the CRB-description/APG-description/APG-spell-text/Bestiary-1 cycles, consider a shared `pcgen_import`-backed codegen path (parse → semantic-map → emit Rust literals) instead of a 5th ad-hoc script. Not a hard requirement, but explicitly flagged so the choice is made deliberately. |
| A9 | epic-6 / 6.5 | Real full-text CRB spell ingestion (`Plant Growth`: "creatures must **hack** or force a way through") collided with the wired-integration audit's zero-tolerance forbidden-token check (`hack`). Self-healed via a named bucket exclusion in `tests/sd24_wired_integration_audit.rs`; the raw grep still shows it as an expected, documented hit. | **Epic 1** or wherever SD-25's own dual-audit test lives — if Epic 7's corpus-intake cycles land more full-text content (spell/equipment descriptions from d20pfsrd/aonprd or LST `.MOD` records), re-scan for the same collision class before assuming a raw-grep hit is real. Consider hardening the standing test to exclude corpus-text fields from the zero-tolerance scan entirely (same way it already excludes test files), rather than adding named exclusions one collision at a time. |
| A10 | epic-6 / 6.4 (ACG) | ACG's corpus has zero `DESC:` tokens (same gap as CRB/APG), but `SPROP:` reached a 98.1% ceiling as a description source — high enough that ACG's cycle judged 6.4 genuinely met, no Open Blocker filed. Suggests checking whether Bestiary-1's own equipment corpus has a `DESC:`-or-`SPROP:`-equivalent convention before assuming it needs the same web-second-source treatment as CRB/APG. | **Epic 7**, equipment/spell corpus intake criterion (Bestiary-1 item) — check for a `SPROP:`-equivalent convention first; only fall back to the d20pfsrd/aonprd web pass if Bestiary-1's own corpus has neither. |
| A11 | epic-6 / 6.2 (ACG) | Same class of measurement error as CRB's spell-count and APG's counts: a `SOURCELONG:` header line double-counted as a record. Also, criterion 6.1's original ACG equipment scope (221, `acg_equip.lst` only) omitted `acg_equipmods.lst` entirely, unlike CRB's own 4-category treatment — SD-24's own cycle widened `EquipmentCategory` with an `Equipmods` variant and ingested 48 more real records (269 total). Confirmed independently 3 times across books (CRB, APG, ACG). | **Epic 7**, equipment/spell corpus intake criterion (Bestiary-1 item) — apply the same `SOURCELONG:`-header exclusion, `.COPY=`-merge rule, and full-category coverage (check for a Bestiary-1 equivalent of an "equipmods" category) from the start, not as a later correction. |
| A12 | epic-6 / 6.1 | `EquipmentTableEntry` (shared shape across CRB/APG/ACG) had no `weight` or `description` field at all before SD-24's field-completion cycles added them — criteria 6.3/6.4 were schema-change-then-populate work, not per-row fill-in. **Already landed** by `crb-field-completion-cycle`/`apg-content-completion-cycle`/`acg-field-completion-cycle` for all 3 existing books; Bestiary-1's own module doesn't exist yet (see A/original epic-6-path-correction below) so it inherits the same schema once created. | **Epic 7**, equipment/spell corpus intake criterion (Bestiary-1 item) — the schema fields already exist on `EquipmentTableEntry`; Bestiary-1's new module just needs to populate them, not add them. |
| A13 | epic-6 / 6.1 | No spell in any book carried full SRD/PRD text before SD-24 (every description was the corpus's short `DESC:` summary; full text lives on a separate `<Name>.MOD` record this repo's spell ingester had never read). **Already landed** for CRB (652/652, 100%) and ACG (144/144, 100%) by their field-completion cycles; APG reached 87.9% (see the criterion 6.5 Open Blocker, A15 below); Bestiary-1 unassessed (no spell list exists for a monster-manual book — likely N/A, confirm during the Bestiary-1 cycle rather than assuming). | **Epic 7**, equipment/spell corpus intake criterion — confirm Bestiary-1 has no spell-list concept (monster stat blocks, not spellcasters) before treating this as a gap; if it does have relevant spell-like-ability text, scope a cycle for it. |
| A14 | epic-6 / 6.1 | No equipment concept exists anywhere in the `beastiary1` module (monster-stat-block-only, 41 monsters). The real corpus does have 7 active Bestiary-1-specific equipment records (`b1_equip_general.lst` ×2, `b1_equip_arms_armor.lst` ×3, `b1_equip_magic_items.lst` ×2) never ingested. No file created in SD-24 (audit-only scope; fabricating rows without an ingest design decision would violate the no-stub doctrine). | **Epic 7**, equipment/spell corpus intake criterion, item 4 (already named "Bestiary 1 equipment + spells" in `epic-breakdown.md`) — this is that item's original source finding, recorded here for full traceability back to SD-24's own audit. |
| A15 | epic-6 / 6.4 (CRB) | `decisions.md §5` requires "a full description... for all of these things" with no "where applicable" carve-out (unlike cost/weight). CRB equipment's `description` field reaches an honest ceiling of 1821/2977 (61.2%) — the remaining ~39% (mostly slot-type markers and 656/658 `cr_equipmods.lst` modifier records) carry no `DESC:` token in the real corpus at all. No text was fabricated to close the gap. **This is SD-24's Open Blocker #1**, filed per `loop-instruction.md §4.2`'s own anticipated "strict-field-coverage threshold cannot be met" row. | **Epic 7**, equipment/spell corpus intake criterion, item 1 (already named "6.4 description field, CRB") — the d20pfsrd/aonprd web-second-source pass is the recommended path to close this without an operator wording change. |
| A16 | epic-6 / 6.4 (APG) | Adding `description: Option<&'static str>` to APG's `EquipmentTableEntry` and populating it finds **zero** `DESC:` tokens on any of APG's 338 equipment records — an even harder ceiling than CRB's 61.2%, confirming PF1 equipment records generally carry no prose description in the LST data at all (the flavor text lives in the printed book's prose, not the machine-readable corpus). **This is SD-24's Open Blocker #2.** | **Epic 7**, equipment/spell corpus intake criterion, item 2 (already named "6.4 description field, APG"). Same recommended resolution as A15. |
| A17 | epic-6 / 6.5 (APG) | The remaining 36/297 APG spell records split between (i) records with no `SCHOOL:`/`CLASSES:` token at all on the base record (mostly Summoner eidolon spells the real PF1 rules grant automatically rather than placing on a leveled spell list, plus a `PRESPELL`-linked variant and one corpus typo, `Wall of Thorms`) and (ii) records with no matching `<Name>.MOD` full-text record. Neither is fabricatable. **This is SD-24's Open Blocker #3.** | **Epic 7**, equipment/spell corpus intake criterion, item 3 (already named "6.5 full spell text, APG"). Same recommended resolution as A15/A16 — this one specifically needs a spell-text source, not equipment description. |

## B. Documentation-staleness corrections — low-effort, batchable

These are all "the plan doc names a path/count/roster that doesn't match reality" — none block anything, all are candidates for one batched documentation-only cycle rather than individual dispatch.

| # | Origin | Correction needed |
|---|---|---|
| B1 | epic-4 / 4.1 | `content-unit-inventory.md §3.1` names a hypothetical `class_fighter.rs`/`class_wizard.rs` per-class-file shape. Real CRB shape: `rules_tables/crb/class_tables.rs` (shared BAB/save table) + `level_up/<class>.rs` (class-specific, one per class). |
| B2 | epic-4 / 4.2 | Same file, same assumption, wrong for APG: APG has 6 separate per-class files (`apg/class_alchemist.rs` etc.) plus shared shape in `apg/mod.rs` — the opposite consolidation pattern from CRB. |
| B3 | epic-4 / 4.3 | Same file, same assumption, wrong for ACG: one file per class (`acg/class_arcanist.rs` ... `class_warpriest.rs`) plus `acg/mod.rs`. |
| B4 | epic-4 / 4.3 | `epic-breakdown.md`'s SD-24 criterion 4.3 header text lists "Alchemist-side" as an ACG class (it's APG-only) and omits "Slayer" (a real ACG class). Real, corrected 10-class ACG roster already documented in `rules_tables::acg::mod.rs`'s doc comment. |
| B5 | epic-6 / 6.1 | `crb/equipment_tables.rs`'s doc comment claiming "bootstrap coverage, one item per category" is stale (real data has been 100% record-complete since an SD-17 merge-dedup pass). **Already corrected in-cycle** by `crb-field-completion-cycle` — register-only, no action needed. |
| B6 | epic-6 / 6.2 | `epic-breakdown.md`/`content-unit-inventory.md`'s citation of CRB spell count "675" should read "652" (measurement error: `.COPY=` variants not merged, `TEMPBONUS`/`TEMPVALUE` sub-choice records and a `SOURCELONG:` header line counted as spells). |
| B7 | epic-6 / 6.2 | Same file's citation of APG equipment "341" should read "338"; APG spell "298" should read "297" (same `SOURCELONG:`-header-miscount class, plus one byte-distinct duplicate spell record). |
| B8 | epic-6 / 6.4 | `apg/equipment_tables.rs`'s doc comment claiming "no `Bomb`/`Acid Bomb` record exists" is half-true — a `Bomb` proficiency-template record does exist (not a purchasable consumable). **Already corrected in-cycle** — register-only. |
| B9 | epic-7 / 7.1 | `content-unit-inventory.md`/`acceptance-and-verification.md` name a standalone `tests/sd24_characterhub_append.rs` integration-test target. No `apps/desktop/src-tauri/tests/` directory exists in this crate — every Tauri command in it uses inline `#[cfg(test)] mod tests`. Applies to 7.2/7.3 too (both used the inline convention correctly). |
| B10 | epic-7 / 7.1 | `technical-design.md §3.1` frames the whole load/mutate/recompute/re-save surface as missing; SD-23 (`e74f3fa`, `f203df8`) already landed `level_up_character`, `add_equipment_selection`, `add_spell_selection`. The real 7.1 gap was narrower (batch-append + real-corpus validation). |
| B11 | epic-7 / 7.3 | `technical-design.md §3.2` sketches `reSaveCharacter`'s request as `{ characterId, character: CharacterSnapshot }` — no `CharacterSnapshot` wire type exists. Real implementation uses `{ characterId, expectedRevisionId, savedAt }` (the revision-conflict-guard shape the criterion's own RED/GREEN text actually describes). |
| B12 | epic-7 / 7.4 | Criterion 7.4's own RED text (`onClick={() => {}}`) is stale — the Add Weapon/Armor/Spell wiring was already fully landed by SD-23 (`885bbf9`), before SD-24 launched. Informational only. |
| B13 | epic-7 / 7.4 | The Add-item picker's mutation path calls the SD-23-era `addEquipmentSelection`/`addSpellSelection`, not the newer `appendToCharacter` batch command — different shapes (single-item vs. batch), left as-is deliberately. Register as an open question (see `risks-and-open-questions.md`) only if UI-affordance unification onto the newest command surface is wanted later — not required. |
| B14 | epic-7 / 7.5 | Reconfirms B1-B3's pattern for a different symbol: criterion 7.5's "Files touched" names `apps/desktop/src-tauri/src/characterHub/characterHubRuntime.ts` (doesn't exist). Real file: `apps/desktop/src/characterHub/characterHubRuntime.ts` (frontend, not src-tauri). `compose_character_input` is a Rust fn at `character_hub.rs:211`, not in that TS file at all. Three independent SD-24 cycles (7.1, 7.4, 7.5) hit this identical stale-path finding. |

**Disposition for all of B:** fold into one documentation-only follow-up cycle (Epic 1 or Epic 7, operator's call) that corrects `content-unit-inventory.md`, `technical-design.md`, and the historical `epic-breakdown.md` citations named above. Not gating anything; batchable in a single cycle since none conflict on files.

## C. Process/tooling lessons — apply to SD-25's own dispatch, not product work

| # | Origin | Lesson | Applied? |
|---|---|---|---|
| C1 | epic-5 / 5.2, 5.3, 5.4, 5.5 | SD-24's invocation-generation step templated the file-touch grant from criterion 5.1 forward verbatim across all 5 of Epic 5's cycles — recurred **4 times** before Epic 5 closed (harmless each time; corrected against `epic-breakdown.md`'s own per-criterion row, no wrong file was ever actually modified). | SD-25 dispatches from per-criterion `cycles/<epic>_<criterion>.md` files (per `scope-draft.md §4`'s file map), each presumably authored fresh rather than templated forward — structurally avoids this recurrence. Confirm this when `scripts/workflow-dispatch.sh` is authored: each cycle's file-touch grant must be read from that criterion's own cycle doc, never copied from the prior cycle's block. |
| C2 | epic-7 / 7.1 | Root-workspace `cargo test --locked --tests` hit a linker Bus-error crash from the shared build volume sitting at 100% disk usage — caused by concurrent `/batch`-worktree `target/` directories (6 sibling worktrees, up to 9.8G each). Self-healed narrowly (cleaned only the affected cycle's own worktree artifacts). | SD-25 inherits this risk identically — any epic with `parallel: yes` + `isolation: worktree` cycles running concurrently will accumulate `target/` dirs the same way. **Operator action, not a cycle fix:** monitor disk during dense parallel phases (Epic 3, Epic 4, Epic 7's corpus intake all have `parallel: yes` rows); prune merged worktrees promptly after each parallel phase completes, the same cleanup SD-24's closing session did by hand (freed 36GB from 9 stale worktrees). |
| C3 | epic-8 / 8.2 | `~/.hermes/profiles/god-emporer/skills/devops/architecture-truth-up/scripts/architecture_truth_up.py`'s `parse_source_dirs_index` regex requires literal `\|\|` table-cell delimiters; the real `docs/architecture/README.md` index table uses standard single-pipe Markdown rows, so the script always returns an empty path→doc mapping and would report a false-negative "no architecture impact" against any real diff. Worked around by performing the truth-up manually. | Out-of-repo (operator's `~/.hermes` profile). **Register only** — flag for the operator to fix the regex (or the README table format) before SD-25's own Epic 8 criterion 8.2 (Opus-tiered architecture closure pipeline) relies on the script instead of a manual pass. |

## D. Already resolved within SD-24 itself — register only, no SD-25 action

| # | Origin | Finding | Why no SD-25 action |
|---|---|---|---|
| D1 | epic-4 / 4.2 | All 6 APG classes are chassis-only: 0 named class features wired (Alchemist 0/24, Cavalier 0/16, Inquisitor 0/19, Oracle 0/19, Summoner 0/17, Witch 0/7), 0 live `pilot_compute.rs` integration, 0 `level_up` modules. | Fully resolved by SD-24's own criterion 4.4 (`remediation-plan.md` enumerates the gap) and 4.5 (formal deferral decision, `apg-acg-multiclass-deferred.md`). The deferral itself — not wiring these features — is the closed, correct outcome per `loop-instruction.md §4.2`'s pre-anticipated hard-stop row. No SD-25 epic reopens this; it's the documented status quo Epic 3/5's multiclass scope already works around (Fighter+Wizard only). |
| D2 | epic-4 / 4.3 | Same finding for all 10 real ACG classes (Arcanist 0/9, Bloodrager 0/19, Brawler 0/14, Hunter 0/21, Investigator 0/95, Shaman 0/10, Skald 0/20, Slayer 0/15, Swashbuckler 0/29, Warpriest 0/18). | Same disposition as D1 — resolved by the same 4.4/4.5 joint deferral decision. |
| D3 | epic-4 / 4.3 | A cycle initially built and pre-commit reverted real production wiring (10 new `level_up::acg_<name>.rs` modules) after discovering the sibling APG cycle had chosen audit-only scope for the identical finding, to keep the bundle's roll-up matrix internally consistent. No production behavior changed as a result. | Pure process learning, no artifact or gap remains. If a future bundle does wire the chassis-only pillar for APG/ACG classes, do both books in the same cycle for consistency — noted, but not itself dispatchable work. |
| D4 | epic-6 / 6.1 | `crb/equipment_tables.rs`'s doc comment claiming bootstrap-only coverage was stale. | Same as B5 (duplicate tag from the original DISCOVERED log — SD-24's own entry cross-references it). Already corrected in-cycle. |
| D5 | epic-6 / 6.4 | `apg/equipment_tables.rs`'s doc comment about a missing `Bomb` record was half-true; corrected in-cycle to note the real (non-purchasable) proficiency-template record. | Same as B8 — already corrected, no residual gap. |
| D6 | epic-6 / 6.2..6.5 | This cycle's granted file-touch set named only APG's own files, but the dispatched criteria were epic-level (spanning CRB+APG+ACG+Bestiary-1) — the cycle correctly scoped itself to APG only and left the other books to sibling concurrent cycles, which did land them. | Pure dispatch-mechanics observation, already resolved by SD-24's own concurrent-cycle model working as designed. For SD-25: name the target book explicitly in each corpus-intake criterion's own label (already done — `epic-breakdown.md`'s Epic 7 criterion lists CRB/APG/ACG/Bestiary-1 as 4 distinct items) rather than relying on the file-touch set alone to disambiguate. |
| D7 | epic-8 / 8.2 | Criterion 8.2's own 5-sub-step acceptance text: sub-step 1 (architecture-docs truth-up) landed; sub-steps 2-4 (graphify-update, opening the `tranche/5-2 → develop` PR, merge-conflict-resolution) did not, since the dispatching cycle's write scope was `docs/architecture/**` only. | **Not SD-25 scope** — this is SD-24's own closure action, being handled directly in this session (opening the `tranche/5-2 → develop` PR) rather than deferred to SD-25. Recorded here only so the register has complete 1:1 coverage of every SD-24 DISCOVERED entry; see the PR itself for final disposition. |

## Cross-references

- `../SD-24-beta-readiness-and-multiclass/progress.md ## DISCOVERED` + `## Open blockers` — full original text for every entry above.
- `./epic-breakdown.md` — Epic 3 / Epic 7 sections carry the dispatchable A-items.
- `./risks-and-open-questions.md §4` — open questions for A1 (GE-07 design decision) and B13 (UI-affordance unification).
- `./scope-draft.md §1.3, §1.7` — epic preambles cross-referencing this register.
