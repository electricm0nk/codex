# Cycle shape-b-license-stripping-preflight — Epic 2 / Criterion 2.0.5

- **Cycle ID:** `shape-b-license-stripping-preflight`
- **Criterion:** 2.0.5
- **Owner:** Backend
- **Status:** complete
- **Route class:** Sonnet (Workflow-dispatched subagent)
- **Started at:** 2026-07-27T20:20:00Z
- **Completed at:** 2026-07-27T20:28:00Z

## Inputs

- `src/rules_core/rules_tables/crb/json_cache.rs` (SD-26's Shape B v0 schema — the pattern extended)
- The OGL 1.0a's §1(e) "Product Identity" section (fetched live from a public mirror during authoring;
  cross-checked against established knowledge, both noted in the output doc)
- `forward-scope-register.md §1.3` (the Class 1 commitment)
- 4 in-scope books' existing `data/corpus/{core_rulebook,advanced_players_guide,advanced_class_guide,beastiary}/`
  records (read-only; confirmed not modified — see Verification)
- 19 future-state books' `data/stubs/<book>.json` manifests (read-only)

## Outputs

- `src/rules_core/shape_b_v1.rs` (new) — the shared, book-agnostic Shape B v1 schema authority:
  `CorpusRecordV1<T>`, additive over v0 via 3 `#[serde(default)]` fields (`license: Option<License>`,
  `pi_field: Option<String>`, `pi_marker: Option<String>`), defaulting to `None` (never a fabricated
  `Ogl`) on any record that hasn't been through a retro-fit; `validate_license()` enforcing the
  "every record has a license field" requirement plus the PI/marker consistency rule.
- `docs/governance/ogl-pi-blacklist.md` (new) — the initial PI-blacklist + OGL-inlinable whitelist +
  per-record-judgment fields, mapped onto this codebase's real struct field names (grepped, not
  invented). **Prominently marked DRAFT, operator-sign-off-required** — not self-executing.
- Per-book `LICENSE.json` shape documented in `ogl-pi-blacklist.md §5` (no literal file checked in yet;
  the first retro-fit cycle, E2.0.6, writes the first real one).
- `tests/sd27_license_stripping_shape_v1.rs` (new dual-audit gate) — 4 tests, including one that walks
  every real on-disk Shape B record across all 4 in-scope books and proves each deserializes cleanly as
  a v1 record with license fields `None`.
- `src/rules_core/mod.rs` — registered `pub mod shape_b_v1;`.
- This receipt.

## Operations

1. Read SD-26's Shape B v0 schema (`json_cache.rs`) in full.
2. Authored the v1 schema as a new shared module (deliberately not book-local, unlike v0 — the
   `isolation: 'worktree'` constraint that motivated v0's per-book duplication doesn't apply here,
   since v1 is authored once after those cycles landed).
3. Authored the PI-blacklist from the OGL's real §1(e) text, mapped onto real field names.
4. Documented the per-book `LICENSE.json` template.
5. Authored the dual-audit test, including a real-corpus walk (not a synthetic-only fixture).
6. Ran the dual-audit gate against this cycle's own diff — clean.
7. Independently re-verified by the orchestrator (not just the implementing subagent) — see
   Verification.

## Verification

- `cargo build --workspace --locked` → clean (one pre-existing, unrelated `dead_code` warning in
  `pilot_compute.rs`, not touched by this cycle).
- `cargo test --workspace --locked --test sd27_license_stripping_shape_v1` → **4/4 passed**, re-run
  independently by the orchestrator, not just trusted from the subagent's report.
- `cargo test --workspace --locked --lib shape_b_v1` → **6/6 passed**, independently re-run.
- Full `cargo test --workspace --locked --no-fail-fast` → 4802 passed / 3 failed. All 3 failures
  (`sd26_pcgen_runner`, `sd26_pilot_case_verification`, `v06_wizard_pilot_case_verification`) are
  pre-existing, environment-dependent tests requiring external fixtures under `/home/ubuntu/workspace/...`
  not present in this sandbox — confirmed identical failure with this cycle's diff stashed out (zero
  regressions introduced).
- `src/rules_core/shape_b_v1.rs` compiles and the v1 schema is documented (module-level and per-item
  doc comments throughout).
- The PI-blacklist maps every real field name in the 4 in-scope books' existing payload structs
  (`ClassCacheData`, `SpellCacheData`, `EquipmentCacheData`, `RaceTraitEntry`, `FeatTableEntry`,
  `MonsterStatBlock`) to either PI, OGL-inlinable, or per-record-judgment — no field left unclassified.
- Dual-audit gate (identifier-discipline + wired-integration four-check), diff-scoped against this
  cycle's own base: `OK_NO_BUNDLE_TAGS`, `AUDIT PASSED. All four checks clean.`

## Notes

- This cycle does **not** modify the 4 in-scope books' records — confirmed via `git status`: only
  `src/rules_core/shape_b_v1.rs` (new), `src/rules_core/mod.rs` (one-line registration),
  `docs/governance/ogl-pi-blacklist.md` (new), `tests/sd27_license_stripping_shape_v1.rs` (new). No
  `data/corpus/` path touched.
- This cycle does not modify the 19 future-state books' stubs.
- The v1 schema is additive: every v0 record is also a v1 record (just missing the license field) —
  proven against the real on-disk corpus, not asserted.
- The 4-grep dual-audit applies and passed.
- The PI-blacklist grep (5th audit) is not yet wired as a standing gate — it's added in 2.0.6+ once
  there's retro-fitted data to grep against.
- **PI-blacklist is a draft.** It requires operator review before any per-book retro-fit cycle (2.0.6+)
  treats its classifications as final. Flagged prominently in the document itself, matching the
  bundle's own framing that PI classification is a legal/content judgment call, not something to
  silently automate.
