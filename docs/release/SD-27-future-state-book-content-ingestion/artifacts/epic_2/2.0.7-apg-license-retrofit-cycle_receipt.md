# Cycle apg-license-retrofit — Epic 2 / Criterion 2.0.7

- **Cycle ID:** `apg-license-retrofit`
- **Criterion:** 2.0.7
- **Owner:** Backend
- **Status:** complete
- **Route class:** Sonnet (Workflow-dispatched subagent, parallel with 2.0.6/2.0.8/2.0.9)
- **Started at:** 2026-07-27T20:36:00Z
- **Completed at:** 2026-07-27T20:44:00Z

Same shape as `2.0.6-crb-license-retrofit-cycle_receipt.md` — see that receipt for the full
methodology, the two shared-test-file fixes (applied once, by the orchestrator, after all 4 parallel
cycles landed), and the registry-update N/A finding (no registry entry exists for any in-scope book).

## Inputs / Outputs specific to this book

- Inputs: `data/corpus/advanced_players_guide/` — 641 existing Shape B v0 records.
- Outputs: all 641 records re-emitted with `license`/`pi_field`/`pi_marker` (297 spell + 338 equipment +
  6 class). `data/corpus/advanced_players_guide/LICENSE.json` (new). `scripts/sd27_apg_license_retrofit.py`
  (the retrofit script, kept as documentation of the exact heuristic term-list methodology used —
  real transparency value for an operator reviewing this classification pass).

## Verification specific to this book

- 641/641 records classified: 641 `OGL`, 0 redacted. 30 records have a `null` description (no free text
  to scan; classified `OGL` directly).
- `tests/sd27_license_stripping_shape_v1.rs` (post-fix): passes, walks this book's records among the
  full 4,435-record corpus.
- Independently spot-checked by the orchestrator: cross-book random sample (see 2.0.6's receipt)
  included 3 APG files, all valid v1 shape.

## Notes

- Scanned against the 20 canonical core deities plus ~34 additional sampled Golarion place/organization
  names (54 terms total) — a heuristic first-pass, not an exhaustive review. Zero matches found; this
  reads as "this specific bounded scan found nothing," not "certified clean."
- `operator_sign_off.signed_off: false` in `LICENSE.json`.
