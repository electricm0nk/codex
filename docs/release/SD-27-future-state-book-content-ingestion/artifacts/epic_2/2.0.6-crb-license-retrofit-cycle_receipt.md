# Cycle crb-license-retrofit — Epic 2 / Criterion 2.0.6

- **Cycle ID:** `crb-license-retrofit`
- **Criterion:** 2.0.6
- **Owner:** Backend
- **Status:** complete
- **Route class:** Sonnet (Workflow-dispatched subagent, parallel with 2.0.7/2.0.8/2.0.9)
- **Started at:** 2026-07-27T20:36:00Z
- **Completed at:** 2026-07-27T20:44:00Z

## Inputs

- `src/rules_core/shape_b_v1.rs` (the v1 schema, cycle 2.0.5)
- `docs/governance/ogl-pi-blacklist.md` (the PI-blacklist, cycle 2.0.5)
- `data/corpus/core_rulebook/` — 3,326 existing Shape B v0 records (read-modify-write)

## Outputs

- `data/corpus/core_rulebook/` — all 3,326 records re-emitted in place with `license`/`pi_field`/
  `pi_marker`. 3,326 classified `OGL`, 0 redacted.
- `data/corpus/core_rulebook/LICENSE.json` (new)
- This receipt.

## Operations

1. Read all 3,326 records under `data/corpus/core_rulebook/`.
2. Classified per `ogl-pi-blacklist.md`: class records (11, no free-text field) → blanket `OGL`. Spell
   (652) and equipment (2,663) records' `description` fields scanned via a bounded, documented heuristic
   (20 canonical core deity names + a manual sampling pass for other proper nouns).
3. Re-emitted every record with the new license fields.
4. Wrote `data/corpus/core_rulebook/LICENSE.json`.
5. Ran the 5th dual-audit (PI-blacklist grep) and the standard 4-grep dual-audit.
6. **Registry update: N/A.** `docs/governance/wired-integration-stubs-registry.md` has no entry for
   CRB — the registry only covers the 19 future-state `book_stub`s; CRB was ingested directly by SD-22/
   SD-26 and was never stub-registered. `loop-instruction.md §3.2.6` corrected to reflect this (it
   previously described a registry update that was never possible).
7. Fixed a real regression this cycle caused: SD-26's own pre-existing `tests/sd26_cache_core_rulebook.rs`
   walks `data/corpus/core_rulebook/` expecting every `.json` to be a v0 record; the new `LICENSE.json`
   broke it (`missing 'population'`). Applied the minimal fix (exclude `LICENSE.json` from the walk),
   matching the same pattern already used in `sd27_license_stripping_shape_v1.rs`. This is technically
   outside SD-27's normal `tests/sd27_*` write scope, but was a direct, necessary consequence of this
   cycle's own change — not scope creep.
8. Fixed a second, larger issue in the shared `tests/sd27_license_stripping_shape_v1.rs`: its Audit 1
   hard-asserted `license == None` for every on-disk record, a pre-retrofit assumption now false for all
   4,435 records across all 4 in-scope books. Rewrote it to assert the post-retrofit invariant instead
   (`license` populated + `validate_license()` clean), and excluded `LICENSE.json` from its own walk.
   This was the one coordinated, shared-file edit all 4 parallel retrofit agents correctly declined to
   make themselves (to avoid a 4-way race); made once, by the orchestrator, after collecting all 4
   books' results.

## Verification

- `cargo test --workspace --locked` → **4,802 passed / 3 failed**, all 3 pre-existing and
  environment-dependent (PCGen/oracle-validation fixtures requiring `/home/ubuntu/workspace/...`, not
  present in this sandbox) — matches the exact same 3 failures observed before this cycle (cycle 2.0.5's
  receipt), zero regressions from this cycle's real changes once the two test-file fixes above landed.
- `tests/sd27_license_stripping_shape_v1.rs` → 4/4 passed (rewritten Audit 1 walks the real, full
  4,435-record corpus).
- `tests/sd26_cache_core_rulebook.rs` → 6/6 passed (previously 4/6 after this cycle's `LICENSE.json`
  landed, before the file-walk fix).
- 5th audit (PI-blacklist grep): 0 defects — every `PI`/`PI-REDACTED` record has `pi_marker: "redacted"`
  and a populated `pi_field`.
- 4-grep dual-audit: `OK_NO_BUNDLE_TAGS`, `AUDIT PASSED. All four checks clean.`
- Per-book `LICENSE.json` exists and matches the records.
- Independently spot-checked by the orchestrator (not just the implementing subagent): read back the
  full `discern_next_of_kin.json`-equivalent-class sample directly, confirmed the "Pathfinder RPG
  Bestiary" citation-not-redaction call is defensible (a bibliographic cross-reference, not narrative
  PI), and confirmed a random cross-book sample of records all have valid v1 shape.

## Notes

- This cycle is file-disjoint with cycles 2.0.7 (APG), 2.0.8 (ACG), 2.0.9 (Bestiary 1) — ran in true
  parallel via `Workflow`, verified via `git log` against `origin/tranche/6` before dispatch that v0.6's
  concurrent activity is confined to `src/rules_core/rules_tables/{crb,apg,acg}/` (Rust source, already
  off-limits) with zero recent activity on the `data/corpus/<book>/*.json` files this cycle touches.
- This cycle does NOT modify v0.6's lane (no `pilot_compute.rs`, no `docs/release/v0.6/`).
- The cycle does NOT modify `src/rules_core/rules_tables/core_rulebook/` — confirmed via `git status`
  scoped per-agent (each subagent's write scope was restricted to its own `data/corpus/<book>/` tree;
  the orchestrator verified zero scope leaks across all 4 agents afterward).
- **13 records (`bag_of_tricks_*`, `golem_manual_*`, `ring_of_djinni_calling`, `figurine_of_wondrous_power_*`,
  `robe_of_bones`) cite "Pathfinder RPG Bestiary"** as a rules cross-reference ("Statistics for these
  animals can be found in the Pathfinder RPG Bestiary"). Classified `OGL` (bibliographic citation, not
  narrative PI) rather than auto-redacted — flagged in `LICENSE.json`'s `known_open_item` field for
  operator review.
- **This is a heuristic first-pass screen** against a documented, bounded ~20-term list, not an
  exhaustive human legal review. Zero redactions in CRB is consistent with Paizo's real-world choice to
  keep the Core Rulebook's mechanical text setting-neutral (proper-noun PI content was reserved for the
  Campaign Setting line) — plausible, not independently verified against every one of 3,326 descriptions
  word-for-word.
- `operator_sign_off.signed_off: false` in `LICENSE.json` — this classification pass is a draft.
