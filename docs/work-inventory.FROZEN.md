# docs/work-inventory.json is FROZEN

**Date frozen:** 2026-09-15 (SD-36 consolidation, Epic B, operator ruling D3).

**SHA-256 at freeze:** `1b78ca67da1aeba8157bc2c70ba941def1f372c0598404b90f192e793a0b7c41`
(`sha256sum docs/work-inventory.json`)

**Contents at freeze:** `generated_at` `2026-09-14T17:43:08Z`, `generated_by`
`cargo run --bin v06_work_inventory`, `totals.units` 49,450 across 38 books,
`by_status`: `sheet-complete` 23,087 / `text-complete` 11,839 /
`oracle-unverifiable` 8,491 / `grounded` 5,222 / `oracle-agree` 811, every
other status word (`literal-verified`, `fixture-verified`,
`ingested-magnitude`, `deferred-with-reason`, `engine-does-not-hold`,
`not-started`, `unmeasurable`) at 0.
(`python3 -c "import json;d=json.load(open('docs/work-inventory.json'));print(d['totals'])"`)

## Why it is frozen

`src/bin/v06_work_inventory.rs`, the producer that generated this file, is
retired (operator ruling D3). The instruments that fed the internal
dashboard from it (`scripts/observer/pf1e_dashboard_producer.py`'s
`cargo run` calls, `scripts/publish-site-dashboard.sh`, the `support_state_matrix`
hand-seeded truth table and its desktop bridge, `reach_gate.rs`) are also
retired. There is no live generator left, so this file is no longer
regenerated by any process — it stays exactly as it is at this SHA
indefinitely, and any future rebuild of PF1e's ingestion state needs a new
producer design, not a patched-up `v06_work_inventory`.

## What still reads this file (must keep working against the frozen content)

Live, non-dashboard readers — this list is why the file is frozen in place,
not deleted, and why editing it (even to "fix" a stale-looking field) is
out of scope for any bundle that has not been explicitly authorized to
redesign the ingestion producer:

- `src/pcgen_import/sheet_rule/mod.rs:280` (`load_population`, the sheet-rule
  pipeline population)
- `src/rules_core/class_feature_pool_catalog.rs` (multiple call sites)
- `src/rules_core/rules_tables/companion_chassis.rs` (multiple call sites)
- `src/bin/gen_cache_class_feature.rs`
- `src/pcgen_import/formula_interpreter_corpus_wide.rs` (multiple call sites)
- Root tests: `equipment_gap_tables`, `feat_gap_tables`,
  `sd35_class_feature_catalogs_read_converted_prose`,
  `sheet_rule_convert_gate`, and others (`git grep -l work-inventory.json --
  tests`)
- ~60 `scripts/*.py` census/ingest/classification tools
- `verify.sh` stages: `shape-coverage-standing-gate`, `missing-engine-tables`,
  `token-coverage`, `reachability-audit`, `sheet-rules-check`
- `scripts/completion_atlas.py`, `scripts/cycle_scope_gate.py`,
  `scripts/token_coverage.py`, `scripts/oracle_harness/bucket_v_parity.py`
  (bucket classification over the frozen units)
- `scripts/site/build_public_status.py` → `site/status-data.json` (D5: the
  public status page, frozen at 100% of 49,450 as of this same date)

## Re-deriving facts from the frozen file

Every command in this doc and in the readers above operates on the
committed file as it stands — there is no live regeneration step to run
first. `python3 scripts/site/check_frozen_status.py` is the ongoing gate
that the committed public projection (`site/status-data.json` +
`site/status-data/<book>.json`) still agrees with this frozen source.
