---
title: Spec-Domain Lifecycle
status: stub (operator directive 2026-07-19)
scope: docs/doctrine-external
artifact_type: stub
canonical_source: ~/workspace/governance/spec-domain-lifecycle.md
---

# Spec-Domain Lifecycle (stub)

This file resolves the `../doctrine-external/spec-domain-lifecycle.md` cross-references in `docs/release/SD-NN/*.md`. The canonical content lives at `~/workspace/governance/spec-domain-lifecycle.md` in the operator's workspace and is NOT bundled in this repo.

A cold-cloud-clone harness: stop and request the canonical governance bundle from the operator. The stub below records only the doctrine-name so the cross-references resolve.

## Authoritative headline

> Each SD-NN bundle follows the lifecycle `planning-ready → running → closed`, with closure-flow requiring a `tranche/N → develop` PR, identifier-cleanup-delta, release-notes generation, and version increment per the `<major>.<tranche-base>.<build>` scheme. The Epic-numbering convention is `Epic 1` = identifier cleanup (governance base requirement), final Epic = build-version numbering, and the very last Epic = closure readiness evaluator (Epic 9 in SD-22's case) followed by closure epilogue.

## Recorded

Created 2026-07-19 during the `docs/release/` tree seed (operator directive 2026-07-18).
