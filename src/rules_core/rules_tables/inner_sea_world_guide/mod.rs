//! Inner Sea World Guide (`SOURCESHORT:ISWG`) — `monster` + `monster_ability`.
//!
//! **9 of this book's 14 monster rows and 14 of its 30 ability rows are
//! ingested.** The other 21 units are excluded for two independent, evidenced
//! reasons, and both are findings rather than shortfalls.
//!
//! # 1. Product Identity, declared by the corpus itself
//!
//! Five monster rows carry `NAMEISPI:YES` — PCGen's own per-record marker that
//! the record's NAME is Product Identity. **They are cited here by line and
//! never by name**, because `pi_table_sweep` rejects a PI term anywhere under
//! `rules_tables/` and a comment recording a removal has no need to
//! instantiate the name it removed. Read them out of the corpus, where they
//! legitimately live:
//!
//! ```text
//! grep -n 'NAMEISPI:YES' iswg_races.lst iswg_races_bestiary.lst
//! iswg_races.lst:13   iswg_races.lst:14   iswg_races.lst:16
//! iswg_races_bestiary.lst:13   iswg_races_bestiary.lst:14
//! ```
//!
//! The marker and an independent reading agree, which is the check that makes
//! the marker usable rather than merely present: each of the five names a
//! setting-specific deity, town, region, nation or unique NPC — Product
//! Identity under OGL §1(e) on its face, not only by upstream declaration.
//! Three of the excluded ability rows are namespaced to the first of them and
//! match `PI_BLACKLIST_TERMS` outright.
//!
//! **They are dropped, not redacted.** `pi_screening` redacts a `description`;
//! it cannot redact a KEY, and `[redacted PI]` as a monster's key is a record
//! nobody can look up. Reclassifying a term is
//! `docs/governance/ogl-pi-blacklist.md` §3's per-book override — an operator
//! decision, not a transcriber's.
//!
//! **`NAMEISPI:YES` was invisible to this program until this book**, and only
//! three of the five are on `PI_BLACKLIST_TERMS` — so a term-list-only screen
//! would have passed two of them. The four books already carrying the chassis
//! contain zero such rows (`grep -c NAMEISPI:YES` over each one's races `.lst`
//! → 0), so nothing already shipped is affected *in this lane*. It is not clean
//! corpus-wide: see this round's receipt for the one shipped record another
//! lane's ingest carries with the marker set.
//!
//! # 2. Orphans — 13 ability rows no monster row of this book claims
//!
//! Rounds 1 and 2 took the books whose every ability row is claimed by a monster
//! row of the same book. From round 3 on there is no such book left:
//!
//! ```text
//! python3 scripts/classify_monster_ability_rows.py inner_sea_world_guide
//! book                    mon  abil row-named prefix ORPHAN
//! inner_sea_world_guide    14    30        25      0      5
//! ```
//!
//! Five are orphans against the whole book — namespaced to `iswg_templates.lst`
//! templates this chassis does not model, at `iswg_abilities_race.lst:86-87`
//! and `:96-98`. The other eight lost their owner to the PI screen above —
//! dropping a monster cascades to the abilities only it claimed, which is why
//! the two screens run in that order.
//!
//! An ability with no owner reaches no screen: the catalog renders an ability
//! underneath its monster. Shipping one would be the record-that-loads-and-is-
//! never-shown `decisions.md §44.2` was written about. `not-ingested` is their
//! honest status.
//!
//! # The first book whose monsters live in TWO files, and why that mattered
//!
//! 4 ingested monster rows are in `iswg_races.lst` (alongside this book's player
//! races) and 5 in `iswg_races_bestiary.lst`. Their line numbers **collide**:
//!
//! ```text
//! iswg_races.lst:10          -> Aluum
//! iswg_races_bestiary.lst:10 -> Fennec (Firefoot)
//! ```
//!
//! Before this book, `MonsterStatBlock` carried a `source_line` and
//! `gen_book_cache` took the *file* from a single per-book spec string. Under
//! that model every row of one of these two files would have been
//! citation-checked against the other. [`MonsterStatBlock::source_file`] and the
//! generator's per-record file lookup exist because of this book;
//! `the_two_races_files_carry_colliding_line_numbers` below is the regression.

mod monster_data;
/// SD-32 `decisions.md §20`, no_record-to-zero wave: base spell
/// declarations transcribed from `iswg_spells.lst`, generated by the
/// config-driven `src/bin/ingest_spells.rs` (`decisions.md §17`).
pub mod spell_list;

pub use super::monster_chassis::{
    MonsterAbilityDelivery, MonsterAbilityFacet, MonsterAbilityRecord, MonsterStatBlock,
    NaturalAttack, Speed,
};

/// Every monster stat block this book defines, in corpus row order.
pub const fn monsters_static() -> &'static [MonsterStatBlock] {
    monster_data::MONSTERS
}

/// Every monster-ability record this book defines, in corpus row order.
pub const fn monster_abilities_static() -> &'static [MonsterAbilityRecord] {
    monster_data::MONSTER_ABILITIES
}

/// Every monster stat block this book defines, in corpus row order.
pub fn monsters() -> &'static [MonsterStatBlock] {
    monsters_static()
}

/// Every monster-ability record this book defines, in corpus row order.
pub fn monster_abilities() -> &'static [MonsterAbilityRecord] {
    monster_abilities_static()
}

#[cfg(test)]
mod tests {
    use super::*;

    /// The corpus unit counts come from `docs/work-inventory.json`, never a line
    /// count over the `.lst`:
    /// `python3 -c "import json; d=json.load(open('docs/work-inventory.json'));
    /// print(sum(1 for u in d['units'] if u['book']=='inner_sea_world_guide'
    /// and u['kind']=='monster'))"` -> 14, `monster_ability` -> 30.
    ///
    /// What ships is 9 and 14. Asserting 14 and 30 here would assert that this
    /// book ships five Product Identity names and thirteen records nothing can
    /// reach.
    #[test]
    fn the_book_ships_nine_monsters_and_fourteen_linked_abilities() {
        assert_eq!(monsters().len(), 9);
        // 14 owned + 13 owner-less (`decisions.md §20`, no_record-to-zero
        // wave 2 follow-on) = 27. The owner-less count is pinned separately
        // below (`every_owner_less_ability_is_a_named_and_pinned_non_reach`).
        // 13 owner-less -> 16 (`decisions.md §24`/round 7, +3): three
        // ability rows at `iswg_abilities_race.lst:24/25/27`, whose own KEY
        // namespace matched the blacklist, now ship under a
        // Codex-generated neutral key instead of being dropped -- their
        // owning monster row (`iswg_races.lst:13`) is still dropped
        // (`NAMEISPI:YES`-declared, `monster`-kind, outside `§24`'s
        // ability-only scope this cycle), so all three ship as orphans.
        // `owned` (14) is UNCHANGED. 27 -> 30.
        let owned = monster_abilities()
            .iter()
            .filter(|a| !a.owners.is_empty())
            .count();
        assert_eq!(owned, 14);
        assert_eq!(monster_abilities().len(), 30);
    }

    /// **Superseded `decisions.md §20` (no_record-to-zero wave 2 follow-on).**
    /// The 13 rows no shipped monster row of this book claims now SHIP with
    /// `owners: &[]`, and this test pins the EXACT set of records that carry
    /// one. `list_monster_catalog` never walks these directly (only a
    /// monster's own `ability_keys`), so shipping them does not surface a
    /// stub; each key is pinned separately, by name, in `reach_gate.rs::
    /// UNREACHED_RECORD_FINDINGS` under
    /// `("inner_sea_world_guide", "monster_abilities")` as a proven
    /// non-reach, not a silent claim of reachability.
    #[test]
    fn every_owner_less_ability_is_a_named_and_pinned_non_reach() {
        use std::collections::hash_map::DefaultHasher;
        use std::hash::{Hash, Hasher};

        let mut unowned: Vec<&str> = monster_abilities()
            .iter()
            .filter(|a| a.owners.is_empty())
            .map(|a| a.key)
            .collect();
        unowned.sort_unstable();

        assert_eq!(
            unowned.len(),
            16,
            "the number of owner-less (unreachable-by-design) monster_ability records \
             changed — re-derive this pin from a real \
             `scripts/transcribe_monster_tables.py inner_sea_world_guide` run, and update the \
             matching `reach_gate.rs::UNREACHED_RECORD_FINDINGS` entry to the same key set. \
             13 -> 16 (`decisions.md §24`/round 7, +3): see \
             `the_book_ships_nine_monsters_and_fourteen_linked_abilities`'s own comment."
        );

        let mut hasher = DefaultHasher::new();
        unowned.hash(&mut hasher);
        let digest = hasher.finish();
        assert_eq!(
            digest, 0xc6a5_c8bd_fa9c_bb39,
            "the owner-less key SET changed (same count, different members) — re-derive and \
             update `reach_gate.rs::UNREACHED_RECORD_FINDINGS` to match exactly"
        );
    }

    /// Every owner named by a shipped ability is itself a shipped monster.
    ///
    /// Not implied by the test above: dropping a monster for Product Identity
    /// while leaving an ability pointing at it would satisfy "owners is
    /// non-empty" and still name a creature the catalog cannot render.
    #[test]
    fn every_owner_named_by_a_shipped_ability_is_a_shipped_monster() {
        for ability in monster_abilities() {
            for owner in ability.owners {
                assert!(
                    monsters().iter().any(|m| m.key == *owner),
                    "{} names owner {owner}, which is not a shipped monster of this book",
                    ability.key
                );
            }
        }
    }

    /// The five excluded MONSTER rows are pinned **individually, by the
    /// corpus line each one is**, so a regeneration that quietly pulls one
    /// back in fails here naming the line that returned.
    ///
    /// By line and not by key on purpose: their keys are the Product Identity,
    /// and `pi_table_sweep` rejects a PI term anywhere under `rules_tables/` —
    /// including a test asserting the term's absence. A line citation is also
    /// the stronger pin, because it survives an upstream rename of the very
    /// name that makes the row excluded. Their own row DECLARES
    /// `NAMEISPI:YES` (`monster`-kind, outside `§24`'s ability-only scope
    /// this cycle), so they are still dropped, unchanged from before.
    #[test]
    fn the_five_product_identity_monster_rows_are_not_records() {
        for (file, line) in [
            ("iswg_races.lst", 13u32),
            ("iswg_races.lst", 14),
            ("iswg_races.lst", 16),
            ("iswg_races_bestiary.lst", 13),
            ("iswg_races_bestiary.lst", 14),
        ] {
            assert!(
                !monsters()
                    .iter()
                    .any(|m| m.source_file == file && m.source_line == line),
                "{file}:{line} carries NAMEISPI:YES in its own corpus row and must not ship"
            );
        }
    }

    /// **Superseded `decisions.md §24` (T9 round 7).** The three ability
    /// lines at `iswg_abilities_race.lst:24/25/27`, namespaced under the
    /// same owner as the PI-declared monster row at `iswg_races.lst:13`
    /// and whose own KEY therefore carries the identical blacklist term,
    /// used to be dropped outright (this test's prior shape,
    /// `the_eight_product_identity_rows_are_not_records`). They now
    /// SHIP, under a Codex-generated neutral name/key derived ONLY from
    /// `(kind, book, source_file, source_line)` — never from the original
    /// string. Pinned by corpus line, same reason as the monster-row test
    /// above. `no_shipped_field_carries_a_product_identity_term` below is
    /// the generic proof the rename actually removed the term; this test
    /// proves the coordinate-to-record mapping and the marker fields.
    #[test]
    fn the_three_name_pi_ability_rows_ship_renamed_not_dropped() {
        for line in [24u32, 25, 27] {
            let record = monster_abilities()
                .iter()
                .find(|a| a.source_line == line)
                .unwrap_or_else(|| {
                    panic!("iswg_abilities_race.lst:{line} must ship (renamed, not dropped)")
                });
            assert!(
                record.codex_generated_name,
                "iswg_abilities_race.lst:{line} shipped but was not marked \
                 `codex_generated_name` -- a renamed record must be visibly renamed \
                 (`decisions.md §24b`-3)"
            );
            assert_eq!(record.rename_reason, Some("name_pi_blocked"));
            assert!(record.name.starts_with("Codex-Named Unit ("));
            assert_eq!(record.name, record.key);
        }
    }

    /// **Superseded `decisions.md §20`.** The five template-owned rows now
    /// ship owner-less (shape measurable, reachability not claimed) instead
    /// of being excluded — a future template surface is what would give them
    /// a real owner, not this transcriber. Each is one of the 16 pinned by
    /// `every_owner_less_ability_is_a_named_and_pinned_non_reach` above.
    #[test]
    fn the_five_template_owned_rows_ship_owner_less() {
        for line in [86u32, 87, 96, 97, 98] {
            let ability = monster_abilities()
                .iter()
                .find(|a| a.source_line == line)
                .unwrap_or_else(|| {
                    panic!(
                        "iswg_abilities_race.lst:{line} ships for shape measurement \
                         (decisions.md §20)"
                    )
                });
            assert!(
                ability.owners.is_empty(),
                "{} was expected owner-less; it is owned by an iswg_templates.lst template, \
                 not by any monster row of this book",
                ability.key
            );
        }
    }

    /// No shipped value anywhere in this book's two tables carries a Product
    /// Identity term.
    ///
    /// The test above pins the rows this round excluded; this one is the
    /// property those exclusions exist to hold, checked against the live
    /// blacklist rather than against a list of names. A term added to
    /// `PI_BLACKLIST_TERMS` later fails here rather than shipping quietly.
    #[test]
    fn no_shipped_field_carries_a_product_identity_term() {
        use crate::rules_core::pi_screening::PI_BLACKLIST_TERMS;
        let mut haystack = String::new();
        for m in monsters() {
            haystack.push_str(m.key);
            haystack.push(' ');
            haystack.push_str(m.name);
            haystack.push(' ');
            for value in [m.race_type, m.race_subtype, m.monster_class, m.source_page] {
                haystack.push_str(value.unwrap_or(""));
                haystack.push(' ');
            }
            for attack in m.natural_attacks {
                haystack.push_str(attack.name);
                haystack.push(' ');
            }
            for key in m.ability_keys.iter().chain(m.external_ability_refs) {
                haystack.push_str(key);
                haystack.push(' ');
            }
        }
        for a in monster_abilities() {
            haystack.push_str(a.key);
            haystack.push(' ');
            haystack.push_str(a.name);
            haystack.push(' ');
            haystack.push_str(a.description.unwrap_or(""));
            haystack.push(' ');
            haystack.push_str(a.source_page.unwrap_or(""));
            haystack.push(' ');
            for value in a.traits.iter().chain(a.description_variables).chain(a.owners) {
                haystack.push_str(value);
                haystack.push(' ');
            }
        }
        let hits: Vec<&str> = PI_BLACKLIST_TERMS
            .iter()
            .copied()
            .filter(|term| haystack.contains(term))
            .collect();
        assert!(hits.is_empty(), "Product Identity terms in shipped values: {hits:?}");
    }

    /// This book's two races files use overlapping line numbers, so a record's
    /// `source_line` is only meaningful together with its `source_file`. Held as
    /// a test because the whole `source_file` field exists for it.
    #[test]
    fn the_two_races_files_carry_colliding_line_numbers() {
        let at = |file: &str, line: u32| {
            monsters()
                .iter()
                .find(|m| m.source_file == file && m.source_line == line)
                .unwrap_or_else(|| panic!("{file}:{line} is a shipped monster row of this book"))
        };
        assert_eq!(at("iswg_races.lst", 10).key, "Aluum");
        assert_eq!(at("iswg_races_bestiary.lst", 10).key, "Fennec (Firefoot)");

        assert_eq!(
            monsters().iter().filter(|m| m.source_file == "iswg_races.lst").count(),
            4
        );
        assert_eq!(
            monsters()
                .iter()
                .filter(|m| m.source_file == "iswg_races_bestiary.lst")
                .count(),
            5
        );
    }

    /// Verbatim spot-check against `iswg_races.lst:10`, checkable against the
    /// named line rather than merely self-consistent.
    #[test]
    fn the_aluum_matches_its_corpus_row() {
        let aluum = monsters()
            .iter()
            .find(|m| m.key == "Aluum")
            .expect("Aluum is in this book");
        assert_eq!(aluum.source_file, "iswg_races.lst");
        assert_eq!(aluum.source_line, 10);
        assert_eq!(aluum.name, "Aluum");
        assert_eq!(aluum.size, Some("L"));
        assert_eq!(aluum.race_type, Some("Construct"));
        assert_eq!(aluum.challenge_rating, Some("10"));
        assert_eq!(aluum.monster_class, Some("Construct:14"));
        assert_eq!(aluum.source_page, Some("p.306"));
        assert_eq!(aluum.speeds, &[Speed { mode: "Walk", feet: 30 }]);
        assert_eq!(
            aluum.natural_attacks,
            &[NaturalAttack { name: "Slam", damage_dice: Some("2d10") }]
        );
        assert_eq!(
            aluum.ability_keys,
            &[
                "Aluum ~ Immunity to Magic",
                "Aluum ~ Paralysis",
                "Aluum ~ Soul Shriek",
            ]
        );
        assert!(aluum.external_ability_refs.is_empty());
    }
}
